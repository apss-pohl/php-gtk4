// GENERATED by gen/gir.php from Gtk.PaperSize - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkPaperSize (boxed value type)
#include "php_gtk4.h"
#include "core/boxed.h"
#include "core/object.h"
#include <cstring>
#include "core/enums.h"
#include "core/collections.h"
#include "core/variant.h"

using namespace phpgtk;

namespace {

const char *const fields[] = {nullptr};

// Boxed field reader: the public scalar fields as PHP properties.
bool read(gpointer data, const char *field, zval *rv) {
  (void)data;
  (void)field;
  (void)rv;
  return false;
}

// Boxed field writer: each field converts like a parameter of its type (core/boxed).
bool write(gpointer data, const char *field, zval *v) {
  (void)data;
  (void)field;
  (void)v;
  return false;
}

}  // namespace

/**
 * Gtk4\GtkPaperSize::__construct(?string $name = null)
 *
 * Creates a new `GtkPaperSize` object by parsing a [PWG
 * 5101.1-2002](ftp://ftp.pwg.org/pub/pwg/candidates/cs-pwgmsn10-20020226-5101.1.pdf) paper name.
 */
ZEND_METHOD(Gtk4_GtkPaperSize, __construct) {
  zend_string *name = nullptr;
  ZEND_PARSE_PARAMETERS_START(0, 1)
  Z_PARAM_OPTIONAL
  Z_PARAM_STR_OR_NULL(name)
  ZEND_PARSE_PARAMETERS_END();
  if (name != nullptr && !phpgtk::check_utf8(name, 1)) RETURN_THROWS();
  gpointer obj = gtk_paper_size_new(name != nullptr ? ZSTR_VAL(name) : nullptr);
  if (obj == nullptr) {
    zend_throw_error(nullptr, "%s(): GTK refused to create the value (see the CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  boxed_adopt(boxed_from_zval(ZEND_THIS), GTK_TYPE_PAPER_SIZE, obj);
}

/**
 * static Gtk4\GtkPaperSize::new_custom(string $name, string $display_name, float $width, float
 * $height, GtkUnit $unit): GtkPaperSize
 *
 * Creates a new `GtkPaperSize` object with the given parameters.
 */
ZEND_METHOD(Gtk4_GtkPaperSize, new_custom) {
  zend_string *name;
  zend_string *display_name;
  double width;
  double height;
  zval *unit;
  ZEND_PARSE_PARAMETERS_START(5, 5)
  Z_PARAM_STR(name)
  Z_PARAM_STR(display_name)
  Z_PARAM_DOUBLE(width)
  Z_PARAM_DOUBLE(height)
  Z_PARAM_OBJECT_OF_CLASS(unit, enum_class_for_type(GTK_TYPE_UNIT))
  ZEND_PARSE_PARAMETERS_END();
  if (!phpgtk::check_utf8(name, 1)) RETURN_THROWS();
  if (!phpgtk::check_utf8(display_name, 2)) RETURN_THROWS();
  gint unit_v = 0;
  if (!enum_from_php(unit, GTK_TYPE_UNIT, &unit_v)) RETURN_THROWS();
  const bool precondition_0 = unit_v != GTK_UNIT_NONE;
  if (!precondition_0) {
    zend_argument_value_error(5, "must not be GtkUnit::None");
    RETURN_THROWS();
  }
  gpointer phpgtk_ret = gtk_paper_size_new_custom(ZSTR_VAL(name), ZSTR_VAL(display_name), width,
                                                  height, static_cast<GtkUnit>(unit_v));
  wrap_boxed(GTK_TYPE_PAPER_SIZE, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(GTK_TYPE_PAPER_SIZE, phpgtk_ret);
}

/**
 * static Gtk4\GtkPaperSize::new_from_gvariant(mixed $variant = null): GtkPaperSize
 *
 * Deserialize a paper size from a `GVariant`.
 */
ZEND_METHOD(Gtk4_GtkPaperSize, new_from_gvariant) {
  zval *variant = nullptr;
  ZEND_PARSE_PARAMETERS_START(0, 1)
  Z_PARAM_OPTIONAL
  Z_PARAM_ZVAL(variant)
  ZEND_PARSE_PARAMETERS_END();
  GVariant *variant_v = nullptr;
  if (variant != nullptr && Z_TYPE_P(variant) != IS_NULL) {
    variant_v = php_to_variant(variant, nullptr);
    if (variant_v == nullptr) RETURN_THROWS();
    g_variant_ref_sink(variant_v);
  }
  gpointer phpgtk_ret = gtk_paper_size_new_from_gvariant(variant_v);
  if (variant_v != nullptr) g_variant_unref(variant_v);
  wrap_boxed(GTK_TYPE_PAPER_SIZE, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(GTK_TYPE_PAPER_SIZE, phpgtk_ret);
}

/**
 * static Gtk4\GtkPaperSize::new_from_ipp(string $ipp_name, float $width, float $height):
 * GtkPaperSize
 *
 * Creates a new `GtkPaperSize` object by using IPP information.
 */
ZEND_METHOD(Gtk4_GtkPaperSize, new_from_ipp) {
  zend_string *ipp_name;
  double width;
  double height;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_STR(ipp_name)
  Z_PARAM_DOUBLE(width)
  Z_PARAM_DOUBLE(height)
  ZEND_PARSE_PARAMETERS_END();
  if (!phpgtk::check_utf8(ipp_name, 1)) RETURN_THROWS();
  gpointer phpgtk_ret = gtk_paper_size_new_from_ipp(ZSTR_VAL(ipp_name), width, height);
  wrap_boxed(GTK_TYPE_PAPER_SIZE, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(GTK_TYPE_PAPER_SIZE, phpgtk_ret);
}

/**
 * static Gtk4\GtkPaperSize::new_from_ppd(string $ppd_name, string $ppd_display_name, float $width,
 * float $height): GtkPaperSize
 *
 * Creates a new `GtkPaperSize` object by using PPD information.
 */
ZEND_METHOD(Gtk4_GtkPaperSize, new_from_ppd) {
  zend_string *ppd_name;
  zend_string *ppd_display_name;
  double width;
  double height;
  ZEND_PARSE_PARAMETERS_START(4, 4)
  Z_PARAM_STR(ppd_name)
  Z_PARAM_STR(ppd_display_name)
  Z_PARAM_DOUBLE(width)
  Z_PARAM_DOUBLE(height)
  ZEND_PARSE_PARAMETERS_END();
  if (!phpgtk::check_utf8(ppd_name, 1)) RETURN_THROWS();
  if (!phpgtk::check_utf8(ppd_display_name, 2)) RETURN_THROWS();
  gpointer phpgtk_ret =
      gtk_paper_size_new_from_ppd(ZSTR_VAL(ppd_name), ZSTR_VAL(ppd_display_name), width, height);
  wrap_boxed(GTK_TYPE_PAPER_SIZE, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(GTK_TYPE_PAPER_SIZE, phpgtk_ret);
}

/**
 * Gtk4\GtkPaperSize::get_default_bottom_margin(GtkUnit $unit): float
 *
 * Gets the default bottom margin for the `GtkPaperSize`.
 */
ZEND_METHOD(Gtk4_GtkPaperSize, get_default_bottom_margin) {
  zval *unit;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(unit, enum_class_for_type(GTK_TYPE_UNIT))
  ZEND_PARSE_PARAMETERS_END();
  GtkPaperSize *self = PHPGTK_BOXED_SELF(GtkPaperSize);
  gint unit_v = 0;
  if (!enum_from_php(unit, GTK_TYPE_UNIT, &unit_v)) RETURN_THROWS();
  RETURN_DOUBLE(gtk_paper_size_get_default_bottom_margin(self, static_cast<GtkUnit>(unit_v)));
}

/**
 * Gtk4\GtkPaperSize::get_default_left_margin(GtkUnit $unit): float
 *
 * Gets the default left margin for the `GtkPaperSize`.
 */
ZEND_METHOD(Gtk4_GtkPaperSize, get_default_left_margin) {
  zval *unit;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(unit, enum_class_for_type(GTK_TYPE_UNIT))
  ZEND_PARSE_PARAMETERS_END();
  GtkPaperSize *self = PHPGTK_BOXED_SELF(GtkPaperSize);
  gint unit_v = 0;
  if (!enum_from_php(unit, GTK_TYPE_UNIT, &unit_v)) RETURN_THROWS();
  RETURN_DOUBLE(gtk_paper_size_get_default_left_margin(self, static_cast<GtkUnit>(unit_v)));
}

/**
 * Gtk4\GtkPaperSize::get_default_right_margin(GtkUnit $unit): float
 *
 * Gets the default right margin for the `GtkPaperSize`.
 */
ZEND_METHOD(Gtk4_GtkPaperSize, get_default_right_margin) {
  zval *unit;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(unit, enum_class_for_type(GTK_TYPE_UNIT))
  ZEND_PARSE_PARAMETERS_END();
  GtkPaperSize *self = PHPGTK_BOXED_SELF(GtkPaperSize);
  gint unit_v = 0;
  if (!enum_from_php(unit, GTK_TYPE_UNIT, &unit_v)) RETURN_THROWS();
  RETURN_DOUBLE(gtk_paper_size_get_default_right_margin(self, static_cast<GtkUnit>(unit_v)));
}

/**
 * Gtk4\GtkPaperSize::get_default_top_margin(GtkUnit $unit): float
 *
 * Gets the default top margin for the `GtkPaperSize`.
 */
ZEND_METHOD(Gtk4_GtkPaperSize, get_default_top_margin) {
  zval *unit;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(unit, enum_class_for_type(GTK_TYPE_UNIT))
  ZEND_PARSE_PARAMETERS_END();
  GtkPaperSize *self = PHPGTK_BOXED_SELF(GtkPaperSize);
  gint unit_v = 0;
  if (!enum_from_php(unit, GTK_TYPE_UNIT, &unit_v)) RETURN_THROWS();
  RETURN_DOUBLE(gtk_paper_size_get_default_top_margin(self, static_cast<GtkUnit>(unit_v)));
}

/**
 * Gtk4\GtkPaperSize::get_display_name(): string
 *
 * Gets the human-readable name of the `GtkPaperSize`.
 */
ZEND_METHOD(Gtk4_GtkPaperSize, get_display_name) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkPaperSize *self = PHPGTK_BOXED_SELF(GtkPaperSize);
  const char *phpgtk_ret = gtk_paper_size_get_display_name(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

/**
 * Gtk4\GtkPaperSize::get_height(GtkUnit $unit): float
 *
 * Gets the paper height of the `GtkPaperSize`, in units of $unit.
 */
ZEND_METHOD(Gtk4_GtkPaperSize, get_height) {
  zval *unit;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(unit, enum_class_for_type(GTK_TYPE_UNIT))
  ZEND_PARSE_PARAMETERS_END();
  GtkPaperSize *self = PHPGTK_BOXED_SELF(GtkPaperSize);
  gint unit_v = 0;
  if (!enum_from_php(unit, GTK_TYPE_UNIT, &unit_v)) RETURN_THROWS();
  RETURN_DOUBLE(gtk_paper_size_get_height(self, static_cast<GtkUnit>(unit_v)));
}

/**
 * Gtk4\GtkPaperSize::get_name(): string
 *
 * Gets the name of the `GtkPaperSize`.
 */
ZEND_METHOD(Gtk4_GtkPaperSize, get_name) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkPaperSize *self = PHPGTK_BOXED_SELF(GtkPaperSize);
  const char *phpgtk_ret = gtk_paper_size_get_name(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

/**
 * Gtk4\GtkPaperSize::get_ppd_name(): string
 *
 * Gets the PPD name of the `GtkPaperSize`, which may be `null`.
 */
ZEND_METHOD(Gtk4_GtkPaperSize, get_ppd_name) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkPaperSize *self = PHPGTK_BOXED_SELF(GtkPaperSize);
  const char *phpgtk_ret = gtk_paper_size_get_ppd_name(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

/**
 * Gtk4\GtkPaperSize::get_width(GtkUnit $unit): float
 *
 * Gets the paper width of the `GtkPaperSize`, in units of $unit.
 */
ZEND_METHOD(Gtk4_GtkPaperSize, get_width) {
  zval *unit;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(unit, enum_class_for_type(GTK_TYPE_UNIT))
  ZEND_PARSE_PARAMETERS_END();
  GtkPaperSize *self = PHPGTK_BOXED_SELF(GtkPaperSize);
  gint unit_v = 0;
  if (!enum_from_php(unit, GTK_TYPE_UNIT, &unit_v)) RETURN_THROWS();
  RETURN_DOUBLE(gtk_paper_size_get_width(self, static_cast<GtkUnit>(unit_v)));
}

/**
 * Gtk4\GtkPaperSize::is_custom(): bool
 *
 * Returns `true` if $size is not a standard paper size.
 */
ZEND_METHOD(Gtk4_GtkPaperSize, is_custom) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkPaperSize *self = PHPGTK_BOXED_SELF(GtkPaperSize);
  RETURN_BOOL(gtk_paper_size_is_custom(self));
}

/**
 * Gtk4\GtkPaperSize::is_equal(GtkPaperSize $size2): bool
 *
 * Compares two `GtkPaperSize` objects.
 */
ZEND_METHOD(Gtk4_GtkPaperSize, is_equal) {
  zval *size2;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(size2, boxed_class_for_type(GTK_TYPE_PAPER_SIZE)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkPaperSize *self = PHPGTK_BOXED_SELF(GtkPaperSize);
  gpointer size2_b = unwrap_boxed(size2, GTK_TYPE_PAPER_SIZE);
  if (size2_b == nullptr) RETURN_THROWS();
  RETURN_BOOL(gtk_paper_size_is_equal(self, static_cast<GtkPaperSize *>(size2_b)));
}

/**
 * Gtk4\GtkPaperSize::is_ipp(): bool
 *
 * Returns `true` if $size is an IPP standard paper size.
 */
ZEND_METHOD(Gtk4_GtkPaperSize, is_ipp) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkPaperSize *self = PHPGTK_BOXED_SELF(GtkPaperSize);
  RETURN_BOOL(gtk_paper_size_is_ipp(self));
}

/**
 * Gtk4\GtkPaperSize::set_size(float $width, float $height, GtkUnit $unit): void
 *
 * Changes the dimensions of a $size to $width x $height.
 */
ZEND_METHOD(Gtk4_GtkPaperSize, set_size) {
  double width;
  double height;
  zval *unit;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_DOUBLE(width)
  Z_PARAM_DOUBLE(height)
  Z_PARAM_OBJECT_OF_CLASS(unit, enum_class_for_type(GTK_TYPE_UNIT))
  ZEND_PARSE_PARAMETERS_END();
  GtkPaperSize *self = PHPGTK_BOXED_SELF(GtkPaperSize);
  gint unit_v = 0;
  if (!enum_from_php(unit, GTK_TYPE_UNIT, &unit_v)) RETURN_THROWS();
  const bool state_holds = gtk_paper_size_is_custom(self);
  if (!state_holds) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "%s(): only a custom paper size (GtkPaperSize::new_custom()) can be resized",
        ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  gtk_paper_size_set_size(self, width, height, static_cast<GtkUnit>(unit_v));
}

/**
 * Gtk4\GtkPaperSize::to_gvariant(): mixed
 *
 * Serialize a paper size to an `a{sv}` variant.
 */
ZEND_METHOD(Gtk4_GtkPaperSize, to_gvariant) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkPaperSize *self = PHPGTK_BOXED_SELF(GtkPaperSize);
  GVariant *phpgtk_ret = gtk_paper_size_to_gvariant(self);
  if (phpgtk_ret == nullptr) RETURN_NULL();
  variant_to_php(phpgtk_ret, return_value);
}

/**
 * static Gtk4\GtkPaperSize::get_default(): string
 *
 * Returns the name of the default paper size, which depends on the current locale.
 */
ZEND_METHOD(Gtk4_GtkPaperSize, get_default) {
  ZEND_PARSE_PARAMETERS_NONE();
  const char *phpgtk_ret = gtk_paper_size_get_default();
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

/**
 * static Gtk4\GtkPaperSize::get_paper_sizes(bool $include_custom): array
 *
 * Creates a list of known paper sizes.
 */
ZEND_METHOD(Gtk4_GtkPaperSize, get_paper_sizes) {
  bool include_custom;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(include_custom)
  ZEND_PARSE_PARAMETERS_END();
  glist_to_php(gtk_paper_size_get_paper_sizes(include_custom), GTK_TYPE_PAPER_SIZE, Transfer::Full,
               return_value);
}

namespace phpgtk {
// MINIT: bind the PHP class to GTK_TYPE_PAPER_SIZE with its field table.
void register_GtkPaperSize(zend_class_entry *ce) {
  register_boxed(BoxedClass{
      .type = GTK_TYPE_PAPER_SIZE, .ce = ce, .fields = fields, .read = read, .write = write});
}
}  // namespace phpgtk
