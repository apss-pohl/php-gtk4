// GENERATED by gen/gir.php from Gtk.ToggleButton - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkToggleButton
#include "php_gtk4.h"
#include "core/object.h"
#include "core/subtype.h"

using namespace phpgtk;

/**
 * Gtk4\GtkToggleButton::__construct()
 *
 * Creates a new toggle button.
 */
ZEND_METHOD(Gtk4_GtkToggleButton, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gtk_toggle_button_new());
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * static Gtk4\GtkToggleButton::new_with_label(string $label): GtkToggleButton
 *
 * Creates a new toggle button with a text label.
 */
ZEND_METHOD(Gtk4_GtkToggleButton, new_with_label) {
  zend_string *label;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(label)
  ZEND_PARSE_PARAMETERS_END();
  GObject *obj = G_OBJECT(gtk_toggle_button_new_with_label(ZSTR_VAL(label)));
  wrap(obj, return_value);
}

/**
 * static Gtk4\GtkToggleButton::new_with_mnemonic(string $label): GtkToggleButton
 *
 * Creates a new `GtkToggleButton` containing a label.
 */
ZEND_METHOD(Gtk4_GtkToggleButton, new_with_mnemonic) {
  zend_string *label;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(label)
  ZEND_PARSE_PARAMETERS_END();
  GObject *obj = G_OBJECT(gtk_toggle_button_new_with_mnemonic(ZSTR_VAL(label)));
  wrap(obj, return_value);
}

/**
 * Gtk4\GtkToggleButton::get_active(): bool
 *
 * Queries a `GtkToggleButton` and returns its current state.
 */
ZEND_METHOD(Gtk4_GtkToggleButton, get_active) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkToggleButton *self = PHPGTK_SELF(GtkToggleButton, GTK_TYPE_TOGGLE_BUTTON);
  RETURN_BOOL(gtk_toggle_button_get_active(self));
}

/**
 * Gtk4\GtkToggleButton::set_active(bool $is_active): void
 *
 * Sets the status of the toggle button.
 */
ZEND_METHOD(Gtk4_GtkToggleButton, set_active) {
  bool is_active;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(is_active)
  ZEND_PARSE_PARAMETERS_END();
  GtkToggleButton *self = PHPGTK_SELF(GtkToggleButton, GTK_TYPE_TOGGLE_BUTTON);
  gtk_toggle_button_set_active(self, is_active);
}

/**
 * Gtk4\GtkToggleButton::set_group(?GtkToggleButton $group): void
 *
 * Adds $self to the group of $group.
 */
ZEND_METHOD(Gtk4_GtkToggleButton, set_group) {
  zval *group = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(group, class_for_gtype(GTK_TYPE_TOGGLE_BUTTON))
  ZEND_PARSE_PARAMETERS_END();
  GtkToggleButton *self = PHPGTK_SELF(GtkToggleButton, GTK_TYPE_TOGGLE_BUTTON);
  GObject *group_o = nullptr;
  if (group != nullptr) {
    group_o = unwrap(group, GTK_TYPE_TOGGLE_BUTTON);
    if (group_o == nullptr) RETURN_THROWS();
  }
  gtk_toggle_button_set_group(self, group_o != nullptr ? GTK_TOGGLE_BUTTON(group_o) : nullptr);
}
