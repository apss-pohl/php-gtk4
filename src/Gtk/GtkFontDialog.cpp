// GENERATED by gen/gir.php from Gtk.FontDialog - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkFontDialog
#include "php_gtk4.h"
#include "core/object.h"
#include "core/boxed.h"
#include "core/subtype.h"
#include "core/callback.h"
#include <array>

using namespace phpgtk;

/**
 * Gtk4\GtkFontDialog::__construct()
 *
 * Creates a new `GtkFontDialog` object.
 */
ZEND_METHOD(Gtk4_GtkFontDialog, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gtk_font_dialog_new());
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

namespace {
// AsyncReadyCallback trampoline for GtkFontDialog::choose_font(): wraps the C arguments, invokes
// the PHP callable once and releases it (async scope).
void cb_choose_font_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\GtkFontDialog::choose_font(?GtkWindow $parent, ?PangoFontDescription $initial_value,
 * ?GCancellable $cancellable, ?callable $callback): void
 *
 * This function initiates a font selection operation by presenting a dialog to the user for
 * selecting a font.
 */
ZEND_METHOD(Gtk4_GtkFontDialog, choose_font) {
  zval *parent = nullptr;
  zval *initial_value = nullptr;
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(4, 4)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(parent, class_for_gtype(GTK_TYPE_WINDOW))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(initial_value,
                                  boxed_class_for_type(PANGO_TYPE_FONT_DESCRIPTION)->ce)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC_OR_NULL(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  GtkFontDialog *self = PHPGTK_SELF(GtkFontDialog, GTK_TYPE_FONT_DIALOG);
  GObject *parent_o = nullptr;
  if (parent != nullptr) {
    parent_o = unwrap(parent, GTK_TYPE_WINDOW);
    if (parent_o == nullptr) RETURN_THROWS();
  }
  gpointer initial_value_b = nullptr;
  if (initial_value != nullptr) {
    initial_value_b = unwrap_boxed(initial_value, PANGO_TYPE_FONT_DESCRIPTION);
    if (initial_value_b == nullptr) RETURN_THROWS();
  }
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback =
      ZEND_FCI_INITIALIZED(fci_callback)
          ? callback_new(&fci_callback.function_name, "GtkFontDialog::choose_font")
          : nullptr;
  gtk_font_dialog_choose_font(self, parent_o != nullptr ? GTK_WINDOW(parent_o) : nullptr,
                              static_cast<PangoFontDescription *>(initial_value_b),
                              cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
                              cb_callback != nullptr ? cb_choose_font_callback : nullptr,
                              cb_callback);
}

namespace {
// AsyncReadyCallback trampoline for GtkFontDialog::choose_font_and_features(): wraps the C
// arguments, invokes the PHP callable once and releases it (async scope).
void cb_choose_font_and_features_callback(GObject *source_object, GAsyncResult *res,
                                          gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\GtkFontDialog::choose_font_and_features(?GtkWindow $parent, ?PangoFontDescription
 * $initial_value, ?GCancellable $cancellable, ?callable $callback): void
 *
 * This function initiates a font selection operation by presenting a dialog to the user for
 * selecting a font and font features.
 */
ZEND_METHOD(Gtk4_GtkFontDialog, choose_font_and_features) {
  zval *parent = nullptr;
  zval *initial_value = nullptr;
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(4, 4)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(parent, class_for_gtype(GTK_TYPE_WINDOW))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(initial_value,
                                  boxed_class_for_type(PANGO_TYPE_FONT_DESCRIPTION)->ce)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC_OR_NULL(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  GtkFontDialog *self = PHPGTK_SELF(GtkFontDialog, GTK_TYPE_FONT_DIALOG);
  GObject *parent_o = nullptr;
  if (parent != nullptr) {
    parent_o = unwrap(parent, GTK_TYPE_WINDOW);
    if (parent_o == nullptr) RETURN_THROWS();
  }
  gpointer initial_value_b = nullptr;
  if (initial_value != nullptr) {
    initial_value_b = unwrap_boxed(initial_value, PANGO_TYPE_FONT_DESCRIPTION);
    if (initial_value_b == nullptr) RETURN_THROWS();
  }
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback =
      ZEND_FCI_INITIALIZED(fci_callback)
          ? callback_new(&fci_callback.function_name, "GtkFontDialog::choose_font_and_features")
          : nullptr;
  gtk_font_dialog_choose_font_and_features(
      self, parent_o != nullptr ? GTK_WINDOW(parent_o) : nullptr,
      static_cast<PangoFontDescription *>(initial_value_b),
      cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
      cb_callback != nullptr ? cb_choose_font_and_features_callback : nullptr, cb_callback);
}

/**
 * Gtk4\GtkFontDialog::choose_font_finish(GAsyncResult $result): ?PangoFontDescription
 *
 * Finishes the `choose_font` call and returns the resulting font description.
 */
ZEND_METHOD(Gtk4_GtkFontDialog, choose_font_finish) {
  zval *result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  GtkFontDialog *self = PHPGTK_SELF(GtkFontDialog, GTK_TYPE_FONT_DIALOG);
  GObject *result_o = unwrap(result, G_TYPE_ASYNC_RESULT);
  if (result_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  PangoFontDescription *phpgtk_ret =
      gtk_font_dialog_choose_font_finish(self, G_ASYNC_RESULT(result_o), &error);
  wrap_boxed(PANGO_TYPE_FONT_DESCRIPTION, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(PANGO_TYPE_FONT_DESCRIPTION, phpgtk_ret);
}

/**
 * Gtk4\GtkFontDialog::get_filter(): ?GtkFilter
 *
 * Returns the filter that decides which fonts to display in the font chooser dialog.
 */
ZEND_METHOD(Gtk4_GtkFontDialog, get_filter) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFontDialog *self = PHPGTK_SELF(GtkFontDialog, GTK_TYPE_FONT_DIALOG);
  GtkFilter *phpgtk_ret = gtk_font_dialog_get_filter(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkFontDialog::get_modal(): bool
 *
 * Returns whether the font chooser dialog blocks interaction with the parent window while it is
 * presented.
 */
ZEND_METHOD(Gtk4_GtkFontDialog, get_modal) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFontDialog *self = PHPGTK_SELF(GtkFontDialog, GTK_TYPE_FONT_DIALOG);
  RETURN_BOOL(gtk_font_dialog_get_modal(self));
}

/**
 * Gtk4\GtkFontDialog::get_title(): string
 *
 * Returns the title that will be shown on the font chooser dialog.
 */
ZEND_METHOD(Gtk4_GtkFontDialog, get_title) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFontDialog *self = PHPGTK_SELF(GtkFontDialog, GTK_TYPE_FONT_DIALOG);
  const char *phpgtk_ret = gtk_font_dialog_get_title(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

/**
 * Gtk4\GtkFontDialog::set_filter(?GtkFilter $filter): void
 *
 * Adds a filter that decides which fonts to display in the font chooser dialog.
 */
ZEND_METHOD(Gtk4_GtkFontDialog, set_filter) {
  zval *filter = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(filter, class_for_gtype(GTK_TYPE_FILTER))
  ZEND_PARSE_PARAMETERS_END();
  GtkFontDialog *self = PHPGTK_SELF(GtkFontDialog, GTK_TYPE_FONT_DIALOG);
  GObject *filter_o = nullptr;
  if (filter != nullptr) {
    filter_o = unwrap(filter, GTK_TYPE_FILTER);
    if (filter_o == nullptr) RETURN_THROWS();
  }
  gtk_font_dialog_set_filter(self, filter_o != nullptr ? GTK_FILTER(filter_o) : nullptr);
}

/**
 * Gtk4\GtkFontDialog::set_modal(bool $modal): void
 *
 * Sets whether the font chooser dialog blocks interaction with the parent window while it is
 * presented.
 */
ZEND_METHOD(Gtk4_GtkFontDialog, set_modal) {
  bool modal;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(modal)
  ZEND_PARSE_PARAMETERS_END();
  GtkFontDialog *self = PHPGTK_SELF(GtkFontDialog, GTK_TYPE_FONT_DIALOG);
  gtk_font_dialog_set_modal(self, modal);
}

/**
 * Gtk4\GtkFontDialog::set_title(string $title): void
 *
 * Sets the title that will be shown on the font chooser dialog.
 */
ZEND_METHOD(Gtk4_GtkFontDialog, set_title) {
  zend_string *title;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(title)
  ZEND_PARSE_PARAMETERS_END();
  GtkFontDialog *self = PHPGTK_SELF(GtkFontDialog, GTK_TYPE_FONT_DIALOG);
  if (!phpgtk::check_utf8(title, 1)) RETURN_THROWS();
  gtk_font_dialog_set_title(self, ZSTR_VAL(title));
}
