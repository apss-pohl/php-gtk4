// GENERATED by gen/gir.php from Gtk.Spinner - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkSpinner
#include "php_gtk4.h"
#include "core/object.h"
#include "core/subtype.h"

using namespace phpgtk;

/**
 * Gtk4\GtkSpinner::__construct()
 *
 * Returns a new spinner widget. Not yet started.
 */
ZEND_METHOD(Gtk4_GtkSpinner, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gtk_spinner_new());
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkSpinner::get_spinning(): bool
 *
 * Returns whether the spinner is spinning.
 */
ZEND_METHOD(Gtk4_GtkSpinner, get_spinning) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkSpinner *self = PHPGTK_SELF(GtkSpinner, GTK_TYPE_SPINNER);
  RETURN_BOOL(gtk_spinner_get_spinning(self));
}

/**
 * Gtk4\GtkSpinner::set_spinning(bool $spinning): void
 *
 * Sets the activity of the spinner.
 */
ZEND_METHOD(Gtk4_GtkSpinner, set_spinning) {
  bool spinning;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(spinning)
  ZEND_PARSE_PARAMETERS_END();
  GtkSpinner *self = PHPGTK_SELF(GtkSpinner, GTK_TYPE_SPINNER);
  gtk_spinner_set_spinning(self, spinning);
}

/**
 * Gtk4\GtkSpinner::start(): void
 *
 * Starts the animation of the spinner.
 */
ZEND_METHOD(Gtk4_GtkSpinner, start) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkSpinner *self = PHPGTK_SELF(GtkSpinner, GTK_TYPE_SPINNER);
  gtk_spinner_start(self);
}

/**
 * Gtk4\GtkSpinner::stop(): void
 *
 * Stops the animation of the spinner.
 */
ZEND_METHOD(Gtk4_GtkSpinner, stop) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkSpinner *self = PHPGTK_SELF(GtkSpinner, GTK_TYPE_SPINNER);
  gtk_spinner_stop(self);
}
