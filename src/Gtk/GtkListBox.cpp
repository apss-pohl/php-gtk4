// GENERATED by gen/gir.php from Gtk.ListBox - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkListBox
#include "php_gtk4.h"
#include "core/object.h"
#include "core/enums.h"
#include "core/collections.h"
#include "core/subtype.h"

using namespace phpgtk;

// The four callbacks a GtkListBox takes (all GIR scope "notified"): the PHP callable lives in a
// Callback owned by GTK, and the box can be told to drop each of them again - which is what the
// teardown clear hooks do, so nothing is finalized after Zend is gone.
#include "core/callback.h"
#include "core/teardown.h"
#include "core/error.h"

#include <array>

namespace {

// GtkListBoxFilterFunc trampoline: (GtkListBoxRow $row) -> bool. A failed call hides the row.
gboolean list_box_filter_func(GtkListBoxRow *row, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  zval arg;
  wrap(G_OBJECT(row), &arg);
  zval retval;
  const bool ok = callback_invoke(cb, 1, &arg, &retval);
  const gboolean keep = ok && !Z_ISUNDEF(retval) && zend_is_true(&retval) ? TRUE : FALSE;
  zval_ptr_dtor(&retval);
  zval_ptr_dtor(&arg);
  return keep;
}

// GDestroyNotify for the filter func.
void list_box_filter_func_free(gpointer data) {
  teardown_untrack_notified(data);
  callback_free(data);
}

// teardown clear hook: drop the PHP callable from the box.
void list_box_filter_func_clear(GObject *owner) {
  gtk_list_box_set_filter_func(GTK_LIST_BOX(owner), nullptr, nullptr, nullptr);
}

// GtkListBoxSortFunc trampoline: (GtkListBoxRow $row1, GtkListBoxRow $row2) -> int, the usual
// negative/zero/positive. A failed call leaves the two rows in the order they were in.
int list_box_sort_func(GtkListBoxRow *row1, GtkListBoxRow *row2, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  wrap(G_OBJECT(row1), args.data());
  wrap(G_OBJECT(row2), &args[1]);
  zval retval;
  const bool ok = callback_invoke(cb, 2, args.data(), &retval);
  const zend_long order = ok && !Z_ISUNDEF(retval) ? zval_get_long(&retval) : 0;
  int sorted = 0;
  if (order < 0) sorted = -1;
  if (order > 0) sorted = 1;
  zval_ptr_dtor(&retval);
  zval_ptr_dtor(args.data());
  zval_ptr_dtor(&args[1]);
  return sorted;
}

// GDestroyNotify for the sort func.
void list_box_sort_func_free(gpointer data) {
  teardown_untrack_notified(data);
  callback_free(data);
}

// teardown clear hook: drop the PHP callable from the box.
void list_box_sort_func_clear(GObject *owner) {
  gtk_list_box_set_sort_func(GTK_LIST_BOX(owner), nullptr, nullptr, nullptr);
}

// GtkListBoxUpdateHeaderFunc trampoline: (GtkListBoxRow $row, ?GtkListBoxRow $before) -> void.
// PHP decides by calling GtkListBoxRow::set_header() on $row, which is what GTK asks for here.
void list_box_header_func(GtkListBoxRow *row, GtkListBoxRow *before, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  wrap(G_OBJECT(row), args.data());
  wrap(before != nullptr ? G_OBJECT(before) : nullptr, &args[1]);
  zval retval;
  callback_invoke(cb, 2, args.data(), &retval);
  zval_ptr_dtor(&retval);
  zval_ptr_dtor(args.data());
  zval_ptr_dtor(&args[1]);
}

// GDestroyNotify for the header func.
void list_box_header_func_free(gpointer data) {
  teardown_untrack_notified(data);
  callback_free(data);
}

// teardown clear hook: drop the PHP callable from the box.
void list_box_header_func_clear(GObject *owner) {
  gtk_list_box_set_header_func(GTK_LIST_BOX(owner), nullptr, nullptr, nullptr);
}

// GtkListBoxCreateWidgetFunc trampoline: (GObject $item) -> GtkWidget, one row per model item.
// The widget is transfer-full, so GTK is handed a reference of its own; anything else - a
// throw, a return that is not a widget - is a TypeError through the boundary and an empty row,
// because GTK dereferences a NULL return.
GtkWidget *list_box_create_widget_func(gpointer item, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  zval arg;
  wrap(static_cast<GObject *>(item), &arg);
  zval retval;
  const bool ok = callback_invoke(cb, 1, &arg, &retval);
  GtkWidget *widget = nullptr;
  if (ok && !Z_ISUNDEF(retval) && Z_TYPE(retval) == IS_OBJECT) {
    GObject *obj = unwrap(&retval, GTK_TYPE_WIDGET);
    if (obj != nullptr) widget = GTK_WIDGET(g_object_ref(obj));  // transfer full
  }
  if (widget == nullptr) {
    if (EG(exception) == nullptr && ok) {
      zend_type_error("%s(): the create function must return a GtkWidget", cb->origin);
    }
    report_pending_exception(cb->origin);
    widget = gtk_label_new(nullptr);  // GTK would dereference a NULL row
  }
  zval_ptr_dtor(&retval);
  zval_ptr_dtor(&arg);
  return widget;
}

// GDestroyNotify for the create-widget func.
void list_box_create_widget_func_free(gpointer data) {
  teardown_untrack_notified(data);
  callback_free(data);
}

// teardown clear hook: unbind the model, which drops the PHP callable with it.
void list_box_create_widget_func_clear(GObject *owner) {
  gtk_list_box_bind_model(GTK_LIST_BOX(owner), nullptr, nullptr, nullptr, nullptr);
}

}  // namespace

/**
 * Gtk4\GtkListBox::__construct()
 *
 * Creates a new `GtkListBox` container.
 */
ZEND_METHOD(Gtk4_GtkListBox, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gtk_list_box_new());
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkListBox::append(GtkWidget $child): void
 *
 * Append a widget to the list.
 */
ZEND_METHOD(Gtk4_GtkListBox, append) {
  zval *child;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkListBox *self = PHPGTK_SELF(GtkListBox, GTK_TYPE_LIST_BOX);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  gtk_list_box_append(self, GTK_WIDGET(child_o));
}

/**
 * Gtk4\GtkListBox::drag_highlight_row(GtkListBoxRow $row): void
 *
 * Add a drag highlight to a row.
 */
ZEND_METHOD(Gtk4_GtkListBox, drag_highlight_row) {
  zval *row;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(row, class_for_gtype(GTK_TYPE_LIST_BOX_ROW))
  ZEND_PARSE_PARAMETERS_END();
  GtkListBox *self = PHPGTK_SELF(GtkListBox, GTK_TYPE_LIST_BOX);
  GObject *row_o = unwrap(row, GTK_TYPE_LIST_BOX_ROW);
  if (row_o == nullptr) RETURN_THROWS();
  gtk_list_box_drag_highlight_row(self, GTK_LIST_BOX_ROW(row_o));
}

/**
 * Gtk4\GtkListBox::drag_unhighlight_row(): void
 *
 * If a row has previously been highlighted via gtk_list_box_drag_highlight_row(), it will have the
 * highlight removed.
 */
ZEND_METHOD(Gtk4_GtkListBox, drag_unhighlight_row) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkListBox *self = PHPGTK_SELF(GtkListBox, GTK_TYPE_LIST_BOX);
  gtk_list_box_drag_unhighlight_row(self);
}

/**
 * Gtk4\GtkListBox::get_activate_on_single_click(): bool
 *
 * Returns whether rows activate on single clicks.
 */
ZEND_METHOD(Gtk4_GtkListBox, get_activate_on_single_click) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkListBox *self = PHPGTK_SELF(GtkListBox, GTK_TYPE_LIST_BOX);
  RETURN_BOOL(gtk_list_box_get_activate_on_single_click(self));
}

/**
 * Gtk4\GtkListBox::get_adjustment(): ?GtkAdjustment
 *
 * Gets the adjustment (if any) that the widget uses to for vertical scrolling.
 */
ZEND_METHOD(Gtk4_GtkListBox, get_adjustment) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkListBox *self = PHPGTK_SELF(GtkListBox, GTK_TYPE_LIST_BOX);
  GtkAdjustment *phpgtk_ret = gtk_list_box_get_adjustment(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkListBox::get_row_at_index(int $index): ?GtkListBoxRow
 *
 * Gets the n-th child in the list (not counting headers).
 */
ZEND_METHOD(Gtk4_GtkListBox, get_row_at_index) {
  zend_long index;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(index)
  ZEND_PARSE_PARAMETERS_END();
  GtkListBox *self = PHPGTK_SELF(GtkListBox, GTK_TYPE_LIST_BOX);
  if (!phpgtk::check_range<int>(index, 1)) RETURN_THROWS();
  GtkListBoxRow *phpgtk_ret = gtk_list_box_get_row_at_index(self, static_cast<int>(index));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkListBox::get_row_at_y(int $y): ?GtkListBoxRow
 *
 * Gets the row at the $y position.
 */
ZEND_METHOD(Gtk4_GtkListBox, get_row_at_y) {
  zend_long y;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(y)
  ZEND_PARSE_PARAMETERS_END();
  GtkListBox *self = PHPGTK_SELF(GtkListBox, GTK_TYPE_LIST_BOX);
  if (!phpgtk::check_range<int>(y, 1)) RETURN_THROWS();
  GtkListBoxRow *phpgtk_ret = gtk_list_box_get_row_at_y(self, static_cast<int>(y));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkListBox::get_selected_row(): ?GtkListBoxRow
 *
 * Gets the selected row, or `null` if no rows are selected.
 */
ZEND_METHOD(Gtk4_GtkListBox, get_selected_row) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkListBox *self = PHPGTK_SELF(GtkListBox, GTK_TYPE_LIST_BOX);
  GtkListBoxRow *phpgtk_ret = gtk_list_box_get_selected_row(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkListBox::get_selected_rows(): array
 *
 * Creates a list of all selected children.
 */
ZEND_METHOD(Gtk4_GtkListBox, get_selected_rows) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkListBox *self = PHPGTK_SELF(GtkListBox, GTK_TYPE_LIST_BOX);
  glist_to_php(gtk_list_box_get_selected_rows(self), GTK_TYPE_LIST_BOX_ROW, Transfer::Container,
               return_value);
}

/**
 * Gtk4\GtkListBox::get_selection_mode(): GtkSelectionMode
 *
 * Gets the selection mode of the listbox.
 */
ZEND_METHOD(Gtk4_GtkListBox, get_selection_mode) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkListBox *self = PHPGTK_SELF(GtkListBox, GTK_TYPE_LIST_BOX);
  enum_to_php(GTK_TYPE_SELECTION_MODE, gtk_list_box_get_selection_mode(self), return_value);
}

/**
 * Gtk4\GtkListBox::get_show_separators(): bool
 *
 * Returns whether the list box should show separators between rows.
 */
ZEND_METHOD(Gtk4_GtkListBox, get_show_separators) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkListBox *self = PHPGTK_SELF(GtkListBox, GTK_TYPE_LIST_BOX);
  RETURN_BOOL(gtk_list_box_get_show_separators(self));
}

/**
 * Gtk4\GtkListBox::insert(GtkWidget $child, int $position): void
 *
 * Insert the $child into the $box at $position.
 */
ZEND_METHOD(Gtk4_GtkListBox, insert) {
  zval *child;
  zend_long position;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  Z_PARAM_LONG(position)
  ZEND_PARSE_PARAMETERS_END();
  GtkListBox *self = PHPGTK_SELF(GtkListBox, GTK_TYPE_LIST_BOX);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  if (!phpgtk::check_range<int>(position, 2)) RETURN_THROWS();
  gtk_list_box_insert(self, GTK_WIDGET(child_o), static_cast<int>(position));
}

/**
 * Gtk4\GtkListBox::invalidate_filter(): void
 *
 * Update the filtering for all rows.
 */
ZEND_METHOD(Gtk4_GtkListBox, invalidate_filter) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkListBox *self = PHPGTK_SELF(GtkListBox, GTK_TYPE_LIST_BOX);
  gtk_list_box_invalidate_filter(self);
}

/**
 * Gtk4\GtkListBox::invalidate_headers(): void
 *
 * Update the separators for all rows.
 */
ZEND_METHOD(Gtk4_GtkListBox, invalidate_headers) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkListBox *self = PHPGTK_SELF(GtkListBox, GTK_TYPE_LIST_BOX);
  gtk_list_box_invalidate_headers(self);
}

/**
 * Gtk4\GtkListBox::invalidate_sort(): void
 *
 * Update the sorting for all rows.
 */
ZEND_METHOD(Gtk4_GtkListBox, invalidate_sort) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkListBox *self = PHPGTK_SELF(GtkListBox, GTK_TYPE_LIST_BOX);
  gtk_list_box_invalidate_sort(self);
}

/**
 * Gtk4\GtkListBox::prepend(GtkWidget $child): void
 *
 * Prepend a widget to the list.
 */
ZEND_METHOD(Gtk4_GtkListBox, prepend) {
  zval *child;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkListBox *self = PHPGTK_SELF(GtkListBox, GTK_TYPE_LIST_BOX);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  gtk_list_box_prepend(self, GTK_WIDGET(child_o));
}

/**
 * Gtk4\GtkListBox::remove(GtkWidget $child): void
 *
 * Removes a child from $box.
 */
ZEND_METHOD(Gtk4_GtkListBox, remove) {
  zval *child;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkListBox *self = PHPGTK_SELF(GtkListBox, GTK_TYPE_LIST_BOX);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  gtk_list_box_remove(self, GTK_WIDGET(child_o));
}

/**
 * Gtk4\GtkListBox::remove_all(): void
 *
 * Removes all rows from $box.
 */
ZEND_METHOD(Gtk4_GtkListBox, remove_all) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkListBox *self = PHPGTK_SELF(GtkListBox, GTK_TYPE_LIST_BOX);
  gtk_list_box_remove_all(self);
}

/**
 * Gtk4\GtkListBox::select_all(): void
 *
 * Select all children of $box, if the selection mode allows it.
 */
ZEND_METHOD(Gtk4_GtkListBox, select_all) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkListBox *self = PHPGTK_SELF(GtkListBox, GTK_TYPE_LIST_BOX);
  gtk_list_box_select_all(self);
}

/**
 * Gtk4\GtkListBox::select_row(?GtkListBoxRow $row): void
 *
 * Make $row the currently selected row.
 */
ZEND_METHOD(Gtk4_GtkListBox, select_row) {
  zval *row = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(row, class_for_gtype(GTK_TYPE_LIST_BOX_ROW))
  ZEND_PARSE_PARAMETERS_END();
  GtkListBox *self = PHPGTK_SELF(GtkListBox, GTK_TYPE_LIST_BOX);
  GObject *row_o = nullptr;
  if (row != nullptr) {
    row_o = unwrap(row, GTK_TYPE_LIST_BOX_ROW);
    if (row_o == nullptr) RETURN_THROWS();
  }
  gtk_list_box_select_row(self, row_o != nullptr ? GTK_LIST_BOX_ROW(row_o) : nullptr);
}

namespace {
// ListBoxForeachFunc trampoline for GtkListBox::selected_foreach(): wraps the C arguments, invokes
// the PHP callable (call scope: released after the call).
void cb_selected_foreach_func(GtkListBox *box, GtkListBoxRow *row, gpointer user_data) {
  auto *cb = static_cast<Callback *>(user_data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(box != nullptr ? G_OBJECT(box) : nullptr, &argv[0]);
  wrap(row != nullptr ? G_OBJECT(row) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
}
}  // namespace

/**
 * Gtk4\GtkListBox::selected_foreach(callable $func): void
 *
 * Calls a function for each selected child.
 */
ZEND_METHOD(Gtk4_GtkListBox, selected_foreach) {
  zend_fcall_info fci_func = empty_fcall_info;
  zend_fcall_info_cache fcc_func = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_FUNC(fci_func, fcc_func)
  ZEND_PARSE_PARAMETERS_END();
  GtkListBox *self = PHPGTK_SELF(GtkListBox, GTK_TYPE_LIST_BOX);
  Callback *cb_func = callback_new(&fci_func.function_name, "GtkListBox::selected_foreach");
  gtk_list_box_selected_foreach(self, cb_selected_foreach_func, cb_func);
  if (cb_func != nullptr) {
    callback_free(cb_func);
    callback_drain();
  }
}

/**
 * Gtk4\GtkListBox::set_activate_on_single_click(bool $single): void
 *
 * If $single is `true`, rows will be activated when you click on them, otherwise you need to
 * double-click.
 */
ZEND_METHOD(Gtk4_GtkListBox, set_activate_on_single_click) {
  bool single;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(single)
  ZEND_PARSE_PARAMETERS_END();
  GtkListBox *self = PHPGTK_SELF(GtkListBox, GTK_TYPE_LIST_BOX);
  gtk_list_box_set_activate_on_single_click(self, single);
}

/**
 * Gtk4\GtkListBox::set_adjustment(?GtkAdjustment $adjustment): void
 *
 * Sets the adjustment (if any) that the widget uses to for vertical scrolling.
 */
ZEND_METHOD(Gtk4_GtkListBox, set_adjustment) {
  zval *adjustment = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(adjustment, class_for_gtype(GTK_TYPE_ADJUSTMENT))
  ZEND_PARSE_PARAMETERS_END();
  GtkListBox *self = PHPGTK_SELF(GtkListBox, GTK_TYPE_LIST_BOX);
  GObject *adjustment_o = nullptr;
  if (adjustment != nullptr) {
    adjustment_o = unwrap(adjustment, GTK_TYPE_ADJUSTMENT);
    if (adjustment_o == nullptr) RETURN_THROWS();
  }
  gtk_list_box_set_adjustment(self,
                              adjustment_o != nullptr ? GTK_ADJUSTMENT(adjustment_o) : nullptr);
}

/**
 * Gtk4\GtkListBox::set_placeholder(?GtkWidget $placeholder): void
 *
 * Sets the placeholder widget that is shown in the list when it doesn't display any visible
 * children.
 */
ZEND_METHOD(Gtk4_GtkListBox, set_placeholder) {
  zval *placeholder = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(placeholder, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkListBox *self = PHPGTK_SELF(GtkListBox, GTK_TYPE_LIST_BOX);
  GObject *placeholder_o = nullptr;
  if (placeholder != nullptr) {
    placeholder_o = unwrap(placeholder, GTK_TYPE_WIDGET);
    if (placeholder_o == nullptr) RETURN_THROWS();
  }
  gtk_list_box_set_placeholder(self,
                               placeholder_o != nullptr ? GTK_WIDGET(placeholder_o) : nullptr);
}

/**
 * Gtk4\GtkListBox::set_selection_mode(GtkSelectionMode $mode): void
 *
 * Sets how selection works in the listbox.
 */
ZEND_METHOD(Gtk4_GtkListBox, set_selection_mode) {
  zval *mode;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(mode, enum_class_for_type(GTK_TYPE_SELECTION_MODE))
  ZEND_PARSE_PARAMETERS_END();
  GtkListBox *self = PHPGTK_SELF(GtkListBox, GTK_TYPE_LIST_BOX);
  gint mode_v = 0;
  if (!enum_from_php(mode, GTK_TYPE_SELECTION_MODE, &mode_v)) RETURN_THROWS();
  gtk_list_box_set_selection_mode(self, static_cast<GtkSelectionMode>(mode_v));
}

/**
 * Gtk4\GtkListBox::set_show_separators(bool $show_separators): void
 *
 * Sets whether the list box should show separators between rows.
 */
ZEND_METHOD(Gtk4_GtkListBox, set_show_separators) {
  bool show_separators;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(show_separators)
  ZEND_PARSE_PARAMETERS_END();
  GtkListBox *self = PHPGTK_SELF(GtkListBox, GTK_TYPE_LIST_BOX);
  gtk_list_box_set_show_separators(self, show_separators);
}

/**
 * Gtk4\GtkListBox::unselect_all(): void
 *
 * Unselect all children of $box, if the selection mode allows it.
 */
ZEND_METHOD(Gtk4_GtkListBox, unselect_all) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkListBox *self = PHPGTK_SELF(GtkListBox, GTK_TYPE_LIST_BOX);
  gtk_list_box_unselect_all(self);
}

/**
 * Gtk4\GtkListBox::unselect_row(GtkListBoxRow $row): void
 *
 * Unselects a single row of $box, if the selection mode allows it.
 */
ZEND_METHOD(Gtk4_GtkListBox, unselect_row) {
  zval *row;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(row, class_for_gtype(GTK_TYPE_LIST_BOX_ROW))
  ZEND_PARSE_PARAMETERS_END();
  GtkListBox *self = PHPGTK_SELF(GtkListBox, GTK_TYPE_LIST_BOX);
  GObject *row_o = unwrap(row, GTK_TYPE_LIST_BOX_ROW);
  if (row_o == nullptr) RETURN_THROWS();
  gtk_list_box_unselect_row(self, GTK_LIST_BOX_ROW(row_o));
}

/**
 * public function bind_model(?GListModel $model, ?callable $create_widget_func): void
 * Fill the box from $model, one row per item: `function (GObject $item): GtkWidget` builds the
 * row. The box follows the model from then on - items added or removed there add and remove
 * rows here - and a null $model unbinds it, taking the callable with it.
 */
ZEND_METHOD(Gtk4_GtkListBox, bind_model) {
  zval *model = nullptr;
  zend_fcall_info fci;
  zend_fcall_info_cache fcc;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(model, class_for_gtype(G_TYPE_LIST_MODEL))
  Z_PARAM_FUNC_OR_NULL(fci, fcc)
  ZEND_PARSE_PARAMETERS_END();
  GtkListBox *self = PHPGTK_SELF(GtkListBox, GTK_TYPE_LIST_BOX);
  GObject *model_o = nullptr;
  if (model != nullptr) {
    model_o = unwrap(model, G_TYPE_LIST_MODEL);
    if (model_o == nullptr) RETURN_THROWS();
  }
  // A model without a create function would leave GTK dereferencing NULL for every item; a
  // create function without a model is worse than useless, because GTK returns before it stores
  // the destroy notify (gtkflowbox.c / gtklistbox.c) and the callable is leaked, never called.
  if (model_o != nullptr && !ZEND_FCI_INITIALIZED(fci)) {
    zend_argument_value_error(2, "must be a callable when a model is given");
    RETURN_THROWS();
  }
  if (model_o == nullptr && ZEND_FCI_INITIALIZED(fci)) {
    zend_argument_value_error(2, "must be null when no model is given");
    RETURN_THROWS();
  }
  if (!ZEND_FCI_INITIALIZED(fci)) {
    gtk_list_box_bind_model(self, nullptr, nullptr, nullptr, nullptr);
  } else {
    Callback *cb = callback_new(&fci.function_name, "GtkListBox::bind_model");
    teardown_track_notified(cb, G_OBJECT(self), list_box_create_widget_func_clear);
    gtk_list_box_bind_model(self, model_o != nullptr ? G_LIST_MODEL(model_o) : nullptr,
                            list_box_create_widget_func, cb, list_box_create_widget_func_free);
  }
  callback_drain();  // the previous func's notify ran inside bind_model()
}

/**
 * public function set_filter_func(?callable $filter_func): void
 * Keep only the rows `function (GtkListBoxRow $row): bool` answers true for; null shows every row
 * again.
 */
ZEND_METHOD(Gtk4_GtkListBox, set_filter_func) {
  zend_fcall_info fci;
  zend_fcall_info_cache fcc;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_FUNC_OR_NULL(fci, fcc)
  ZEND_PARSE_PARAMETERS_END();
  GtkListBox *self = PHPGTK_SELF(GtkListBox, GTK_TYPE_LIST_BOX);
  if (!ZEND_FCI_INITIALIZED(fci)) {
    gtk_list_box_set_filter_func(self, nullptr, nullptr, nullptr);
  } else {
    Callback *cb = callback_new(&fci.function_name, "GtkListBox::set_filter_func");
    teardown_track_notified(cb, G_OBJECT(self), list_box_filter_func_clear);
    gtk_list_box_set_filter_func(self, list_box_filter_func, cb, list_box_filter_func_free);
  }
  callback_drain();  // the previous func's notify ran inside the setter
}

/**
 * public function set_header_func(?callable $update_header): void
 * Give rows headers: `function (GtkListBoxRow $row, ?GtkListBoxRow $before): void` calls {@see
 * GtkListBoxRow::set_header()} on $row, and $before is the row above it (null for the first). Null
 * removes every header.
 */
ZEND_METHOD(Gtk4_GtkListBox, set_header_func) {
  zend_fcall_info fci;
  zend_fcall_info_cache fcc;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_FUNC_OR_NULL(fci, fcc)
  ZEND_PARSE_PARAMETERS_END();
  GtkListBox *self = PHPGTK_SELF(GtkListBox, GTK_TYPE_LIST_BOX);
  if (!ZEND_FCI_INITIALIZED(fci)) {
    gtk_list_box_set_header_func(self, nullptr, nullptr, nullptr);
  } else {
    Callback *cb = callback_new(&fci.function_name, "GtkListBox::set_header_func");
    teardown_track_notified(cb, G_OBJECT(self), list_box_header_func_clear);
    gtk_list_box_set_header_func(self, list_box_header_func, cb, list_box_header_func_free);
  }
  callback_drain();  // the previous func's notify ran inside the setter
}

/**
 * public function set_sort_func(?callable $sort_func): void
 * Order the rows by `function (GtkListBoxRow $row1, GtkListBoxRow $row2): int` - negative, zero or
 * positive as usual; null leaves them in the order they were added.
 */
ZEND_METHOD(Gtk4_GtkListBox, set_sort_func) {
  zend_fcall_info fci;
  zend_fcall_info_cache fcc;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_FUNC_OR_NULL(fci, fcc)
  ZEND_PARSE_PARAMETERS_END();
  GtkListBox *self = PHPGTK_SELF(GtkListBox, GTK_TYPE_LIST_BOX);
  if (!ZEND_FCI_INITIALIZED(fci)) {
    gtk_list_box_set_sort_func(self, nullptr, nullptr, nullptr);
  } else {
    Callback *cb = callback_new(&fci.function_name, "GtkListBox::set_sort_func");
    teardown_track_notified(cb, G_OBJECT(self), list_box_sort_func_clear);
    gtk_list_box_set_sort_func(self, list_box_sort_func, cb, list_box_sort_func_free);
  }
  callback_drain();  // the previous func's notify ran inside the setter
}
