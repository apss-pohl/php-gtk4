// GENERATED by gen/gir.php from Gtk.FlowBox - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkFlowBox
#include "php_gtk4.h"
#include "core/object.h"
#include "core/enums.h"
#include "core/collections.h"
#include "core/subtype.h"

using namespace phpgtk;

// The three callbacks a GtkFlowBox takes (all GIR scope "notified"), the same shape as
// GtkListBox's: the PHP callable lives in a Callback owned by GTK, and the box can be told to
// drop it again - which is what the teardown clear hooks do.
#include "core/callback.h"
#include "core/teardown.h"
#include "core/error.h"

#include <array>

namespace {

// GtkFlowBoxFilterFunc trampoline: (GtkFlowBoxChild $child) -> bool. A failed call hides it.
gboolean flow_box_filter_func(GtkFlowBoxChild *child, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  zval arg;
  wrap(G_OBJECT(child), &arg);
  zval retval;
  const bool ok = callback_invoke(cb, 1, &arg, &retval);
  const gboolean keep = ok && !Z_ISUNDEF(retval) && zend_is_true(&retval) ? TRUE : FALSE;
  zval_ptr_dtor(&retval);
  zval_ptr_dtor(&arg);
  return keep;
}

// GDestroyNotify for the filter func.
void flow_box_filter_func_free(gpointer data) {
  teardown_untrack_notified(data);
  callback_free(data);
}

// teardown clear hook: drop the PHP callable from the box.
void flow_box_filter_func_clear(GObject *owner) {
  gtk_flow_box_set_filter_func(GTK_FLOW_BOX(owner), nullptr, nullptr, nullptr);
}

// GtkFlowBoxSortFunc trampoline: (GtkFlowBoxChild $child1, GtkFlowBoxChild $child2) -> int, the
// usual negative/zero/positive. A failed call leaves the two where they were.
int flow_box_sort_func(GtkFlowBoxChild *child1, GtkFlowBoxChild *child2, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  wrap(G_OBJECT(child1), args.data());
  wrap(G_OBJECT(child2), &args[1]);
  zval retval;
  const bool ok = callback_invoke(cb, 2, args.data(), &retval);
  const zend_long order = ok && !Z_ISUNDEF(retval) ? zval_get_long(&retval) : 0;
  int sorted = 0;
  if (order < 0) sorted = -1;
  if (order > 0) sorted = 1;
  zval_ptr_dtor(&retval);
  zval_ptr_dtor(args.data());
  zval_ptr_dtor(&args[1]);
  return sorted;
}

// GDestroyNotify for the sort func.
void flow_box_sort_func_free(gpointer data) {
  teardown_untrack_notified(data);
  callback_free(data);
}

// teardown clear hook: drop the PHP callable from the box.
void flow_box_sort_func_clear(GObject *owner) {
  gtk_flow_box_set_sort_func(GTK_FLOW_BOX(owner), nullptr, nullptr, nullptr);
}

// GtkFlowBoxCreateWidgetFunc trampoline: (GObject $item) -> GtkWidget, one child per model item.
// Transfer full, so GTK is handed a reference of its own; a throw or a return that is not a
// widget is reported and answered with an empty label, because GTK dereferences a NULL child.
GtkWidget *flow_box_create_widget_func(gpointer item, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  zval arg;
  wrap(static_cast<GObject *>(item), &arg);
  zval retval;
  const bool ok = callback_invoke(cb, 1, &arg, &retval);
  GtkWidget *widget = nullptr;
  if (ok && !Z_ISUNDEF(retval) && Z_TYPE(retval) == IS_OBJECT) {
    GObject *obj = unwrap(&retval, GTK_TYPE_WIDGET);
    if (obj != nullptr) widget = GTK_WIDGET(g_object_ref(obj));  // transfer full
  }
  if (widget == nullptr) {
    if (EG(exception) == nullptr && ok) {
      zend_type_error("%s(): the create function must return a GtkWidget", cb->origin);
    }
    report_pending_exception(cb->origin);
    widget = gtk_label_new(nullptr);
  }
  zval_ptr_dtor(&retval);
  zval_ptr_dtor(&arg);
  return widget;
}

// GDestroyNotify for the create-widget func.
void flow_box_create_widget_func_free(gpointer data) {
  teardown_untrack_notified(data);
  callback_free(data);
}

// teardown clear hook: unbind the model, which drops the PHP callable with it.
void flow_box_create_widget_func_clear(GObject *owner) {
  gtk_flow_box_bind_model(GTK_FLOW_BOX(owner), nullptr, nullptr, nullptr, nullptr);
}

}  // namespace

/**
 * Gtk4\GtkFlowBox::__construct()
 *
 * Creates a `GtkFlowBox`.
 */
ZEND_METHOD(Gtk4_GtkFlowBox, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gtk_flow_box_new());
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkFlowBox::append(GtkWidget $child): void
 *
 * Adds $child to the end of $self.
 */
ZEND_METHOD(Gtk4_GtkFlowBox, append) {
  zval *child;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkFlowBox *self = PHPGTK_SELF(GtkFlowBox, GTK_TYPE_FLOW_BOX);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  gtk_flow_box_append(self, GTK_WIDGET(child_o));
}

/**
 * Gtk4\GtkFlowBox::get_activate_on_single_click(): bool
 *
 * Returns whether children activate on single clicks.
 */
ZEND_METHOD(Gtk4_GtkFlowBox, get_activate_on_single_click) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFlowBox *self = PHPGTK_SELF(GtkFlowBox, GTK_TYPE_FLOW_BOX);
  RETURN_BOOL(gtk_flow_box_get_activate_on_single_click(self));
}

/**
 * Gtk4\GtkFlowBox::get_child_at_index(int $idx): ?GtkFlowBoxChild
 *
 * Gets the nth child in the $box.
 */
ZEND_METHOD(Gtk4_GtkFlowBox, get_child_at_index) {
  zend_long idx;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(idx)
  ZEND_PARSE_PARAMETERS_END();
  GtkFlowBox *self = PHPGTK_SELF(GtkFlowBox, GTK_TYPE_FLOW_BOX);
  if (!phpgtk::check_range<int>(idx, 1)) RETURN_THROWS();
  GtkFlowBoxChild *phpgtk_ret = gtk_flow_box_get_child_at_index(self, static_cast<int>(idx));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkFlowBox::get_child_at_pos(int $x, int $y): ?GtkFlowBoxChild
 *
 * Gets the child in the ($x, $y) position.
 */
ZEND_METHOD(Gtk4_GtkFlowBox, get_child_at_pos) {
  zend_long x;
  zend_long y;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_LONG(x)
  Z_PARAM_LONG(y)
  ZEND_PARSE_PARAMETERS_END();
  GtkFlowBox *self = PHPGTK_SELF(GtkFlowBox, GTK_TYPE_FLOW_BOX);
  if (!phpgtk::check_range<int>(x, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(y, 2)) RETURN_THROWS();
  GtkFlowBoxChild *phpgtk_ret =
      gtk_flow_box_get_child_at_pos(self, static_cast<int>(x), static_cast<int>(y));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkFlowBox::get_column_spacing(): int
 *
 * Gets the horizontal spacing.
 */
ZEND_METHOD(Gtk4_GtkFlowBox, get_column_spacing) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFlowBox *self = PHPGTK_SELF(GtkFlowBox, GTK_TYPE_FLOW_BOX);
  RETURN_LONG(static_cast<zend_long>(gtk_flow_box_get_column_spacing(self)));
}

/**
 * Gtk4\GtkFlowBox::get_homogeneous(): bool
 *
 * Returns whether the box is homogeneous.
 */
ZEND_METHOD(Gtk4_GtkFlowBox, get_homogeneous) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFlowBox *self = PHPGTK_SELF(GtkFlowBox, GTK_TYPE_FLOW_BOX);
  RETURN_BOOL(gtk_flow_box_get_homogeneous(self));
}

/**
 * Gtk4\GtkFlowBox::get_max_children_per_line(): int
 *
 * Gets the maximum number of children per line.
 */
ZEND_METHOD(Gtk4_GtkFlowBox, get_max_children_per_line) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFlowBox *self = PHPGTK_SELF(GtkFlowBox, GTK_TYPE_FLOW_BOX);
  RETURN_LONG(static_cast<zend_long>(gtk_flow_box_get_max_children_per_line(self)));
}

/**
 * Gtk4\GtkFlowBox::get_min_children_per_line(): int
 *
 * Gets the minimum number of children per line.
 */
ZEND_METHOD(Gtk4_GtkFlowBox, get_min_children_per_line) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFlowBox *self = PHPGTK_SELF(GtkFlowBox, GTK_TYPE_FLOW_BOX);
  RETURN_LONG(static_cast<zend_long>(gtk_flow_box_get_min_children_per_line(self)));
}

/**
 * Gtk4\GtkFlowBox::get_row_spacing(): int
 *
 * Gets the vertical spacing.
 */
ZEND_METHOD(Gtk4_GtkFlowBox, get_row_spacing) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFlowBox *self = PHPGTK_SELF(GtkFlowBox, GTK_TYPE_FLOW_BOX);
  RETURN_LONG(static_cast<zend_long>(gtk_flow_box_get_row_spacing(self)));
}

/**
 * Gtk4\GtkFlowBox::get_selected_children(): array
 *
 * Creates a list of all selected children.
 */
ZEND_METHOD(Gtk4_GtkFlowBox, get_selected_children) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFlowBox *self = PHPGTK_SELF(GtkFlowBox, GTK_TYPE_FLOW_BOX);
  glist_to_php(gtk_flow_box_get_selected_children(self), GTK_TYPE_FLOW_BOX_CHILD,
               Transfer::Container, return_value);
}

/**
 * Gtk4\GtkFlowBox::get_selection_mode(): GtkSelectionMode
 *
 * Gets the selection mode of $box.
 */
ZEND_METHOD(Gtk4_GtkFlowBox, get_selection_mode) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFlowBox *self = PHPGTK_SELF(GtkFlowBox, GTK_TYPE_FLOW_BOX);
  enum_to_php(GTK_TYPE_SELECTION_MODE, gtk_flow_box_get_selection_mode(self), return_value);
}

/**
 * Gtk4\GtkFlowBox::insert(GtkWidget $widget, int $position): void
 *
 * Inserts the $widget into $box at $position.
 */
ZEND_METHOD(Gtk4_GtkFlowBox, insert) {
  zval *widget;
  zend_long position;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(widget, class_for_gtype(GTK_TYPE_WIDGET))
  Z_PARAM_LONG(position)
  ZEND_PARSE_PARAMETERS_END();
  GtkFlowBox *self = PHPGTK_SELF(GtkFlowBox, GTK_TYPE_FLOW_BOX);
  GObject *widget_o = unwrap(widget, GTK_TYPE_WIDGET);
  if (widget_o == nullptr) RETURN_THROWS();
  if (!phpgtk::check_range<int>(position, 2)) RETURN_THROWS();
  gtk_flow_box_insert(self, GTK_WIDGET(widget_o), static_cast<int>(position));
}

/**
 * Gtk4\GtkFlowBox::invalidate_filter(): void
 *
 * Updates the filtering for all children.
 */
ZEND_METHOD(Gtk4_GtkFlowBox, invalidate_filter) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFlowBox *self = PHPGTK_SELF(GtkFlowBox, GTK_TYPE_FLOW_BOX);
  gtk_flow_box_invalidate_filter(self);
}

/**
 * Gtk4\GtkFlowBox::invalidate_sort(): void
 *
 * Updates the sorting for all children.
 */
ZEND_METHOD(Gtk4_GtkFlowBox, invalidate_sort) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFlowBox *self = PHPGTK_SELF(GtkFlowBox, GTK_TYPE_FLOW_BOX);
  gtk_flow_box_invalidate_sort(self);
}

/**
 * Gtk4\GtkFlowBox::prepend(GtkWidget $child): void
 *
 * Adds $child to the start of $self.
 */
ZEND_METHOD(Gtk4_GtkFlowBox, prepend) {
  zval *child;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkFlowBox *self = PHPGTK_SELF(GtkFlowBox, GTK_TYPE_FLOW_BOX);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  gtk_flow_box_prepend(self, GTK_WIDGET(child_o));
}

/**
 * Gtk4\GtkFlowBox::remove(GtkWidget $widget): void
 *
 * Removes a child from $box.
 */
ZEND_METHOD(Gtk4_GtkFlowBox, remove) {
  zval *widget;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(widget, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkFlowBox *self = PHPGTK_SELF(GtkFlowBox, GTK_TYPE_FLOW_BOX);
  GObject *widget_o = unwrap(widget, GTK_TYPE_WIDGET);
  if (widget_o == nullptr) RETURN_THROWS();
  gtk_flow_box_remove(self, GTK_WIDGET(widget_o));
}

/**
 * Gtk4\GtkFlowBox::remove_all(): void
 *
 * Removes all children from $box.
 */
ZEND_METHOD(Gtk4_GtkFlowBox, remove_all) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFlowBox *self = PHPGTK_SELF(GtkFlowBox, GTK_TYPE_FLOW_BOX);
  gtk_flow_box_remove_all(self);
}

/**
 * Gtk4\GtkFlowBox::select_all(): void
 *
 * Select all children of $box, if the selection mode allows it.
 */
ZEND_METHOD(Gtk4_GtkFlowBox, select_all) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFlowBox *self = PHPGTK_SELF(GtkFlowBox, GTK_TYPE_FLOW_BOX);
  gtk_flow_box_select_all(self);
}

/**
 * Gtk4\GtkFlowBox::select_child(GtkFlowBoxChild $child): void
 *
 * Selects a single child of $box, if the selection mode allows it.
 */
ZEND_METHOD(Gtk4_GtkFlowBox, select_child) {
  zval *child;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_FLOW_BOX_CHILD))
  ZEND_PARSE_PARAMETERS_END();
  GtkFlowBox *self = PHPGTK_SELF(GtkFlowBox, GTK_TYPE_FLOW_BOX);
  GObject *child_o = unwrap(child, GTK_TYPE_FLOW_BOX_CHILD);
  if (child_o == nullptr) RETURN_THROWS();
  gtk_flow_box_select_child(self, GTK_FLOW_BOX_CHILD(child_o));
}

namespace {
// FlowBoxForeachFunc trampoline for GtkFlowBox::selected_foreach(): wraps the C arguments, invokes
// the PHP callable (call scope: released after the call).
void cb_selected_foreach_func(GtkFlowBox *box, GtkFlowBoxChild *child, gpointer user_data) {
  auto *cb = static_cast<Callback *>(user_data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(box != nullptr ? G_OBJECT(box) : nullptr, &argv[0]);
  wrap(child != nullptr ? G_OBJECT(child) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
}
}  // namespace

/**
 * Gtk4\GtkFlowBox::selected_foreach(callable $func): void
 *
 * Calls a function for each selected child.
 */
ZEND_METHOD(Gtk4_GtkFlowBox, selected_foreach) {
  zend_fcall_info fci_func = empty_fcall_info;
  zend_fcall_info_cache fcc_func = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_FUNC(fci_func, fcc_func)
  ZEND_PARSE_PARAMETERS_END();
  GtkFlowBox *self = PHPGTK_SELF(GtkFlowBox, GTK_TYPE_FLOW_BOX);
  Callback *cb_func = callback_new(&fci_func.function_name, "GtkFlowBox::selected_foreach");
  gtk_flow_box_selected_foreach(self, cb_selected_foreach_func, cb_func);
  if (cb_func != nullptr) {
    callback_free(cb_func);
    callback_drain();
  }
}

/**
 * Gtk4\GtkFlowBox::set_activate_on_single_click(bool $single): void
 *
 * If $single is `true`, children will be activated when you click on them, otherwise you need to
 * double-click.
 */
ZEND_METHOD(Gtk4_GtkFlowBox, set_activate_on_single_click) {
  bool single;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(single)
  ZEND_PARSE_PARAMETERS_END();
  GtkFlowBox *self = PHPGTK_SELF(GtkFlowBox, GTK_TYPE_FLOW_BOX);
  gtk_flow_box_set_activate_on_single_click(self, single);
}

/**
 * Gtk4\GtkFlowBox::set_column_spacing(int $spacing): void
 *
 * Sets the horizontal space to add between children.
 */
ZEND_METHOD(Gtk4_GtkFlowBox, set_column_spacing) {
  zend_long spacing;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(spacing)
  ZEND_PARSE_PARAMETERS_END();
  GtkFlowBox *self = PHPGTK_SELF(GtkFlowBox, GTK_TYPE_FLOW_BOX);
  if (!phpgtk::check_range<guint>(spacing, 1)) RETURN_THROWS();
  gtk_flow_box_set_column_spacing(self, static_cast<guint>(spacing));
}

/**
 * Gtk4\GtkFlowBox::set_hadjustment(GtkAdjustment $adjustment): void
 *
 * Hooks up an adjustment to focus handling in $box.
 */
ZEND_METHOD(Gtk4_GtkFlowBox, set_hadjustment) {
  zval *adjustment;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(adjustment, class_for_gtype(GTK_TYPE_ADJUSTMENT))
  ZEND_PARSE_PARAMETERS_END();
  GtkFlowBox *self = PHPGTK_SELF(GtkFlowBox, GTK_TYPE_FLOW_BOX);
  GObject *adjustment_o = unwrap(adjustment, GTK_TYPE_ADJUSTMENT);
  if (adjustment_o == nullptr) RETURN_THROWS();
  gtk_flow_box_set_hadjustment(self, GTK_ADJUSTMENT(adjustment_o));
}

/**
 * Gtk4\GtkFlowBox::set_homogeneous(bool $homogeneous): void
 *
 * Sets whether or not all children of $box are given equal space in the box.
 */
ZEND_METHOD(Gtk4_GtkFlowBox, set_homogeneous) {
  bool homogeneous;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(homogeneous)
  ZEND_PARSE_PARAMETERS_END();
  GtkFlowBox *self = PHPGTK_SELF(GtkFlowBox, GTK_TYPE_FLOW_BOX);
  gtk_flow_box_set_homogeneous(self, homogeneous);
}

/**
 * Gtk4\GtkFlowBox::set_max_children_per_line(int $n_children): void
 *
 * Sets the maximum number of children to request and allocate space for in $box’s orientation.
 */
ZEND_METHOD(Gtk4_GtkFlowBox, set_max_children_per_line) {
  zend_long n_children;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(n_children)
  ZEND_PARSE_PARAMETERS_END();
  GtkFlowBox *self = PHPGTK_SELF(GtkFlowBox, GTK_TYPE_FLOW_BOX);
  if (!phpgtk::check_range<guint>(n_children, 1)) RETURN_THROWS();
  gtk_flow_box_set_max_children_per_line(self, static_cast<guint>(n_children));
}

/**
 * Gtk4\GtkFlowBox::set_min_children_per_line(int $n_children): void
 *
 * Sets the minimum number of children to line up in $box’s orientation before flowing.
 */
ZEND_METHOD(Gtk4_GtkFlowBox, set_min_children_per_line) {
  zend_long n_children;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(n_children)
  ZEND_PARSE_PARAMETERS_END();
  GtkFlowBox *self = PHPGTK_SELF(GtkFlowBox, GTK_TYPE_FLOW_BOX);
  if (!phpgtk::check_range<guint>(n_children, 1)) RETURN_THROWS();
  gtk_flow_box_set_min_children_per_line(self, static_cast<guint>(n_children));
}

/**
 * Gtk4\GtkFlowBox::set_row_spacing(int $spacing): void
 *
 * Sets the vertical space to add between children.
 */
ZEND_METHOD(Gtk4_GtkFlowBox, set_row_spacing) {
  zend_long spacing;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(spacing)
  ZEND_PARSE_PARAMETERS_END();
  GtkFlowBox *self = PHPGTK_SELF(GtkFlowBox, GTK_TYPE_FLOW_BOX);
  if (!phpgtk::check_range<guint>(spacing, 1)) RETURN_THROWS();
  gtk_flow_box_set_row_spacing(self, static_cast<guint>(spacing));
}

/**
 * Gtk4\GtkFlowBox::set_selection_mode(GtkSelectionMode $mode): void
 *
 * Sets how selection works in $box.
 */
ZEND_METHOD(Gtk4_GtkFlowBox, set_selection_mode) {
  zval *mode;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(mode, enum_class_for_type(GTK_TYPE_SELECTION_MODE))
  ZEND_PARSE_PARAMETERS_END();
  GtkFlowBox *self = PHPGTK_SELF(GtkFlowBox, GTK_TYPE_FLOW_BOX);
  gint mode_v = 0;
  if (!enum_from_php(mode, GTK_TYPE_SELECTION_MODE, &mode_v)) RETURN_THROWS();
  gtk_flow_box_set_selection_mode(self, static_cast<GtkSelectionMode>(mode_v));
}

/**
 * Gtk4\GtkFlowBox::set_vadjustment(GtkAdjustment $adjustment): void
 *
 * Hooks up an adjustment to focus handling in $box.
 */
ZEND_METHOD(Gtk4_GtkFlowBox, set_vadjustment) {
  zval *adjustment;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(adjustment, class_for_gtype(GTK_TYPE_ADJUSTMENT))
  ZEND_PARSE_PARAMETERS_END();
  GtkFlowBox *self = PHPGTK_SELF(GtkFlowBox, GTK_TYPE_FLOW_BOX);
  GObject *adjustment_o = unwrap(adjustment, GTK_TYPE_ADJUSTMENT);
  if (adjustment_o == nullptr) RETURN_THROWS();
  gtk_flow_box_set_vadjustment(self, GTK_ADJUSTMENT(adjustment_o));
}

/**
 * Gtk4\GtkFlowBox::unselect_all(): void
 *
 * Unselect all children of $box, if the selection mode allows it.
 */
ZEND_METHOD(Gtk4_GtkFlowBox, unselect_all) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFlowBox *self = PHPGTK_SELF(GtkFlowBox, GTK_TYPE_FLOW_BOX);
  gtk_flow_box_unselect_all(self);
}

/**
 * Gtk4\GtkFlowBox::unselect_child(GtkFlowBoxChild $child): void
 *
 * Unselects a single child of $box, if the selection mode allows it.
 */
ZEND_METHOD(Gtk4_GtkFlowBox, unselect_child) {
  zval *child;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_FLOW_BOX_CHILD))
  ZEND_PARSE_PARAMETERS_END();
  GtkFlowBox *self = PHPGTK_SELF(GtkFlowBox, GTK_TYPE_FLOW_BOX);
  GObject *child_o = unwrap(child, GTK_TYPE_FLOW_BOX_CHILD);
  if (child_o == nullptr) RETURN_THROWS();
  gtk_flow_box_unselect_child(self, GTK_FLOW_BOX_CHILD(child_o));
}

/**
 * public function bind_model(?GListModel $model, ?callable $create_widget_func): void
 * Fill the box from $model, one child per item: `function (GObject $item): GtkWidget` builds the
 * child. The box follows the model from then on, and a null $model unbinds it, taking the
 * callable with it.
 */
ZEND_METHOD(Gtk4_GtkFlowBox, bind_model) {
  zval *model = nullptr;
  zend_fcall_info fci;
  zend_fcall_info_cache fcc;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(model, class_for_gtype(G_TYPE_LIST_MODEL))
  Z_PARAM_FUNC_OR_NULL(fci, fcc)
  ZEND_PARSE_PARAMETERS_END();
  GtkFlowBox *self = PHPGTK_SELF(GtkFlowBox, GTK_TYPE_FLOW_BOX);
  GObject *model_o = nullptr;
  if (model != nullptr) {
    model_o = unwrap(model, G_TYPE_LIST_MODEL);
    if (model_o == nullptr) RETURN_THROWS();
  }
  // A model without a create function would leave GTK dereferencing NULL for every item; a
  // create function without a model is worse than useless, because GTK returns before it stores
  // the destroy notify (gtkflowbox.c / gtklistbox.c) and the callable is leaked, never called.
  if (model_o != nullptr && !ZEND_FCI_INITIALIZED(fci)) {
    zend_argument_value_error(2, "must be a callable when a model is given");
    RETURN_THROWS();
  }
  if (model_o == nullptr && ZEND_FCI_INITIALIZED(fci)) {
    zend_argument_value_error(2, "must be null when no model is given");
    RETURN_THROWS();
  }
  if (!ZEND_FCI_INITIALIZED(fci)) {
    gtk_flow_box_bind_model(self, nullptr, nullptr, nullptr, nullptr);
  } else {
    Callback *cb = callback_new(&fci.function_name, "GtkFlowBox::bind_model");
    teardown_track_notified(cb, G_OBJECT(self), flow_box_create_widget_func_clear);
    gtk_flow_box_bind_model(self, model_o != nullptr ? G_LIST_MODEL(model_o) : nullptr,
                            flow_box_create_widget_func, cb, flow_box_create_widget_func_free);
  }
  callback_drain();  // the previous func's notify ran inside bind_model()
}

/**
 * public function set_filter_func(?callable $filter_func): void
 * Keep only the children `function (GtkFlowBoxChild $child): bool` answers true for; null shows
 * every child again.
 */
ZEND_METHOD(Gtk4_GtkFlowBox, set_filter_func) {
  zend_fcall_info fci;
  zend_fcall_info_cache fcc;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_FUNC_OR_NULL(fci, fcc)
  ZEND_PARSE_PARAMETERS_END();
  GtkFlowBox *self = PHPGTK_SELF(GtkFlowBox, GTK_TYPE_FLOW_BOX);
  if (!ZEND_FCI_INITIALIZED(fci)) {
    gtk_flow_box_set_filter_func(self, nullptr, nullptr, nullptr);
  } else {
    Callback *cb = callback_new(&fci.function_name, "GtkFlowBox::set_filter_func");
    teardown_track_notified(cb, G_OBJECT(self), flow_box_filter_func_clear);
    gtk_flow_box_set_filter_func(self, flow_box_filter_func, cb, flow_box_filter_func_free);
  }
  callback_drain();  // the previous func's notify ran inside the setter
}

/**
 * public function set_sort_func(?callable $sort_func): void
 * Order the children by `function (GtkFlowBoxChild $child1, GtkFlowBoxChild $child2): int` -
 * negative, zero or positive as usual; null leaves them in the order they were added.
 */
ZEND_METHOD(Gtk4_GtkFlowBox, set_sort_func) {
  zend_fcall_info fci;
  zend_fcall_info_cache fcc;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_FUNC_OR_NULL(fci, fcc)
  ZEND_PARSE_PARAMETERS_END();
  GtkFlowBox *self = PHPGTK_SELF(GtkFlowBox, GTK_TYPE_FLOW_BOX);
  if (!ZEND_FCI_INITIALIZED(fci)) {
    gtk_flow_box_set_sort_func(self, nullptr, nullptr, nullptr);
  } else {
    Callback *cb = callback_new(&fci.function_name, "GtkFlowBox::set_sort_func");
    teardown_track_notified(cb, G_OBJECT(self), flow_box_sort_func_clear);
    gtk_flow_box_set_sort_func(self, flow_box_sort_func, cb, flow_box_sort_func_free);
  }
  callback_drain();  // the previous func's notify ran inside the setter
}
