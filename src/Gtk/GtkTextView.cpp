// GENERATED by gen/gir.php from Gtk.TextView - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkTextView
#include "php_gtk4.h"
#include "core/object.h"
#include "core/enums.h"
#include "core/boxed.h"
#include "core/fundamental.h"
#include "core/subtype.h"
#include "core/error.h"
#include <array>

using namespace phpgtk;

/**
 * Gtk4\GtkTextView::__construct()
 *
 * Creates a new `GtkTextView`.
 */
ZEND_METHOD(Gtk4_GtkTextView, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gtk_text_view_new());
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * static Gtk4\GtkTextView::new_with_buffer(GtkTextBuffer $buffer): GtkTextView
 *
 * Creates a new `GtkTextView` widget displaying the buffer $buffer.
 */
ZEND_METHOD(Gtk4_GtkTextView, new_with_buffer) {
  zval *buffer;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(buffer, class_for_gtype(GTK_TYPE_TEXT_BUFFER))
  ZEND_PARSE_PARAMETERS_END();
  GObject *buffer_o = unwrap(buffer, GTK_TYPE_TEXT_BUFFER);
  if (buffer_o == nullptr) RETURN_THROWS();
  GObject *obj = G_OBJECT(gtk_text_view_new_with_buffer(GTK_TEXT_BUFFER(buffer_o)));
  wrap(obj, return_value);
}

/**
 * Gtk4\GtkTextView::add_overlay(GtkWidget $child, int $xpos, int $ypos): void
 *
 * Adds $child at a fixed coordinate in the `GtkTextView`'s text window.
 */
ZEND_METHOD(Gtk4_GtkTextView, add_overlay) {
  zval *child;
  zend_long xpos;
  zend_long ypos;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  Z_PARAM_LONG(xpos)
  Z_PARAM_LONG(ypos)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  if (!phpgtk::check_range<int>(xpos, 2)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(ypos, 3)) RETURN_THROWS();
  gtk_text_view_add_overlay(self, GTK_WIDGET(child_o), static_cast<int>(xpos),
                            static_cast<int>(ypos));
}

/**
 * Gtk4\GtkTextView::backward_display_line(GtkTextIter $iter): bool
 *
 * Moves the given $iter backward by one display (wrapped) line.
 */
ZEND_METHOD(Gtk4_GtkTextView, backward_display_line) {
  zval *iter;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(iter, boxed_class_for_type(GTK_TYPE_TEXT_ITER)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  gpointer iter_b = unwrap_boxed(iter, GTK_TYPE_TEXT_ITER);
  if (iter_b == nullptr) RETURN_THROWS();
  RETURN_BOOL(gtk_text_view_backward_display_line(self, static_cast<GtkTextIter *>(iter_b)));
}

/**
 * Gtk4\GtkTextView::backward_display_line_start(GtkTextIter $iter): bool
 *
 * Moves the given $iter backward to the next display line start.
 */
ZEND_METHOD(Gtk4_GtkTextView, backward_display_line_start) {
  zval *iter;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(iter, boxed_class_for_type(GTK_TYPE_TEXT_ITER)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  gpointer iter_b = unwrap_boxed(iter, GTK_TYPE_TEXT_ITER);
  if (iter_b == nullptr) RETURN_THROWS();
  RETURN_BOOL(gtk_text_view_backward_display_line_start(self, static_cast<GtkTextIter *>(iter_b)));
}

/**
 * Gtk4\GtkTextView::buffer_to_window_coords(GtkTextWindowType $win, int $buffer_x, int $buffer_y):
 * array
 *
 * Converts buffer coordinates to window coordinates.
 */
ZEND_METHOD(Gtk4_GtkTextView, buffer_to_window_coords) {
  zval *win;
  zend_long buffer_x;
  zend_long buffer_y;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_OBJECT_OF_CLASS(win, enum_class_for_type(GTK_TYPE_TEXT_WINDOW_TYPE))
  Z_PARAM_LONG(buffer_x)
  Z_PARAM_LONG(buffer_y)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  gint win_v = 0;
  if (!enum_from_php(win, GTK_TYPE_TEXT_WINDOW_TYPE, &win_v)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(buffer_x, 2)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(buffer_y, 3)) RETURN_THROWS();
  int window_x = 0;
  int window_y = 0;
  gtk_text_view_buffer_to_window_coords(self, static_cast<GtkTextWindowType>(win_v),
                                        static_cast<int>(buffer_x), static_cast<int>(buffer_y),
                                        &window_x, &window_y);
  array_init_size(return_value, 2);
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(window_x));
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(window_y));
    add_next_index_zval(return_value, &item);
  }
}

/**
 * Gtk4\GtkTextView::forward_display_line(GtkTextIter $iter): bool
 *
 * Moves the given $iter forward by one display (wrapped) line.
 */
ZEND_METHOD(Gtk4_GtkTextView, forward_display_line) {
  zval *iter;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(iter, boxed_class_for_type(GTK_TYPE_TEXT_ITER)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  gpointer iter_b = unwrap_boxed(iter, GTK_TYPE_TEXT_ITER);
  if (iter_b == nullptr) RETURN_THROWS();
  RETURN_BOOL(gtk_text_view_forward_display_line(self, static_cast<GtkTextIter *>(iter_b)));
}

/**
 * Gtk4\GtkTextView::forward_display_line_end(GtkTextIter $iter): bool
 *
 * Moves the given $iter forward to the next display line end.
 */
ZEND_METHOD(Gtk4_GtkTextView, forward_display_line_end) {
  zval *iter;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(iter, boxed_class_for_type(GTK_TYPE_TEXT_ITER)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  gpointer iter_b = unwrap_boxed(iter, GTK_TYPE_TEXT_ITER);
  if (iter_b == nullptr) RETURN_THROWS();
  RETURN_BOOL(gtk_text_view_forward_display_line_end(self, static_cast<GtkTextIter *>(iter_b)));
}

/**
 * Gtk4\GtkTextView::get_accepts_tab(): bool
 *
 * Returns whether pressing the <kbd>Tab</kbd> key inserts a tab characters.
 */
ZEND_METHOD(Gtk4_GtkTextView, get_accepts_tab) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  RETURN_BOOL(gtk_text_view_get_accepts_tab(self));
}

/**
 * Gtk4\GtkTextView::get_bottom_margin(): int
 *
 * Gets the bottom margin for text in the $text_view.
 */
ZEND_METHOD(Gtk4_GtkTextView, get_bottom_margin) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  RETURN_LONG(static_cast<zend_long>(gtk_text_view_get_bottom_margin(self)));
}

/**
 * Gtk4\GtkTextView::get_buffer(): GtkTextBuffer
 *
 * Returns the `GtkTextBuffer` being displayed by this text view.
 */
ZEND_METHOD(Gtk4_GtkTextView, get_buffer) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  GtkTextBuffer *phpgtk_ret = gtk_text_view_get_buffer(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkTextView::get_cursor_locations(?GtkTextIter $iter): array
 *
 * Determine the positions of the strong and weak cursors if the insertion point is at $iter.
 */
ZEND_METHOD(Gtk4_GtkTextView, get_cursor_locations) {
  zval *iter = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(iter, boxed_class_for_type(GTK_TYPE_TEXT_ITER)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  gpointer iter_b = nullptr;
  if (iter != nullptr) {
    iter_b = unwrap_boxed(iter, GTK_TYPE_TEXT_ITER);
    if (iter_b == nullptr) RETURN_THROWS();
  }
  GdkRectangle strong{};
  GdkRectangle weak{};
  gtk_text_view_get_cursor_locations(self, static_cast<GtkTextIter *>(iter_b), &strong, &weak);
  array_init_size(return_value, 2);
  {
    zval item;
    wrap_boxed(GDK_TYPE_RECTANGLE, &strong, &item);
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    wrap_boxed(GDK_TYPE_RECTANGLE, &weak, &item);
    add_next_index_zval(return_value, &item);
  }
}

/**
 * Gtk4\GtkTextView::get_cursor_visible(): bool
 *
 * Find out whether the cursor should be displayed.
 */
ZEND_METHOD(Gtk4_GtkTextView, get_cursor_visible) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  RETURN_BOOL(gtk_text_view_get_cursor_visible(self));
}

/**
 * Gtk4\GtkTextView::get_editable(): bool
 *
 * Returns the default editability of the `GtkTextView`.
 */
ZEND_METHOD(Gtk4_GtkTextView, get_editable) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  RETURN_BOOL(gtk_text_view_get_editable(self));
}

/**
 * Gtk4\GtkTextView::get_gutter(GtkTextWindowType $win): ?GtkWidget
 *
 * Gets a `GtkWidget` that has previously been set as gutter.
 */
ZEND_METHOD(Gtk4_GtkTextView, get_gutter) {
  zval *win;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(win, enum_class_for_type(GTK_TYPE_TEXT_WINDOW_TYPE))
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  gint win_v = 0;
  if (!enum_from_php(win, GTK_TYPE_TEXT_WINDOW_TYPE, &win_v)) RETURN_THROWS();
  GtkWidget *phpgtk_ret = gtk_text_view_get_gutter(self, static_cast<GtkTextWindowType>(win_v));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkTextView::get_indent(): int
 *
 * Gets the default indentation of paragraphs in $text_view.
 */
ZEND_METHOD(Gtk4_GtkTextView, get_indent) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  RETURN_LONG(static_cast<zend_long>(gtk_text_view_get_indent(self)));
}

/**
 * Gtk4\GtkTextView::get_input_hints(): int
 *
 * Gets the `input-hints` of the `GtkTextView`.
 */
ZEND_METHOD(Gtk4_GtkTextView, get_input_hints) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  RETURN_LONG(static_cast<zend_long>(gtk_text_view_get_input_hints(self)));
}

/**
 * Gtk4\GtkTextView::get_input_purpose(): GtkInputPurpose
 *
 * Gets the `input-purpose` of the `GtkTextView`.
 */
ZEND_METHOD(Gtk4_GtkTextView, get_input_purpose) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  enum_to_php(GTK_TYPE_INPUT_PURPOSE, gtk_text_view_get_input_purpose(self), return_value);
}

/**
 * Gtk4\GtkTextView::get_iter_at_location(int $x, int $y): ?GtkTextIter
 *
 * Retrieves the iterator at buffer coordinates $x and $y.
 */
ZEND_METHOD(Gtk4_GtkTextView, get_iter_at_location) {
  zend_long x;
  zend_long y;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_LONG(x)
  Z_PARAM_LONG(y)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  if (!phpgtk::check_range<int>(x, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(y, 2)) RETURN_THROWS();
  GtkTextIter iter{};
  if (!gtk_text_view_get_iter_at_location(self, &iter, static_cast<int>(x), static_cast<int>(y)))
    RETURN_NULL();
  wrap_boxed(GTK_TYPE_TEXT_ITER, &iter, return_value);
}

/**
 * Gtk4\GtkTextView::get_iter_at_position(int $x, int $y): ?array
 *
 * Retrieves the iterator pointing to the character at buffer coordinates $x and $y.
 */
ZEND_METHOD(Gtk4_GtkTextView, get_iter_at_position) {
  zend_long x;
  zend_long y;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_LONG(x)
  Z_PARAM_LONG(y)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  if (!phpgtk::check_range<int>(x, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(y, 2)) RETURN_THROWS();
  GtkTextIter iter{};
  int trailing = 0;
  if (!gtk_text_view_get_iter_at_position(self, &iter, &trailing, static_cast<int>(x),
                                          static_cast<int>(y)))
    RETURN_NULL();
  array_init_size(return_value, 2);
  {
    zval item;
    wrap_boxed(GTK_TYPE_TEXT_ITER, &iter, &item);
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(trailing));
    add_next_index_zval(return_value, &item);
  }
}

/**
 * Gtk4\GtkTextView::get_iter_location(GtkTextIter $iter): GdkRectangle
 *
 * Gets a rectangle which roughly contains the character at $iter.
 */
ZEND_METHOD(Gtk4_GtkTextView, get_iter_location) {
  zval *iter;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(iter, boxed_class_for_type(GTK_TYPE_TEXT_ITER)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  gpointer iter_b = unwrap_boxed(iter, GTK_TYPE_TEXT_ITER);
  if (iter_b == nullptr) RETURN_THROWS();
  GdkRectangle location{};
  gtk_text_view_get_iter_location(self, static_cast<GtkTextIter *>(iter_b), &location);
  wrap_boxed(GDK_TYPE_RECTANGLE, &location, return_value);
}

/**
 * Gtk4\GtkTextView::get_justification(): GtkJustification
 *
 * Gets the default justification of paragraphs in $text_view.
 */
ZEND_METHOD(Gtk4_GtkTextView, get_justification) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  enum_to_php(GTK_TYPE_JUSTIFICATION, gtk_text_view_get_justification(self), return_value);
}

/**
 * Gtk4\GtkTextView::get_left_margin(): int
 *
 * Gets the default left margin size of paragraphs in the $text_view.
 */
ZEND_METHOD(Gtk4_GtkTextView, get_left_margin) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  RETURN_LONG(static_cast<zend_long>(gtk_text_view_get_left_margin(self)));
}

/**
 * Gtk4\GtkTextView::get_line_at_y(int $y): array
 *
 * Gets the `GtkTextIter` at the start of the line containing the coordinate $y.
 */
ZEND_METHOD(Gtk4_GtkTextView, get_line_at_y) {
  zend_long y;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(y)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  if (!phpgtk::check_range<int>(y, 1)) RETURN_THROWS();
  GtkTextIter target_iter{};
  int line_top = 0;
  gtk_text_view_get_line_at_y(self, &target_iter, static_cast<int>(y), &line_top);
  array_init_size(return_value, 2);
  {
    zval item;
    wrap_boxed(GTK_TYPE_TEXT_ITER, &target_iter, &item);
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(line_top));
    add_next_index_zval(return_value, &item);
  }
}

/**
 * Gtk4\GtkTextView::get_line_yrange(GtkTextIter $iter): array
 *
 * Gets the y coordinate of the top of the line containing $iter, and the height of the line.
 */
ZEND_METHOD(Gtk4_GtkTextView, get_line_yrange) {
  zval *iter;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(iter, boxed_class_for_type(GTK_TYPE_TEXT_ITER)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  gpointer iter_b = unwrap_boxed(iter, GTK_TYPE_TEXT_ITER);
  if (iter_b == nullptr) RETURN_THROWS();
  int y = 0;
  int height = 0;
  gtk_text_view_get_line_yrange(self, static_cast<GtkTextIter *>(iter_b), &y, &height);
  array_init_size(return_value, 2);
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(y));
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(height));
    add_next_index_zval(return_value, &item);
  }
}

/**
 * Gtk4\GtkTextView::get_monospace(): bool
 *
 * Gets whether the `GtkTextView` uses monospace styling.
 */
ZEND_METHOD(Gtk4_GtkTextView, get_monospace) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  RETURN_BOOL(gtk_text_view_get_monospace(self));
}

/**
 * Gtk4\GtkTextView::get_overwrite(): bool
 *
 * Returns whether the `GtkTextView` is in overwrite mode or not.
 */
ZEND_METHOD(Gtk4_GtkTextView, get_overwrite) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  RETURN_BOOL(gtk_text_view_get_overwrite(self));
}

/**
 * Gtk4\GtkTextView::get_pixels_above_lines(): int
 *
 * Gets the default number of pixels to put above paragraphs.
 */
ZEND_METHOD(Gtk4_GtkTextView, get_pixels_above_lines) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  RETURN_LONG(static_cast<zend_long>(gtk_text_view_get_pixels_above_lines(self)));
}

/**
 * Gtk4\GtkTextView::get_pixels_below_lines(): int
 *
 * Gets the default number of pixels to put below paragraphs.
 */
ZEND_METHOD(Gtk4_GtkTextView, get_pixels_below_lines) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  RETURN_LONG(static_cast<zend_long>(gtk_text_view_get_pixels_below_lines(self)));
}

/**
 * Gtk4\GtkTextView::get_pixels_inside_wrap(): int
 *
 * Gets the default number of pixels to put between wrapped lines inside a paragraph.
 */
ZEND_METHOD(Gtk4_GtkTextView, get_pixels_inside_wrap) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  RETURN_LONG(static_cast<zend_long>(gtk_text_view_get_pixels_inside_wrap(self)));
}

/**
 * Gtk4\GtkTextView::get_right_margin(): int
 *
 * Gets the default right margin for text in $text_view.
 */
ZEND_METHOD(Gtk4_GtkTextView, get_right_margin) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  RETURN_LONG(static_cast<zend_long>(gtk_text_view_get_right_margin(self)));
}

/**
 * Gtk4\GtkTextView::get_top_margin(): int
 *
 * Gets the top margin for text in the $text_view.
 */
ZEND_METHOD(Gtk4_GtkTextView, get_top_margin) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  RETURN_LONG(static_cast<zend_long>(gtk_text_view_get_top_margin(self)));
}

/**
 * Gtk4\GtkTextView::get_visible_rect(): GdkRectangle
 *
 * Fills $visible_rect with the currently-visible region of the buffer, in buffer coordinates.
 */
ZEND_METHOD(Gtk4_GtkTextView, get_visible_rect) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  GdkRectangle visible_rect{};
  gtk_text_view_get_visible_rect(self, &visible_rect);
  wrap_boxed(GDK_TYPE_RECTANGLE, &visible_rect, return_value);
}

/**
 * Gtk4\GtkTextView::get_wrap_mode(): GtkWrapMode
 *
 * Gets the line wrapping for the view.
 */
ZEND_METHOD(Gtk4_GtkTextView, get_wrap_mode) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  enum_to_php(GTK_TYPE_WRAP_MODE, gtk_text_view_get_wrap_mode(self), return_value);
}

/**
 * Gtk4\GtkTextView::im_context_filter_keypress(GdkEvent $event): bool
 *
 * Allow the `GtkTextView` input method to internally handle key press and release events.
 */
ZEND_METHOD(Gtk4_GtkTextView, im_context_filter_keypress) {
  zval *event;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(event, fundamental_class_for_type(GDK_TYPE_EVENT)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  gpointer event_f = unwrap_fundamental(event, GDK_TYPE_EVENT);
  if (event_f == nullptr) RETURN_THROWS();
  RETURN_BOOL(gtk_text_view_im_context_filter_keypress(self, static_cast<GdkEvent *>(event_f)));
}

/**
 * Gtk4\GtkTextView::move_mark_onscreen(GtkTextMark $mark): bool
 *
 * Moves a mark within the buffer so that it's located within the currently-visible text area.
 */
ZEND_METHOD(Gtk4_GtkTextView, move_mark_onscreen) {
  zval *mark;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(mark, class_for_gtype(GTK_TYPE_TEXT_MARK))
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  GObject *mark_o = unwrap(mark, GTK_TYPE_TEXT_MARK);
  if (mark_o == nullptr) RETURN_THROWS();
  RETURN_BOOL(gtk_text_view_move_mark_onscreen(self, GTK_TEXT_MARK(mark_o)));
}

/**
 * Gtk4\GtkTextView::move_overlay(GtkWidget $child, int $xpos, int $ypos): void
 *
 * Updates the position of a child.
 */
ZEND_METHOD(Gtk4_GtkTextView, move_overlay) {
  zval *child;
  zend_long xpos;
  zend_long ypos;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  Z_PARAM_LONG(xpos)
  Z_PARAM_LONG(ypos)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  if (!phpgtk::check_range<int>(xpos, 2)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(ypos, 3)) RETURN_THROWS();
  gtk_text_view_move_overlay(self, GTK_WIDGET(child_o), static_cast<int>(xpos),
                             static_cast<int>(ypos));
}

/**
 * Gtk4\GtkTextView::move_visually(GtkTextIter $iter, int $count): bool
 *
 * Move the iterator a given number of characters visually, treating it as the strong cursor
 * position.
 */
ZEND_METHOD(Gtk4_GtkTextView, move_visually) {
  zval *iter;
  zend_long count;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(iter, boxed_class_for_type(GTK_TYPE_TEXT_ITER)->ce)
  Z_PARAM_LONG(count)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  gpointer iter_b = unwrap_boxed(iter, GTK_TYPE_TEXT_ITER);
  if (iter_b == nullptr) RETURN_THROWS();
  if (!phpgtk::check_range<int>(count, 2)) RETURN_THROWS();
  RETURN_BOOL(gtk_text_view_move_visually(self, static_cast<GtkTextIter *>(iter_b),
                                          static_cast<int>(count)));
}

/**
 * Gtk4\GtkTextView::place_cursor_onscreen(): bool
 *
 * Moves the cursor to the currently visible region of the buffer.
 */
ZEND_METHOD(Gtk4_GtkTextView, place_cursor_onscreen) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  RETURN_BOOL(gtk_text_view_place_cursor_onscreen(self));
}

/**
 * Gtk4\GtkTextView::remove(GtkWidget $child): void
 *
 * Removes a child widget from $text_view.
 */
ZEND_METHOD(Gtk4_GtkTextView, remove) {
  zval *child;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  gtk_text_view_remove(self, GTK_WIDGET(child_o));
}

/**
 * Gtk4\GtkTextView::reset_cursor_blink(): void
 *
 * Ensures that the cursor is shown.
 */
ZEND_METHOD(Gtk4_GtkTextView, reset_cursor_blink) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  gtk_text_view_reset_cursor_blink(self);
}

/**
 * Gtk4\GtkTextView::reset_im_context(): void
 *
 * Reset the input method context of the text view if needed.
 */
ZEND_METHOD(Gtk4_GtkTextView, reset_im_context) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  gtk_text_view_reset_im_context(self);
}

/**
 * Gtk4\GtkTextView::scroll_mark_onscreen(GtkTextMark $mark): void
 *
 * Scrolls $text_view the minimum distance such that $mark is contained within the visible area of
 * the widget.
 */
ZEND_METHOD(Gtk4_GtkTextView, scroll_mark_onscreen) {
  zval *mark;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(mark, class_for_gtype(GTK_TYPE_TEXT_MARK))
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  GObject *mark_o = unwrap(mark, GTK_TYPE_TEXT_MARK);
  if (mark_o == nullptr) RETURN_THROWS();
  gtk_text_view_scroll_mark_onscreen(self, GTK_TEXT_MARK(mark_o));
}

/**
 * Gtk4\GtkTextView::scroll_to_iter(GtkTextIter $iter, float $within_margin, bool $use_align, float
 * $xalign, float $yalign): bool
 *
 * Scrolls $text_view so that $iter is on the screen in the position indicated by $xalign and
 * $yalign.
 */
ZEND_METHOD(Gtk4_GtkTextView, scroll_to_iter) {
  zval *iter;
  double within_margin;
  bool use_align;
  double xalign;
  double yalign;
  ZEND_PARSE_PARAMETERS_START(5, 5)
  Z_PARAM_OBJECT_OF_CLASS(iter, boxed_class_for_type(GTK_TYPE_TEXT_ITER)->ce)
  Z_PARAM_DOUBLE(within_margin)
  Z_PARAM_BOOL(use_align)
  Z_PARAM_DOUBLE(xalign)
  Z_PARAM_DOUBLE(yalign)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  gpointer iter_b = unwrap_boxed(iter, GTK_TYPE_TEXT_ITER);
  if (iter_b == nullptr) RETURN_THROWS();
  RETURN_BOOL(gtk_text_view_scroll_to_iter(self, static_cast<GtkTextIter *>(iter_b), within_margin,
                                           use_align, xalign, yalign));
}

/**
 * Gtk4\GtkTextView::scroll_to_mark(GtkTextMark $mark, float $within_margin, bool $use_align, float
 * $xalign, float $yalign): void
 *
 * Scrolls $text_view so that $mark is on the screen in the position indicated by $xalign and
 * $yalign.
 */
ZEND_METHOD(Gtk4_GtkTextView, scroll_to_mark) {
  zval *mark;
  double within_margin;
  bool use_align;
  double xalign;
  double yalign;
  ZEND_PARSE_PARAMETERS_START(5, 5)
  Z_PARAM_OBJECT_OF_CLASS(mark, class_for_gtype(GTK_TYPE_TEXT_MARK))
  Z_PARAM_DOUBLE(within_margin)
  Z_PARAM_BOOL(use_align)
  Z_PARAM_DOUBLE(xalign)
  Z_PARAM_DOUBLE(yalign)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  GObject *mark_o = unwrap(mark, GTK_TYPE_TEXT_MARK);
  if (mark_o == nullptr) RETURN_THROWS();
  gtk_text_view_scroll_to_mark(self, GTK_TEXT_MARK(mark_o), within_margin, use_align, xalign,
                               yalign);
}

/**
 * Gtk4\GtkTextView::set_accepts_tab(bool $accepts_tab): void
 *
 * Sets the behavior of the text widget when the <kbd>Tab</kbd> key is pressed.
 */
ZEND_METHOD(Gtk4_GtkTextView, set_accepts_tab) {
  bool accepts_tab;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(accepts_tab)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  gtk_text_view_set_accepts_tab(self, accepts_tab);
}

/**
 * Gtk4\GtkTextView::set_bottom_margin(int $bottom_margin): void
 *
 * Sets the bottom margin for text in $text_view.
 */
ZEND_METHOD(Gtk4_GtkTextView, set_bottom_margin) {
  zend_long bottom_margin;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(bottom_margin)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  if (!phpgtk::check_range<int>(bottom_margin, 1)) RETURN_THROWS();
  gtk_text_view_set_bottom_margin(self, static_cast<int>(bottom_margin));
}

/**
 * Gtk4\GtkTextView::set_buffer(?GtkTextBuffer $buffer): void
 *
 * Sets $buffer as the buffer being displayed by $text_view.
 */
ZEND_METHOD(Gtk4_GtkTextView, set_buffer) {
  zval *buffer = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(buffer, class_for_gtype(GTK_TYPE_TEXT_BUFFER))
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  GObject *buffer_o = nullptr;
  if (buffer != nullptr) {
    buffer_o = unwrap(buffer, GTK_TYPE_TEXT_BUFFER);
    if (buffer_o == nullptr) RETURN_THROWS();
  }
  gtk_text_view_set_buffer(self, buffer_o != nullptr ? GTK_TEXT_BUFFER(buffer_o) : nullptr);
}

/**
 * Gtk4\GtkTextView::set_cursor_visible(bool $setting): void
 *
 * Toggles whether the insertion point should be displayed.
 */
ZEND_METHOD(Gtk4_GtkTextView, set_cursor_visible) {
  bool setting;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(setting)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  gtk_text_view_set_cursor_visible(self, setting);
}

/**
 * Gtk4\GtkTextView::set_editable(bool $setting): void
 *
 * Sets the default editability of the `GtkTextView`.
 */
ZEND_METHOD(Gtk4_GtkTextView, set_editable) {
  bool setting;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(setting)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  gtk_text_view_set_editable(self, setting);
}

/**
 * Gtk4\GtkTextView::set_extra_menu(?GMenuModel $model): void
 *
 * Sets a menu model to add when constructing the context menu for $text_view.
 */
ZEND_METHOD(Gtk4_GtkTextView, set_extra_menu) {
  zval *model = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(model, class_for_gtype(G_TYPE_MENU_MODEL))
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  GObject *model_o = nullptr;
  if (model != nullptr) {
    model_o = unwrap(model, G_TYPE_MENU_MODEL);
    if (model_o == nullptr) RETURN_THROWS();
  }
  gtk_text_view_set_extra_menu(self, model_o != nullptr ? G_MENU_MODEL(model_o) : nullptr);
}

/**
 * Gtk4\GtkTextView::set_gutter(GtkTextWindowType $win, ?GtkWidget $widget): void
 *
 * Places $widget into the gutter specified by $win.
 */
ZEND_METHOD(Gtk4_GtkTextView, set_gutter) {
  zval *win;
  zval *widget = nullptr;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(win, enum_class_for_type(GTK_TYPE_TEXT_WINDOW_TYPE))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(widget, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  gint win_v = 0;
  if (!enum_from_php(win, GTK_TYPE_TEXT_WINDOW_TYPE, &win_v)) RETURN_THROWS();
  GObject *widget_o = nullptr;
  if (widget != nullptr) {
    widget_o = unwrap(widget, GTK_TYPE_WIDGET);
    if (widget_o == nullptr) RETURN_THROWS();
  }
  gtk_text_view_set_gutter(self, static_cast<GtkTextWindowType>(win_v),
                           widget_o != nullptr ? GTK_WIDGET(widget_o) : nullptr);
}

/**
 * Gtk4\GtkTextView::set_indent(int $indent): void
 *
 * Sets the default indentation for paragraphs in $text_view.
 */
ZEND_METHOD(Gtk4_GtkTextView, set_indent) {
  zend_long indent;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(indent)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  if (!phpgtk::check_range<int>(indent, 1)) RETURN_THROWS();
  gtk_text_view_set_indent(self, static_cast<int>(indent));
}

/**
 * Gtk4\GtkTextView::set_input_hints(int $hints): void
 *
 * Sets the `input-hints` of the `GtkTextView`.
 */
ZEND_METHOD(Gtk4_GtkTextView, set_input_hints) {
  zend_long hints;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(hints)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  if (!phpgtk::check_flags(GTK_TYPE_INPUT_HINTS, hints, 1)) RETURN_THROWS();
  gtk_text_view_set_input_hints(self, static_cast<GtkInputHints>(hints));
}

/**
 * Gtk4\GtkTextView::set_input_purpose(GtkInputPurpose $purpose): void
 *
 * Sets the `input-purpose` of the `GtkTextView`.
 */
ZEND_METHOD(Gtk4_GtkTextView, set_input_purpose) {
  zval *purpose;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(purpose, enum_class_for_type(GTK_TYPE_INPUT_PURPOSE))
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  gint purpose_v = 0;
  if (!enum_from_php(purpose, GTK_TYPE_INPUT_PURPOSE, &purpose_v)) RETURN_THROWS();
  gtk_text_view_set_input_purpose(self, static_cast<GtkInputPurpose>(purpose_v));
}

/**
 * Gtk4\GtkTextView::set_justification(GtkJustification $justification): void
 *
 * Sets the default justification of text in $text_view.
 */
ZEND_METHOD(Gtk4_GtkTextView, set_justification) {
  zval *justification;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(justification, enum_class_for_type(GTK_TYPE_JUSTIFICATION))
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  gint justification_v = 0;
  if (!enum_from_php(justification, GTK_TYPE_JUSTIFICATION, &justification_v)) RETURN_THROWS();
  gtk_text_view_set_justification(self, static_cast<GtkJustification>(justification_v));
}

/**
 * Gtk4\GtkTextView::set_left_margin(int $left_margin): void
 *
 * Sets the default left margin for text in $text_view.
 */
ZEND_METHOD(Gtk4_GtkTextView, set_left_margin) {
  zend_long left_margin;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(left_margin)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  if (!phpgtk::check_range<int>(left_margin, 1)) RETURN_THROWS();
  gtk_text_view_set_left_margin(self, static_cast<int>(left_margin));
}

/**
 * Gtk4\GtkTextView::set_monospace(bool $monospace): void
 *
 * Sets whether the `GtkTextView` should display text in monospace styling.
 */
ZEND_METHOD(Gtk4_GtkTextView, set_monospace) {
  bool monospace;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(monospace)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  gtk_text_view_set_monospace(self, monospace);
}

/**
 * Gtk4\GtkTextView::set_overwrite(bool $overwrite): void
 *
 * Changes the `GtkTextView` overwrite mode.
 */
ZEND_METHOD(Gtk4_GtkTextView, set_overwrite) {
  bool overwrite;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(overwrite)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  gtk_text_view_set_overwrite(self, overwrite);
}

/**
 * Gtk4\GtkTextView::set_pixels_above_lines(int $pixels_above_lines): void
 *
 * Sets the default number of blank pixels above paragraphs in $text_view.
 */
ZEND_METHOD(Gtk4_GtkTextView, set_pixels_above_lines) {
  zend_long pixels_above_lines;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(pixels_above_lines)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  if (!phpgtk::check_range<int>(pixels_above_lines, 1)) RETURN_THROWS();
  gtk_text_view_set_pixels_above_lines(self, static_cast<int>(pixels_above_lines));
}

/**
 * Gtk4\GtkTextView::set_pixels_below_lines(int $pixels_below_lines): void
 *
 * Sets the default number of pixels of blank space to put below paragraphs in $text_view.
 */
ZEND_METHOD(Gtk4_GtkTextView, set_pixels_below_lines) {
  zend_long pixels_below_lines;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(pixels_below_lines)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  if (!phpgtk::check_range<int>(pixels_below_lines, 1)) RETURN_THROWS();
  gtk_text_view_set_pixels_below_lines(self, static_cast<int>(pixels_below_lines));
}

/**
 * Gtk4\GtkTextView::set_pixels_inside_wrap(int $pixels_inside_wrap): void
 *
 * Sets the default number of pixels of blank space to leave between display/wrapped lines within a
 * paragraph.
 */
ZEND_METHOD(Gtk4_GtkTextView, set_pixels_inside_wrap) {
  zend_long pixels_inside_wrap;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(pixels_inside_wrap)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  if (!phpgtk::check_range<int>(pixels_inside_wrap, 1)) RETURN_THROWS();
  gtk_text_view_set_pixels_inside_wrap(self, static_cast<int>(pixels_inside_wrap));
}

/**
 * Gtk4\GtkTextView::set_right_margin(int $right_margin): void
 *
 * Sets the default right margin for text in the text view.
 */
ZEND_METHOD(Gtk4_GtkTextView, set_right_margin) {
  zend_long right_margin;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(right_margin)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  if (!phpgtk::check_range<int>(right_margin, 1)) RETURN_THROWS();
  gtk_text_view_set_right_margin(self, static_cast<int>(right_margin));
}

/**
 * Gtk4\GtkTextView::set_top_margin(int $top_margin): void
 *
 * Sets the top margin for text in $text_view.
 */
ZEND_METHOD(Gtk4_GtkTextView, set_top_margin) {
  zend_long top_margin;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(top_margin)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  if (!phpgtk::check_range<int>(top_margin, 1)) RETURN_THROWS();
  gtk_text_view_set_top_margin(self, static_cast<int>(top_margin));
}

/**
 * Gtk4\GtkTextView::set_wrap_mode(GtkWrapMode $wrap_mode): void
 *
 * Sets the line wrapping for the view.
 */
ZEND_METHOD(Gtk4_GtkTextView, set_wrap_mode) {
  zval *wrap_mode;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(wrap_mode, enum_class_for_type(GTK_TYPE_WRAP_MODE))
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  gint wrap_mode_v = 0;
  if (!enum_from_php(wrap_mode, GTK_TYPE_WRAP_MODE, &wrap_mode_v)) RETURN_THROWS();
  gtk_text_view_set_wrap_mode(self, static_cast<GtkWrapMode>(wrap_mode_v));
}

/**
 * Gtk4\GtkTextView::starts_display_line(GtkTextIter $iter): bool
 *
 * Determines whether $iter is at the start of a display line.
 */
ZEND_METHOD(Gtk4_GtkTextView, starts_display_line) {
  zval *iter;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(iter, boxed_class_for_type(GTK_TYPE_TEXT_ITER)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  gpointer iter_b = unwrap_boxed(iter, GTK_TYPE_TEXT_ITER);
  if (iter_b == nullptr) RETURN_THROWS();
  RETURN_BOOL(gtk_text_view_starts_display_line(self, static_cast<GtkTextIter *>(iter_b)));
}

/**
 * Gtk4\GtkTextView::window_to_buffer_coords(GtkTextWindowType $win, int $window_x, int $window_y):
 * array
 *
 * Converts coordinates on the window identified by $win to buffer coordinates.
 */
ZEND_METHOD(Gtk4_GtkTextView, window_to_buffer_coords) {
  zval *win;
  zend_long window_x;
  zend_long window_y;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_OBJECT_OF_CLASS(win, enum_class_for_type(GTK_TYPE_TEXT_WINDOW_TYPE))
  Z_PARAM_LONG(window_x)
  Z_PARAM_LONG(window_y)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  gint win_v = 0;
  if (!enum_from_php(win, GTK_TYPE_TEXT_WINDOW_TYPE, &win_v)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(window_x, 2)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(window_y, 3)) RETURN_THROWS();
  int buffer_x = 0;
  int buffer_y = 0;
  gtk_text_view_window_to_buffer_coords(self, static_cast<GtkTextWindowType>(win_v),
                                        static_cast<int>(window_x), static_cast<int>(window_y),
                                        &buffer_x, &buffer_y);
  array_init_size(return_value, 2);
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(buffer_x));
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(buffer_y));
    add_next_index_zval(return_value, &item);
  }
}

/**
 * public function get_extra_menu(): ?GMenuModel
 * Gets the menu model added to the context menu, or `null` if none has been set. (GTK's
 * documentation says "or NULL" but the 4.14 GIR annotation misses the nullable; declared
 * here so the type stays true.)
 */
ZEND_METHOD(Gtk4_GtkTextView, get_extra_menu) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  GMenuModel *phpgtk_ret = gtk_text_view_get_extra_menu(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

// vfunc thunks and installers: file-local, installed by class_init of a PHP subtype
namespace {

// vfunc thunk: GTK_TEXT_VIEW_CLASS->backspace -> $this->vfunc_backspace() on a PHP subclass
void vfunc_thunk_backspace(GtkTextView *self) {
  zval zself;
  zend_function *fn =
      EG(exception) == nullptr ? subtype_vfunc(G_OBJECT(self), "vfunc_backspace", &zself) : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    auto *native = GTK_TEXT_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->backspace != nullptr) native->backspace(self);
    return;
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkTextView::vfunc_backspace");
}

// vfunc installer: GTK_TEXT_VIEW_CLASS->backspace (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_backspace(gpointer klass) {
  GTK_TEXT_VIEW_CLASS(klass)->backspace = vfunc_thunk_backspace;
}

// vfunc thunk: GTK_TEXT_VIEW_CLASS->copy_clipboard -> $this->vfunc_copy_clipboard() on a PHP
// subclass
void vfunc_thunk_copy_clipboard(GtkTextView *self) {
  zval zself;
  zend_function *fn = EG(exception) == nullptr
                          ? subtype_vfunc(G_OBJECT(self), "vfunc_copy_clipboard", &zself)
                          : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    auto *native = GTK_TEXT_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->copy_clipboard != nullptr) native->copy_clipboard(self);
    return;
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkTextView::vfunc_copy_clipboard");
}

// vfunc installer: GTK_TEXT_VIEW_CLASS->copy_clipboard (called from class_init / iface_init of a
// PHP subtype)
void vfunc_install_copy_clipboard(gpointer klass) {
  GTK_TEXT_VIEW_CLASS(klass)->copy_clipboard = vfunc_thunk_copy_clipboard;
}

// vfunc thunk: GTK_TEXT_VIEW_CLASS->create_buffer -> $this->vfunc_create_buffer() on a PHP subclass
GtkTextBuffer *vfunc_thunk_create_buffer(GtkTextView *self) {
  zval zself;
  zend_function *fn = EG(exception) == nullptr
                          ? subtype_vfunc(G_OBJECT(self), "vfunc_create_buffer", &zself)
                          : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    auto *native = GTK_TEXT_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->create_buffer != nullptr ? native->create_buffer(self) : nullptr;
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  GtkTextBuffer *result = nullptr;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    if (Z_TYPE(ret) == IS_OBJECT) {
      GObject *o = unwrap(&ret, GTK_TYPE_TEXT_BUFFER);
      result = o != nullptr ? GTK_TEXT_BUFFER(o) : nullptr;
    }
  }
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkTextView::vfunc_create_buffer");
  return result;
}

// vfunc installer: GTK_TEXT_VIEW_CLASS->create_buffer (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_create_buffer(gpointer klass) {
  GTK_TEXT_VIEW_CLASS(klass)->create_buffer = vfunc_thunk_create_buffer;
}

// vfunc thunk: GTK_TEXT_VIEW_CLASS->cut_clipboard -> $this->vfunc_cut_clipboard() on a PHP subclass
void vfunc_thunk_cut_clipboard(GtkTextView *self) {
  zval zself;
  zend_function *fn = EG(exception) == nullptr
                          ? subtype_vfunc(G_OBJECT(self), "vfunc_cut_clipboard", &zself)
                          : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    auto *native = GTK_TEXT_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->cut_clipboard != nullptr) native->cut_clipboard(self);
    return;
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkTextView::vfunc_cut_clipboard");
}

// vfunc installer: GTK_TEXT_VIEW_CLASS->cut_clipboard (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_cut_clipboard(gpointer klass) {
  GTK_TEXT_VIEW_CLASS(klass)->cut_clipboard = vfunc_thunk_cut_clipboard;
}

// vfunc thunk: GTK_TEXT_VIEW_CLASS->delete_from_cursor -> $this->vfunc_delete_from_cursor() on a
// PHP subclass
void vfunc_thunk_delete_from_cursor(GtkTextView *self, GtkDeleteType type, int count) {
  zval zself;
  zend_function *fn = EG(exception) == nullptr
                          ? subtype_vfunc(G_OBJECT(self), "vfunc_delete_from_cursor", &zself)
                          : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    auto *native = GTK_TEXT_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->delete_from_cursor != nullptr) native->delete_from_cursor(self, type, count);
    return;
  }
  std::array<zval, 2> args{};
  zval *argv = args.data();
  ZVAL_LONG(&argv[0], static_cast<zend_long>(type));
  ZVAL_LONG(&argv[1], static_cast<zend_long>(count));
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 2, args.data());
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkTextView::vfunc_delete_from_cursor");
}

// vfunc installer: GTK_TEXT_VIEW_CLASS->delete_from_cursor (called from class_init / iface_init of
// a PHP subtype)
void vfunc_install_delete_from_cursor(gpointer klass) {
  GTK_TEXT_VIEW_CLASS(klass)->delete_from_cursor = vfunc_thunk_delete_from_cursor;
}

// vfunc thunk: GTK_TEXT_VIEW_CLASS->extend_selection -> $this->vfunc_extend_selection() on a PHP
// subclass
gboolean vfunc_thunk_extend_selection(GtkTextView *self, GtkTextExtendSelection granularity,
                                      const GtkTextIter *location, GtkTextIter *start,
                                      GtkTextIter *end) {
  zval zself;
  zend_function *fn = EG(exception) == nullptr
                          ? subtype_vfunc(G_OBJECT(self), "vfunc_extend_selection", &zself)
                          : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    auto *native = GTK_TEXT_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->extend_selection != nullptr
               ? native->extend_selection(self, granularity, location, start, end)
               : FALSE;
  }
  std::array<zval, 4> args{};
  zval *argv = args.data();
  ZVAL_LONG(&argv[0], static_cast<zend_long>(granularity));
  wrap_boxed(GTK_TYPE_TEXT_ITER, location, &argv[1]);
  wrap_boxed(GTK_TYPE_TEXT_ITER, start, &argv[2]);
  wrap_boxed(GTK_TYPE_TEXT_ITER, end, &argv[3]);
  zval ret;
  ZVAL_UNDEF(&ret);
  gboolean result = FALSE;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 4, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = zend_is_true(&ret) ? TRUE : FALSE;
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkTextView::vfunc_extend_selection");
  return result;
}

// vfunc installer: GTK_TEXT_VIEW_CLASS->extend_selection (called from class_init / iface_init of a
// PHP subtype)
void vfunc_install_extend_selection(gpointer klass) {
  GTK_TEXT_VIEW_CLASS(klass)->extend_selection = vfunc_thunk_extend_selection;
}

// vfunc thunk: GTK_TEXT_VIEW_CLASS->insert_at_cursor -> $this->vfunc_insert_at_cursor() on a PHP
// subclass
void vfunc_thunk_insert_at_cursor(GtkTextView *self, const char *str) {
  zval zself;
  zend_function *fn = EG(exception) == nullptr
                          ? subtype_vfunc(G_OBJECT(self), "vfunc_insert_at_cursor", &zself)
                          : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    auto *native = GTK_TEXT_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->insert_at_cursor != nullptr) native->insert_at_cursor(self, str);
    return;
  }
  std::array<zval, 1> args{};
  zval *argv = args.data();
  if (str == nullptr) {
    ZVAL_NULL(&argv[0]);
  } else {
    ZVAL_STRING(&argv[0], str);
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkTextView::vfunc_insert_at_cursor");
}

// vfunc installer: GTK_TEXT_VIEW_CLASS->insert_at_cursor (called from class_init / iface_init of a
// PHP subtype)
void vfunc_install_insert_at_cursor(gpointer klass) {
  GTK_TEXT_VIEW_CLASS(klass)->insert_at_cursor = vfunc_thunk_insert_at_cursor;
}

// vfunc thunk: GTK_TEXT_VIEW_CLASS->insert_emoji -> $this->vfunc_insert_emoji() on a PHP subclass
void vfunc_thunk_insert_emoji(GtkTextView *self) {
  zval zself;
  zend_function *fn = EG(exception) == nullptr
                          ? subtype_vfunc(G_OBJECT(self), "vfunc_insert_emoji", &zself)
                          : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    auto *native = GTK_TEXT_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->insert_emoji != nullptr) native->insert_emoji(self);
    return;
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkTextView::vfunc_insert_emoji");
}

// vfunc installer: GTK_TEXT_VIEW_CLASS->insert_emoji (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_insert_emoji(gpointer klass) {
  GTK_TEXT_VIEW_CLASS(klass)->insert_emoji = vfunc_thunk_insert_emoji;
}

// vfunc thunk: GTK_TEXT_VIEW_CLASS->move_cursor -> $this->vfunc_move_cursor() on a PHP subclass
void vfunc_thunk_move_cursor(GtkTextView *self, GtkMovementStep step, int count,
                             gboolean extend_selection) {
  zval zself;
  zend_function *fn = EG(exception) == nullptr
                          ? subtype_vfunc(G_OBJECT(self), "vfunc_move_cursor", &zself)
                          : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    auto *native = GTK_TEXT_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->move_cursor != nullptr) native->move_cursor(self, step, count, extend_selection);
    return;
  }
  std::array<zval, 3> args{};
  zval *argv = args.data();
  ZVAL_LONG(&argv[0], static_cast<zend_long>(step));
  ZVAL_LONG(&argv[1], static_cast<zend_long>(count));
  ZVAL_BOOL(&argv[2], extend_selection != FALSE);
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 3, args.data());
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkTextView::vfunc_move_cursor");
}

// vfunc installer: GTK_TEXT_VIEW_CLASS->move_cursor (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_move_cursor(gpointer klass) {
  GTK_TEXT_VIEW_CLASS(klass)->move_cursor = vfunc_thunk_move_cursor;
}

// vfunc thunk: GTK_TEXT_VIEW_CLASS->paste_clipboard -> $this->vfunc_paste_clipboard() on a PHP
// subclass
void vfunc_thunk_paste_clipboard(GtkTextView *self) {
  zval zself;
  zend_function *fn = EG(exception) == nullptr
                          ? subtype_vfunc(G_OBJECT(self), "vfunc_paste_clipboard", &zself)
                          : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    auto *native = GTK_TEXT_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->paste_clipboard != nullptr) native->paste_clipboard(self);
    return;
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkTextView::vfunc_paste_clipboard");
}

// vfunc installer: GTK_TEXT_VIEW_CLASS->paste_clipboard (called from class_init / iface_init of a
// PHP subtype)
void vfunc_install_paste_clipboard(gpointer klass) {
  GTK_TEXT_VIEW_CLASS(klass)->paste_clipboard = vfunc_thunk_paste_clipboard;
}

// vfunc thunk: GTK_TEXT_VIEW_CLASS->set_anchor -> $this->vfunc_set_anchor() on a PHP subclass
void vfunc_thunk_set_anchor(GtkTextView *self) {
  zval zself;
  zend_function *fn = EG(exception) == nullptr
                          ? subtype_vfunc(G_OBJECT(self), "vfunc_set_anchor", &zself)
                          : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    auto *native = GTK_TEXT_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->set_anchor != nullptr) native->set_anchor(self);
    return;
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkTextView::vfunc_set_anchor");
}

// vfunc installer: GTK_TEXT_VIEW_CLASS->set_anchor (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_set_anchor(gpointer klass) {
  GTK_TEXT_VIEW_CLASS(klass)->set_anchor = vfunc_thunk_set_anchor;
}

// vfunc thunk: GTK_TEXT_VIEW_CLASS->snapshot_layer -> $this->vfunc_snapshot_layer() on a PHP
// subclass
void vfunc_thunk_snapshot_layer(GtkTextView *self, GtkTextViewLayer layer, GtkSnapshot *snapshot) {
  zval zself;
  zend_function *fn = EG(exception) == nullptr
                          ? subtype_vfunc(G_OBJECT(self), "vfunc_snapshot_layer", &zself)
                          : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    auto *native = GTK_TEXT_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->snapshot_layer != nullptr) native->snapshot_layer(self, layer, snapshot);
    return;
  }
  std::array<zval, 2> args{};
  zval *argv = args.data();
  ZVAL_LONG(&argv[0], static_cast<zend_long>(layer));
  wrap(snapshot != nullptr ? G_OBJECT(snapshot) : nullptr, &argv[1]);
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 2, args.data());
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkTextView::vfunc_snapshot_layer");
}

// vfunc installer: GTK_TEXT_VIEW_CLASS->snapshot_layer (called from class_init / iface_init of a
// PHP subtype)
void vfunc_install_snapshot_layer(gpointer klass) {
  GTK_TEXT_VIEW_CLASS(klass)->snapshot_layer = vfunc_thunk_snapshot_layer;
}

// vfunc thunk: GTK_TEXT_VIEW_CLASS->toggle_overwrite -> $this->vfunc_toggle_overwrite() on a PHP
// subclass
void vfunc_thunk_toggle_overwrite(GtkTextView *self) {
  zval zself;
  zend_function *fn = EG(exception) == nullptr
                          ? subtype_vfunc(G_OBJECT(self), "vfunc_toggle_overwrite", &zself)
                          : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    auto *native = GTK_TEXT_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->toggle_overwrite != nullptr) native->toggle_overwrite(self);
    return;
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkTextView::vfunc_toggle_overwrite");
}

// vfunc installer: GTK_TEXT_VIEW_CLASS->toggle_overwrite (called from class_init / iface_init of a
// PHP subtype)
void vfunc_install_toggle_overwrite(gpointer klass) {
  GTK_TEXT_VIEW_CLASS(klass)->toggle_overwrite = vfunc_thunk_toggle_overwrite;
}

}  // namespace

/**
 * Gtk4\GtkTextView::vfunc_backspace(): void
 *
 * Native `backspace` (TextViewClass.backspace): the GTK implementation below any PHP subclass, for
 * `parent::vfunc_backspace()` from an override. The class handler for the `GtkTextView::backspace`
 * keybinding signal.
 */
ZEND_METHOD(Gtk4_GtkTextView, vfunc_backspace) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkTextView::vfunc_backspace(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_TEXT_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->backspace == nullptr) {
    return;
  }
  klass->backspace(self);
}

/**
 * Gtk4\GtkTextView::vfunc_copy_clipboard(): void
 *
 * Native `copy_clipboard` (TextViewClass.copy_clipboard): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_copy_clipboard()` from an override. The class handler for the
 * `GtkTextView::copy-clipboard` keybinding signal.
 */
ZEND_METHOD(Gtk4_GtkTextView, vfunc_copy_clipboard) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkTextView::vfunc_copy_clipboard(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_TEXT_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->copy_clipboard == nullptr) {
    return;
  }
  klass->copy_clipboard(self);
}

/**
 * Gtk4\GtkTextView::vfunc_create_buffer(): GtkTextBuffer
 *
 * Native `create_buffer` (TextViewClass.create_buffer): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_create_buffer()` from an override. The create_buffer vfunc is
 * called to create a `GtkTextBuffer` for the text view. The default implementation is to just call
 * gtk_text_buffer_new().
 */
ZEND_METHOD(Gtk4_GtkTextView, vfunc_create_buffer) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkTextView::vfunc_create_buffer(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_TEXT_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->create_buffer == nullptr) {
    enum_to_php(G_TYPE_NONE, 0, return_value);
    return;
  }
  GtkTextBuffer *phpgtk_ret = klass->create_buffer(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkTextView::vfunc_cut_clipboard(): void
 *
 * Native `cut_clipboard` (TextViewClass.cut_clipboard): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_cut_clipboard()` from an override. The class handler for the
 * `GtkTextView::cut-clipboard` keybinding signal
 */
ZEND_METHOD(Gtk4_GtkTextView, vfunc_cut_clipboard) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkTextView::vfunc_cut_clipboard(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_TEXT_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->cut_clipboard == nullptr) {
    return;
  }
  klass->cut_clipboard(self);
}

/**
 * Gtk4\GtkTextView::vfunc_delete_from_cursor(int $type, int $count): void
 *
 * Native `delete_from_cursor` (TextViewClass.delete_from_cursor): the GTK implementation below any
 * PHP subclass, for `parent::vfunc_delete_from_cursor()` from an override. The class handler for
 * the `GtkTextView::delete-from-cursor` keybinding signal.
 */
ZEND_METHOD(Gtk4_GtkTextView, vfunc_delete_from_cursor) {
  zend_long type;
  zend_long count;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_LONG(type)
  Z_PARAM_LONG(count)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkTextView::vfunc_delete_from_cursor(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_TEXT_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->delete_from_cursor == nullptr) {
    return;
  }
  if (!phpgtk::check_flags(GTK_TYPE_DELETE_TYPE, type, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(count, 2)) RETURN_THROWS();
  klass->delete_from_cursor(self, static_cast<GtkDeleteType>(type), static_cast<int>(count));
}

/**
 * Gtk4\GtkTextView::vfunc_extend_selection(int $granularity, GtkTextIter $location, GtkTextIter
 * $start, GtkTextIter $end): bool
 *
 * Native `extend_selection` (TextViewClass.extend_selection): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_extend_selection()` from an override. The class handler for the
 * `GtkTextView::extend-selection` signal.
 */
ZEND_METHOD(Gtk4_GtkTextView, vfunc_extend_selection) {
  zend_long granularity;
  zval *location;
  zval *start;
  zval *end;
  ZEND_PARSE_PARAMETERS_START(4, 4)
  Z_PARAM_LONG(granularity)
  Z_PARAM_OBJECT_OF_CLASS(location, boxed_class_for_type(GTK_TYPE_TEXT_ITER)->ce)
  Z_PARAM_OBJECT_OF_CLASS(start, boxed_class_for_type(GTK_TYPE_TEXT_ITER)->ce)
  Z_PARAM_OBJECT_OF_CLASS(end, boxed_class_for_type(GTK_TYPE_TEXT_ITER)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkTextView::vfunc_extend_selection(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_TEXT_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->extend_selection == nullptr) {
    RETURN_FALSE;
  }
  if (!phpgtk::check_flags(GTK_TYPE_TEXT_EXTEND_SELECTION, granularity, 1)) RETURN_THROWS();
  gpointer location_b = unwrap_boxed(location, GTK_TYPE_TEXT_ITER);
  if (location_b == nullptr) RETURN_THROWS();
  gpointer start_b = unwrap_boxed(start, GTK_TYPE_TEXT_ITER);
  if (start_b == nullptr) RETURN_THROWS();
  gpointer end_b = unwrap_boxed(end, GTK_TYPE_TEXT_ITER);
  if (end_b == nullptr) RETURN_THROWS();
  RETURN_BOOL(klass->extend_selection(self, static_cast<GtkTextExtendSelection>(granularity),
                                      static_cast<GtkTextIter *>(location_b),
                                      static_cast<GtkTextIter *>(start_b),
                                      static_cast<GtkTextIter *>(end_b)));
}

/**
 * Gtk4\GtkTextView::vfunc_insert_at_cursor(string $str): void
 *
 * Native `insert_at_cursor` (TextViewClass.insert_at_cursor): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_insert_at_cursor()` from an override. The class handler for the
 * `GtkTextView::insert-at-cursor` keybinding signal.
 */
ZEND_METHOD(Gtk4_GtkTextView, vfunc_insert_at_cursor) {
  zend_string *str;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(str)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkTextView::vfunc_insert_at_cursor(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_TEXT_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->insert_at_cursor == nullptr) {
    return;
  }
  if (!phpgtk::check_utf8(str, 1)) RETURN_THROWS();
  klass->insert_at_cursor(self, ZSTR_VAL(str));
}

/**
 * Gtk4\GtkTextView::vfunc_insert_emoji(): void
 *
 * Native `insert_emoji` (TextViewClass.insert_emoji): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_insert_emoji()` from an override. The class handler for the
 * `GtkTextView::insert-emoji` signal.
 */
ZEND_METHOD(Gtk4_GtkTextView, vfunc_insert_emoji) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkTextView::vfunc_insert_emoji(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_TEXT_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->insert_emoji == nullptr) {
    return;
  }
  klass->insert_emoji(self);
}

/**
 * Gtk4\GtkTextView::vfunc_move_cursor(int $step, int $count, bool $extend_selection): void
 *
 * Native `move_cursor` (TextViewClass.move_cursor): the GTK implementation below any PHP subclass,
 * for `parent::vfunc_move_cursor()` from an override. The class handler for the
 * `GtkTextView::move-cursor` keybinding signal.
 */
ZEND_METHOD(Gtk4_GtkTextView, vfunc_move_cursor) {
  zend_long step;
  zend_long count;
  bool extend_selection;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_LONG(step)
  Z_PARAM_LONG(count)
  Z_PARAM_BOOL(extend_selection)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkTextView::vfunc_move_cursor(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_TEXT_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->move_cursor == nullptr) {
    return;
  }
  if (!phpgtk::check_flags(GTK_TYPE_MOVEMENT_STEP, step, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(count, 2)) RETURN_THROWS();
  klass->move_cursor(self, static_cast<GtkMovementStep>(step), static_cast<int>(count),
                     extend_selection);
}

/**
 * Gtk4\GtkTextView::vfunc_paste_clipboard(): void
 *
 * Native `paste_clipboard` (TextViewClass.paste_clipboard): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_paste_clipboard()` from an override. The class handler for the
 * `GtkTextView::paste-clipboard` keybinding signal.
 */
ZEND_METHOD(Gtk4_GtkTextView, vfunc_paste_clipboard) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkTextView::vfunc_paste_clipboard(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_TEXT_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->paste_clipboard == nullptr) {
    return;
  }
  klass->paste_clipboard(self);
}

/**
 * Gtk4\GtkTextView::vfunc_set_anchor(): void
 *
 * Native `set_anchor` (TextViewClass.set_anchor): the GTK implementation below any PHP subclass,
 * for `parent::vfunc_set_anchor()` from an override. The class handler for the
 * `GtkTextView::set-anchor` keybinding signal.
 */
ZEND_METHOD(Gtk4_GtkTextView, vfunc_set_anchor) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkTextView::vfunc_set_anchor(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_TEXT_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->set_anchor == nullptr) {
    return;
  }
  klass->set_anchor(self);
}

/**
 * Gtk4\GtkTextView::vfunc_snapshot_layer(int $layer, GtkSnapshot $snapshot): void
 *
 * Native `snapshot_layer` (TextViewClass.snapshot_layer): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_snapshot_layer()` from an override. The snapshot_layer vfunc is
 * called before and after the text view is drawing its own text. Applications can override this
 * vfunc in a subclass to draw customized content underneath or above the text. In the
 * %GTK_TEXT_VIEW_LAYER_BELOW_TEXT and %GTK_TEXT_VIEW_LAYER_ABOVE_TEXT layers the drawing is done
 * in the buffer coordinate space.
 */
ZEND_METHOD(Gtk4_GtkTextView, vfunc_snapshot_layer) {
  zend_long layer;
  zval *snapshot;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_LONG(layer)
  Z_PARAM_OBJECT_OF_CLASS(snapshot, class_for_gtype(GTK_TYPE_SNAPSHOT))
  ZEND_PARSE_PARAMETERS_END();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkTextView::vfunc_snapshot_layer(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_TEXT_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->snapshot_layer == nullptr) {
    return;
  }
  if (!phpgtk::check_flags(GTK_TYPE_TEXT_VIEW_LAYER, layer, 1)) RETURN_THROWS();
  GObject *snapshot_o = unwrap(snapshot, GTK_TYPE_SNAPSHOT);
  if (snapshot_o == nullptr) RETURN_THROWS();
  klass->snapshot_layer(self, static_cast<GtkTextViewLayer>(layer), GTK_SNAPSHOT(snapshot_o));
}

/**
 * Gtk4\GtkTextView::vfunc_toggle_overwrite(): void
 *
 * Native `toggle_overwrite` (TextViewClass.toggle_overwrite): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_toggle_overwrite()` from an override. The class handler for the
 * `GtkTextView::toggle-overwrite` keybinding signal.
 */
ZEND_METHOD(Gtk4_GtkTextView, vfunc_toggle_overwrite) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkTextView *self = PHPGTK_SELF(GtkTextView, GTK_TYPE_TEXT_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkTextView::vfunc_toggle_overwrite(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_TEXT_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->toggle_overwrite == nullptr) {
    return;
  }
  klass->toggle_overwrite(self);
}

// MINIT: the vfunc thunks of GtkTextView (core/subtype.h).
void register_vfuncs_GtkTextView() {
  register_vfunc(GTK_TYPE_TEXT_VIEW, "backspace", vfunc_install_backspace);
  register_vfunc(GTK_TYPE_TEXT_VIEW, "copy_clipboard", vfunc_install_copy_clipboard);
  register_vfunc(GTK_TYPE_TEXT_VIEW, "create_buffer", vfunc_install_create_buffer);
  register_vfunc(GTK_TYPE_TEXT_VIEW, "cut_clipboard", vfunc_install_cut_clipboard);
  register_vfunc(GTK_TYPE_TEXT_VIEW, "delete_from_cursor", vfunc_install_delete_from_cursor);
  register_vfunc(GTK_TYPE_TEXT_VIEW, "extend_selection", vfunc_install_extend_selection);
  register_vfunc(GTK_TYPE_TEXT_VIEW, "insert_at_cursor", vfunc_install_insert_at_cursor);
  register_vfunc(GTK_TYPE_TEXT_VIEW, "insert_emoji", vfunc_install_insert_emoji);
  register_vfunc(GTK_TYPE_TEXT_VIEW, "move_cursor", vfunc_install_move_cursor);
  register_vfunc(GTK_TYPE_TEXT_VIEW, "paste_clipboard", vfunc_install_paste_clipboard);
  register_vfunc(GTK_TYPE_TEXT_VIEW, "set_anchor", vfunc_install_set_anchor);
  register_vfunc(GTK_TYPE_TEXT_VIEW, "snapshot_layer", vfunc_install_snapshot_layer);
  register_vfunc(GTK_TYPE_TEXT_VIEW, "toggle_overwrite", vfunc_install_toggle_overwrite);
}
