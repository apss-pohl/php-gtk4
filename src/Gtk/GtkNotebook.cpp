// GENERATED by gen/gir.php from Gtk.Notebook - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkNotebook
#include "php_gtk4.h"
#include "core/object.h"
#include "core/enums.h"
#include "core/subtype.h"

using namespace phpgtk;

/**
 * Gtk4\GtkNotebook::__construct()
 *
 * Creates a new `GtkNotebook` widget with no pages.
 */
ZEND_METHOD(Gtk4_GtkNotebook, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gtk_notebook_new());
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkNotebook::append_page(GtkWidget $child, ?GtkWidget $tab_label): int
 *
 * Appends a page to $notebook.
 */
ZEND_METHOD(Gtk4_GtkNotebook, append_page) {
  zval *child;
  zval *tab_label = nullptr;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(tab_label, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  GObject *tab_label_o = nullptr;
  if (tab_label != nullptr) {
    tab_label_o = unwrap(tab_label, GTK_TYPE_WIDGET);
    if (tab_label_o == nullptr) RETURN_THROWS();
  }
  RETURN_LONG(static_cast<zend_long>(gtk_notebook_append_page(
      self, GTK_WIDGET(child_o), tab_label_o != nullptr ? GTK_WIDGET(tab_label_o) : nullptr)));
}

/**
 * Gtk4\GtkNotebook::append_page_menu(GtkWidget $child, ?GtkWidget $tab_label, ?GtkWidget
 * $menu_label): int
 *
 * Appends a page to $notebook, specifying the widget to use as the label in the popup menu.
 */
ZEND_METHOD(Gtk4_GtkNotebook, append_page_menu) {
  zval *child;
  zval *tab_label = nullptr;
  zval *menu_label = nullptr;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(tab_label, class_for_gtype(GTK_TYPE_WIDGET))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(menu_label, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  GObject *tab_label_o = nullptr;
  if (tab_label != nullptr) {
    tab_label_o = unwrap(tab_label, GTK_TYPE_WIDGET);
    if (tab_label_o == nullptr) RETURN_THROWS();
  }
  GObject *menu_label_o = nullptr;
  if (menu_label != nullptr) {
    menu_label_o = unwrap(menu_label, GTK_TYPE_WIDGET);
    if (menu_label_o == nullptr) RETURN_THROWS();
  }
  RETURN_LONG(static_cast<zend_long>(gtk_notebook_append_page_menu(
      self, GTK_WIDGET(child_o), tab_label_o != nullptr ? GTK_WIDGET(tab_label_o) : nullptr,
      menu_label_o != nullptr ? GTK_WIDGET(menu_label_o) : nullptr)));
}

/**
 * Gtk4\GtkNotebook::detach_tab(GtkWidget $child): void
 *
 * Removes the child from the notebook.
 */
ZEND_METHOD(Gtk4_GtkNotebook, detach_tab) {
  zval *child;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  gtk_notebook_detach_tab(self, GTK_WIDGET(child_o));
}

/**
 * Gtk4\GtkNotebook::get_action_widget(GtkPackType $pack_type): ?GtkWidget
 *
 * Gets one of the action widgets.
 */
ZEND_METHOD(Gtk4_GtkNotebook, get_action_widget) {
  zval *pack_type;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(pack_type, enum_class_for_type(GTK_TYPE_PACK_TYPE))
  ZEND_PARSE_PARAMETERS_END();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  gint pack_type_v = 0;
  if (!enum_from_php(pack_type, GTK_TYPE_PACK_TYPE, &pack_type_v)) RETURN_THROWS();
  GtkWidget *phpgtk_ret =
      gtk_notebook_get_action_widget(self, static_cast<GtkPackType>(pack_type_v));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkNotebook::get_current_page(): int
 *
 * Returns the page number of the current page.
 */
ZEND_METHOD(Gtk4_GtkNotebook, get_current_page) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  RETURN_LONG(static_cast<zend_long>(gtk_notebook_get_current_page(self)));
}

/**
 * Gtk4\GtkNotebook::get_group_name(): ?string
 *
 * Gets the current group name for $notebook.
 */
ZEND_METHOD(Gtk4_GtkNotebook, get_group_name) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  PHPGTK_RETURN_STRING_OR_NULL(gtk_notebook_get_group_name(self));
}

/**
 * Gtk4\GtkNotebook::get_menu_label(GtkWidget $child): ?GtkWidget
 *
 * Retrieves the menu label widget of the page containing $child.
 */
ZEND_METHOD(Gtk4_GtkNotebook, get_menu_label) {
  zval *child;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  GtkWidget *phpgtk_ret = gtk_notebook_get_menu_label(self, GTK_WIDGET(child_o));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkNotebook::get_menu_label_text(GtkWidget $child): ?string
 *
 * Retrieves the text of the menu label for the page containing $child.
 */
ZEND_METHOD(Gtk4_GtkNotebook, get_menu_label_text) {
  zval *child;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  PHPGTK_RETURN_STRING_OR_NULL(gtk_notebook_get_menu_label_text(self, GTK_WIDGET(child_o)));
}

/**
 * Gtk4\GtkNotebook::get_n_pages(): int
 *
 * Gets the number of pages in a notebook.
 */
ZEND_METHOD(Gtk4_GtkNotebook, get_n_pages) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  RETURN_LONG(static_cast<zend_long>(gtk_notebook_get_n_pages(self)));
}

/**
 * Gtk4\GtkNotebook::get_nth_page(int $page_num): ?GtkWidget
 *
 * Returns the child widget contained in page number $page_num.
 */
ZEND_METHOD(Gtk4_GtkNotebook, get_nth_page) {
  zend_long page_num;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(page_num)
  ZEND_PARSE_PARAMETERS_END();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  if (!phpgtk::check_range<int>(page_num, 1)) RETURN_THROWS();
  GtkWidget *phpgtk_ret = gtk_notebook_get_nth_page(self, static_cast<int>(page_num));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkNotebook::get_page(GtkWidget $child): ?GtkNotebookPage
 *
 * Returns the `GtkNotebookPage` for $child.
 */
ZEND_METHOD(Gtk4_GtkNotebook, get_page) {
  zval *child;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  GtkNotebookPage *phpgtk_ret = gtk_notebook_get_page(self, GTK_WIDGET(child_o));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkNotebook::get_pages(): GListModel
 *
 * Returns a `GListModel` that contains the pages of the notebook.
 */
ZEND_METHOD(Gtk4_GtkNotebook, get_pages) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  GListModel *phpgtk_ret = gtk_notebook_get_pages(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * Gtk4\GtkNotebook::get_scrollable(): bool
 *
 * Returns whether the tab label area has arrows for scrolling.
 */
ZEND_METHOD(Gtk4_GtkNotebook, get_scrollable) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  RETURN_BOOL(gtk_notebook_get_scrollable(self));
}

/**
 * Gtk4\GtkNotebook::get_show_border(): bool
 *
 * Returns whether a bevel will be drawn around the notebook pages.
 */
ZEND_METHOD(Gtk4_GtkNotebook, get_show_border) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  RETURN_BOOL(gtk_notebook_get_show_border(self));
}

/**
 * Gtk4\GtkNotebook::get_show_tabs(): bool
 *
 * Returns whether the tabs of the notebook are shown.
 */
ZEND_METHOD(Gtk4_GtkNotebook, get_show_tabs) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  RETURN_BOOL(gtk_notebook_get_show_tabs(self));
}

/**
 * Gtk4\GtkNotebook::get_tab_detachable(GtkWidget $child): bool
 *
 * Returns whether the tab contents can be detached from $notebook.
 */
ZEND_METHOD(Gtk4_GtkNotebook, get_tab_detachable) {
  zval *child;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  RETURN_BOOL(gtk_notebook_get_tab_detachable(self, GTK_WIDGET(child_o)));
}

/**
 * Gtk4\GtkNotebook::get_tab_label(GtkWidget $child): ?GtkWidget
 *
 * Returns the tab label widget for the page $child.
 */
ZEND_METHOD(Gtk4_GtkNotebook, get_tab_label) {
  zval *child;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  GtkWidget *phpgtk_ret = gtk_notebook_get_tab_label(self, GTK_WIDGET(child_o));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkNotebook::get_tab_label_text(GtkWidget $child): ?string
 *
 * Retrieves the text of the tab label for the page containing $child.
 */
ZEND_METHOD(Gtk4_GtkNotebook, get_tab_label_text) {
  zval *child;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  PHPGTK_RETURN_STRING_OR_NULL(gtk_notebook_get_tab_label_text(self, GTK_WIDGET(child_o)));
}

/**
 * Gtk4\GtkNotebook::get_tab_pos(): GtkPositionType
 *
 * Gets the edge at which the tabs are drawn.
 */
ZEND_METHOD(Gtk4_GtkNotebook, get_tab_pos) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  enum_to_php(GTK_TYPE_POSITION_TYPE, gtk_notebook_get_tab_pos(self), return_value);
}

/**
 * Gtk4\GtkNotebook::get_tab_reorderable(GtkWidget $child): bool
 *
 * Gets whether the tab can be reordered via drag and drop or not.
 */
ZEND_METHOD(Gtk4_GtkNotebook, get_tab_reorderable) {
  zval *child;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  RETURN_BOOL(gtk_notebook_get_tab_reorderable(self, GTK_WIDGET(child_o)));
}

/**
 * Gtk4\GtkNotebook::insert_page(GtkWidget $child, ?GtkWidget $tab_label, int $position): int
 *
 * Insert a page into $notebook at the given position.
 */
ZEND_METHOD(Gtk4_GtkNotebook, insert_page) {
  zval *child;
  zval *tab_label = nullptr;
  zend_long position;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(tab_label, class_for_gtype(GTK_TYPE_WIDGET))
  Z_PARAM_LONG(position)
  ZEND_PARSE_PARAMETERS_END();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  GObject *tab_label_o = nullptr;
  if (tab_label != nullptr) {
    tab_label_o = unwrap(tab_label, GTK_TYPE_WIDGET);
    if (tab_label_o == nullptr) RETURN_THROWS();
  }
  if (!phpgtk::check_range<int>(position, 3)) RETURN_THROWS();
  RETURN_LONG(static_cast<zend_long>(gtk_notebook_insert_page(
      self, GTK_WIDGET(child_o), tab_label_o != nullptr ? GTK_WIDGET(tab_label_o) : nullptr,
      static_cast<int>(position))));
}

/**
 * Gtk4\GtkNotebook::insert_page_menu(GtkWidget $child, ?GtkWidget $tab_label, ?GtkWidget
 * $menu_label, int $position): int
 *
 * Insert a page into $notebook at the given position, specifying the widget to use as the label in
 * the popup menu.
 */
ZEND_METHOD(Gtk4_GtkNotebook, insert_page_menu) {
  zval *child;
  zval *tab_label = nullptr;
  zval *menu_label = nullptr;
  zend_long position;
  ZEND_PARSE_PARAMETERS_START(4, 4)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(tab_label, class_for_gtype(GTK_TYPE_WIDGET))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(menu_label, class_for_gtype(GTK_TYPE_WIDGET))
  Z_PARAM_LONG(position)
  ZEND_PARSE_PARAMETERS_END();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  GObject *tab_label_o = nullptr;
  if (tab_label != nullptr) {
    tab_label_o = unwrap(tab_label, GTK_TYPE_WIDGET);
    if (tab_label_o == nullptr) RETURN_THROWS();
  }
  GObject *menu_label_o = nullptr;
  if (menu_label != nullptr) {
    menu_label_o = unwrap(menu_label, GTK_TYPE_WIDGET);
    if (menu_label_o == nullptr) RETURN_THROWS();
  }
  if (!phpgtk::check_range<int>(position, 4)) RETURN_THROWS();
  RETURN_LONG(static_cast<zend_long>(gtk_notebook_insert_page_menu(
      self, GTK_WIDGET(child_o), tab_label_o != nullptr ? GTK_WIDGET(tab_label_o) : nullptr,
      menu_label_o != nullptr ? GTK_WIDGET(menu_label_o) : nullptr, static_cast<int>(position))));
}

/**
 * Gtk4\GtkNotebook::next_page(): void
 *
 * Switches to the next page.
 */
ZEND_METHOD(Gtk4_GtkNotebook, next_page) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  gtk_notebook_next_page(self);
}

/**
 * Gtk4\GtkNotebook::page_num(GtkWidget $child): int
 *
 * Finds the index of the page which contains the given child widget.
 */
ZEND_METHOD(Gtk4_GtkNotebook, page_num) {
  zval *child;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  RETURN_LONG(static_cast<zend_long>(gtk_notebook_page_num(self, GTK_WIDGET(child_o))));
}

/**
 * Gtk4\GtkNotebook::popup_disable(): void
 *
 * Disables the popup menu.
 */
ZEND_METHOD(Gtk4_GtkNotebook, popup_disable) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  gtk_notebook_popup_disable(self);
}

/**
 * Gtk4\GtkNotebook::popup_enable(): void
 *
 * Enables the popup menu.
 */
ZEND_METHOD(Gtk4_GtkNotebook, popup_enable) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  gtk_notebook_popup_enable(self);
}

/**
 * Gtk4\GtkNotebook::prepend_page(GtkWidget $child, ?GtkWidget $tab_label): int
 *
 * Prepends a page to $notebook.
 */
ZEND_METHOD(Gtk4_GtkNotebook, prepend_page) {
  zval *child;
  zval *tab_label = nullptr;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(tab_label, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  GObject *tab_label_o = nullptr;
  if (tab_label != nullptr) {
    tab_label_o = unwrap(tab_label, GTK_TYPE_WIDGET);
    if (tab_label_o == nullptr) RETURN_THROWS();
  }
  RETURN_LONG(static_cast<zend_long>(gtk_notebook_prepend_page(
      self, GTK_WIDGET(child_o), tab_label_o != nullptr ? GTK_WIDGET(tab_label_o) : nullptr)));
}

/**
 * Gtk4\GtkNotebook::prepend_page_menu(GtkWidget $child, ?GtkWidget $tab_label, ?GtkWidget
 * $menu_label): int
 *
 * Prepends a page to $notebook, specifying the widget to use as the label in the popup menu.
 */
ZEND_METHOD(Gtk4_GtkNotebook, prepend_page_menu) {
  zval *child;
  zval *tab_label = nullptr;
  zval *menu_label = nullptr;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(tab_label, class_for_gtype(GTK_TYPE_WIDGET))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(menu_label, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  GObject *tab_label_o = nullptr;
  if (tab_label != nullptr) {
    tab_label_o = unwrap(tab_label, GTK_TYPE_WIDGET);
    if (tab_label_o == nullptr) RETURN_THROWS();
  }
  GObject *menu_label_o = nullptr;
  if (menu_label != nullptr) {
    menu_label_o = unwrap(menu_label, GTK_TYPE_WIDGET);
    if (menu_label_o == nullptr) RETURN_THROWS();
  }
  RETURN_LONG(static_cast<zend_long>(gtk_notebook_prepend_page_menu(
      self, GTK_WIDGET(child_o), tab_label_o != nullptr ? GTK_WIDGET(tab_label_o) : nullptr,
      menu_label_o != nullptr ? GTK_WIDGET(menu_label_o) : nullptr)));
}

/**
 * Gtk4\GtkNotebook::prev_page(): void
 *
 * Switches to the previous page.
 */
ZEND_METHOD(Gtk4_GtkNotebook, prev_page) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  gtk_notebook_prev_page(self);
}

/**
 * Gtk4\GtkNotebook::remove_page(int $page_num): void
 *
 * Removes a page from the notebook given its index in the notebook.
 */
ZEND_METHOD(Gtk4_GtkNotebook, remove_page) {
  zend_long page_num;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(page_num)
  ZEND_PARSE_PARAMETERS_END();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  if (!phpgtk::check_range<int>(page_num, 1)) RETURN_THROWS();
  gtk_notebook_remove_page(self, static_cast<int>(page_num));
}

/**
 * Gtk4\GtkNotebook::reorder_child(GtkWidget $child, int $position): void
 *
 * Reorders the page containing $child, so that it appears in position $position.
 */
ZEND_METHOD(Gtk4_GtkNotebook, reorder_child) {
  zval *child;
  zend_long position;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  Z_PARAM_LONG(position)
  ZEND_PARSE_PARAMETERS_END();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  if (!phpgtk::check_range<int>(position, 2)) RETURN_THROWS();
  gtk_notebook_reorder_child(self, GTK_WIDGET(child_o), static_cast<int>(position));
}

/**
 * Gtk4\GtkNotebook::set_action_widget(GtkWidget $widget, GtkPackType $pack_type): void
 *
 * Sets $widget as one of the action widgets.
 */
ZEND_METHOD(Gtk4_GtkNotebook, set_action_widget) {
  zval *widget;
  zval *pack_type;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(widget, class_for_gtype(GTK_TYPE_WIDGET))
  Z_PARAM_OBJECT_OF_CLASS(pack_type, enum_class_for_type(GTK_TYPE_PACK_TYPE))
  ZEND_PARSE_PARAMETERS_END();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  GObject *widget_o = unwrap(widget, GTK_TYPE_WIDGET);
  if (widget_o == nullptr) RETURN_THROWS();
  gint pack_type_v = 0;
  if (!enum_from_php(pack_type, GTK_TYPE_PACK_TYPE, &pack_type_v)) RETURN_THROWS();
  gtk_notebook_set_action_widget(self, GTK_WIDGET(widget_o), static_cast<GtkPackType>(pack_type_v));
}

/**
 * Gtk4\GtkNotebook::set_current_page(int $page_num): void
 *
 * Switches to the page number $page_num.
 */
ZEND_METHOD(Gtk4_GtkNotebook, set_current_page) {
  zend_long page_num;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(page_num)
  ZEND_PARSE_PARAMETERS_END();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  if (!phpgtk::check_range<int>(page_num, 1)) RETURN_THROWS();
  gtk_notebook_set_current_page(self, static_cast<int>(page_num));
}

/**
 * Gtk4\GtkNotebook::set_group_name(?string $group_name): void
 *
 * Sets a group name for $notebook.
 */
ZEND_METHOD(Gtk4_GtkNotebook, set_group_name) {
  zend_string *group_name = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR_OR_NULL(group_name)
  ZEND_PARSE_PARAMETERS_END();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  if (group_name != nullptr && !phpgtk::check_utf8(group_name, 1)) RETURN_THROWS();
  gtk_notebook_set_group_name(self, group_name != nullptr ? ZSTR_VAL(group_name) : nullptr);
}

/**
 * Gtk4\GtkNotebook::set_menu_label(GtkWidget $child, ?GtkWidget $menu_label): void
 *
 * Changes the menu label for the page containing $child.
 */
ZEND_METHOD(Gtk4_GtkNotebook, set_menu_label) {
  zval *child;
  zval *menu_label = nullptr;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(menu_label, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  GObject *menu_label_o = nullptr;
  if (menu_label != nullptr) {
    menu_label_o = unwrap(menu_label, GTK_TYPE_WIDGET);
    if (menu_label_o == nullptr) RETURN_THROWS();
  }
  gtk_notebook_set_menu_label(self, GTK_WIDGET(child_o),
                              menu_label_o != nullptr ? GTK_WIDGET(menu_label_o) : nullptr);
}

/**
 * Gtk4\GtkNotebook::set_menu_label_text(GtkWidget $child, string $menu_text): void
 *
 * Creates a new label and sets it as the menu label of $child.
 */
ZEND_METHOD(Gtk4_GtkNotebook, set_menu_label_text) {
  zval *child;
  zend_string *menu_text;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  Z_PARAM_STR(menu_text)
  ZEND_PARSE_PARAMETERS_END();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  if (!phpgtk::check_utf8(menu_text, 2)) RETURN_THROWS();
  gtk_notebook_set_menu_label_text(self, GTK_WIDGET(child_o), ZSTR_VAL(menu_text));
}

/**
 * Gtk4\GtkNotebook::set_scrollable(bool $scrollable): void
 *
 * Sets whether the tab label area will have arrows for scrolling if there are too many tabs to fit
 * in the area.
 */
ZEND_METHOD(Gtk4_GtkNotebook, set_scrollable) {
  bool scrollable;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(scrollable)
  ZEND_PARSE_PARAMETERS_END();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  gtk_notebook_set_scrollable(self, scrollable);
}

/**
 * Gtk4\GtkNotebook::set_show_border(bool $show_border): void
 *
 * Sets whether a bevel will be drawn around the notebook pages.
 */
ZEND_METHOD(Gtk4_GtkNotebook, set_show_border) {
  bool show_border;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(show_border)
  ZEND_PARSE_PARAMETERS_END();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  gtk_notebook_set_show_border(self, show_border);
}

/**
 * Gtk4\GtkNotebook::set_show_tabs(bool $show_tabs): void
 *
 * Sets whether to show the tabs for the notebook or not.
 */
ZEND_METHOD(Gtk4_GtkNotebook, set_show_tabs) {
  bool show_tabs;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(show_tabs)
  ZEND_PARSE_PARAMETERS_END();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  gtk_notebook_set_show_tabs(self, show_tabs);
}

/**
 * Gtk4\GtkNotebook::set_tab_detachable(GtkWidget $child, bool $detachable): void
 *
 * Sets whether the tab can be detached from $notebook to another notebook or widget.
 */
ZEND_METHOD(Gtk4_GtkNotebook, set_tab_detachable) {
  zval *child;
  bool detachable;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  Z_PARAM_BOOL(detachable)
  ZEND_PARSE_PARAMETERS_END();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  gtk_notebook_set_tab_detachable(self, GTK_WIDGET(child_o), detachable);
}

/**
 * Gtk4\GtkNotebook::set_tab_label(GtkWidget $child, ?GtkWidget $tab_label): void
 *
 * Changes the tab label for $child.
 */
ZEND_METHOD(Gtk4_GtkNotebook, set_tab_label) {
  zval *child;
  zval *tab_label = nullptr;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(tab_label, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  GObject *tab_label_o = nullptr;
  if (tab_label != nullptr) {
    tab_label_o = unwrap(tab_label, GTK_TYPE_WIDGET);
    if (tab_label_o == nullptr) RETURN_THROWS();
  }
  gtk_notebook_set_tab_label(self, GTK_WIDGET(child_o),
                             tab_label_o != nullptr ? GTK_WIDGET(tab_label_o) : nullptr);
}

/**
 * Gtk4\GtkNotebook::set_tab_label_text(GtkWidget $child, string $tab_text): void
 *
 * Creates a new label and sets it as the tab label for the page containing $child.
 */
ZEND_METHOD(Gtk4_GtkNotebook, set_tab_label_text) {
  zval *child;
  zend_string *tab_text;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  Z_PARAM_STR(tab_text)
  ZEND_PARSE_PARAMETERS_END();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  if (!phpgtk::check_utf8(tab_text, 2)) RETURN_THROWS();
  gtk_notebook_set_tab_label_text(self, GTK_WIDGET(child_o), ZSTR_VAL(tab_text));
}

/**
 * Gtk4\GtkNotebook::set_tab_pos(GtkPositionType $pos): void
 *
 * Sets the edge at which the tabs are drawn.
 */
ZEND_METHOD(Gtk4_GtkNotebook, set_tab_pos) {
  zval *pos;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(pos, enum_class_for_type(GTK_TYPE_POSITION_TYPE))
  ZEND_PARSE_PARAMETERS_END();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  gint pos_v = 0;
  if (!enum_from_php(pos, GTK_TYPE_POSITION_TYPE, &pos_v)) RETURN_THROWS();
  gtk_notebook_set_tab_pos(self, static_cast<GtkPositionType>(pos_v));
}

/**
 * Gtk4\GtkNotebook::set_tab_reorderable(GtkWidget $child, bool $reorderable): void
 *
 * Sets whether the notebook tab can be reordered via drag and drop or not.
 */
ZEND_METHOD(Gtk4_GtkNotebook, set_tab_reorderable) {
  zval *child;
  bool reorderable;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  Z_PARAM_BOOL(reorderable)
  ZEND_PARSE_PARAMETERS_END();
  GtkNotebook *self = PHPGTK_SELF(GtkNotebook, GTK_TYPE_NOTEBOOK);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  gtk_notebook_set_tab_reorderable(self, GTK_WIDGET(child_o), reorderable);
}
