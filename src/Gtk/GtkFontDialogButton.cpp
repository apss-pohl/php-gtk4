// GENERATED by gen/gir.php from Gtk.FontDialogButton - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkFontDialogButton
#include "php_gtk4.h"
#include "core/object.h"
#include "core/enums.h"
#include "core/boxed.h"
#include "core/subtype.h"

using namespace phpgtk;

/**
 * Gtk4\GtkFontDialogButton::__construct(?GtkFontDialog $dialog = null)
 *
 * Creates a new `GtkFontDialogButton` with the given `GtkFontDialog`.
 */
ZEND_METHOD(Gtk4_GtkFontDialogButton, __construct) {
  zval *dialog = nullptr;
  ZEND_PARSE_PARAMETERS_START(0, 1)
  Z_PARAM_OPTIONAL
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(dialog, class_for_gtype(GTK_TYPE_FONT_DIALOG))
  ZEND_PARSE_PARAMETERS_END();
  GObject *dialog_o = nullptr;
  if (dialog != nullptr) {
    dialog_o = unwrap(dialog, GTK_TYPE_FONT_DIALOG);
    if (dialog_o == nullptr) RETURN_THROWS();
  }
  if (dialog_o != nullptr) g_object_ref(dialog_o);  // transfer full
  GObject *obj = subtype_new(ZEND_THIS, "dialog",
                             dialog_o != nullptr ? GTK_FONT_DIALOG(dialog_o) : nullptr, nullptr);
  if (obj != nullptr) {
    if (dialog_o != nullptr) g_object_unref(dialog_o);
  }
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(
        gtk_font_dialog_button_new(dialog_o != nullptr ? GTK_FONT_DIALOG(dialog_o) : nullptr));
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkFontDialogButton::get_dialog(): ?GtkFontDialog
 *
 * Returns the `GtkFontDialog` of $self.
 */
ZEND_METHOD(Gtk4_GtkFontDialogButton, get_dialog) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFontDialogButton *self = PHPGTK_SELF(GtkFontDialogButton, GTK_TYPE_FONT_DIALOG_BUTTON);
  GtkFontDialog *phpgtk_ret = gtk_font_dialog_button_get_dialog(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkFontDialogButton::get_font_desc(): ?PangoFontDescription
 *
 * Returns the font of the button.
 */
ZEND_METHOD(Gtk4_GtkFontDialogButton, get_font_desc) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFontDialogButton *self = PHPGTK_SELF(GtkFontDialogButton, GTK_TYPE_FONT_DIALOG_BUTTON);
  PangoFontDescription *phpgtk_ret = gtk_font_dialog_button_get_font_desc(self);
  wrap_boxed(PANGO_TYPE_FONT_DESCRIPTION, phpgtk_ret, return_value);
}

/**
 * Gtk4\GtkFontDialogButton::get_font_features(): ?string
 *
 * Returns the font features of the button.
 */
ZEND_METHOD(Gtk4_GtkFontDialogButton, get_font_features) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFontDialogButton *self = PHPGTK_SELF(GtkFontDialogButton, GTK_TYPE_FONT_DIALOG_BUTTON);
  PHPGTK_RETURN_STRING_OR_NULL(gtk_font_dialog_button_get_font_features(self));
}

/**
 * Gtk4\GtkFontDialogButton::get_level(): GtkFontLevel
 *
 * Returns the level of detail at which this dialog lets the user select fonts.
 */
ZEND_METHOD(Gtk4_GtkFontDialogButton, get_level) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFontDialogButton *self = PHPGTK_SELF(GtkFontDialogButton, GTK_TYPE_FONT_DIALOG_BUTTON);
  enum_to_php(GTK_TYPE_FONT_LEVEL, gtk_font_dialog_button_get_level(self), return_value);
}

/**
 * Gtk4\GtkFontDialogButton::get_use_font(): bool
 *
 * Returns whether the selected font is used in the label.
 */
ZEND_METHOD(Gtk4_GtkFontDialogButton, get_use_font) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFontDialogButton *self = PHPGTK_SELF(GtkFontDialogButton, GTK_TYPE_FONT_DIALOG_BUTTON);
  RETURN_BOOL(gtk_font_dialog_button_get_use_font(self));
}

/**
 * Gtk4\GtkFontDialogButton::get_use_size(): bool
 *
 * Returns whether the selected font size is used in the label.
 */
ZEND_METHOD(Gtk4_GtkFontDialogButton, get_use_size) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFontDialogButton *self = PHPGTK_SELF(GtkFontDialogButton, GTK_TYPE_FONT_DIALOG_BUTTON);
  RETURN_BOOL(gtk_font_dialog_button_get_use_size(self));
}

/**
 * Gtk4\GtkFontDialogButton::set_dialog(GtkFontDialog $dialog): void
 *
 * Sets a `GtkFontDialog` object to use for creating the font chooser dialog that is presented when
 * the user clicks the button.
 */
ZEND_METHOD(Gtk4_GtkFontDialogButton, set_dialog) {
  zval *dialog;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(dialog, class_for_gtype(GTK_TYPE_FONT_DIALOG))
  ZEND_PARSE_PARAMETERS_END();
  GtkFontDialogButton *self = PHPGTK_SELF(GtkFontDialogButton, GTK_TYPE_FONT_DIALOG_BUTTON);
  GObject *dialog_o = unwrap(dialog, GTK_TYPE_FONT_DIALOG);
  if (dialog_o == nullptr) RETURN_THROWS();
  gtk_font_dialog_button_set_dialog(self, GTK_FONT_DIALOG(dialog_o));
}

/**
 * Gtk4\GtkFontDialogButton::set_font_desc(PangoFontDescription $font_desc): void
 *
 * Sets the font of the button.
 */
ZEND_METHOD(Gtk4_GtkFontDialogButton, set_font_desc) {
  zval *font_desc;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(font_desc, boxed_class_for_type(PANGO_TYPE_FONT_DESCRIPTION)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkFontDialogButton *self = PHPGTK_SELF(GtkFontDialogButton, GTK_TYPE_FONT_DIALOG_BUTTON);
  gpointer font_desc_b = unwrap_boxed(font_desc, PANGO_TYPE_FONT_DESCRIPTION);
  if (font_desc_b == nullptr) RETURN_THROWS();
  gtk_font_dialog_button_set_font_desc(self, static_cast<PangoFontDescription *>(font_desc_b));
}

/**
 * Gtk4\GtkFontDialogButton::set_font_features(?string $font_features): void
 *
 * Sets the font features of the button.
 */
ZEND_METHOD(Gtk4_GtkFontDialogButton, set_font_features) {
  zend_string *font_features = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR_OR_NULL(font_features)
  ZEND_PARSE_PARAMETERS_END();
  GtkFontDialogButton *self = PHPGTK_SELF(GtkFontDialogButton, GTK_TYPE_FONT_DIALOG_BUTTON);
  if (font_features != nullptr && !phpgtk::check_utf8(font_features, 1)) RETURN_THROWS();
  gtk_font_dialog_button_set_font_features(
      self, font_features != nullptr ? ZSTR_VAL(font_features) : nullptr);
}

/**
 * Gtk4\GtkFontDialogButton::set_level(GtkFontLevel $level): void
 *
 * Sets the level of detail at which this dialog lets the user select fonts.
 */
ZEND_METHOD(Gtk4_GtkFontDialogButton, set_level) {
  zval *level;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(level, enum_class_for_type(GTK_TYPE_FONT_LEVEL))
  ZEND_PARSE_PARAMETERS_END();
  GtkFontDialogButton *self = PHPGTK_SELF(GtkFontDialogButton, GTK_TYPE_FONT_DIALOG_BUTTON);
  gint level_v = 0;
  if (!enum_from_php(level, GTK_TYPE_FONT_LEVEL, &level_v)) RETURN_THROWS();
  gtk_font_dialog_button_set_level(self, static_cast<GtkFontLevel>(level_v));
}

/**
 * Gtk4\GtkFontDialogButton::set_use_font(bool $use_font): void
 *
 * If $use_font is `TRUE`, the font name will be written using the selected font.
 */
ZEND_METHOD(Gtk4_GtkFontDialogButton, set_use_font) {
  bool use_font;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(use_font)
  ZEND_PARSE_PARAMETERS_END();
  GtkFontDialogButton *self = PHPGTK_SELF(GtkFontDialogButton, GTK_TYPE_FONT_DIALOG_BUTTON);
  gtk_font_dialog_button_set_use_font(self, use_font);
}

/**
 * Gtk4\GtkFontDialogButton::set_use_size(bool $use_size): void
 *
 * If $use_size is `TRUE`, the font name will be written using the selected font size.
 */
ZEND_METHOD(Gtk4_GtkFontDialogButton, set_use_size) {
  bool use_size;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(use_size)
  ZEND_PARSE_PARAMETERS_END();
  GtkFontDialogButton *self = PHPGTK_SELF(GtkFontDialogButton, GTK_TYPE_FONT_DIALOG_BUTTON);
  gtk_font_dialog_button_set_use_size(self, use_size);
}
