// GENERATED by gen/gir.php from Gtk.FileDialog - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkFileDialog
#include "php_gtk4.h"
#include "core/object.h"
#include "core/gerror.h"
#include "core/subtype.h"
#include "core/callback.h"
#include <array>

using namespace phpgtk;

/**
 * Gtk4\GtkFileDialog::__construct()
 *
 * Creates a new `GtkFileDialog` object.
 */
ZEND_METHOD(Gtk4_GtkFileDialog, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gtk_file_dialog_new());
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkFileDialog::get_accept_label(): ?string
 */
ZEND_METHOD(Gtk4_GtkFileDialog, get_accept_label) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFileDialog *self = PHPGTK_SELF(GtkFileDialog, GTK_TYPE_FILE_DIALOG);
  PHPGTK_RETURN_STRING_OR_NULL(gtk_file_dialog_get_accept_label(self));
}

/**
 * Gtk4\GtkFileDialog::get_default_filter(): ?GtkFileFilter
 *
 * Gets the filter that will be selected by default in the file chooser dialog.
 */
ZEND_METHOD(Gtk4_GtkFileDialog, get_default_filter) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFileDialog *self = PHPGTK_SELF(GtkFileDialog, GTK_TYPE_FILE_DIALOG);
  GtkFileFilter *phpgtk_ret = gtk_file_dialog_get_default_filter(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkFileDialog::get_filters(): ?GListModel
 *
 * Gets the filters that will be offered to the user in the file chooser dialog.
 */
ZEND_METHOD(Gtk4_GtkFileDialog, get_filters) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFileDialog *self = PHPGTK_SELF(GtkFileDialog, GTK_TYPE_FILE_DIALOG);
  GListModel *phpgtk_ret = gtk_file_dialog_get_filters(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkFileDialog::get_initial_file(): ?string
 *
 * Gets the file that will be initially selected in the file chooser dialog.
 */
ZEND_METHOD(Gtk4_GtkFileDialog, get_initial_file) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFileDialog *self = PHPGTK_SELF(GtkFileDialog, GTK_TYPE_FILE_DIALOG);
  GFile *file = gtk_file_dialog_get_initial_file(self);
  if (file == nullptr) RETURN_NULL();
  char *file_s = g_file_get_path(file);
  if (file_s == nullptr) file_s = g_file_get_uri(file);
  if (file_s == nullptr) RETURN_EMPTY_STRING();
  RETVAL_STRING(file_s);
  g_free(file_s);
}

/**
 * Gtk4\GtkFileDialog::get_initial_folder(): ?string
 *
 * Gets the folder that will be set as the initial folder in the file chooser dialog.
 */
ZEND_METHOD(Gtk4_GtkFileDialog, get_initial_folder) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFileDialog *self = PHPGTK_SELF(GtkFileDialog, GTK_TYPE_FILE_DIALOG);
  GFile *file = gtk_file_dialog_get_initial_folder(self);
  if (file == nullptr) RETURN_NULL();
  char *file_s = g_file_get_path(file);
  if (file_s == nullptr) file_s = g_file_get_uri(file);
  if (file_s == nullptr) RETURN_EMPTY_STRING();
  RETVAL_STRING(file_s);
  g_free(file_s);
}

/**
 * Gtk4\GtkFileDialog::get_initial_name(): ?string
 *
 * Gets the name for the file that should be initially set.
 */
ZEND_METHOD(Gtk4_GtkFileDialog, get_initial_name) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFileDialog *self = PHPGTK_SELF(GtkFileDialog, GTK_TYPE_FILE_DIALOG);
  PHPGTK_RETURN_STRING_OR_NULL(gtk_file_dialog_get_initial_name(self));
}

/**
 * Gtk4\GtkFileDialog::get_modal(): bool
 *
 * Returns whether the file chooser dialog blocks interaction with the parent window while it is
 * presented.
 */
ZEND_METHOD(Gtk4_GtkFileDialog, get_modal) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFileDialog *self = PHPGTK_SELF(GtkFileDialog, GTK_TYPE_FILE_DIALOG);
  RETURN_BOOL(gtk_file_dialog_get_modal(self));
}

/**
 * Gtk4\GtkFileDialog::get_title(): string
 *
 * Returns the title that will be shown on the file chooser dialog.
 */
ZEND_METHOD(Gtk4_GtkFileDialog, get_title) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFileDialog *self = PHPGTK_SELF(GtkFileDialog, GTK_TYPE_FILE_DIALOG);
  const char *phpgtk_ret = gtk_file_dialog_get_title(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

namespace {
// AsyncReadyCallback trampoline for GtkFileDialog::open(): wraps the C arguments, invokes the PHP
// callable once and releases it (async scope).
void cb_open_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\GtkFileDialog::open(?GtkWindow $parent, ?GCancellable $cancellable, ?callable $callback):
 * void
 *
 * This function initiates a file selection operation by presenting a file chooser dialog to the
 * user.
 */
ZEND_METHOD(Gtk4_GtkFileDialog, open) {
  zval *parent = nullptr;
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(parent, class_for_gtype(GTK_TYPE_WINDOW))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC_OR_NULL(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  GtkFileDialog *self = PHPGTK_SELF(GtkFileDialog, GTK_TYPE_FILE_DIALOG);
  GObject *parent_o = nullptr;
  if (parent != nullptr) {
    parent_o = unwrap(parent, GTK_TYPE_WINDOW);
    if (parent_o == nullptr) RETURN_THROWS();
  }
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback = ZEND_FCI_INITIALIZED(fci_callback)
                              ? callback_new(&fci_callback.function_name, "GtkFileDialog::open")
                              : nullptr;
  gtk_file_dialog_open(self, parent_o != nullptr ? GTK_WINDOW(parent_o) : nullptr,
                       cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
                       cb_callback != nullptr ? cb_open_callback : nullptr, cb_callback);
}

/**
 * Gtk4\GtkFileDialog::open_finish(GAsyncResult $result): ?string
 *
 * Finishes the `open` call and returns the resulting file.
 */
ZEND_METHOD(Gtk4_GtkFileDialog, open_finish) {
  zval *result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  GtkFileDialog *self = PHPGTK_SELF(GtkFileDialog, GTK_TYPE_FILE_DIALOG);
  GObject *result_o = unwrap(result, G_TYPE_ASYNC_RESULT);
  if (result_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  GFile *file = gtk_file_dialog_open_finish(self, G_ASYNC_RESULT(result_o), &error);
  if (file == nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  if (file == nullptr) RETURN_NULL();
  char *file_s = g_file_get_path(file);
  if (file_s == nullptr) file_s = g_file_get_uri(file);
  g_object_unref(file);
  if (file_s == nullptr) RETURN_EMPTY_STRING();
  RETVAL_STRING(file_s);
  g_free(file_s);
}

namespace {
// AsyncReadyCallback trampoline for GtkFileDialog::open_multiple(): wraps the C arguments, invokes
// the PHP callable once and releases it (async scope).
void cb_open_multiple_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\GtkFileDialog::open_multiple(?GtkWindow $parent, ?GCancellable $cancellable, ?callable
 * $callback): void
 *
 * This function initiates a multi-file selection operation by presenting a file chooser dialog to
 * the user.
 */
ZEND_METHOD(Gtk4_GtkFileDialog, open_multiple) {
  zval *parent = nullptr;
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(parent, class_for_gtype(GTK_TYPE_WINDOW))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC_OR_NULL(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  GtkFileDialog *self = PHPGTK_SELF(GtkFileDialog, GTK_TYPE_FILE_DIALOG);
  GObject *parent_o = nullptr;
  if (parent != nullptr) {
    parent_o = unwrap(parent, GTK_TYPE_WINDOW);
    if (parent_o == nullptr) RETURN_THROWS();
  }
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback =
      ZEND_FCI_INITIALIZED(fci_callback)
          ? callback_new(&fci_callback.function_name, "GtkFileDialog::open_multiple")
          : nullptr;
  gtk_file_dialog_open_multiple(self, parent_o != nullptr ? GTK_WINDOW(parent_o) : nullptr,
                                cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
                                cb_callback != nullptr ? cb_open_multiple_callback : nullptr,
                                cb_callback);
}

/**
 * Gtk4\GtkFileDialog::open_multiple_finish(GAsyncResult $result): ?GListModel
 *
 * Finishes the `open` call and returns the resulting files in a `GListModel`.
 */
ZEND_METHOD(Gtk4_GtkFileDialog, open_multiple_finish) {
  zval *result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  GtkFileDialog *self = PHPGTK_SELF(GtkFileDialog, GTK_TYPE_FILE_DIALOG);
  GObject *result_o = unwrap(result, G_TYPE_ASYNC_RESULT);
  if (result_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  GListModel *phpgtk_ret =
      gtk_file_dialog_open_multiple_finish(self, G_ASYNC_RESULT(result_o), &error);
  if (phpgtk_ret == nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

namespace {
// AsyncReadyCallback trampoline for GtkFileDialog::save(): wraps the C arguments, invokes the PHP
// callable once and releases it (async scope).
void cb_save_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\GtkFileDialog::save(?GtkWindow $parent, ?GCancellable $cancellable, ?callable $callback):
 * void
 *
 * This function initiates a file save operation by presenting a file chooser dialog to the user.
 */
ZEND_METHOD(Gtk4_GtkFileDialog, save) {
  zval *parent = nullptr;
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(parent, class_for_gtype(GTK_TYPE_WINDOW))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC_OR_NULL(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  GtkFileDialog *self = PHPGTK_SELF(GtkFileDialog, GTK_TYPE_FILE_DIALOG);
  GObject *parent_o = nullptr;
  if (parent != nullptr) {
    parent_o = unwrap(parent, GTK_TYPE_WINDOW);
    if (parent_o == nullptr) RETURN_THROWS();
  }
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback = ZEND_FCI_INITIALIZED(fci_callback)
                              ? callback_new(&fci_callback.function_name, "GtkFileDialog::save")
                              : nullptr;
  gtk_file_dialog_save(self, parent_o != nullptr ? GTK_WINDOW(parent_o) : nullptr,
                       cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
                       cb_callback != nullptr ? cb_save_callback : nullptr, cb_callback);
}

/**
 * Gtk4\GtkFileDialog::save_finish(GAsyncResult $result): ?string
 *
 * Finishes the `save` call and returns the resulting file.
 */
ZEND_METHOD(Gtk4_GtkFileDialog, save_finish) {
  zval *result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  GtkFileDialog *self = PHPGTK_SELF(GtkFileDialog, GTK_TYPE_FILE_DIALOG);
  GObject *result_o = unwrap(result, G_TYPE_ASYNC_RESULT);
  if (result_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  GFile *file = gtk_file_dialog_save_finish(self, G_ASYNC_RESULT(result_o), &error);
  if (file == nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  if (file == nullptr) RETURN_NULL();
  char *file_s = g_file_get_path(file);
  if (file_s == nullptr) file_s = g_file_get_uri(file);
  g_object_unref(file);
  if (file_s == nullptr) RETURN_EMPTY_STRING();
  RETVAL_STRING(file_s);
  g_free(file_s);
}

namespace {
// AsyncReadyCallback trampoline for GtkFileDialog::select_folder(): wraps the C arguments, invokes
// the PHP callable once and releases it (async scope).
void cb_select_folder_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\GtkFileDialog::select_folder(?GtkWindow $parent, ?GCancellable $cancellable, ?callable
 * $callback): void
 *
 * This function initiates a directory selection operation by presenting a file chooser dialog to
 * the user.
 */
ZEND_METHOD(Gtk4_GtkFileDialog, select_folder) {
  zval *parent = nullptr;
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(parent, class_for_gtype(GTK_TYPE_WINDOW))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC_OR_NULL(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  GtkFileDialog *self = PHPGTK_SELF(GtkFileDialog, GTK_TYPE_FILE_DIALOG);
  GObject *parent_o = nullptr;
  if (parent != nullptr) {
    parent_o = unwrap(parent, GTK_TYPE_WINDOW);
    if (parent_o == nullptr) RETURN_THROWS();
  }
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback =
      ZEND_FCI_INITIALIZED(fci_callback)
          ? callback_new(&fci_callback.function_name, "GtkFileDialog::select_folder")
          : nullptr;
  gtk_file_dialog_select_folder(self, parent_o != nullptr ? GTK_WINDOW(parent_o) : nullptr,
                                cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
                                cb_callback != nullptr ? cb_select_folder_callback : nullptr,
                                cb_callback);
}

/**
 * Gtk4\GtkFileDialog::select_folder_finish(GAsyncResult $result): ?string
 *
 * Finishes the `select_folder` call and returns the resulting file.
 */
ZEND_METHOD(Gtk4_GtkFileDialog, select_folder_finish) {
  zval *result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  GtkFileDialog *self = PHPGTK_SELF(GtkFileDialog, GTK_TYPE_FILE_DIALOG);
  GObject *result_o = unwrap(result, G_TYPE_ASYNC_RESULT);
  if (result_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  GFile *file = gtk_file_dialog_select_folder_finish(self, G_ASYNC_RESULT(result_o), &error);
  if (file == nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  if (file == nullptr) RETURN_NULL();
  char *file_s = g_file_get_path(file);
  if (file_s == nullptr) file_s = g_file_get_uri(file);
  g_object_unref(file);
  if (file_s == nullptr) RETURN_EMPTY_STRING();
  RETVAL_STRING(file_s);
  g_free(file_s);
}

namespace {
// AsyncReadyCallback trampoline for GtkFileDialog::select_multiple_folders(): wraps the C
// arguments, invokes the PHP callable once and releases it (async scope).
void cb_select_multiple_folders_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\GtkFileDialog::select_multiple_folders(?GtkWindow $parent, ?GCancellable $cancellable,
 * ?callable $callback): void
 *
 * This function initiates a multi-directory selection operation by presenting a file chooser
 * dialog to the user.
 */
ZEND_METHOD(Gtk4_GtkFileDialog, select_multiple_folders) {
  zval *parent = nullptr;
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(parent, class_for_gtype(GTK_TYPE_WINDOW))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC_OR_NULL(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  GtkFileDialog *self = PHPGTK_SELF(GtkFileDialog, GTK_TYPE_FILE_DIALOG);
  GObject *parent_o = nullptr;
  if (parent != nullptr) {
    parent_o = unwrap(parent, GTK_TYPE_WINDOW);
    if (parent_o == nullptr) RETURN_THROWS();
  }
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback =
      ZEND_FCI_INITIALIZED(fci_callback)
          ? callback_new(&fci_callback.function_name, "GtkFileDialog::select_multiple_folders")
          : nullptr;
  gtk_file_dialog_select_multiple_folders(
      self, parent_o != nullptr ? GTK_WINDOW(parent_o) : nullptr,
      cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
      cb_callback != nullptr ? cb_select_multiple_folders_callback : nullptr, cb_callback);
}

/**
 * Gtk4\GtkFileDialog::select_multiple_folders_finish(GAsyncResult $result): ?GListModel
 *
 * Finishes the `select_multiple_folders` call and returns the resulting files in a `GListModel`.
 */
ZEND_METHOD(Gtk4_GtkFileDialog, select_multiple_folders_finish) {
  zval *result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  GtkFileDialog *self = PHPGTK_SELF(GtkFileDialog, GTK_TYPE_FILE_DIALOG);
  GObject *result_o = unwrap(result, G_TYPE_ASYNC_RESULT);
  if (result_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  GListModel *phpgtk_ret =
      gtk_file_dialog_select_multiple_folders_finish(self, G_ASYNC_RESULT(result_o), &error);
  if (phpgtk_ret == nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * Gtk4\GtkFileDialog::set_accept_label(?string $accept_label): void
 *
 * Sets the label shown on the file chooser's accept button.
 */
ZEND_METHOD(Gtk4_GtkFileDialog, set_accept_label) {
  zend_string *accept_label = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR_OR_NULL(accept_label)
  ZEND_PARSE_PARAMETERS_END();
  GtkFileDialog *self = PHPGTK_SELF(GtkFileDialog, GTK_TYPE_FILE_DIALOG);
  if (accept_label != nullptr && !phpgtk::check_utf8(accept_label, 1)) RETURN_THROWS();
  gtk_file_dialog_set_accept_label(self,
                                   accept_label != nullptr ? ZSTR_VAL(accept_label) : nullptr);
}

/**
 * Gtk4\GtkFileDialog::set_default_filter(?GtkFileFilter $filter): void
 *
 * Sets the filter that will be selected by default in the file chooser dialog.
 */
ZEND_METHOD(Gtk4_GtkFileDialog, set_default_filter) {
  zval *filter = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(filter, class_for_gtype(GTK_TYPE_FILE_FILTER))
  ZEND_PARSE_PARAMETERS_END();
  GtkFileDialog *self = PHPGTK_SELF(GtkFileDialog, GTK_TYPE_FILE_DIALOG);
  GObject *filter_o = nullptr;
  if (filter != nullptr) {
    filter_o = unwrap(filter, GTK_TYPE_FILE_FILTER);
    if (filter_o == nullptr) RETURN_THROWS();
  }
  gtk_file_dialog_set_default_filter(self,
                                     filter_o != nullptr ? GTK_FILE_FILTER(filter_o) : nullptr);
}

/**
 * Gtk4\GtkFileDialog::set_filters(?GListModel $filters): void
 *
 * Sets the filters that will be offered to the user in the file chooser dialog.
 */
ZEND_METHOD(Gtk4_GtkFileDialog, set_filters) {
  zval *filters = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(filters, class_for_gtype(G_TYPE_LIST_MODEL))
  ZEND_PARSE_PARAMETERS_END();
  GtkFileDialog *self = PHPGTK_SELF(GtkFileDialog, GTK_TYPE_FILE_DIALOG);
  GObject *filters_o = nullptr;
  if (filters != nullptr) {
    filters_o = unwrap(filters, G_TYPE_LIST_MODEL);
    if (filters_o == nullptr) RETURN_THROWS();
  }
  gtk_file_dialog_set_filters(self, filters_o != nullptr ? G_LIST_MODEL(filters_o) : nullptr);
}

/**
 * Gtk4\GtkFileDialog::set_initial_file(?string $file): void
 *
 * Sets the file that will be initially selected in the file chooser dialog.
 */
ZEND_METHOD(Gtk4_GtkFileDialog, set_initial_file) {
  zend_string *file = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_PATH_STR_OR_NULL(file)
  ZEND_PARSE_PARAMETERS_END();
  GtkFileDialog *self = PHPGTK_SELF(GtkFileDialog, GTK_TYPE_FILE_DIALOG);
  GFile *file_f = file != nullptr ? g_file_new_for_commandline_arg(ZSTR_VAL(file)) : nullptr;
  gtk_file_dialog_set_initial_file(self, file_f);
  if (file_f != nullptr) g_object_unref(file_f);
}

/**
 * Gtk4\GtkFileDialog::set_initial_folder(?string $folder): void
 *
 * Sets the folder that will be set as the initial folder in the file chooser dialog.
 */
ZEND_METHOD(Gtk4_GtkFileDialog, set_initial_folder) {
  zend_string *folder = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_PATH_STR_OR_NULL(folder)
  ZEND_PARSE_PARAMETERS_END();
  GtkFileDialog *self = PHPGTK_SELF(GtkFileDialog, GTK_TYPE_FILE_DIALOG);
  GFile *folder_f = folder != nullptr ? g_file_new_for_commandline_arg(ZSTR_VAL(folder)) : nullptr;
  gtk_file_dialog_set_initial_folder(self, folder_f);
  if (folder_f != nullptr) g_object_unref(folder_f);
}

/**
 * Gtk4\GtkFileDialog::set_initial_name(?string $name): void
 *
 * Sets the name for the file that should be initially set. For saving dialogs, this will usually
 * be pre-entered into the name field.
 */
ZEND_METHOD(Gtk4_GtkFileDialog, set_initial_name) {
  zend_string *name = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR_OR_NULL(name)
  ZEND_PARSE_PARAMETERS_END();
  GtkFileDialog *self = PHPGTK_SELF(GtkFileDialog, GTK_TYPE_FILE_DIALOG);
  if (name != nullptr && !phpgtk::check_utf8(name, 1)) RETURN_THROWS();
  gtk_file_dialog_set_initial_name(self, name != nullptr ? ZSTR_VAL(name) : nullptr);
}

/**
 * Gtk4\GtkFileDialog::set_modal(bool $modal): void
 *
 * Sets whether the file chooser dialog blocks interaction with the parent window while it is
 * presented.
 */
ZEND_METHOD(Gtk4_GtkFileDialog, set_modal) {
  bool modal;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(modal)
  ZEND_PARSE_PARAMETERS_END();
  GtkFileDialog *self = PHPGTK_SELF(GtkFileDialog, GTK_TYPE_FILE_DIALOG);
  gtk_file_dialog_set_modal(self, modal);
}

/**
 * Gtk4\GtkFileDialog::set_title(string $title): void
 *
 * Sets the title that will be shown on the file chooser dialog.
 */
ZEND_METHOD(Gtk4_GtkFileDialog, set_title) {
  zend_string *title;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(title)
  ZEND_PARSE_PARAMETERS_END();
  GtkFileDialog *self = PHPGTK_SELF(GtkFileDialog, GTK_TYPE_FILE_DIALOG);
  if (!phpgtk::check_utf8(title, 1)) RETURN_THROWS();
  gtk_file_dialog_set_title(self, ZSTR_VAL(title));
}
