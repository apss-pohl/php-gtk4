// GENERATED by gen/gir.php from Gtk.TextChildAnchor - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkTextChildAnchor
#include "php_gtk4.h"
#include "core/object.h"
#include "core/subtype.h"

using namespace phpgtk;

/**
 * Gtk4\GtkTextChildAnchor::__construct()
 *
 * Creates a new `GtkTextChildAnchor`.
 */
ZEND_METHOD(Gtk4_GtkTextChildAnchor, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gtk_text_child_anchor_new());
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * static Gtk4\GtkTextChildAnchor::new_with_replacement(string $character): GtkTextChildAnchor
 *
 * Creates a new `GtkTextChildAnchor` with the given replacement character.
 */
ZEND_METHOD(Gtk4_GtkTextChildAnchor, new_with_replacement) {
  zend_string *character;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(character)
  ZEND_PARSE_PARAMETERS_END();
  if (!phpgtk::check_utf8(character, 1)) RETURN_THROWS();
  const bool precondition_0 = g_utf8_strlen(ZSTR_VAL(character), -1) == 1;
  if (!precondition_0) {
    zend_argument_value_error(1, "must be exactly one character");
    RETURN_THROWS();
  }
  GObject *obj = G_OBJECT(gtk_text_child_anchor_new_with_replacement(ZSTR_VAL(character)));
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}
