// GENERATED by gen/gir.php from Gtk.DragIcon - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkDragIcon
#include "php_gtk4.h"
#include "core/object.h"

using namespace phpgtk;

/**
 * Gtk4\GtkDragIcon::__construct()
 *
 * GtkDragIcon has no constructor in GTK: instances come from GTK, never from `new`.
 */
ZEND_METHOD(Gtk4_GtkDragIcon, __construct) {
  // Private: never called (object_init_ex() in wrap() skips constructors).
  ZEND_PARSE_PARAMETERS_NONE();
}

/**
 * static Gtk4\GtkDragIcon::get_for_drag(GdkDrag $drag): GtkWidget
 *
 * Gets the `GtkDragIcon` in use with $drag.
 */
ZEND_METHOD(Gtk4_GtkDragIcon, get_for_drag) {
  zval *drag;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(drag, class_for_gtype(GDK_TYPE_DRAG))
  ZEND_PARSE_PARAMETERS_END();
  GObject *drag_o = unwrap(drag, GDK_TYPE_DRAG);
  if (drag_o == nullptr) RETURN_THROWS();
  GtkWidget *phpgtk_ret = gtk_drag_icon_get_for_drag(GDK_DRAG(drag_o));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * static Gtk4\GtkDragIcon::set_from_paintable(GdkDrag $drag, GdkPaintable $paintable, int $hot_x,
 * int $hot_y): void
 *
 * Creates a `GtkDragIcon` that shows $paintable, and associates it with the drag operation.
 */
ZEND_METHOD(Gtk4_GtkDragIcon, set_from_paintable) {
  zval *drag;
  zval *paintable;
  zend_long hot_x;
  zend_long hot_y;
  ZEND_PARSE_PARAMETERS_START(4, 4)
  Z_PARAM_OBJECT_OF_CLASS(drag, class_for_gtype(GDK_TYPE_DRAG))
  Z_PARAM_OBJECT_OF_CLASS(paintable, class_for_gtype(GDK_TYPE_PAINTABLE))
  Z_PARAM_LONG(hot_x)
  Z_PARAM_LONG(hot_y)
  ZEND_PARSE_PARAMETERS_END();
  GObject *drag_o = unwrap(drag, GDK_TYPE_DRAG);
  if (drag_o == nullptr) RETURN_THROWS();
  GObject *paintable_o = unwrap(paintable, GDK_TYPE_PAINTABLE);
  if (paintable_o == nullptr) RETURN_THROWS();
  if (!phpgtk::check_range<int>(hot_x, 3)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(hot_y, 4)) RETURN_THROWS();
  gtk_drag_icon_set_from_paintable(GDK_DRAG(drag_o), GDK_PAINTABLE(paintable_o),
                                   static_cast<int>(hot_x), static_cast<int>(hot_y));
}

/**
 * Gtk4\GtkDragIcon::get_child(): ?GtkWidget
 *
 * Gets the widget currently used as drag icon.
 */
ZEND_METHOD(Gtk4_GtkDragIcon, get_child) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkDragIcon *self = PHPGTK_SELF(GtkDragIcon, GTK_TYPE_DRAG_ICON);
  GtkWidget *phpgtk_ret = gtk_drag_icon_get_child(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkDragIcon::set_child(?GtkWidget $child): void
 *
 * Sets the widget to display as the drag icon.
 */
ZEND_METHOD(Gtk4_GtkDragIcon, set_child) {
  zval *child = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(child, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkDragIcon *self = PHPGTK_SELF(GtkDragIcon, GTK_TYPE_DRAG_ICON);
  GObject *child_o = nullptr;
  if (child != nullptr) {
    child_o = unwrap(child, GTK_TYPE_WIDGET);
    if (child_o == nullptr) RETURN_THROWS();
  }
  gtk_drag_icon_set_child(self, child_o != nullptr ? GTK_WIDGET(child_o) : nullptr);
}
