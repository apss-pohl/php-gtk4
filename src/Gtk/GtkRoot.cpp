// GENERATED by gen/gir.php from Gtk.Root - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkRoot
#include "php_gtk4.h"
#include "core/object.h"

using namespace phpgtk;

/**
 * Gtk4\GtkRoot::get_focus(): ?GtkWidget
 *
 * Retrieves the current focused widget within the root.
 */
ZEND_METHOD(Gtk4_GtkRoot, get_focus) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkRoot *self = PHPGTK_SELF(GtkRoot, GTK_TYPE_ROOT);
  GtkWidget *result = gtk_root_get_focus(self);
  wrap(result != nullptr ? G_OBJECT(result) : nullptr, return_value);
}

/**
 * Gtk4\GtkRoot::set_focus(?GtkWidget $focus): void
 *
 * If $focus is not the current focus widget, and is focusable, sets it as the focus widget for the
 * root.
 */
ZEND_METHOD(Gtk4_GtkRoot, set_focus) {
  zval *focus = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(focus, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkRoot *self = PHPGTK_SELF(GtkRoot, GTK_TYPE_ROOT);
  GObject *focus_o = nullptr;
  if (focus != nullptr) {
    focus_o = unwrap(focus, GTK_TYPE_WIDGET);
    if (focus_o == nullptr) RETURN_THROWS();
  }
  gtk_root_set_focus(self, focus_o != nullptr ? GTK_WIDGET(focus_o) : nullptr);
}
