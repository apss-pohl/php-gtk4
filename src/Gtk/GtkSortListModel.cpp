// GENERATED by gen/gir.php from Gtk.SortListModel - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkSortListModel
#include "php_gtk4.h"
#include "core/object.h"
#include "core/subtype.h"

using namespace phpgtk;

/**
 * Gtk4\GtkSortListModel::__construct(?GListModel $model = null, ?GtkSorter $sorter = null)
 *
 * Creates a new sort list model that uses the $sorter to sort $model.
 */
ZEND_METHOD(Gtk4_GtkSortListModel, __construct) {
  zval *model = nullptr;
  zval *sorter = nullptr;
  ZEND_PARSE_PARAMETERS_START(0, 2)
  Z_PARAM_OPTIONAL
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(model, class_for_gtype(G_TYPE_LIST_MODEL))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(sorter, class_for_gtype(GTK_TYPE_SORTER))
  ZEND_PARSE_PARAMETERS_END();
  GObject *model_o = nullptr;
  if (model != nullptr) {
    model_o = unwrap(model, G_TYPE_LIST_MODEL);
    if (model_o == nullptr) RETURN_THROWS();
  }
  if (model_o != nullptr) g_object_ref(model_o);  // transfer full
  GObject *sorter_o = nullptr;
  if (sorter != nullptr) {
    sorter_o = unwrap(sorter, GTK_TYPE_SORTER);
    if (sorter_o == nullptr) RETURN_THROWS();
  }
  if (sorter_o != nullptr) g_object_ref(sorter_o);  // transfer full
  GObject *obj =
      subtype_new(ZEND_THIS, "model", model_o != nullptr ? G_LIST_MODEL(model_o) : nullptr,
                  "sorter", sorter_o != nullptr ? GTK_SORTER(sorter_o) : nullptr, nullptr);
  if (obj != nullptr) {
    if (model_o != nullptr) g_object_unref(model_o);
    if (sorter_o != nullptr) g_object_unref(sorter_o);
  }
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gtk_sort_list_model_new(model_o != nullptr ? G_LIST_MODEL(model_o) : nullptr,
                                           sorter_o != nullptr ? GTK_SORTER(sorter_o) : nullptr));
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkSortListModel::get_incremental(): bool
 *
 * Returns whether incremental sorting is enabled.
 */
ZEND_METHOD(Gtk4_GtkSortListModel, get_incremental) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkSortListModel *self = PHPGTK_SELF(GtkSortListModel, GTK_TYPE_SORT_LIST_MODEL);
  RETURN_BOOL(gtk_sort_list_model_get_incremental(self));
}

/**
 * Gtk4\GtkSortListModel::get_model(): ?GListModel
 *
 * Gets the model currently sorted or `null` if none.
 */
ZEND_METHOD(Gtk4_GtkSortListModel, get_model) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkSortListModel *self = PHPGTK_SELF(GtkSortListModel, GTK_TYPE_SORT_LIST_MODEL);
  GListModel *result = gtk_sort_list_model_get_model(self);
  wrap(result != nullptr ? G_OBJECT(result) : nullptr, return_value);
}

/**
 * Gtk4\GtkSortListModel::get_pending(): int
 *
 * Estimates progress of an ongoing sorting operation.
 */
ZEND_METHOD(Gtk4_GtkSortListModel, get_pending) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkSortListModel *self = PHPGTK_SELF(GtkSortListModel, GTK_TYPE_SORT_LIST_MODEL);
  RETURN_LONG(static_cast<zend_long>(gtk_sort_list_model_get_pending(self)));
}

/**
 * Gtk4\GtkSortListModel::get_section_sorter(): ?GtkSorter
 *
 * Gets the section sorter that is used to sort items of $self into sections.
 */
ZEND_METHOD(Gtk4_GtkSortListModel, get_section_sorter) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkSortListModel *self = PHPGTK_SELF(GtkSortListModel, GTK_TYPE_SORT_LIST_MODEL);
  GtkSorter *result = gtk_sort_list_model_get_section_sorter(self);
  wrap(result != nullptr ? G_OBJECT(result) : nullptr, return_value);
}

/**
 * Gtk4\GtkSortListModel::get_sorter(): ?GtkSorter
 *
 * Gets the sorter that is used to sort $self.
 */
ZEND_METHOD(Gtk4_GtkSortListModel, get_sorter) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkSortListModel *self = PHPGTK_SELF(GtkSortListModel, GTK_TYPE_SORT_LIST_MODEL);
  GtkSorter *result = gtk_sort_list_model_get_sorter(self);
  wrap(result != nullptr ? G_OBJECT(result) : nullptr, return_value);
}

/**
 * Gtk4\GtkSortListModel::set_incremental(bool $incremental): void
 *
 * Sets the sort model to do an incremental sort.
 */
ZEND_METHOD(Gtk4_GtkSortListModel, set_incremental) {
  bool incremental;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(incremental)
  ZEND_PARSE_PARAMETERS_END();
  GtkSortListModel *self = PHPGTK_SELF(GtkSortListModel, GTK_TYPE_SORT_LIST_MODEL);
  gtk_sort_list_model_set_incremental(self, incremental);
}

/**
 * Gtk4\GtkSortListModel::set_model(?GListModel $model): void
 *
 * Sets the model to be sorted.
 */
ZEND_METHOD(Gtk4_GtkSortListModel, set_model) {
  zval *model = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(model, class_for_gtype(G_TYPE_LIST_MODEL))
  ZEND_PARSE_PARAMETERS_END();
  GtkSortListModel *self = PHPGTK_SELF(GtkSortListModel, GTK_TYPE_SORT_LIST_MODEL);
  GObject *model_o = nullptr;
  if (model != nullptr) {
    model_o = unwrap(model, G_TYPE_LIST_MODEL);
    if (model_o == nullptr) RETURN_THROWS();
  }
  gtk_sort_list_model_set_model(self, model_o != nullptr ? G_LIST_MODEL(model_o) : nullptr);
}

/**
 * Gtk4\GtkSortListModel::set_section_sorter(?GtkSorter $sorter): void
 *
 * Sets a new section sorter on $self.
 */
ZEND_METHOD(Gtk4_GtkSortListModel, set_section_sorter) {
  zval *sorter = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(sorter, class_for_gtype(GTK_TYPE_SORTER))
  ZEND_PARSE_PARAMETERS_END();
  GtkSortListModel *self = PHPGTK_SELF(GtkSortListModel, GTK_TYPE_SORT_LIST_MODEL);
  GObject *sorter_o = nullptr;
  if (sorter != nullptr) {
    sorter_o = unwrap(sorter, GTK_TYPE_SORTER);
    if (sorter_o == nullptr) RETURN_THROWS();
  }
  gtk_sort_list_model_set_section_sorter(self,
                                         sorter_o != nullptr ? GTK_SORTER(sorter_o) : nullptr);
}

/**
 * Gtk4\GtkSortListModel::set_sorter(?GtkSorter $sorter): void
 *
 * Sets a new sorter on $self.
 */
ZEND_METHOD(Gtk4_GtkSortListModel, set_sorter) {
  zval *sorter = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(sorter, class_for_gtype(GTK_TYPE_SORTER))
  ZEND_PARSE_PARAMETERS_END();
  GtkSortListModel *self = PHPGTK_SELF(GtkSortListModel, GTK_TYPE_SORT_LIST_MODEL);
  GObject *sorter_o = nullptr;
  if (sorter != nullptr) {
    sorter_o = unwrap(sorter, GTK_TYPE_SORTER);
    if (sorter_o == nullptr) RETURN_THROWS();
  }
  gtk_sort_list_model_set_sorter(self, sorter_o != nullptr ? GTK_SORTER(sorter_o) : nullptr);
}
