// GENERATED by gen/gir.php from Gtk.Filter - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkFilter
#include "php_gtk4.h"
#include "core/object.h"
#include "core/enums.h"
#include "core/subtype.h"
#include "core/error.h"
#include <array>

using namespace phpgtk;

/**
 * Gtk4\GtkFilter::__construct()
 *
 * GtkFilter is has no constructor in GTK: `new` only works on a PHP subclass (which gets its own
 * GType).
 */
ZEND_METHOD(Gtk4_GtkFilter, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) == nullptr) {
      zend_throw_error(
          nullptr, "GtkFilter is has no constructor in GTK: subclass it in PHP (new MyClass())");
    }
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkFilter::changed(GtkFilterChange $change): void
 *
 * Notifies all users of the filter that it has changed.
 */
ZEND_METHOD(Gtk4_GtkFilter, changed) {
  zval *change;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(change, enum_class_for_type(GTK_TYPE_FILTER_CHANGE))
  ZEND_PARSE_PARAMETERS_END();
  GtkFilter *self = PHPGTK_SELF(GtkFilter, GTK_TYPE_FILTER);
  gint change_v = 0;
  if (!enum_from_php(change, GTK_TYPE_FILTER_CHANGE, &change_v)) RETURN_THROWS();
  gtk_filter_changed(self, static_cast<GtkFilterChange>(change_v));
}

/**
 * Gtk4\GtkFilter::get_strictness(): GtkFilterMatch
 *
 * Gets the known strictness of $filters.
 */
ZEND_METHOD(Gtk4_GtkFilter, get_strictness) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFilter *self = PHPGTK_SELF(GtkFilter, GTK_TYPE_FILTER);
  enum_to_php(GTK_TYPE_FILTER_MATCH, gtk_filter_get_strictness(self), return_value);
}

/**
 * Gtk4\GtkFilter::match(GObject $item): bool
 *
 * Checks if the given $item is matched by the filter or not.
 */
ZEND_METHOD(Gtk4_GtkFilter, match) {
  zval *item;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(item, class_for_gtype(G_TYPE_OBJECT))
  ZEND_PARSE_PARAMETERS_END();
  GtkFilter *self = PHPGTK_SELF(GtkFilter, GTK_TYPE_FILTER);
  GObject *item_o = unwrap(item, G_TYPE_OBJECT);
  if (item_o == nullptr) RETURN_THROWS();
  RETURN_BOOL(gtk_filter_match(self, G_OBJECT(item_o)));
}

// vfunc thunk: GTK_FILTER_CLASS->get_strictness -> $this->vfunc_get_strictness() on a PHP subclass
static GtkFilterMatch vfunc_thunk_get_strictness(GtkFilter *self) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_get_strictness", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown): GTK's own
    auto *native = GTK_FILTER_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->get_strictness != nullptr ? native->get_strictness(self)
                                             : static_cast<GtkFilterMatch>(0);
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  GtkFilterMatch result = static_cast<GtkFilterMatch>(0);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    gint v = 0;
    if (enum_from_php(&ret, GTK_TYPE_FILTER_MATCH, &v)) result = static_cast<GtkFilterMatch>(v);
  }
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkFilter::vfunc_get_strictness");
  return result;
}

// vfunc installer: GTK_FILTER_CLASS->get_strictness (called from class_init of a PHP subtype)
static void vfunc_install_get_strictness(gpointer klass) {
  GTK_FILTER_CLASS(klass)->get_strictness = vfunc_thunk_get_strictness;
}

/**
 * Gtk4\GtkFilter::vfunc_get_strictness(): GtkFilterMatch
 *
 * Native `get_strictness` (FilterClass.get_strictness): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_get_strictness()` from an override. Gets the known strictness of
 * $filters.
 */
ZEND_METHOD(Gtk4_GtkFilter, vfunc_get_strictness) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFilter *self = PHPGTK_SELF(GtkFilter, GTK_TYPE_FILTER);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkFilter::vfunc_get_strictness(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_FILTER_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->get_strictness == nullptr) {
    enum_to_php(GTK_TYPE_FILTER_MATCH, 0, return_value);
    return;
  }
  enum_to_php(GTK_TYPE_FILTER_MATCH, klass->get_strictness(self), return_value);
}

// vfunc thunk: GTK_FILTER_CLASS->match -> $this->vfunc_match() on a PHP subclass
static gboolean vfunc_thunk_match(GtkFilter *self, gpointer item) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_match", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown): GTK's own
    auto *native = GTK_FILTER_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->match != nullptr ? native->match(self, item) : FALSE;
  }
  std::array<zval, 1> args{};
  zval *argv = args.data();
  wrap(item != nullptr ? G_OBJECT(item) : nullptr, &argv[0]);
  zval ret;
  ZVAL_UNDEF(&ret);
  gboolean result = FALSE;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = zend_is_true(&ret) ? TRUE : FALSE;
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkFilter::vfunc_match");
  return result;
}

// vfunc installer: GTK_FILTER_CLASS->match (called from class_init of a PHP subtype)
static void vfunc_install_match(gpointer klass) {
  GTK_FILTER_CLASS(klass)->match = vfunc_thunk_match;
}

/**
 * Gtk4\GtkFilter::vfunc_match(?GObject $item): bool
 *
 * Native `match` (FilterClass.match): the GTK implementation below any PHP subclass, for
 * `parent::vfunc_match()` from an override. Checks if the given $item is matched by the filter or
 * not.
 */
ZEND_METHOD(Gtk4_GtkFilter, vfunc_match) {
  zval *item = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(item, class_for_gtype(G_TYPE_OBJECT))
  ZEND_PARSE_PARAMETERS_END();
  GtkFilter *self = PHPGTK_SELF(GtkFilter, GTK_TYPE_FILTER);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(spl_ce_LogicException, 0,
                            "GtkFilter::vfunc_match(): for parent:: chaining from a PHP subclass "
                            "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_FILTER_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->match == nullptr) {
    RETURN_FALSE;
  }
  GObject *item_o = nullptr;
  if (item != nullptr) {
    item_o = unwrap(item, G_TYPE_OBJECT);
    if (item_o == nullptr) RETURN_THROWS();
  }
  RETURN_BOOL(klass->match(self, item_o != nullptr ? G_OBJECT(item_o) : nullptr));
}

// MINIT: the vfunc thunks of GtkFilter (core/subtype.h).
void register_vfuncs_GtkFilter() {
  register_vfunc(GTK_TYPE_FILTER, "get_strictness", vfunc_install_get_strictness);
  register_vfunc(GTK_TYPE_FILTER, "match", vfunc_install_match);
}
