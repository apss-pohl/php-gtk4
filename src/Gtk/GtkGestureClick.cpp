// GENERATED by gen/gir.php from Gtk.GestureClick - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkGestureClick
#include "php_gtk4.h"
#include "core/object.h"
#include "core/subtype.h"

using namespace phpgtk;

/**
 * Gtk4\GtkGestureClick::__construct()
 *
 * Returns a newly created `GtkGesture` that recognizes single and multiple presses.
 */
ZEND_METHOD(Gtk4_GtkGestureClick, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gtk_gesture_click_new());
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}
