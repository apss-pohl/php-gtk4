// GENERATED by gen/gir.php from Gtk.PrintDialog - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkPrintDialog
#include "php_gtk4.h"
#include "core/object.h"
#include "core/gerror.h"
#include "core/boxed.h"
#include "core/subtype.h"
#include "core/callback.h"
#include <array>

using namespace phpgtk;

/**
 * Gtk4\GtkPrintDialog::__construct()
 *
 * Creates a new `GtkPrintDialog` object.
 */
ZEND_METHOD(Gtk4_GtkPrintDialog, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gtk_print_dialog_new());
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkPrintDialog::get_accept_label(): string
 *
 * Returns the label that will be shown on the accept button of the print dialog.
 */
ZEND_METHOD(Gtk4_GtkPrintDialog, get_accept_label) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkPrintDialog *self = PHPGTK_SELF(GtkPrintDialog, GTK_TYPE_PRINT_DIALOG);
  const char *phpgtk_ret = gtk_print_dialog_get_accept_label(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

/**
 * Gtk4\GtkPrintDialog::get_modal(): bool
 *
 * Returns whether the print dialog blocks interaction with the parent window while it is
 * presented.
 */
ZEND_METHOD(Gtk4_GtkPrintDialog, get_modal) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkPrintDialog *self = PHPGTK_SELF(GtkPrintDialog, GTK_TYPE_PRINT_DIALOG);
  RETURN_BOOL(gtk_print_dialog_get_modal(self));
}

/**
 * Gtk4\GtkPrintDialog::get_page_setup(): ?GtkPageSetup
 *
 * Returns the page setup.
 */
ZEND_METHOD(Gtk4_GtkPrintDialog, get_page_setup) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkPrintDialog *self = PHPGTK_SELF(GtkPrintDialog, GTK_TYPE_PRINT_DIALOG);
  GtkPageSetup *phpgtk_ret = gtk_print_dialog_get_page_setup(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkPrintDialog::get_print_settings(): ?GtkPrintSettings
 *
 * Returns the print settings for the print dialog.
 */
ZEND_METHOD(Gtk4_GtkPrintDialog, get_print_settings) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkPrintDialog *self = PHPGTK_SELF(GtkPrintDialog, GTK_TYPE_PRINT_DIALOG);
  GtkPrintSettings *phpgtk_ret = gtk_print_dialog_get_print_settings(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkPrintDialog::get_title(): string
 *
 * Returns the title that will be shown on the print dialog.
 */
ZEND_METHOD(Gtk4_GtkPrintDialog, get_title) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkPrintDialog *self = PHPGTK_SELF(GtkPrintDialog, GTK_TYPE_PRINT_DIALOG);
  const char *phpgtk_ret = gtk_print_dialog_get_title(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

namespace {
// AsyncReadyCallback trampoline for GtkPrintDialog::print(): wraps the C arguments, invokes the PHP
// callable once and releases it (async scope).
void cb_print_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\GtkPrintDialog::print(?GtkWindow $parent, ?GtkPrintSetup $setup, ?GCancellable $cancellable,
 * ?callable $callback): void
 *
 * This function prints content from a stream.
 */
ZEND_METHOD(Gtk4_GtkPrintDialog, print) {
  zval *parent = nullptr;
  zval *setup = nullptr;
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(4, 4)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(parent, class_for_gtype(GTK_TYPE_WINDOW))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(setup, boxed_class_for_type(GTK_TYPE_PRINT_SETUP)->ce)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC_OR_NULL(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  GtkPrintDialog *self = PHPGTK_SELF(GtkPrintDialog, GTK_TYPE_PRINT_DIALOG);
  GObject *parent_o = nullptr;
  if (parent != nullptr) {
    parent_o = unwrap(parent, GTK_TYPE_WINDOW);
    if (parent_o == nullptr) RETURN_THROWS();
  }
  gpointer setup_b = nullptr;
  if (setup != nullptr) {
    setup_b = unwrap_boxed(setup, GTK_TYPE_PRINT_SETUP);
    if (setup_b == nullptr) RETURN_THROWS();
  }
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback = ZEND_FCI_INITIALIZED(fci_callback)
                              ? callback_new(&fci_callback.function_name, "GtkPrintDialog::print")
                              : nullptr;
  gtk_print_dialog_print(self, parent_o != nullptr ? GTK_WINDOW(parent_o) : nullptr,
                         static_cast<GtkPrintSetup *>(setup_b),
                         cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
                         cb_callback != nullptr ? cb_print_callback : nullptr, cb_callback);
}

namespace {
// AsyncReadyCallback trampoline for GtkPrintDialog::print_file(): wraps the C arguments, invokes
// the PHP callable once and releases it (async scope).
void cb_print_file_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\GtkPrintDialog::print_file(?GtkWindow $parent, ?GtkPrintSetup $setup, string $file,
 * ?GCancellable $cancellable, ?callable $callback): void
 *
 * This function prints a file.
 */
ZEND_METHOD(Gtk4_GtkPrintDialog, print_file) {
  zval *parent = nullptr;
  zval *setup = nullptr;
  zend_string *file;
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(5, 5)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(parent, class_for_gtype(GTK_TYPE_WINDOW))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(setup, boxed_class_for_type(GTK_TYPE_PRINT_SETUP)->ce)
  Z_PARAM_PATH_STR(file)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC_OR_NULL(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  GtkPrintDialog *self = PHPGTK_SELF(GtkPrintDialog, GTK_TYPE_PRINT_DIALOG);
  GObject *parent_o = nullptr;
  if (parent != nullptr) {
    parent_o = unwrap(parent, GTK_TYPE_WINDOW);
    if (parent_o == nullptr) RETURN_THROWS();
  }
  gpointer setup_b = nullptr;
  if (setup != nullptr) {
    setup_b = unwrap_boxed(setup, GTK_TYPE_PRINT_SETUP);
    if (setup_b == nullptr) RETURN_THROWS();
  }
  GFile *file_f = g_file_new_for_commandline_arg(ZSTR_VAL(file));
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback =
      ZEND_FCI_INITIALIZED(fci_callback)
          ? callback_new(&fci_callback.function_name, "GtkPrintDialog::print_file")
          : nullptr;
  gtk_print_dialog_print_file(self, parent_o != nullptr ? GTK_WINDOW(parent_o) : nullptr,
                              static_cast<GtkPrintSetup *>(setup_b), file_f,
                              cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
                              cb_callback != nullptr ? cb_print_file_callback : nullptr,
                              cb_callback);
  if (file_f != nullptr) g_object_unref(file_f);
}

/**
 * Gtk4\GtkPrintDialog::print_file_finish(GAsyncResult $result): bool
 *
 * Finishes the `print_file` call and returns the results.
 */
ZEND_METHOD(Gtk4_GtkPrintDialog, print_file_finish) {
  zval *result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  GtkPrintDialog *self = PHPGTK_SELF(GtkPrintDialog, GTK_TYPE_PRINT_DIALOG);
  GObject *result_o = unwrap(result, G_TYPE_ASYNC_RESULT);
  if (result_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  const gboolean ok = gtk_print_dialog_print_file_finish(self, G_ASYNC_RESULT(result_o), &error);
  if (error != nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  RETURN_BOOL(ok);
}

/**
 * Gtk4\GtkPrintDialog::set_accept_label(string $accept_label): void
 *
 * Sets the label that will be shown on the accept button of the print dialog shown for `setup`.
 */
ZEND_METHOD(Gtk4_GtkPrintDialog, set_accept_label) {
  zend_string *accept_label;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(accept_label)
  ZEND_PARSE_PARAMETERS_END();
  GtkPrintDialog *self = PHPGTK_SELF(GtkPrintDialog, GTK_TYPE_PRINT_DIALOG);
  if (!phpgtk::check_utf8(accept_label, 1)) RETURN_THROWS();
  gtk_print_dialog_set_accept_label(self, ZSTR_VAL(accept_label));
}

/**
 * Gtk4\GtkPrintDialog::set_modal(bool $modal): void
 *
 * Sets whether the print dialog blocks interaction with the parent window while it is presented.
 */
ZEND_METHOD(Gtk4_GtkPrintDialog, set_modal) {
  bool modal;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(modal)
  ZEND_PARSE_PARAMETERS_END();
  GtkPrintDialog *self = PHPGTK_SELF(GtkPrintDialog, GTK_TYPE_PRINT_DIALOG);
  gtk_print_dialog_set_modal(self, modal);
}

/**
 * Gtk4\GtkPrintDialog::set_page_setup(GtkPageSetup $page_setup): void
 *
 * Set the page setup for the print dialog.
 */
ZEND_METHOD(Gtk4_GtkPrintDialog, set_page_setup) {
  zval *page_setup;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(page_setup, class_for_gtype(GTK_TYPE_PAGE_SETUP))
  ZEND_PARSE_PARAMETERS_END();
  GtkPrintDialog *self = PHPGTK_SELF(GtkPrintDialog, GTK_TYPE_PRINT_DIALOG);
  GObject *page_setup_o = unwrap(page_setup, GTK_TYPE_PAGE_SETUP);
  if (page_setup_o == nullptr) RETURN_THROWS();
  gtk_print_dialog_set_page_setup(self, GTK_PAGE_SETUP(page_setup_o));
}

/**
 * Gtk4\GtkPrintDialog::set_print_settings(GtkPrintSettings $print_settings): void
 *
 * Sets the print settings for the print dialog.
 */
ZEND_METHOD(Gtk4_GtkPrintDialog, set_print_settings) {
  zval *print_settings;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(print_settings, class_for_gtype(GTK_TYPE_PRINT_SETTINGS))
  ZEND_PARSE_PARAMETERS_END();
  GtkPrintDialog *self = PHPGTK_SELF(GtkPrintDialog, GTK_TYPE_PRINT_DIALOG);
  GObject *print_settings_o = unwrap(print_settings, GTK_TYPE_PRINT_SETTINGS);
  if (print_settings_o == nullptr) RETURN_THROWS();
  gtk_print_dialog_set_print_settings(self, GTK_PRINT_SETTINGS(print_settings_o));
}

/**
 * Gtk4\GtkPrintDialog::set_title(string $title): void
 *
 * Sets the title that will be shown on the print dialog.
 */
ZEND_METHOD(Gtk4_GtkPrintDialog, set_title) {
  zend_string *title;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(title)
  ZEND_PARSE_PARAMETERS_END();
  GtkPrintDialog *self = PHPGTK_SELF(GtkPrintDialog, GTK_TYPE_PRINT_DIALOG);
  if (!phpgtk::check_utf8(title, 1)) RETURN_THROWS();
  gtk_print_dialog_set_title(self, ZSTR_VAL(title));
}

namespace {
// AsyncReadyCallback trampoline for GtkPrintDialog::setup(): wraps the C arguments, invokes the PHP
// callable once and releases it (async scope).
void cb_setup_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\GtkPrintDialog::setup(?GtkWindow $parent, ?GCancellable $cancellable, ?callable $callback):
 * void
 *
 * This function presents a print dialog to let the user select a printer, and set up print
 * settings and page setup.
 */
ZEND_METHOD(Gtk4_GtkPrintDialog, setup) {
  zval *parent = nullptr;
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(parent, class_for_gtype(GTK_TYPE_WINDOW))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC_OR_NULL(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  GtkPrintDialog *self = PHPGTK_SELF(GtkPrintDialog, GTK_TYPE_PRINT_DIALOG);
  GObject *parent_o = nullptr;
  if (parent != nullptr) {
    parent_o = unwrap(parent, GTK_TYPE_WINDOW);
    if (parent_o == nullptr) RETURN_THROWS();
  }
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback = ZEND_FCI_INITIALIZED(fci_callback)
                              ? callback_new(&fci_callback.function_name, "GtkPrintDialog::setup")
                              : nullptr;
  gtk_print_dialog_setup(self, parent_o != nullptr ? GTK_WINDOW(parent_o) : nullptr,
                         cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
                         cb_callback != nullptr ? cb_setup_callback : nullptr, cb_callback);
}

/**
 * Gtk4\GtkPrintDialog::setup_finish(GAsyncResult $result): ?GtkPrintSetup
 *
 * Finishes the `setup` call.
 */
ZEND_METHOD(Gtk4_GtkPrintDialog, setup_finish) {
  zval *result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  GtkPrintDialog *self = PHPGTK_SELF(GtkPrintDialog, GTK_TYPE_PRINT_DIALOG);
  GObject *result_o = unwrap(result, G_TYPE_ASYNC_RESULT);
  if (result_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  GtkPrintSetup *phpgtk_ret = gtk_print_dialog_setup_finish(self, G_ASYNC_RESULT(result_o), &error);
  wrap_boxed(GTK_TYPE_PRINT_SETUP, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(GTK_TYPE_PRINT_SETUP, phpgtk_ret);
}
