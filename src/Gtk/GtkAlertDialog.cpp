// GENERATED by gen/gir.php from Gtk.AlertDialog - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkAlertDialog
#include "php_gtk4.h"
#include "core/object.h"
#include "core/collections.h"
#include "core/gerror.h"
#include "core/subtype.h"
#include "core/callback.h"
#include <array>

using namespace phpgtk;

/**
 * Gtk4\GtkAlertDialog::__construct()
 *
 * A GtkAlertDialog with default properties (GTK's own constructor is varargs-only; set the
 * properties afterwards).
 */
ZEND_METHOD(Gtk4_GtkAlertDialog, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = static_cast<GObject *>(g_object_new(GTK_TYPE_ALERT_DIALOG, nullptr));
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

namespace {
// AsyncReadyCallback trampoline for GtkAlertDialog::choose(): wraps the C arguments, invokes the
// PHP callable once and releases it (async scope).
void cb_choose_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\GtkAlertDialog::choose(?GtkWindow $parent, ?GCancellable $cancellable, ?callable $callback):
 * void
 *
 * This function shows the alert to the user.
 */
ZEND_METHOD(Gtk4_GtkAlertDialog, choose) {
  zval *parent = nullptr;
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(parent, class_for_gtype(GTK_TYPE_WINDOW))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC_OR_NULL(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  GtkAlertDialog *self = PHPGTK_SELF(GtkAlertDialog, GTK_TYPE_ALERT_DIALOG);
  GObject *parent_o = nullptr;
  if (parent != nullptr) {
    parent_o = unwrap(parent, GTK_TYPE_WINDOW);
    if (parent_o == nullptr) RETURN_THROWS();
  }
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback = ZEND_FCI_INITIALIZED(fci_callback)
                              ? callback_new(&fci_callback.function_name, "GtkAlertDialog::choose")
                              : nullptr;
  gtk_alert_dialog_choose(self, parent_o != nullptr ? GTK_WINDOW(parent_o) : nullptr,
                          cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
                          cb_callback != nullptr ? cb_choose_callback : nullptr, cb_callback);
}

/**
 * Gtk4\GtkAlertDialog::choose_finish(GAsyncResult $result): int
 *
 * Finishes the `choose` call and returns the index of the button that was clicked.
 */
ZEND_METHOD(Gtk4_GtkAlertDialog, choose_finish) {
  zval *result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  GtkAlertDialog *self = PHPGTK_SELF(GtkAlertDialog, GTK_TYPE_ALERT_DIALOG);
  GObject *result_o = unwrap(result, G_TYPE_ASYNC_RESULT);
  if (result_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  const auto value = static_cast<zend_long>(
      gtk_alert_dialog_choose_finish(self, G_ASYNC_RESULT(result_o), &error));
  if (error != nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  RETURN_LONG(value);
}

/**
 * Gtk4\GtkAlertDialog::get_buttons(): array
 *
 * Returns the button labels for the alert.
 */
ZEND_METHOD(Gtk4_GtkAlertDialog, get_buttons) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkAlertDialog *self = PHPGTK_SELF(GtkAlertDialog, GTK_TYPE_ALERT_DIALOG);
  strv_to_php(gtk_alert_dialog_get_buttons(self), return_value);
}

/**
 * Gtk4\GtkAlertDialog::get_cancel_button(): int
 *
 * Returns the index of the cancel button.
 */
ZEND_METHOD(Gtk4_GtkAlertDialog, get_cancel_button) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkAlertDialog *self = PHPGTK_SELF(GtkAlertDialog, GTK_TYPE_ALERT_DIALOG);
  RETURN_LONG(static_cast<zend_long>(gtk_alert_dialog_get_cancel_button(self)));
}

/**
 * Gtk4\GtkAlertDialog::get_default_button(): int
 *
 * Returns the index of the default button.
 */
ZEND_METHOD(Gtk4_GtkAlertDialog, get_default_button) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkAlertDialog *self = PHPGTK_SELF(GtkAlertDialog, GTK_TYPE_ALERT_DIALOG);
  RETURN_LONG(static_cast<zend_long>(gtk_alert_dialog_get_default_button(self)));
}

/**
 * Gtk4\GtkAlertDialog::get_detail(): string
 *
 * Returns the detail text that will be shown in the alert.
 */
ZEND_METHOD(Gtk4_GtkAlertDialog, get_detail) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkAlertDialog *self = PHPGTK_SELF(GtkAlertDialog, GTK_TYPE_ALERT_DIALOG);
  const char *phpgtk_ret = gtk_alert_dialog_get_detail(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

/**
 * Gtk4\GtkAlertDialog::get_message(): string
 *
 * Returns the message that will be shown in the alert.
 */
ZEND_METHOD(Gtk4_GtkAlertDialog, get_message) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkAlertDialog *self = PHPGTK_SELF(GtkAlertDialog, GTK_TYPE_ALERT_DIALOG);
  const char *phpgtk_ret = gtk_alert_dialog_get_message(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

/**
 * Gtk4\GtkAlertDialog::get_modal(): bool
 *
 * Returns whether the alert blocks interaction with the parent window while it is presented.
 */
ZEND_METHOD(Gtk4_GtkAlertDialog, get_modal) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkAlertDialog *self = PHPGTK_SELF(GtkAlertDialog, GTK_TYPE_ALERT_DIALOG);
  RETURN_BOOL(gtk_alert_dialog_get_modal(self));
}

/**
 * Gtk4\GtkAlertDialog::set_buttons(array $labels): void
 *
 * Sets the button labels for the alert.
 */
ZEND_METHOD(Gtk4_GtkAlertDialog, set_buttons) {
  zval *labels;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_ARRAY(labels)
  ZEND_PARSE_PARAMETERS_END();
  GtkAlertDialog *self = PHPGTK_SELF(GtkAlertDialog, GTK_TYPE_ALERT_DIALOG);
  char **labels_v = strv_from_php(labels);
  if (labels_v == nullptr) RETURN_THROWS();
  gtk_alert_dialog_set_buttons(self, const_cast<const char **>(labels_v));
  g_strfreev(labels_v);
}

/**
 * Gtk4\GtkAlertDialog::set_cancel_button(int $button): void
 *
 * Sets the index of the cancel button.
 */
ZEND_METHOD(Gtk4_GtkAlertDialog, set_cancel_button) {
  zend_long button;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(button)
  ZEND_PARSE_PARAMETERS_END();
  GtkAlertDialog *self = PHPGTK_SELF(GtkAlertDialog, GTK_TYPE_ALERT_DIALOG);
  if (!phpgtk::check_range<int>(button, 1)) RETURN_THROWS();
  gtk_alert_dialog_set_cancel_button(self, static_cast<int>(button));
}

/**
 * Gtk4\GtkAlertDialog::set_default_button(int $button): void
 *
 * Sets the index of the default button.
 */
ZEND_METHOD(Gtk4_GtkAlertDialog, set_default_button) {
  zend_long button;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(button)
  ZEND_PARSE_PARAMETERS_END();
  GtkAlertDialog *self = PHPGTK_SELF(GtkAlertDialog, GTK_TYPE_ALERT_DIALOG);
  if (!phpgtk::check_range<int>(button, 1)) RETURN_THROWS();
  gtk_alert_dialog_set_default_button(self, static_cast<int>(button));
}

/**
 * Gtk4\GtkAlertDialog::set_detail(string $detail): void
 *
 * Sets the detail text that will be shown in the alert.
 */
ZEND_METHOD(Gtk4_GtkAlertDialog, set_detail) {
  zend_string *detail;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(detail)
  ZEND_PARSE_PARAMETERS_END();
  GtkAlertDialog *self = PHPGTK_SELF(GtkAlertDialog, GTK_TYPE_ALERT_DIALOG);
  if (!phpgtk::check_utf8(detail, 1)) RETURN_THROWS();
  gtk_alert_dialog_set_detail(self, ZSTR_VAL(detail));
}

/**
 * Gtk4\GtkAlertDialog::set_message(string $message): void
 *
 * Sets the message that will be shown in the alert.
 */
ZEND_METHOD(Gtk4_GtkAlertDialog, set_message) {
  zend_string *message;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(message)
  ZEND_PARSE_PARAMETERS_END();
  GtkAlertDialog *self = PHPGTK_SELF(GtkAlertDialog, GTK_TYPE_ALERT_DIALOG);
  if (!phpgtk::check_utf8(message, 1)) RETURN_THROWS();
  gtk_alert_dialog_set_message(self, ZSTR_VAL(message));
}

/**
 * Gtk4\GtkAlertDialog::set_modal(bool $modal): void
 *
 * Sets whether the alert blocks interaction with the parent window while it is presented.
 */
ZEND_METHOD(Gtk4_GtkAlertDialog, set_modal) {
  bool modal;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(modal)
  ZEND_PARSE_PARAMETERS_END();
  GtkAlertDialog *self = PHPGTK_SELF(GtkAlertDialog, GTK_TYPE_ALERT_DIALOG);
  gtk_alert_dialog_set_modal(self, modal);
}

/**
 * Gtk4\GtkAlertDialog::show(?GtkWindow $parent): void
 *
 * Show the alert to the user.
 */
ZEND_METHOD(Gtk4_GtkAlertDialog, show) {
  zval *parent = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(parent, class_for_gtype(GTK_TYPE_WINDOW))
  ZEND_PARSE_PARAMETERS_END();
  GtkAlertDialog *self = PHPGTK_SELF(GtkAlertDialog, GTK_TYPE_ALERT_DIALOG);
  GObject *parent_o = nullptr;
  if (parent != nullptr) {
    parent_o = unwrap(parent, GTK_TYPE_WINDOW);
    if (parent_o == nullptr) RETURN_THROWS();
  }
  gtk_alert_dialog_show(self, parent_o != nullptr ? GTK_WINDOW(parent_o) : nullptr);
}
