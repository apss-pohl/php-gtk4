// GENERATED by gen/gir.php from Gtk.Requisition - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkRequisition (boxed value type)
#include "php_gtk4.h"
#include "core/boxed.h"
#include "core/object.h"
#include <cstring>

using namespace phpgtk;

namespace {

const char *const fields[] = {"width", "height", nullptr};

// Boxed field reader: the public scalar fields as PHP properties.
bool read(gpointer data, const char *field, zval *rv) {
  auto *value = static_cast<GtkRequisition *>(data);
  if (strcmp(field, "width") == 0) {
    ZVAL_LONG(rv, static_cast<zend_long>(value->width));
    return true;
  }
  if (strcmp(field, "height") == 0) {
    ZVAL_LONG(rv, static_cast<zend_long>(value->height));
    return true;
  }
  return false;
}

// Boxed field writer: coerces to the field's C type.
bool write(gpointer data, const char *field, zval *v) {
  auto *value = static_cast<GtkRequisition *>(data);
  if (strcmp(field, "width") == 0) {
    value->width = static_cast<int>(zval_get_long(v));
    return true;
  }
  if (strcmp(field, "height") == 0) {
    value->height = static_cast<int>(zval_get_long(v));
    return true;
  }
  return false;
}

}  // namespace

/**
 * Gtk4\GtkRequisition::__construct()
 *
 * Allocates a new `GtkRequisition`.
 */
ZEND_METHOD(Gtk4_GtkRequisition, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  gpointer obj = gtk_requisition_new();
  if (obj == nullptr) {
    zend_throw_error(nullptr, "%s(): GTK refused to create the value (see the CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  boxed_adopt(boxed_from_zval(ZEND_THIS), GTK_TYPE_REQUISITION, obj);
}

namespace phpgtk {
// MINIT: bind the PHP class to GTK_TYPE_REQUISITION with its field table.
void register_GtkRequisition(zend_class_entry *ce) {
  register_boxed(BoxedClass{
      .type = GTK_TYPE_REQUISITION, .ce = ce, .fields = fields, .read = read, .write = write});
}
}  // namespace phpgtk
