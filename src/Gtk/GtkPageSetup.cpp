// GENERATED by gen/gir.php from Gtk.PageSetup - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkPageSetup
#include "php_gtk4.h"
#include "core/object.h"
#include "core/enums.h"
#include "core/gerror.h"
#include "core/variant.h"
#include "core/boxed.h"
#include "core/subtype.h"

using namespace phpgtk;

/**
 * Gtk4\GtkPageSetup::__construct()
 *
 * Creates a new `GtkPageSetup`.
 */
ZEND_METHOD(Gtk4_GtkPageSetup, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gtk_page_setup_new());
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * static Gtk4\GtkPageSetup::new_from_file(string $file_name): GtkPageSetup
 *
 * Reads the page setup from the file $file_name.
 */
ZEND_METHOD(Gtk4_GtkPageSetup, new_from_file) {
  zend_string *file_name;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_PATH_STR(file_name)
  ZEND_PARSE_PARAMETERS_END();
  zend_string *file_name_abs = phpgtk::absolute_filename(file_name, 1);
  if (file_name_abs == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  GtkPageSetup *call_result = gtk_page_setup_new_from_file(ZSTR_VAL(file_name_abs), &error);
  if (file_name_abs != nullptr) zend_string_release(file_name_abs);
  GObject *obj = G_OBJECT(call_result);
  if (obj == nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * static Gtk4\GtkPageSetup::new_from_gvariant(mixed $variant = null): GtkPageSetup
 *
 * Desrialize a page setup from an a{sv} variant.
 */
ZEND_METHOD(Gtk4_GtkPageSetup, new_from_gvariant) {
  zval *variant = nullptr;
  ZEND_PARSE_PARAMETERS_START(0, 1)
  Z_PARAM_OPTIONAL
  Z_PARAM_ZVAL(variant)
  ZEND_PARSE_PARAMETERS_END();
  GVariant *variant_v = nullptr;
  if (variant != nullptr && Z_TYPE_P(variant) != IS_NULL) {
    variant_v = php_to_variant(variant, nullptr);
    if (variant_v == nullptr) RETURN_THROWS();
    g_variant_ref_sink(variant_v);
  }
  GtkPageSetup *call_result = gtk_page_setup_new_from_gvariant(variant_v);
  if (variant_v != nullptr) g_variant_unref(variant_v);
  GObject *obj = G_OBJECT(call_result);
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * static Gtk4\GtkPageSetup::new_from_key_file(GKeyFile $key_file, ?string $group_name = null):
 * GtkPageSetup
 *
 * Reads the page setup from the group $group_name in the key file $key_file.
 */
ZEND_METHOD(Gtk4_GtkPageSetup, new_from_key_file) {
  zval *key_file;
  zend_string *group_name = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 2)
  Z_PARAM_OBJECT_OF_CLASS(key_file, boxed_class_for_type(G_TYPE_KEY_FILE)->ce)
  Z_PARAM_OPTIONAL
  Z_PARAM_STR_OR_NULL(group_name)
  ZEND_PARSE_PARAMETERS_END();
  gpointer key_file_b = unwrap_boxed(key_file, G_TYPE_KEY_FILE);
  if (key_file_b == nullptr) RETURN_THROWS();
  if (group_name != nullptr && !phpgtk::check_utf8(group_name, 2)) RETURN_THROWS();
  GError *error = nullptr;
  GObject *obj = G_OBJECT(gtk_page_setup_new_from_key_file(
      static_cast<GKeyFile *>(key_file_b), group_name != nullptr ? ZSTR_VAL(group_name) : nullptr,
      &error));
  if (obj == nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * Gtk4\GtkPageSetup::copy(): GtkPageSetup
 *
 * Copies a `GtkPageSetup`.
 */
ZEND_METHOD(Gtk4_GtkPageSetup, copy) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkPageSetup *self = PHPGTK_SELF(GtkPageSetup, GTK_TYPE_PAGE_SETUP);
  GtkPageSetup *phpgtk_ret = gtk_page_setup_copy(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * Gtk4\GtkPageSetup::get_bottom_margin(GtkUnit $unit): float
 *
 * Gets the bottom margin in units of $unit.
 */
ZEND_METHOD(Gtk4_GtkPageSetup, get_bottom_margin) {
  zval *unit;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(unit, enum_class_for_type(GTK_TYPE_UNIT))
  ZEND_PARSE_PARAMETERS_END();
  GtkPageSetup *self = PHPGTK_SELF(GtkPageSetup, GTK_TYPE_PAGE_SETUP);
  gint unit_v = 0;
  if (!enum_from_php(unit, GTK_TYPE_UNIT, &unit_v)) RETURN_THROWS();
  RETURN_DOUBLE(gtk_page_setup_get_bottom_margin(self, static_cast<GtkUnit>(unit_v)));
}

/**
 * Gtk4\GtkPageSetup::get_left_margin(GtkUnit $unit): float
 *
 * Gets the left margin in units of $unit.
 */
ZEND_METHOD(Gtk4_GtkPageSetup, get_left_margin) {
  zval *unit;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(unit, enum_class_for_type(GTK_TYPE_UNIT))
  ZEND_PARSE_PARAMETERS_END();
  GtkPageSetup *self = PHPGTK_SELF(GtkPageSetup, GTK_TYPE_PAGE_SETUP);
  gint unit_v = 0;
  if (!enum_from_php(unit, GTK_TYPE_UNIT, &unit_v)) RETURN_THROWS();
  RETURN_DOUBLE(gtk_page_setup_get_left_margin(self, static_cast<GtkUnit>(unit_v)));
}

/**
 * Gtk4\GtkPageSetup::get_orientation(): GtkPageOrientation
 *
 * Gets the page orientation of the `GtkPageSetup`.
 */
ZEND_METHOD(Gtk4_GtkPageSetup, get_orientation) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkPageSetup *self = PHPGTK_SELF(GtkPageSetup, GTK_TYPE_PAGE_SETUP);
  enum_to_php(GTK_TYPE_PAGE_ORIENTATION, gtk_page_setup_get_orientation(self), return_value);
}

/**
 * Gtk4\GtkPageSetup::get_page_height(GtkUnit $unit): float
 *
 * Returns the page height in units of $unit.
 */
ZEND_METHOD(Gtk4_GtkPageSetup, get_page_height) {
  zval *unit;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(unit, enum_class_for_type(GTK_TYPE_UNIT))
  ZEND_PARSE_PARAMETERS_END();
  GtkPageSetup *self = PHPGTK_SELF(GtkPageSetup, GTK_TYPE_PAGE_SETUP);
  gint unit_v = 0;
  if (!enum_from_php(unit, GTK_TYPE_UNIT, &unit_v)) RETURN_THROWS();
  RETURN_DOUBLE(gtk_page_setup_get_page_height(self, static_cast<GtkUnit>(unit_v)));
}

/**
 * Gtk4\GtkPageSetup::get_page_width(GtkUnit $unit): float
 *
 * Returns the page width in units of $unit.
 */
ZEND_METHOD(Gtk4_GtkPageSetup, get_page_width) {
  zval *unit;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(unit, enum_class_for_type(GTK_TYPE_UNIT))
  ZEND_PARSE_PARAMETERS_END();
  GtkPageSetup *self = PHPGTK_SELF(GtkPageSetup, GTK_TYPE_PAGE_SETUP);
  gint unit_v = 0;
  if (!enum_from_php(unit, GTK_TYPE_UNIT, &unit_v)) RETURN_THROWS();
  RETURN_DOUBLE(gtk_page_setup_get_page_width(self, static_cast<GtkUnit>(unit_v)));
}

/**
 * Gtk4\GtkPageSetup::get_paper_height(GtkUnit $unit): float
 *
 * Returns the paper height in units of $unit.
 */
ZEND_METHOD(Gtk4_GtkPageSetup, get_paper_height) {
  zval *unit;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(unit, enum_class_for_type(GTK_TYPE_UNIT))
  ZEND_PARSE_PARAMETERS_END();
  GtkPageSetup *self = PHPGTK_SELF(GtkPageSetup, GTK_TYPE_PAGE_SETUP);
  gint unit_v = 0;
  if (!enum_from_php(unit, GTK_TYPE_UNIT, &unit_v)) RETURN_THROWS();
  RETURN_DOUBLE(gtk_page_setup_get_paper_height(self, static_cast<GtkUnit>(unit_v)));
}

/**
 * Gtk4\GtkPageSetup::get_paper_size(): GtkPaperSize
 *
 * Gets the paper size of the `GtkPageSetup`.
 */
ZEND_METHOD(Gtk4_GtkPageSetup, get_paper_size) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkPageSetup *self = PHPGTK_SELF(GtkPageSetup, GTK_TYPE_PAGE_SETUP);
  GtkPaperSize *phpgtk_ret = gtk_page_setup_get_paper_size(self);
  wrap_boxed(GTK_TYPE_PAPER_SIZE, phpgtk_ret, return_value);
}

/**
 * Gtk4\GtkPageSetup::get_paper_width(GtkUnit $unit): float
 *
 * Returns the paper width in units of $unit.
 */
ZEND_METHOD(Gtk4_GtkPageSetup, get_paper_width) {
  zval *unit;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(unit, enum_class_for_type(GTK_TYPE_UNIT))
  ZEND_PARSE_PARAMETERS_END();
  GtkPageSetup *self = PHPGTK_SELF(GtkPageSetup, GTK_TYPE_PAGE_SETUP);
  gint unit_v = 0;
  if (!enum_from_php(unit, GTK_TYPE_UNIT, &unit_v)) RETURN_THROWS();
  RETURN_DOUBLE(gtk_page_setup_get_paper_width(self, static_cast<GtkUnit>(unit_v)));
}

/**
 * Gtk4\GtkPageSetup::get_right_margin(GtkUnit $unit): float
 *
 * Gets the right margin in units of $unit.
 */
ZEND_METHOD(Gtk4_GtkPageSetup, get_right_margin) {
  zval *unit;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(unit, enum_class_for_type(GTK_TYPE_UNIT))
  ZEND_PARSE_PARAMETERS_END();
  GtkPageSetup *self = PHPGTK_SELF(GtkPageSetup, GTK_TYPE_PAGE_SETUP);
  gint unit_v = 0;
  if (!enum_from_php(unit, GTK_TYPE_UNIT, &unit_v)) RETURN_THROWS();
  RETURN_DOUBLE(gtk_page_setup_get_right_margin(self, static_cast<GtkUnit>(unit_v)));
}

/**
 * Gtk4\GtkPageSetup::get_top_margin(GtkUnit $unit): float
 *
 * Gets the top margin in units of $unit.
 */
ZEND_METHOD(Gtk4_GtkPageSetup, get_top_margin) {
  zval *unit;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(unit, enum_class_for_type(GTK_TYPE_UNIT))
  ZEND_PARSE_PARAMETERS_END();
  GtkPageSetup *self = PHPGTK_SELF(GtkPageSetup, GTK_TYPE_PAGE_SETUP);
  gint unit_v = 0;
  if (!enum_from_php(unit, GTK_TYPE_UNIT, &unit_v)) RETURN_THROWS();
  RETURN_DOUBLE(gtk_page_setup_get_top_margin(self, static_cast<GtkUnit>(unit_v)));
}

/**
 * Gtk4\GtkPageSetup::load_file(string $file_name): bool
 *
 * Reads the page setup from the file $file_name.
 */
ZEND_METHOD(Gtk4_GtkPageSetup, load_file) {
  zend_string *file_name;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_PATH_STR(file_name)
  ZEND_PARSE_PARAMETERS_END();
  GtkPageSetup *self = PHPGTK_SELF(GtkPageSetup, GTK_TYPE_PAGE_SETUP);
  zend_string *file_name_abs = phpgtk::absolute_filename(file_name, 1);
  if (file_name_abs == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  const gboolean ok = gtk_page_setup_load_file(self, ZSTR_VAL(file_name_abs), &error);
  if (file_name_abs != nullptr) zend_string_release(file_name_abs);
  if (error != nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  RETURN_BOOL(ok);
}

/**
 * Gtk4\GtkPageSetup::load_key_file(GKeyFile $key_file, ?string $group_name): bool
 *
 * Reads the page setup from the group $group_name in the key file $key_file.
 */
ZEND_METHOD(Gtk4_GtkPageSetup, load_key_file) {
  zval *key_file;
  zend_string *group_name = nullptr;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(key_file, boxed_class_for_type(G_TYPE_KEY_FILE)->ce)
  Z_PARAM_STR_OR_NULL(group_name)
  ZEND_PARSE_PARAMETERS_END();
  GtkPageSetup *self = PHPGTK_SELF(GtkPageSetup, GTK_TYPE_PAGE_SETUP);
  gpointer key_file_b = unwrap_boxed(key_file, G_TYPE_KEY_FILE);
  if (key_file_b == nullptr) RETURN_THROWS();
  if (group_name != nullptr && !phpgtk::check_utf8(group_name, 2)) RETURN_THROWS();
  GError *error = nullptr;
  const gboolean ok =
      gtk_page_setup_load_key_file(self, static_cast<GKeyFile *>(key_file_b),
                                   group_name != nullptr ? ZSTR_VAL(group_name) : nullptr, &error);
  if (error != nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  RETURN_BOOL(ok);
}

/**
 * Gtk4\GtkPageSetup::set_bottom_margin(float $margin, GtkUnit $unit): void
 *
 * Sets the bottom margin of the `GtkPageSetup`.
 */
ZEND_METHOD(Gtk4_GtkPageSetup, set_bottom_margin) {
  double margin;
  zval *unit;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_DOUBLE(margin)
  Z_PARAM_OBJECT_OF_CLASS(unit, enum_class_for_type(GTK_TYPE_UNIT))
  ZEND_PARSE_PARAMETERS_END();
  GtkPageSetup *self = PHPGTK_SELF(GtkPageSetup, GTK_TYPE_PAGE_SETUP);
  gint unit_v = 0;
  if (!enum_from_php(unit, GTK_TYPE_UNIT, &unit_v)) RETURN_THROWS();
  const bool precondition_0 = unit_v != GTK_UNIT_NONE;
  if (!precondition_0) {
    zend_argument_value_error(2, "must not be GtkUnit::None");
    RETURN_THROWS();
  }
  gtk_page_setup_set_bottom_margin(self, margin, static_cast<GtkUnit>(unit_v));
}

/**
 * Gtk4\GtkPageSetup::set_left_margin(float $margin, GtkUnit $unit): void
 *
 * Sets the left margin of the `GtkPageSetup`.
 */
ZEND_METHOD(Gtk4_GtkPageSetup, set_left_margin) {
  double margin;
  zval *unit;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_DOUBLE(margin)
  Z_PARAM_OBJECT_OF_CLASS(unit, enum_class_for_type(GTK_TYPE_UNIT))
  ZEND_PARSE_PARAMETERS_END();
  GtkPageSetup *self = PHPGTK_SELF(GtkPageSetup, GTK_TYPE_PAGE_SETUP);
  gint unit_v = 0;
  if (!enum_from_php(unit, GTK_TYPE_UNIT, &unit_v)) RETURN_THROWS();
  const bool precondition_0 = unit_v != GTK_UNIT_NONE;
  if (!precondition_0) {
    zend_argument_value_error(2, "must not be GtkUnit::None");
    RETURN_THROWS();
  }
  gtk_page_setup_set_left_margin(self, margin, static_cast<GtkUnit>(unit_v));
}

/**
 * Gtk4\GtkPageSetup::set_orientation(GtkPageOrientation $orientation): void
 *
 * Sets the page orientation of the `GtkPageSetup`.
 */
ZEND_METHOD(Gtk4_GtkPageSetup, set_orientation) {
  zval *orientation;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(orientation, enum_class_for_type(GTK_TYPE_PAGE_ORIENTATION))
  ZEND_PARSE_PARAMETERS_END();
  GtkPageSetup *self = PHPGTK_SELF(GtkPageSetup, GTK_TYPE_PAGE_SETUP);
  gint orientation_v = 0;
  if (!enum_from_php(orientation, GTK_TYPE_PAGE_ORIENTATION, &orientation_v)) RETURN_THROWS();
  gtk_page_setup_set_orientation(self, static_cast<GtkPageOrientation>(orientation_v));
}

/**
 * Gtk4\GtkPageSetup::set_paper_size(GtkPaperSize $size): void
 *
 * Sets the paper size of the `GtkPageSetup` without changing the margins.
 */
ZEND_METHOD(Gtk4_GtkPageSetup, set_paper_size) {
  zval *size;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(size, boxed_class_for_type(GTK_TYPE_PAPER_SIZE)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkPageSetup *self = PHPGTK_SELF(GtkPageSetup, GTK_TYPE_PAGE_SETUP);
  gpointer size_b = unwrap_boxed(size, GTK_TYPE_PAPER_SIZE);
  if (size_b == nullptr) RETURN_THROWS();
  gtk_page_setup_set_paper_size(self, static_cast<GtkPaperSize *>(size_b));
}

/**
 * Gtk4\GtkPageSetup::set_paper_size_and_default_margins(GtkPaperSize $size): void
 *
 * Sets the paper size of the `GtkPageSetup` and modifies the margins according to the new paper
 * size.
 */
ZEND_METHOD(Gtk4_GtkPageSetup, set_paper_size_and_default_margins) {
  zval *size;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(size, boxed_class_for_type(GTK_TYPE_PAPER_SIZE)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkPageSetup *self = PHPGTK_SELF(GtkPageSetup, GTK_TYPE_PAGE_SETUP);
  gpointer size_b = unwrap_boxed(size, GTK_TYPE_PAPER_SIZE);
  if (size_b == nullptr) RETURN_THROWS();
  gtk_page_setup_set_paper_size_and_default_margins(self, static_cast<GtkPaperSize *>(size_b));
}

/**
 * Gtk4\GtkPageSetup::set_right_margin(float $margin, GtkUnit $unit): void
 *
 * Sets the right margin of the `GtkPageSetup`.
 */
ZEND_METHOD(Gtk4_GtkPageSetup, set_right_margin) {
  double margin;
  zval *unit;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_DOUBLE(margin)
  Z_PARAM_OBJECT_OF_CLASS(unit, enum_class_for_type(GTK_TYPE_UNIT))
  ZEND_PARSE_PARAMETERS_END();
  GtkPageSetup *self = PHPGTK_SELF(GtkPageSetup, GTK_TYPE_PAGE_SETUP);
  gint unit_v = 0;
  if (!enum_from_php(unit, GTK_TYPE_UNIT, &unit_v)) RETURN_THROWS();
  const bool precondition_0 = unit_v != GTK_UNIT_NONE;
  if (!precondition_0) {
    zend_argument_value_error(2, "must not be GtkUnit::None");
    RETURN_THROWS();
  }
  gtk_page_setup_set_right_margin(self, margin, static_cast<GtkUnit>(unit_v));
}

/**
 * Gtk4\GtkPageSetup::set_top_margin(float $margin, GtkUnit $unit): void
 *
 * Sets the top margin of the `GtkPageSetup`.
 */
ZEND_METHOD(Gtk4_GtkPageSetup, set_top_margin) {
  double margin;
  zval *unit;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_DOUBLE(margin)
  Z_PARAM_OBJECT_OF_CLASS(unit, enum_class_for_type(GTK_TYPE_UNIT))
  ZEND_PARSE_PARAMETERS_END();
  GtkPageSetup *self = PHPGTK_SELF(GtkPageSetup, GTK_TYPE_PAGE_SETUP);
  gint unit_v = 0;
  if (!enum_from_php(unit, GTK_TYPE_UNIT, &unit_v)) RETURN_THROWS();
  const bool precondition_0 = unit_v != GTK_UNIT_NONE;
  if (!precondition_0) {
    zend_argument_value_error(2, "must not be GtkUnit::None");
    RETURN_THROWS();
  }
  gtk_page_setup_set_top_margin(self, margin, static_cast<GtkUnit>(unit_v));
}

/**
 * Gtk4\GtkPageSetup::to_file(string $file_name): bool
 *
 * This function saves the information from $setup to $file_name.
 */
ZEND_METHOD(Gtk4_GtkPageSetup, to_file) {
  zend_string *file_name;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_PATH_STR(file_name)
  ZEND_PARSE_PARAMETERS_END();
  GtkPageSetup *self = PHPGTK_SELF(GtkPageSetup, GTK_TYPE_PAGE_SETUP);
  zend_string *file_name_abs = phpgtk::absolute_filename(file_name, 1);
  if (file_name_abs == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  const gboolean ok = gtk_page_setup_to_file(self, ZSTR_VAL(file_name_abs), &error);
  if (file_name_abs != nullptr) zend_string_release(file_name_abs);
  if (error != nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  RETURN_BOOL(ok);
}

/**
 * Gtk4\GtkPageSetup::to_gvariant(): mixed
 *
 * Serialize page setup to an a{sv} variant.
 */
ZEND_METHOD(Gtk4_GtkPageSetup, to_gvariant) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkPageSetup *self = PHPGTK_SELF(GtkPageSetup, GTK_TYPE_PAGE_SETUP);
  GVariant *phpgtk_ret = gtk_page_setup_to_gvariant(self);
  if (phpgtk_ret == nullptr) RETURN_NULL();
  variant_to_php(phpgtk_ret, return_value);
}

/**
 * Gtk4\GtkPageSetup::to_key_file(GKeyFile $key_file, ?string $group_name): void
 *
 * This function adds the page setup from $setup to $key_file.
 */
ZEND_METHOD(Gtk4_GtkPageSetup, to_key_file) {
  zval *key_file;
  zend_string *group_name = nullptr;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(key_file, boxed_class_for_type(G_TYPE_KEY_FILE)->ce)
  Z_PARAM_STR_OR_NULL(group_name)
  ZEND_PARSE_PARAMETERS_END();
  GtkPageSetup *self = PHPGTK_SELF(GtkPageSetup, GTK_TYPE_PAGE_SETUP);
  gpointer key_file_b = unwrap_boxed(key_file, G_TYPE_KEY_FILE);
  if (key_file_b == nullptr) RETURN_THROWS();
  if (group_name != nullptr && !phpgtk::check_utf8(group_name, 2)) RETURN_THROWS();
  gtk_page_setup_to_key_file(self, static_cast<GKeyFile *>(key_file_b),
                             group_name != nullptr ? ZSTR_VAL(group_name) : nullptr);
}
