// GENERATED by gen/gir.php from Gtk.DrawingArea - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkDrawingArea
#include "php_gtk4.h"
#include "core/object.h"
#include "core/subtype.h"
#include "core/error.h"
#include <array>

using namespace phpgtk;

// GtkDrawingAreaDrawFunc (notified scope) - the template for "notified" callbacks.
#include "Cairo/CairoContext.h"
#include "core/callback.h"
#include "core/teardown.h"

namespace {

// GtkDrawingAreaDrawFunc trampoline: (GtkDrawingArea, CairoContext, int, int) -> void.
void draw_func(GtkDrawingArea *area, cairo_t *cr, int width, int height, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  zval args[4];
  wrap(G_OBJECT(area), &args[0]);
  wrap_cairo(cr, &args[1]);
  ZVAL_LONG(&args[2], width);
  ZVAL_LONG(&args[3], height);
  zval retval;
  callback_invoke(cb, 4, args, &retval);
  zval_ptr_dtor(&retval);
  for (zval &a : args) zval_ptr_dtor(&a);
}

// GDestroyNotify: GTK dropped the draw func (replaced, widget finalized or RSHUTDOWN).
void draw_func_free(gpointer data) {
  teardown_untrack_notified(data);
  callback_free(data);
}

// teardown clear hook: make the area release its PHP callable.
void draw_func_clear(GObject *owner) {
  gtk_drawing_area_set_draw_func(GTK_DRAWING_AREA(owner), nullptr, nullptr, nullptr);
}

}  // namespace

/**
 * Gtk4\GtkDrawingArea::__construct()
 *
 * Creates a new drawing area.
 */
ZEND_METHOD(Gtk4_GtkDrawingArea, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gtk_drawing_area_new());
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkDrawingArea::get_content_height(): int
 *
 * Retrieves the content height of the `GtkDrawingArea`.
 */
ZEND_METHOD(Gtk4_GtkDrawingArea, get_content_height) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkDrawingArea *self = PHPGTK_SELF(GtkDrawingArea, GTK_TYPE_DRAWING_AREA);
  RETURN_LONG(static_cast<zend_long>(gtk_drawing_area_get_content_height(self)));
}

/**
 * Gtk4\GtkDrawingArea::get_content_width(): int
 *
 * Retrieves the content width of the `GtkDrawingArea`.
 */
ZEND_METHOD(Gtk4_GtkDrawingArea, get_content_width) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkDrawingArea *self = PHPGTK_SELF(GtkDrawingArea, GTK_TYPE_DRAWING_AREA);
  RETURN_LONG(static_cast<zend_long>(gtk_drawing_area_get_content_width(self)));
}

/**
 * Gtk4\GtkDrawingArea::set_content_height(int $height): void
 *
 * Sets the desired height of the contents of the drawing area.
 */
ZEND_METHOD(Gtk4_GtkDrawingArea, set_content_height) {
  zend_long height;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(height)
  ZEND_PARSE_PARAMETERS_END();
  GtkDrawingArea *self = PHPGTK_SELF(GtkDrawingArea, GTK_TYPE_DRAWING_AREA);
  gtk_drawing_area_set_content_height(self, static_cast<int>(height));
}

/**
 * Gtk4\GtkDrawingArea::set_content_width(int $width): void
 *
 * Sets the desired width of the contents of the drawing area.
 */
ZEND_METHOD(Gtk4_GtkDrawingArea, set_content_width) {
  zend_long width;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(width)
  ZEND_PARSE_PARAMETERS_END();
  GtkDrawingArea *self = PHPGTK_SELF(GtkDrawingArea, GTK_TYPE_DRAWING_AREA);
  gtk_drawing_area_set_content_width(self, static_cast<int>(width));
}

/**
 * public function set_draw_func(?callable $draw_func): void
 * Install (or with null, remove) the draw function: `function (GtkDrawingArea $area,
 * CairoContext $cr, int $width, int $height): void`. Kept until replaced or the widget dies.
 */
ZEND_METHOD(Gtk4_GtkDrawingArea, set_draw_func) {
  zend_fcall_info fci;
  zend_fcall_info_cache fcc;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_FUNC_OR_NULL(fci, fcc)
  ZEND_PARSE_PARAMETERS_END();
  GtkDrawingArea *area = PHPGTK_SELF(GtkDrawingArea, GTK_TYPE_DRAWING_AREA);
  if (!ZEND_FCI_INITIALIZED(fci)) {
    gtk_drawing_area_set_draw_func(area, nullptr, nullptr, nullptr);
  } else {
    Callback *cb = callback_new(&fci.function_name, "GtkDrawingArea::set_draw_func");
    teardown_track_notified(cb, G_OBJECT(area), draw_func_clear);
    gtk_drawing_area_set_draw_func(area, draw_func, cb, draw_func_free);
  }
  callback_drain();  // the previous draw func's notify ran inside the setter
}

// vfunc thunks and installers: file-local, installed by class_init of a PHP subtype
namespace {

// vfunc thunk: GTK_DRAWING_AREA_CLASS->resize -> $this->vfunc_resize() on a PHP subclass
void vfunc_thunk_resize(GtkDrawingArea *self, int width, int height) {
  zval zself;
  zend_function *fn =
      EG(exception) == nullptr ? subtype_vfunc(G_OBJECT(self), "vfunc_resize", &zself) : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    auto *native = GTK_DRAWING_AREA_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->resize != nullptr) native->resize(self, width, height);
    return;
  }
  std::array<zval, 2> args{};
  zval *argv = args.data();
  ZVAL_LONG(&argv[0], static_cast<zend_long>(width));
  ZVAL_LONG(&argv[1], static_cast<zend_long>(height));
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 2, args.data());
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkDrawingArea::vfunc_resize");
}

// vfunc installer: GTK_DRAWING_AREA_CLASS->resize (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_resize(gpointer klass) {
  GTK_DRAWING_AREA_CLASS(klass)->resize = vfunc_thunk_resize;
}

}  // namespace

/**
 * Gtk4\GtkDrawingArea::vfunc_resize(int $width, int $height): void
 *
 * Native `resize` (DrawingAreaClass.resize): the GTK implementation below any PHP subclass, for
 * `parent::vfunc_resize()` from an override.
 */
ZEND_METHOD(Gtk4_GtkDrawingArea, vfunc_resize) {
  zend_long width;
  zend_long height;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_LONG(width)
  Z_PARAM_LONG(height)
  ZEND_PARSE_PARAMETERS_END();
  GtkDrawingArea *self = PHPGTK_SELF(GtkDrawingArea, GTK_TYPE_DRAWING_AREA);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkDrawingArea::vfunc_resize(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_DRAWING_AREA_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->resize == nullptr) {
    return;
  }
  klass->resize(self, static_cast<int>(width), static_cast<int>(height));
}

// MINIT: the vfunc thunks of GtkDrawingArea (core/subtype.h).
void register_vfuncs_GtkDrawingArea() {
  register_vfunc(GTK_TYPE_DRAWING_AREA, "resize", vfunc_install_resize);
}
