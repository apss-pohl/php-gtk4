// GENERATED by gen/gir.php from Gtk.FlowBoxChild - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkFlowBoxChild
#include "php_gtk4.h"
#include "core/object.h"
#include "core/subtype.h"
#include "core/error.h"

using namespace phpgtk;

/**
 * Gtk4\GtkFlowBoxChild::__construct()
 *
 * Creates a new `GtkFlowBoxChild`.
 */
ZEND_METHOD(Gtk4_GtkFlowBoxChild, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gtk_flow_box_child_new());
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkFlowBoxChild::changed(): void
 *
 * Marks $child as changed, causing any state that depends on this to be updated.
 */
ZEND_METHOD(Gtk4_GtkFlowBoxChild, changed) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFlowBoxChild *self = PHPGTK_SELF(GtkFlowBoxChild, GTK_TYPE_FLOW_BOX_CHILD);
  gtk_flow_box_child_changed(self);
}

/**
 * Gtk4\GtkFlowBoxChild::get_child(): ?GtkWidget
 *
 * Gets the child widget of $self.
 */
ZEND_METHOD(Gtk4_GtkFlowBoxChild, get_child) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFlowBoxChild *self = PHPGTK_SELF(GtkFlowBoxChild, GTK_TYPE_FLOW_BOX_CHILD);
  GtkWidget *phpgtk_ret = gtk_flow_box_child_get_child(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkFlowBoxChild::get_index(): int
 *
 * Gets the current index of the $child in its `GtkFlowBox` container.
 */
ZEND_METHOD(Gtk4_GtkFlowBoxChild, get_index) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFlowBoxChild *self = PHPGTK_SELF(GtkFlowBoxChild, GTK_TYPE_FLOW_BOX_CHILD);
  RETURN_LONG(static_cast<zend_long>(gtk_flow_box_child_get_index(self)));
}

/**
 * Gtk4\GtkFlowBoxChild::is_selected(): bool
 *
 * Returns whether the $child is currently selected in its `GtkFlowBox` container.
 */
ZEND_METHOD(Gtk4_GtkFlowBoxChild, is_selected) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFlowBoxChild *self = PHPGTK_SELF(GtkFlowBoxChild, GTK_TYPE_FLOW_BOX_CHILD);
  RETURN_BOOL(gtk_flow_box_child_is_selected(self));
}

/**
 * Gtk4\GtkFlowBoxChild::set_child(?GtkWidget $child): void
 *
 * Sets the child widget of $self.
 */
ZEND_METHOD(Gtk4_GtkFlowBoxChild, set_child) {
  zval *child = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(child, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkFlowBoxChild *self = PHPGTK_SELF(GtkFlowBoxChild, GTK_TYPE_FLOW_BOX_CHILD);
  GObject *child_o = nullptr;
  if (child != nullptr) {
    child_o = unwrap(child, GTK_TYPE_WIDGET);
    if (child_o == nullptr) RETURN_THROWS();
  }
  gtk_flow_box_child_set_child(self, child_o != nullptr ? GTK_WIDGET(child_o) : nullptr);
}

// vfunc thunks and installers: file-local, installed by class_init of a PHP subtype
namespace {

// vfunc thunk: GTK_FLOW_BOX_CHILD_CLASS->activate -> $this->vfunc_activate() on a PHP subclass
void vfunc_thunk_activate(GtkFlowBoxChild *self) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_activate", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = GTK_FLOW_BOX_CHILD_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->activate != nullptr) native->activate(self);
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkFlowBoxChild::vfunc_activate");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: GTK_FLOW_BOX_CHILD_CLASS->activate (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_activate(gpointer klass) {
  GTK_FLOW_BOX_CHILD_CLASS(klass)->activate = vfunc_thunk_activate;
}

}  // namespace

/**
 * Gtk4\GtkFlowBoxChild::vfunc_activate(): void
 *
 * Native `activate` (FlowBoxChildClass.activate): the GTK implementation below any PHP subclass,
 * for `parent::vfunc_activate()` from an override.
 */
ZEND_METHOD(Gtk4_GtkFlowBoxChild, vfunc_activate) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFlowBoxChild *self = PHPGTK_SELF(GtkFlowBoxChild, GTK_TYPE_FLOW_BOX_CHILD);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkFlowBoxChild::vfunc_activate(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_FLOW_BOX_CHILD_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->activate == nullptr) {
    return;
  }
  klass->activate(self);
}

// MINIT: the vfunc thunks of GtkFlowBoxChild (core/subtype.h).
void register_vfuncs_GtkFlowBoxChild() {
  register_vfunc(GTK_TYPE_FLOW_BOX_CHILD, "activate", vfunc_install_activate);
}
