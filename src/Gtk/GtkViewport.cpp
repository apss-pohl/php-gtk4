// GENERATED by gen/gir.php from Gtk.Viewport - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkViewport
#include "php_gtk4.h"
#include "core/object.h"
#include "core/subtype.h"

using namespace phpgtk;

/**
 * Gtk4\GtkViewport::__construct(?GtkAdjustment $hadjustment = null, ?GtkAdjustment $vadjustment =
 * null)
 *
 * Creates a new `GtkViewport`.
 */
ZEND_METHOD(Gtk4_GtkViewport, __construct) {
  zval *hadjustment = nullptr;
  zval *vadjustment = nullptr;
  ZEND_PARSE_PARAMETERS_START(0, 2)
  Z_PARAM_OPTIONAL
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(hadjustment, class_for_gtype(GTK_TYPE_ADJUSTMENT))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(vadjustment, class_for_gtype(GTK_TYPE_ADJUSTMENT))
  ZEND_PARSE_PARAMETERS_END();
  GObject *hadjustment_o = nullptr;
  if (hadjustment != nullptr) {
    hadjustment_o = unwrap(hadjustment, GTK_TYPE_ADJUSTMENT);
    if (hadjustment_o == nullptr) RETURN_THROWS();
  }
  GObject *vadjustment_o = nullptr;
  if (vadjustment != nullptr) {
    vadjustment_o = unwrap(vadjustment, GTK_TYPE_ADJUSTMENT);
    if (vadjustment_o == nullptr) RETURN_THROWS();
  }
  GObject *obj = subtype_new(
      ZEND_THIS, "hadjustment", hadjustment_o != nullptr ? GTK_ADJUSTMENT(hadjustment_o) : nullptr,
      "vadjustment", vadjustment_o != nullptr ? GTK_ADJUSTMENT(vadjustment_o) : nullptr, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(
        gtk_viewport_new(hadjustment_o != nullptr ? GTK_ADJUSTMENT(hadjustment_o) : nullptr,
                         vadjustment_o != nullptr ? GTK_ADJUSTMENT(vadjustment_o) : nullptr));
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkViewport::get_child(): ?GtkWidget
 *
 * Gets the child widget of $viewport.
 */
ZEND_METHOD(Gtk4_GtkViewport, get_child) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkViewport *self = PHPGTK_SELF(GtkViewport, GTK_TYPE_VIEWPORT);
  GtkWidget *result = gtk_viewport_get_child(self);
  wrap(result != nullptr ? G_OBJECT(result) : nullptr, return_value);
}

/**
 * Gtk4\GtkViewport::get_scroll_to_focus(): bool
 *
 * Gets whether the viewport is scrolling to keep the focused child in view.
 */
ZEND_METHOD(Gtk4_GtkViewport, get_scroll_to_focus) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkViewport *self = PHPGTK_SELF(GtkViewport, GTK_TYPE_VIEWPORT);
  RETURN_BOOL(gtk_viewport_get_scroll_to_focus(self));
}

/**
 * Gtk4\GtkViewport::set_child(?GtkWidget $child): void
 *
 * Sets the child widget of $viewport.
 */
ZEND_METHOD(Gtk4_GtkViewport, set_child) {
  zval *child = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(child, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkViewport *self = PHPGTK_SELF(GtkViewport, GTK_TYPE_VIEWPORT);
  GObject *child_o = nullptr;
  if (child != nullptr) {
    child_o = unwrap(child, GTK_TYPE_WIDGET);
    if (child_o == nullptr) RETURN_THROWS();
  }
  gtk_viewport_set_child(self, child_o != nullptr ? GTK_WIDGET(child_o) : nullptr);
}

/**
 * Gtk4\GtkViewport::set_scroll_to_focus(bool $scroll_to_focus): void
 *
 * Sets whether the viewport should automatically scroll to keep the focused child in view.
 */
ZEND_METHOD(Gtk4_GtkViewport, set_scroll_to_focus) {
  bool scroll_to_focus;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(scroll_to_focus)
  ZEND_PARSE_PARAMETERS_END();
  GtkViewport *self = PHPGTK_SELF(GtkViewport, GTK_TYPE_VIEWPORT);
  gtk_viewport_set_scroll_to_focus(self, scroll_to_focus);
}
