// GENERATED by gen/gir.php from Gtk.ListView - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkListView
#include "php_gtk4.h"
#include "core/object.h"
#include "core/enums.h"
#include "core/boxed.h"
#include "core/subtype.h"

using namespace phpgtk;

/**
 * Gtk4\GtkListView::__construct(?GtkSelectionModel $model = null, ?GtkListItemFactory $factory =
 * null)
 *
 * Creates a new `GtkListView` that uses the given $factory for mapping items to widgets.
 */
ZEND_METHOD(Gtk4_GtkListView, __construct) {
  zval *model = nullptr;
  zval *factory = nullptr;
  ZEND_PARSE_PARAMETERS_START(0, 2)
  Z_PARAM_OPTIONAL
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(model, class_for_gtype(GTK_TYPE_SELECTION_MODEL))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(factory, class_for_gtype(GTK_TYPE_LIST_ITEM_FACTORY))
  ZEND_PARSE_PARAMETERS_END();
  GObject *model_o = nullptr;
  if (model != nullptr) {
    model_o = unwrap(model, GTK_TYPE_SELECTION_MODEL);
    if (model_o == nullptr) RETURN_THROWS();
  }
  if (model_o != nullptr) g_object_ref(model_o);  // transfer full
  GObject *factory_o = nullptr;
  if (factory != nullptr) {
    factory_o = unwrap(factory, GTK_TYPE_LIST_ITEM_FACTORY);
    if (factory_o == nullptr) RETURN_THROWS();
  }
  if (factory_o != nullptr) g_object_ref(factory_o);  // transfer full
  GObject *obj = subtype_new(
      ZEND_THIS, "model", model_o != nullptr ? GTK_SELECTION_MODEL(model_o) : nullptr, "factory",
      factory_o != nullptr ? GTK_LIST_ITEM_FACTORY(factory_o) : nullptr, nullptr);
  if (obj != nullptr) {
    if (model_o != nullptr) g_object_unref(model_o);
    if (factory_o != nullptr) g_object_unref(factory_o);
  }
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(
        gtk_list_view_new(model_o != nullptr ? GTK_SELECTION_MODEL(model_o) : nullptr,
                          factory_o != nullptr ? GTK_LIST_ITEM_FACTORY(factory_o) : nullptr));
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkListView::get_enable_rubberband(): bool
 *
 * Returns whether rows can be selected by dragging with the mouse.
 */
ZEND_METHOD(Gtk4_GtkListView, get_enable_rubberband) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkListView *self = PHPGTK_SELF(GtkListView, GTK_TYPE_LIST_VIEW);
  RETURN_BOOL(gtk_list_view_get_enable_rubberband(self));
}

/**
 * Gtk4\GtkListView::get_factory(): ?GtkListItemFactory
 *
 * Gets the factory that's currently used to populate list items.
 */
ZEND_METHOD(Gtk4_GtkListView, get_factory) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkListView *self = PHPGTK_SELF(GtkListView, GTK_TYPE_LIST_VIEW);
  GtkListItemFactory *phpgtk_ret = gtk_list_view_get_factory(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkListView::get_header_factory(): ?GtkListItemFactory
 *
 * Gets the factory that's currently used to populate section headers.
 */
ZEND_METHOD(Gtk4_GtkListView, get_header_factory) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkListView *self = PHPGTK_SELF(GtkListView, GTK_TYPE_LIST_VIEW);
  GtkListItemFactory *phpgtk_ret = gtk_list_view_get_header_factory(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkListView::get_model(): ?GtkSelectionModel
 *
 * Gets the model that's currently used to read the items displayed.
 */
ZEND_METHOD(Gtk4_GtkListView, get_model) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkListView *self = PHPGTK_SELF(GtkListView, GTK_TYPE_LIST_VIEW);
  GtkSelectionModel *phpgtk_ret = gtk_list_view_get_model(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkListView::get_show_separators(): bool
 *
 * Returns whether the list box should show separators between rows.
 */
ZEND_METHOD(Gtk4_GtkListView, get_show_separators) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkListView *self = PHPGTK_SELF(GtkListView, GTK_TYPE_LIST_VIEW);
  RETURN_BOOL(gtk_list_view_get_show_separators(self));
}

/**
 * Gtk4\GtkListView::get_single_click_activate(): bool
 *
 * Returns whether rows will be activated on single click and selected on hover.
 */
ZEND_METHOD(Gtk4_GtkListView, get_single_click_activate) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkListView *self = PHPGTK_SELF(GtkListView, GTK_TYPE_LIST_VIEW);
  RETURN_BOOL(gtk_list_view_get_single_click_activate(self));
}

/**
 * Gtk4\GtkListView::get_tab_behavior(): GtkListTabBehavior
 *
 * Gets the behavior set for the <kbd>Tab</kbd> key.
 */
ZEND_METHOD(Gtk4_GtkListView, get_tab_behavior) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkListView *self = PHPGTK_SELF(GtkListView, GTK_TYPE_LIST_VIEW);
  enum_to_php(GTK_TYPE_LIST_TAB_BEHAVIOR, gtk_list_view_get_tab_behavior(self), return_value);
}

/**
 * Gtk4\GtkListView::scroll_to(int $pos, int $flags, ?GtkScrollInfo $scroll): void
 *
 * Scrolls to the item at the given position and performs the actions specified in $flags.
 */
ZEND_METHOD(Gtk4_GtkListView, scroll_to) {
  zend_long pos;
  zend_long flags;
  zval *scroll = nullptr;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_LONG(pos)
  Z_PARAM_LONG(flags)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(scroll, boxed_class_for_type(GTK_TYPE_SCROLL_INFO)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkListView *self = PHPGTK_SELF(GtkListView, GTK_TYPE_LIST_VIEW);
  if (!phpgtk::check_range<guint>(pos, 1)) RETURN_THROWS();
  if (!phpgtk::check_flags(GTK_TYPE_LIST_SCROLL_FLAGS, flags, 2)) RETURN_THROWS();
  gpointer scroll_b = nullptr;
  if (scroll != nullptr) {
    scroll_b = unwrap_boxed(scroll, GTK_TYPE_SCROLL_INFO);
    if (scroll_b == nullptr) RETURN_THROWS();
  }
  if (scroll_b != nullptr) scroll_b = g_boxed_copy(GTK_TYPE_SCROLL_INFO, scroll_b);
  const bool precondition_0 =
      gtk_list_view_get_model(self) != nullptr &&
      pos < g_list_model_get_n_items(G_LIST_MODEL(gtk_list_view_get_model(self)));
  if (!precondition_0) {
    zend_argument_value_error(1, "must be the position of an item");
    RETURN_THROWS();
  }
  gtk_list_view_scroll_to(self, static_cast<guint>(pos), static_cast<GtkListScrollFlags>(flags),
                          static_cast<GtkScrollInfo *>(scroll_b));
}

/**
 * Gtk4\GtkListView::set_enable_rubberband(bool $enable_rubberband): void
 *
 * Sets whether selections can be changed by dragging with the mouse.
 */
ZEND_METHOD(Gtk4_GtkListView, set_enable_rubberband) {
  bool enable_rubberband;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(enable_rubberband)
  ZEND_PARSE_PARAMETERS_END();
  GtkListView *self = PHPGTK_SELF(GtkListView, GTK_TYPE_LIST_VIEW);
  gtk_list_view_set_enable_rubberband(self, enable_rubberband);
}

/**
 * Gtk4\GtkListView::set_factory(?GtkListItemFactory $factory): void
 *
 * Sets the `GtkListItemFactory` to use for populating list items.
 */
ZEND_METHOD(Gtk4_GtkListView, set_factory) {
  zval *factory = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(factory, class_for_gtype(GTK_TYPE_LIST_ITEM_FACTORY))
  ZEND_PARSE_PARAMETERS_END();
  GtkListView *self = PHPGTK_SELF(GtkListView, GTK_TYPE_LIST_VIEW);
  GObject *factory_o = nullptr;
  if (factory != nullptr) {
    factory_o = unwrap(factory, GTK_TYPE_LIST_ITEM_FACTORY);
    if (factory_o == nullptr) RETURN_THROWS();
  }
  gtk_list_view_set_factory(self,
                            factory_o != nullptr ? GTK_LIST_ITEM_FACTORY(factory_o) : nullptr);
}

/**
 * Gtk4\GtkListView::set_header_factory(?GtkListItemFactory $factory): void
 *
 * Sets the `GtkListItemFactory` to use for populating the `ListHeader` objects used in section
 * headers.
 */
ZEND_METHOD(Gtk4_GtkListView, set_header_factory) {
  zval *factory = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(factory, class_for_gtype(GTK_TYPE_LIST_ITEM_FACTORY))
  ZEND_PARSE_PARAMETERS_END();
  GtkListView *self = PHPGTK_SELF(GtkListView, GTK_TYPE_LIST_VIEW);
  GObject *factory_o = nullptr;
  if (factory != nullptr) {
    factory_o = unwrap(factory, GTK_TYPE_LIST_ITEM_FACTORY);
    if (factory_o == nullptr) RETURN_THROWS();
  }
  gtk_list_view_set_header_factory(
      self, factory_o != nullptr ? GTK_LIST_ITEM_FACTORY(factory_o) : nullptr);
}

/**
 * Gtk4\GtkListView::set_model(?GtkSelectionModel $model): void
 *
 * Sets the model to use.
 */
ZEND_METHOD(Gtk4_GtkListView, set_model) {
  zval *model = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(model, class_for_gtype(GTK_TYPE_SELECTION_MODEL))
  ZEND_PARSE_PARAMETERS_END();
  GtkListView *self = PHPGTK_SELF(GtkListView, GTK_TYPE_LIST_VIEW);
  GObject *model_o = nullptr;
  if (model != nullptr) {
    model_o = unwrap(model, GTK_TYPE_SELECTION_MODEL);
    if (model_o == nullptr) RETURN_THROWS();
  }
  gtk_list_view_set_model(self, model_o != nullptr ? GTK_SELECTION_MODEL(model_o) : nullptr);
}

/**
 * Gtk4\GtkListView::set_show_separators(bool $show_separators): void
 *
 * Sets whether the list box should show separators between rows.
 */
ZEND_METHOD(Gtk4_GtkListView, set_show_separators) {
  bool show_separators;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(show_separators)
  ZEND_PARSE_PARAMETERS_END();
  GtkListView *self = PHPGTK_SELF(GtkListView, GTK_TYPE_LIST_VIEW);
  gtk_list_view_set_show_separators(self, show_separators);
}

/**
 * Gtk4\GtkListView::set_single_click_activate(bool $single_click_activate): void
 *
 * Sets whether rows should be activated on single click and selected on hover.
 */
ZEND_METHOD(Gtk4_GtkListView, set_single_click_activate) {
  bool single_click_activate;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(single_click_activate)
  ZEND_PARSE_PARAMETERS_END();
  GtkListView *self = PHPGTK_SELF(GtkListView, GTK_TYPE_LIST_VIEW);
  gtk_list_view_set_single_click_activate(self, single_click_activate);
}

/**
 * Gtk4\GtkListView::set_tab_behavior(GtkListTabBehavior $tab_behavior): void
 *
 * Sets the behavior of the <kbd>Tab</kbd> and <kbd>Shift</kbd>+<kbd>Tab</kbd> keys.
 */
ZEND_METHOD(Gtk4_GtkListView, set_tab_behavior) {
  zval *tab_behavior;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(tab_behavior, enum_class_for_type(GTK_TYPE_LIST_TAB_BEHAVIOR))
  ZEND_PARSE_PARAMETERS_END();
  GtkListView *self = PHPGTK_SELF(GtkListView, GTK_TYPE_LIST_VIEW);
  gint tab_behavior_v = 0;
  if (!enum_from_php(tab_behavior, GTK_TYPE_LIST_TAB_BEHAVIOR, &tab_behavior_v)) RETURN_THROWS();
  gtk_list_view_set_tab_behavior(self, static_cast<GtkListTabBehavior>(tab_behavior_v));
}
