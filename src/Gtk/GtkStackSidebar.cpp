// GENERATED by gen/gir.php from Gtk.StackSidebar - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkStackSidebar
#include "php_gtk4.h"
#include "core/object.h"
#include "core/subtype.h"

using namespace phpgtk;

/**
 * Gtk4\GtkStackSidebar::__construct()
 *
 * Creates a new `GtkStackSidebar`.
 */
ZEND_METHOD(Gtk4_GtkStackSidebar, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gtk_stack_sidebar_new());
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkStackSidebar::get_stack(): ?GtkStack
 *
 * Retrieves the stack.
 */
ZEND_METHOD(Gtk4_GtkStackSidebar, get_stack) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkStackSidebar *self = PHPGTK_SELF(GtkStackSidebar, GTK_TYPE_STACK_SIDEBAR);
  GtkStack *phpgtk_ret = gtk_stack_sidebar_get_stack(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkStackSidebar::set_stack(GtkStack $stack): void
 *
 * Set the `GtkStack` associated with this `GtkStackSidebar`.
 */
ZEND_METHOD(Gtk4_GtkStackSidebar, set_stack) {
  zval *stack;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(stack, class_for_gtype(GTK_TYPE_STACK))
  ZEND_PARSE_PARAMETERS_END();
  GtkStackSidebar *self = PHPGTK_SELF(GtkStackSidebar, GTK_TYPE_STACK_SIDEBAR);
  GObject *stack_o = unwrap(stack, GTK_TYPE_STACK);
  if (stack_o == nullptr) RETURN_THROWS();
  gtk_stack_sidebar_set_stack(self, GTK_STACK(stack_o));
}
