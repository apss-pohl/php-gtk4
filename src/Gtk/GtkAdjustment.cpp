// GENERATED by gen/gir.php from Gtk.Adjustment - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkAdjustment
#include "php_gtk4.h"
#include "core/object.h"
#include "core/subtype.h"
#include "core/error.h"

using namespace phpgtk;

/**
 * Gtk4\GtkAdjustment::__construct(float $value, float $lower, float $upper, float $step_increment,
 * float $page_increment, float $page_size)
 *
 * Creates a new `GtkAdjustment`.
 */
ZEND_METHOD(Gtk4_GtkAdjustment, __construct) {
  double value;
  double lower;
  double upper;
  double step_increment;
  double page_increment;
  double page_size;
  ZEND_PARSE_PARAMETERS_START(6, 6)
  Z_PARAM_DOUBLE(value)
  Z_PARAM_DOUBLE(lower)
  Z_PARAM_DOUBLE(upper)
  Z_PARAM_DOUBLE(step_increment)
  Z_PARAM_DOUBLE(page_increment)
  Z_PARAM_DOUBLE(page_size)
  ZEND_PARSE_PARAMETERS_END();
  GObject *obj = subtype_new(ZEND_THIS, "value", value, "lower", lower, "upper", upper,
                             "step-increment", step_increment, "page-increment", page_increment,
                             "page-size", page_size, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(
        gtk_adjustment_new(value, lower, upper, step_increment, page_increment, page_size));
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkAdjustment::clamp_page(float $lower, float $upper): void
 *
 * Updates the value property to ensure that the range between $lower and $upper is in the current
 * page.
 */
ZEND_METHOD(Gtk4_GtkAdjustment, clamp_page) {
  double lower;
  double upper;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_DOUBLE(lower)
  Z_PARAM_DOUBLE(upper)
  ZEND_PARSE_PARAMETERS_END();
  GtkAdjustment *self = PHPGTK_SELF(GtkAdjustment, GTK_TYPE_ADJUSTMENT);
  gtk_adjustment_clamp_page(self, lower, upper);
}

/**
 * Gtk4\GtkAdjustment::configure(float $value, float $lower, float $upper, float $step_increment,
 * float $page_increment, float $page_size): void
 *
 * Sets all properties of the adjustment at once.
 */
ZEND_METHOD(Gtk4_GtkAdjustment, configure) {
  double value;
  double lower;
  double upper;
  double step_increment;
  double page_increment;
  double page_size;
  ZEND_PARSE_PARAMETERS_START(6, 6)
  Z_PARAM_DOUBLE(value)
  Z_PARAM_DOUBLE(lower)
  Z_PARAM_DOUBLE(upper)
  Z_PARAM_DOUBLE(step_increment)
  Z_PARAM_DOUBLE(page_increment)
  Z_PARAM_DOUBLE(page_size)
  ZEND_PARSE_PARAMETERS_END();
  GtkAdjustment *self = PHPGTK_SELF(GtkAdjustment, GTK_TYPE_ADJUSTMENT);
  gtk_adjustment_configure(self, value, lower, upper, step_increment, page_increment, page_size);
}

/**
 * Gtk4\GtkAdjustment::get_lower(): float
 *
 * Retrieves the minimum value of the adjustment.
 */
ZEND_METHOD(Gtk4_GtkAdjustment, get_lower) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkAdjustment *self = PHPGTK_SELF(GtkAdjustment, GTK_TYPE_ADJUSTMENT);
  RETURN_DOUBLE(gtk_adjustment_get_lower(self));
}

/**
 * Gtk4\GtkAdjustment::get_minimum_increment(): float
 *
 * Gets the smaller of step increment and page increment.
 */
ZEND_METHOD(Gtk4_GtkAdjustment, get_minimum_increment) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkAdjustment *self = PHPGTK_SELF(GtkAdjustment, GTK_TYPE_ADJUSTMENT);
  RETURN_DOUBLE(gtk_adjustment_get_minimum_increment(self));
}

/**
 * Gtk4\GtkAdjustment::get_page_increment(): float
 *
 * Retrieves the page increment of the adjustment.
 */
ZEND_METHOD(Gtk4_GtkAdjustment, get_page_increment) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkAdjustment *self = PHPGTK_SELF(GtkAdjustment, GTK_TYPE_ADJUSTMENT);
  RETURN_DOUBLE(gtk_adjustment_get_page_increment(self));
}

/**
 * Gtk4\GtkAdjustment::get_page_size(): float
 *
 * Retrieves the page size of the adjustment.
 */
ZEND_METHOD(Gtk4_GtkAdjustment, get_page_size) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkAdjustment *self = PHPGTK_SELF(GtkAdjustment, GTK_TYPE_ADJUSTMENT);
  RETURN_DOUBLE(gtk_adjustment_get_page_size(self));
}

/**
 * Gtk4\GtkAdjustment::get_step_increment(): float
 *
 * Retrieves the step increment of the adjustment.
 */
ZEND_METHOD(Gtk4_GtkAdjustment, get_step_increment) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkAdjustment *self = PHPGTK_SELF(GtkAdjustment, GTK_TYPE_ADJUSTMENT);
  RETURN_DOUBLE(gtk_adjustment_get_step_increment(self));
}

/**
 * Gtk4\GtkAdjustment::get_upper(): float
 *
 * Retrieves the maximum value of the adjustment.
 */
ZEND_METHOD(Gtk4_GtkAdjustment, get_upper) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkAdjustment *self = PHPGTK_SELF(GtkAdjustment, GTK_TYPE_ADJUSTMENT);
  RETURN_DOUBLE(gtk_adjustment_get_upper(self));
}

/**
 * Gtk4\GtkAdjustment::get_value(): float
 *
 * Gets the current value of the adjustment.
 */
ZEND_METHOD(Gtk4_GtkAdjustment, get_value) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkAdjustment *self = PHPGTK_SELF(GtkAdjustment, GTK_TYPE_ADJUSTMENT);
  RETURN_DOUBLE(gtk_adjustment_get_value(self));
}

/**
 * Gtk4\GtkAdjustment::set_lower(float $lower): void
 *
 * Sets the minimum value of the adjustment.
 */
ZEND_METHOD(Gtk4_GtkAdjustment, set_lower) {
  double lower;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_DOUBLE(lower)
  ZEND_PARSE_PARAMETERS_END();
  GtkAdjustment *self = PHPGTK_SELF(GtkAdjustment, GTK_TYPE_ADJUSTMENT);
  gtk_adjustment_set_lower(self, lower);
}

/**
 * Gtk4\GtkAdjustment::set_page_increment(float $page_increment): void
 *
 * Sets the page increment of the adjustment.
 */
ZEND_METHOD(Gtk4_GtkAdjustment, set_page_increment) {
  double page_increment;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_DOUBLE(page_increment)
  ZEND_PARSE_PARAMETERS_END();
  GtkAdjustment *self = PHPGTK_SELF(GtkAdjustment, GTK_TYPE_ADJUSTMENT);
  gtk_adjustment_set_page_increment(self, page_increment);
}

/**
 * Gtk4\GtkAdjustment::set_page_size(float $page_size): void
 *
 * Sets the page size of the adjustment.
 */
ZEND_METHOD(Gtk4_GtkAdjustment, set_page_size) {
  double page_size;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_DOUBLE(page_size)
  ZEND_PARSE_PARAMETERS_END();
  GtkAdjustment *self = PHPGTK_SELF(GtkAdjustment, GTK_TYPE_ADJUSTMENT);
  gtk_adjustment_set_page_size(self, page_size);
}

/**
 * Gtk4\GtkAdjustment::set_step_increment(float $step_increment): void
 *
 * Sets the step increment of the adjustment.
 */
ZEND_METHOD(Gtk4_GtkAdjustment, set_step_increment) {
  double step_increment;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_DOUBLE(step_increment)
  ZEND_PARSE_PARAMETERS_END();
  GtkAdjustment *self = PHPGTK_SELF(GtkAdjustment, GTK_TYPE_ADJUSTMENT);
  gtk_adjustment_set_step_increment(self, step_increment);
}

/**
 * Gtk4\GtkAdjustment::set_upper(float $upper): void
 *
 * Sets the maximum value of the adjustment.
 */
ZEND_METHOD(Gtk4_GtkAdjustment, set_upper) {
  double upper;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_DOUBLE(upper)
  ZEND_PARSE_PARAMETERS_END();
  GtkAdjustment *self = PHPGTK_SELF(GtkAdjustment, GTK_TYPE_ADJUSTMENT);
  gtk_adjustment_set_upper(self, upper);
}

/**
 * Gtk4\GtkAdjustment::set_value(float $value): void
 *
 * Sets the `GtkAdjustment` value.
 */
ZEND_METHOD(Gtk4_GtkAdjustment, set_value) {
  double value;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_DOUBLE(value)
  ZEND_PARSE_PARAMETERS_END();
  GtkAdjustment *self = PHPGTK_SELF(GtkAdjustment, GTK_TYPE_ADJUSTMENT);
  gtk_adjustment_set_value(self, value);
}

// vfunc thunks and installers: file-local, installed by class_init of a PHP subtype
namespace {

// vfunc thunk: GTK_ADJUSTMENT_CLASS->changed -> $this->vfunc_changed() on a PHP subclass
void vfunc_thunk_changed(GtkAdjustment *self) {
  zval zself;
  zend_function *fn =
      EG(exception) == nullptr ? subtype_vfunc(G_OBJECT(self), "vfunc_changed", &zself) : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    auto *native = GTK_ADJUSTMENT_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->changed != nullptr) native->changed(self);
    return;
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkAdjustment::vfunc_changed");
}

// vfunc installer: GTK_ADJUSTMENT_CLASS->changed (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_changed(gpointer klass) {
  GTK_ADJUSTMENT_CLASS(klass)->changed = vfunc_thunk_changed;
}

// vfunc thunk: GTK_ADJUSTMENT_CLASS->value_changed -> $this->vfunc_value_changed() on a PHP
// subclass
void vfunc_thunk_value_changed(GtkAdjustment *self) {
  zval zself;
  zend_function *fn = EG(exception) == nullptr
                          ? subtype_vfunc(G_OBJECT(self), "vfunc_value_changed", &zself)
                          : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    auto *native = GTK_ADJUSTMENT_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->value_changed != nullptr) native->value_changed(self);
    return;
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkAdjustment::vfunc_value_changed");
}

// vfunc installer: GTK_ADJUSTMENT_CLASS->value_changed (called from class_init / iface_init of a
// PHP subtype)
void vfunc_install_value_changed(gpointer klass) {
  GTK_ADJUSTMENT_CLASS(klass)->value_changed = vfunc_thunk_value_changed;
}

}  // namespace

/**
 * Gtk4\GtkAdjustment::vfunc_changed(): void
 *
 * Native `changed` (AdjustmentClass.changed): the GTK implementation below any PHP subclass, for
 * `parent::vfunc_changed()` from an override.
 */
ZEND_METHOD(Gtk4_GtkAdjustment, vfunc_changed) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkAdjustment *self = PHPGTK_SELF(GtkAdjustment, GTK_TYPE_ADJUSTMENT);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkAdjustment::vfunc_changed(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_ADJUSTMENT_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->changed == nullptr) {
    return;
  }
  klass->changed(self);
}

/**
 * Gtk4\GtkAdjustment::vfunc_value_changed(): void
 *
 * Native `value_changed` (AdjustmentClass.value_changed): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_value_changed()` from an override.
 */
ZEND_METHOD(Gtk4_GtkAdjustment, vfunc_value_changed) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkAdjustment *self = PHPGTK_SELF(GtkAdjustment, GTK_TYPE_ADJUSTMENT);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkAdjustment::vfunc_value_changed(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_ADJUSTMENT_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->value_changed == nullptr) {
    return;
  }
  klass->value_changed(self);
}

// MINIT: the vfunc thunks of GtkAdjustment (core/subtype.h).
void register_vfuncs_GtkAdjustment() {
  register_vfunc(GTK_TYPE_ADJUSTMENT, "changed", vfunc_install_changed);
  register_vfunc(GTK_TYPE_ADJUSTMENT, "value_changed", vfunc_install_value_changed);
}
