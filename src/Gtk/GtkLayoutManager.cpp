// GENERATED by gen/gir.php from Gtk.LayoutManager - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkLayoutManager
#include "php_gtk4.h"
#include "core/object.h"
#include "core/enums.h"
#include "core/subtype.h"
#include "core/error.h"
#include <array>

using namespace phpgtk;

/**
 * Gtk4\GtkLayoutManager::__construct()
 *
 * GtkLayoutManager is abstract in GTK: `new` only works on a PHP subclass (which gets its own
 * GType).
 */
ZEND_METHOD(Gtk4_GtkLayoutManager, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) == nullptr) {
      zend_throw_error(nullptr,
                       "GtkLayoutManager is abstract in GTK: subclass it in PHP (new MyClass())");
    }
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkLayoutManager::allocate(GtkWidget $widget, int $width, int $height, int $baseline): void
 *
 * Assigns the given $width, $height, and $baseline to a $widget, and computes the position and
 * sizes of the children of the $widget using the layout management policy of $manager.
 */
ZEND_METHOD(Gtk4_GtkLayoutManager, allocate) {
  zval *widget;
  zend_long width;
  zend_long height;
  zend_long baseline;
  ZEND_PARSE_PARAMETERS_START(4, 4)
  Z_PARAM_OBJECT_OF_CLASS(widget, class_for_gtype(GTK_TYPE_WIDGET))
  Z_PARAM_LONG(width)
  Z_PARAM_LONG(height)
  Z_PARAM_LONG(baseline)
  ZEND_PARSE_PARAMETERS_END();
  GtkLayoutManager *self = PHPGTK_SELF(GtkLayoutManager, GTK_TYPE_LAYOUT_MANAGER);
  GObject *widget_o = unwrap(widget, GTK_TYPE_WIDGET);
  if (widget_o == nullptr) RETURN_THROWS();
  if (!phpgtk::check_range<int>(width, 2)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(height, 3)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(baseline, 4)) RETURN_THROWS();
  gtk_layout_manager_allocate(self, GTK_WIDGET(widget_o), static_cast<int>(width),
                              static_cast<int>(height), static_cast<int>(baseline));
}

/**
 * Gtk4\GtkLayoutManager::get_widget(): ?GtkWidget
 *
 * Retrieves the `GtkWidget` using the given `GtkLayoutManager`.
 */
ZEND_METHOD(Gtk4_GtkLayoutManager, get_widget) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkLayoutManager *self = PHPGTK_SELF(GtkLayoutManager, GTK_TYPE_LAYOUT_MANAGER);
  GtkWidget *phpgtk_ret = gtk_layout_manager_get_widget(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkLayoutManager::layout_changed(): void
 *
 * Queues a resize on the `GtkWidget` using $manager, if any.
 */
ZEND_METHOD(Gtk4_GtkLayoutManager, layout_changed) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkLayoutManager *self = PHPGTK_SELF(GtkLayoutManager, GTK_TYPE_LAYOUT_MANAGER);
  gtk_layout_manager_layout_changed(self);
}

/**
 * Gtk4\GtkLayoutManager::measure(GtkWidget $widget, GtkOrientation $orientation, int $for_size):
 * array
 *
 * Measures the size of the $widget using $manager, for the given $orientation and size.
 */
ZEND_METHOD(Gtk4_GtkLayoutManager, measure) {
  zval *widget;
  zval *orientation;
  zend_long for_size;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_OBJECT_OF_CLASS(widget, class_for_gtype(GTK_TYPE_WIDGET))
  Z_PARAM_OBJECT_OF_CLASS(orientation, enum_class_for_type(GTK_TYPE_ORIENTATION))
  Z_PARAM_LONG(for_size)
  ZEND_PARSE_PARAMETERS_END();
  GtkLayoutManager *self = PHPGTK_SELF(GtkLayoutManager, GTK_TYPE_LAYOUT_MANAGER);
  GObject *widget_o = unwrap(widget, GTK_TYPE_WIDGET);
  if (widget_o == nullptr) RETURN_THROWS();
  gint orientation_v = 0;
  if (!enum_from_php(orientation, GTK_TYPE_ORIENTATION, &orientation_v)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(for_size, 3)) RETURN_THROWS();
  int minimum = 0;
  int natural = 0;
  int minimum_baseline = 0;
  int natural_baseline = 0;
  gtk_layout_manager_measure(self, GTK_WIDGET(widget_o), static_cast<GtkOrientation>(orientation_v),
                             static_cast<int>(for_size), &minimum, &natural, &minimum_baseline,
                             &natural_baseline);
  array_init_size(return_value, 4);
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(minimum));
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(natural));
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(minimum_baseline));
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(natural_baseline));
    add_next_index_zval(return_value, &item);
  }
}

/**
 * public function get_layout_child(GtkWidget $child): GtkLayoutChild
 * Retrieves a `GtkLayoutChild` instance for the `GtkLayoutManager`, creating one if necessary.
 *
 * GTK looks the manager up through the child's parent: a child with no parent is a
 * `g_return_val_if_fail` that answers NULL, which the declared return type does not allow.
 */
ZEND_METHOD(Gtk4_GtkLayoutManager, get_layout_child) {
  zval *zchild;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(zchild, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkLayoutManager *self = PHPGTK_SELF(GtkLayoutManager, GTK_TYPE_LAYOUT_MANAGER);
  GObject *child_o = unwrap(zchild, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  GtkWidget *child = GTK_WIDGET(child_o);
  GtkWidget *parent = gtk_widget_get_parent(child);
  if (parent == nullptr) {
    zend_throw_exception(spl_ce_LogicException,
                         "Gtk4\\GtkLayoutManager::get_layout_child(): the child has no parent, so "
                         "no layout manager holds it",
                         0);
    RETURN_THROWS();
  }
  // ... and it has to be *this* manager's child: GTK checks the parent's manager and CRITICALs
  // ("does not use the given layout manager of type ...") before answering NULL.
  if (gtk_widget_get_layout_manager(parent) != self) {
    zend_throw_exception(spl_ce_LogicException,
                         "Gtk4\\GtkLayoutManager::get_layout_child(): the child's parent is laid "
                         "out by a different manager",
                         0);
    RETURN_THROWS();
  }
  GtkLayoutChild *lc = gtk_layout_manager_get_layout_child(self, child);
  if (lc == nullptr) {
    zend_throw_error(nullptr, "Gtk4\\GtkLayoutManager::get_layout_child(): GTK returned no child");
    RETURN_THROWS();
  }
  wrap(G_OBJECT(lc), return_value);
}

/**
 * public function get_request_mode(): GtkSizeRequestMode
 * Retrieves the request mode of $manager.
 *
 * GTK reads the mode off the widget the manager was set on: without one it is a
 * `g_return_val_if_fail` that tells PHP nothing and answers with the first enum value.
 */
ZEND_METHOD(Gtk4_GtkLayoutManager, get_request_mode) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkLayoutManager *self = PHPGTK_SELF(GtkLayoutManager, GTK_TYPE_LAYOUT_MANAGER);
  if (gtk_layout_manager_get_widget(self) == nullptr) {
    zend_throw_exception(spl_ce_LogicException,
                         "Gtk4\\GtkLayoutManager::get_request_mode(): the manager is not set on "
                         "a widget yet",
                         0);
    RETURN_THROWS();
  }
  enum_to_php(GTK_TYPE_SIZE_REQUEST_MODE, gtk_layout_manager_get_request_mode(self), return_value);
}

// vfunc thunks and installers: file-local, installed by class_init of a PHP subtype
namespace {

// vfunc thunk: GTK_LAYOUT_MANAGER_CLASS->allocate -> $this->vfunc_allocate() on a PHP subclass
void vfunc_thunk_allocate(GtkLayoutManager *self, GtkWidget *widget, int width, int height,
                          int baseline) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_allocate", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = GTK_LAYOUT_MANAGER_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->allocate != nullptr) native->allocate(self, widget, width, height, baseline);
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 4> args{};
  zval *argv = args.data();
  wrap(widget != nullptr ? G_OBJECT(widget) : nullptr, &argv[0]);
  ZVAL_LONG(&argv[1], static_cast<zend_long>(width));
  ZVAL_LONG(&argv[2], static_cast<zend_long>(height));
  ZVAL_LONG(&argv[3], static_cast<zend_long>(baseline));
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 4, args.data());
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkLayoutManager::vfunc_allocate");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: GTK_LAYOUT_MANAGER_CLASS->allocate (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_allocate(gpointer klass) {
  GTK_LAYOUT_MANAGER_CLASS(klass)->allocate = vfunc_thunk_allocate;
}

// vfunc thunk: GTK_LAYOUT_MANAGER_CLASS->create_layout_child -> $this->vfunc_create_layout_child()
// on a PHP subclass
GtkLayoutChild *vfunc_thunk_create_layout_child(GtkLayoutManager *self, GtkWidget *widget,
                                                GtkWidget *for_child) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_create_layout_child", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = GTK_LAYOUT_MANAGER_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->create_layout_child != nullptr
               ? native->create_layout_child(self, widget, for_child)
               : nullptr;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(widget != nullptr ? G_OBJECT(widget) : nullptr, &argv[0]);
  wrap(for_child != nullptr ? G_OBJECT(for_child) : nullptr, &argv[1]);
  zval ret;
  ZVAL_UNDEF(&ret);
  GtkLayoutChild *result = nullptr;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 2, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    if (Z_TYPE(ret) == IS_OBJECT) {
      GObject *o = unwrap(&ret, GTK_TYPE_LAYOUT_CHILD);
      result = o != nullptr ? GTK_LAYOUT_CHILD(o) : nullptr;
      if (result != nullptr) g_object_ref(result);  // transfer full
    }
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkLayoutManager::vfunc_create_layout_child");
  zend_exception_restore();  // the parked one, previous of whatever this threw
  return result;
}

// vfunc installer: GTK_LAYOUT_MANAGER_CLASS->create_layout_child (called from class_init /
// iface_init of a PHP subtype)
void vfunc_install_create_layout_child(gpointer klass) {
  GTK_LAYOUT_MANAGER_CLASS(klass)->create_layout_child = vfunc_thunk_create_layout_child;
}

// vfunc thunk: GTK_LAYOUT_MANAGER_CLASS->get_request_mode -> $this->vfunc_get_request_mode() on a
// PHP subclass
GtkSizeRequestMode vfunc_thunk_get_request_mode(GtkLayoutManager *self, GtkWidget *widget) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_get_request_mode", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = GTK_LAYOUT_MANAGER_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->get_request_mode != nullptr ? native->get_request_mode(self, widget)
                                               : static_cast<GtkSizeRequestMode>(0);
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 1> args{};
  zval *argv = args.data();
  wrap(widget != nullptr ? G_OBJECT(widget) : nullptr, &argv[0]);
  zval ret;
  ZVAL_UNDEF(&ret);
  GtkSizeRequestMode result = static_cast<GtkSizeRequestMode>(0);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    gint v = 0;
    if (enum_from_php(&ret, GTK_TYPE_SIZE_REQUEST_MODE, &v))
      result = static_cast<GtkSizeRequestMode>(v);
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkLayoutManager::vfunc_get_request_mode");
  zend_exception_restore();  // the parked one, previous of whatever this threw
  return result;
}

// vfunc installer: GTK_LAYOUT_MANAGER_CLASS->get_request_mode (called from class_init / iface_init
// of a PHP subtype)
void vfunc_install_get_request_mode(gpointer klass) {
  GTK_LAYOUT_MANAGER_CLASS(klass)->get_request_mode = vfunc_thunk_get_request_mode;
}

// vfunc thunk: GTK_LAYOUT_MANAGER_CLASS->measure -> $this->vfunc_measure() on a PHP subclass
void vfunc_thunk_measure(GtkLayoutManager *self, GtkWidget *widget, GtkOrientation orientation,
                         int for_size, int *minimum, int *natural, int *minimum_baseline,
                         int *natural_baseline) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_measure", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = GTK_LAYOUT_MANAGER_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->measure != nullptr)
      native->measure(self, widget, orientation, for_size, minimum, natural, minimum_baseline,
                      natural_baseline);
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 3> args{};
  zval *argv = args.data();
  wrap(widget != nullptr ? G_OBJECT(widget) : nullptr, &argv[0]);
  enum_to_php(GTK_TYPE_ORIENTATION, static_cast<gint>(orientation), &argv[1]);
  ZVAL_LONG(&argv[2], static_cast<zend_long>(for_size));
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 3, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    if (Z_TYPE(ret) == IS_ARRAY) {
      if (zval *e0 = zend_hash_index_find(Z_ARRVAL(ret), 0); e0 != nullptr && minimum != nullptr) {
        *minimum = phpgtk::check_range<int>(zval_get_long(e0), 0)
                       ? static_cast<int>(zval_get_long(e0))
                       : *minimum;
      }
      if (zval *e1 = zend_hash_index_find(Z_ARRVAL(ret), 1); e1 != nullptr && natural != nullptr) {
        *natural = phpgtk::check_range<int>(zval_get_long(e1), 0)
                       ? static_cast<int>(zval_get_long(e1))
                       : *natural;
      }
      if (zval *e2 = zend_hash_index_find(Z_ARRVAL(ret), 2);
          e2 != nullptr && minimum_baseline != nullptr) {
        *minimum_baseline = phpgtk::check_range<int>(zval_get_long(e2), 0)
                                ? static_cast<int>(zval_get_long(e2))
                                : *minimum_baseline;
      }
      if (zval *e3 = zend_hash_index_find(Z_ARRVAL(ret), 3);
          e3 != nullptr && natural_baseline != nullptr) {
        *natural_baseline = phpgtk::check_range<int>(zval_get_long(e3), 0)
                                ? static_cast<int>(zval_get_long(e3))
                                : *natural_baseline;
      }
    }
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkLayoutManager::vfunc_measure");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: GTK_LAYOUT_MANAGER_CLASS->measure (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_measure(gpointer klass) {
  GTK_LAYOUT_MANAGER_CLASS(klass)->measure = vfunc_thunk_measure;
}

// vfunc thunk: GTK_LAYOUT_MANAGER_CLASS->root -> $this->vfunc_root() on a PHP subclass
void vfunc_thunk_root(GtkLayoutManager *self) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_root", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = GTK_LAYOUT_MANAGER_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->root != nullptr) native->root(self);
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkLayoutManager::vfunc_root");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: GTK_LAYOUT_MANAGER_CLASS->root (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_root(gpointer klass) {
  GTK_LAYOUT_MANAGER_CLASS(klass)->root = vfunc_thunk_root;
}

// vfunc thunk: GTK_LAYOUT_MANAGER_CLASS->unroot -> $this->vfunc_unroot() on a PHP subclass
void vfunc_thunk_unroot(GtkLayoutManager *self) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_unroot", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = GTK_LAYOUT_MANAGER_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->unroot != nullptr) native->unroot(self);
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkLayoutManager::vfunc_unroot");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: GTK_LAYOUT_MANAGER_CLASS->unroot (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_unroot(gpointer klass) {
  GTK_LAYOUT_MANAGER_CLASS(klass)->unroot = vfunc_thunk_unroot;
}

}  // namespace

/**
 * Gtk4\GtkLayoutManager::vfunc_allocate(GtkWidget $widget, int $width, int $height, int $baseline):
 * void
 *
 * Native `allocate` (LayoutManagerClass.allocate): the GTK implementation below any PHP subclass,
 * for `parent::vfunc_allocate()` from an override. Assigns the given $width, $height, and
 * $baseline to a $widget, and computes the position and sizes of the children of the $widget using
 * the layout management policy of $manager.
 */
ZEND_METHOD(Gtk4_GtkLayoutManager, vfunc_allocate) {
  zval *widget;
  zend_long width;
  zend_long height;
  zend_long baseline;
  ZEND_PARSE_PARAMETERS_START(4, 4)
  Z_PARAM_OBJECT_OF_CLASS(widget, class_for_gtype(GTK_TYPE_WIDGET))
  Z_PARAM_LONG(width)
  Z_PARAM_LONG(height)
  Z_PARAM_LONG(baseline)
  ZEND_PARSE_PARAMETERS_END();
  GtkLayoutManager *self = PHPGTK_SELF(GtkLayoutManager, GTK_TYPE_LAYOUT_MANAGER);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkLayoutManager::vfunc_allocate(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_LAYOUT_MANAGER_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->allocate == nullptr) {
    return;
  }
  GObject *widget_o = unwrap(widget, GTK_TYPE_WIDGET);
  if (widget_o == nullptr) RETURN_THROWS();
  if (!phpgtk::check_range<int>(width, 2)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(height, 3)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(baseline, 4)) RETURN_THROWS();
  klass->allocate(self, GTK_WIDGET(widget_o), static_cast<int>(width), static_cast<int>(height),
                  static_cast<int>(baseline));
}

/**
 * Gtk4\GtkLayoutManager::vfunc_create_layout_child(GtkWidget $widget, GtkWidget $for_child):
 * GtkLayoutChild
 *
 * Native `create_layout_child` (LayoutManagerClass.create_layout_child): the GTK implementation
 * below any PHP subclass, for `parent::vfunc_create_layout_child()` from an override. Create a
 * `GtkLayoutChild` instance for the given $for_child widget.
 */
ZEND_METHOD(Gtk4_GtkLayoutManager, vfunc_create_layout_child) {
  zval *widget;
  zval *for_child;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(widget, class_for_gtype(GTK_TYPE_WIDGET))
  Z_PARAM_OBJECT_OF_CLASS(for_child, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkLayoutManager *self = PHPGTK_SELF(GtkLayoutManager, GTK_TYPE_LAYOUT_MANAGER);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkLayoutManager::vfunc_create_layout_child(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_LAYOUT_MANAGER_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->create_layout_child == nullptr) {
    enum_to_php(G_TYPE_NONE, 0, return_value);
    return;
  }
  GObject *widget_o = unwrap(widget, GTK_TYPE_WIDGET);
  if (widget_o == nullptr) RETURN_THROWS();
  GObject *for_child_o = unwrap(for_child, GTK_TYPE_WIDGET);
  if (for_child_o == nullptr) RETURN_THROWS();
  GtkLayoutChild *phpgtk_ret =
      klass->create_layout_child(self, GTK_WIDGET(widget_o), GTK_WIDGET(for_child_o));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * Gtk4\GtkLayoutManager::vfunc_get_request_mode(GtkWidget $widget): GtkSizeRequestMode
 *
 * Native `get_request_mode` (LayoutManagerClass.get_request_mode): the GTK implementation below
 * any PHP subclass, for `parent::vfunc_get_request_mode()` from an override. a virtual function,
 * used to return the preferred request mode for the layout manager; for instance, "width for
 * height" or "height for width"; see `GtkSizeRequestMode`
 */
ZEND_METHOD(Gtk4_GtkLayoutManager, vfunc_get_request_mode) {
  zval *widget;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(widget, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkLayoutManager *self = PHPGTK_SELF(GtkLayoutManager, GTK_TYPE_LAYOUT_MANAGER);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkLayoutManager::vfunc_get_request_mode(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_LAYOUT_MANAGER_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->get_request_mode == nullptr) {
    enum_to_php(GTK_TYPE_SIZE_REQUEST_MODE, 0, return_value);
    return;
  }
  GObject *widget_o = unwrap(widget, GTK_TYPE_WIDGET);
  if (widget_o == nullptr) RETURN_THROWS();
  enum_to_php(GTK_TYPE_SIZE_REQUEST_MODE, klass->get_request_mode(self, GTK_WIDGET(widget_o)),
              return_value);
}

/**
 * Gtk4\GtkLayoutManager::vfunc_measure(GtkWidget $widget, GtkOrientation $orientation, int
 * $for_size): array
 *
 * Native `measure` (LayoutManagerClass.measure): the GTK implementation below any PHP subclass,
 * for `parent::vfunc_measure()` from an override. Measures the size of the $widget using $manager,
 * for the given $orientation and size.
 */
ZEND_METHOD(Gtk4_GtkLayoutManager, vfunc_measure) {
  zval *widget;
  zval *orientation;
  zend_long for_size;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_OBJECT_OF_CLASS(widget, class_for_gtype(GTK_TYPE_WIDGET))
  Z_PARAM_OBJECT_OF_CLASS(orientation, enum_class_for_type(GTK_TYPE_ORIENTATION))
  Z_PARAM_LONG(for_size)
  ZEND_PARSE_PARAMETERS_END();
  GtkLayoutManager *self = PHPGTK_SELF(GtkLayoutManager, GTK_TYPE_LAYOUT_MANAGER);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkLayoutManager::vfunc_measure(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_LAYOUT_MANAGER_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->measure == nullptr) {
    array_init_size(return_value, 4);
    add_next_index_long(return_value, 0);
    add_next_index_long(return_value, 0);
    add_next_index_long(return_value, -1);
    add_next_index_long(return_value, -1);
    return;
  }
  GObject *widget_o = unwrap(widget, GTK_TYPE_WIDGET);
  if (widget_o == nullptr) RETURN_THROWS();
  gint orientation_v = 0;
  if (!enum_from_php(orientation, GTK_TYPE_ORIENTATION, &orientation_v)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(for_size, 3)) RETURN_THROWS();
  int minimum = 0;
  int natural = 0;
  int minimum_baseline = -1;
  int natural_baseline = -1;
  klass->measure(self, GTK_WIDGET(widget_o), static_cast<GtkOrientation>(orientation_v),
                 static_cast<int>(for_size), &minimum, &natural, &minimum_baseline,
                 &natural_baseline);
  array_init_size(return_value, 4);
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(minimum));
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(natural));
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(minimum_baseline));
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(natural_baseline));
    add_next_index_zval(return_value, &item);
  }
}

/**
 * Gtk4\GtkLayoutManager::vfunc_root(): void
 *
 * Native `root` (LayoutManagerClass.root): the GTK implementation below any PHP subclass, for
 * `parent::vfunc_root()` from an override. a virtual function, called when the widget using the
 * layout manager is attached to a `GtkRoot`
 */
ZEND_METHOD(Gtk4_GtkLayoutManager, vfunc_root) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkLayoutManager *self = PHPGTK_SELF(GtkLayoutManager, GTK_TYPE_LAYOUT_MANAGER);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkLayoutManager::vfunc_root(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_LAYOUT_MANAGER_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->root == nullptr) {
    return;
  }
  klass->root(self);
}

/**
 * Gtk4\GtkLayoutManager::vfunc_unroot(): void
 *
 * Native `unroot` (LayoutManagerClass.unroot): the GTK implementation below any PHP subclass, for
 * `parent::vfunc_unroot()` from an override. a virtual function, called when the widget using the
 * layout manager is detached from a `GtkRoot`
 */
ZEND_METHOD(Gtk4_GtkLayoutManager, vfunc_unroot) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkLayoutManager *self = PHPGTK_SELF(GtkLayoutManager, GTK_TYPE_LAYOUT_MANAGER);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkLayoutManager::vfunc_unroot(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_LAYOUT_MANAGER_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->unroot == nullptr) {
    return;
  }
  klass->unroot(self);
}

// MINIT: the vfunc thunks of GtkLayoutManager (core/subtype.h).
void register_vfuncs_GtkLayoutManager() {
  register_vfunc(GTK_TYPE_LAYOUT_MANAGER, "allocate", vfunc_install_allocate);
  register_vfunc(GTK_TYPE_LAYOUT_MANAGER, "create_layout_child", vfunc_install_create_layout_child);
  register_vfunc(GTK_TYPE_LAYOUT_MANAGER, "get_request_mode", vfunc_install_get_request_mode);
  register_vfunc(GTK_TYPE_LAYOUT_MANAGER, "measure", vfunc_install_measure);
  register_vfunc(GTK_TYPE_LAYOUT_MANAGER, "root", vfunc_install_root);
  register_vfunc(GTK_TYPE_LAYOUT_MANAGER, "unroot", vfunc_install_unroot);
}
