// GENERATED by gen/gir.php from Gtk.FileFilter - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkFileFilter
#include "php_gtk4.h"
#include "core/object.h"
#include "core/collections.h"
#include "core/variant.h"
#include "core/subtype.h"

using namespace phpgtk;

/**
 * Gtk4\GtkFileFilter::__construct()
 *
 * Creates a new `GtkFileFilter` with no rules added to it.
 */
ZEND_METHOD(Gtk4_GtkFileFilter, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gtk_file_filter_new());
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkFileFilter::add_mime_type(string $mime_type): void
 *
 * Adds a rule allowing a given mime type to $filter.
 */
ZEND_METHOD(Gtk4_GtkFileFilter, add_mime_type) {
  zend_string *mime_type;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(mime_type)
  ZEND_PARSE_PARAMETERS_END();
  GtkFileFilter *self = PHPGTK_SELF(GtkFileFilter, GTK_TYPE_FILE_FILTER);
  if (!phpgtk::check_utf8(mime_type, 1)) RETURN_THROWS();
  gtk_file_filter_add_mime_type(self, ZSTR_VAL(mime_type));
}

/**
 * Gtk4\GtkFileFilter::add_pattern(string $pattern): void
 *
 * Adds a rule allowing a shell style glob to a filter.
 */
ZEND_METHOD(Gtk4_GtkFileFilter, add_pattern) {
  zend_string *pattern;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(pattern)
  ZEND_PARSE_PARAMETERS_END();
  GtkFileFilter *self = PHPGTK_SELF(GtkFileFilter, GTK_TYPE_FILE_FILTER);
  if (!phpgtk::check_utf8(pattern, 1)) RETURN_THROWS();
  gtk_file_filter_add_pattern(self, ZSTR_VAL(pattern));
}

/**
 * Gtk4\GtkFileFilter::add_pixbuf_formats(): void
 *
 * Adds a rule allowing image files in the formats supported by GdkPixbuf.
 */
ZEND_METHOD(Gtk4_GtkFileFilter, add_pixbuf_formats) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFileFilter *self = PHPGTK_SELF(GtkFileFilter, GTK_TYPE_FILE_FILTER);
  gtk_file_filter_add_pixbuf_formats(self);
}

/**
 * Gtk4\GtkFileFilter::add_suffix(string $suffix): void
 *
 * Adds a suffix match rule to a filter.
 */
ZEND_METHOD(Gtk4_GtkFileFilter, add_suffix) {
  zend_string *suffix;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(suffix)
  ZEND_PARSE_PARAMETERS_END();
  GtkFileFilter *self = PHPGTK_SELF(GtkFileFilter, GTK_TYPE_FILE_FILTER);
  if (!phpgtk::check_utf8(suffix, 1)) RETURN_THROWS();
  gtk_file_filter_add_suffix(self, ZSTR_VAL(suffix));
}

/**
 * Gtk4\GtkFileFilter::get_attributes(): array
 *
 * Gets the attributes that need to be filled in for the `GFileInfo` passed to this filter.
 */
ZEND_METHOD(Gtk4_GtkFileFilter, get_attributes) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFileFilter *self = PHPGTK_SELF(GtkFileFilter, GTK_TYPE_FILE_FILTER);
  strv_to_php(gtk_file_filter_get_attributes(self), return_value);
}

/**
 * Gtk4\GtkFileFilter::get_name(): ?string
 *
 * Gets the human-readable name for the filter.
 */
ZEND_METHOD(Gtk4_GtkFileFilter, get_name) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFileFilter *self = PHPGTK_SELF(GtkFileFilter, GTK_TYPE_FILE_FILTER);
  PHPGTK_RETURN_STRING_OR_NULL(gtk_file_filter_get_name(self));
}

/**
 * Gtk4\GtkFileFilter::set_name(?string $name): void
 *
 * Sets a human-readable name of the filter.
 */
ZEND_METHOD(Gtk4_GtkFileFilter, set_name) {
  zend_string *name = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR_OR_NULL(name)
  ZEND_PARSE_PARAMETERS_END();
  GtkFileFilter *self = PHPGTK_SELF(GtkFileFilter, GTK_TYPE_FILE_FILTER);
  if (name != nullptr && !phpgtk::check_utf8(name, 1)) RETURN_THROWS();
  gtk_file_filter_set_name(self, name != nullptr ? ZSTR_VAL(name) : nullptr);
}

/**
 * Gtk4\GtkFileFilter::to_gvariant(): mixed
 *
 * Serialize a file filter to an `a{sv}` variant.
 */
ZEND_METHOD(Gtk4_GtkFileFilter, to_gvariant) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFileFilter *self = PHPGTK_SELF(GtkFileFilter, GTK_TYPE_FILE_FILTER);
  GVariant *phpgtk_ret = gtk_file_filter_to_gvariant(self);
  if (phpgtk_ret == nullptr) RETURN_NULL();
  variant_to_php(phpgtk_ret, return_value);
}

/**
 * public static function new_from_gvariant(mixed $variant): GtkFileFilter
 * Deserialize a file filter from a GVariant.
 *
 * The variant is what to_gvariant() produced: `[name, [[rule, pattern], ...]]`. GTK wants
 * exactly `(sa(us))` and inference cannot build a tuple from a PHP list, so the type is named
 * here - without it the filter comes back empty. The argument is required: GTK dereferences it.
 */
ZEND_METHOD(Gtk4_GtkFileFilter, new_from_gvariant) {
  zval *variant = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_ZVAL(variant)
  ZEND_PARSE_PARAMETERS_END();
  GVariant *variant_v = php_to_variant(variant, G_VARIANT_TYPE("(sa(us))"));
  if (variant_v == nullptr) RETURN_THROWS();
  g_variant_ref_sink(variant_v);
  GtkFileFilter *filter = gtk_file_filter_new_from_gvariant(variant_v);
  g_variant_unref(variant_v);
  GObject *obj = G_OBJECT(filter);
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}
