// GENERATED by gen/gir.php from Gtk.UriLauncher - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkUriLauncher
#include "php_gtk4.h"
#include "core/object.h"
#include "core/gerror.h"
#include "core/subtype.h"
#include "core/callback.h"
#include <array>

using namespace phpgtk;

/**
 * Gtk4\GtkUriLauncher::__construct(?string $uri = null)
 *
 * Creates a new `GtkUriLauncher` object.
 */
ZEND_METHOD(Gtk4_GtkUriLauncher, __construct) {
  zend_string *uri = nullptr;
  ZEND_PARSE_PARAMETERS_START(0, 1)
  Z_PARAM_OPTIONAL
  Z_PARAM_STR_OR_NULL(uri)
  ZEND_PARSE_PARAMETERS_END();
  if (uri != nullptr && !phpgtk::check_utf8(uri, 1)) RETURN_THROWS();
  GObject *obj = subtype_new(ZEND_THIS, "uri", uri != nullptr ? ZSTR_VAL(uri) : nullptr, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gtk_uri_launcher_new(uri != nullptr ? ZSTR_VAL(uri) : nullptr));
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkUriLauncher::get_uri(): ?string
 *
 * Gets the uri that will be opened.
 */
ZEND_METHOD(Gtk4_GtkUriLauncher, get_uri) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkUriLauncher *self = PHPGTK_SELF(GtkUriLauncher, GTK_TYPE_URI_LAUNCHER);
  PHPGTK_RETURN_STRING_OR_NULL(gtk_uri_launcher_get_uri(self));
}

namespace {
// AsyncReadyCallback trampoline for GtkUriLauncher::launch(): wraps the C arguments, invokes the
// PHP callable once and releases it (async scope).
void cb_launch_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\GtkUriLauncher::launch(?GtkWindow $parent, ?GCancellable $cancellable, ?callable $callback):
 * void
 *
 * Launch an application to open the uri.
 */
ZEND_METHOD(Gtk4_GtkUriLauncher, launch) {
  zval *parent = nullptr;
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(parent, class_for_gtype(GTK_TYPE_WINDOW))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC_OR_NULL(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  GtkUriLauncher *self = PHPGTK_SELF(GtkUriLauncher, GTK_TYPE_URI_LAUNCHER);
  GObject *parent_o = nullptr;
  if (parent != nullptr) {
    parent_o = unwrap(parent, GTK_TYPE_WINDOW);
    if (parent_o == nullptr) RETURN_THROWS();
  }
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback = ZEND_FCI_INITIALIZED(fci_callback)
                              ? callback_new(&fci_callback.function_name, "GtkUriLauncher::launch")
                              : nullptr;
  gtk_uri_launcher_launch(self, parent_o != nullptr ? GTK_WINDOW(parent_o) : nullptr,
                          cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
                          cb_callback != nullptr ? cb_launch_callback : nullptr, cb_callback);
}

/**
 * Gtk4\GtkUriLauncher::launch_finish(GAsyncResult $result): bool
 *
 * Finishes the `launch` call and returns the result.
 */
ZEND_METHOD(Gtk4_GtkUriLauncher, launch_finish) {
  zval *result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  GtkUriLauncher *self = PHPGTK_SELF(GtkUriLauncher, GTK_TYPE_URI_LAUNCHER);
  GObject *result_o = unwrap(result, G_TYPE_ASYNC_RESULT);
  if (result_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  const gboolean ok = gtk_uri_launcher_launch_finish(self, G_ASYNC_RESULT(result_o), &error);
  if (!ok) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  RETURN_BOOL(ok);
}

/**
 * Gtk4\GtkUriLauncher::set_uri(?string $uri): void
 *
 * Sets the uri that will be opened.
 */
ZEND_METHOD(Gtk4_GtkUriLauncher, set_uri) {
  zend_string *uri = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR_OR_NULL(uri)
  ZEND_PARSE_PARAMETERS_END();
  GtkUriLauncher *self = PHPGTK_SELF(GtkUriLauncher, GTK_TYPE_URI_LAUNCHER);
  if (uri != nullptr && !phpgtk::check_utf8(uri, 1)) RETURN_THROWS();
  gtk_uri_launcher_set_uri(self, uri != nullptr ? ZSTR_VAL(uri) : nullptr);
}
