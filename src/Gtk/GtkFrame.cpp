// GENERATED by gen/gir.php from Gtk.Frame - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkFrame
#include "php_gtk4.h"
#include "core/object.h"
#include "core/subtype.h"

using namespace phpgtk;

/**
 * Gtk4\GtkFrame::__construct(?string $label = null)
 *
 * Creates a new `GtkFrame`, with optional label $label.
 */
ZEND_METHOD(Gtk4_GtkFrame, __construct) {
  zend_string *label = nullptr;
  ZEND_PARSE_PARAMETERS_START(0, 1)
  Z_PARAM_OPTIONAL
  Z_PARAM_STR_OR_NULL(label)
  ZEND_PARSE_PARAMETERS_END();
  if (label != nullptr && !phpgtk::check_utf8(label, 1)) RETURN_THROWS();
  GObject *obj =
      subtype_new(ZEND_THIS, "label", label != nullptr ? ZSTR_VAL(label) : nullptr, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gtk_frame_new(label != nullptr ? ZSTR_VAL(label) : nullptr));
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkFrame::get_child(): ?GtkWidget
 *
 * Gets the child widget of $frame.
 */
ZEND_METHOD(Gtk4_GtkFrame, get_child) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFrame *self = PHPGTK_SELF(GtkFrame, GTK_TYPE_FRAME);
  GtkWidget *result = gtk_frame_get_child(self);
  wrap(result != nullptr ? G_OBJECT(result) : nullptr, return_value);
}

/**
 * Gtk4\GtkFrame::get_label(): ?string
 *
 * Returns the frame labels text.
 */
ZEND_METHOD(Gtk4_GtkFrame, get_label) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFrame *self = PHPGTK_SELF(GtkFrame, GTK_TYPE_FRAME);
  PHPGTK_RETURN_STRING_OR_NULL(gtk_frame_get_label(self));
}

/**
 * Gtk4\GtkFrame::get_label_align(): float
 *
 * Retrieves the X alignment of the frame’s label.
 */
ZEND_METHOD(Gtk4_GtkFrame, get_label_align) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFrame *self = PHPGTK_SELF(GtkFrame, GTK_TYPE_FRAME);
  RETURN_DOUBLE(gtk_frame_get_label_align(self));
}

/**
 * Gtk4\GtkFrame::get_label_widget(): ?GtkWidget
 *
 * Retrieves the label widget for the frame.
 */
ZEND_METHOD(Gtk4_GtkFrame, get_label_widget) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFrame *self = PHPGTK_SELF(GtkFrame, GTK_TYPE_FRAME);
  GtkWidget *result = gtk_frame_get_label_widget(self);
  wrap(result != nullptr ? G_OBJECT(result) : nullptr, return_value);
}

/**
 * Gtk4\GtkFrame::set_child(?GtkWidget $child): void
 *
 * Sets the child widget of $frame.
 */
ZEND_METHOD(Gtk4_GtkFrame, set_child) {
  zval *child = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(child, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkFrame *self = PHPGTK_SELF(GtkFrame, GTK_TYPE_FRAME);
  GObject *child_o = nullptr;
  if (child != nullptr) {
    child_o = unwrap(child, GTK_TYPE_WIDGET);
    if (child_o == nullptr) RETURN_THROWS();
  }
  gtk_frame_set_child(self, child_o != nullptr ? GTK_WIDGET(child_o) : nullptr);
}

/**
 * Gtk4\GtkFrame::set_label(?string $label): void
 *
 * Creates a new `GtkLabel` with the $label and sets it as the frame's label widget.
 */
ZEND_METHOD(Gtk4_GtkFrame, set_label) {
  zend_string *label = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR_OR_NULL(label)
  ZEND_PARSE_PARAMETERS_END();
  GtkFrame *self = PHPGTK_SELF(GtkFrame, GTK_TYPE_FRAME);
  if (label != nullptr && !phpgtk::check_utf8(label, 1)) RETURN_THROWS();
  gtk_frame_set_label(self, label != nullptr ? ZSTR_VAL(label) : nullptr);
}

/**
 * Gtk4\GtkFrame::set_label_align(float $xalign): void
 *
 * Sets the X alignment of the frame widget’s label.
 */
ZEND_METHOD(Gtk4_GtkFrame, set_label_align) {
  double xalign;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_DOUBLE(xalign)
  ZEND_PARSE_PARAMETERS_END();
  GtkFrame *self = PHPGTK_SELF(GtkFrame, GTK_TYPE_FRAME);
  gtk_frame_set_label_align(self, static_cast<float>(xalign));
}

/**
 * Gtk4\GtkFrame::set_label_widget(?GtkWidget $label_widget): void
 *
 * Sets the label widget for the frame.
 */
ZEND_METHOD(Gtk4_GtkFrame, set_label_widget) {
  zval *label_widget = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(label_widget, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkFrame *self = PHPGTK_SELF(GtkFrame, GTK_TYPE_FRAME);
  GObject *label_widget_o = nullptr;
  if (label_widget != nullptr) {
    label_widget_o = unwrap(label_widget, GTK_TYPE_WIDGET);
    if (label_widget_o == nullptr) RETURN_THROWS();
  }
  gtk_frame_set_label_widget(self,
                             label_widget_o != nullptr ? GTK_WIDGET(label_widget_o) : nullptr);
}
