// GENERATED by gen/gir.php from Gtk.CssProvider - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkCssProvider
#include "php_gtk4.h"
#include "core/object.h"
#include "core/variant.h"
#include "core/subtype.h"

using namespace phpgtk;

/**
 * Gtk4\GtkCssProvider::__construct()
 *
 * Returns a newly created `GtkCssProvider`.
 */
ZEND_METHOD(Gtk4_GtkCssProvider, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gtk_css_provider_new());
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkCssProvider::load_from_bytes(string $data): void
 *
 * Loads $data into $css_provider.
 */
ZEND_METHOD(Gtk4_GtkCssProvider, load_from_bytes) {
  zend_string *data;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(data)
  ZEND_PARSE_PARAMETERS_END();
  GtkCssProvider *self = PHPGTK_SELF(GtkCssProvider, GTK_TYPE_CSS_PROVIDER);
  GBytes *data_b = g_bytes_new(ZSTR_VAL(data), ZSTR_LEN(data));
  gtk_css_provider_load_from_bytes(self, data_b);
  g_bytes_unref(data_b);
}

/**
 * Gtk4\GtkCssProvider::load_from_file(string $file): void
 *
 * Loads the data contained in $file into $css_provider.
 */
ZEND_METHOD(Gtk4_GtkCssProvider, load_from_file) {
  zend_string *file;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_PATH_STR(file)
  ZEND_PARSE_PARAMETERS_END();
  GtkCssProvider *self = PHPGTK_SELF(GtkCssProvider, GTK_TYPE_CSS_PROVIDER);
  GFile *file_f = g_file_new_for_commandline_arg(ZSTR_VAL(file));
  gtk_css_provider_load_from_file(self, file_f);
  if (file_f != nullptr) g_object_unref(file_f);
}

/**
 * Gtk4\GtkCssProvider::load_from_path(string $path): void
 *
 * Loads the data contained in $path into $css_provider.
 */
ZEND_METHOD(Gtk4_GtkCssProvider, load_from_path) {
  zend_string *path;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_PATH_STR(path)
  ZEND_PARSE_PARAMETERS_END();
  GtkCssProvider *self = PHPGTK_SELF(GtkCssProvider, GTK_TYPE_CSS_PROVIDER);
  zend_string *path_abs = phpgtk::absolute_filename(path, 1);
  if (path_abs == nullptr) RETURN_THROWS();
  gtk_css_provider_load_from_path(self, ZSTR_VAL(path_abs));
  if (path_abs != nullptr) zend_string_release(path_abs);
}

/**
 * Gtk4\GtkCssProvider::load_from_resource(string $resource_path): void
 *
 * Loads the data contained in the resource at $resource_path into the $css_provider.
 */
ZEND_METHOD(Gtk4_GtkCssProvider, load_from_resource) {
  zend_string *resource_path;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(resource_path)
  ZEND_PARSE_PARAMETERS_END();
  GtkCssProvider *self = PHPGTK_SELF(GtkCssProvider, GTK_TYPE_CSS_PROVIDER);
  if (!phpgtk::check_utf8(resource_path, 1)) RETURN_THROWS();
  gtk_css_provider_load_from_resource(self, ZSTR_VAL(resource_path));
}

/**
 * Gtk4\GtkCssProvider::load_from_string(string $string): void
 *
 * Loads $string into $css_provider.
 */
ZEND_METHOD(Gtk4_GtkCssProvider, load_from_string) {
  zend_string *string;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(string)
  ZEND_PARSE_PARAMETERS_END();
  GtkCssProvider *self = PHPGTK_SELF(GtkCssProvider, GTK_TYPE_CSS_PROVIDER);
  if (!phpgtk::check_utf8(string, 1)) RETURN_THROWS();
  gtk_css_provider_load_from_string(self, ZSTR_VAL(string));
}

/**
 * Gtk4\GtkCssProvider::load_named(string $name, ?string $variant): void
 *
 * Loads a theme from the usual theme paths.
 */
ZEND_METHOD(Gtk4_GtkCssProvider, load_named) {
  zend_string *name;
  zend_string *variant = nullptr;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_STR(name)
  Z_PARAM_STR_OR_NULL(variant)
  ZEND_PARSE_PARAMETERS_END();
  GtkCssProvider *self = PHPGTK_SELF(GtkCssProvider, GTK_TYPE_CSS_PROVIDER);
  if (!phpgtk::check_utf8(name, 1)) RETURN_THROWS();
  if (variant != nullptr && !phpgtk::check_utf8(variant, 2)) RETURN_THROWS();
  gtk_css_provider_load_named(self, ZSTR_VAL(name),
                              variant != nullptr ? ZSTR_VAL(variant) : nullptr);
}

/**
 * Gtk4\GtkCssProvider::to_string(): string
 *
 * Converts the $provider into a string representation in CSS format.
 */
ZEND_METHOD(Gtk4_GtkCssProvider, to_string) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkCssProvider *self = PHPGTK_SELF(GtkCssProvider, GTK_TYPE_CSS_PROVIDER);
  char *phpgtk_ret = gtk_css_provider_to_string(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETVAL_STRING(phpgtk_ret);
  g_free(phpgtk_ret);
}
