// GENERATED by gen/gir.php from Gtk.SelectionModel - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkSelectionModel
#include "php_gtk4.h"
#include "core/object.h"
#include "core/boxed.h"
#include "core/subtype.h"
#include "core/error.h"
#include <array>

using namespace phpgtk;

/**
 * Gtk4\GtkSelectionModel::get_selection(): GtkBitset
 *
 * Gets the set containing all currently selected items in the model.
 */
ZEND_METHOD(Gtk4_GtkSelectionModel, get_selection) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkSelectionModel *self = PHPGTK_SELF(GtkSelectionModel, GTK_TYPE_SELECTION_MODEL);
  GtkBitset *phpgtk_ret = gtk_selection_model_get_selection(self);
  wrap_boxed(GTK_TYPE_BITSET, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(GTK_TYPE_BITSET, phpgtk_ret);
}

/**
 * Gtk4\GtkSelectionModel::get_selection_in_range(int $position, int $n_items): GtkBitset
 *
 * Gets the set of selected items in a range.
 */
ZEND_METHOD(Gtk4_GtkSelectionModel, get_selection_in_range) {
  zend_long position;
  zend_long n_items;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_LONG(position)
  Z_PARAM_LONG(n_items)
  ZEND_PARSE_PARAMETERS_END();
  GtkSelectionModel *self = PHPGTK_SELF(GtkSelectionModel, GTK_TYPE_SELECTION_MODEL);
  if (!phpgtk::check_range<guint>(position, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<guint>(n_items, 2)) RETURN_THROWS();
  GtkBitset *phpgtk_ret = gtk_selection_model_get_selection_in_range(
      self, static_cast<guint>(position), static_cast<guint>(n_items));
  wrap_boxed(GTK_TYPE_BITSET, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(GTK_TYPE_BITSET, phpgtk_ret);
}

/**
 * Gtk4\GtkSelectionModel::is_selected(int $position): bool
 *
 * Checks if the given item is selected.
 */
ZEND_METHOD(Gtk4_GtkSelectionModel, is_selected) {
  zend_long position;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(position)
  ZEND_PARSE_PARAMETERS_END();
  GtkSelectionModel *self = PHPGTK_SELF(GtkSelectionModel, GTK_TYPE_SELECTION_MODEL);
  if (!phpgtk::check_range<guint>(position, 1)) RETURN_THROWS();
  RETURN_BOOL(gtk_selection_model_is_selected(self, static_cast<guint>(position)));
}

/**
 * Gtk4\GtkSelectionModel::select_all(): bool
 *
 * Requests to select all items in the model.
 */
ZEND_METHOD(Gtk4_GtkSelectionModel, select_all) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkSelectionModel *self = PHPGTK_SELF(GtkSelectionModel, GTK_TYPE_SELECTION_MODEL);
  RETURN_BOOL(gtk_selection_model_select_all(self));
}

/**
 * Gtk4\GtkSelectionModel::select_item(int $position, bool $unselect_rest): bool
 *
 * Requests to select an item in the model.
 */
ZEND_METHOD(Gtk4_GtkSelectionModel, select_item) {
  zend_long position;
  bool unselect_rest;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_LONG(position)
  Z_PARAM_BOOL(unselect_rest)
  ZEND_PARSE_PARAMETERS_END();
  GtkSelectionModel *self = PHPGTK_SELF(GtkSelectionModel, GTK_TYPE_SELECTION_MODEL);
  if (!phpgtk::check_range<guint>(position, 1)) RETURN_THROWS();
  RETURN_BOOL(gtk_selection_model_select_item(self, static_cast<guint>(position), unselect_rest));
}

/**
 * Gtk4\GtkSelectionModel::select_range(int $position, int $n_items, bool $unselect_rest): bool
 *
 * Requests to select a range of items in the model.
 */
ZEND_METHOD(Gtk4_GtkSelectionModel, select_range) {
  zend_long position;
  zend_long n_items;
  bool unselect_rest;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_LONG(position)
  Z_PARAM_LONG(n_items)
  Z_PARAM_BOOL(unselect_rest)
  ZEND_PARSE_PARAMETERS_END();
  GtkSelectionModel *self = PHPGTK_SELF(GtkSelectionModel, GTK_TYPE_SELECTION_MODEL);
  if (!phpgtk::check_range<guint>(position, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<guint>(n_items, 2)) RETURN_THROWS();
  RETURN_BOOL(gtk_selection_model_select_range(self, static_cast<guint>(position),
                                               static_cast<guint>(n_items), unselect_rest));
}

/**
 * Gtk4\GtkSelectionModel::set_selection(GtkBitset $selected, GtkBitset $mask): bool
 *
 * Make selection changes.
 */
ZEND_METHOD(Gtk4_GtkSelectionModel, set_selection) {
  zval *selected;
  zval *mask;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(selected, boxed_class_for_type(GTK_TYPE_BITSET)->ce)
  Z_PARAM_OBJECT_OF_CLASS(mask, boxed_class_for_type(GTK_TYPE_BITSET)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkSelectionModel *self = PHPGTK_SELF(GtkSelectionModel, GTK_TYPE_SELECTION_MODEL);
  gpointer selected_b = unwrap_boxed(selected, GTK_TYPE_BITSET);
  if (selected_b == nullptr) RETURN_THROWS();
  gpointer mask_b = unwrap_boxed(mask, GTK_TYPE_BITSET);
  if (mask_b == nullptr) RETURN_THROWS();
  RETURN_BOOL(gtk_selection_model_set_selection(self, static_cast<GtkBitset *>(selected_b),
                                                static_cast<GtkBitset *>(mask_b)));
}

/**
 * Gtk4\GtkSelectionModel::unselect_all(): bool
 *
 * Requests to unselect all items in the model.
 */
ZEND_METHOD(Gtk4_GtkSelectionModel, unselect_all) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkSelectionModel *self = PHPGTK_SELF(GtkSelectionModel, GTK_TYPE_SELECTION_MODEL);
  RETURN_BOOL(gtk_selection_model_unselect_all(self));
}

/**
 * Gtk4\GtkSelectionModel::unselect_item(int $position): bool
 *
 * Requests to unselect an item in the model.
 */
ZEND_METHOD(Gtk4_GtkSelectionModel, unselect_item) {
  zend_long position;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(position)
  ZEND_PARSE_PARAMETERS_END();
  GtkSelectionModel *self = PHPGTK_SELF(GtkSelectionModel, GTK_TYPE_SELECTION_MODEL);
  if (!phpgtk::check_range<guint>(position, 1)) RETURN_THROWS();
  RETURN_BOOL(gtk_selection_model_unselect_item(self, static_cast<guint>(position)));
}

/**
 * Gtk4\GtkSelectionModel::unselect_range(int $position, int $n_items): bool
 *
 * Requests to unselect a range of items in the model.
 */
ZEND_METHOD(Gtk4_GtkSelectionModel, unselect_range) {
  zend_long position;
  zend_long n_items;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_LONG(position)
  Z_PARAM_LONG(n_items)
  ZEND_PARSE_PARAMETERS_END();
  GtkSelectionModel *self = PHPGTK_SELF(GtkSelectionModel, GTK_TYPE_SELECTION_MODEL);
  if (!phpgtk::check_range<guint>(position, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<guint>(n_items, 2)) RETURN_THROWS();
  RETURN_BOOL(gtk_selection_model_unselect_range(self, static_cast<guint>(position),
                                                 static_cast<guint>(n_items)));
}

/**
 * public function selection_changed(int $position, int $n_items): void
 * Helper function for implementations of `GtkSelectionModel`.
 *
 * The range has to lie inside the model: GTK asserts
 * `position + n_items <= g_list_model_get_n_items()` and drops the notification otherwise, so a
 * range computed one item too long silently fails to repaint. Refuse it instead - and check the
 * sum in 64-bit, before the cast to guint that would wrap it.
 */
ZEND_METHOD(Gtk4_GtkSelectionModel, selection_changed) {
  zend_long position;
  zend_long n_items;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_LONG(position)
  Z_PARAM_LONG(n_items)
  ZEND_PARSE_PARAMETERS_END();
  GtkSelectionModel *self = PHPGTK_SELF(GtkSelectionModel, GTK_TYPE_SELECTION_MODEL);
  if (!phpgtk::check_range<guint>(position, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<guint>(n_items, 2)) RETURN_THROWS();
  auto in_model = static_cast<zend_long>(g_list_model_get_n_items(G_LIST_MODEL(self)));
  if (position + n_items > in_model) {
    zend_argument_value_error(2,
                              "must not reach past the end of the model (" ZEND_LONG_FMT
                              " item(s)), " ZEND_LONG_FMT " item(s) from " ZEND_LONG_FMT " given",
                              in_model, n_items, position);
    RETURN_THROWS();
  }
  gtk_selection_model_selection_changed(self, static_cast<guint>(position),
                                        static_cast<guint>(n_items));
}

// vfunc thunks and installers: file-local, installed by class_init of a PHP subtype
namespace {

// vfunc thunk: static_cast<GtkSelectionModelInterface *>->is_selected -> $this->is_selected() on a
// PHP subclass
gboolean vfunc_thunk_is_selected(GtkSelectionModel *self, guint position) {
  zval zself;
  zend_function *fn =
      EG(exception) == nullptr ? subtype_vfunc(G_OBJECT(self), "is_selected", &zself) : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    // an interface implemented in PHP has no native implementation below it
    return FALSE;
  }
  std::array<zval, 1> args{};
  zval *argv = args.data();
  ZVAL_LONG(&argv[0], static_cast<zend_long>(position));
  zval ret;
  ZVAL_UNDEF(&ret);
  gboolean result = FALSE;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = zend_is_true(&ret) ? TRUE : FALSE;
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkSelectionModel::is_selected");
  return result;
}

// vfunc installer: static_cast<GtkSelectionModelInterface *>->is_selected (called from class_init /
// iface_init of a PHP subtype)
void vfunc_install_is_selected(gpointer klass) {
  static_cast<GtkSelectionModelInterface *>(klass)->is_selected = vfunc_thunk_is_selected;
}

// vfunc thunk: static_cast<GtkSelectionModelInterface *>->select_all -> $this->select_all() on a
// PHP subclass
gboolean vfunc_thunk_select_all(GtkSelectionModel *self) {
  zval zself;
  zend_function *fn =
      EG(exception) == nullptr ? subtype_vfunc(G_OBJECT(self), "select_all", &zself) : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    // an interface implemented in PHP has no native implementation below it
    return FALSE;
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  gboolean result = FALSE;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = zend_is_true(&ret) ? TRUE : FALSE;
  }
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkSelectionModel::select_all");
  return result;
}

// vfunc installer: static_cast<GtkSelectionModelInterface *>->select_all (called from class_init /
// iface_init of a PHP subtype)
void vfunc_install_select_all(gpointer klass) {
  static_cast<GtkSelectionModelInterface *>(klass)->select_all = vfunc_thunk_select_all;
}

// vfunc thunk: static_cast<GtkSelectionModelInterface *>->select_item -> $this->select_item() on a
// PHP subclass
gboolean vfunc_thunk_select_item(GtkSelectionModel *self, guint position, gboolean unselect_rest) {
  zval zself;
  zend_function *fn =
      EG(exception) == nullptr ? subtype_vfunc(G_OBJECT(self), "select_item", &zself) : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    // an interface implemented in PHP has no native implementation below it
    return FALSE;
  }
  std::array<zval, 2> args{};
  zval *argv = args.data();
  ZVAL_LONG(&argv[0], static_cast<zend_long>(position));
  ZVAL_BOOL(&argv[1], unselect_rest != FALSE);
  zval ret;
  ZVAL_UNDEF(&ret);
  gboolean result = FALSE;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 2, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = zend_is_true(&ret) ? TRUE : FALSE;
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkSelectionModel::select_item");
  return result;
}

// vfunc installer: static_cast<GtkSelectionModelInterface *>->select_item (called from class_init /
// iface_init of a PHP subtype)
void vfunc_install_select_item(gpointer klass) {
  static_cast<GtkSelectionModelInterface *>(klass)->select_item = vfunc_thunk_select_item;
}

// vfunc thunk: static_cast<GtkSelectionModelInterface *>->select_range -> $this->select_range() on
// a PHP subclass
gboolean vfunc_thunk_select_range(GtkSelectionModel *self, guint position, guint n_items,
                                  gboolean unselect_rest) {
  zval zself;
  zend_function *fn =
      EG(exception) == nullptr ? subtype_vfunc(G_OBJECT(self), "select_range", &zself) : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    // an interface implemented in PHP has no native implementation below it
    return FALSE;
  }
  std::array<zval, 3> args{};
  zval *argv = args.data();
  ZVAL_LONG(&argv[0], static_cast<zend_long>(position));
  ZVAL_LONG(&argv[1], static_cast<zend_long>(n_items));
  ZVAL_BOOL(&argv[2], unselect_rest != FALSE);
  zval ret;
  ZVAL_UNDEF(&ret);
  gboolean result = FALSE;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 3, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = zend_is_true(&ret) ? TRUE : FALSE;
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkSelectionModel::select_range");
  return result;
}

// vfunc installer: static_cast<GtkSelectionModelInterface *>->select_range (called from class_init
// / iface_init of a PHP subtype)
void vfunc_install_select_range(gpointer klass) {
  static_cast<GtkSelectionModelInterface *>(klass)->select_range = vfunc_thunk_select_range;
}

// vfunc thunk: static_cast<GtkSelectionModelInterface *>->set_selection -> $this->set_selection()
// on a PHP subclass
gboolean vfunc_thunk_set_selection(GtkSelectionModel *self, GtkBitset *selected, GtkBitset *mask) {
  zval zself;
  zend_function *fn =
      EG(exception) == nullptr ? subtype_vfunc(G_OBJECT(self), "set_selection", &zself) : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    // an interface implemented in PHP has no native implementation below it
    return FALSE;
  }
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap_boxed(GTK_TYPE_BITSET, selected, &argv[0]);
  wrap_boxed(GTK_TYPE_BITSET, mask, &argv[1]);
  zval ret;
  ZVAL_UNDEF(&ret);
  gboolean result = FALSE;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 2, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = zend_is_true(&ret) ? TRUE : FALSE;
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkSelectionModel::set_selection");
  return result;
}

// vfunc installer: static_cast<GtkSelectionModelInterface *>->set_selection (called from class_init
// / iface_init of a PHP subtype)
void vfunc_install_set_selection(gpointer klass) {
  static_cast<GtkSelectionModelInterface *>(klass)->set_selection = vfunc_thunk_set_selection;
}

// vfunc thunk: static_cast<GtkSelectionModelInterface *>->unselect_all -> $this->unselect_all() on
// a PHP subclass
gboolean vfunc_thunk_unselect_all(GtkSelectionModel *self) {
  zval zself;
  zend_function *fn =
      EG(exception) == nullptr ? subtype_vfunc(G_OBJECT(self), "unselect_all", &zself) : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    // an interface implemented in PHP has no native implementation below it
    return FALSE;
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  gboolean result = FALSE;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = zend_is_true(&ret) ? TRUE : FALSE;
  }
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkSelectionModel::unselect_all");
  return result;
}

// vfunc installer: static_cast<GtkSelectionModelInterface *>->unselect_all (called from class_init
// / iface_init of a PHP subtype)
void vfunc_install_unselect_all(gpointer klass) {
  static_cast<GtkSelectionModelInterface *>(klass)->unselect_all = vfunc_thunk_unselect_all;
}

// vfunc thunk: static_cast<GtkSelectionModelInterface *>->unselect_item -> $this->unselect_item()
// on a PHP subclass
gboolean vfunc_thunk_unselect_item(GtkSelectionModel *self, guint position) {
  zval zself;
  zend_function *fn =
      EG(exception) == nullptr ? subtype_vfunc(G_OBJECT(self), "unselect_item", &zself) : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    // an interface implemented in PHP has no native implementation below it
    return FALSE;
  }
  std::array<zval, 1> args{};
  zval *argv = args.data();
  ZVAL_LONG(&argv[0], static_cast<zend_long>(position));
  zval ret;
  ZVAL_UNDEF(&ret);
  gboolean result = FALSE;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = zend_is_true(&ret) ? TRUE : FALSE;
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkSelectionModel::unselect_item");
  return result;
}

// vfunc installer: static_cast<GtkSelectionModelInterface *>->unselect_item (called from class_init
// / iface_init of a PHP subtype)
void vfunc_install_unselect_item(gpointer klass) {
  static_cast<GtkSelectionModelInterface *>(klass)->unselect_item = vfunc_thunk_unselect_item;
}

// vfunc thunk: static_cast<GtkSelectionModelInterface *>->unselect_range -> $this->unselect_range()
// on a PHP subclass
gboolean vfunc_thunk_unselect_range(GtkSelectionModel *self, guint position, guint n_items) {
  zval zself;
  zend_function *fn =
      EG(exception) == nullptr ? subtype_vfunc(G_OBJECT(self), "unselect_range", &zself) : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    // an interface implemented in PHP has no native implementation below it
    return FALSE;
  }
  std::array<zval, 2> args{};
  zval *argv = args.data();
  ZVAL_LONG(&argv[0], static_cast<zend_long>(position));
  ZVAL_LONG(&argv[1], static_cast<zend_long>(n_items));
  zval ret;
  ZVAL_UNDEF(&ret);
  gboolean result = FALSE;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 2, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = zend_is_true(&ret) ? TRUE : FALSE;
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkSelectionModel::unselect_range");
  return result;
}

// vfunc installer: static_cast<GtkSelectionModelInterface *>->unselect_range (called from
// class_init / iface_init of a PHP subtype)
void vfunc_install_unselect_range(gpointer klass) {
  static_cast<GtkSelectionModelInterface *>(klass)->unselect_range = vfunc_thunk_unselect_range;
}

}  // namespace

// MINIT: the vfunc thunks of GtkSelectionModel (core/subtype.h).
void register_vfuncs_GtkSelectionModel() {
  register_iface_vfunc(GTK_TYPE_SELECTION_MODEL, "is_selected", vfunc_install_is_selected);
  register_iface_vfunc(GTK_TYPE_SELECTION_MODEL, "select_all", vfunc_install_select_all);
  register_iface_vfunc(GTK_TYPE_SELECTION_MODEL, "select_item", vfunc_install_select_item);
  register_iface_vfunc(GTK_TYPE_SELECTION_MODEL, "select_range", vfunc_install_select_range);
  register_iface_vfunc(GTK_TYPE_SELECTION_MODEL, "set_selection", vfunc_install_set_selection);
  register_iface_vfunc(GTK_TYPE_SELECTION_MODEL, "unselect_all", vfunc_install_unselect_all);
  register_iface_vfunc(GTK_TYPE_SELECTION_MODEL, "unselect_item", vfunc_install_unselect_item);
  register_iface_vfunc(GTK_TYPE_SELECTION_MODEL, "unselect_range", vfunc_install_unselect_range);
}

/**
 * Gtk4\GtkSelectionModelObject::__construct()
 *
 * Never called: these handles only come from wrap().
 */
ZEND_METHOD(Gtk4_GtkSelectionModelObject, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
}
