// GENERATED by gen/gir.php from Gtk.IconTheme - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkIconTheme
#include "php_gtk4.h"
#include "core/object.h"
#include "core/enums.h"
#include "core/collections.h"
#include "core/subtype.h"

using namespace phpgtk;

/**
 * Gtk4\GtkIconTheme::__construct()
 *
 * Creates a new icon theme object.
 */
ZEND_METHOD(Gtk4_GtkIconTheme, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gtk_icon_theme_new());
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * static Gtk4\GtkIconTheme::get_for_display(GdkDisplay $display): GtkIconTheme
 *
 * Gets the icon theme object associated with $display.
 */
ZEND_METHOD(Gtk4_GtkIconTheme, get_for_display) {
  zval *display;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(display, class_for_gtype(GDK_TYPE_DISPLAY))
  ZEND_PARSE_PARAMETERS_END();
  GObject *display_o = unwrap(display, GDK_TYPE_DISPLAY);
  if (display_o == nullptr) RETURN_THROWS();
  GtkIconTheme *phpgtk_ret = gtk_icon_theme_get_for_display(GDK_DISPLAY(display_o));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkIconTheme::add_resource_path(string $path): void
 *
 * Adds a resource path that will be looked at when looking for icons, similar to search paths.
 */
ZEND_METHOD(Gtk4_GtkIconTheme, add_resource_path) {
  zend_string *path;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(path)
  ZEND_PARSE_PARAMETERS_END();
  GtkIconTheme *self = PHPGTK_SELF(GtkIconTheme, GTK_TYPE_ICON_THEME);
  if (!phpgtk::check_utf8(path, 1)) RETURN_THROWS();
  gtk_icon_theme_add_resource_path(self, ZSTR_VAL(path));
}

/**
 * Gtk4\GtkIconTheme::add_search_path(string $path): void
 *
 * Appends a directory to the search path.
 */
ZEND_METHOD(Gtk4_GtkIconTheme, add_search_path) {
  zend_string *path;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_PATH_STR(path)
  ZEND_PARSE_PARAMETERS_END();
  GtkIconTheme *self = PHPGTK_SELF(GtkIconTheme, GTK_TYPE_ICON_THEME);
  zend_string *path_abs = phpgtk::absolute_filename(path, 1);
  if (path_abs == nullptr) RETURN_THROWS();
  gtk_icon_theme_add_search_path(self, ZSTR_VAL(path_abs));
  if (path_abs != nullptr) zend_string_release(path_abs);
}

/**
 * Gtk4\GtkIconTheme::get_display(): ?GdkDisplay
 *
 * Returns the display that the `GtkIconTheme` object was created for.
 */
ZEND_METHOD(Gtk4_GtkIconTheme, get_display) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkIconTheme *self = PHPGTK_SELF(GtkIconTheme, GTK_TYPE_ICON_THEME);
  GdkDisplay *phpgtk_ret = gtk_icon_theme_get_display(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkIconTheme::get_icon_names(): array
 *
 * Lists the names of icons in the current icon theme.
 */
ZEND_METHOD(Gtk4_GtkIconTheme, get_icon_names) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkIconTheme *self = PHPGTK_SELF(GtkIconTheme, GTK_TYPE_ICON_THEME);
  strv_to_php(gtk_icon_theme_get_icon_names(self), Transfer::Full, return_value);
}

/**
 * Gtk4\GtkIconTheme::get_resource_path(): array
 *
 * Gets the current resource path.
 */
ZEND_METHOD(Gtk4_GtkIconTheme, get_resource_path) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkIconTheme *self = PHPGTK_SELF(GtkIconTheme, GTK_TYPE_ICON_THEME);
  strv_to_php(gtk_icon_theme_get_resource_path(self), Transfer::Full, return_value);
}

/**
 * Gtk4\GtkIconTheme::get_theme_name(): string
 *
 * Gets the current icon theme name.
 */
ZEND_METHOD(Gtk4_GtkIconTheme, get_theme_name) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkIconTheme *self = PHPGTK_SELF(GtkIconTheme, GTK_TYPE_ICON_THEME);
  char *phpgtk_ret = gtk_icon_theme_get_theme_name(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETVAL_STRING(phpgtk_ret);
  g_free(phpgtk_ret);
}

/**
 * Gtk4\GtkIconTheme::has_icon(string $icon_name): bool
 *
 * Checks whether an icon theme includes an icon for a particular name.
 */
ZEND_METHOD(Gtk4_GtkIconTheme, has_icon) {
  zend_string *icon_name;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(icon_name)
  ZEND_PARSE_PARAMETERS_END();
  GtkIconTheme *self = PHPGTK_SELF(GtkIconTheme, GTK_TYPE_ICON_THEME);
  if (!phpgtk::check_utf8(icon_name, 1)) RETURN_THROWS();
  RETURN_BOOL(gtk_icon_theme_has_icon(self, ZSTR_VAL(icon_name)));
}

/**
 * Gtk4\GtkIconTheme::lookup_icon(string $icon_name, ?array $fallbacks, int $size, int $scale,
 * GtkTextDirection $direction, int $flags): GtkIconPaintable
 *
 * Looks up a named icon for a desired size and window scale, returning a `GtkIconPaintable`.
 */
ZEND_METHOD(Gtk4_GtkIconTheme, lookup_icon) {
  zend_string *icon_name;
  zval *fallbacks = nullptr;
  zend_long size;
  zend_long scale;
  zval *direction;
  zend_long flags;
  ZEND_PARSE_PARAMETERS_START(6, 6)
  Z_PARAM_STR(icon_name)
  Z_PARAM_ARRAY_OR_NULL(fallbacks)
  Z_PARAM_LONG(size)
  Z_PARAM_LONG(scale)
  Z_PARAM_OBJECT_OF_CLASS(direction, enum_class_for_type(GTK_TYPE_TEXT_DIRECTION))
  Z_PARAM_LONG(flags)
  ZEND_PARSE_PARAMETERS_END();
  GtkIconTheme *self = PHPGTK_SELF(GtkIconTheme, GTK_TYPE_ICON_THEME);
  if (!phpgtk::check_utf8(icon_name, 1)) RETURN_THROWS();
  char **fallbacks_v = nullptr;
  if (fallbacks != nullptr) {
    fallbacks_v = strv_from_php(fallbacks);
    if (fallbacks_v == nullptr) RETURN_THROWS();
  }
  if (!phpgtk::check_range<int>(size, 3)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(scale, 4)) RETURN_THROWS();
  if (!phpgtk::check_domain(scale, 1, ZEND_LONG_MAX, 4)) RETURN_THROWS();
  gint direction_v = 0;
  if (!enum_from_php(direction, GTK_TYPE_TEXT_DIRECTION, &direction_v)) RETURN_THROWS();
  if (!phpgtk::check_flags(GTK_TYPE_ICON_LOOKUP_FLAGS, flags, 6)) RETURN_THROWS();
  GtkIconPaintable *call_result = gtk_icon_theme_lookup_icon(
      self, ZSTR_VAL(icon_name), const_cast<const char **>(fallbacks_v), static_cast<int>(size),
      static_cast<int>(scale), static_cast<GtkTextDirection>(direction_v),
      static_cast<GtkIconLookupFlags>(flags));
  if (fallbacks_v != nullptr) g_strfreev(fallbacks_v);
  GtkIconPaintable *phpgtk_ret = call_result;
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * Gtk4\GtkIconTheme::set_resource_path(?array $path): void
 *
 * Sets the resource paths that will be looked at when looking for icons, similar to search paths.
 */
ZEND_METHOD(Gtk4_GtkIconTheme, set_resource_path) {
  zval *path = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_ARRAY_OR_NULL(path)
  ZEND_PARSE_PARAMETERS_END();
  GtkIconTheme *self = PHPGTK_SELF(GtkIconTheme, GTK_TYPE_ICON_THEME);
  char **path_v = nullptr;
  if (path != nullptr) {
    path_v = strv_from_php(path);
    if (path_v == nullptr) RETURN_THROWS();
  }
  gtk_icon_theme_set_resource_path(self, const_cast<const char **>(path_v));
  if (path_v != nullptr) g_strfreev(path_v);
}

/**
 * public function set_theme_name(?string $theme_name): void
 * Set the icon theme this object looks names up in.
 *
 * GTK refuses it on the theme a display owns - `g_return_if_fail (!self->is_display_singleton)`,
 * a CRITICAL that changes nothing - because that one follows the desktop's setting. There is no
 * public predicate for it, but the singleton is by definition the theme its own display answers
 * with, so that is what this compares. A theme of one's own (`new GtkIconTheme()`) is free.
 */
ZEND_METHOD(Gtk4_GtkIconTheme, set_theme_name) {
  zend_string *theme_name = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR_OR_NULL(theme_name)
  ZEND_PARSE_PARAMETERS_END();
  GtkIconTheme *self = PHPGTK_SELF(GtkIconTheme, GTK_TYPE_ICON_THEME);
  if (theme_name != nullptr && !check_utf8(theme_name, 1)) RETURN_THROWS();
  GdkDisplay *display = gtk_icon_theme_get_display(self);
  if (display != nullptr && gtk_icon_theme_get_for_display(display) == self) {
    zend_throw_exception(spl_ce_LogicException,
                         "Gtk4\\GtkIconTheme::set_theme_name(): the theme of a display follows "
                         "the desktop setting; make one of your own to point it elsewhere",
                         0);
    RETURN_THROWS();
  }
  gtk_icon_theme_set_theme_name(self, theme_name == nullptr ? nullptr : ZSTR_VAL(theme_name));
}
