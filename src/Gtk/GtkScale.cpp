// GENERATED by gen/gir.php from Gtk.Scale - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkScale
#include "php_gtk4.h"
#include "core/object.h"
#include "core/enums.h"
#include "core/subtype.h"
#include "core/error.h"

using namespace phpgtk;

// GtkScaleFormatValueFunc (notified scope): the PHP callable lives in a Callback owned by GTK.
#include "core/callback.h"
#include "core/teardown.h"

#include <array>

namespace {

// GtkScaleFormatValueFunc trampoline: (GtkScale $scale, float $value) -> string. GTK frees the
// returned string; a failed call (exception reported through the boundary) shows an empty label.
char *format_value_func(GtkScale *scale, double value, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  wrap(G_OBJECT(scale), args.data());
  ZVAL_DOUBLE(&args[1], value);
  zval retval;
  const bool ok = callback_invoke(cb, 2, args.data(), &retval);
  char *text = nullptr;
  if (ok && !Z_ISUNDEF(retval)) {
    zend_string *s = zval_get_string(&retval);
    text = g_strdup(ZSTR_VAL(s));
    zend_string_release(s);
  }
  zval_ptr_dtor(&retval);
  zval_ptr_dtor(args.data());
  return text != nullptr ? text : g_strdup("");
}

// GDestroyNotify for the format func.
void format_value_func_free(gpointer data) {
  teardown_untrack_notified(data);
  callback_free(data);
}

// teardown clear hook: drop the PHP callable from the scale.
void format_value_func_clear(GObject *owner) {
  gtk_scale_set_format_value_func(GTK_SCALE(owner), nullptr, nullptr, nullptr);
}

// Install (or with null, remove) a PHP format func on `scale`.
void install_format_value_func(GtkScale *scale, zend_fcall_info *fci, const char *origin) {
  if (!ZEND_FCI_INITIALIZED(*fci)) {
    gtk_scale_set_format_value_func(scale, nullptr, nullptr, nullptr);
  } else {
    Callback *cb = callback_new(&fci->function_name, origin);
    teardown_track_notified(cb, G_OBJECT(scale), format_value_func_clear);
    gtk_scale_set_format_value_func(scale, format_value_func, cb, format_value_func_free);
  }
  callback_drain();  // the previous func's notify ran inside the setter
}

}  // namespace

/**
 * Gtk4\GtkScale::__construct(GtkOrientation $orientation, ?GtkAdjustment $adjustment = null)
 *
 * Creates a new `GtkScale`.
 */
ZEND_METHOD(Gtk4_GtkScale, __construct) {
  zval *orientation;
  zval *adjustment = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 2)
  Z_PARAM_OBJECT_OF_CLASS(orientation, enum_class_for_type(GTK_TYPE_ORIENTATION))
  Z_PARAM_OPTIONAL
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(adjustment, class_for_gtype(GTK_TYPE_ADJUSTMENT))
  ZEND_PARSE_PARAMETERS_END();
  gint orientation_v = 0;
  if (!enum_from_php(orientation, GTK_TYPE_ORIENTATION, &orientation_v)) RETURN_THROWS();
  GObject *adjustment_o = nullptr;
  if (adjustment != nullptr) {
    adjustment_o = unwrap(adjustment, GTK_TYPE_ADJUSTMENT);
    if (adjustment_o == nullptr) RETURN_THROWS();
  }
  GObject *obj = subtype_new(
      ZEND_THIS, "orientation", static_cast<GtkOrientation>(orientation_v), "adjustment",
      adjustment_o != nullptr ? GTK_ADJUSTMENT(adjustment_o) : nullptr, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gtk_scale_new(static_cast<GtkOrientation>(orientation_v),
                                 adjustment_o != nullptr ? GTK_ADJUSTMENT(adjustment_o) : nullptr));
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * static Gtk4\GtkScale::new_with_range(GtkOrientation $orientation, float $min, float $max, float
 * $step): GtkScale
 *
 * Creates a new scale widget with a range from $min to $max.
 */
ZEND_METHOD(Gtk4_GtkScale, new_with_range) {
  zval *orientation;
  double min;
  double max;
  double step;
  ZEND_PARSE_PARAMETERS_START(4, 4)
  Z_PARAM_OBJECT_OF_CLASS(orientation, enum_class_for_type(GTK_TYPE_ORIENTATION))
  Z_PARAM_DOUBLE(min)
  Z_PARAM_DOUBLE(max)
  Z_PARAM_DOUBLE(step)
  ZEND_PARSE_PARAMETERS_END();
  gint orientation_v = 0;
  if (!enum_from_php(orientation, GTK_TYPE_ORIENTATION, &orientation_v)) RETURN_THROWS();
  GObject *obj = G_OBJECT(
      gtk_scale_new_with_range(static_cast<GtkOrientation>(orientation_v), min, max, step));
  wrap(obj, return_value);
}

/**
 * Gtk4\GtkScale::add_mark(float $value, GtkPositionType $position, ?string $markup): void
 *
 * Adds a mark at $value.
 */
ZEND_METHOD(Gtk4_GtkScale, add_mark) {
  double value;
  zval *position;
  zend_string *markup = nullptr;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_DOUBLE(value)
  Z_PARAM_OBJECT_OF_CLASS(position, enum_class_for_type(GTK_TYPE_POSITION_TYPE))
  Z_PARAM_STR_OR_NULL(markup)
  ZEND_PARSE_PARAMETERS_END();
  GtkScale *self = PHPGTK_SELF(GtkScale, GTK_TYPE_SCALE);
  gint position_v = 0;
  if (!enum_from_php(position, GTK_TYPE_POSITION_TYPE, &position_v)) RETURN_THROWS();
  gtk_scale_add_mark(self, value, static_cast<GtkPositionType>(position_v),
                     markup != nullptr ? ZSTR_VAL(markup) : nullptr);
}

/**
 * Gtk4\GtkScale::clear_marks(): void
 *
 * Removes any marks that have been added.
 */
ZEND_METHOD(Gtk4_GtkScale, clear_marks) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkScale *self = PHPGTK_SELF(GtkScale, GTK_TYPE_SCALE);
  gtk_scale_clear_marks(self);
}

/**
 * Gtk4\GtkScale::get_digits(): int
 *
 * Gets the number of decimal places that are displayed in the value.
 */
ZEND_METHOD(Gtk4_GtkScale, get_digits) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkScale *self = PHPGTK_SELF(GtkScale, GTK_TYPE_SCALE);
  RETURN_LONG(static_cast<zend_long>(gtk_scale_get_digits(self)));
}

/**
 * Gtk4\GtkScale::get_draw_value(): bool
 *
 * Returns whether the current value is displayed as a string next to the slider.
 */
ZEND_METHOD(Gtk4_GtkScale, get_draw_value) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkScale *self = PHPGTK_SELF(GtkScale, GTK_TYPE_SCALE);
  RETURN_BOOL(gtk_scale_get_draw_value(self));
}

/**
 * Gtk4\GtkScale::get_has_origin(): bool
 *
 * Returns whether the scale has an origin.
 */
ZEND_METHOD(Gtk4_GtkScale, get_has_origin) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkScale *self = PHPGTK_SELF(GtkScale, GTK_TYPE_SCALE);
  RETURN_BOOL(gtk_scale_get_has_origin(self));
}

/**
 * Gtk4\GtkScale::get_layout_offsets(): array
 *
 * Obtains the coordinates where the scale will draw the `PangoLayout` representing the text in the
 * scale.
 */
ZEND_METHOD(Gtk4_GtkScale, get_layout_offsets) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkScale *self = PHPGTK_SELF(GtkScale, GTK_TYPE_SCALE);
  int x = 0;
  int y = 0;
  gtk_scale_get_layout_offsets(self, &x, &y);
  array_init_size(return_value, 2);
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(x));
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(y));
    add_next_index_zval(return_value, &item);
  }
}

/**
 * Gtk4\GtkScale::get_value_pos(): GtkPositionType
 *
 * Gets the position in which the current value is displayed.
 */
ZEND_METHOD(Gtk4_GtkScale, get_value_pos) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkScale *self = PHPGTK_SELF(GtkScale, GTK_TYPE_SCALE);
  enum_to_php(GTK_TYPE_POSITION_TYPE, gtk_scale_get_value_pos(self), return_value);
}

/**
 * Gtk4\GtkScale::set_digits(int $digits): void
 *
 * Sets the number of decimal places that are displayed in the value.
 */
ZEND_METHOD(Gtk4_GtkScale, set_digits) {
  zend_long digits;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(digits)
  ZEND_PARSE_PARAMETERS_END();
  GtkScale *self = PHPGTK_SELF(GtkScale, GTK_TYPE_SCALE);
  gtk_scale_set_digits(self, static_cast<int>(digits));
}

/**
 * Gtk4\GtkScale::set_draw_value(bool $draw_value): void
 *
 * Specifies whether the current value is displayed as a string next to the slider.
 */
ZEND_METHOD(Gtk4_GtkScale, set_draw_value) {
  bool draw_value;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(draw_value)
  ZEND_PARSE_PARAMETERS_END();
  GtkScale *self = PHPGTK_SELF(GtkScale, GTK_TYPE_SCALE);
  gtk_scale_set_draw_value(self, draw_value);
}

/**
 * Gtk4\GtkScale::set_has_origin(bool $has_origin): void
 *
 * Sets whether the scale has an origin.
 */
ZEND_METHOD(Gtk4_GtkScale, set_has_origin) {
  bool has_origin;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(has_origin)
  ZEND_PARSE_PARAMETERS_END();
  GtkScale *self = PHPGTK_SELF(GtkScale, GTK_TYPE_SCALE);
  gtk_scale_set_has_origin(self, has_origin);
}

/**
 * Gtk4\GtkScale::set_value_pos(GtkPositionType $pos): void
 *
 * Sets the position in which the current value is displayed.
 */
ZEND_METHOD(Gtk4_GtkScale, set_value_pos) {
  zval *pos;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(pos, enum_class_for_type(GTK_TYPE_POSITION_TYPE))
  ZEND_PARSE_PARAMETERS_END();
  GtkScale *self = PHPGTK_SELF(GtkScale, GTK_TYPE_SCALE);
  gint pos_v = 0;
  if (!enum_from_php(pos, GTK_TYPE_POSITION_TYPE, &pos_v)) RETURN_THROWS();
  gtk_scale_set_value_pos(self, static_cast<GtkPositionType>(pos_v));
}

/**
 * public function set_format_value_func(?callable $func): void
 * The callable that renders the value label: `function (GtkScale $scale, float $value): string`;
 * null goes back to GTK's own formatting (`digits` decimals).
 */
ZEND_METHOD(Gtk4_GtkScale, set_format_value_func) {
  zend_fcall_info fci;
  zend_fcall_info_cache fcc;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_FUNC_OR_NULL(fci, fcc)
  ZEND_PARSE_PARAMETERS_END();
  GtkScale *scale = PHPGTK_SELF(GtkScale, GTK_TYPE_SCALE);
  install_format_value_func(scale, &fci, "GtkScale::set_format_value_func");
}

// vfunc thunks and installers: file-local, installed by class_init of a PHP subtype
namespace {

// vfunc thunk: GTK_SCALE_CLASS->get_layout_offsets -> $this->vfunc_get_layout_offsets() on a PHP
// subclass
void vfunc_thunk_get_layout_offsets(GtkScale *self, int *x, int *y) {
  zval zself;
  zend_function *fn = EG(exception) == nullptr
                          ? subtype_vfunc(G_OBJECT(self), "vfunc_get_layout_offsets", &zself)
                          : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    auto *native = GTK_SCALE_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->get_layout_offsets != nullptr) native->get_layout_offsets(self, x, y);
    return;
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    if (Z_TYPE(ret) == IS_ARRAY) {
      if (zval *e0 = zend_hash_index_find(Z_ARRVAL(ret), 0); e0 != nullptr && x != nullptr) {
        *x = static_cast<int>(zval_get_long(e0));
      }
      if (zval *e1 = zend_hash_index_find(Z_ARRVAL(ret), 1); e1 != nullptr && y != nullptr) {
        *y = static_cast<int>(zval_get_long(e1));
      }
    }
  }
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkScale::vfunc_get_layout_offsets");
}

// vfunc installer: GTK_SCALE_CLASS->get_layout_offsets (called from class_init / iface_init of a
// PHP subtype)
void vfunc_install_get_layout_offsets(gpointer klass) {
  GTK_SCALE_CLASS(klass)->get_layout_offsets = vfunc_thunk_get_layout_offsets;
}

}  // namespace

/**
 * Gtk4\GtkScale::vfunc_get_layout_offsets(): array
 *
 * Native `get_layout_offsets` (ScaleClass.get_layout_offsets): the GTK implementation below any
 * PHP subclass, for `parent::vfunc_get_layout_offsets()` from an override. Obtains the coordinates
 * where the scale will draw the `PangoLayout` representing the text in the scale.
 */
ZEND_METHOD(Gtk4_GtkScale, vfunc_get_layout_offsets) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkScale *self = PHPGTK_SELF(GtkScale, GTK_TYPE_SCALE);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkScale::vfunc_get_layout_offsets(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_SCALE_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->get_layout_offsets == nullptr) {
    array_init_size(return_value, 2);
    add_next_index_long(return_value, 0);
    add_next_index_long(return_value, 0);
    return;
  }
  int x = 0;
  int y = 0;
  klass->get_layout_offsets(self, &x, &y);
  array_init_size(return_value, 2);
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(x));
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(y));
    add_next_index_zval(return_value, &item);
  }
}

// MINIT: the vfunc thunks of GtkScale (core/subtype.h).
void register_vfuncs_GtkScale() {
  register_vfunc(GTK_TYPE_SCALE, "get_layout_offsets", vfunc_install_get_layout_offsets);
}
