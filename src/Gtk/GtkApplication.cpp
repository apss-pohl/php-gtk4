// GENERATED by gen/gir.php from Gtk.Application - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkApplication
#include "php_gtk4.h"
#include "core/object.h"
#include "core/collections.h"
#include "core/subtype.h"
#include "core/error.h"
#include <array>

using namespace phpgtk;

/**
 * Gtk4\GtkApplication::__construct(?string $application_id, int $flags)
 *
 * Creates a new `GtkApplication` instance.
 */
ZEND_METHOD(Gtk4_GtkApplication, __construct) {
  zend_string *application_id = nullptr;
  zend_long flags;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_STR_OR_NULL(application_id)
  Z_PARAM_LONG(flags)
  ZEND_PARSE_PARAMETERS_END();
  if (application_id != nullptr && !phpgtk::check_utf8(application_id, 1)) RETURN_THROWS();
  if (!phpgtk::check_flags(G_TYPE_APPLICATION_FLAGS, flags, 2)) RETURN_THROWS();
  GObject *obj = subtype_new(ZEND_THIS, "application-id",
                             application_id != nullptr ? ZSTR_VAL(application_id) : nullptr,
                             "flags", static_cast<GApplicationFlags>(flags), nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj =
        G_OBJECT(gtk_application_new(application_id != nullptr ? ZSTR_VAL(application_id) : nullptr,
                                     static_cast<GApplicationFlags>(flags)));
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkApplication::add_window(GtkWindow $window): void
 *
 * Adds a window to `application`.
 */
ZEND_METHOD(Gtk4_GtkApplication, add_window) {
  zval *window;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(window, class_for_gtype(GTK_TYPE_WINDOW))
  ZEND_PARSE_PARAMETERS_END();
  GtkApplication *self = PHPGTK_SELF(GtkApplication, GTK_TYPE_APPLICATION);
  GObject *window_o = unwrap(window, GTK_TYPE_WINDOW);
  if (window_o == nullptr) RETURN_THROWS();
  gtk_application_add_window(self, GTK_WINDOW(window_o));
}

/**
 * Gtk4\GtkApplication::get_accels_for_action(string $detailed_action_name): array
 *
 * Gets the accelerators that are currently associated with the given action.
 */
ZEND_METHOD(Gtk4_GtkApplication, get_accels_for_action) {
  zend_string *detailed_action_name;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(detailed_action_name)
  ZEND_PARSE_PARAMETERS_END();
  GtkApplication *self = PHPGTK_SELF(GtkApplication, GTK_TYPE_APPLICATION);
  if (!phpgtk::check_utf8(detailed_action_name, 1)) RETURN_THROWS();
  strv_to_php(gtk_application_get_accels_for_action(self, ZSTR_VAL(detailed_action_name)),
              Transfer::Full, return_value);
}

/**
 * Gtk4\GtkApplication::get_actions_for_accel(string $accel): array
 *
 * Returns the list of actions (possibly empty) that `accel` maps to.
 */
ZEND_METHOD(Gtk4_GtkApplication, get_actions_for_accel) {
  zend_string *accel;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(accel)
  ZEND_PARSE_PARAMETERS_END();
  GtkApplication *self = PHPGTK_SELF(GtkApplication, GTK_TYPE_APPLICATION);
  if (!phpgtk::check_utf8(accel, 1)) RETURN_THROWS();
  if (!(gtk_accelerator_parse(ZSTR_VAL(accel), nullptr, nullptr))) {
    zend_argument_value_error(1, "must be a valid accelerator string (like <Control>q)");
    RETURN_THROWS();
  }
  strv_to_php(gtk_application_get_actions_for_accel(self, ZSTR_VAL(accel)), Transfer::Full,
              return_value);
}

/**
 * Gtk4\GtkApplication::get_active_window(): ?GtkWindow
 *
 * Gets the “active” window for the application.
 */
ZEND_METHOD(Gtk4_GtkApplication, get_active_window) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkApplication *self = PHPGTK_SELF(GtkApplication, GTK_TYPE_APPLICATION);
  GtkWindow *phpgtk_ret = gtk_application_get_active_window(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkApplication::get_menu_by_id(string $id): ?GMenu
 *
 * Gets a menu from automatically loaded resources.
 */
ZEND_METHOD(Gtk4_GtkApplication, get_menu_by_id) {
  zend_string *id;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(id)
  ZEND_PARSE_PARAMETERS_END();
  GtkApplication *self = PHPGTK_SELF(GtkApplication, GTK_TYPE_APPLICATION);
  if (!phpgtk::check_utf8(id, 1)) RETURN_THROWS();
  GMenu *phpgtk_ret = gtk_application_get_menu_by_id(self, ZSTR_VAL(id));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkApplication::get_menubar(): ?GMenuModel
 *
 * Returns the menu model that has been set with `set_menubar`.
 */
ZEND_METHOD(Gtk4_GtkApplication, get_menubar) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkApplication *self = PHPGTK_SELF(GtkApplication, GTK_TYPE_APPLICATION);
  GMenuModel *phpgtk_ret = gtk_application_get_menubar(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkApplication::get_window_by_id(int $id): ?GtkWindow
 *
 * Returns the `ApplicationWindow` with the given ID.
 */
ZEND_METHOD(Gtk4_GtkApplication, get_window_by_id) {
  zend_long id;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(id)
  ZEND_PARSE_PARAMETERS_END();
  GtkApplication *self = PHPGTK_SELF(GtkApplication, GTK_TYPE_APPLICATION);
  if (!phpgtk::check_range<guint>(id, 1)) RETURN_THROWS();
  GtkWindow *phpgtk_ret = gtk_application_get_window_by_id(self, static_cast<guint>(id));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkApplication::get_windows(): array
 *
 * Gets a list of the `Window` instances associated with `application`.
 */
ZEND_METHOD(Gtk4_GtkApplication, get_windows) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkApplication *self = PHPGTK_SELF(GtkApplication, GTK_TYPE_APPLICATION);
  glist_to_php(gtk_application_get_windows(self), GTK_TYPE_WINDOW, Transfer::None, return_value);
}

/**
 * Gtk4\GtkApplication::list_action_descriptions(): array
 *
 * Lists the detailed action names which have associated accelerators.
 */
ZEND_METHOD(Gtk4_GtkApplication, list_action_descriptions) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkApplication *self = PHPGTK_SELF(GtkApplication, GTK_TYPE_APPLICATION);
  strv_to_php(gtk_application_list_action_descriptions(self), Transfer::Full, return_value);
}

/**
 * Gtk4\GtkApplication::remove_window(GtkWindow $window): void
 *
 * Remove a window from `application`.
 */
ZEND_METHOD(Gtk4_GtkApplication, remove_window) {
  zval *window;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(window, class_for_gtype(GTK_TYPE_WINDOW))
  ZEND_PARSE_PARAMETERS_END();
  GtkApplication *self = PHPGTK_SELF(GtkApplication, GTK_TYPE_APPLICATION);
  GObject *window_o = unwrap(window, GTK_TYPE_WINDOW);
  if (window_o == nullptr) RETURN_THROWS();
  gtk_application_remove_window(self, GTK_WINDOW(window_o));
}

/**
 * Gtk4\GtkApplication::set_accels_for_action(string $detailed_action_name, array $accels): void
 *
 * Sets zero or more keyboard accelerators that will trigger the given action.
 */
ZEND_METHOD(Gtk4_GtkApplication, set_accels_for_action) {
  zend_string *detailed_action_name;
  zval *accels;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_STR(detailed_action_name)
  Z_PARAM_ARRAY(accels)
  ZEND_PARSE_PARAMETERS_END();
  GtkApplication *self = PHPGTK_SELF(GtkApplication, GTK_TYPE_APPLICATION);
  if (!phpgtk::check_utf8(detailed_action_name, 1)) RETURN_THROWS();
  char **accels_v = strv_from_php(accels);
  if (accels_v == nullptr) RETURN_THROWS();
  gtk_application_set_accels_for_action(self, ZSTR_VAL(detailed_action_name),
                                        const_cast<const char **>(accels_v));
  g_strfreev(accels_v);
}

/**
 * Gtk4\GtkApplication::set_menubar(?GMenuModel $menubar): void
 *
 * Sets or unsets the menubar for windows of `application`.
 */
ZEND_METHOD(Gtk4_GtkApplication, set_menubar) {
  zval *menubar = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(menubar, class_for_gtype(G_TYPE_MENU_MODEL))
  ZEND_PARSE_PARAMETERS_END();
  GtkApplication *self = PHPGTK_SELF(GtkApplication, GTK_TYPE_APPLICATION);
  GObject *menubar_o = nullptr;
  if (menubar != nullptr) {
    menubar_o = unwrap(menubar, G_TYPE_MENU_MODEL);
    if (menubar_o == nullptr) RETURN_THROWS();
  }
  const bool state_holds = g_application_get_is_registered(G_APPLICATION(self)) == TRUE;
  if (!state_holds) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "%s(): the application is not registered yet - the menubar exists from `startup` on",
        ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  gtk_application_set_menubar(self, menubar_o != nullptr ? G_MENU_MODEL(menubar_o) : nullptr);
}

// vfunc thunks and installers: file-local, installed by class_init of a PHP subtype
namespace {

// vfunc thunk: GTK_APPLICATION_CLASS->window_added -> $this->vfunc_window_added() on a PHP subclass
void vfunc_thunk_window_added(GtkApplication *self, GtkWindow *window) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_window_added", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = GTK_APPLICATION_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->window_added != nullptr) native->window_added(self, window);
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 1> args{};
  zval *argv = args.data();
  wrap(window != nullptr ? G_OBJECT(window) : nullptr, &argv[0]);
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkApplication::vfunc_window_added");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: GTK_APPLICATION_CLASS->window_added (called from class_init / iface_init of a
// PHP subtype)
void vfunc_install_window_added(gpointer klass) {
  GTK_APPLICATION_CLASS(klass)->window_added = vfunc_thunk_window_added;
}

// vfunc thunk: GTK_APPLICATION_CLASS->window_removed -> $this->vfunc_window_removed() on a PHP
// subclass
void vfunc_thunk_window_removed(GtkApplication *self, GtkWindow *window) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_window_removed", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = GTK_APPLICATION_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->window_removed != nullptr) native->window_removed(self, window);
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 1> args{};
  zval *argv = args.data();
  wrap(window != nullptr ? G_OBJECT(window) : nullptr, &argv[0]);
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkApplication::vfunc_window_removed");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: GTK_APPLICATION_CLASS->window_removed (called from class_init / iface_init of a
// PHP subtype)
void vfunc_install_window_removed(gpointer klass) {
  GTK_APPLICATION_CLASS(klass)->window_removed = vfunc_thunk_window_removed;
}

}  // namespace

/**
 * Gtk4\GtkApplication::vfunc_window_added(GtkWindow $window): void
 *
 * Native `window_added` (ApplicationClass.window_added): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_window_added()` from an override. Signal emitted when a `GtkWindow`
 * is added to application through gtk_application_add_window().
 */
ZEND_METHOD(Gtk4_GtkApplication, vfunc_window_added) {
  zval *window;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(window, class_for_gtype(GTK_TYPE_WINDOW))
  ZEND_PARSE_PARAMETERS_END();
  GtkApplication *self = PHPGTK_SELF(GtkApplication, GTK_TYPE_APPLICATION);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkApplication::vfunc_window_added(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_APPLICATION_CLASS(subtype_native_class(G_OBJECT(self)));
  GObject *window_o = unwrap(window, GTK_TYPE_WINDOW);
  if (window_o == nullptr) RETURN_THROWS();
  if (klass->window_added == nullptr) {
    return;
  }
  klass->window_added(self, GTK_WINDOW(window_o));
}

/**
 * Gtk4\GtkApplication::vfunc_window_removed(GtkWindow $window): void
 *
 * Native `window_removed` (ApplicationClass.window_removed): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_window_removed()` from an override. Signal emitted when a
 * `GtkWindow` is removed from application, either as a side-effect of being destroyed or
 * explicitly through gtk_application_remove_window().
 */
ZEND_METHOD(Gtk4_GtkApplication, vfunc_window_removed) {
  zval *window;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(window, class_for_gtype(GTK_TYPE_WINDOW))
  ZEND_PARSE_PARAMETERS_END();
  GtkApplication *self = PHPGTK_SELF(GtkApplication, GTK_TYPE_APPLICATION);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkApplication::vfunc_window_removed(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_APPLICATION_CLASS(subtype_native_class(G_OBJECT(self)));
  GObject *window_o = unwrap(window, GTK_TYPE_WINDOW);
  if (window_o == nullptr) RETURN_THROWS();
  if (klass->window_removed == nullptr) {
    return;
  }
  klass->window_removed(self, GTK_WINDOW(window_o));
}

// MINIT: the vfunc thunks of GtkApplication (core/subtype.h).
void register_vfuncs_GtkApplication() {
  register_vfunc(GTK_TYPE_APPLICATION, "window_added", vfunc_install_window_added);
  register_vfunc(GTK_TYPE_APPLICATION, "window_removed", vfunc_install_window_removed);
}
