// GENERATED by gen/gir.php from Gtk.TextTagTable - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkTextTagTable
#include "php_gtk4.h"
#include "core/object.h"
#include "core/subtype.h"
#include "core/callback.h"
#include <array>

using namespace phpgtk;

/**
 * Gtk4\GtkTextTagTable::__construct()
 *
 * Creates a new `GtkTextTagTable`.
 */
ZEND_METHOD(Gtk4_GtkTextTagTable, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gtk_text_tag_table_new());
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkTextTagTable::add(GtkTextTag $tag): bool
 *
 * Add a tag to the table.
 */
ZEND_METHOD(Gtk4_GtkTextTagTable, add) {
  zval *tag;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(tag, class_for_gtype(GTK_TYPE_TEXT_TAG))
  ZEND_PARSE_PARAMETERS_END();
  GtkTextTagTable *self = PHPGTK_SELF(GtkTextTagTable, GTK_TYPE_TEXT_TAG_TABLE);
  GObject *tag_o = unwrap(tag, GTK_TYPE_TEXT_TAG);
  if (tag_o == nullptr) RETURN_THROWS();
  RETURN_BOOL(gtk_text_tag_table_add(self, GTK_TEXT_TAG(tag_o)));
}

namespace {
// TextTagTableForeach trampoline for GtkTextTagTable::foreach(): wraps the C arguments, invokes the
// PHP callable (call scope: released after the call).
void cb_foreach_func(GtkTextTag *tag, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 1> args{};
  zval *argv = args.data();
  wrap(tag != nullptr ? G_OBJECT(tag) : nullptr, &argv[0]);
  zval ret;
  callback_invoke(cb, 1, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
}
}  // namespace

/**
 * Gtk4\GtkTextTagTable::foreach(callable $func): void
 *
 * Calls $func on each tag in $table, with user data $data.
 */
ZEND_METHOD(Gtk4_GtkTextTagTable, foreach) {
  zend_fcall_info fci_func = empty_fcall_info;
  zend_fcall_info_cache fcc_func = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_FUNC(fci_func, fcc_func)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextTagTable *self = PHPGTK_SELF(GtkTextTagTable, GTK_TYPE_TEXT_TAG_TABLE);
  Callback *cb_func = callback_new(&fci_func.function_name, "GtkTextTagTable::foreach");
  gtk_text_tag_table_foreach(self, cb_foreach_func, cb_func);
  if (cb_func != nullptr) {
    callback_free(cb_func);
    callback_drain();
  }
}

/**
 * Gtk4\GtkTextTagTable::get_size(): int
 *
 * Returns the size of the table (number of tags)
 */
ZEND_METHOD(Gtk4_GtkTextTagTable, get_size) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkTextTagTable *self = PHPGTK_SELF(GtkTextTagTable, GTK_TYPE_TEXT_TAG_TABLE);
  RETURN_LONG(static_cast<zend_long>(gtk_text_tag_table_get_size(self)));
}

/**
 * Gtk4\GtkTextTagTable::lookup(string $name): ?GtkTextTag
 *
 * Look up a named tag.
 */
ZEND_METHOD(Gtk4_GtkTextTagTable, lookup) {
  zend_string *name;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(name)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextTagTable *self = PHPGTK_SELF(GtkTextTagTable, GTK_TYPE_TEXT_TAG_TABLE);
  if (!phpgtk::check_utf8(name, 1)) RETURN_THROWS();
  GtkTextTag *phpgtk_ret = gtk_text_tag_table_lookup(self, ZSTR_VAL(name));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkTextTagTable::remove(GtkTextTag $tag): void
 *
 * Remove a tag from the table.
 */
ZEND_METHOD(Gtk4_GtkTextTagTable, remove) {
  zval *tag;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(tag, class_for_gtype(GTK_TYPE_TEXT_TAG))
  ZEND_PARSE_PARAMETERS_END();
  GtkTextTagTable *self = PHPGTK_SELF(GtkTextTagTable, GTK_TYPE_TEXT_TAG_TABLE);
  GObject *tag_o = unwrap(tag, GTK_TYPE_TEXT_TAG);
  if (tag_o == nullptr) RETURN_THROWS();
  gtk_text_tag_table_remove(self, GTK_TEXT_TAG(tag_o));
}
