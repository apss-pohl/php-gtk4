// GENERATED by gen/gir.php from Gtk.Sorter - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkSorter
#include "php_gtk4.h"
#include "core/object.h"
#include "core/enums.h"
#include "core/subtype.h"
#include "core/error.h"
#include <array>

using namespace phpgtk;

/**
 * Gtk4\GtkSorter::__construct()
 *
 * A GtkSorter with default properties (GTK's own constructor is varargs-only; set the
 * properties afterwards).
 */
ZEND_METHOD(Gtk4_GtkSorter, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = static_cast<GObject *>(g_object_new(GTK_TYPE_SORTER, nullptr));
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkSorter::changed(GtkSorterChange $change): void
 *
 * Notifies all users of the sorter that it has changed.
 */
ZEND_METHOD(Gtk4_GtkSorter, changed) {
  zval *change;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(change, enum_class_for_type(GTK_TYPE_SORTER_CHANGE))
  ZEND_PARSE_PARAMETERS_END();
  GtkSorter *self = PHPGTK_SELF(GtkSorter, GTK_TYPE_SORTER);
  gint change_v = 0;
  if (!enum_from_php(change, GTK_TYPE_SORTER_CHANGE, &change_v)) RETURN_THROWS();
  gtk_sorter_changed(self, static_cast<GtkSorterChange>(change_v));
}

/**
 * Gtk4\GtkSorter::compare(GObject $item1, GObject $item2): GtkOrdering
 *
 * Compares two given items according to the sort order implemented by the sorter.
 */
ZEND_METHOD(Gtk4_GtkSorter, compare) {
  zval *item1;
  zval *item2;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(item1, class_for_gtype(G_TYPE_OBJECT))
  Z_PARAM_OBJECT_OF_CLASS(item2, class_for_gtype(G_TYPE_OBJECT))
  ZEND_PARSE_PARAMETERS_END();
  GtkSorter *self = PHPGTK_SELF(GtkSorter, GTK_TYPE_SORTER);
  GObject *item1_o = unwrap(item1, G_TYPE_OBJECT);
  if (item1_o == nullptr) RETURN_THROWS();
  GObject *item2_o = unwrap(item2, G_TYPE_OBJECT);
  if (item2_o == nullptr) RETURN_THROWS();
  enum_to_php(GTK_TYPE_ORDERING, gtk_sorter_compare(self, G_OBJECT(item1_o), G_OBJECT(item2_o)),
              return_value);
}

/**
 * Gtk4\GtkSorter::get_order(): GtkSorterOrder
 *
 * Gets the order that $self conforms to.
 */
ZEND_METHOD(Gtk4_GtkSorter, get_order) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkSorter *self = PHPGTK_SELF(GtkSorter, GTK_TYPE_SORTER);
  enum_to_php(GTK_TYPE_SORTER_ORDER, gtk_sorter_get_order(self), return_value);
}

// vfunc thunks and installers: file-local, installed by class_init of a PHP subtype
namespace {

// vfunc thunk: GTK_SORTER_CLASS->compare -> $this->vfunc_compare() on a PHP subclass
GtkOrdering vfunc_thunk_compare(GtkSorter *self, gpointer item1, gpointer item2) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_compare", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = GTK_SORTER_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->compare != nullptr ? native->compare(self, item1, item2)
                                      : static_cast<GtkOrdering>(0);
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(item1 != nullptr ? G_OBJECT(item1) : nullptr, &argv[0]);
  wrap(item2 != nullptr ? G_OBJECT(item2) : nullptr, &argv[1]);
  zval ret;
  ZVAL_UNDEF(&ret);
  GtkOrdering result = static_cast<GtkOrdering>(0);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 2, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    gint v = 0;
    if (enum_from_php(&ret, GTK_TYPE_ORDERING, &v)) result = static_cast<GtkOrdering>(v);
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkSorter::vfunc_compare");
  zend_exception_restore();  // the parked one, previous of whatever this threw
  return result;
}

// vfunc installer: GTK_SORTER_CLASS->compare (called from class_init / iface_init of a PHP subtype)
void vfunc_install_compare(gpointer klass) {
  GTK_SORTER_CLASS(klass)->compare = vfunc_thunk_compare;
}

// vfunc thunk: GTK_SORTER_CLASS->get_order -> $this->vfunc_get_order() on a PHP subclass
GtkSorterOrder vfunc_thunk_get_order(GtkSorter *self) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_get_order", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = GTK_SORTER_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->get_order != nullptr ? native->get_order(self) : static_cast<GtkSorterOrder>(0);
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  zval ret;
  ZVAL_UNDEF(&ret);
  GtkSorterOrder result = static_cast<GtkSorterOrder>(0);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    gint v = 0;
    if (enum_from_php(&ret, GTK_TYPE_SORTER_ORDER, &v)) result = static_cast<GtkSorterOrder>(v);
  }
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkSorter::vfunc_get_order");
  zend_exception_restore();  // the parked one, previous of whatever this threw
  return result;
}

// vfunc installer: GTK_SORTER_CLASS->get_order (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_get_order(gpointer klass) {
  GTK_SORTER_CLASS(klass)->get_order = vfunc_thunk_get_order;
}

}  // namespace

/**
 * Gtk4\GtkSorter::vfunc_compare(?GObject $item1, ?GObject $item2): GtkOrdering
 *
 * Native `compare` (SorterClass.compare): the GTK implementation below any PHP subclass, for
 * `parent::vfunc_compare()` from an override. Compares two given items according to the sort order
 * implemented by the sorter.
 */
ZEND_METHOD(Gtk4_GtkSorter, vfunc_compare) {
  zval *item1 = nullptr;
  zval *item2 = nullptr;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(item1, class_for_gtype(G_TYPE_OBJECT))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(item2, class_for_gtype(G_TYPE_OBJECT))
  ZEND_PARSE_PARAMETERS_END();
  GtkSorter *self = PHPGTK_SELF(GtkSorter, GTK_TYPE_SORTER);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(spl_ce_LogicException, 0,
                            "GtkSorter::vfunc_compare(): for parent:: chaining from a PHP subclass "
                            "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_SORTER_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->compare == nullptr) {
    enum_to_php(GTK_TYPE_ORDERING, 0, return_value);
    return;
  }
  GObject *item1_o = nullptr;
  if (item1 != nullptr) {
    item1_o = unwrap(item1, G_TYPE_OBJECT);
    if (item1_o == nullptr) RETURN_THROWS();
  }
  GObject *item2_o = nullptr;
  if (item2 != nullptr) {
    item2_o = unwrap(item2, G_TYPE_OBJECT);
    if (item2_o == nullptr) RETURN_THROWS();
  }
  enum_to_php(GTK_TYPE_ORDERING,
              klass->compare(self, item1_o != nullptr ? G_OBJECT(item1_o) : nullptr,
                             item2_o != nullptr ? G_OBJECT(item2_o) : nullptr),
              return_value);
}

/**
 * Gtk4\GtkSorter::vfunc_get_order(): GtkSorterOrder
 *
 * Native `get_order` (SorterClass.get_order): the GTK implementation below any PHP subclass, for
 * `parent::vfunc_get_order()` from an override. Gets the order that $self conforms to.
 */
ZEND_METHOD(Gtk4_GtkSorter, vfunc_get_order) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkSorter *self = PHPGTK_SELF(GtkSorter, GTK_TYPE_SORTER);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkSorter::vfunc_get_order(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_SORTER_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->get_order == nullptr) {
    enum_to_php(GTK_TYPE_SORTER_ORDER, 0, return_value);
    return;
  }
  enum_to_php(GTK_TYPE_SORTER_ORDER, klass->get_order(self), return_value);
}

// MINIT: the vfunc thunks of GtkSorter (core/subtype.h).
void register_vfuncs_GtkSorter() {
  register_vfunc(GTK_TYPE_SORTER, "compare", vfunc_install_compare);
  register_vfunc(GTK_TYPE_SORTER, "get_order", vfunc_install_get_order);
}
