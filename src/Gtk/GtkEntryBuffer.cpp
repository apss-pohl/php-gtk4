// GENERATED by gen/gir.php from Gtk.EntryBuffer - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkEntryBuffer
#include "php_gtk4.h"
#include "core/object.h"
#include "core/subtype.h"
#include "core/error.h"
#include <array>

using namespace phpgtk;

/**
 * Gtk4\GtkEntryBuffer::delete_text(int $position, int $n_chars): int
 *
 * Deletes a sequence of characters from the buffer.
 */
ZEND_METHOD(Gtk4_GtkEntryBuffer, delete_text) {
  zend_long position;
  zend_long n_chars;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_LONG(position)
  Z_PARAM_LONG(n_chars)
  ZEND_PARSE_PARAMETERS_END();
  GtkEntryBuffer *self = PHPGTK_SELF(GtkEntryBuffer, GTK_TYPE_ENTRY_BUFFER);
  if (!phpgtk::check_range<guint>(position, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(n_chars, 2)) RETURN_THROWS();
  RETURN_LONG(static_cast<zend_long>(
      gtk_entry_buffer_delete_text(self, static_cast<guint>(position), static_cast<int>(n_chars))));
}

/**
 * Gtk4\GtkEntryBuffer::emit_deleted_text(int $position, int $n_chars): void
 *
 * Used when subclassing `GtkEntryBuffer`.
 */
ZEND_METHOD(Gtk4_GtkEntryBuffer, emit_deleted_text) {
  zend_long position;
  zend_long n_chars;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_LONG(position)
  Z_PARAM_LONG(n_chars)
  ZEND_PARSE_PARAMETERS_END();
  GtkEntryBuffer *self = PHPGTK_SELF(GtkEntryBuffer, GTK_TYPE_ENTRY_BUFFER);
  if (!phpgtk::check_range<guint>(position, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<guint>(n_chars, 2)) RETURN_THROWS();
  gtk_entry_buffer_emit_deleted_text(self, static_cast<guint>(position),
                                     static_cast<guint>(n_chars));
}

/**
 * Gtk4\GtkEntryBuffer::emit_inserted_text(int $position, string $chars, int $n_chars): void
 *
 * Used when subclassing `GtkEntryBuffer`.
 */
ZEND_METHOD(Gtk4_GtkEntryBuffer, emit_inserted_text) {
  zend_long position;
  zend_string *chars;
  zend_long n_chars;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_LONG(position)
  Z_PARAM_STR(chars)
  Z_PARAM_LONG(n_chars)
  ZEND_PARSE_PARAMETERS_END();
  GtkEntryBuffer *self = PHPGTK_SELF(GtkEntryBuffer, GTK_TYPE_ENTRY_BUFFER);
  if (!phpgtk::check_range<guint>(position, 1)) RETURN_THROWS();
  if (!phpgtk::check_utf8(chars, 2)) RETURN_THROWS();
  if (!phpgtk::check_range<guint>(n_chars, 3)) RETURN_THROWS();
  gtk_entry_buffer_emit_inserted_text(self, static_cast<guint>(position), ZSTR_VAL(chars),
                                      static_cast<guint>(n_chars));
}

/**
 * Gtk4\GtkEntryBuffer::get_bytes(): int
 *
 * Retrieves the length in bytes of the buffer.
 */
ZEND_METHOD(Gtk4_GtkEntryBuffer, get_bytes) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkEntryBuffer *self = PHPGTK_SELF(GtkEntryBuffer, GTK_TYPE_ENTRY_BUFFER);
  RETURN_LONG(static_cast<zend_long>(gtk_entry_buffer_get_bytes(self)));
}

/**
 * Gtk4\GtkEntryBuffer::get_length(): int
 *
 * Retrieves the length in characters of the buffer.
 */
ZEND_METHOD(Gtk4_GtkEntryBuffer, get_length) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkEntryBuffer *self = PHPGTK_SELF(GtkEntryBuffer, GTK_TYPE_ENTRY_BUFFER);
  RETURN_LONG(static_cast<zend_long>(gtk_entry_buffer_get_length(self)));
}

/**
 * Gtk4\GtkEntryBuffer::get_max_length(): int
 *
 * Retrieves the maximum allowed length of the text in $buffer.
 */
ZEND_METHOD(Gtk4_GtkEntryBuffer, get_max_length) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkEntryBuffer *self = PHPGTK_SELF(GtkEntryBuffer, GTK_TYPE_ENTRY_BUFFER);
  RETURN_LONG(static_cast<zend_long>(gtk_entry_buffer_get_max_length(self)));
}

/**
 * Gtk4\GtkEntryBuffer::get_text(): string
 *
 * Retrieves the contents of the buffer.
 */
ZEND_METHOD(Gtk4_GtkEntryBuffer, get_text) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkEntryBuffer *self = PHPGTK_SELF(GtkEntryBuffer, GTK_TYPE_ENTRY_BUFFER);
  const char *phpgtk_ret = gtk_entry_buffer_get_text(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

/**
 * Gtk4\GtkEntryBuffer::insert_text(int $position, string $chars, int $n_chars): int
 *
 * Inserts $n_chars characters of $chars into the contents of the buffer, at position $position.
 */
ZEND_METHOD(Gtk4_GtkEntryBuffer, insert_text) {
  zend_long position;
  zend_string *chars;
  zend_long n_chars;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_LONG(position)
  Z_PARAM_STR(chars)
  Z_PARAM_LONG(n_chars)
  ZEND_PARSE_PARAMETERS_END();
  GtkEntryBuffer *self = PHPGTK_SELF(GtkEntryBuffer, GTK_TYPE_ENTRY_BUFFER);
  if (!phpgtk::check_range<guint>(position, 1)) RETURN_THROWS();
  if (!phpgtk::check_utf8(chars, 2)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(n_chars, 3)) RETURN_THROWS();
  RETURN_LONG(static_cast<zend_long>(gtk_entry_buffer_insert_text(
      self, static_cast<guint>(position), ZSTR_VAL(chars), static_cast<int>(n_chars))));
}

/**
 * Gtk4\GtkEntryBuffer::set_max_length(int $max_length): void
 *
 * Sets the maximum allowed length of the contents of the buffer.
 */
ZEND_METHOD(Gtk4_GtkEntryBuffer, set_max_length) {
  zend_long max_length;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(max_length)
  ZEND_PARSE_PARAMETERS_END();
  GtkEntryBuffer *self = PHPGTK_SELF(GtkEntryBuffer, GTK_TYPE_ENTRY_BUFFER);
  if (!phpgtk::check_range<int>(max_length, 1)) RETURN_THROWS();
  gtk_entry_buffer_set_max_length(self, static_cast<int>(max_length));
}

/**
 * Gtk4\GtkEntryBuffer::set_text(string $chars, int $n_chars): void
 *
 * Sets the text in the buffer.
 */
ZEND_METHOD(Gtk4_GtkEntryBuffer, set_text) {
  zend_string *chars;
  zend_long n_chars;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_STR(chars)
  Z_PARAM_LONG(n_chars)
  ZEND_PARSE_PARAMETERS_END();
  GtkEntryBuffer *self = PHPGTK_SELF(GtkEntryBuffer, GTK_TYPE_ENTRY_BUFFER);
  if (!phpgtk::check_utf8(chars, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(n_chars, 2)) RETURN_THROWS();
  gtk_entry_buffer_set_text(self, ZSTR_VAL(chars), static_cast<int>(n_chars));
}

/**
 * public function __construct(?string $initial_chars, int $n_initial_chars)
 * Create a new `GtkEntryBuffer` object.
 */
ZEND_METHOD(Gtk4_GtkEntryBuffer, __construct) {
  zend_string *initial_chars = nullptr;
  zend_long n_initial_chars;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_STR_OR_NULL(initial_chars)
  Z_PARAM_LONG(n_initial_chars)
  ZEND_PARSE_PARAMETERS_END();
  // Not gtk_entry_buffer_new() first: that always instantiates the native GType, so a PHP
  // subclass' vfunc_insert_text()/vfunc_delete_text()/... would never run - silently, because
  // the slots themselves are bound and nothing reports them as unbound. n_initial_chars is a
  // length rather than a construct property, so the text is applied after the instance exists,
  // which is all gtk_entry_buffer_new() does with its two arguments.
  if (initial_chars != nullptr && !phpgtk::check_utf8(initial_chars, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(n_initial_chars, 2)) RETURN_THROWS();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj =
        G_OBJECT(gtk_entry_buffer_new(initial_chars != nullptr ? ZSTR_VAL(initial_chars) : nullptr,
                                      static_cast<int>(n_initial_chars)));
  } else if (initial_chars != nullptr) {
    gtk_entry_buffer_set_text(GTK_ENTRY_BUFFER(obj), ZSTR_VAL(initial_chars),
                              static_cast<int>(n_initial_chars));
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

// vfunc thunks and installers: file-local, installed by class_init of a PHP subtype
namespace {

// vfunc thunk: GTK_ENTRY_BUFFER_CLASS->delete_text -> $this->vfunc_delete_text() on a PHP subclass
guint vfunc_thunk_delete_text(GtkEntryBuffer *self, guint position, guint n_chars) {
  zval zself;
  zend_function *fn = EG(exception) == nullptr
                          ? subtype_vfunc(G_OBJECT(self), "vfunc_delete_text", &zself)
                          : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    auto *native = GTK_ENTRY_BUFFER_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->delete_text != nullptr ? native->delete_text(self, position, n_chars) : 0;
  }
  std::array<zval, 2> args{};
  zval *argv = args.data();
  ZVAL_LONG(&argv[0], static_cast<zend_long>(position));
  ZVAL_LONG(&argv[1], static_cast<zend_long>(n_chars));
  zval ret;
  ZVAL_UNDEF(&ret);
  guint result = 0;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 2, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = static_cast<guint>(zval_get_long(&ret));
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkEntryBuffer::vfunc_delete_text");
  return result;
}

// vfunc installer: GTK_ENTRY_BUFFER_CLASS->delete_text (called from class_init / iface_init of a
// PHP subtype)
void vfunc_install_delete_text(gpointer klass) {
  GTK_ENTRY_BUFFER_CLASS(klass)->delete_text = vfunc_thunk_delete_text;
}

// vfunc thunk: GTK_ENTRY_BUFFER_CLASS->deleted_text -> $this->vfunc_deleted_text() on a PHP
// subclass
void vfunc_thunk_deleted_text(GtkEntryBuffer *self, guint position, guint n_chars) {
  zval zself;
  zend_function *fn = EG(exception) == nullptr
                          ? subtype_vfunc(G_OBJECT(self), "vfunc_deleted_text", &zself)
                          : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    auto *native = GTK_ENTRY_BUFFER_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->deleted_text != nullptr) native->deleted_text(self, position, n_chars);
    return;
  }
  std::array<zval, 2> args{};
  zval *argv = args.data();
  ZVAL_LONG(&argv[0], static_cast<zend_long>(position));
  ZVAL_LONG(&argv[1], static_cast<zend_long>(n_chars));
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 2, args.data());
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkEntryBuffer::vfunc_deleted_text");
}

// vfunc installer: GTK_ENTRY_BUFFER_CLASS->deleted_text (called from class_init / iface_init of a
// PHP subtype)
void vfunc_install_deleted_text(gpointer klass) {
  GTK_ENTRY_BUFFER_CLASS(klass)->deleted_text = vfunc_thunk_deleted_text;
}

// vfunc thunk: GTK_ENTRY_BUFFER_CLASS->get_length -> $this->vfunc_get_length() on a PHP subclass
guint vfunc_thunk_get_length(GtkEntryBuffer *self) {
  zval zself;
  zend_function *fn = EG(exception) == nullptr
                          ? subtype_vfunc(G_OBJECT(self), "vfunc_get_length", &zself)
                          : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    auto *native = GTK_ENTRY_BUFFER_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->get_length != nullptr ? native->get_length(self) : 0;
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  guint result = 0;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = static_cast<guint>(zval_get_long(&ret));
  }
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkEntryBuffer::vfunc_get_length");
  return result;
}

// vfunc installer: GTK_ENTRY_BUFFER_CLASS->get_length (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_get_length(gpointer klass) {
  GTK_ENTRY_BUFFER_CLASS(klass)->get_length = vfunc_thunk_get_length;
}

// vfunc thunk: GTK_ENTRY_BUFFER_CLASS->insert_text -> $this->vfunc_insert_text() on a PHP subclass
guint vfunc_thunk_insert_text(GtkEntryBuffer *self, guint position, const char *chars,
                              guint n_chars) {
  zval zself;
  zend_function *fn = EG(exception) == nullptr
                          ? subtype_vfunc(G_OBJECT(self), "vfunc_insert_text", &zself)
                          : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    auto *native = GTK_ENTRY_BUFFER_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->insert_text != nullptr ? native->insert_text(self, position, chars, n_chars) : 0;
  }
  std::array<zval, 3> args{};
  zval *argv = args.data();
  ZVAL_LONG(&argv[0], static_cast<zend_long>(position));
  if (chars == nullptr) {
    ZVAL_NULL(&argv[1]);
  } else {
    ZVAL_STRING(&argv[1], chars);
  }
  ZVAL_LONG(&argv[2], static_cast<zend_long>(n_chars));
  zval ret;
  ZVAL_UNDEF(&ret);
  guint result = 0;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 3, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = static_cast<guint>(zval_get_long(&ret));
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkEntryBuffer::vfunc_insert_text");
  return result;
}

// vfunc installer: GTK_ENTRY_BUFFER_CLASS->insert_text (called from class_init / iface_init of a
// PHP subtype)
void vfunc_install_insert_text(gpointer klass) {
  GTK_ENTRY_BUFFER_CLASS(klass)->insert_text = vfunc_thunk_insert_text;
}

// vfunc thunk: GTK_ENTRY_BUFFER_CLASS->inserted_text -> $this->vfunc_inserted_text() on a PHP
// subclass
void vfunc_thunk_inserted_text(GtkEntryBuffer *self, guint position, const char *chars,
                               guint n_chars) {
  zval zself;
  zend_function *fn = EG(exception) == nullptr
                          ? subtype_vfunc(G_OBJECT(self), "vfunc_inserted_text", &zself)
                          : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    auto *native = GTK_ENTRY_BUFFER_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->inserted_text != nullptr) native->inserted_text(self, position, chars, n_chars);
    return;
  }
  std::array<zval, 3> args{};
  zval *argv = args.data();
  ZVAL_LONG(&argv[0], static_cast<zend_long>(position));
  if (chars == nullptr) {
    ZVAL_NULL(&argv[1]);
  } else {
    ZVAL_STRING(&argv[1], chars);
  }
  ZVAL_LONG(&argv[2], static_cast<zend_long>(n_chars));
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 3, args.data());
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkEntryBuffer::vfunc_inserted_text");
}

// vfunc installer: GTK_ENTRY_BUFFER_CLASS->inserted_text (called from class_init / iface_init of a
// PHP subtype)
void vfunc_install_inserted_text(gpointer klass) {
  GTK_ENTRY_BUFFER_CLASS(klass)->inserted_text = vfunc_thunk_inserted_text;
}

}  // namespace

/**
 * Gtk4\GtkEntryBuffer::vfunc_delete_text(int $position, int $n_chars): int
 *
 * Native `delete_text` (EntryBufferClass.delete_text): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_delete_text()` from an override. Deletes a sequence of characters
 * from the buffer.
 */
ZEND_METHOD(Gtk4_GtkEntryBuffer, vfunc_delete_text) {
  zend_long position;
  zend_long n_chars;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_LONG(position)
  Z_PARAM_LONG(n_chars)
  ZEND_PARSE_PARAMETERS_END();
  GtkEntryBuffer *self = PHPGTK_SELF(GtkEntryBuffer, GTK_TYPE_ENTRY_BUFFER);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkEntryBuffer::vfunc_delete_text(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_ENTRY_BUFFER_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->delete_text == nullptr) {
    RETURN_LONG(0);
  }
  if (!phpgtk::check_range<guint>(position, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<guint>(n_chars, 2)) RETURN_THROWS();
  RETURN_LONG(static_cast<zend_long>(
      klass->delete_text(self, static_cast<guint>(position), static_cast<guint>(n_chars))));
}

/**
 * Gtk4\GtkEntryBuffer::vfunc_deleted_text(int $position, int $n_chars): void
 *
 * Native `deleted_text` (EntryBufferClass.deleted_text): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_deleted_text()` from an override.
 */
ZEND_METHOD(Gtk4_GtkEntryBuffer, vfunc_deleted_text) {
  zend_long position;
  zend_long n_chars;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_LONG(position)
  Z_PARAM_LONG(n_chars)
  ZEND_PARSE_PARAMETERS_END();
  GtkEntryBuffer *self = PHPGTK_SELF(GtkEntryBuffer, GTK_TYPE_ENTRY_BUFFER);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkEntryBuffer::vfunc_deleted_text(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_ENTRY_BUFFER_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->deleted_text == nullptr) {
    return;
  }
  if (!phpgtk::check_range<guint>(position, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<guint>(n_chars, 2)) RETURN_THROWS();
  klass->deleted_text(self, static_cast<guint>(position), static_cast<guint>(n_chars));
}

/**
 * Gtk4\GtkEntryBuffer::vfunc_get_length(): int
 *
 * Native `get_length` (EntryBufferClass.get_length): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_get_length()` from an override. Retrieves the length in characters
 * of the buffer.
 */
ZEND_METHOD(Gtk4_GtkEntryBuffer, vfunc_get_length) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkEntryBuffer *self = PHPGTK_SELF(GtkEntryBuffer, GTK_TYPE_ENTRY_BUFFER);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkEntryBuffer::vfunc_get_length(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_ENTRY_BUFFER_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->get_length == nullptr) {
    RETURN_LONG(0);
  }
  RETURN_LONG(static_cast<zend_long>(klass->get_length(self)));
}

/**
 * Gtk4\GtkEntryBuffer::vfunc_insert_text(int $position, string $chars, int $n_chars): int
 *
 * Native `insert_text` (EntryBufferClass.insert_text): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_insert_text()` from an override. Inserts $n_chars characters of
 * $chars into the contents of the buffer, at position $position.
 */
ZEND_METHOD(Gtk4_GtkEntryBuffer, vfunc_insert_text) {
  zend_long position;
  zend_string *chars;
  zend_long n_chars;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_LONG(position)
  Z_PARAM_STR(chars)
  Z_PARAM_LONG(n_chars)
  ZEND_PARSE_PARAMETERS_END();
  GtkEntryBuffer *self = PHPGTK_SELF(GtkEntryBuffer, GTK_TYPE_ENTRY_BUFFER);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkEntryBuffer::vfunc_insert_text(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_ENTRY_BUFFER_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->insert_text == nullptr) {
    RETURN_LONG(0);
  }
  if (!phpgtk::check_range<guint>(position, 1)) RETURN_THROWS();
  if (!phpgtk::check_utf8(chars, 2)) RETURN_THROWS();
  if (!phpgtk::check_range<guint>(n_chars, 3)) RETURN_THROWS();
  RETURN_LONG(static_cast<zend_long>(klass->insert_text(
      self, static_cast<guint>(position), ZSTR_VAL(chars), static_cast<guint>(n_chars))));
}

/**
 * Gtk4\GtkEntryBuffer::vfunc_inserted_text(int $position, string $chars, int $n_chars): void
 *
 * Native `inserted_text` (EntryBufferClass.inserted_text): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_inserted_text()` from an override.
 */
ZEND_METHOD(Gtk4_GtkEntryBuffer, vfunc_inserted_text) {
  zend_long position;
  zend_string *chars;
  zend_long n_chars;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_LONG(position)
  Z_PARAM_STR(chars)
  Z_PARAM_LONG(n_chars)
  ZEND_PARSE_PARAMETERS_END();
  GtkEntryBuffer *self = PHPGTK_SELF(GtkEntryBuffer, GTK_TYPE_ENTRY_BUFFER);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkEntryBuffer::vfunc_inserted_text(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_ENTRY_BUFFER_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->inserted_text == nullptr) {
    return;
  }
  if (!phpgtk::check_range<guint>(position, 1)) RETURN_THROWS();
  if (!phpgtk::check_utf8(chars, 2)) RETURN_THROWS();
  if (!phpgtk::check_range<guint>(n_chars, 3)) RETURN_THROWS();
  klass->inserted_text(self, static_cast<guint>(position), ZSTR_VAL(chars),
                       static_cast<guint>(n_chars));
}

// MINIT: the vfunc thunks of GtkEntryBuffer (core/subtype.h).
void register_vfuncs_GtkEntryBuffer() {
  register_vfunc(GTK_TYPE_ENTRY_BUFFER, "delete_text", vfunc_install_delete_text);
  register_vfunc(GTK_TYPE_ENTRY_BUFFER, "deleted_text", vfunc_install_deleted_text);
  register_vfunc(GTK_TYPE_ENTRY_BUFFER, "get_length", vfunc_install_get_length);
  register_vfunc(GTK_TYPE_ENTRY_BUFFER, "insert_text", vfunc_install_insert_text);
  register_vfunc(GTK_TYPE_ENTRY_BUFFER, "inserted_text", vfunc_install_inserted_text);
}
