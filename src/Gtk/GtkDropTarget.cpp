// GENERATED by gen/gir.php from Gtk.DropTarget - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkDropTarget
#include "php_gtk4.h"
#include "core/object.h"
#include "core/boxed.h"

using namespace phpgtk;

// Payload types are named from PHP (core/marshal, gtype_from_php_name / php_name_for_gtype);
// GIR's own GType parameters and GValue results are what the generator skips here.
#include "core/marshal.h"
#include "core/subtype.h"

#include <vector>

/**
 * Gtk4\GtkDropTarget::get_actions(): int
 *
 * Gets the actions that this drop target supports.
 */
ZEND_METHOD(Gtk4_GtkDropTarget, get_actions) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkDropTarget *self = PHPGTK_SELF(GtkDropTarget, GTK_TYPE_DROP_TARGET);
  RETURN_LONG(static_cast<zend_long>(gtk_drop_target_get_actions(self)));
}

/**
 * Gtk4\GtkDropTarget::get_current_drop(): ?GdkDrop
 *
 * Gets the currently handled drop operation.
 */
ZEND_METHOD(Gtk4_GtkDropTarget, get_current_drop) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkDropTarget *self = PHPGTK_SELF(GtkDropTarget, GTK_TYPE_DROP_TARGET);
  GdkDrop *phpgtk_ret = gtk_drop_target_get_current_drop(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkDropTarget::get_formats(): ?GdkContentFormats
 *
 * Gets the data formats that this drop target accepts.
 */
ZEND_METHOD(Gtk4_GtkDropTarget, get_formats) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkDropTarget *self = PHPGTK_SELF(GtkDropTarget, GTK_TYPE_DROP_TARGET);
  GdkContentFormats *phpgtk_ret = gtk_drop_target_get_formats(self);
  wrap_boxed(GDK_TYPE_CONTENT_FORMATS, phpgtk_ret, return_value);
}

/**
 * Gtk4\GtkDropTarget::get_preload(): bool
 *
 * Gets whether data should be preloaded on hover.
 */
ZEND_METHOD(Gtk4_GtkDropTarget, get_preload) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkDropTarget *self = PHPGTK_SELF(GtkDropTarget, GTK_TYPE_DROP_TARGET);
  RETURN_BOOL(gtk_drop_target_get_preload(self));
}

/**
 * Gtk4\GtkDropTarget::reject(): void
 *
 * Rejects the ongoing drop operation.
 */
ZEND_METHOD(Gtk4_GtkDropTarget, reject) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkDropTarget *self = PHPGTK_SELF(GtkDropTarget, GTK_TYPE_DROP_TARGET);
  gtk_drop_target_reject(self);
}

/**
 * Gtk4\GtkDropTarget::set_actions(int $actions): void
 *
 * Sets the actions that this drop target supports.
 */
ZEND_METHOD(Gtk4_GtkDropTarget, set_actions) {
  zend_long actions;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(actions)
  ZEND_PARSE_PARAMETERS_END();
  GtkDropTarget *self = PHPGTK_SELF(GtkDropTarget, GTK_TYPE_DROP_TARGET);
  if (!phpgtk::check_flags(GDK_TYPE_DRAG_ACTION, actions, 1)) RETURN_THROWS();
  gtk_drop_target_set_actions(self, static_cast<GdkDragAction>(actions));
}

/**
 * Gtk4\GtkDropTarget::set_preload(bool $preload): void
 *
 * Sets whether data should be preloaded on hover.
 */
ZEND_METHOD(Gtk4_GtkDropTarget, set_preload) {
  bool preload;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(preload)
  ZEND_PARSE_PARAMETERS_END();
  GtkDropTarget *self = PHPGTK_SELF(GtkDropTarget, GTK_TYPE_DROP_TARGET);
  gtk_drop_target_set_preload(self, preload);
}

/**
 * public function __construct(string $type, int $actions)
 * A drop target accepting $type, offering $actions (a GdkDragAction mask).
 *
 * GIR's constructor takes a GType, which PHP has no spelling for; the type is named the way a
 * payload's is elsewhere - "string"/"int"/"float"/"bool" or a registered class name
 * (core/marshal, gtype_from_php_name). Without it a drop target accepts nothing at all.
 */
ZEND_METHOD(Gtk4_GtkDropTarget, __construct) {
  zend_string *type;
  zend_long actions;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_STR(type)
  Z_PARAM_LONG(actions)
  ZEND_PARSE_PARAMETERS_END();
  const GType t = gtype_from_php_name(type, 1);
  if (t == 0) RETURN_THROWS();
  if (!check_flags(GDK_TYPE_DRAG_ACTION, actions, 2)) RETURN_THROWS();

  GObject *obj = subtype_new(ZEND_THIS, "actions", static_cast<GdkDragAction>(actions), nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gtk_drop_target_new(t, static_cast<GdkDragAction>(actions)));
  } else {
    GType only = t;
    gtk_drop_target_set_gtypes(GTK_DROP_TARGET(obj), &only, 1);  // a subtype misses the ctor arg
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * public function get_gtypes(): array
 * The types this target accepts, in the spelling set_gtypes() takes.
 *
 * @return list<string>
 */
ZEND_METHOD(Gtk4_GtkDropTarget, get_gtypes) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkDropTarget *self = PHPGTK_SELF(GtkDropTarget, GTK_TYPE_DROP_TARGET);
  gsize count = 0;
  const GType *types = gtk_drop_target_get_gtypes(self, &count);
  array_init_size(return_value, static_cast<uint32_t>(count));
  for (gsize i = 0; types != nullptr && i < count; i++) {
    zend_string *name = php_name_for_gtype(types[i]);
    add_next_index_str(return_value, name);
  }
}

/**
 * public function get_value(): mixed
 * The value being dropped, or null outside a drop.
 *
 * GIR answers with a GValue; the marshaller turns it into the PHP value the source offered.
 */
ZEND_METHOD(Gtk4_GtkDropTarget, get_value) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkDropTarget *self = PHPGTK_SELF(GtkDropTarget, GTK_TYPE_DROP_TARGET);
  const GValue *gv = gtk_drop_target_get_value(self);
  if (gv == nullptr || !G_IS_VALUE(gv)) RETURN_NULL();
  to_php(gv, return_value);
}

/**
 * public function set_gtypes(array $types): void
 * The types this target accepts, named as in the constructor.
 */
ZEND_METHOD(Gtk4_GtkDropTarget, set_gtypes) {
  HashTable *types;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_ARRAY_HT(types)
  ZEND_PARSE_PARAMETERS_END();
  GtkDropTarget *self = PHPGTK_SELF(GtkDropTarget, GTK_TYPE_DROP_TARGET);

  std::vector<GType> resolved;
  resolved.reserve(zend_hash_num_elements(types));
  zval *entry = nullptr;
  // NOLINTNEXTLINE(readability-math-missing-parentheses) Zend macro expansion
  ZEND_HASH_FOREACH_VAL(types, entry) {
    if (Z_TYPE_P(entry) != IS_STRING) {
      zend_argument_type_error(1, "must be a list of type names");
      RETURN_THROWS();
    }
    const GType t = gtype_from_php_name(Z_STR_P(entry), 1);
    if (t == 0) RETURN_THROWS();
    resolved.push_back(t);
  }
  ZEND_HASH_FOREACH_END();
  gtk_drop_target_set_gtypes(self, resolved.empty() ? nullptr : resolved.data(), resolved.size());
}
