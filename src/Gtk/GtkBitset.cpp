// GENERATED by gen/gir.php from Gtk.Bitset - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkBitset (boxed value type)
#include "php_gtk4.h"
#include "core/boxed.h"
#include "core/object.h"
#include <cstring>

using namespace phpgtk;

namespace {

const char *const fields[] = {nullptr};

// Boxed field reader: the public scalar fields as PHP properties.
bool read(gpointer data, const char *field, zval *rv) {
  (void)data;
  (void)field;
  (void)rv;
  return false;
}

// Boxed field writer: each field converts like a parameter of its type (core/boxed).
bool write(gpointer data, const char *field, zval *v) {
  (void)data;
  (void)field;
  (void)v;
  return false;
}

}  // namespace

/**
 * Gtk4\GtkBitset::__construct()
 *
 * GtkBitset values come from GTK, never from `new`.
 */
ZEND_METHOD(Gtk4_GtkBitset, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
}

/**
 * static Gtk4\GtkBitset::new_empty(): GtkBitset
 *
 * Creates a new empty bitset.
 */
ZEND_METHOD(Gtk4_GtkBitset, new_empty) {
  ZEND_PARSE_PARAMETERS_NONE();
  gpointer phpgtk_ret = gtk_bitset_new_empty();
  wrap_boxed(GTK_TYPE_BITSET, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(GTK_TYPE_BITSET, phpgtk_ret);
}

/**
 * static Gtk4\GtkBitset::new_range(int $start, int $n_items): GtkBitset
 *
 * Creates a bitset with the given range set.
 */
ZEND_METHOD(Gtk4_GtkBitset, new_range) {
  zend_long start;
  zend_long n_items;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_LONG(start)
  Z_PARAM_LONG(n_items)
  ZEND_PARSE_PARAMETERS_END();
  if (!phpgtk::check_range<guint>(start, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<guint>(n_items, 2)) RETURN_THROWS();
  gpointer phpgtk_ret =
      gtk_bitset_new_range(static_cast<guint>(start), static_cast<guint>(n_items));
  wrap_boxed(GTK_TYPE_BITSET, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(GTK_TYPE_BITSET, phpgtk_ret);
}

/**
 * Gtk4\GtkBitset::add(int $value): bool
 *
 * Adds $value to $self if it wasn't part of it before.
 */
ZEND_METHOD(Gtk4_GtkBitset, add) {
  zend_long value;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(value)
  ZEND_PARSE_PARAMETERS_END();
  GtkBitset *self = PHPGTK_BOXED_SELF(GtkBitset);
  if (!phpgtk::check_range<guint>(value, 1)) RETURN_THROWS();
  RETURN_BOOL(gtk_bitset_add(self, static_cast<guint>(value)));
}

/**
 * Gtk4\GtkBitset::add_range(int $start, int $n_items): void
 *
 * Adds all values from $start (inclusive) to $start + $n_items (exclusive) in $self.
 */
ZEND_METHOD(Gtk4_GtkBitset, add_range) {
  zend_long start;
  zend_long n_items;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_LONG(start)
  Z_PARAM_LONG(n_items)
  ZEND_PARSE_PARAMETERS_END();
  GtkBitset *self = PHPGTK_BOXED_SELF(GtkBitset);
  if (!phpgtk::check_range<guint>(start, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<guint>(n_items, 2)) RETURN_THROWS();
  gtk_bitset_add_range(self, static_cast<guint>(start), static_cast<guint>(n_items));
}

/**
 * Gtk4\GtkBitset::add_range_closed(int $first, int $last): void
 *
 * Adds the closed range [$first, $last], so $first, $last and all values in between. $first must
 * be smaller than $last.
 */
ZEND_METHOD(Gtk4_GtkBitset, add_range_closed) {
  zend_long first;
  zend_long last;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_LONG(first)
  Z_PARAM_LONG(last)
  ZEND_PARSE_PARAMETERS_END();
  GtkBitset *self = PHPGTK_BOXED_SELF(GtkBitset);
  if (!phpgtk::check_range<guint>(first, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<guint>(last, 2)) RETURN_THROWS();
  gtk_bitset_add_range_closed(self, static_cast<guint>(first), static_cast<guint>(last));
}

/**
 * Gtk4\GtkBitset::add_rectangle(int $start, int $width, int $height, int $stride): void
 *
 * Interprets the values as a 2-dimensional boolean grid with the given $stride and inside that
 * grid, adds a rectangle with the given $width and $height.
 */
ZEND_METHOD(Gtk4_GtkBitset, add_rectangle) {
  zend_long start;
  zend_long width;
  zend_long height;
  zend_long stride;
  ZEND_PARSE_PARAMETERS_START(4, 4)
  Z_PARAM_LONG(start)
  Z_PARAM_LONG(width)
  Z_PARAM_LONG(height)
  Z_PARAM_LONG(stride)
  ZEND_PARSE_PARAMETERS_END();
  GtkBitset *self = PHPGTK_BOXED_SELF(GtkBitset);
  if (!phpgtk::check_range<guint>(start, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<guint>(width, 2)) RETURN_THROWS();
  if (!phpgtk::check_range<guint>(height, 3)) RETURN_THROWS();
  if (!phpgtk::check_range<guint>(stride, 4)) RETURN_THROWS();
  gtk_bitset_add_rectangle(self, static_cast<guint>(start), static_cast<guint>(width),
                           static_cast<guint>(height), static_cast<guint>(stride));
}

/**
 * Gtk4\GtkBitset::contains(int $value): bool
 *
 * Checks if the given $value has been added to $self
 */
ZEND_METHOD(Gtk4_GtkBitset, contains) {
  zend_long value;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(value)
  ZEND_PARSE_PARAMETERS_END();
  GtkBitset *self = PHPGTK_BOXED_SELF(GtkBitset);
  if (!phpgtk::check_range<guint>(value, 1)) RETURN_THROWS();
  RETURN_BOOL(gtk_bitset_contains(self, static_cast<guint>(value)));
}

/**
 * Gtk4\GtkBitset::difference(GtkBitset $other): void
 *
 * Sets $self to be the symmetric difference of $self and $other.
 */
ZEND_METHOD(Gtk4_GtkBitset, difference) {
  zval *other;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(other, boxed_class_for_type(GTK_TYPE_BITSET)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkBitset *self = PHPGTK_BOXED_SELF(GtkBitset);
  gpointer other_b = unwrap_boxed(other, GTK_TYPE_BITSET);
  if (other_b == nullptr) RETURN_THROWS();
  gtk_bitset_difference(self, static_cast<GtkBitset *>(other_b));
}

/**
 * Gtk4\GtkBitset::equals(GtkBitset $other): bool
 *
 * Returns `true` if $self and $other contain the same values.
 */
ZEND_METHOD(Gtk4_GtkBitset, equals) {
  zval *other;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(other, boxed_class_for_type(GTK_TYPE_BITSET)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkBitset *self = PHPGTK_BOXED_SELF(GtkBitset);
  gpointer other_b = unwrap_boxed(other, GTK_TYPE_BITSET);
  if (other_b == nullptr) RETURN_THROWS();
  RETURN_BOOL(gtk_bitset_equals(self, static_cast<GtkBitset *>(other_b)));
}

/**
 * Gtk4\GtkBitset::get_maximum(): int
 *
 * Returns the largest value in $self.
 */
ZEND_METHOD(Gtk4_GtkBitset, get_maximum) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkBitset *self = PHPGTK_BOXED_SELF(GtkBitset);
  RETURN_LONG(static_cast<zend_long>(gtk_bitset_get_maximum(self)));
}

/**
 * Gtk4\GtkBitset::get_minimum(): int
 *
 * Returns the smallest value in $self.
 */
ZEND_METHOD(Gtk4_GtkBitset, get_minimum) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkBitset *self = PHPGTK_BOXED_SELF(GtkBitset);
  RETURN_LONG(static_cast<zend_long>(gtk_bitset_get_minimum(self)));
}

/**
 * Gtk4\GtkBitset::get_nth(int $nth): int
 *
 * Returns the value of the $nth item in self.
 */
ZEND_METHOD(Gtk4_GtkBitset, get_nth) {
  zend_long nth;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(nth)
  ZEND_PARSE_PARAMETERS_END();
  GtkBitset *self = PHPGTK_BOXED_SELF(GtkBitset);
  if (!phpgtk::check_range<guint>(nth, 1)) RETURN_THROWS();
  RETURN_LONG(static_cast<zend_long>(gtk_bitset_get_nth(self, static_cast<guint>(nth))));
}

/**
 * Gtk4\GtkBitset::get_size(): int
 *
 * Gets the number of values that were added to the set.
 */
ZEND_METHOD(Gtk4_GtkBitset, get_size) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkBitset *self = PHPGTK_BOXED_SELF(GtkBitset);
  RETURN_LONG(static_cast<zend_long>(gtk_bitset_get_size(self)));
}

/**
 * Gtk4\GtkBitset::get_size_in_range(int $first, int $last): int
 *
 * Gets the number of values that are part of the set from $first to $last (inclusive).
 */
ZEND_METHOD(Gtk4_GtkBitset, get_size_in_range) {
  zend_long first;
  zend_long last;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_LONG(first)
  Z_PARAM_LONG(last)
  ZEND_PARSE_PARAMETERS_END();
  GtkBitset *self = PHPGTK_BOXED_SELF(GtkBitset);
  if (!phpgtk::check_range<guint>(first, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<guint>(last, 2)) RETURN_THROWS();
  RETURN_LONG(static_cast<zend_long>(
      gtk_bitset_get_size_in_range(self, static_cast<guint>(first), static_cast<guint>(last))));
}

/**
 * Gtk4\GtkBitset::intersect(GtkBitset $other): void
 *
 * Sets $self to be the intersection of $self and $other.
 */
ZEND_METHOD(Gtk4_GtkBitset, intersect) {
  zval *other;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(other, boxed_class_for_type(GTK_TYPE_BITSET)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkBitset *self = PHPGTK_BOXED_SELF(GtkBitset);
  gpointer other_b = unwrap_boxed(other, GTK_TYPE_BITSET);
  if (other_b == nullptr) RETURN_THROWS();
  gtk_bitset_intersect(self, static_cast<GtkBitset *>(other_b));
}

/**
 * Gtk4\GtkBitset::is_empty(): bool
 *
 * Check if no value is contained in bitset.
 */
ZEND_METHOD(Gtk4_GtkBitset, is_empty) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkBitset *self = PHPGTK_BOXED_SELF(GtkBitset);
  RETURN_BOOL(gtk_bitset_is_empty(self));
}

/**
 * Gtk4\GtkBitset::remove(int $value): bool
 *
 * Removes $value from $self if it was part of it before.
 */
ZEND_METHOD(Gtk4_GtkBitset, remove) {
  zend_long value;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(value)
  ZEND_PARSE_PARAMETERS_END();
  GtkBitset *self = PHPGTK_BOXED_SELF(GtkBitset);
  if (!phpgtk::check_range<guint>(value, 1)) RETURN_THROWS();
  RETURN_BOOL(gtk_bitset_remove(self, static_cast<guint>(value)));
}

/**
 * Gtk4\GtkBitset::remove_all(): void
 *
 * Removes all values from the bitset so that it is empty again.
 */
ZEND_METHOD(Gtk4_GtkBitset, remove_all) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkBitset *self = PHPGTK_BOXED_SELF(GtkBitset);
  gtk_bitset_remove_all(self);
}

/**
 * Gtk4\GtkBitset::remove_range(int $start, int $n_items): void
 *
 * Removes all values from $start (inclusive) to $start + $n_items (exclusive) in $self.
 */
ZEND_METHOD(Gtk4_GtkBitset, remove_range) {
  zend_long start;
  zend_long n_items;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_LONG(start)
  Z_PARAM_LONG(n_items)
  ZEND_PARSE_PARAMETERS_END();
  GtkBitset *self = PHPGTK_BOXED_SELF(GtkBitset);
  if (!phpgtk::check_range<guint>(start, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<guint>(n_items, 2)) RETURN_THROWS();
  gtk_bitset_remove_range(self, static_cast<guint>(start), static_cast<guint>(n_items));
}

/**
 * Gtk4\GtkBitset::remove_range_closed(int $first, int $last): void
 *
 * Removes the closed range [$first, $last], so $first, $last and all values in between. $first
 * must be smaller than $last.
 */
ZEND_METHOD(Gtk4_GtkBitset, remove_range_closed) {
  zend_long first;
  zend_long last;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_LONG(first)
  Z_PARAM_LONG(last)
  ZEND_PARSE_PARAMETERS_END();
  GtkBitset *self = PHPGTK_BOXED_SELF(GtkBitset);
  if (!phpgtk::check_range<guint>(first, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<guint>(last, 2)) RETURN_THROWS();
  gtk_bitset_remove_range_closed(self, static_cast<guint>(first), static_cast<guint>(last));
}

/**
 * Gtk4\GtkBitset::remove_rectangle(int $start, int $width, int $height, int $stride): void
 *
 * Interprets the values as a 2-dimensional boolean grid with the given $stride and inside that
 * grid, removes a rectangle with the given $width and $height.
 */
ZEND_METHOD(Gtk4_GtkBitset, remove_rectangle) {
  zend_long start;
  zend_long width;
  zend_long height;
  zend_long stride;
  ZEND_PARSE_PARAMETERS_START(4, 4)
  Z_PARAM_LONG(start)
  Z_PARAM_LONG(width)
  Z_PARAM_LONG(height)
  Z_PARAM_LONG(stride)
  ZEND_PARSE_PARAMETERS_END();
  GtkBitset *self = PHPGTK_BOXED_SELF(GtkBitset);
  if (!phpgtk::check_range<guint>(start, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<guint>(width, 2)) RETURN_THROWS();
  if (!phpgtk::check_range<guint>(height, 3)) RETURN_THROWS();
  if (!phpgtk::check_range<guint>(stride, 4)) RETURN_THROWS();
  gtk_bitset_remove_rectangle(self, static_cast<guint>(start), static_cast<guint>(width),
                              static_cast<guint>(height), static_cast<guint>(stride));
}

/**
 * Gtk4\GtkBitset::shift_left(int $amount): void
 *
 * Shifts all values in $self to the left by $amount.
 */
ZEND_METHOD(Gtk4_GtkBitset, shift_left) {
  zend_long amount;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(amount)
  ZEND_PARSE_PARAMETERS_END();
  GtkBitset *self = PHPGTK_BOXED_SELF(GtkBitset);
  if (!phpgtk::check_range<guint>(amount, 1)) RETURN_THROWS();
  gtk_bitset_shift_left(self, static_cast<guint>(amount));
}

/**
 * Gtk4\GtkBitset::shift_right(int $amount): void
 *
 * Shifts all values in $self to the right by $amount.
 */
ZEND_METHOD(Gtk4_GtkBitset, shift_right) {
  zend_long amount;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(amount)
  ZEND_PARSE_PARAMETERS_END();
  GtkBitset *self = PHPGTK_BOXED_SELF(GtkBitset);
  if (!phpgtk::check_range<guint>(amount, 1)) RETURN_THROWS();
  gtk_bitset_shift_right(self, static_cast<guint>(amount));
}

/**
 * Gtk4\GtkBitset::splice(int $position, int $removed, int $added): void
 *
 * This is a support function for `GListModel` handling, by mirroring the
 * `GlistModel::items-changed` signal.
 */
ZEND_METHOD(Gtk4_GtkBitset, splice) {
  zend_long position;
  zend_long removed;
  zend_long added;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_LONG(position)
  Z_PARAM_LONG(removed)
  Z_PARAM_LONG(added)
  ZEND_PARSE_PARAMETERS_END();
  GtkBitset *self = PHPGTK_BOXED_SELF(GtkBitset);
  if (!phpgtk::check_range<guint>(position, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<guint>(removed, 2)) RETURN_THROWS();
  if (!phpgtk::check_range<guint>(added, 3)) RETURN_THROWS();
  gtk_bitset_splice(self, static_cast<guint>(position), static_cast<guint>(removed),
                    static_cast<guint>(added));
}

/**
 * Gtk4\GtkBitset::subtract(GtkBitset $other): void
 *
 * Sets $self to be the subtraction of $other from $self.
 */
ZEND_METHOD(Gtk4_GtkBitset, subtract) {
  zval *other;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(other, boxed_class_for_type(GTK_TYPE_BITSET)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkBitset *self = PHPGTK_BOXED_SELF(GtkBitset);
  gpointer other_b = unwrap_boxed(other, GTK_TYPE_BITSET);
  if (other_b == nullptr) RETURN_THROWS();
  gtk_bitset_subtract(self, static_cast<GtkBitset *>(other_b));
}

/**
 * Gtk4\GtkBitset::union(GtkBitset $other): void
 *
 * Sets $self to be the union of $self and $other.
 */
ZEND_METHOD(Gtk4_GtkBitset, union) {
  zval *other;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(other, boxed_class_for_type(GTK_TYPE_BITSET)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkBitset *self = PHPGTK_BOXED_SELF(GtkBitset);
  gpointer other_b = unwrap_boxed(other, GTK_TYPE_BITSET);
  if (other_b == nullptr) RETURN_THROWS();
  gtk_bitset_union(self, static_cast<GtkBitset *>(other_b));
}

namespace phpgtk {
// MINIT: bind the PHP class to GTK_TYPE_BITSET with its field table.
void register_GtkBitset(zend_class_entry *ce) {
  register_boxed(BoxedClass{
      .type = GTK_TYPE_BITSET,
      .ce = ce,
      .fields = fields,
      .read = read,
      .write = write,
      .copy =
          [](gconstpointer d) {
            return static_cast<gpointer>(gtk_bitset_copy(static_cast<const GtkBitset *>(d)));
          },
      .equal =
          [](gconstpointer a, gconstpointer b) {
            return gtk_bitset_equals(static_cast<const GtkBitset *>(a),
                                     static_cast<const GtkBitset *>(b)) != FALSE;
          }});
}
}  // namespace phpgtk
