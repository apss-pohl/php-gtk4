// GENERATED by gen/gir.php from Gtk.ColorDialogButton - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkColorDialogButton
#include "php_gtk4.h"
#include "core/object.h"
#include "core/boxed.h"
#include "core/subtype.h"

using namespace phpgtk;

/**
 * Gtk4\GtkColorDialogButton::__construct(?GtkColorDialog $dialog = null)
 *
 * Creates a new `GtkColorDialogButton` with the given `GtkColorDialog`.
 */
ZEND_METHOD(Gtk4_GtkColorDialogButton, __construct) {
  zval *dialog = nullptr;
  ZEND_PARSE_PARAMETERS_START(0, 1)
  Z_PARAM_OPTIONAL
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(dialog, class_for_gtype(GTK_TYPE_COLOR_DIALOG))
  ZEND_PARSE_PARAMETERS_END();
  GObject *dialog_o = nullptr;
  if (dialog != nullptr) {
    dialog_o = unwrap(dialog, GTK_TYPE_COLOR_DIALOG);
    if (dialog_o == nullptr) RETURN_THROWS();
  }
  if (dialog_o != nullptr) g_object_ref(dialog_o);  // transfer full
  GObject *obj = subtype_new(ZEND_THIS, "dialog",
                             dialog_o != nullptr ? GTK_COLOR_DIALOG(dialog_o) : nullptr, nullptr);
  if (obj != nullptr) {
    if (dialog_o != nullptr) g_object_unref(dialog_o);
  }
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(
        gtk_color_dialog_button_new(dialog_o != nullptr ? GTK_COLOR_DIALOG(dialog_o) : nullptr));
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkColorDialogButton::get_dialog(): ?GtkColorDialog
 *
 * Returns the `GtkColorDialog` of $self.
 */
ZEND_METHOD(Gtk4_GtkColorDialogButton, get_dialog) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkColorDialogButton *self = PHPGTK_SELF(GtkColorDialogButton, GTK_TYPE_COLOR_DIALOG_BUTTON);
  GtkColorDialog *phpgtk_ret = gtk_color_dialog_button_get_dialog(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkColorDialogButton::get_rgba(): GdkRGBA
 *
 * Returns the color of the button.
 */
ZEND_METHOD(Gtk4_GtkColorDialogButton, get_rgba) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkColorDialogButton *self = PHPGTK_SELF(GtkColorDialogButton, GTK_TYPE_COLOR_DIALOG_BUTTON);
  const GdkRGBA *phpgtk_ret = gtk_color_dialog_button_get_rgba(self);
  wrap_boxed(GDK_TYPE_RGBA, phpgtk_ret, return_value);
}

/**
 * Gtk4\GtkColorDialogButton::set_dialog(GtkColorDialog $dialog): void
 *
 * Sets a `GtkColorDialog` object to use for creating the color chooser dialog that is presented
 * when the user clicks the button.
 */
ZEND_METHOD(Gtk4_GtkColorDialogButton, set_dialog) {
  zval *dialog;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(dialog, class_for_gtype(GTK_TYPE_COLOR_DIALOG))
  ZEND_PARSE_PARAMETERS_END();
  GtkColorDialogButton *self = PHPGTK_SELF(GtkColorDialogButton, GTK_TYPE_COLOR_DIALOG_BUTTON);
  GObject *dialog_o = unwrap(dialog, GTK_TYPE_COLOR_DIALOG);
  if (dialog_o == nullptr) RETURN_THROWS();
  gtk_color_dialog_button_set_dialog(self, GTK_COLOR_DIALOG(dialog_o));
}

/**
 * Gtk4\GtkColorDialogButton::set_rgba(GdkRGBA $color): void
 *
 * Sets the color of the button.
 */
ZEND_METHOD(Gtk4_GtkColorDialogButton, set_rgba) {
  zval *color;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(color, boxed_class_for_type(GDK_TYPE_RGBA)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkColorDialogButton *self = PHPGTK_SELF(GtkColorDialogButton, GTK_TYPE_COLOR_DIALOG_BUTTON);
  gpointer color_b = unwrap_boxed(color, GDK_TYPE_RGBA);
  if (color_b == nullptr) RETURN_THROWS();
  gtk_color_dialog_button_set_rgba(self, static_cast<GdkRGBA *>(color_b));
}
