// GENERATED by gen/gir.php from Gtk.Overlay - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkOverlay
#include "php_gtk4.h"
#include "core/object.h"
#include "core/subtype.h"

using namespace phpgtk;

/**
 * Gtk4\GtkOverlay::__construct()
 *
 * Creates a new `GtkOverlay`.
 */
ZEND_METHOD(Gtk4_GtkOverlay, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gtk_overlay_new());
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkOverlay::add_overlay(GtkWidget $widget): void
 *
 * Adds $widget to $overlay.
 */
ZEND_METHOD(Gtk4_GtkOverlay, add_overlay) {
  zval *widget;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(widget, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkOverlay *self = PHPGTK_SELF(GtkOverlay, GTK_TYPE_OVERLAY);
  GObject *widget_o = unwrap(widget, GTK_TYPE_WIDGET);
  if (widget_o == nullptr) RETURN_THROWS();
  gtk_overlay_add_overlay(self, GTK_WIDGET(widget_o));
}

/**
 * Gtk4\GtkOverlay::get_child(): ?GtkWidget
 *
 * Gets the child widget of $overlay.
 */
ZEND_METHOD(Gtk4_GtkOverlay, get_child) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkOverlay *self = PHPGTK_SELF(GtkOverlay, GTK_TYPE_OVERLAY);
  GtkWidget *phpgtk_ret = gtk_overlay_get_child(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkOverlay::get_clip_overlay(GtkWidget $widget): bool
 *
 * Gets whether $widget should be clipped within the parent.
 */
ZEND_METHOD(Gtk4_GtkOverlay, get_clip_overlay) {
  zval *widget;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(widget, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkOverlay *self = PHPGTK_SELF(GtkOverlay, GTK_TYPE_OVERLAY);
  GObject *widget_o = unwrap(widget, GTK_TYPE_WIDGET);
  if (widget_o == nullptr) RETURN_THROWS();
  RETURN_BOOL(gtk_overlay_get_clip_overlay(self, GTK_WIDGET(widget_o)));
}

/**
 * Gtk4\GtkOverlay::get_measure_overlay(GtkWidget $widget): bool
 *
 * Gets whether $widget's size is included in the measurement of $overlay.
 */
ZEND_METHOD(Gtk4_GtkOverlay, get_measure_overlay) {
  zval *widget;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(widget, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkOverlay *self = PHPGTK_SELF(GtkOverlay, GTK_TYPE_OVERLAY);
  GObject *widget_o = unwrap(widget, GTK_TYPE_WIDGET);
  if (widget_o == nullptr) RETURN_THROWS();
  RETURN_BOOL(gtk_overlay_get_measure_overlay(self, GTK_WIDGET(widget_o)));
}

/**
 * Gtk4\GtkOverlay::remove_overlay(GtkWidget $widget): void
 *
 * Removes an overlay that was added with gtk_overlay_add_overlay().
 */
ZEND_METHOD(Gtk4_GtkOverlay, remove_overlay) {
  zval *widget;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(widget, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkOverlay *self = PHPGTK_SELF(GtkOverlay, GTK_TYPE_OVERLAY);
  GObject *widget_o = unwrap(widget, GTK_TYPE_WIDGET);
  if (widget_o == nullptr) RETURN_THROWS();
  gtk_overlay_remove_overlay(self, GTK_WIDGET(widget_o));
}

/**
 * Gtk4\GtkOverlay::set_child(?GtkWidget $child): void
 *
 * Sets the child widget of $overlay.
 */
ZEND_METHOD(Gtk4_GtkOverlay, set_child) {
  zval *child = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(child, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkOverlay *self = PHPGTK_SELF(GtkOverlay, GTK_TYPE_OVERLAY);
  GObject *child_o = nullptr;
  if (child != nullptr) {
    child_o = unwrap(child, GTK_TYPE_WIDGET);
    if (child_o == nullptr) RETURN_THROWS();
  }
  gtk_overlay_set_child(self, child_o != nullptr ? GTK_WIDGET(child_o) : nullptr);
}

/**
 * Gtk4\GtkOverlay::set_clip_overlay(GtkWidget $widget, bool $clip_overlay): void
 *
 * Sets whether $widget should be clipped within the parent.
 */
ZEND_METHOD(Gtk4_GtkOverlay, set_clip_overlay) {
  zval *widget;
  bool clip_overlay;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(widget, class_for_gtype(GTK_TYPE_WIDGET))
  Z_PARAM_BOOL(clip_overlay)
  ZEND_PARSE_PARAMETERS_END();
  GtkOverlay *self = PHPGTK_SELF(GtkOverlay, GTK_TYPE_OVERLAY);
  GObject *widget_o = unwrap(widget, GTK_TYPE_WIDGET);
  if (widget_o == nullptr) RETURN_THROWS();
  if (widget_o != nullptr && gtk_widget_get_parent(GTK_WIDGET(widget_o)) != GTK_WIDGET(self)) {
    zend_throw_exception_ex(spl_ce_LogicException, 0,
                            "%s(): Argument #1 ($widget) is not a child of this %s",
                            ZSTR_VAL(EX(func)->common.function_name), G_OBJECT_TYPE_NAME(self));
    RETURN_THROWS();
  }
  gtk_overlay_set_clip_overlay(self, GTK_WIDGET(widget_o), clip_overlay);
}

/**
 * Gtk4\GtkOverlay::set_measure_overlay(GtkWidget $widget, bool $measure): void
 *
 * Sets whether $widget is included in the measured size of $overlay.
 */
ZEND_METHOD(Gtk4_GtkOverlay, set_measure_overlay) {
  zval *widget;
  bool measure;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(widget, class_for_gtype(GTK_TYPE_WIDGET))
  Z_PARAM_BOOL(measure)
  ZEND_PARSE_PARAMETERS_END();
  GtkOverlay *self = PHPGTK_SELF(GtkOverlay, GTK_TYPE_OVERLAY);
  GObject *widget_o = unwrap(widget, GTK_TYPE_WIDGET);
  if (widget_o == nullptr) RETURN_THROWS();
  if (widget_o != nullptr && gtk_widget_get_parent(GTK_WIDGET(widget_o)) != GTK_WIDGET(self)) {
    zend_throw_exception_ex(spl_ce_LogicException, 0,
                            "%s(): Argument #1 ($widget) is not a child of this %s",
                            ZSTR_VAL(EX(func)->common.function_name), G_OBJECT_TYPE_NAME(self));
    RETURN_THROWS();
  }
  gtk_overlay_set_measure_overlay(self, GTK_WIDGET(widget_o), measure);
}
