// GENERATED by gen/gir.php from Gtk.NoSelection - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkNoSelection
#include "php_gtk4.h"
#include "core/object.h"
#include "core/subtype.h"

using namespace phpgtk;

/**
 * Gtk4\GtkNoSelection::__construct(?GListModel $model = null)
 *
 * Creates a new selection to handle $model.
 */
ZEND_METHOD(Gtk4_GtkNoSelection, __construct) {
  zval *model = nullptr;
  ZEND_PARSE_PARAMETERS_START(0, 1)
  Z_PARAM_OPTIONAL
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(model, class_for_gtype(G_TYPE_LIST_MODEL))
  ZEND_PARSE_PARAMETERS_END();
  GObject *model_o = nullptr;
  if (model != nullptr) {
    model_o = unwrap(model, G_TYPE_LIST_MODEL);
    if (model_o == nullptr) RETURN_THROWS();
  }
  if (model_o != nullptr) g_object_ref(model_o);  // transfer full
  GObject *obj = subtype_new(ZEND_THIS, "model",
                             model_o != nullptr ? G_LIST_MODEL(model_o) : nullptr, nullptr);
  if (obj != nullptr) {
    if (model_o != nullptr) g_object_unref(model_o);
  }
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gtk_no_selection_new(model_o != nullptr ? G_LIST_MODEL(model_o) : nullptr));
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkNoSelection::get_model(): ?GListModel
 *
 * Gets the model that $self is wrapping.
 */
ZEND_METHOD(Gtk4_GtkNoSelection, get_model) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkNoSelection *self = PHPGTK_SELF(GtkNoSelection, GTK_TYPE_NO_SELECTION);
  GListModel *phpgtk_ret = gtk_no_selection_get_model(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkNoSelection::set_model(?GListModel $model): void
 *
 * Sets the model that $self should wrap.
 */
ZEND_METHOD(Gtk4_GtkNoSelection, set_model) {
  zval *model = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(model, class_for_gtype(G_TYPE_LIST_MODEL))
  ZEND_PARSE_PARAMETERS_END();
  GtkNoSelection *self = PHPGTK_SELF(GtkNoSelection, GTK_TYPE_NO_SELECTION);
  GObject *model_o = nullptr;
  if (model != nullptr) {
    model_o = unwrap(model, G_TYPE_LIST_MODEL);
    if (model_o == nullptr) RETURN_THROWS();
  }
  gtk_no_selection_set_model(self, model_o != nullptr ? G_LIST_MODEL(model_o) : nullptr);
}
