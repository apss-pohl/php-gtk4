// GENERATED by gen/gir.php from Gtk.Snapshot - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkSnapshot
#include "php_gtk4.h"
#include "core/object.h"
#include "core/enums.h"
#include "core/boxed.h"
#include "core/fundamental.h"
#include "core/subtype.h"
#include <cairo-gobject.h>
#include "Gsk/GskRoundedRectType.h"

using namespace phpgtk;

// append_border(): the four widths and colours come as PHP lists (src/Gsk/GskColorStops.h).
#include "Gsk/GskColorStops.h"

#include <array>

/**
 * Gtk4\GtkSnapshot::__construct()
 *
 * Creates a new `GtkSnapshot`.
 */
ZEND_METHOD(Gtk4_GtkSnapshot, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gtk_snapshot_new());
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkSnapshot::append_cairo(GrapheneRect $bounds): CairoContext
 *
 * Creates a new `CairoNode` and appends it to the current render node of $snapshot, without
 * changing the current node.
 */
ZEND_METHOD(Gtk4_GtkSnapshot, append_cairo) {
  zval *bounds;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(bounds, boxed_class_for_type(GRAPHENE_TYPE_RECT)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkSnapshot *self = PHPGTK_SELF(GtkSnapshot, GTK_TYPE_SNAPSHOT);
  gpointer bounds_b = unwrap_boxed(bounds, GRAPHENE_TYPE_RECT);
  if (bounds_b == nullptr) RETURN_THROWS();
  gpointer phpgtk_ret = gtk_snapshot_append_cairo(self, static_cast<graphene_rect_t *>(bounds_b));
  wrap_fundamental(CAIRO_GOBJECT_TYPE_CONTEXT, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) cairo_destroy(static_cast<cairo_t *>(phpgtk_ret));
}

/**
 * Gtk4\GtkSnapshot::append_color(GdkRGBA $color, GrapheneRect $bounds): void
 *
 * Creates a new render node drawing the $color into the given $bounds and appends it to the
 * current render node of $snapshot.
 */
ZEND_METHOD(Gtk4_GtkSnapshot, append_color) {
  zval *color;
  zval *bounds;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(color, boxed_class_for_type(GDK_TYPE_RGBA)->ce)
  Z_PARAM_OBJECT_OF_CLASS(bounds, boxed_class_for_type(GRAPHENE_TYPE_RECT)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkSnapshot *self = PHPGTK_SELF(GtkSnapshot, GTK_TYPE_SNAPSHOT);
  gpointer color_b = unwrap_boxed(color, GDK_TYPE_RGBA);
  if (color_b == nullptr) RETURN_THROWS();
  gpointer bounds_b = unwrap_boxed(bounds, GRAPHENE_TYPE_RECT);
  if (bounds_b == nullptr) RETURN_THROWS();
  gtk_snapshot_append_color(self, static_cast<GdkRGBA *>(color_b),
                            static_cast<graphene_rect_t *>(bounds_b));
}

/**
 * Gtk4\GtkSnapshot::append_fill(GskPath $path, GskFillRule $fill_rule, GdkRGBA $color): void
 *
 * A convenience method to fill a path with a color.
 */
ZEND_METHOD(Gtk4_GtkSnapshot, append_fill) {
  zval *path;
  zval *fill_rule;
  zval *color;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_OBJECT_OF_CLASS(path, boxed_class_for_type(GSK_TYPE_PATH)->ce)
  Z_PARAM_OBJECT_OF_CLASS(fill_rule, enum_class_for_type(GSK_TYPE_FILL_RULE))
  Z_PARAM_OBJECT_OF_CLASS(color, boxed_class_for_type(GDK_TYPE_RGBA)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkSnapshot *self = PHPGTK_SELF(GtkSnapshot, GTK_TYPE_SNAPSHOT);
  gpointer path_b = unwrap_boxed(path, GSK_TYPE_PATH);
  if (path_b == nullptr) RETURN_THROWS();
  gint fill_rule_v = 0;
  if (!enum_from_php(fill_rule, GSK_TYPE_FILL_RULE, &fill_rule_v)) RETURN_THROWS();
  gpointer color_b = unwrap_boxed(color, GDK_TYPE_RGBA);
  if (color_b == nullptr) RETURN_THROWS();
  gtk_snapshot_append_fill(self, static_cast<GskPath *>(path_b),
                           static_cast<GskFillRule>(fill_rule_v), static_cast<GdkRGBA *>(color_b));
}

/**
 * Gtk4\GtkSnapshot::append_inset_shadow(GskRoundedRect $outline, GdkRGBA $color, float $dx, float
 * $dy, float $spread, float $blur_radius): void
 *
 * Appends an inset shadow into the box given by $outline.
 */
ZEND_METHOD(Gtk4_GtkSnapshot, append_inset_shadow) {
  zval *outline;
  zval *color;
  double dx;
  double dy;
  double spread;
  double blur_radius;
  ZEND_PARSE_PARAMETERS_START(6, 6)
  Z_PARAM_OBJECT_OF_CLASS(outline, boxed_class_for_type(PHPGTK_TYPE_GSK_ROUNDED_RECT)->ce)
  Z_PARAM_OBJECT_OF_CLASS(color, boxed_class_for_type(GDK_TYPE_RGBA)->ce)
  Z_PARAM_DOUBLE(dx)
  Z_PARAM_DOUBLE(dy)
  Z_PARAM_DOUBLE(spread)
  Z_PARAM_DOUBLE(blur_radius)
  ZEND_PARSE_PARAMETERS_END();
  GtkSnapshot *self = PHPGTK_SELF(GtkSnapshot, GTK_TYPE_SNAPSHOT);
  gpointer outline_b = unwrap_boxed(outline, PHPGTK_TYPE_GSK_ROUNDED_RECT);
  if (outline_b == nullptr) RETURN_THROWS();
  gpointer color_b = unwrap_boxed(color, GDK_TYPE_RGBA);
  if (color_b == nullptr) RETURN_THROWS();
  gtk_snapshot_append_inset_shadow(self, static_cast<GskRoundedRect *>(outline_b),
                                   static_cast<GdkRGBA *>(color_b), static_cast<float>(dx),
                                   static_cast<float>(dy), static_cast<float>(spread),
                                   static_cast<float>(blur_radius));
}

/**
 * Gtk4\GtkSnapshot::append_layout(PangoLayout $layout, GdkRGBA $color): void
 */
ZEND_METHOD(Gtk4_GtkSnapshot, append_layout) {
  zval *layout;
  zval *color;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(layout, class_for_gtype(PANGO_TYPE_LAYOUT))
  Z_PARAM_OBJECT_OF_CLASS(color, boxed_class_for_type(GDK_TYPE_RGBA)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkSnapshot *self = PHPGTK_SELF(GtkSnapshot, GTK_TYPE_SNAPSHOT);
  GObject *layout_o = unwrap(layout, PANGO_TYPE_LAYOUT);
  if (layout_o == nullptr) RETURN_THROWS();
  gpointer color_b = unwrap_boxed(color, GDK_TYPE_RGBA);
  if (color_b == nullptr) RETURN_THROWS();
  gtk_snapshot_append_layout(self, PANGO_LAYOUT(layout_o), static_cast<GdkRGBA *>(color_b));
}

/**
 * Gtk4\GtkSnapshot::append_node(GskRenderNode $node): void
 *
 * Appends $node to the current render node of $snapshot, without changing the current node.
 */
ZEND_METHOD(Gtk4_GtkSnapshot, append_node) {
  zval *node;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(node, fundamental_class_for_type(GSK_TYPE_RENDER_NODE)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkSnapshot *self = PHPGTK_SELF(GtkSnapshot, GTK_TYPE_SNAPSHOT);
  gpointer node_f = unwrap_fundamental(node, GSK_TYPE_RENDER_NODE);
  if (node_f == nullptr) RETURN_THROWS();
  gtk_snapshot_append_node(self, static_cast<GskRenderNode *>(node_f));
}

/**
 * Gtk4\GtkSnapshot::append_outset_shadow(GskRoundedRect $outline, GdkRGBA $color, float $dx, float
 * $dy, float $spread, float $blur_radius): void
 *
 * Appends an outset shadow node around the box given by $outline.
 */
ZEND_METHOD(Gtk4_GtkSnapshot, append_outset_shadow) {
  zval *outline;
  zval *color;
  double dx;
  double dy;
  double spread;
  double blur_radius;
  ZEND_PARSE_PARAMETERS_START(6, 6)
  Z_PARAM_OBJECT_OF_CLASS(outline, boxed_class_for_type(PHPGTK_TYPE_GSK_ROUNDED_RECT)->ce)
  Z_PARAM_OBJECT_OF_CLASS(color, boxed_class_for_type(GDK_TYPE_RGBA)->ce)
  Z_PARAM_DOUBLE(dx)
  Z_PARAM_DOUBLE(dy)
  Z_PARAM_DOUBLE(spread)
  Z_PARAM_DOUBLE(blur_radius)
  ZEND_PARSE_PARAMETERS_END();
  GtkSnapshot *self = PHPGTK_SELF(GtkSnapshot, GTK_TYPE_SNAPSHOT);
  gpointer outline_b = unwrap_boxed(outline, PHPGTK_TYPE_GSK_ROUNDED_RECT);
  if (outline_b == nullptr) RETURN_THROWS();
  gpointer color_b = unwrap_boxed(color, GDK_TYPE_RGBA);
  if (color_b == nullptr) RETURN_THROWS();
  gtk_snapshot_append_outset_shadow(self, static_cast<GskRoundedRect *>(outline_b),
                                    static_cast<GdkRGBA *>(color_b), static_cast<float>(dx),
                                    static_cast<float>(dy), static_cast<float>(spread),
                                    static_cast<float>(blur_radius));
}

/**
 * Gtk4\GtkSnapshot::append_scaled_texture(GdkTexture $texture, GskScalingFilter $filter,
 * GrapheneRect $bounds): void
 *
 * Creates a new render node drawing the $texture into the given $bounds and appends it to the
 * current render node of $snapshot.
 */
ZEND_METHOD(Gtk4_GtkSnapshot, append_scaled_texture) {
  zval *texture;
  zval *filter;
  zval *bounds;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_OBJECT_OF_CLASS(texture, class_for_gtype(GDK_TYPE_TEXTURE))
  Z_PARAM_OBJECT_OF_CLASS(filter, enum_class_for_type(GSK_TYPE_SCALING_FILTER))
  Z_PARAM_OBJECT_OF_CLASS(bounds, boxed_class_for_type(GRAPHENE_TYPE_RECT)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkSnapshot *self = PHPGTK_SELF(GtkSnapshot, GTK_TYPE_SNAPSHOT);
  GObject *texture_o = unwrap(texture, GDK_TYPE_TEXTURE);
  if (texture_o == nullptr) RETURN_THROWS();
  gint filter_v = 0;
  if (!enum_from_php(filter, GSK_TYPE_SCALING_FILTER, &filter_v)) RETURN_THROWS();
  gpointer bounds_b = unwrap_boxed(bounds, GRAPHENE_TYPE_RECT);
  if (bounds_b == nullptr) RETURN_THROWS();
  gtk_snapshot_append_scaled_texture(self, GDK_TEXTURE(texture_o),
                                     static_cast<GskScalingFilter>(filter_v),
                                     static_cast<graphene_rect_t *>(bounds_b));
}

/**
 * Gtk4\GtkSnapshot::append_stroke(GskPath $path, GskStroke $stroke, GdkRGBA $color): void
 *
 * A convenience method to stroke a path with a color.
 */
ZEND_METHOD(Gtk4_GtkSnapshot, append_stroke) {
  zval *path;
  zval *stroke;
  zval *color;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_OBJECT_OF_CLASS(path, boxed_class_for_type(GSK_TYPE_PATH)->ce)
  Z_PARAM_OBJECT_OF_CLASS(stroke, boxed_class_for_type(GSK_TYPE_STROKE)->ce)
  Z_PARAM_OBJECT_OF_CLASS(color, boxed_class_for_type(GDK_TYPE_RGBA)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkSnapshot *self = PHPGTK_SELF(GtkSnapshot, GTK_TYPE_SNAPSHOT);
  gpointer path_b = unwrap_boxed(path, GSK_TYPE_PATH);
  if (path_b == nullptr) RETURN_THROWS();
  gpointer stroke_b = unwrap_boxed(stroke, GSK_TYPE_STROKE);
  if (stroke_b == nullptr) RETURN_THROWS();
  gpointer color_b = unwrap_boxed(color, GDK_TYPE_RGBA);
  if (color_b == nullptr) RETURN_THROWS();
  gtk_snapshot_append_stroke(self, static_cast<GskPath *>(path_b),
                             static_cast<GskStroke *>(stroke_b), static_cast<GdkRGBA *>(color_b));
}

/**
 * Gtk4\GtkSnapshot::append_texture(GdkTexture $texture, GrapheneRect $bounds): void
 *
 * Creates a new render node drawing the $texture into the given $bounds and appends it to the
 * current render node of $snapshot.
 */
ZEND_METHOD(Gtk4_GtkSnapshot, append_texture) {
  zval *texture;
  zval *bounds;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(texture, class_for_gtype(GDK_TYPE_TEXTURE))
  Z_PARAM_OBJECT_OF_CLASS(bounds, boxed_class_for_type(GRAPHENE_TYPE_RECT)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkSnapshot *self = PHPGTK_SELF(GtkSnapshot, GTK_TYPE_SNAPSHOT);
  GObject *texture_o = unwrap(texture, GDK_TYPE_TEXTURE);
  if (texture_o == nullptr) RETURN_THROWS();
  gpointer bounds_b = unwrap_boxed(bounds, GRAPHENE_TYPE_RECT);
  if (bounds_b == nullptr) RETURN_THROWS();
  gtk_snapshot_append_texture(self, GDK_TEXTURE(texture_o),
                              static_cast<graphene_rect_t *>(bounds_b));
}

/**
 * Gtk4\GtkSnapshot::gl_shader_pop_texture(): void
 *
 * Removes the top element from the stack of render nodes and adds it to the nearest `GLShaderNode`
 * below it.
 */
ZEND_METHOD(Gtk4_GtkSnapshot, gl_shader_pop_texture) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkSnapshot *self = PHPGTK_SELF(GtkSnapshot, GTK_TYPE_SNAPSHOT);
  gtk_snapshot_gl_shader_pop_texture(self);
}

/**
 * Gtk4\GtkSnapshot::perspective(float $depth): void
 *
 * Applies a perspective projection transform.
 */
ZEND_METHOD(Gtk4_GtkSnapshot, perspective) {
  double depth;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_DOUBLE(depth)
  ZEND_PARSE_PARAMETERS_END();
  GtkSnapshot *self = PHPGTK_SELF(GtkSnapshot, GTK_TYPE_SNAPSHOT);
  gtk_snapshot_perspective(self, static_cast<float>(depth));
}

/**
 * Gtk4\GtkSnapshot::pop(): void
 *
 * Removes the top element from the stack of render nodes, and appends it to the node underneath
 * it.
 */
ZEND_METHOD(Gtk4_GtkSnapshot, pop) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkSnapshot *self = PHPGTK_SELF(GtkSnapshot, GTK_TYPE_SNAPSHOT);
  gtk_snapshot_pop(self);
}

/**
 * Gtk4\GtkSnapshot::push_blend(GskBlendMode $blend_mode): void
 *
 * Blends together two images with the given blend mode.
 */
ZEND_METHOD(Gtk4_GtkSnapshot, push_blend) {
  zval *blend_mode;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(blend_mode, enum_class_for_type(GSK_TYPE_BLEND_MODE))
  ZEND_PARSE_PARAMETERS_END();
  GtkSnapshot *self = PHPGTK_SELF(GtkSnapshot, GTK_TYPE_SNAPSHOT);
  gint blend_mode_v = 0;
  if (!enum_from_php(blend_mode, GSK_TYPE_BLEND_MODE, &blend_mode_v)) RETURN_THROWS();
  gtk_snapshot_push_blend(self, static_cast<GskBlendMode>(blend_mode_v));
}

/**
 * Gtk4\GtkSnapshot::push_blur(float $radius): void
 *
 * Blurs an image.
 */
ZEND_METHOD(Gtk4_GtkSnapshot, push_blur) {
  double radius;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_DOUBLE(radius)
  ZEND_PARSE_PARAMETERS_END();
  GtkSnapshot *self = PHPGTK_SELF(GtkSnapshot, GTK_TYPE_SNAPSHOT);
  gtk_snapshot_push_blur(self, radius);
}

/**
 * Gtk4\GtkSnapshot::push_clip(GrapheneRect $bounds): void
 *
 * Clips an image to a rectangle.
 */
ZEND_METHOD(Gtk4_GtkSnapshot, push_clip) {
  zval *bounds;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(bounds, boxed_class_for_type(GRAPHENE_TYPE_RECT)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkSnapshot *self = PHPGTK_SELF(GtkSnapshot, GTK_TYPE_SNAPSHOT);
  gpointer bounds_b = unwrap_boxed(bounds, GRAPHENE_TYPE_RECT);
  if (bounds_b == nullptr) RETURN_THROWS();
  gtk_snapshot_push_clip(self, static_cast<graphene_rect_t *>(bounds_b));
}

/**
 * Gtk4\GtkSnapshot::push_color_matrix(GrapheneMatrix $color_matrix, GrapheneVec4 $color_offset):
 * void
 *
 * Modifies the colors of an image by applying an affine transformation in RGB space.
 */
ZEND_METHOD(Gtk4_GtkSnapshot, push_color_matrix) {
  zval *color_matrix;
  zval *color_offset;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(color_matrix, boxed_class_for_type(GRAPHENE_TYPE_MATRIX)->ce)
  Z_PARAM_OBJECT_OF_CLASS(color_offset, boxed_class_for_type(GRAPHENE_TYPE_VEC4)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkSnapshot *self = PHPGTK_SELF(GtkSnapshot, GTK_TYPE_SNAPSHOT);
  gpointer color_matrix_b = unwrap_boxed(color_matrix, GRAPHENE_TYPE_MATRIX);
  if (color_matrix_b == nullptr) RETURN_THROWS();
  gpointer color_offset_b = unwrap_boxed(color_offset, GRAPHENE_TYPE_VEC4);
  if (color_offset_b == nullptr) RETURN_THROWS();
  gtk_snapshot_push_color_matrix(self, static_cast<graphene_matrix_t *>(color_matrix_b),
                                 static_cast<graphene_vec4_t *>(color_offset_b));
}

/**
 * Gtk4\GtkSnapshot::push_cross_fade(float $progress): void
 *
 * Snapshots a cross-fade operation between two images with the given $progress.
 */
ZEND_METHOD(Gtk4_GtkSnapshot, push_cross_fade) {
  double progress;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_DOUBLE(progress)
  ZEND_PARSE_PARAMETERS_END();
  GtkSnapshot *self = PHPGTK_SELF(GtkSnapshot, GTK_TYPE_SNAPSHOT);
  gtk_snapshot_push_cross_fade(self, progress);
}

/**
 * Gtk4\GtkSnapshot::push_fill(GskPath $path, GskFillRule $fill_rule): void
 *
 * Fills the area given by $path and $fill_rule with an image and discards everything outside of
 * it.
 */
ZEND_METHOD(Gtk4_GtkSnapshot, push_fill) {
  zval *path;
  zval *fill_rule;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(path, boxed_class_for_type(GSK_TYPE_PATH)->ce)
  Z_PARAM_OBJECT_OF_CLASS(fill_rule, enum_class_for_type(GSK_TYPE_FILL_RULE))
  ZEND_PARSE_PARAMETERS_END();
  GtkSnapshot *self = PHPGTK_SELF(GtkSnapshot, GTK_TYPE_SNAPSHOT);
  gpointer path_b = unwrap_boxed(path, GSK_TYPE_PATH);
  if (path_b == nullptr) RETURN_THROWS();
  gint fill_rule_v = 0;
  if (!enum_from_php(fill_rule, GSK_TYPE_FILL_RULE, &fill_rule_v)) RETURN_THROWS();
  gtk_snapshot_push_fill(self, static_cast<GskPath *>(path_b),
                         static_cast<GskFillRule>(fill_rule_v));
}

/**
 * Gtk4\GtkSnapshot::push_mask(GskMaskMode $mask_mode): void
 *
 * Until the first call to `pop`, the mask image for the mask operation will be recorded.
 */
ZEND_METHOD(Gtk4_GtkSnapshot, push_mask) {
  zval *mask_mode;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(mask_mode, enum_class_for_type(GSK_TYPE_MASK_MODE))
  ZEND_PARSE_PARAMETERS_END();
  GtkSnapshot *self = PHPGTK_SELF(GtkSnapshot, GTK_TYPE_SNAPSHOT);
  gint mask_mode_v = 0;
  if (!enum_from_php(mask_mode, GSK_TYPE_MASK_MODE, &mask_mode_v)) RETURN_THROWS();
  gtk_snapshot_push_mask(self, static_cast<GskMaskMode>(mask_mode_v));
}

/**
 * Gtk4\GtkSnapshot::push_opacity(float $opacity): void
 *
 * Modifies the opacity of an image.
 */
ZEND_METHOD(Gtk4_GtkSnapshot, push_opacity) {
  double opacity;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_DOUBLE(opacity)
  ZEND_PARSE_PARAMETERS_END();
  GtkSnapshot *self = PHPGTK_SELF(GtkSnapshot, GTK_TYPE_SNAPSHOT);
  gtk_snapshot_push_opacity(self, opacity);
}

/**
 * Gtk4\GtkSnapshot::push_repeat(GrapheneRect $bounds, ?GrapheneRect $child_bounds): void
 *
 * Creates a node that repeats the child node.
 */
ZEND_METHOD(Gtk4_GtkSnapshot, push_repeat) {
  zval *bounds;
  zval *child_bounds = nullptr;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(bounds, boxed_class_for_type(GRAPHENE_TYPE_RECT)->ce)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(child_bounds, boxed_class_for_type(GRAPHENE_TYPE_RECT)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkSnapshot *self = PHPGTK_SELF(GtkSnapshot, GTK_TYPE_SNAPSHOT);
  gpointer bounds_b = unwrap_boxed(bounds, GRAPHENE_TYPE_RECT);
  if (bounds_b == nullptr) RETURN_THROWS();
  gpointer child_bounds_b = nullptr;
  if (child_bounds != nullptr) {
    child_bounds_b = unwrap_boxed(child_bounds, GRAPHENE_TYPE_RECT);
    if (child_bounds_b == nullptr) RETURN_THROWS();
  }
  gtk_snapshot_push_repeat(self, static_cast<graphene_rect_t *>(bounds_b),
                           static_cast<graphene_rect_t *>(child_bounds_b));
}

/**
 * Gtk4\GtkSnapshot::push_rounded_clip(GskRoundedRect $bounds): void
 *
 * Clips an image to a rounded rectangle.
 */
ZEND_METHOD(Gtk4_GtkSnapshot, push_rounded_clip) {
  zval *bounds;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(bounds, boxed_class_for_type(PHPGTK_TYPE_GSK_ROUNDED_RECT)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkSnapshot *self = PHPGTK_SELF(GtkSnapshot, GTK_TYPE_SNAPSHOT);
  gpointer bounds_b = unwrap_boxed(bounds, PHPGTK_TYPE_GSK_ROUNDED_RECT);
  if (bounds_b == nullptr) RETURN_THROWS();
  gtk_snapshot_push_rounded_clip(self, static_cast<GskRoundedRect *>(bounds_b));
}

/**
 * Gtk4\GtkSnapshot::push_stroke(GskPath $path, GskStroke $stroke): void
 *
 * Strokes the given $path with the attributes given by $stroke and an image.
 */
ZEND_METHOD(Gtk4_GtkSnapshot, push_stroke) {
  zval *path;
  zval *stroke;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(path, boxed_class_for_type(GSK_TYPE_PATH)->ce)
  Z_PARAM_OBJECT_OF_CLASS(stroke, boxed_class_for_type(GSK_TYPE_STROKE)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkSnapshot *self = PHPGTK_SELF(GtkSnapshot, GTK_TYPE_SNAPSHOT);
  gpointer path_b = unwrap_boxed(path, GSK_TYPE_PATH);
  if (path_b == nullptr) RETURN_THROWS();
  gpointer stroke_b = unwrap_boxed(stroke, GSK_TYPE_STROKE);
  if (stroke_b == nullptr) RETURN_THROWS();
  gtk_snapshot_push_stroke(self, static_cast<GskPath *>(path_b),
                           static_cast<GskStroke *>(stroke_b));
}

/**
 * Gtk4\GtkSnapshot::restore(): void
 *
 * Restores $snapshot to the state saved by a preceding call to `save` and removes that state from
 * the stack of saved states.
 */
ZEND_METHOD(Gtk4_GtkSnapshot, restore) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkSnapshot *self = PHPGTK_SELF(GtkSnapshot, GTK_TYPE_SNAPSHOT);
  gtk_snapshot_restore(self);
}

/**
 * Gtk4\GtkSnapshot::rotate(float $angle): void
 *
 * Rotates @$snapshot's coordinate system by $angle degrees in 2D space - or in 3D speak, rotates
 * around the Z axis. The rotation happens around the origin point of (0, 0) in the $snapshot's
 * current coordinate system.
 */
ZEND_METHOD(Gtk4_GtkSnapshot, rotate) {
  double angle;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_DOUBLE(angle)
  ZEND_PARSE_PARAMETERS_END();
  GtkSnapshot *self = PHPGTK_SELF(GtkSnapshot, GTK_TYPE_SNAPSHOT);
  gtk_snapshot_rotate(self, static_cast<float>(angle));
}

/**
 * Gtk4\GtkSnapshot::rotate_3d(float $angle, GrapheneVec3 $axis): void
 *
 * Rotates $snapshot's coordinate system by $angle degrees around $axis.
 */
ZEND_METHOD(Gtk4_GtkSnapshot, rotate_3d) {
  double angle;
  zval *axis;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_DOUBLE(angle)
  Z_PARAM_OBJECT_OF_CLASS(axis, boxed_class_for_type(GRAPHENE_TYPE_VEC3)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkSnapshot *self = PHPGTK_SELF(GtkSnapshot, GTK_TYPE_SNAPSHOT);
  gpointer axis_b = unwrap_boxed(axis, GRAPHENE_TYPE_VEC3);
  if (axis_b == nullptr) RETURN_THROWS();
  gtk_snapshot_rotate_3d(self, static_cast<float>(angle), static_cast<graphene_vec3_t *>(axis_b));
}

/**
 * Gtk4\GtkSnapshot::save(): void
 *
 * Makes a copy of the current state of $snapshot and saves it on an internal stack.
 */
ZEND_METHOD(Gtk4_GtkSnapshot, save) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkSnapshot *self = PHPGTK_SELF(GtkSnapshot, GTK_TYPE_SNAPSHOT);
  gtk_snapshot_save(self);
}

/**
 * Gtk4\GtkSnapshot::scale(float $factor_x, float $factor_y): void
 *
 * Scales $snapshot's coordinate system in 2-dimensional space by the given factors.
 */
ZEND_METHOD(Gtk4_GtkSnapshot, scale) {
  double factor_x;
  double factor_y;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_DOUBLE(factor_x)
  Z_PARAM_DOUBLE(factor_y)
  ZEND_PARSE_PARAMETERS_END();
  GtkSnapshot *self = PHPGTK_SELF(GtkSnapshot, GTK_TYPE_SNAPSHOT);
  gtk_snapshot_scale(self, static_cast<float>(factor_x), static_cast<float>(factor_y));
}

/**
 * Gtk4\GtkSnapshot::scale_3d(float $factor_x, float $factor_y, float $factor_z): void
 *
 * Scales $snapshot's coordinate system by the given factors.
 */
ZEND_METHOD(Gtk4_GtkSnapshot, scale_3d) {
  double factor_x;
  double factor_y;
  double factor_z;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_DOUBLE(factor_x)
  Z_PARAM_DOUBLE(factor_y)
  Z_PARAM_DOUBLE(factor_z)
  ZEND_PARSE_PARAMETERS_END();
  GtkSnapshot *self = PHPGTK_SELF(GtkSnapshot, GTK_TYPE_SNAPSHOT);
  gtk_snapshot_scale_3d(self, static_cast<float>(factor_x), static_cast<float>(factor_y),
                        static_cast<float>(factor_z));
}

/**
 * Gtk4\GtkSnapshot::to_node(): ?GskRenderNode
 *
 * Returns the render node that was constructed by $snapshot.
 */
ZEND_METHOD(Gtk4_GtkSnapshot, to_node) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkSnapshot *self = PHPGTK_SELF(GtkSnapshot, GTK_TYPE_SNAPSHOT);
  gpointer phpgtk_ret = gtk_snapshot_to_node(self);
  wrap_fundamental(GSK_TYPE_RENDER_NODE, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) gsk_render_node_unref(static_cast<GskRenderNode *>(phpgtk_ret));
}

/**
 * Gtk4\GtkSnapshot::to_paintable(?GrapheneSize $size): ?GdkPaintable
 *
 * Returns a paintable encapsulating the render node that was constructed by $snapshot.
 */
ZEND_METHOD(Gtk4_GtkSnapshot, to_paintable) {
  zval *size = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(size, boxed_class_for_type(GRAPHENE_TYPE_SIZE)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkSnapshot *self = PHPGTK_SELF(GtkSnapshot, GTK_TYPE_SNAPSHOT);
  gpointer size_b = nullptr;
  if (size != nullptr) {
    size_b = unwrap_boxed(size, GRAPHENE_TYPE_SIZE);
    if (size_b == nullptr) RETURN_THROWS();
  }
  GdkPaintable *phpgtk_ret =
      gtk_snapshot_to_paintable(self, static_cast<graphene_size_t *>(size_b));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * Gtk4\GtkSnapshot::transform(?GskTransform $transform): void
 *
 * Transforms $snapshot's coordinate system with the given $transform.
 */
ZEND_METHOD(Gtk4_GtkSnapshot, transform) {
  zval *transform = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(transform, boxed_class_for_type(GSK_TYPE_TRANSFORM)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkSnapshot *self = PHPGTK_SELF(GtkSnapshot, GTK_TYPE_SNAPSHOT);
  gpointer transform_b = nullptr;
  if (transform != nullptr) {
    transform_b = unwrap_boxed(transform, GSK_TYPE_TRANSFORM);
    if (transform_b == nullptr) RETURN_THROWS();
  }
  gtk_snapshot_transform(self, static_cast<GskTransform *>(transform_b));
}

/**
 * Gtk4\GtkSnapshot::transform_matrix(GrapheneMatrix $matrix): void
 *
 * Transforms $snapshot's coordinate system with the given $matrix.
 */
ZEND_METHOD(Gtk4_GtkSnapshot, transform_matrix) {
  zval *matrix;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(matrix, boxed_class_for_type(GRAPHENE_TYPE_MATRIX)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkSnapshot *self = PHPGTK_SELF(GtkSnapshot, GTK_TYPE_SNAPSHOT);
  gpointer matrix_b = unwrap_boxed(matrix, GRAPHENE_TYPE_MATRIX);
  if (matrix_b == nullptr) RETURN_THROWS();
  gtk_snapshot_transform_matrix(self, static_cast<graphene_matrix_t *>(matrix_b));
}

/**
 * Gtk4\GtkSnapshot::translate(GraphenePoint $point): void
 *
 * Translates $snapshot's coordinate system by $point in 2-dimensional space.
 */
ZEND_METHOD(Gtk4_GtkSnapshot, translate) {
  zval *point;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(point, boxed_class_for_type(GRAPHENE_TYPE_POINT)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkSnapshot *self = PHPGTK_SELF(GtkSnapshot, GTK_TYPE_SNAPSHOT);
  gpointer point_b = unwrap_boxed(point, GRAPHENE_TYPE_POINT);
  if (point_b == nullptr) RETURN_THROWS();
  gtk_snapshot_translate(self, static_cast<graphene_point_t *>(point_b));
}

/**
 * Gtk4\GtkSnapshot::translate_3d(GraphenePoint3D $point): void
 *
 * Translates $snapshot's coordinate system by $point.
 */
ZEND_METHOD(Gtk4_GtkSnapshot, translate_3d) {
  zval *point;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(point, boxed_class_for_type(GRAPHENE_TYPE_POINT3D)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GtkSnapshot *self = PHPGTK_SELF(GtkSnapshot, GTK_TYPE_SNAPSHOT);
  gpointer point_b = unwrap_boxed(point, GRAPHENE_TYPE_POINT3D);
  if (point_b == nullptr) RETURN_THROWS();
  gtk_snapshot_translate_3d(self, static_cast<graphene_point3d_t *>(point_b));
}

/**
 * public function append_border(GskRoundedRect $outline, array $widths, array $colors): void
 * Appends a border inside $outline: $widths are the four widths (top, right, bottom, left) as
 * floats, $colors the four GdkRGBA colours in the same order - what a GskBorderNode is.
 *
 * GIR takes two fixed-size C arrays; PHP lists of exactly four are the same thing.
 */
ZEND_METHOD(Gtk4_GtkSnapshot, append_border) {
  zval *outline;
  zval *widths;
  zval *colors;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_OBJECT_OF_CLASS(outline, boxed_class_for_type(PHPGTK_TYPE_GSK_ROUNDED_RECT)->ce)
  Z_PARAM_ARRAY(widths)
  Z_PARAM_ARRAY(colors)
  ZEND_PARSE_PARAMETERS_END();
  GtkSnapshot *self = PHPGTK_SELF(GtkSnapshot, GTK_TYPE_SNAPSHOT);
  std::array<float, 4> border_width{};
  std::array<GdkRGBA, 4> border_color{};
  if (!border_from_php(widths, 2, colors, 3, border_width, border_color)) RETURN_THROWS();
  gtk_snapshot_append_border(
      self,
      static_cast<const GskRoundedRect *>(unwrap_boxed(outline, PHPGTK_TYPE_GSK_ROUNDED_RECT)),
      border_width.data(), border_color.data());
}
