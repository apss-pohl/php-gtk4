// GENERATED by gen/gir.php from Gtk.Gesture - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkGesture
#include "php_gtk4.h"
#include "core/object.h"
#include "core/enums.h"
#include "core/collections.h"
#include "core/boxed.h"
#include "core/subtype.h"

using namespace phpgtk;

/**
 * Gtk4\GtkGesture::__construct()
 *
 * GtkGesture is abstract in GTK: `new` only works on a PHP subclass (which gets its own GType).
 */
ZEND_METHOD(Gtk4_GtkGesture, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) == nullptr) {
      zend_throw_error(nullptr,
                       "GtkGesture is abstract in GTK: subclass it in PHP (new MyClass())");
    }
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkGesture::get_bounding_box(): ?GdkRectangle
 *
 * If there are touch sequences being currently handled by $gesture, returns `true` and fills in
 * $rect with the bounding box containing all active touches.
 */
ZEND_METHOD(Gtk4_GtkGesture, get_bounding_box) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkGesture *self = PHPGTK_SELF(GtkGesture, GTK_TYPE_GESTURE);
  GdkRectangle rect{};
  if (!gtk_gesture_get_bounding_box(self, &rect)) RETURN_NULL();
  wrap_boxed(GDK_TYPE_RECTANGLE, &rect, return_value);
}

/**
 * Gtk4\GtkGesture::get_bounding_box_center(): ?array
 *
 * If there are touch sequences being currently handled by $gesture, returns `true` and fills in $x
 * and $y with the center of the bounding box containing all active touches.
 */
ZEND_METHOD(Gtk4_GtkGesture, get_bounding_box_center) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkGesture *self = PHPGTK_SELF(GtkGesture, GTK_TYPE_GESTURE);
  double x = 0;
  double y = 0;
  if (!gtk_gesture_get_bounding_box_center(self, &x, &y)) RETURN_NULL();
  array_init_size(return_value, 2);
  {
    zval item;
    ZVAL_DOUBLE(&item, x);
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_DOUBLE(&item, y);
    add_next_index_zval(return_value, &item);
  }
}

/**
 * Gtk4\GtkGesture::get_group(): array
 *
 * Returns all gestures in the group of $gesture
 */
ZEND_METHOD(Gtk4_GtkGesture, get_group) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkGesture *self = PHPGTK_SELF(GtkGesture, GTK_TYPE_GESTURE);
  glist_to_php(gtk_gesture_get_group(self), GTK_TYPE_GESTURE, Transfer::Container, return_value);
}

/**
 * Gtk4\GtkGesture::group(GtkGesture $gesture): void
 *
 * Adds $gesture to the same group than $group_gesture.
 */
ZEND_METHOD(Gtk4_GtkGesture, group) {
  zval *gesture;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(gesture, class_for_gtype(GTK_TYPE_GESTURE))
  ZEND_PARSE_PARAMETERS_END();
  GtkGesture *self = PHPGTK_SELF(GtkGesture, GTK_TYPE_GESTURE);
  GObject *gesture_o = unwrap(gesture, GTK_TYPE_GESTURE);
  if (gesture_o == nullptr) RETURN_THROWS();
  gtk_gesture_group(self, GTK_GESTURE(gesture_o));
}

/**
 * Gtk4\GtkGesture::is_active(): bool
 *
 * Returns `true` if the gesture is currently active.
 */
ZEND_METHOD(Gtk4_GtkGesture, is_active) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkGesture *self = PHPGTK_SELF(GtkGesture, GTK_TYPE_GESTURE);
  RETURN_BOOL(gtk_gesture_is_active(self));
}

/**
 * Gtk4\GtkGesture::is_grouped_with(GtkGesture $other): bool
 *
 * Returns `true` if both gestures pertain to the same group.
 */
ZEND_METHOD(Gtk4_GtkGesture, is_grouped_with) {
  zval *other;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(other, class_for_gtype(GTK_TYPE_GESTURE))
  ZEND_PARSE_PARAMETERS_END();
  GtkGesture *self = PHPGTK_SELF(GtkGesture, GTK_TYPE_GESTURE);
  GObject *other_o = unwrap(other, GTK_TYPE_GESTURE);
  if (other_o == nullptr) RETURN_THROWS();
  RETURN_BOOL(gtk_gesture_is_grouped_with(self, GTK_GESTURE(other_o)));
}

/**
 * Gtk4\GtkGesture::is_recognized(): bool
 *
 * Returns `true` if the gesture is currently recognized.
 */
ZEND_METHOD(Gtk4_GtkGesture, is_recognized) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkGesture *self = PHPGTK_SELF(GtkGesture, GTK_TYPE_GESTURE);
  RETURN_BOOL(gtk_gesture_is_recognized(self));
}

/**
 * Gtk4\GtkGesture::set_state(GtkEventSequenceState $state): bool
 *
 * Sets the state of all sequences that $gesture is currently interacting with.
 */
ZEND_METHOD(Gtk4_GtkGesture, set_state) {
  zval *state;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(state, enum_class_for_type(GTK_TYPE_EVENT_SEQUENCE_STATE))
  ZEND_PARSE_PARAMETERS_END();
  GtkGesture *self = PHPGTK_SELF(GtkGesture, GTK_TYPE_GESTURE);
  gint state_v = 0;
  if (!enum_from_php(state, GTK_TYPE_EVENT_SEQUENCE_STATE, &state_v)) RETURN_THROWS();
  RETURN_BOOL(gtk_gesture_set_state(self, static_cast<GtkEventSequenceState>(state_v)));
}

/**
 * Gtk4\GtkGesture::ungroup(): void
 *
 * Separates $gesture into an isolated group.
 */
ZEND_METHOD(Gtk4_GtkGesture, ungroup) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkGesture *self = PHPGTK_SELF(GtkGesture, GTK_TYPE_GESTURE);
  gtk_gesture_ungroup(self);
}
