// GENERATED by gen/gir.php from Gtk.ScrolledWindow - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkScrolledWindow
#include "php_gtk4.h"
#include "core/object.h"
#include "core/enums.h"
#include "core/subtype.h"

using namespace phpgtk;

/**
 * Gtk4\GtkScrolledWindow::__construct()
 *
 * Creates a new scrolled window.
 */
ZEND_METHOD(Gtk4_GtkScrolledWindow, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gtk_scrolled_window_new());
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkScrolledWindow::get_child(): ?GtkWidget
 *
 * Gets the child widget of $scrolled_window.
 */
ZEND_METHOD(Gtk4_GtkScrolledWindow, get_child) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkScrolledWindow *self = PHPGTK_SELF(GtkScrolledWindow, GTK_TYPE_SCROLLED_WINDOW);
  GtkWidget *phpgtk_ret = gtk_scrolled_window_get_child(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkScrolledWindow::get_hadjustment(): GtkAdjustment
 *
 * Returns the horizontal scrollbar’s adjustment.
 */
ZEND_METHOD(Gtk4_GtkScrolledWindow, get_hadjustment) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkScrolledWindow *self = PHPGTK_SELF(GtkScrolledWindow, GTK_TYPE_SCROLLED_WINDOW);
  GtkAdjustment *phpgtk_ret = gtk_scrolled_window_get_hadjustment(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkScrolledWindow::get_has_frame(): bool
 *
 * Gets whether the scrolled window draws a frame.
 */
ZEND_METHOD(Gtk4_GtkScrolledWindow, get_has_frame) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkScrolledWindow *self = PHPGTK_SELF(GtkScrolledWindow, GTK_TYPE_SCROLLED_WINDOW);
  RETURN_BOOL(gtk_scrolled_window_get_has_frame(self));
}

/**
 * Gtk4\GtkScrolledWindow::get_hscrollbar(): GtkWidget
 *
 * Returns the horizontal scrollbar of $scrolled_window.
 */
ZEND_METHOD(Gtk4_GtkScrolledWindow, get_hscrollbar) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkScrolledWindow *self = PHPGTK_SELF(GtkScrolledWindow, GTK_TYPE_SCROLLED_WINDOW);
  GtkWidget *phpgtk_ret = gtk_scrolled_window_get_hscrollbar(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkScrolledWindow::get_kinetic_scrolling(): bool
 *
 * Returns the specified kinetic scrolling behavior.
 */
ZEND_METHOD(Gtk4_GtkScrolledWindow, get_kinetic_scrolling) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkScrolledWindow *self = PHPGTK_SELF(GtkScrolledWindow, GTK_TYPE_SCROLLED_WINDOW);
  RETURN_BOOL(gtk_scrolled_window_get_kinetic_scrolling(self));
}

/**
 * Gtk4\GtkScrolledWindow::get_max_content_height(): int
 *
 * Returns the maximum content height set.
 */
ZEND_METHOD(Gtk4_GtkScrolledWindow, get_max_content_height) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkScrolledWindow *self = PHPGTK_SELF(GtkScrolledWindow, GTK_TYPE_SCROLLED_WINDOW);
  RETURN_LONG(static_cast<zend_long>(gtk_scrolled_window_get_max_content_height(self)));
}

/**
 * Gtk4\GtkScrolledWindow::get_max_content_width(): int
 *
 * Returns the maximum content width set.
 */
ZEND_METHOD(Gtk4_GtkScrolledWindow, get_max_content_width) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkScrolledWindow *self = PHPGTK_SELF(GtkScrolledWindow, GTK_TYPE_SCROLLED_WINDOW);
  RETURN_LONG(static_cast<zend_long>(gtk_scrolled_window_get_max_content_width(self)));
}

/**
 * Gtk4\GtkScrolledWindow::get_min_content_height(): int
 *
 * Gets the minimal content height of $scrolled_window.
 */
ZEND_METHOD(Gtk4_GtkScrolledWindow, get_min_content_height) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkScrolledWindow *self = PHPGTK_SELF(GtkScrolledWindow, GTK_TYPE_SCROLLED_WINDOW);
  RETURN_LONG(static_cast<zend_long>(gtk_scrolled_window_get_min_content_height(self)));
}

/**
 * Gtk4\GtkScrolledWindow::get_min_content_width(): int
 *
 * Gets the minimum content width of $scrolled_window.
 */
ZEND_METHOD(Gtk4_GtkScrolledWindow, get_min_content_width) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkScrolledWindow *self = PHPGTK_SELF(GtkScrolledWindow, GTK_TYPE_SCROLLED_WINDOW);
  RETURN_LONG(static_cast<zend_long>(gtk_scrolled_window_get_min_content_width(self)));
}

/**
 * Gtk4\GtkScrolledWindow::get_overlay_scrolling(): bool
 *
 * Returns whether overlay scrolling is enabled for this scrolled window.
 */
ZEND_METHOD(Gtk4_GtkScrolledWindow, get_overlay_scrolling) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkScrolledWindow *self = PHPGTK_SELF(GtkScrolledWindow, GTK_TYPE_SCROLLED_WINDOW);
  RETURN_BOOL(gtk_scrolled_window_get_overlay_scrolling(self));
}

/**
 * Gtk4\GtkScrolledWindow::get_placement(): GtkCornerType
 *
 * Gets the placement of the contents with respect to the scrollbars.
 */
ZEND_METHOD(Gtk4_GtkScrolledWindow, get_placement) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkScrolledWindow *self = PHPGTK_SELF(GtkScrolledWindow, GTK_TYPE_SCROLLED_WINDOW);
  enum_to_php(GTK_TYPE_CORNER_TYPE, gtk_scrolled_window_get_placement(self), return_value);
}

/**
 * Gtk4\GtkScrolledWindow::get_policy(): array
 *
 * Retrieves the current policy values for the horizontal and vertical scrollbars.
 */
ZEND_METHOD(Gtk4_GtkScrolledWindow, get_policy) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkScrolledWindow *self = PHPGTK_SELF(GtkScrolledWindow, GTK_TYPE_SCROLLED_WINDOW);
  GtkPolicyType hscrollbar_policy = static_cast<GtkPolicyType>(0);
  GtkPolicyType vscrollbar_policy = static_cast<GtkPolicyType>(0);
  gtk_scrolled_window_get_policy(self, &hscrollbar_policy, &vscrollbar_policy);
  array_init_size(return_value, 2);
  {
    zval item;
    enum_to_php(GTK_TYPE_POLICY_TYPE, static_cast<gint>(hscrollbar_policy), &item);
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    enum_to_php(GTK_TYPE_POLICY_TYPE, static_cast<gint>(vscrollbar_policy), &item);
    add_next_index_zval(return_value, &item);
  }
}

/**
 * Gtk4\GtkScrolledWindow::get_propagate_natural_height(): bool
 *
 * Reports whether the natural height of the child will be calculated and propagated through the
 * scrolled window’s requested natural height.
 */
ZEND_METHOD(Gtk4_GtkScrolledWindow, get_propagate_natural_height) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkScrolledWindow *self = PHPGTK_SELF(GtkScrolledWindow, GTK_TYPE_SCROLLED_WINDOW);
  RETURN_BOOL(gtk_scrolled_window_get_propagate_natural_height(self));
}

/**
 * Gtk4\GtkScrolledWindow::get_propagate_natural_width(): bool
 *
 * Reports whether the natural width of the child will be calculated and propagated through the
 * scrolled window’s requested natural width.
 */
ZEND_METHOD(Gtk4_GtkScrolledWindow, get_propagate_natural_width) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkScrolledWindow *self = PHPGTK_SELF(GtkScrolledWindow, GTK_TYPE_SCROLLED_WINDOW);
  RETURN_BOOL(gtk_scrolled_window_get_propagate_natural_width(self));
}

/**
 * Gtk4\GtkScrolledWindow::get_vadjustment(): GtkAdjustment
 *
 * Returns the vertical scrollbar’s adjustment.
 */
ZEND_METHOD(Gtk4_GtkScrolledWindow, get_vadjustment) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkScrolledWindow *self = PHPGTK_SELF(GtkScrolledWindow, GTK_TYPE_SCROLLED_WINDOW);
  GtkAdjustment *phpgtk_ret = gtk_scrolled_window_get_vadjustment(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkScrolledWindow::get_vscrollbar(): GtkWidget
 *
 * Returns the vertical scrollbar of $scrolled_window.
 */
ZEND_METHOD(Gtk4_GtkScrolledWindow, get_vscrollbar) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkScrolledWindow *self = PHPGTK_SELF(GtkScrolledWindow, GTK_TYPE_SCROLLED_WINDOW);
  GtkWidget *phpgtk_ret = gtk_scrolled_window_get_vscrollbar(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkScrolledWindow::set_child(?GtkWidget $child): void
 *
 * Sets the child widget of $scrolled_window.
 */
ZEND_METHOD(Gtk4_GtkScrolledWindow, set_child) {
  zval *child = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(child, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkScrolledWindow *self = PHPGTK_SELF(GtkScrolledWindow, GTK_TYPE_SCROLLED_WINDOW);
  GObject *child_o = nullptr;
  if (child != nullptr) {
    child_o = unwrap(child, GTK_TYPE_WIDGET);
    if (child_o == nullptr) RETURN_THROWS();
  }
  gtk_scrolled_window_set_child(self, child_o != nullptr ? GTK_WIDGET(child_o) : nullptr);
}

/**
 * Gtk4\GtkScrolledWindow::set_hadjustment(?GtkAdjustment $hadjustment): void
 *
 * Sets the `GtkAdjustment` for the horizontal scrollbar.
 */
ZEND_METHOD(Gtk4_GtkScrolledWindow, set_hadjustment) {
  zval *hadjustment = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(hadjustment, class_for_gtype(GTK_TYPE_ADJUSTMENT))
  ZEND_PARSE_PARAMETERS_END();
  GtkScrolledWindow *self = PHPGTK_SELF(GtkScrolledWindow, GTK_TYPE_SCROLLED_WINDOW);
  GObject *hadjustment_o = nullptr;
  if (hadjustment != nullptr) {
    hadjustment_o = unwrap(hadjustment, GTK_TYPE_ADJUSTMENT);
    if (hadjustment_o == nullptr) RETURN_THROWS();
  }
  gtk_scrolled_window_set_hadjustment(
      self, hadjustment_o != nullptr ? GTK_ADJUSTMENT(hadjustment_o) : nullptr);
}

/**
 * Gtk4\GtkScrolledWindow::set_has_frame(bool $has_frame): void
 *
 * Changes the frame drawn around the contents of $scrolled_window.
 */
ZEND_METHOD(Gtk4_GtkScrolledWindow, set_has_frame) {
  bool has_frame;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(has_frame)
  ZEND_PARSE_PARAMETERS_END();
  GtkScrolledWindow *self = PHPGTK_SELF(GtkScrolledWindow, GTK_TYPE_SCROLLED_WINDOW);
  gtk_scrolled_window_set_has_frame(self, has_frame);
}

/**
 * Gtk4\GtkScrolledWindow::set_kinetic_scrolling(bool $kinetic_scrolling): void
 *
 * Turns kinetic scrolling on or off.
 */
ZEND_METHOD(Gtk4_GtkScrolledWindow, set_kinetic_scrolling) {
  bool kinetic_scrolling;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(kinetic_scrolling)
  ZEND_PARSE_PARAMETERS_END();
  GtkScrolledWindow *self = PHPGTK_SELF(GtkScrolledWindow, GTK_TYPE_SCROLLED_WINDOW);
  gtk_scrolled_window_set_kinetic_scrolling(self, kinetic_scrolling);
}

/**
 * Gtk4\GtkScrolledWindow::set_max_content_height(int $height): void
 *
 * Sets the maximum height that $scrolled_window should keep visible.
 */
ZEND_METHOD(Gtk4_GtkScrolledWindow, set_max_content_height) {
  zend_long height;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(height)
  ZEND_PARSE_PARAMETERS_END();
  GtkScrolledWindow *self = PHPGTK_SELF(GtkScrolledWindow, GTK_TYPE_SCROLLED_WINDOW);
  if (!phpgtk::check_range<int>(height, 1)) RETURN_THROWS();
  gtk_scrolled_window_set_max_content_height(self, static_cast<int>(height));
}

/**
 * Gtk4\GtkScrolledWindow::set_max_content_width(int $width): void
 *
 * Sets the maximum width that $scrolled_window should keep visible.
 */
ZEND_METHOD(Gtk4_GtkScrolledWindow, set_max_content_width) {
  zend_long width;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(width)
  ZEND_PARSE_PARAMETERS_END();
  GtkScrolledWindow *self = PHPGTK_SELF(GtkScrolledWindow, GTK_TYPE_SCROLLED_WINDOW);
  if (!phpgtk::check_range<int>(width, 1)) RETURN_THROWS();
  gtk_scrolled_window_set_max_content_width(self, static_cast<int>(width));
}

/**
 * Gtk4\GtkScrolledWindow::set_min_content_height(int $height): void
 *
 * Sets the minimum height that $scrolled_window should keep visible.
 */
ZEND_METHOD(Gtk4_GtkScrolledWindow, set_min_content_height) {
  zend_long height;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(height)
  ZEND_PARSE_PARAMETERS_END();
  GtkScrolledWindow *self = PHPGTK_SELF(GtkScrolledWindow, GTK_TYPE_SCROLLED_WINDOW);
  if (!phpgtk::check_range<int>(height, 1)) RETURN_THROWS();
  gtk_scrolled_window_set_min_content_height(self, static_cast<int>(height));
}

/**
 * Gtk4\GtkScrolledWindow::set_min_content_width(int $width): void
 *
 * Sets the minimum width that $scrolled_window should keep visible.
 */
ZEND_METHOD(Gtk4_GtkScrolledWindow, set_min_content_width) {
  zend_long width;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(width)
  ZEND_PARSE_PARAMETERS_END();
  GtkScrolledWindow *self = PHPGTK_SELF(GtkScrolledWindow, GTK_TYPE_SCROLLED_WINDOW);
  if (!phpgtk::check_range<int>(width, 1)) RETURN_THROWS();
  gtk_scrolled_window_set_min_content_width(self, static_cast<int>(width));
}

/**
 * Gtk4\GtkScrolledWindow::set_overlay_scrolling(bool $overlay_scrolling): void
 *
 * Enables or disables overlay scrolling for this scrolled window.
 */
ZEND_METHOD(Gtk4_GtkScrolledWindow, set_overlay_scrolling) {
  bool overlay_scrolling;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(overlay_scrolling)
  ZEND_PARSE_PARAMETERS_END();
  GtkScrolledWindow *self = PHPGTK_SELF(GtkScrolledWindow, GTK_TYPE_SCROLLED_WINDOW);
  gtk_scrolled_window_set_overlay_scrolling(self, overlay_scrolling);
}

/**
 * Gtk4\GtkScrolledWindow::set_placement(GtkCornerType $window_placement): void
 *
 * Sets the placement of the contents with respect to the scrollbars for the scrolled window.
 */
ZEND_METHOD(Gtk4_GtkScrolledWindow, set_placement) {
  zval *window_placement;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(window_placement, enum_class_for_type(GTK_TYPE_CORNER_TYPE))
  ZEND_PARSE_PARAMETERS_END();
  GtkScrolledWindow *self = PHPGTK_SELF(GtkScrolledWindow, GTK_TYPE_SCROLLED_WINDOW);
  gint window_placement_v = 0;
  if (!enum_from_php(window_placement, GTK_TYPE_CORNER_TYPE, &window_placement_v)) RETURN_THROWS();
  gtk_scrolled_window_set_placement(self, static_cast<GtkCornerType>(window_placement_v));
}

/**
 * Gtk4\GtkScrolledWindow::set_policy(GtkPolicyType $hscrollbar_policy, GtkPolicyType
 * $vscrollbar_policy): void
 *
 * Sets the scrollbar policy for the horizontal and vertical scrollbars.
 */
ZEND_METHOD(Gtk4_GtkScrolledWindow, set_policy) {
  zval *hscrollbar_policy;
  zval *vscrollbar_policy;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(hscrollbar_policy, enum_class_for_type(GTK_TYPE_POLICY_TYPE))
  Z_PARAM_OBJECT_OF_CLASS(vscrollbar_policy, enum_class_for_type(GTK_TYPE_POLICY_TYPE))
  ZEND_PARSE_PARAMETERS_END();
  GtkScrolledWindow *self = PHPGTK_SELF(GtkScrolledWindow, GTK_TYPE_SCROLLED_WINDOW);
  gint hscrollbar_policy_v = 0;
  if (!enum_from_php(hscrollbar_policy, GTK_TYPE_POLICY_TYPE, &hscrollbar_policy_v))
    RETURN_THROWS();
  gint vscrollbar_policy_v = 0;
  if (!enum_from_php(vscrollbar_policy, GTK_TYPE_POLICY_TYPE, &vscrollbar_policy_v))
    RETURN_THROWS();
  gtk_scrolled_window_set_policy(self, static_cast<GtkPolicyType>(hscrollbar_policy_v),
                                 static_cast<GtkPolicyType>(vscrollbar_policy_v));
}

/**
 * Gtk4\GtkScrolledWindow::set_propagate_natural_height(bool $propagate): void
 *
 * Sets whether the natural height of the child should be calculated and propagated through the
 * scrolled window’s requested natural height.
 */
ZEND_METHOD(Gtk4_GtkScrolledWindow, set_propagate_natural_height) {
  bool propagate;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(propagate)
  ZEND_PARSE_PARAMETERS_END();
  GtkScrolledWindow *self = PHPGTK_SELF(GtkScrolledWindow, GTK_TYPE_SCROLLED_WINDOW);
  gtk_scrolled_window_set_propagate_natural_height(self, propagate);
}

/**
 * Gtk4\GtkScrolledWindow::set_propagate_natural_width(bool $propagate): void
 *
 * Sets whether the natural width of the child should be calculated and propagated through the
 * scrolled window’s requested natural width.
 */
ZEND_METHOD(Gtk4_GtkScrolledWindow, set_propagate_natural_width) {
  bool propagate;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(propagate)
  ZEND_PARSE_PARAMETERS_END();
  GtkScrolledWindow *self = PHPGTK_SELF(GtkScrolledWindow, GTK_TYPE_SCROLLED_WINDOW);
  gtk_scrolled_window_set_propagate_natural_width(self, propagate);
}

/**
 * Gtk4\GtkScrolledWindow::set_vadjustment(?GtkAdjustment $vadjustment): void
 *
 * Sets the `GtkAdjustment` for the vertical scrollbar.
 */
ZEND_METHOD(Gtk4_GtkScrolledWindow, set_vadjustment) {
  zval *vadjustment = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(vadjustment, class_for_gtype(GTK_TYPE_ADJUSTMENT))
  ZEND_PARSE_PARAMETERS_END();
  GtkScrolledWindow *self = PHPGTK_SELF(GtkScrolledWindow, GTK_TYPE_SCROLLED_WINDOW);
  GObject *vadjustment_o = nullptr;
  if (vadjustment != nullptr) {
    vadjustment_o = unwrap(vadjustment, GTK_TYPE_ADJUSTMENT);
    if (vadjustment_o == nullptr) RETURN_THROWS();
  }
  gtk_scrolled_window_set_vadjustment(
      self, vadjustment_o != nullptr ? GTK_ADJUSTMENT(vadjustment_o) : nullptr);
}

/**
 * Gtk4\GtkScrolledWindow::unset_placement(): void
 *
 * Unsets the placement of the contents with respect to the scrollbars.
 */
ZEND_METHOD(Gtk4_GtkScrolledWindow, unset_placement) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkScrolledWindow *self = PHPGTK_SELF(GtkScrolledWindow, GTK_TYPE_SCROLLED_WINDOW);
  gtk_scrolled_window_unset_placement(self);
}
