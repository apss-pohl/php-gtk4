// GENERATED by gen/gir.php from Gtk.GestureSwipe - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkGestureSwipe
#include "php_gtk4.h"
#include "core/object.h"
#include "core/subtype.h"

using namespace phpgtk;

/**
 * Gtk4\GtkGestureSwipe::__construct()
 *
 * Returns a newly created `GtkGesture` that recognizes swipes.
 */
ZEND_METHOD(Gtk4_GtkGestureSwipe, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gtk_gesture_swipe_new());
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkGestureSwipe::get_velocity(): ?array
 *
 * Gets the current velocity.
 */
ZEND_METHOD(Gtk4_GtkGestureSwipe, get_velocity) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkGestureSwipe *self = PHPGTK_SELF(GtkGestureSwipe, GTK_TYPE_GESTURE_SWIPE);
  double velocity_x = 0;
  double velocity_y = 0;
  if (!gtk_gesture_swipe_get_velocity(self, &velocity_x, &velocity_y)) RETURN_NULL();
  array_init_size(return_value, 2);
  {
    zval item;
    ZVAL_DOUBLE(&item, velocity_x);
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_DOUBLE(&item, velocity_y);
    add_next_index_zval(return_value, &item);
  }
}
