// GENERATED by gen/gir.php from Gtk.FilterListModel - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkFilterListModel
#include "php_gtk4.h"
#include "core/object.h"
#include "core/subtype.h"

using namespace phpgtk;

/**
 * Gtk4\GtkFilterListModel::__construct(?GListModel $model = null, ?GtkFilter $filter = null)
 *
 * Creates a new `GtkFilterListModel` that will filter $model using the given $filter.
 */
ZEND_METHOD(Gtk4_GtkFilterListModel, __construct) {
  zval *model = nullptr;
  zval *filter = nullptr;
  ZEND_PARSE_PARAMETERS_START(0, 2)
  Z_PARAM_OPTIONAL
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(model, class_for_gtype(G_TYPE_LIST_MODEL))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(filter, class_for_gtype(GTK_TYPE_FILTER))
  ZEND_PARSE_PARAMETERS_END();
  GObject *model_o = nullptr;
  if (model != nullptr) {
    model_o = unwrap(model, G_TYPE_LIST_MODEL);
    if (model_o == nullptr) RETURN_THROWS();
  }
  if (model_o != nullptr) g_object_ref(model_o);  // transfer full
  GObject *filter_o = nullptr;
  if (filter != nullptr) {
    filter_o = unwrap(filter, GTK_TYPE_FILTER);
    if (filter_o == nullptr) RETURN_THROWS();
  }
  if (filter_o != nullptr) g_object_ref(filter_o);  // transfer full
  GObject *obj =
      subtype_new(ZEND_THIS, "model", model_o != nullptr ? G_LIST_MODEL(model_o) : nullptr,
                  "filter", filter_o != nullptr ? GTK_FILTER(filter_o) : nullptr, nullptr);
  if (obj != nullptr) {
    if (model_o != nullptr) g_object_unref(model_o);
    if (filter_o != nullptr) g_object_unref(filter_o);
  }
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gtk_filter_list_model_new(model_o != nullptr ? G_LIST_MODEL(model_o) : nullptr,
                                             filter_o != nullptr ? GTK_FILTER(filter_o) : nullptr));
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkFilterListModel::get_filter(): ?GtkFilter
 *
 * Gets the `GtkFilter` currently set on $self.
 */
ZEND_METHOD(Gtk4_GtkFilterListModel, get_filter) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFilterListModel *self = PHPGTK_SELF(GtkFilterListModel, GTK_TYPE_FILTER_LIST_MODEL);
  GtkFilter *result = gtk_filter_list_model_get_filter(self);
  wrap(result != nullptr ? G_OBJECT(result) : nullptr, return_value);
}

/**
 * Gtk4\GtkFilterListModel::get_incremental(): bool
 *
 * Returns whether incremental filtering is enabled.
 */
ZEND_METHOD(Gtk4_GtkFilterListModel, get_incremental) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFilterListModel *self = PHPGTK_SELF(GtkFilterListModel, GTK_TYPE_FILTER_LIST_MODEL);
  RETURN_BOOL(gtk_filter_list_model_get_incremental(self));
}

/**
 * Gtk4\GtkFilterListModel::get_model(): ?GListModel
 *
 * Gets the model currently filtered or `null` if none.
 */
ZEND_METHOD(Gtk4_GtkFilterListModel, get_model) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFilterListModel *self = PHPGTK_SELF(GtkFilterListModel, GTK_TYPE_FILTER_LIST_MODEL);
  GListModel *result = gtk_filter_list_model_get_model(self);
  wrap(result != nullptr ? G_OBJECT(result) : nullptr, return_value);
}

/**
 * Gtk4\GtkFilterListModel::get_pending(): int
 *
 * Returns the number of items that have not been filtered yet.
 */
ZEND_METHOD(Gtk4_GtkFilterListModel, get_pending) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkFilterListModel *self = PHPGTK_SELF(GtkFilterListModel, GTK_TYPE_FILTER_LIST_MODEL);
  RETURN_LONG(static_cast<zend_long>(gtk_filter_list_model_get_pending(self)));
}

/**
 * Gtk4\GtkFilterListModel::set_filter(?GtkFilter $filter): void
 *
 * Sets the filter used to filter items.
 */
ZEND_METHOD(Gtk4_GtkFilterListModel, set_filter) {
  zval *filter = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(filter, class_for_gtype(GTK_TYPE_FILTER))
  ZEND_PARSE_PARAMETERS_END();
  GtkFilterListModel *self = PHPGTK_SELF(GtkFilterListModel, GTK_TYPE_FILTER_LIST_MODEL);
  GObject *filter_o = nullptr;
  if (filter != nullptr) {
    filter_o = unwrap(filter, GTK_TYPE_FILTER);
    if (filter_o == nullptr) RETURN_THROWS();
  }
  gtk_filter_list_model_set_filter(self, filter_o != nullptr ? GTK_FILTER(filter_o) : nullptr);
}

/**
 * Gtk4\GtkFilterListModel::set_incremental(bool $incremental): void
 *
 * Sets the filter model to do an incremental sort.
 */
ZEND_METHOD(Gtk4_GtkFilterListModel, set_incremental) {
  bool incremental;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(incremental)
  ZEND_PARSE_PARAMETERS_END();
  GtkFilterListModel *self = PHPGTK_SELF(GtkFilterListModel, GTK_TYPE_FILTER_LIST_MODEL);
  gtk_filter_list_model_set_incremental(self, incremental);
}

/**
 * Gtk4\GtkFilterListModel::set_model(?GListModel $model): void
 *
 * Sets the model to be filtered.
 */
ZEND_METHOD(Gtk4_GtkFilterListModel, set_model) {
  zval *model = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(model, class_for_gtype(G_TYPE_LIST_MODEL))
  ZEND_PARSE_PARAMETERS_END();
  GtkFilterListModel *self = PHPGTK_SELF(GtkFilterListModel, GTK_TYPE_FILTER_LIST_MODEL);
  GObject *model_o = nullptr;
  if (model != nullptr) {
    model_o = unwrap(model, G_TYPE_LIST_MODEL);
    if (model_o == nullptr) RETURN_THROWS();
  }
  gtk_filter_list_model_set_model(self, model_o != nullptr ? G_LIST_MODEL(model_o) : nullptr);
}
