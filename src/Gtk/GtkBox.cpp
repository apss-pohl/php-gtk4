// GENERATED by gen/gir.php from Gtk.Box - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkBox
#include "php_gtk4.h"
#include "core/object.h"
#include "core/enums.h"
#include "core/subtype.h"

using namespace phpgtk;

/**
 * Gtk4\GtkBox::__construct(GtkOrientation $orientation, int $spacing)
 *
 * Creates a new `GtkBox`.
 */
ZEND_METHOD(Gtk4_GtkBox, __construct) {
  zval *orientation;
  zend_long spacing;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(orientation, enum_class_for_type(GTK_TYPE_ORIENTATION))
  Z_PARAM_LONG(spacing)
  ZEND_PARSE_PARAMETERS_END();
  gint orientation_v = 0;
  if (!enum_from_php(orientation, GTK_TYPE_ORIENTATION, &orientation_v)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(spacing, 2)) RETURN_THROWS();
  GObject *obj = subtype_new(ZEND_THIS, "orientation", static_cast<GtkOrientation>(orientation_v),
                             "spacing", static_cast<int>(spacing), nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(
        gtk_box_new(static_cast<GtkOrientation>(orientation_v), static_cast<int>(spacing)));
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkBox::append(GtkWidget $child): void
 *
 * Adds $child as the last child to $box.
 */
ZEND_METHOD(Gtk4_GtkBox, append) {
  zval *child;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkBox *self = PHPGTK_SELF(GtkBox, GTK_TYPE_BOX);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  gtk_box_append(self, GTK_WIDGET(child_o));
}

/**
 * Gtk4\GtkBox::get_baseline_child(): int
 *
 * Gets the value set by gtk_box_set_baseline_child().
 */
ZEND_METHOD(Gtk4_GtkBox, get_baseline_child) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkBox *self = PHPGTK_SELF(GtkBox, GTK_TYPE_BOX);
  RETURN_LONG(static_cast<zend_long>(gtk_box_get_baseline_child(self)));
}

/**
 * Gtk4\GtkBox::get_baseline_position(): GtkBaselinePosition
 *
 * Gets the value set by gtk_box_set_baseline_position().
 */
ZEND_METHOD(Gtk4_GtkBox, get_baseline_position) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkBox *self = PHPGTK_SELF(GtkBox, GTK_TYPE_BOX);
  enum_to_php(GTK_TYPE_BASELINE_POSITION, gtk_box_get_baseline_position(self), return_value);
}

/**
 * Gtk4\GtkBox::get_homogeneous(): bool
 *
 * Returns whether the box is homogeneous (all children are the same size).
 */
ZEND_METHOD(Gtk4_GtkBox, get_homogeneous) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkBox *self = PHPGTK_SELF(GtkBox, GTK_TYPE_BOX);
  RETURN_BOOL(gtk_box_get_homogeneous(self));
}

/**
 * Gtk4\GtkBox::get_spacing(): int
 *
 * Gets the value set by gtk_box_set_spacing().
 */
ZEND_METHOD(Gtk4_GtkBox, get_spacing) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkBox *self = PHPGTK_SELF(GtkBox, GTK_TYPE_BOX);
  RETURN_LONG(static_cast<zend_long>(gtk_box_get_spacing(self)));
}

/**
 * Gtk4\GtkBox::insert_child_after(GtkWidget $child, ?GtkWidget $sibling): void
 *
 * Inserts $child in the position after $sibling in the list of $box children.
 */
ZEND_METHOD(Gtk4_GtkBox, insert_child_after) {
  zval *child;
  zval *sibling = nullptr;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(sibling, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkBox *self = PHPGTK_SELF(GtkBox, GTK_TYPE_BOX);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  GObject *sibling_o = nullptr;
  if (sibling != nullptr) {
    sibling_o = unwrap(sibling, GTK_TYPE_WIDGET);
    if (sibling_o == nullptr) RETURN_THROWS();
  }
  gtk_box_insert_child_after(self, GTK_WIDGET(child_o),
                             sibling_o != nullptr ? GTK_WIDGET(sibling_o) : nullptr);
}

/**
 * Gtk4\GtkBox::prepend(GtkWidget $child): void
 *
 * Adds $child as the first child to $box.
 */
ZEND_METHOD(Gtk4_GtkBox, prepend) {
  zval *child;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkBox *self = PHPGTK_SELF(GtkBox, GTK_TYPE_BOX);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  gtk_box_prepend(self, GTK_WIDGET(child_o));
}

/**
 * Gtk4\GtkBox::remove(GtkWidget $child): void
 *
 * Removes a child widget from $box.
 */
ZEND_METHOD(Gtk4_GtkBox, remove) {
  zval *child;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkBox *self = PHPGTK_SELF(GtkBox, GTK_TYPE_BOX);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  gtk_box_remove(self, GTK_WIDGET(child_o));
}

/**
 * Gtk4\GtkBox::reorder_child_after(GtkWidget $child, ?GtkWidget $sibling): void
 *
 * Moves $child to the position after $sibling in the list of $box children.
 */
ZEND_METHOD(Gtk4_GtkBox, reorder_child_after) {
  zval *child;
  zval *sibling = nullptr;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(sibling, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkBox *self = PHPGTK_SELF(GtkBox, GTK_TYPE_BOX);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  if (child_o != nullptr && gtk_widget_get_parent(GTK_WIDGET(child_o)) != GTK_WIDGET(self)) {
    zend_throw_exception_ex(spl_ce_LogicException, 0,
                            "%s(): Argument #1 ($child) is not a child of this %s",
                            ZSTR_VAL(EX(func)->common.function_name), G_OBJECT_TYPE_NAME(self));
    RETURN_THROWS();
  }
  GObject *sibling_o = nullptr;
  if (sibling != nullptr) {
    sibling_o = unwrap(sibling, GTK_TYPE_WIDGET);
    if (sibling_o == nullptr) RETURN_THROWS();
  }
  if (sibling_o != nullptr && gtk_widget_get_parent(GTK_WIDGET(sibling_o)) != GTK_WIDGET(self)) {
    zend_throw_exception_ex(spl_ce_LogicException, 0,
                            "%s(): Argument #2 ($sibling) is not a child of this %s",
                            ZSTR_VAL(EX(func)->common.function_name), G_OBJECT_TYPE_NAME(self));
    RETURN_THROWS();
  }
  gtk_box_reorder_child_after(self, GTK_WIDGET(child_o),
                              sibling_o != nullptr ? GTK_WIDGET(sibling_o) : nullptr);
}

/**
 * Gtk4\GtkBox::set_baseline_child(int $child): void
 *
 * Sets the baseline child of a box.
 */
ZEND_METHOD(Gtk4_GtkBox, set_baseline_child) {
  zend_long child;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(child)
  ZEND_PARSE_PARAMETERS_END();
  GtkBox *self = PHPGTK_SELF(GtkBox, GTK_TYPE_BOX);
  if (!phpgtk::check_range<int>(child, 1)) RETURN_THROWS();
  gtk_box_set_baseline_child(self, static_cast<int>(child));
}

/**
 * Gtk4\GtkBox::set_baseline_position(GtkBaselinePosition $position): void
 *
 * Sets the baseline position of a box.
 */
ZEND_METHOD(Gtk4_GtkBox, set_baseline_position) {
  zval *position;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(position, enum_class_for_type(GTK_TYPE_BASELINE_POSITION))
  ZEND_PARSE_PARAMETERS_END();
  GtkBox *self = PHPGTK_SELF(GtkBox, GTK_TYPE_BOX);
  gint position_v = 0;
  if (!enum_from_php(position, GTK_TYPE_BASELINE_POSITION, &position_v)) RETURN_THROWS();
  gtk_box_set_baseline_position(self, static_cast<GtkBaselinePosition>(position_v));
}

/**
 * Gtk4\GtkBox::set_homogeneous(bool $homogeneous): void
 *
 * Sets whether or not all children of $box are given equal space in the box.
 */
ZEND_METHOD(Gtk4_GtkBox, set_homogeneous) {
  bool homogeneous;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(homogeneous)
  ZEND_PARSE_PARAMETERS_END();
  GtkBox *self = PHPGTK_SELF(GtkBox, GTK_TYPE_BOX);
  gtk_box_set_homogeneous(self, homogeneous);
}

/**
 * Gtk4\GtkBox::set_spacing(int $spacing): void
 *
 * Sets the number of pixels to place between children of $box.
 */
ZEND_METHOD(Gtk4_GtkBox, set_spacing) {
  zend_long spacing;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(spacing)
  ZEND_PARSE_PARAMETERS_END();
  GtkBox *self = PHPGTK_SELF(GtkBox, GTK_TYPE_BOX);
  if (!phpgtk::check_range<int>(spacing, 1)) RETURN_THROWS();
  gtk_box_set_spacing(self, static_cast<int>(spacing));
}

/**
 * public function get_children(): array
 * This box's children, in order (GTK has no such call: it walks first-child/next-sibling).
 *
 * @return list<GtkWidget>
 */
ZEND_METHOD(Gtk4_GtkBox, get_children) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkBox *self = PHPGTK_SELF(GtkBox, GTK_TYPE_BOX);
  array_init(return_value);
  for (GtkWidget *c = gtk_widget_get_first_child(GTK_WIDGET(self)); c != nullptr;
       c = gtk_widget_get_next_sibling(c)) {
    zval item;
    wrap(G_OBJECT(c), &item);
    add_next_index_zval(return_value, &item);
  }
}
