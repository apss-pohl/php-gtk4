// GENERATED by gen/gir.php from Gtk.IconPaintable - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkIconPaintable
#include "php_gtk4.h"
#include "core/object.h"

using namespace phpgtk;

/**
 * Gtk4\GtkIconPaintable::__construct()
 *
 * GtkIconPaintable has no constructor in GTK: instances come from GTK, never from `new`.
 */
ZEND_METHOD(Gtk4_GtkIconPaintable, __construct) {
  // Private: never called (object_init_ex() in wrap() skips constructors).
  ZEND_PARSE_PARAMETERS_NONE();
}

/**
 * static Gtk4\GtkIconPaintable::new_for_file(string $file, int $size, int $scale): GtkIconPaintable
 *
 * Creates a `GtkIconPaintable` for a file with a given size and scale.
 */
ZEND_METHOD(Gtk4_GtkIconPaintable, new_for_file) {
  zend_string *file;
  zend_long size;
  zend_long scale;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_PATH_STR(file)
  Z_PARAM_LONG(size)
  Z_PARAM_LONG(scale)
  ZEND_PARSE_PARAMETERS_END();
  GFile *file_f = g_file_new_for_commandline_arg(ZSTR_VAL(file));
  if (!phpgtk::check_range<int>(size, 2)) RETURN_THROWS();
  if (!phpgtk::check_domain(size, 0, ZEND_LONG_MAX, 2)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(scale, 3)) RETURN_THROWS();
  if (!phpgtk::check_domain(scale, 0, ZEND_LONG_MAX, 3)) RETURN_THROWS();
  GtkIconPaintable *call_result =
      gtk_icon_paintable_new_for_file(file_f, static_cast<int>(size), static_cast<int>(scale));
  if (file_f != nullptr) g_object_unref(file_f);
  GObject *obj = G_OBJECT(call_result);
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * Gtk4\GtkIconPaintable::get_file(): ?string
 *
 * Gets the `GFile` that was used to load the icon.
 */
ZEND_METHOD(Gtk4_GtkIconPaintable, get_file) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkIconPaintable *self = PHPGTK_SELF(GtkIconPaintable, GTK_TYPE_ICON_PAINTABLE);
  GFile *file = gtk_icon_paintable_get_file(self);
  if (file == nullptr) RETURN_NULL();
  char *file_s = g_file_get_path(file);
  if (file_s == nullptr) file_s = g_file_get_uri(file);
  g_object_unref(file);
  if (file_s == nullptr) RETURN_EMPTY_STRING();
  RETVAL_STRING(file_s);
  g_free(file_s);
}

/**
 * Gtk4\GtkIconPaintable::get_icon_name(): ?string
 *
 * Get the icon name being used for this icon.
 */
ZEND_METHOD(Gtk4_GtkIconPaintable, get_icon_name) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkIconPaintable *self = PHPGTK_SELF(GtkIconPaintable, GTK_TYPE_ICON_PAINTABLE);
  PHPGTK_RETURN_STRING_OR_NULL(gtk_icon_paintable_get_icon_name(self));
}

/**
 * Gtk4\GtkIconPaintable::is_symbolic(): bool
 *
 * Checks if the icon is symbolic or not.
 */
ZEND_METHOD(Gtk4_GtkIconPaintable, is_symbolic) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkIconPaintable *self = PHPGTK_SELF(GtkIconPaintable, GTK_TYPE_ICON_PAINTABLE);
  RETURN_BOOL(gtk_icon_paintable_is_symbolic(self));
}
