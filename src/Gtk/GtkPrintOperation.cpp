// GENERATED by gen/gir.php from Gtk.PrintOperation - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkPrintOperation
#include "php_gtk4.h"
#include "core/object.h"
#include "core/enums.h"
#include "core/gerror.h"
#include "core/subtype.h"
#include "core/error.h"
#include <array>

using namespace phpgtk;

// run() remembers its result so get_error() can tell whether there is one to hand out.
namespace {

// The GtkPrintOperationResult of the last run(), kept as qdata on the operation (a heap gint,
// freed with the operation).
GQuark last_result_quark() {
  static const GQuark quark = g_quark_from_static_string("php-gtk4-print-last-result");
  return quark;
}

}  // namespace

/**
 * Gtk4\GtkPrintOperation::__construct()
 *
 * Creates a new `GtkPrintOperation`.
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gtk_print_operation_new());
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkPrintOperation::cancel(): void
 *
 * Cancels a running print operation.
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, cancel) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  gtk_print_operation_cancel(self);
}

/**
 * Gtk4\GtkPrintOperation::draw_page_finish(): void
 *
 * Signal that drawing of particular page is complete.
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, draw_page_finish) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  gtk_print_operation_draw_page_finish(self);
}

/**
 * Gtk4\GtkPrintOperation::get_default_page_setup(): ?GtkPageSetup
 *
 * Returns the default page setup.
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, get_default_page_setup) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  GtkPageSetup *phpgtk_ret = gtk_print_operation_get_default_page_setup(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkPrintOperation::get_embed_page_setup(): bool
 *
 * Gets whether page setup selection combos are embedded
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, get_embed_page_setup) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  RETURN_BOOL(gtk_print_operation_get_embed_page_setup(self));
}

/**
 * Gtk4\GtkPrintOperation::get_has_selection(): bool
 *
 * Gets whether there is a selection.
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, get_has_selection) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  RETURN_BOOL(gtk_print_operation_get_has_selection(self));
}

/**
 * Gtk4\GtkPrintOperation::get_n_pages_to_print(): int
 *
 * Returns the number of pages that will be printed.
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, get_n_pages_to_print) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  RETURN_LONG(static_cast<zend_long>(gtk_print_operation_get_n_pages_to_print(self)));
}

/**
 * Gtk4\GtkPrintOperation::get_print_settings(): ?GtkPrintSettings
 *
 * Returns the current print settings.
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, get_print_settings) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  GtkPrintSettings *phpgtk_ret = gtk_print_operation_get_print_settings(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkPrintOperation::get_status(): GtkPrintStatus
 *
 * Returns the status of the print operation.
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, get_status) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  enum_to_php(GTK_TYPE_PRINT_STATUS, gtk_print_operation_get_status(self), return_value);
}

/**
 * Gtk4\GtkPrintOperation::get_status_string(): string
 *
 * Returns a string representation of the status of the print operation.
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, get_status_string) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  const char *phpgtk_ret = gtk_print_operation_get_status_string(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

/**
 * Gtk4\GtkPrintOperation::get_support_selection(): bool
 *
 * Gets whether the application supports print of selection
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, get_support_selection) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  RETURN_BOOL(gtk_print_operation_get_support_selection(self));
}

/**
 * Gtk4\GtkPrintOperation::is_finished(): bool
 *
 * A convenience function to find out if the print operation is finished.
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, is_finished) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  RETURN_BOOL(gtk_print_operation_is_finished(self));
}

/**
 * Gtk4\GtkPrintOperation::set_allow_async(bool $allow_async): void
 *
 * Sets whether gtk_print_operation_run() may return before the print operation is completed.
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, set_allow_async) {
  bool allow_async;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(allow_async)
  ZEND_PARSE_PARAMETERS_END();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  gtk_print_operation_set_allow_async(self, allow_async);
}

/**
 * Gtk4\GtkPrintOperation::set_current_page(int $current_page): void
 *
 * Sets the current page.
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, set_current_page) {
  zend_long current_page;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(current_page)
  ZEND_PARSE_PARAMETERS_END();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  if (!phpgtk::check_range<int>(current_page, 1)) RETURN_THROWS();
  if (!phpgtk::check_domain(current_page, 0, ZEND_LONG_MAX, 1)) RETURN_THROWS();
  gtk_print_operation_set_current_page(self, static_cast<int>(current_page));
}

/**
 * Gtk4\GtkPrintOperation::set_custom_tab_label(?string $label): void
 *
 * Sets the label for the tab holding custom widgets.
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, set_custom_tab_label) {
  zend_string *label = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR_OR_NULL(label)
  ZEND_PARSE_PARAMETERS_END();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  if (label != nullptr && !phpgtk::check_utf8(label, 1)) RETURN_THROWS();
  gtk_print_operation_set_custom_tab_label(self, label != nullptr ? ZSTR_VAL(label) : nullptr);
}

/**
 * Gtk4\GtkPrintOperation::set_default_page_setup(?GtkPageSetup $default_page_setup): void
 *
 * Makes $default_page_setup the default page setup for $op.
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, set_default_page_setup) {
  zval *default_page_setup = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(default_page_setup, class_for_gtype(GTK_TYPE_PAGE_SETUP))
  ZEND_PARSE_PARAMETERS_END();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  GObject *default_page_setup_o = nullptr;
  if (default_page_setup != nullptr) {
    default_page_setup_o = unwrap(default_page_setup, GTK_TYPE_PAGE_SETUP);
    if (default_page_setup_o == nullptr) RETURN_THROWS();
  }
  gtk_print_operation_set_default_page_setup(
      self, default_page_setup_o != nullptr ? GTK_PAGE_SETUP(default_page_setup_o) : nullptr);
}

/**
 * Gtk4\GtkPrintOperation::set_defer_drawing(): void
 *
 * Sets up the `GtkPrintOperation` to wait for calling of
 * [method@Gtk.PrintOperation.draw_page_finish from application.
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, set_defer_drawing) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  gtk_print_operation_set_defer_drawing(self);
}

/**
 * Gtk4\GtkPrintOperation::set_embed_page_setup(bool $embed): void
 *
 * Embed page size combo box and orientation combo box into page setup page.
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, set_embed_page_setup) {
  bool embed;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(embed)
  ZEND_PARSE_PARAMETERS_END();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  gtk_print_operation_set_embed_page_setup(self, embed);
}

/**
 * Gtk4\GtkPrintOperation::set_export_filename(string $filename): void
 *
 * Sets up the `GtkPrintOperation` to generate a file instead of showing the print dialog.
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, set_export_filename) {
  zend_string *filename;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_PATH_STR(filename)
  ZEND_PARSE_PARAMETERS_END();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  zend_string *filename_abs = phpgtk::absolute_filename(filename, 1);
  if (filename_abs == nullptr) RETURN_THROWS();
  gtk_print_operation_set_export_filename(self, ZSTR_VAL(filename_abs));
  if (filename_abs != nullptr) zend_string_release(filename_abs);
}

/**
 * Gtk4\GtkPrintOperation::set_has_selection(bool $has_selection): void
 *
 * Sets whether there is a selection to print.
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, set_has_selection) {
  bool has_selection;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(has_selection)
  ZEND_PARSE_PARAMETERS_END();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  gtk_print_operation_set_has_selection(self, has_selection);
}

/**
 * Gtk4\GtkPrintOperation::set_job_name(string $job_name): void
 *
 * Sets the name of the print job.
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, set_job_name) {
  zend_string *job_name;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(job_name)
  ZEND_PARSE_PARAMETERS_END();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  if (!phpgtk::check_utf8(job_name, 1)) RETURN_THROWS();
  gtk_print_operation_set_job_name(self, ZSTR_VAL(job_name));
}

/**
 * Gtk4\GtkPrintOperation::set_n_pages(int $n_pages): void
 *
 * Sets the number of pages in the document.
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, set_n_pages) {
  zend_long n_pages;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(n_pages)
  ZEND_PARSE_PARAMETERS_END();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  if (!phpgtk::check_range<int>(n_pages, 1)) RETURN_THROWS();
  if (!phpgtk::check_domain(n_pages, 1, ZEND_LONG_MAX, 1)) RETURN_THROWS();
  gtk_print_operation_set_n_pages(self, static_cast<int>(n_pages));
}

/**
 * Gtk4\GtkPrintOperation::set_print_settings(?GtkPrintSettings $print_settings): void
 *
 * Sets the print settings for $op.
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, set_print_settings) {
  zval *print_settings = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(print_settings, class_for_gtype(GTK_TYPE_PRINT_SETTINGS))
  ZEND_PARSE_PARAMETERS_END();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  GObject *print_settings_o = nullptr;
  if (print_settings != nullptr) {
    print_settings_o = unwrap(print_settings, GTK_TYPE_PRINT_SETTINGS);
    if (print_settings_o == nullptr) RETURN_THROWS();
  }
  gtk_print_operation_set_print_settings(
      self, print_settings_o != nullptr ? GTK_PRINT_SETTINGS(print_settings_o) : nullptr);
}

/**
 * Gtk4\GtkPrintOperation::set_show_progress(bool $show_progress): void
 *
 * If $show_progress is `true`, the print operation will show a progress dialog during the print
 * operation.
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, set_show_progress) {
  bool show_progress;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(show_progress)
  ZEND_PARSE_PARAMETERS_END();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  gtk_print_operation_set_show_progress(self, show_progress);
}

/**
 * Gtk4\GtkPrintOperation::set_support_selection(bool $support_selection): void
 *
 * Sets whether selection is supported by `GtkPrintOperation`.
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, set_support_selection) {
  bool support_selection;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(support_selection)
  ZEND_PARSE_PARAMETERS_END();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  gtk_print_operation_set_support_selection(self, support_selection);
}

/**
 * Gtk4\GtkPrintOperation::set_track_print_status(bool $track_status): void
 *
 * If track_status is `true`, the print operation will try to continue report on the status of the
 * print job in the printer queues and printer.
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, set_track_print_status) {
  bool track_status;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(track_status)
  ZEND_PARSE_PARAMETERS_END();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  gtk_print_operation_set_track_print_status(self, track_status);
}

/**
 * Gtk4\GtkPrintOperation::set_unit(GtkUnit $unit): void
 *
 * Sets up the transformation for the cairo context obtained from `GtkPrintContext` in such a way
 * that distances are measured in units of $unit.
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, set_unit) {
  zval *unit;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(unit, enum_class_for_type(GTK_TYPE_UNIT))
  ZEND_PARSE_PARAMETERS_END();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  gint unit_v = 0;
  if (!enum_from_php(unit, GTK_TYPE_UNIT, &unit_v)) RETURN_THROWS();
  gtk_print_operation_set_unit(self, static_cast<GtkUnit>(unit_v));
}

/**
 * Gtk4\GtkPrintOperation::set_use_full_page(bool $full_page): void
 *
 * If $full_page is `true`, the transformation for the cairo context obtained from
 * `GtkPrintContext` puts the origin at the top left corner of the page.
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, set_use_full_page) {
  bool full_page;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(full_page)
  ZEND_PARSE_PARAMETERS_END();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  gtk_print_operation_set_use_full_page(self, full_page);
}

/**
 * public function get_error(): void
 * Throws the GError behind a run() that answered GtkPrintOperationResult::Error (the `done`
 * signal reports the same result); a LogicException when the last run did not end in one.
 *
 * GTK asserts when there is no error to propagate, so the binding asks first.
 *
 * @throws GError the error of the failed operation
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, get_error) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  const auto *remembered =
      static_cast<const gint *>(g_object_get_qdata(G_OBJECT(self), last_result_quark()));
  const gint last = remembered == nullptr ? -1 : *remembered;
  if (last != GTK_PRINT_OPERATION_RESULT_ERROR) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0, "%s(): the last run() did not end in an error (result %s)",
        ZSTR_VAL(EX(func)->common.function_name), last < 0 ? "none - it never ran" : "not Error");
    RETURN_THROWS();
  }
  GError *error = nullptr;
  gtk_print_operation_get_error(self, &error);
  if (error != nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
}

/**
 * public function run(GtkPrintOperationAction $action, ?GtkWindow $parent): GtkPrintOperationResult
 * Runs the print operation: ACTION_PRINT_DIALOG shows the print dialog (over $parent), ACTION_PRINT
 * prints straight away with the current settings, ACTION_PREVIEW shows the preview, ACTION_EXPORT
 * writes the pages to the file set_export_filename() named - without any dialog. Blocks until the
 * operation is done unless set_allow_async(true) was called; a failure is a GError.
 *
 * An export without a file name is a LogicException here, where GTK would only complain.
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, run) {
  zval *action;
  zval *parent = nullptr;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(action, enum_class_for_type(GTK_TYPE_PRINT_OPERATION_ACTION))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(parent, class_for_gtype(GTK_TYPE_WINDOW))
  ZEND_PARSE_PARAMETERS_END();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  gint action_v = 0;
  if (!enum_from_php(action, GTK_TYPE_PRINT_OPERATION_ACTION, &action_v)) RETURN_THROWS();
  gchar *export_filename = nullptr;  // no getter in GTK: the property
  if (action_v == GTK_PRINT_OPERATION_ACTION_EXPORT) {
    g_object_get(self, "export-filename", &export_filename, nullptr);
  }
  const bool export_without_file =
      action_v == GTK_PRINT_OPERATION_ACTION_EXPORT && export_filename == nullptr;
  g_free(export_filename);
  if (export_without_file) {
    zend_throw_exception_ex(spl_ce_LogicException, 0,
                            "%s(): ACTION_EXPORT needs set_export_filename() first",
                            ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  GObject *parent_o = nullptr;
  if (parent != nullptr) {
    parent_o = unwrap(parent, GTK_TYPE_WINDOW);
    if (parent_o == nullptr) RETURN_THROWS();
  }
  GError *error = nullptr;
  const GtkPrintOperationResult result =
      gtk_print_operation_run(self, static_cast<GtkPrintOperationAction>(action_v),
                              parent_o != nullptr ? GTK_WINDOW(parent_o) : nullptr, &error);
  auto *remembered = g_new(gint, 1);
  *remembered = static_cast<gint>(result);
  g_object_set_qdata_full(G_OBJECT(self), last_result_quark(), remembered, g_free);
  if (error != nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  enum_to_php(GTK_TYPE_PRINT_OPERATION_RESULT, static_cast<gint>(result), return_value);
}

// vfunc thunks and installers: file-local, installed by class_init of a PHP subtype
namespace {

// vfunc thunk: GTK_PRINT_OPERATION_CLASS->begin_print -> $this->vfunc_begin_print() on a PHP
// subclass
void vfunc_thunk_begin_print(GtkPrintOperation *self, GtkPrintContext *context) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_begin_print", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = GTK_PRINT_OPERATION_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->begin_print != nullptr) native->begin_print(self, context);
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 1> args{};
  zval *argv = args.data();
  wrap(context != nullptr ? G_OBJECT(context) : nullptr, &argv[0]);
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkPrintOperation::vfunc_begin_print");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: GTK_PRINT_OPERATION_CLASS->begin_print (called from class_init / iface_init of a
// PHP subtype)
void vfunc_install_begin_print(gpointer klass) {
  GTK_PRINT_OPERATION_CLASS(klass)->begin_print = vfunc_thunk_begin_print;
}

// vfunc thunk: GTK_PRINT_OPERATION_CLASS->create_custom_widget ->
// $this->vfunc_create_custom_widget() on a PHP subclass
GtkWidget *vfunc_thunk_create_custom_widget(GtkPrintOperation *self) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_create_custom_widget", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = GTK_PRINT_OPERATION_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->create_custom_widget != nullptr ? native->create_custom_widget(self) : nullptr;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  zval ret;
  ZVAL_UNDEF(&ret);
  GtkWidget *result = nullptr;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    if (Z_TYPE(ret) == IS_OBJECT) {
      GObject *o = unwrap(&ret, GTK_TYPE_WIDGET);
      result = o != nullptr ? GTK_WIDGET(o) : nullptr;
    }
  }
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkPrintOperation::vfunc_create_custom_widget");
  zend_exception_restore();  // the parked one, previous of whatever this threw
  return result;
}

// vfunc installer: GTK_PRINT_OPERATION_CLASS->create_custom_widget (called from class_init /
// iface_init of a PHP subtype)
void vfunc_install_create_custom_widget(gpointer klass) {
  GTK_PRINT_OPERATION_CLASS(klass)->create_custom_widget = vfunc_thunk_create_custom_widget;
}

// vfunc thunk: GTK_PRINT_OPERATION_CLASS->custom_widget_apply -> $this->vfunc_custom_widget_apply()
// on a PHP subclass
void vfunc_thunk_custom_widget_apply(GtkPrintOperation *self, GtkWidget *widget) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_custom_widget_apply", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = GTK_PRINT_OPERATION_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->custom_widget_apply != nullptr) native->custom_widget_apply(self, widget);
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 1> args{};
  zval *argv = args.data();
  wrap(widget != nullptr ? G_OBJECT(widget) : nullptr, &argv[0]);
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkPrintOperation::vfunc_custom_widget_apply");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: GTK_PRINT_OPERATION_CLASS->custom_widget_apply (called from class_init /
// iface_init of a PHP subtype)
void vfunc_install_custom_widget_apply(gpointer klass) {
  GTK_PRINT_OPERATION_CLASS(klass)->custom_widget_apply = vfunc_thunk_custom_widget_apply;
}

// vfunc thunk: GTK_PRINT_OPERATION_CLASS->done -> $this->vfunc_done() on a PHP subclass
void vfunc_thunk_done(GtkPrintOperation *self, GtkPrintOperationResult result) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_done", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = GTK_PRINT_OPERATION_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->done != nullptr) native->done(self, result);
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 1> args{};
  zval *argv = args.data();
  enum_to_php(GTK_TYPE_PRINT_OPERATION_RESULT, static_cast<gint>(result), &argv[0]);
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkPrintOperation::vfunc_done");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: GTK_PRINT_OPERATION_CLASS->done (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_done(gpointer klass) {
  GTK_PRINT_OPERATION_CLASS(klass)->done = vfunc_thunk_done;
}

// vfunc thunk: GTK_PRINT_OPERATION_CLASS->draw_page -> $this->vfunc_draw_page() on a PHP subclass
void vfunc_thunk_draw_page(GtkPrintOperation *self, GtkPrintContext *context, int page_nr) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_draw_page", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = GTK_PRINT_OPERATION_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->draw_page != nullptr) native->draw_page(self, context, page_nr);
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(context != nullptr ? G_OBJECT(context) : nullptr, &argv[0]);
  ZVAL_LONG(&argv[1], static_cast<zend_long>(page_nr));
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 2, args.data());
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkPrintOperation::vfunc_draw_page");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: GTK_PRINT_OPERATION_CLASS->draw_page (called from class_init / iface_init of a
// PHP subtype)
void vfunc_install_draw_page(gpointer klass) {
  GTK_PRINT_OPERATION_CLASS(klass)->draw_page = vfunc_thunk_draw_page;
}

// vfunc thunk: GTK_PRINT_OPERATION_CLASS->end_print -> $this->vfunc_end_print() on a PHP subclass
void vfunc_thunk_end_print(GtkPrintOperation *self, GtkPrintContext *context) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_end_print", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = GTK_PRINT_OPERATION_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->end_print != nullptr) native->end_print(self, context);
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 1> args{};
  zval *argv = args.data();
  wrap(context != nullptr ? G_OBJECT(context) : nullptr, &argv[0]);
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkPrintOperation::vfunc_end_print");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: GTK_PRINT_OPERATION_CLASS->end_print (called from class_init / iface_init of a
// PHP subtype)
void vfunc_install_end_print(gpointer klass) {
  GTK_PRINT_OPERATION_CLASS(klass)->end_print = vfunc_thunk_end_print;
}

// vfunc thunk: GTK_PRINT_OPERATION_CLASS->paginate -> $this->vfunc_paginate() on a PHP subclass
gboolean vfunc_thunk_paginate(GtkPrintOperation *self, GtkPrintContext *context) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_paginate", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = GTK_PRINT_OPERATION_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->paginate != nullptr ? native->paginate(self, context) : FALSE;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 1> args{};
  zval *argv = args.data();
  wrap(context != nullptr ? G_OBJECT(context) : nullptr, &argv[0]);
  zval ret;
  ZVAL_UNDEF(&ret);
  gboolean result = FALSE;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = zend_is_true(&ret) ? TRUE : FALSE;
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkPrintOperation::vfunc_paginate");
  zend_exception_restore();  // the parked one, previous of whatever this threw
  return result;
}

// vfunc installer: GTK_PRINT_OPERATION_CLASS->paginate (called from class_init / iface_init of a
// PHP subtype)
void vfunc_install_paginate(gpointer klass) {
  GTK_PRINT_OPERATION_CLASS(klass)->paginate = vfunc_thunk_paginate;
}

// vfunc thunk: GTK_PRINT_OPERATION_CLASS->request_page_setup -> $this->vfunc_request_page_setup()
// on a PHP subclass
void vfunc_thunk_request_page_setup(GtkPrintOperation *self, GtkPrintContext *context, int page_nr,
                                    GtkPageSetup *setup) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_request_page_setup", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = GTK_PRINT_OPERATION_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->request_page_setup != nullptr)
      native->request_page_setup(self, context, page_nr, setup);
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 3> args{};
  zval *argv = args.data();
  wrap(context != nullptr ? G_OBJECT(context) : nullptr, &argv[0]);
  ZVAL_LONG(&argv[1], static_cast<zend_long>(page_nr));
  wrap(setup != nullptr ? G_OBJECT(setup) : nullptr, &argv[2]);
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 3, args.data());
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkPrintOperation::vfunc_request_page_setup");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: GTK_PRINT_OPERATION_CLASS->request_page_setup (called from class_init /
// iface_init of a PHP subtype)
void vfunc_install_request_page_setup(gpointer klass) {
  GTK_PRINT_OPERATION_CLASS(klass)->request_page_setup = vfunc_thunk_request_page_setup;
}

// vfunc thunk: GTK_PRINT_OPERATION_CLASS->status_changed -> $this->vfunc_status_changed() on a PHP
// subclass
void vfunc_thunk_status_changed(GtkPrintOperation *self) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_status_changed", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = GTK_PRINT_OPERATION_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->status_changed != nullptr) native->status_changed(self);
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkPrintOperation::vfunc_status_changed");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: GTK_PRINT_OPERATION_CLASS->status_changed (called from class_init / iface_init
// of a PHP subtype)
void vfunc_install_status_changed(gpointer klass) {
  GTK_PRINT_OPERATION_CLASS(klass)->status_changed = vfunc_thunk_status_changed;
}

// vfunc thunk: GTK_PRINT_OPERATION_CLASS->update_custom_widget ->
// $this->vfunc_update_custom_widget() on a PHP subclass
void vfunc_thunk_update_custom_widget(GtkPrintOperation *self, GtkWidget *widget,
                                      GtkPageSetup *setup, GtkPrintSettings *settings) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_update_custom_widget", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = GTK_PRINT_OPERATION_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->update_custom_widget != nullptr)
      native->update_custom_widget(self, widget, setup, settings);
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 3> args{};
  zval *argv = args.data();
  wrap(widget != nullptr ? G_OBJECT(widget) : nullptr, &argv[0]);
  wrap(setup != nullptr ? G_OBJECT(setup) : nullptr, &argv[1]);
  wrap(settings != nullptr ? G_OBJECT(settings) : nullptr, &argv[2]);
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 3, args.data());
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GtkPrintOperation::vfunc_update_custom_widget");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: GTK_PRINT_OPERATION_CLASS->update_custom_widget (called from class_init /
// iface_init of a PHP subtype)
void vfunc_install_update_custom_widget(gpointer klass) {
  GTK_PRINT_OPERATION_CLASS(klass)->update_custom_widget = vfunc_thunk_update_custom_widget;
}

}  // namespace

/**
 * Gtk4\GtkPrintOperation::vfunc_begin_print(GtkPrintContext $context): void
 *
 * Native `begin_print` (PrintOperationClass.begin_print): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_begin_print()` from an override. Signal emitted after the user has
 * finished changing print settings in the dialog, before the actual rendering starts.
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, vfunc_begin_print) {
  zval *context;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(context, class_for_gtype(GTK_TYPE_PRINT_CONTEXT))
  ZEND_PARSE_PARAMETERS_END();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkPrintOperation::vfunc_begin_print(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_PRINT_OPERATION_CLASS(subtype_native_class(G_OBJECT(self)));
  GObject *context_o = unwrap(context, GTK_TYPE_PRINT_CONTEXT);
  if (context_o == nullptr) RETURN_THROWS();
  if (klass->begin_print == nullptr) {
    return;
  }
  klass->begin_print(self, GTK_PRINT_CONTEXT(context_o));
}

/**
 * Gtk4\GtkPrintOperation::vfunc_create_custom_widget(): GtkWidget
 *
 * Native `create_custom_widget` (PrintOperationClass.create_custom_widget): the GTK implementation
 * below any PHP subclass, for `parent::vfunc_create_custom_widget()` from an override. Signal
 * emitted when displaying the print dialog.
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, vfunc_create_custom_widget) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(spl_ce_LogicException, 0,
                            "GtkPrintOperation::vfunc_create_custom_widget(): for parent:: "
                            "chaining from a PHP subclass "
                            "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_PRINT_OPERATION_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->create_custom_widget == nullptr) {
    enum_to_php(G_TYPE_NONE, 0, return_value);
    return;
  }
  GtkWidget *phpgtk_ret = klass->create_custom_widget(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkPrintOperation::vfunc_custom_widget_apply(GtkWidget $widget): void
 *
 * Native `custom_widget_apply` (PrintOperationClass.custom_widget_apply): the GTK implementation
 * below any PHP subclass, for `parent::vfunc_custom_widget_apply()` from an override. Signal
 * emitted right before “begin-print” if you added a custom widget in the
 * “create-custom-widget” handler.
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, vfunc_custom_widget_apply) {
  zval *widget;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(widget, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkPrintOperation::vfunc_custom_widget_apply(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_PRINT_OPERATION_CLASS(subtype_native_class(G_OBJECT(self)));
  GObject *widget_o = unwrap(widget, GTK_TYPE_WIDGET);
  if (widget_o == nullptr) RETURN_THROWS();
  if (klass->custom_widget_apply == nullptr) {
    return;
  }
  klass->custom_widget_apply(self, GTK_WIDGET(widget_o));
}

/**
 * Gtk4\GtkPrintOperation::vfunc_done(GtkPrintOperationResult $result): void
 *
 * Native `done` (PrintOperationClass.done): the GTK implementation below any PHP subclass, for
 * `parent::vfunc_done()` from an override. Signal emitted when the print operation run has
 * finished doing everything required for printing.
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, vfunc_done) {
  zval *result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(result, enum_class_for_type(GTK_TYPE_PRINT_OPERATION_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkPrintOperation::vfunc_done(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_PRINT_OPERATION_CLASS(subtype_native_class(G_OBJECT(self)));
  gint result_v = 0;
  if (!enum_from_php(result, GTK_TYPE_PRINT_OPERATION_RESULT, &result_v)) RETURN_THROWS();
  if (klass->done == nullptr) {
    return;
  }
  klass->done(self, static_cast<GtkPrintOperationResult>(result_v));
}

/**
 * Gtk4\GtkPrintOperation::vfunc_draw_page(GtkPrintContext $context, int $page_nr): void
 *
 * Native `draw_page` (PrintOperationClass.draw_page): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_draw_page()` from an override. Signal emitted for every page that
 * is printed.
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, vfunc_draw_page) {
  zval *context;
  zend_long page_nr;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(context, class_for_gtype(GTK_TYPE_PRINT_CONTEXT))
  Z_PARAM_LONG(page_nr)
  ZEND_PARSE_PARAMETERS_END();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkPrintOperation::vfunc_draw_page(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_PRINT_OPERATION_CLASS(subtype_native_class(G_OBJECT(self)));
  GObject *context_o = unwrap(context, GTK_TYPE_PRINT_CONTEXT);
  if (context_o == nullptr) RETURN_THROWS();
  if (!phpgtk::check_range<int>(page_nr, 2)) RETURN_THROWS();
  if (klass->draw_page == nullptr) {
    return;
  }
  klass->draw_page(self, GTK_PRINT_CONTEXT(context_o), static_cast<int>(page_nr));
}

/**
 * Gtk4\GtkPrintOperation::vfunc_end_print(GtkPrintContext $context): void
 *
 * Native `end_print` (PrintOperationClass.end_print): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_end_print()` from an override. Signal emitted after all pages have
 * been rendered.
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, vfunc_end_print) {
  zval *context;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(context, class_for_gtype(GTK_TYPE_PRINT_CONTEXT))
  ZEND_PARSE_PARAMETERS_END();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkPrintOperation::vfunc_end_print(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_PRINT_OPERATION_CLASS(subtype_native_class(G_OBJECT(self)));
  GObject *context_o = unwrap(context, GTK_TYPE_PRINT_CONTEXT);
  if (context_o == nullptr) RETURN_THROWS();
  if (klass->end_print == nullptr) {
    return;
  }
  klass->end_print(self, GTK_PRINT_CONTEXT(context_o));
}

/**
 * Gtk4\GtkPrintOperation::vfunc_paginate(GtkPrintContext $context): bool
 *
 * Native `paginate` (PrintOperationClass.paginate): the GTK implementation below any PHP subclass,
 * for `parent::vfunc_paginate()` from an override. Signal emitted after the “begin-print”
 * signal, but before the actual rendering starts.
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, vfunc_paginate) {
  zval *context;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(context, class_for_gtype(GTK_TYPE_PRINT_CONTEXT))
  ZEND_PARSE_PARAMETERS_END();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkPrintOperation::vfunc_paginate(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_PRINT_OPERATION_CLASS(subtype_native_class(G_OBJECT(self)));
  GObject *context_o = unwrap(context, GTK_TYPE_PRINT_CONTEXT);
  if (context_o == nullptr) RETURN_THROWS();
  if (klass->paginate == nullptr) {
    RETURN_FALSE;
  }
  RETURN_BOOL(klass->paginate(self, GTK_PRINT_CONTEXT(context_o)));
}

/**
 * Gtk4\GtkPrintOperation::vfunc_request_page_setup(GtkPrintContext $context, int $page_nr,
 * GtkPageSetup $setup): void
 *
 * Native `request_page_setup` (PrintOperationClass.request_page_setup): the GTK implementation
 * below any PHP subclass, for `parent::vfunc_request_page_setup()` from an override. Emitted once
 * for every page that is printed, to give the application a chance to modify the page setup.
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, vfunc_request_page_setup) {
  zval *context;
  zend_long page_nr;
  zval *setup;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_OBJECT_OF_CLASS(context, class_for_gtype(GTK_TYPE_PRINT_CONTEXT))
  Z_PARAM_LONG(page_nr)
  Z_PARAM_OBJECT_OF_CLASS(setup, class_for_gtype(GTK_TYPE_PAGE_SETUP))
  ZEND_PARSE_PARAMETERS_END();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkPrintOperation::vfunc_request_page_setup(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_PRINT_OPERATION_CLASS(subtype_native_class(G_OBJECT(self)));
  GObject *context_o = unwrap(context, GTK_TYPE_PRINT_CONTEXT);
  if (context_o == nullptr) RETURN_THROWS();
  if (!phpgtk::check_range<int>(page_nr, 2)) RETURN_THROWS();
  GObject *setup_o = unwrap(setup, GTK_TYPE_PAGE_SETUP);
  if (setup_o == nullptr) RETURN_THROWS();
  if (klass->request_page_setup == nullptr) {
    return;
  }
  klass->request_page_setup(self, GTK_PRINT_CONTEXT(context_o), static_cast<int>(page_nr),
                            GTK_PAGE_SETUP(setup_o));
}

/**
 * Gtk4\GtkPrintOperation::vfunc_status_changed(): void
 *
 * Native `status_changed` (PrintOperationClass.status_changed): the GTK implementation below any
 * PHP subclass, for `parent::vfunc_status_changed()` from an override. Emitted at between the
 * various phases of the print operation.
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, vfunc_status_changed) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GtkPrintOperation::vfunc_status_changed(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_PRINT_OPERATION_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->status_changed == nullptr) {
    return;
  }
  klass->status_changed(self);
}

/**
 * Gtk4\GtkPrintOperation::vfunc_update_custom_widget(GtkWidget $widget, GtkPageSetup $setup,
 * GtkPrintSettings $settings): void
 *
 * Native `update_custom_widget` (PrintOperationClass.update_custom_widget): the GTK implementation
 * below any PHP subclass, for `parent::vfunc_update_custom_widget()` from an override. Emitted
 * after change of selected printer.
 */
ZEND_METHOD(Gtk4_GtkPrintOperation, vfunc_update_custom_widget) {
  zval *widget;
  zval *setup;
  zval *settings;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_OBJECT_OF_CLASS(widget, class_for_gtype(GTK_TYPE_WIDGET))
  Z_PARAM_OBJECT_OF_CLASS(setup, class_for_gtype(GTK_TYPE_PAGE_SETUP))
  Z_PARAM_OBJECT_OF_CLASS(settings, class_for_gtype(GTK_TYPE_PRINT_SETTINGS))
  ZEND_PARSE_PARAMETERS_END();
  GtkPrintOperation *self = PHPGTK_SELF(GtkPrintOperation, GTK_TYPE_PRINT_OPERATION);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(spl_ce_LogicException, 0,
                            "GtkPrintOperation::vfunc_update_custom_widget(): for parent:: "
                            "chaining from a PHP subclass "
                            "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GTK_PRINT_OPERATION_CLASS(subtype_native_class(G_OBJECT(self)));
  GObject *widget_o = unwrap(widget, GTK_TYPE_WIDGET);
  if (widget_o == nullptr) RETURN_THROWS();
  GObject *setup_o = unwrap(setup, GTK_TYPE_PAGE_SETUP);
  if (setup_o == nullptr) RETURN_THROWS();
  GObject *settings_o = unwrap(settings, GTK_TYPE_PRINT_SETTINGS);
  if (settings_o == nullptr) RETURN_THROWS();
  if (klass->update_custom_widget == nullptr) {
    return;
  }
  klass->update_custom_widget(self, GTK_WIDGET(widget_o), GTK_PAGE_SETUP(setup_o),
                              GTK_PRINT_SETTINGS(settings_o));
}

// MINIT: the vfunc thunks of GtkPrintOperation (core/subtype.h).
void register_vfuncs_GtkPrintOperation() {
  register_vfunc(GTK_TYPE_PRINT_OPERATION, "begin_print", vfunc_install_begin_print);
  register_vfunc(GTK_TYPE_PRINT_OPERATION, "create_custom_widget",
                 vfunc_install_create_custom_widget);
  register_vfunc(GTK_TYPE_PRINT_OPERATION, "custom_widget_apply",
                 vfunc_install_custom_widget_apply);
  register_vfunc(GTK_TYPE_PRINT_OPERATION, "done", vfunc_install_done);
  register_vfunc(GTK_TYPE_PRINT_OPERATION, "draw_page", vfunc_install_draw_page);
  register_vfunc(GTK_TYPE_PRINT_OPERATION, "end_print", vfunc_install_end_print);
  register_vfunc(GTK_TYPE_PRINT_OPERATION, "paginate", vfunc_install_paginate);
  register_vfunc(GTK_TYPE_PRINT_OPERATION, "request_page_setup", vfunc_install_request_page_setup);
  register_vfunc(GTK_TYPE_PRINT_OPERATION, "status_changed", vfunc_install_status_changed);
  register_vfunc(GTK_TYPE_PRINT_OPERATION, "update_custom_widget",
                 vfunc_install_update_custom_widget);
}
