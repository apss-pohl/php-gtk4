// GENERATED by gen/gir.php from Gtk.GestureZoom - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkGestureZoom
#include "php_gtk4.h"
#include "core/object.h"
#include "core/subtype.h"

using namespace phpgtk;

/**
 * Gtk4\GtkGestureZoom::__construct()
 *
 * Returns a newly created `GtkGesture` that recognizes pinch/zoom gestures.
 */
ZEND_METHOD(Gtk4_GtkGestureZoom, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gtk_gesture_zoom_new());
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkGestureZoom::get_scale_delta(): float
 *
 * Gets the scale delta.
 */
ZEND_METHOD(Gtk4_GtkGestureZoom, get_scale_delta) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkGestureZoom *self = PHPGTK_SELF(GtkGestureZoom, GTK_TYPE_GESTURE_ZOOM);
  RETURN_DOUBLE(gtk_gesture_zoom_get_scale_delta(self));
}
