// GENERATED by gen/gir.php from Gtk.GestureDrag - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkGestureDrag
#include "php_gtk4.h"
#include "core/object.h"
#include "core/subtype.h"

using namespace phpgtk;

/**
 * Gtk4\GtkGestureDrag::__construct()
 *
 * Returns a newly created `GtkGesture` that recognizes drags.
 */
ZEND_METHOD(Gtk4_GtkGestureDrag, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gtk_gesture_drag_new());
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkGestureDrag::get_offset(): ?array
 *
 * Gets the offset from the start point.
 */
ZEND_METHOD(Gtk4_GtkGestureDrag, get_offset) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkGestureDrag *self = PHPGTK_SELF(GtkGestureDrag, GTK_TYPE_GESTURE_DRAG);
  double x = 0;
  double y = 0;
  if (!gtk_gesture_drag_get_offset(self, &x, &y)) RETURN_NULL();
  array_init_size(return_value, 2);
  {
    zval item;
    ZVAL_DOUBLE(&item, x);
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_DOUBLE(&item, y);
    add_next_index_zval(return_value, &item);
  }
}

/**
 * Gtk4\GtkGestureDrag::get_start_point(): ?array
 *
 * Gets the point where the drag started.
 */
ZEND_METHOD(Gtk4_GtkGestureDrag, get_start_point) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkGestureDrag *self = PHPGTK_SELF(GtkGestureDrag, GTK_TYPE_GESTURE_DRAG);
  double x = 0;
  double y = 0;
  if (!gtk_gesture_drag_get_start_point(self, &x, &y)) RETURN_NULL();
  array_init_size(return_value, 2);
  {
    zval item;
    ZVAL_DOUBLE(&item, x);
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_DOUBLE(&item, y);
    add_next_index_zval(return_value, &item);
  }
}
