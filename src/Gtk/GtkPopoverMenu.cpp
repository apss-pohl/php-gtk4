// GENERATED by gen/gir.php from Gtk.PopoverMenu - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkPopoverMenu
#include "php_gtk4.h"
#include "core/object.h"
#include "core/subtype.h"

using namespace phpgtk;

/**
 * Gtk4\GtkPopoverMenu::__construct()
 *
 * A GtkPopoverMenu with default properties (GTK's own constructor is varargs-only; set the
 * properties afterwards).
 */
ZEND_METHOD(Gtk4_GtkPopoverMenu, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = static_cast<GObject *>(g_object_new(GTK_TYPE_POPOVER_MENU, nullptr));
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * static Gtk4\GtkPopoverMenu::new_from_model(?GMenuModel $model = null): GtkPopoverMenu
 *
 * Creates a `GtkPopoverMenu` and populates it according to $model.
 */
ZEND_METHOD(Gtk4_GtkPopoverMenu, new_from_model) {
  zval *model = nullptr;
  ZEND_PARSE_PARAMETERS_START(0, 1)
  Z_PARAM_OPTIONAL
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(model, class_for_gtype(G_TYPE_MENU_MODEL))
  ZEND_PARSE_PARAMETERS_END();
  GObject *model_o = nullptr;
  if (model != nullptr) {
    model_o = unwrap(model, G_TYPE_MENU_MODEL);
    if (model_o == nullptr) RETURN_THROWS();
  }
  GObject *obj = G_OBJECT(
      gtk_popover_menu_new_from_model(model_o != nullptr ? G_MENU_MODEL(model_o) : nullptr));
  wrap(obj, return_value);
}

/**
 * static Gtk4\GtkPopoverMenu::new_from_model_full(GMenuModel $model, int $flags): GtkPopoverMenu
 *
 * Creates a `GtkPopoverMenu` and populates it according to $model.
 */
ZEND_METHOD(Gtk4_GtkPopoverMenu, new_from_model_full) {
  zval *model;
  zend_long flags;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(model, class_for_gtype(G_TYPE_MENU_MODEL))
  Z_PARAM_LONG(flags)
  ZEND_PARSE_PARAMETERS_END();
  GObject *model_o = unwrap(model, G_TYPE_MENU_MODEL);
  if (model_o == nullptr) RETURN_THROWS();
  GObject *obj = G_OBJECT(gtk_popover_menu_new_from_model_full(
      G_MENU_MODEL(model_o), static_cast<GtkPopoverMenuFlags>(flags)));
  wrap(obj, return_value);
}

/**
 * Gtk4\GtkPopoverMenu::add_child(GtkWidget $child, string $id): bool
 *
 * Adds a custom widget to a generated menu.
 */
ZEND_METHOD(Gtk4_GtkPopoverMenu, add_child) {
  zval *child;
  zend_string *id;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  Z_PARAM_STR(id)
  ZEND_PARSE_PARAMETERS_END();
  GtkPopoverMenu *self = PHPGTK_SELF(GtkPopoverMenu, GTK_TYPE_POPOVER_MENU);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  if (!phpgtk::check_utf8(id, 2)) RETURN_THROWS();
  RETURN_BOOL(gtk_popover_menu_add_child(self, GTK_WIDGET(child_o), ZSTR_VAL(id)));
}

/**
 * Gtk4\GtkPopoverMenu::get_flags(): int
 *
 * Returns the flags that $popover uses to create/display a menu from its model.
 */
ZEND_METHOD(Gtk4_GtkPopoverMenu, get_flags) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkPopoverMenu *self = PHPGTK_SELF(GtkPopoverMenu, GTK_TYPE_POPOVER_MENU);
  RETURN_LONG(static_cast<zend_long>(gtk_popover_menu_get_flags(self)));
}

/**
 * Gtk4\GtkPopoverMenu::get_menu_model(): ?GMenuModel
 *
 * Returns the menu model used to populate the popover.
 */
ZEND_METHOD(Gtk4_GtkPopoverMenu, get_menu_model) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkPopoverMenu *self = PHPGTK_SELF(GtkPopoverMenu, GTK_TYPE_POPOVER_MENU);
  GMenuModel *result = gtk_popover_menu_get_menu_model(self);
  wrap(result != nullptr ? G_OBJECT(result) : nullptr, return_value);
}

/**
 * Gtk4\GtkPopoverMenu::remove_child(GtkWidget $child): bool
 *
 * Removes a widget that has previously been added with [method@Gtk.PopoverMenu.add_child()]
 */
ZEND_METHOD(Gtk4_GtkPopoverMenu, remove_child) {
  zval *child;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkPopoverMenu *self = PHPGTK_SELF(GtkPopoverMenu, GTK_TYPE_POPOVER_MENU);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  RETURN_BOOL(gtk_popover_menu_remove_child(self, GTK_WIDGET(child_o)));
}

/**
 * Gtk4\GtkPopoverMenu::set_flags(int $flags): void
 *
 * Sets the flags that $popover uses to create/display a menu from its model.
 */
ZEND_METHOD(Gtk4_GtkPopoverMenu, set_flags) {
  zend_long flags;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(flags)
  ZEND_PARSE_PARAMETERS_END();
  GtkPopoverMenu *self = PHPGTK_SELF(GtkPopoverMenu, GTK_TYPE_POPOVER_MENU);
  gtk_popover_menu_set_flags(self, static_cast<GtkPopoverMenuFlags>(flags));
}

/**
 * Gtk4\GtkPopoverMenu::set_menu_model(?GMenuModel $model): void
 *
 * Sets a new menu model on $popover.
 */
ZEND_METHOD(Gtk4_GtkPopoverMenu, set_menu_model) {
  zval *model = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(model, class_for_gtype(G_TYPE_MENU_MODEL))
  ZEND_PARSE_PARAMETERS_END();
  GtkPopoverMenu *self = PHPGTK_SELF(GtkPopoverMenu, GTK_TYPE_POPOVER_MENU);
  GObject *model_o = nullptr;
  if (model != nullptr) {
    model_o = unwrap(model, G_TYPE_MENU_MODEL);
    if (model_o == nullptr) RETURN_THROWS();
  }
  gtk_popover_menu_set_menu_model(self, model_o != nullptr ? G_MENU_MODEL(model_o) : nullptr);
}
