// GENERATED by gen/gir.php from Gtk.TextTag - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkTextTag
#include "php_gtk4.h"
#include "core/object.h"
#include "core/subtype.h"

using namespace phpgtk;

/**
 * Gtk4\GtkTextTag::__construct(?string $name = null)
 *
 * Creates a `GtkTextTag`.
 */
ZEND_METHOD(Gtk4_GtkTextTag, __construct) {
  zend_string *name = nullptr;
  ZEND_PARSE_PARAMETERS_START(0, 1)
  Z_PARAM_OPTIONAL
  Z_PARAM_STR_OR_NULL(name)
  ZEND_PARSE_PARAMETERS_END();
  if (name != nullptr && !phpgtk::check_utf8(name, 1)) RETURN_THROWS();
  GObject *obj =
      subtype_new(ZEND_THIS, "name", name != nullptr ? ZSTR_VAL(name) : nullptr, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gtk_text_tag_new(name != nullptr ? ZSTR_VAL(name) : nullptr));
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkTextTag::changed(bool $size_changed): void
 *
 * Emits the [signal@Gtk.TextTagTable::tag-changed] signal on the `GtkTextTagTable` where the tag
 * is included.
 */
ZEND_METHOD(Gtk4_GtkTextTag, changed) {
  bool size_changed;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(size_changed)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextTag *self = PHPGTK_SELF(GtkTextTag, GTK_TYPE_TEXT_TAG);
  gtk_text_tag_changed(self, size_changed);
}

/**
 * Gtk4\GtkTextTag::get_priority(): int
 *
 * Get the tag priority.
 */
ZEND_METHOD(Gtk4_GtkTextTag, get_priority) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkTextTag *self = PHPGTK_SELF(GtkTextTag, GTK_TYPE_TEXT_TAG);
  RETURN_LONG(static_cast<zend_long>(gtk_text_tag_get_priority(self)));
}

/**
 * Gtk4\GtkTextTag::set_priority(int $priority): void
 *
 * Sets the priority of a `GtkTextTag`.
 */
ZEND_METHOD(Gtk4_GtkTextTag, set_priority) {
  zend_long priority;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(priority)
  ZEND_PARSE_PARAMETERS_END();
  GtkTextTag *self = PHPGTK_SELF(GtkTextTag, GTK_TYPE_TEXT_TAG);
  if (!phpgtk::check_range<int>(priority, 1)) RETURN_THROWS();
  gtk_text_tag_set_priority(self, static_cast<int>(priority));
}
