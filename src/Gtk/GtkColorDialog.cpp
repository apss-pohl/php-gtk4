// GENERATED by gen/gir.php from Gtk.ColorDialog - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkColorDialog
#include "php_gtk4.h"
#include "core/object.h"
#include "core/boxed.h"
#include "core/subtype.h"
#include "core/callback.h"
#include <array>

using namespace phpgtk;

/**
 * Gtk4\GtkColorDialog::__construct()
 *
 * Creates a new `GtkColorDialog` object.
 */
ZEND_METHOD(Gtk4_GtkColorDialog, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gtk_color_dialog_new());
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

namespace {
// AsyncReadyCallback trampoline for GtkColorDialog::choose_rgba(): wraps the C arguments, invokes
// the PHP callable once and releases it (async scope).
void cb_choose_rgba_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\GtkColorDialog::choose_rgba(?GtkWindow $parent, ?GdkRGBA $initial_color, ?GCancellable
 * $cancellable, ?callable $callback): void
 *
 * This function initiates a color choice operation by presenting a color chooser dialog to the
 * user.
 */
ZEND_METHOD(Gtk4_GtkColorDialog, choose_rgba) {
  zval *parent = nullptr;
  zval *initial_color = nullptr;
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(4, 4)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(parent, class_for_gtype(GTK_TYPE_WINDOW))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(initial_color, boxed_class_for_type(GDK_TYPE_RGBA)->ce)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC_OR_NULL(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  GtkColorDialog *self = PHPGTK_SELF(GtkColorDialog, GTK_TYPE_COLOR_DIALOG);
  GObject *parent_o = nullptr;
  if (parent != nullptr) {
    parent_o = unwrap(parent, GTK_TYPE_WINDOW);
    if (parent_o == nullptr) RETURN_THROWS();
  }
  gpointer initial_color_b = nullptr;
  if (initial_color != nullptr) {
    initial_color_b = unwrap_boxed(initial_color, GDK_TYPE_RGBA);
    if (initial_color_b == nullptr) RETURN_THROWS();
  }
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback =
      ZEND_FCI_INITIALIZED(fci_callback)
          ? callback_new(&fci_callback.function_name, "GtkColorDialog::choose_rgba")
          : nullptr;
  gtk_color_dialog_choose_rgba(self, parent_o != nullptr ? GTK_WINDOW(parent_o) : nullptr,
                               static_cast<GdkRGBA *>(initial_color_b),
                               cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
                               cb_callback != nullptr ? cb_choose_rgba_callback : nullptr,
                               cb_callback);
}

/**
 * Gtk4\GtkColorDialog::choose_rgba_finish(GAsyncResult $result): ?GdkRGBA
 *
 * Finishes the `choose_rgba` call and returns the resulting color.
 */
ZEND_METHOD(Gtk4_GtkColorDialog, choose_rgba_finish) {
  zval *result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  GtkColorDialog *self = PHPGTK_SELF(GtkColorDialog, GTK_TYPE_COLOR_DIALOG);
  GObject *result_o = unwrap(result, G_TYPE_ASYNC_RESULT);
  if (result_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  GdkRGBA *phpgtk_ret = gtk_color_dialog_choose_rgba_finish(self, G_ASYNC_RESULT(result_o), &error);
  wrap_boxed(GDK_TYPE_RGBA, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(GDK_TYPE_RGBA, phpgtk_ret);
}

/**
 * Gtk4\GtkColorDialog::get_modal(): bool
 *
 * Returns whether the color chooser dialog blocks interaction with the parent window while it is
 * presented.
 */
ZEND_METHOD(Gtk4_GtkColorDialog, get_modal) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkColorDialog *self = PHPGTK_SELF(GtkColorDialog, GTK_TYPE_COLOR_DIALOG);
  RETURN_BOOL(gtk_color_dialog_get_modal(self));
}

/**
 * Gtk4\GtkColorDialog::get_title(): string
 *
 * Returns the title that will be shown on the color chooser dialog.
 */
ZEND_METHOD(Gtk4_GtkColorDialog, get_title) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkColorDialog *self = PHPGTK_SELF(GtkColorDialog, GTK_TYPE_COLOR_DIALOG);
  const char *phpgtk_ret = gtk_color_dialog_get_title(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

/**
 * Gtk4\GtkColorDialog::get_with_alpha(): bool
 *
 * Returns whether colors may have alpha.
 */
ZEND_METHOD(Gtk4_GtkColorDialog, get_with_alpha) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkColorDialog *self = PHPGTK_SELF(GtkColorDialog, GTK_TYPE_COLOR_DIALOG);
  RETURN_BOOL(gtk_color_dialog_get_with_alpha(self));
}

/**
 * Gtk4\GtkColorDialog::set_modal(bool $modal): void
 *
 * Sets whether the color chooser dialog blocks interaction with the parent window while it is
 * presented.
 */
ZEND_METHOD(Gtk4_GtkColorDialog, set_modal) {
  bool modal;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(modal)
  ZEND_PARSE_PARAMETERS_END();
  GtkColorDialog *self = PHPGTK_SELF(GtkColorDialog, GTK_TYPE_COLOR_DIALOG);
  gtk_color_dialog_set_modal(self, modal);
}

/**
 * Gtk4\GtkColorDialog::set_title(string $title): void
 *
 * Sets the title that will be shown on the color chooser dialog.
 */
ZEND_METHOD(Gtk4_GtkColorDialog, set_title) {
  zend_string *title;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(title)
  ZEND_PARSE_PARAMETERS_END();
  GtkColorDialog *self = PHPGTK_SELF(GtkColorDialog, GTK_TYPE_COLOR_DIALOG);
  if (!phpgtk::check_utf8(title, 1)) RETURN_THROWS();
  gtk_color_dialog_set_title(self, ZSTR_VAL(title));
}

/**
 * Gtk4\GtkColorDialog::set_with_alpha(bool $with_alpha): void
 *
 * Sets whether colors may have alpha.
 */
ZEND_METHOD(Gtk4_GtkColorDialog, set_with_alpha) {
  bool with_alpha;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(with_alpha)
  ZEND_PARSE_PARAMETERS_END();
  GtkColorDialog *self = PHPGTK_SELF(GtkColorDialog, GTK_TYPE_COLOR_DIALOG);
  gtk_color_dialog_set_with_alpha(self, with_alpha);
}
