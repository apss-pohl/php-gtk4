// GENERATED by gen/gir.php from Gtk.StringList - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkStringList
#include "php_gtk4.h"
#include "core/object.h"
#include "core/collections.h"

using namespace phpgtk;

/**
 * Gtk4\GtkStringList::__construct(?array $strings = null)
 *
 * Creates a new `GtkStringList` with the given $strings.
 */
ZEND_METHOD(Gtk4_GtkStringList, __construct) {
  zval *strings = nullptr;
  ZEND_PARSE_PARAMETERS_START(0, 1)
  Z_PARAM_OPTIONAL
  Z_PARAM_ARRAY_OR_NULL(strings)
  ZEND_PARSE_PARAMETERS_END();
  char **strings_v = nullptr;
  if (strings != nullptr) {
    strings_v = strv_from_php(strings);
    if (strings_v == nullptr) RETURN_THROWS();
  }
  GtkStringList *call_result = gtk_string_list_new(const_cast<const char **>(strings_v));
  if (strings_v != nullptr) g_strfreev(strings_v);
  GObject *obj = G_OBJECT(call_result);
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkStringList::append(string $string): void
 *
 * Appends $string to $self.
 */
ZEND_METHOD(Gtk4_GtkStringList, append) {
  zend_string *string;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(string)
  ZEND_PARSE_PARAMETERS_END();
  GtkStringList *self = PHPGTK_SELF(GtkStringList, GTK_TYPE_STRING_LIST);
  if (!phpgtk::check_utf8(string, 1)) RETURN_THROWS();
  gtk_string_list_append(self, ZSTR_VAL(string));
}

/**
 * Gtk4\GtkStringList::get_string(int $position): ?string
 *
 * Gets the string that is at $position in $self.
 */
ZEND_METHOD(Gtk4_GtkStringList, get_string) {
  zend_long position;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(position)
  ZEND_PARSE_PARAMETERS_END();
  GtkStringList *self = PHPGTK_SELF(GtkStringList, GTK_TYPE_STRING_LIST);
  if (!phpgtk::check_range<guint>(position, 1)) RETURN_THROWS();
  PHPGTK_RETURN_STRING_OR_NULL(gtk_string_list_get_string(self, static_cast<guint>(position)));
}

/**
 * Gtk4\GtkStringList::remove(int $position): void
 *
 * Removes the string at $position from $self.
 */
ZEND_METHOD(Gtk4_GtkStringList, remove) {
  zend_long position;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(position)
  ZEND_PARSE_PARAMETERS_END();
  GtkStringList *self = PHPGTK_SELF(GtkStringList, GTK_TYPE_STRING_LIST);
  if (!phpgtk::check_range<guint>(position, 1)) RETURN_THROWS();
  const bool precondition_0 = position < g_list_model_get_n_items(G_LIST_MODEL(self));
  if (!precondition_0) {
    zend_argument_value_error(1, "must be the position of a string");
    RETURN_THROWS();
  }
  gtk_string_list_remove(self, static_cast<guint>(position));
}

/**
 * Gtk4\GtkStringList::splice(int $position, int $n_removals, ?array $additions): void
 *
 * Changes $self by removing $n_removals strings and adding $additions to it.
 */
ZEND_METHOD(Gtk4_GtkStringList, splice) {
  zend_long position;
  zend_long n_removals;
  zval *additions = nullptr;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_LONG(position)
  Z_PARAM_LONG(n_removals)
  Z_PARAM_ARRAY_OR_NULL(additions)
  ZEND_PARSE_PARAMETERS_END();
  GtkStringList *self = PHPGTK_SELF(GtkStringList, GTK_TYPE_STRING_LIST);
  if (!phpgtk::check_range<guint>(position, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<guint>(n_removals, 2)) RETURN_THROWS();
  char **additions_v = nullptr;
  if (additions != nullptr) {
    additions_v = strv_from_php(additions);
    if (additions_v == nullptr) RETURN_THROWS();
  }
  const bool precondition_0 = position + n_removals <= g_list_model_get_n_items(G_LIST_MODEL(self));
  if (!precondition_0) {
    zend_argument_value_error(2, "must not remove past the end of the list");
    RETURN_THROWS();
  }
  gtk_string_list_splice(self, static_cast<guint>(position), static_cast<guint>(n_removals),
                         const_cast<const char **>(additions_v));
  if (additions_v != nullptr) g_strfreev(additions_v);
}

/**
 * Gtk4\GtkStringList::take(string $string): void
 *
 * Adds $string to self at the end, and takes ownership of it.
 */
ZEND_METHOD(Gtk4_GtkStringList, take) {
  zend_string *string;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(string)
  ZEND_PARSE_PARAMETERS_END();
  GtkStringList *self = PHPGTK_SELF(GtkStringList, GTK_TYPE_STRING_LIST);
  if (!phpgtk::check_utf8(string, 1)) RETURN_THROWS();
  gtk_string_list_take(self, g_strdup(ZSTR_VAL(string)));
}
