// GENERATED by gen/gir.php from Gtk.ActionBar - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkActionBar
#include "php_gtk4.h"
#include "core/object.h"
#include "core/subtype.h"

using namespace phpgtk;

/**
 * Gtk4\GtkActionBar::__construct()
 *
 * Creates a new `GtkActionBar` widget.
 */
ZEND_METHOD(Gtk4_GtkActionBar, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gtk_action_bar_new());
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkActionBar::get_center_widget(): ?GtkWidget
 *
 * Retrieves the center bar widget of the bar.
 */
ZEND_METHOD(Gtk4_GtkActionBar, get_center_widget) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkActionBar *self = PHPGTK_SELF(GtkActionBar, GTK_TYPE_ACTION_BAR);
  GtkWidget *phpgtk_ret = gtk_action_bar_get_center_widget(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GtkActionBar::get_revealed(): bool
 *
 * Gets whether the contents of the action bar are revealed.
 */
ZEND_METHOD(Gtk4_GtkActionBar, get_revealed) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkActionBar *self = PHPGTK_SELF(GtkActionBar, GTK_TYPE_ACTION_BAR);
  RETURN_BOOL(gtk_action_bar_get_revealed(self));
}

/**
 * Gtk4\GtkActionBar::pack_end(GtkWidget $child): void
 *
 * Adds $child to $action_bar, packed with reference to the end of the $action_bar.
 */
ZEND_METHOD(Gtk4_GtkActionBar, pack_end) {
  zval *child;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkActionBar *self = PHPGTK_SELF(GtkActionBar, GTK_TYPE_ACTION_BAR);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  gtk_action_bar_pack_end(self, GTK_WIDGET(child_o));
}

/**
 * Gtk4\GtkActionBar::pack_start(GtkWidget $child): void
 *
 * Adds $child to $action_bar, packed with reference to the start of the $action_bar.
 */
ZEND_METHOD(Gtk4_GtkActionBar, pack_start) {
  zval *child;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkActionBar *self = PHPGTK_SELF(GtkActionBar, GTK_TYPE_ACTION_BAR);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  gtk_action_bar_pack_start(self, GTK_WIDGET(child_o));
}

/**
 * Gtk4\GtkActionBar::remove(GtkWidget $child): void
 *
 * Removes a child from $action_bar.
 */
ZEND_METHOD(Gtk4_GtkActionBar, remove) {
  zval *child;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkActionBar *self = PHPGTK_SELF(GtkActionBar, GTK_TYPE_ACTION_BAR);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  gtk_action_bar_remove(self, GTK_WIDGET(child_o));
}

/**
 * Gtk4\GtkActionBar::set_center_widget(?GtkWidget $center_widget): void
 *
 * Sets the center widget for the `GtkActionBar`.
 */
ZEND_METHOD(Gtk4_GtkActionBar, set_center_widget) {
  zval *center_widget = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(center_widget, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkActionBar *self = PHPGTK_SELF(GtkActionBar, GTK_TYPE_ACTION_BAR);
  GObject *center_widget_o = nullptr;
  if (center_widget != nullptr) {
    center_widget_o = unwrap(center_widget, GTK_TYPE_WIDGET);
    if (center_widget_o == nullptr) RETURN_THROWS();
  }
  gtk_action_bar_set_center_widget(
      self, center_widget_o != nullptr ? GTK_WIDGET(center_widget_o) : nullptr);
}

/**
 * Gtk4\GtkActionBar::set_revealed(bool $revealed): void
 *
 * Reveals or conceals the content of the action bar.
 */
ZEND_METHOD(Gtk4_GtkActionBar, set_revealed) {
  bool revealed;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(revealed)
  ZEND_PARSE_PARAMETERS_END();
  GtkActionBar *self = PHPGTK_SELF(GtkActionBar, GTK_TYPE_ACTION_BAR);
  gtk_action_bar_set_revealed(self, revealed);
}
