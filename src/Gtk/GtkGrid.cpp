// GENERATED by gen/gir.php from Gtk.Grid - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GtkGrid
#include "php_gtk4.h"
#include "core/object.h"
#include "core/enums.h"
#include "core/subtype.h"

using namespace phpgtk;

/**
 * Gtk4\GtkGrid::__construct()
 *
 * Creates a new grid widget.
 */
ZEND_METHOD(Gtk4_GtkGrid, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gtk_grid_new());
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GtkGrid::attach(GtkWidget $child, int $column, int $row, int $width, int $height): void
 *
 * Adds a widget to the grid.
 */
ZEND_METHOD(Gtk4_GtkGrid, attach) {
  zval *child;
  zend_long column;
  zend_long row;
  zend_long width;
  zend_long height;
  ZEND_PARSE_PARAMETERS_START(5, 5)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  Z_PARAM_LONG(column)
  Z_PARAM_LONG(row)
  Z_PARAM_LONG(width)
  Z_PARAM_LONG(height)
  ZEND_PARSE_PARAMETERS_END();
  GtkGrid *self = PHPGTK_SELF(GtkGrid, GTK_TYPE_GRID);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  gtk_grid_attach(self, GTK_WIDGET(child_o), static_cast<int>(column), static_cast<int>(row),
                  static_cast<int>(width), static_cast<int>(height));
}

/**
 * Gtk4\GtkGrid::attach_next_to(GtkWidget $child, ?GtkWidget $sibling, GtkPositionType $side, int
 * $width, int $height): void
 *
 * Adds a widget to the grid.
 */
ZEND_METHOD(Gtk4_GtkGrid, attach_next_to) {
  zval *child;
  zval *sibling = nullptr;
  zval *side;
  zend_long width;
  zend_long height;
  ZEND_PARSE_PARAMETERS_START(5, 5)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(sibling, class_for_gtype(GTK_TYPE_WIDGET))
  Z_PARAM_OBJECT_OF_CLASS(side, enum_class_for_type(GTK_TYPE_POSITION_TYPE))
  Z_PARAM_LONG(width)
  Z_PARAM_LONG(height)
  ZEND_PARSE_PARAMETERS_END();
  GtkGrid *self = PHPGTK_SELF(GtkGrid, GTK_TYPE_GRID);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  GObject *sibling_o = nullptr;
  if (sibling != nullptr) {
    sibling_o = unwrap(sibling, GTK_TYPE_WIDGET);
    if (sibling_o == nullptr) RETURN_THROWS();
  }
  gint side_v = 0;
  if (!enum_from_php(side, GTK_TYPE_POSITION_TYPE, &side_v)) RETURN_THROWS();
  gtk_grid_attach_next_to(
      self, GTK_WIDGET(child_o), sibling_o != nullptr ? GTK_WIDGET(sibling_o) : nullptr,
      static_cast<GtkPositionType>(side_v), static_cast<int>(width), static_cast<int>(height));
}

/**
 * Gtk4\GtkGrid::get_baseline_row(): int
 *
 * Returns which row defines the global baseline of $grid.
 */
ZEND_METHOD(Gtk4_GtkGrid, get_baseline_row) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkGrid *self = PHPGTK_SELF(GtkGrid, GTK_TYPE_GRID);
  RETURN_LONG(static_cast<zend_long>(gtk_grid_get_baseline_row(self)));
}

/**
 * Gtk4\GtkGrid::get_child_at(int $column, int $row): ?GtkWidget
 *
 * Gets the child of $grid whose area covers the grid cell at $column, $row.
 */
ZEND_METHOD(Gtk4_GtkGrid, get_child_at) {
  zend_long column;
  zend_long row;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_LONG(column)
  Z_PARAM_LONG(row)
  ZEND_PARSE_PARAMETERS_END();
  GtkGrid *self = PHPGTK_SELF(GtkGrid, GTK_TYPE_GRID);
  GtkWidget *result = gtk_grid_get_child_at(self, static_cast<int>(column), static_cast<int>(row));
  wrap(result != nullptr ? G_OBJECT(result) : nullptr, return_value);
}

/**
 * Gtk4\GtkGrid::get_column_homogeneous(): bool
 *
 * Returns whether all columns of $grid have the same width.
 */
ZEND_METHOD(Gtk4_GtkGrid, get_column_homogeneous) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkGrid *self = PHPGTK_SELF(GtkGrid, GTK_TYPE_GRID);
  RETURN_BOOL(gtk_grid_get_column_homogeneous(self));
}

/**
 * Gtk4\GtkGrid::get_column_spacing(): int
 *
 * Returns the amount of space between the columns of $grid.
 */
ZEND_METHOD(Gtk4_GtkGrid, get_column_spacing) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkGrid *self = PHPGTK_SELF(GtkGrid, GTK_TYPE_GRID);
  RETURN_LONG(static_cast<zend_long>(gtk_grid_get_column_spacing(self)));
}

/**
 * Gtk4\GtkGrid::get_row_baseline_position(int $row): GtkBaselinePosition
 *
 * Returns the baseline position of $row.
 */
ZEND_METHOD(Gtk4_GtkGrid, get_row_baseline_position) {
  zend_long row;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(row)
  ZEND_PARSE_PARAMETERS_END();
  GtkGrid *self = PHPGTK_SELF(GtkGrid, GTK_TYPE_GRID);
  enum_to_php(GTK_TYPE_BASELINE_POSITION,
              gtk_grid_get_row_baseline_position(self, static_cast<int>(row)), return_value);
}

/**
 * Gtk4\GtkGrid::get_row_homogeneous(): bool
 *
 * Returns whether all rows of $grid have the same height.
 */
ZEND_METHOD(Gtk4_GtkGrid, get_row_homogeneous) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkGrid *self = PHPGTK_SELF(GtkGrid, GTK_TYPE_GRID);
  RETURN_BOOL(gtk_grid_get_row_homogeneous(self));
}

/**
 * Gtk4\GtkGrid::get_row_spacing(): int
 *
 * Returns the amount of space between the rows of $grid.
 */
ZEND_METHOD(Gtk4_GtkGrid, get_row_spacing) {
  ZEND_PARSE_PARAMETERS_NONE();
  GtkGrid *self = PHPGTK_SELF(GtkGrid, GTK_TYPE_GRID);
  RETURN_LONG(static_cast<zend_long>(gtk_grid_get_row_spacing(self)));
}

/**
 * Gtk4\GtkGrid::insert_column(int $position): void
 *
 * Inserts a column at the specified position.
 */
ZEND_METHOD(Gtk4_GtkGrid, insert_column) {
  zend_long position;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(position)
  ZEND_PARSE_PARAMETERS_END();
  GtkGrid *self = PHPGTK_SELF(GtkGrid, GTK_TYPE_GRID);
  gtk_grid_insert_column(self, static_cast<int>(position));
}

/**
 * Gtk4\GtkGrid::insert_next_to(GtkWidget $sibling, GtkPositionType $side): void
 *
 * Inserts a row or column at the specified position.
 */
ZEND_METHOD(Gtk4_GtkGrid, insert_next_to) {
  zval *sibling;
  zval *side;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(sibling, class_for_gtype(GTK_TYPE_WIDGET))
  Z_PARAM_OBJECT_OF_CLASS(side, enum_class_for_type(GTK_TYPE_POSITION_TYPE))
  ZEND_PARSE_PARAMETERS_END();
  GtkGrid *self = PHPGTK_SELF(GtkGrid, GTK_TYPE_GRID);
  GObject *sibling_o = unwrap(sibling, GTK_TYPE_WIDGET);
  if (sibling_o == nullptr) RETURN_THROWS();
  gint side_v = 0;
  if (!enum_from_php(side, GTK_TYPE_POSITION_TYPE, &side_v)) RETURN_THROWS();
  gtk_grid_insert_next_to(self, GTK_WIDGET(sibling_o), static_cast<GtkPositionType>(side_v));
}

/**
 * Gtk4\GtkGrid::insert_row(int $position): void
 *
 * Inserts a row at the specified position.
 */
ZEND_METHOD(Gtk4_GtkGrid, insert_row) {
  zend_long position;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(position)
  ZEND_PARSE_PARAMETERS_END();
  GtkGrid *self = PHPGTK_SELF(GtkGrid, GTK_TYPE_GRID);
  gtk_grid_insert_row(self, static_cast<int>(position));
}

/**
 * Gtk4\GtkGrid::query_child(GtkWidget $child): array
 *
 * Queries the attach points and spans of $child inside the given `GtkGrid`.
 */
ZEND_METHOD(Gtk4_GtkGrid, query_child) {
  zval *child;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkGrid *self = PHPGTK_SELF(GtkGrid, GTK_TYPE_GRID);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  int column = 0;
  int row = 0;
  int width = 0;
  int height = 0;
  gtk_grid_query_child(self, GTK_WIDGET(child_o), &column, &row, &width, &height);
  array_init_size(return_value, 4);
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(column));
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(row));
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(width));
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(height));
    add_next_index_zval(return_value, &item);
  }
}

/**
 * Gtk4\GtkGrid::remove(GtkWidget $child): void
 *
 * Removes a child from $grid.
 */
ZEND_METHOD(Gtk4_GtkGrid, remove) {
  zval *child;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(child, class_for_gtype(GTK_TYPE_WIDGET))
  ZEND_PARSE_PARAMETERS_END();
  GtkGrid *self = PHPGTK_SELF(GtkGrid, GTK_TYPE_GRID);
  GObject *child_o = unwrap(child, GTK_TYPE_WIDGET);
  if (child_o == nullptr) RETURN_THROWS();
  gtk_grid_remove(self, GTK_WIDGET(child_o));
}

/**
 * Gtk4\GtkGrid::remove_column(int $position): void
 *
 * Removes a column from the grid.
 */
ZEND_METHOD(Gtk4_GtkGrid, remove_column) {
  zend_long position;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(position)
  ZEND_PARSE_PARAMETERS_END();
  GtkGrid *self = PHPGTK_SELF(GtkGrid, GTK_TYPE_GRID);
  gtk_grid_remove_column(self, static_cast<int>(position));
}

/**
 * Gtk4\GtkGrid::remove_row(int $position): void
 *
 * Removes a row from the grid.
 */
ZEND_METHOD(Gtk4_GtkGrid, remove_row) {
  zend_long position;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(position)
  ZEND_PARSE_PARAMETERS_END();
  GtkGrid *self = PHPGTK_SELF(GtkGrid, GTK_TYPE_GRID);
  gtk_grid_remove_row(self, static_cast<int>(position));
}

/**
 * Gtk4\GtkGrid::set_baseline_row(int $row): void
 *
 * Sets which row defines the global baseline for the entire grid.
 */
ZEND_METHOD(Gtk4_GtkGrid, set_baseline_row) {
  zend_long row;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(row)
  ZEND_PARSE_PARAMETERS_END();
  GtkGrid *self = PHPGTK_SELF(GtkGrid, GTK_TYPE_GRID);
  gtk_grid_set_baseline_row(self, static_cast<int>(row));
}

/**
 * Gtk4\GtkGrid::set_column_homogeneous(bool $homogeneous): void
 *
 * Sets whether all columns of $grid will have the same width.
 */
ZEND_METHOD(Gtk4_GtkGrid, set_column_homogeneous) {
  bool homogeneous;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(homogeneous)
  ZEND_PARSE_PARAMETERS_END();
  GtkGrid *self = PHPGTK_SELF(GtkGrid, GTK_TYPE_GRID);
  gtk_grid_set_column_homogeneous(self, homogeneous);
}

/**
 * Gtk4\GtkGrid::set_column_spacing(int $spacing): void
 *
 * Sets the amount of space between columns of $grid.
 */
ZEND_METHOD(Gtk4_GtkGrid, set_column_spacing) {
  zend_long spacing;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(spacing)
  ZEND_PARSE_PARAMETERS_END();
  GtkGrid *self = PHPGTK_SELF(GtkGrid, GTK_TYPE_GRID);
  gtk_grid_set_column_spacing(self, static_cast<guint>(spacing));
}

/**
 * Gtk4\GtkGrid::set_row_baseline_position(int $row, GtkBaselinePosition $pos): void
 *
 * Sets how the baseline should be positioned on $row of the grid, in case that row is assigned
 * more space than is requested.
 */
ZEND_METHOD(Gtk4_GtkGrid, set_row_baseline_position) {
  zend_long row;
  zval *pos;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_LONG(row)
  Z_PARAM_OBJECT_OF_CLASS(pos, enum_class_for_type(GTK_TYPE_BASELINE_POSITION))
  ZEND_PARSE_PARAMETERS_END();
  GtkGrid *self = PHPGTK_SELF(GtkGrid, GTK_TYPE_GRID);
  gint pos_v = 0;
  if (!enum_from_php(pos, GTK_TYPE_BASELINE_POSITION, &pos_v)) RETURN_THROWS();
  gtk_grid_set_row_baseline_position(self, static_cast<int>(row),
                                     static_cast<GtkBaselinePosition>(pos_v));
}

/**
 * Gtk4\GtkGrid::set_row_homogeneous(bool $homogeneous): void
 *
 * Sets whether all rows of $grid will have the same height.
 */
ZEND_METHOD(Gtk4_GtkGrid, set_row_homogeneous) {
  bool homogeneous;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(homogeneous)
  ZEND_PARSE_PARAMETERS_END();
  GtkGrid *self = PHPGTK_SELF(GtkGrid, GTK_TYPE_GRID);
  gtk_grid_set_row_homogeneous(self, homogeneous);
}

/**
 * Gtk4\GtkGrid::set_row_spacing(int $spacing): void
 *
 * Sets the amount of space between rows of $grid.
 */
ZEND_METHOD(Gtk4_GtkGrid, set_row_spacing) {
  zend_long spacing;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(spacing)
  ZEND_PARSE_PARAMETERS_END();
  GtkGrid *self = PHPGTK_SELF(GtkGrid, GTK_TYPE_GRID);
  gtk_grid_set_row_spacing(self, static_cast<guint>(spacing));
}
