// GENERATED by gen/gir.php from WebKit.WebViewSessionState - do not edit (gen/overrides,
// gen/skip.txt). Gtk4\WebKitWebViewSessionState (boxed value type)
#include "php_gtk4.h"
#include <webkit/webkit.h>
#include "core/boxed.h"
#include "core/object.h"
#include <cstring>

using namespace phpgtk;

namespace {

const char *const fields[] = {nullptr};

// Boxed field reader: the public scalar fields as PHP properties.
bool read(gpointer data, const char *field, zval *rv) {
  (void)data;
  (void)field;
  (void)rv;
  return false;
}

// Boxed field writer: each field converts like a parameter of its type (core/boxed).
bool write(gpointer data, const char *field, zval *v) {
  (void)data;
  (void)field;
  (void)v;
  return false;
}

}  // namespace

/**
 * Gtk4\WebKitWebViewSessionState::__construct(string $data)
 *
 * Creates a new #WebKitWebViewSessionState from serialized data.
 */
ZEND_METHOD(Gtk4_WebKitWebViewSessionState, __construct) {
  zend_string *data;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(data)
  ZEND_PARSE_PARAMETERS_END();
  GBytes *data_b = g_bytes_new(ZSTR_VAL(data), ZSTR_LEN(data));
  gpointer obj = webkit_web_view_session_state_new(data_b);
  g_bytes_unref(data_b);
  if (obj == nullptr) {
    zend_throw_error(nullptr, "%s(): GTK refused to create the value (see the CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  boxed_adopt(boxed_from_zval(ZEND_THIS), WEBKIT_TYPE_WEB_VIEW_SESSION_STATE, obj);
}

/**
 * Gtk4\WebKitWebViewSessionState::serialize(): string
 *
 * Serializes a #WebKitWebViewSessionState.
 */
ZEND_METHOD(Gtk4_WebKitWebViewSessionState, serialize) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebViewSessionState *self = PHPGTK_BOXED_SELF(WebKitWebViewSessionState);
  GBytes *bytes = webkit_web_view_session_state_serialize(self);
  gsize size = 0;
  const auto *data = static_cast<const char *>(g_bytes_get_data(bytes, &size));
  RETVAL_STRINGL(data, size);
  g_bytes_unref(bytes);
}

namespace phpgtk {
// MINIT: bind the PHP class to WEBKIT_TYPE_WEB_VIEW_SESSION_STATE with its field table.
void register_WebKitWebViewSessionState(zend_class_entry *ce) {
  register_boxed(BoxedClass{.type = WEBKIT_TYPE_WEB_VIEW_SESSION_STATE,
                            .ce = ce,
                            .fields = fields,
                            .read = read,
                            .write = write});
}
}  // namespace phpgtk
