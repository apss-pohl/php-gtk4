// GENERATED by gen/gir.php from WebKit.WebView - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\WebKitWebView
#include "php_gtk4.h"
#include <webkit/webkit.h>
#include "core/object.h"
#include "core/enums.h"
#include "core/collections.h"
#include "core/gerror.h"
#include "core/variant.h"
#include "core/boxed.h"
#include "core/subtype.h"
#include "core/error.h"
#include "core/callback.h"
#include <array>

using namespace phpgtk;

/**
 * Gtk4\WebKitWebView::__construct()
 *
 * Creates a new #WebKitWebView with the default #WebKitWebContext.
 */
ZEND_METHOD(Gtk4_WebKitWebView, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(webkit_web_view_new());
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

namespace {
// AsyncReadyCallback trampoline for WebKitWebView::call_async_javascript_function(): wraps the C
// arguments, invokes the PHP callable once and releases it (async scope).
void cb_call_async_javascript_function_callback(GObject *source_object, GAsyncResult *res,
                                                gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\WebKitWebView::call_async_javascript_function(string $body, int $length, mixed $arguments,
 * ?string $world_name, ?string $source_uri, ?GCancellable $cancellable, ?callable $callback): void
 *
 * Asynchronously call $body with $arguments in the script world with name $world_name of the main
 * frame current context in $web_view. The $arguments values must be one of the following types, or
 * contain only the following GVariant types: number, string and dictionary. The result of the
 * operation can be a Promise that will be properly passed to the callback. If $world_name is
 * `null`, the default world is used. Any value that is not `null` is a distin ct world. The
 * $source_uri will be shown in exceptions and doesn't affect the behavior of the script. When not
 * provided, the document URL is used.
 */
ZEND_METHOD(Gtk4_WebKitWebView, call_async_javascript_function) {
  zend_string *body;
  zend_long length;
  zval *arguments = nullptr;
  zend_string *world_name = nullptr;
  zend_string *source_uri = nullptr;
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(7, 7)
  Z_PARAM_STR(body)
  Z_PARAM_LONG(length)
  Z_PARAM_ZVAL(arguments)
  Z_PARAM_STR_OR_NULL(world_name)
  Z_PARAM_STR_OR_NULL(source_uri)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC_OR_NULL(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!phpgtk::check_utf8(body, 1)) RETURN_THROWS();
  GVariant *arguments_v = nullptr;
  if (arguments != nullptr && Z_TYPE_P(arguments) != IS_NULL) {
    arguments_v = php_to_variant(arguments, nullptr);
    if (arguments_v == nullptr) RETURN_THROWS();
    g_variant_ref_sink(arguments_v);
  }
  if (world_name != nullptr && !phpgtk::check_utf8(world_name, 4)) RETURN_THROWS();
  if (source_uri != nullptr && !phpgtk::check_utf8(source_uri, 5)) RETURN_THROWS();
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback = ZEND_FCI_INITIALIZED(fci_callback)
                              ? callback_new(&fci_callback.function_name,
                                             "WebKitWebView::call_async_javascript_function")
                              : nullptr;
  const bool precondition_0 =
      length == -1 || (length >= 0 && static_cast<gsize>(length) <= ZSTR_LEN(body));
  if (!precondition_0) {
    zend_argument_value_error(2, "must be -1 or at most the length of the body");
    RETURN_THROWS();
  }
  webkit_web_view_call_async_javascript_function(
      self, ZSTR_VAL(body), static_cast<gssize>(length), arguments_v,
      world_name != nullptr ? ZSTR_VAL(world_name) : nullptr,
      source_uri != nullptr ? ZSTR_VAL(source_uri) : nullptr,
      cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
      cb_callback != nullptr ? cb_call_async_javascript_function_callback : nullptr, cb_callback);
  if (arguments_v != nullptr) g_variant_unref(arguments_v);
}

/**
 * Gtk4\WebKitWebView::call_async_javascript_function_finish(GAsyncResult $result): JSCValue
 *
 * Finish an asynchronous operation started with webkit_web_view_call_async_javascript_function().
 */
ZEND_METHOD(Gtk4_WebKitWebView, call_async_javascript_function_finish) {
  zval *result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  GObject *result_o = unwrap(result, G_TYPE_ASYNC_RESULT);
  if (result_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  JSCValue *phpgtk_ret =
      webkit_web_view_call_async_javascript_function_finish(self, G_ASYNC_RESULT(result_o), &error);
  if (phpgtk_ret == nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

namespace {
// AsyncReadyCallback trampoline for WebKitWebView::can_execute_editing_command(): wraps the C
// arguments, invokes the PHP callable once and releases it (async scope).
void cb_can_execute_editing_command_callback(GObject *source_object, GAsyncResult *res,
                                             gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\WebKitWebView::can_execute_editing_command(string $command, ?GCancellable $cancellable,
 * ?callable $callback): void
 *
 * Asynchronously check if it is possible to execute the given editing command.
 */
ZEND_METHOD(Gtk4_WebKitWebView, can_execute_editing_command) {
  zend_string *command;
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_STR(command)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC_OR_NULL(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!phpgtk::check_utf8(command, 1)) RETURN_THROWS();
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback =
      ZEND_FCI_INITIALIZED(fci_callback)
          ? callback_new(&fci_callback.function_name, "WebKitWebView::can_execute_editing_command")
          : nullptr;
  webkit_web_view_can_execute_editing_command(
      self, ZSTR_VAL(command), cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
      cb_callback != nullptr ? cb_can_execute_editing_command_callback : nullptr, cb_callback);
}

/**
 * Gtk4\WebKitWebView::can_execute_editing_command_finish(GAsyncResult $result): bool
 *
 * Finish an asynchronous operation started with webkit_web_view_can_execute_editing_command().
 */
ZEND_METHOD(Gtk4_WebKitWebView, can_execute_editing_command_finish) {
  zval *result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  GObject *result_o = unwrap(result, G_TYPE_ASYNC_RESULT);
  if (result_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  const gboolean ok =
      webkit_web_view_can_execute_editing_command_finish(self, G_ASYNC_RESULT(result_o), &error);
  if (!ok) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  RETURN_BOOL(ok);
}

/**
 * Gtk4\WebKitWebView::can_go_back(): bool
 *
 * Determines whether $web_view has a previous history item.
 */
ZEND_METHOD(Gtk4_WebKitWebView, can_go_back) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  RETURN_BOOL(webkit_web_view_can_go_back(self));
}

/**
 * Gtk4\WebKitWebView::can_go_forward(): bool
 *
 * Determines whether $web_view has a next history item.
 */
ZEND_METHOD(Gtk4_WebKitWebView, can_go_forward) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  RETURN_BOOL(webkit_web_view_can_go_forward(self));
}

/**
 * Gtk4\WebKitWebView::can_show_mime_type(string $mime_type): bool
 *
 * Whether or not a MIME type can be displayed in $web_view.
 */
ZEND_METHOD(Gtk4_WebKitWebView, can_show_mime_type) {
  zend_string *mime_type;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(mime_type)
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!phpgtk::check_utf8(mime_type, 1)) RETURN_THROWS();
  RETURN_BOOL(webkit_web_view_can_show_mime_type(self, ZSTR_VAL(mime_type)));
}

/**
 * Gtk4\WebKitWebView::download_uri(string $uri): WebKitDownload
 *
 * Requests downloading of the specified URI string for $web_view.
 */
ZEND_METHOD(Gtk4_WebKitWebView, download_uri) {
  zend_string *uri;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(uri)
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!phpgtk::check_utf8(uri, 1)) RETURN_THROWS();
  WebKitDownload *phpgtk_ret = webkit_web_view_download_uri(self, ZSTR_VAL(uri));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

namespace {
// AsyncReadyCallback trampoline for WebKitWebView::evaluate_javascript(): wraps the C arguments,
// invokes the PHP callable once and releases it (async scope).
void cb_evaluate_javascript_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\WebKitWebView::evaluate_javascript(string $script, int $length, ?string $world_name, ?string
 * $source_uri, ?GCancellable $cancellable, ?callable $callback): void
 *
 * Asynchronously evaluate $script in the script world with name $world_name of the main frame
 * current context in $web_view. If $world_name is `null`, the default world is used. Any value
 * that is not `null` is a distinct world. The $source_uri will be shown in exceptions and doesn't
 * affect the behavior of the script. When not provided, the document URL is used.
 */
ZEND_METHOD(Gtk4_WebKitWebView, evaluate_javascript) {
  zend_string *script;
  zend_long length;
  zend_string *world_name = nullptr;
  zend_string *source_uri = nullptr;
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(6, 6)
  Z_PARAM_STR(script)
  Z_PARAM_LONG(length)
  Z_PARAM_STR_OR_NULL(world_name)
  Z_PARAM_STR_OR_NULL(source_uri)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC_OR_NULL(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!phpgtk::check_utf8(script, 1)) RETURN_THROWS();
  if (world_name != nullptr && !phpgtk::check_utf8(world_name, 3)) RETURN_THROWS();
  if (source_uri != nullptr && !phpgtk::check_utf8(source_uri, 4)) RETURN_THROWS();
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback =
      ZEND_FCI_INITIALIZED(fci_callback)
          ? callback_new(&fci_callback.function_name, "WebKitWebView::evaluate_javascript")
          : nullptr;
  const bool precondition_0 =
      length == -1 || (length >= 0 && static_cast<gsize>(length) <= ZSTR_LEN(script));
  if (!precondition_0) {
    zend_argument_value_error(2, "must be -1 or at most the length of the script");
    RETURN_THROWS();
  }
  webkit_web_view_evaluate_javascript(
      self, ZSTR_VAL(script), static_cast<gssize>(length),
      world_name != nullptr ? ZSTR_VAL(world_name) : nullptr,
      source_uri != nullptr ? ZSTR_VAL(source_uri) : nullptr,
      cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
      cb_callback != nullptr ? cb_evaluate_javascript_callback : nullptr, cb_callback);
}

/**
 * Gtk4\WebKitWebView::evaluate_javascript_finish(GAsyncResult $result): JSCValue
 *
 * Finish an asynchronous operation started with webkit_web_view_evaluate_javascript().
 */
ZEND_METHOD(Gtk4_WebKitWebView, evaluate_javascript_finish) {
  zval *result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  GObject *result_o = unwrap(result, G_TYPE_ASYNC_RESULT);
  if (result_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  JSCValue *phpgtk_ret =
      webkit_web_view_evaluate_javascript_finish(self, G_ASYNC_RESULT(result_o), &error);
  if (phpgtk_ret == nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * Gtk4\WebKitWebView::execute_editing_command(string $command): void
 *
 * Request to execute the given $command for $web_view.
 */
ZEND_METHOD(Gtk4_WebKitWebView, execute_editing_command) {
  zend_string *command;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(command)
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!phpgtk::check_utf8(command, 1)) RETURN_THROWS();
  webkit_web_view_execute_editing_command(self, ZSTR_VAL(command));
}

/**
 * Gtk4\WebKitWebView::execute_editing_command_with_argument(string $command, string $argument):
 * void
 *
 * Request to execute the given $command with $argument for $web_view.
 */
ZEND_METHOD(Gtk4_WebKitWebView, execute_editing_command_with_argument) {
  zend_string *command;
  zend_string *argument;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_STR(command)
  Z_PARAM_STR(argument)
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!phpgtk::check_utf8(command, 1)) RETURN_THROWS();
  if (!phpgtk::check_utf8(argument, 2)) RETURN_THROWS();
  webkit_web_view_execute_editing_command_with_argument(self, ZSTR_VAL(command),
                                                        ZSTR_VAL(argument));
}

/**
 * Gtk4\WebKitWebView::get_automation_presentation_type():
 * WebKitAutomationBrowsingContextPresentation
 *
 * Get the presentation type of #WebKitWebView when created for automation.
 */
ZEND_METHOD(Gtk4_WebKitWebView, get_automation_presentation_type) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  enum_to_php(WEBKIT_TYPE_AUTOMATION_BROWSING_CONTEXT_PRESENTATION,
              webkit_web_view_get_automation_presentation_type(self), return_value);
}

/**
 * Gtk4\WebKitWebView::get_back_forward_list(): WebKitBackForwardList
 *
 * Obtains the #WebKitBackForwardList associated with the given #WebKitWebView.
 */
ZEND_METHOD(Gtk4_WebKitWebView, get_back_forward_list) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  WebKitBackForwardList *phpgtk_ret = webkit_web_view_get_back_forward_list(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\WebKitWebView::get_background_color(): GdkRGBA
 *
 * Gets the color that is used to draw the $web_view background.
 */
ZEND_METHOD(Gtk4_WebKitWebView, get_background_color) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  GdkRGBA rgba{};
  webkit_web_view_get_background_color(self, &rgba);
  wrap_boxed(GDK_TYPE_RGBA, &rgba, return_value);
}

/**
 * Gtk4\WebKitWebView::get_camera_capture_state(): WebKitMediaCaptureState
 *
 * Get the camera capture state of a #WebKitWebView.
 */
ZEND_METHOD(Gtk4_WebKitWebView, get_camera_capture_state) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  enum_to_php(WEBKIT_TYPE_MEDIA_CAPTURE_STATE, webkit_web_view_get_camera_capture_state(self),
              return_value);
}

/**
 * Gtk4\WebKitWebView::get_context(): WebKitWebContext
 *
 * Gets the web context of $web_view.
 */
ZEND_METHOD(Gtk4_WebKitWebView, get_context) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  WebKitWebContext *phpgtk_ret = webkit_web_view_get_context(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\WebKitWebView::get_custom_charset(): string
 *
 * Returns the current custom character encoding name of $web_view.
 */
ZEND_METHOD(Gtk4_WebKitWebView, get_custom_charset) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  const char *phpgtk_ret = webkit_web_view_get_custom_charset(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

/**
 * Gtk4\WebKitWebView::get_default_content_security_policy(): ?string
 *
 * Gets the configured default Content-Security-Policy.
 */
ZEND_METHOD(Gtk4_WebKitWebView, get_default_content_security_policy) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  PHPGTK_RETURN_STRING_OR_NULL(webkit_web_view_get_default_content_security_policy(self));
}

/**
 * Gtk4\WebKitWebView::get_display_capture_state(): WebKitMediaCaptureState
 *
 * Get the display capture state of a #WebKitWebView.
 */
ZEND_METHOD(Gtk4_WebKitWebView, get_display_capture_state) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  enum_to_php(WEBKIT_TYPE_MEDIA_CAPTURE_STATE, webkit_web_view_get_display_capture_state(self),
              return_value);
}

/**
 * Gtk4\WebKitWebView::get_editor_state(): WebKitEditorState
 *
 * Gets the web editor state of $web_view.
 */
ZEND_METHOD(Gtk4_WebKitWebView, get_editor_state) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  WebKitEditorState *phpgtk_ret = webkit_web_view_get_editor_state(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\WebKitWebView::get_estimated_load_progress(): float
 *
 * Gets the value of the #WebKitWebView:estimated-load-progress property.
 */
ZEND_METHOD(Gtk4_WebKitWebView, get_estimated_load_progress) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  RETURN_DOUBLE(webkit_web_view_get_estimated_load_progress(self));
}

/**
 * Gtk4\WebKitWebView::get_favicon(): ?GdkTexture
 *
 * Returns favicon currently associated to $web_view.
 */
ZEND_METHOD(Gtk4_WebKitWebView, get_favicon) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  GdkTexture *phpgtk_ret = webkit_web_view_get_favicon(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\WebKitWebView::get_find_controller(): WebKitFindController
 *
 * Gets the #WebKitFindController.
 */
ZEND_METHOD(Gtk4_WebKitWebView, get_find_controller) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  WebKitFindController *phpgtk_ret = webkit_web_view_get_find_controller(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\WebKitWebView::get_input_method_context(): ?WebKitInputMethodContext
 *
 * Get the #WebKitInputMethodContext currently in use by $web_view.
 */
ZEND_METHOD(Gtk4_WebKitWebView, get_input_method_context) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  WebKitInputMethodContext *phpgtk_ret = webkit_web_view_get_input_method_context(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\WebKitWebView::get_inspector(): WebKitWebInspector
 *
 * Get the #WebKitWebInspector associated to $web_view
 */
ZEND_METHOD(Gtk4_WebKitWebView, get_inspector) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  WebKitWebInspector *phpgtk_ret = webkit_web_view_get_inspector(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\WebKitWebView::get_is_muted(): bool
 *
 * Gets the mute state of $web_view.
 */
ZEND_METHOD(Gtk4_WebKitWebView, get_is_muted) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  RETURN_BOOL(webkit_web_view_get_is_muted(self));
}

/**
 * Gtk4\WebKitWebView::get_is_web_process_responsive(): bool
 *
 * Get whether the current web process of a #WebKitWebView is responsive.
 */
ZEND_METHOD(Gtk4_WebKitWebView, get_is_web_process_responsive) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  RETURN_BOOL(webkit_web_view_get_is_web_process_responsive(self));
}

/**
 * Gtk4\WebKitWebView::get_main_resource(): ?WebKitWebResource
 *
 * Return the main resource of $web_view.
 */
ZEND_METHOD(Gtk4_WebKitWebView, get_main_resource) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  WebKitWebResource *phpgtk_ret = webkit_web_view_get_main_resource(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\WebKitWebView::get_microphone_capture_state(): WebKitMediaCaptureState
 *
 * Get the microphone capture state of a #WebKitWebView.
 */
ZEND_METHOD(Gtk4_WebKitWebView, get_microphone_capture_state) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  enum_to_php(WEBKIT_TYPE_MEDIA_CAPTURE_STATE, webkit_web_view_get_microphone_capture_state(self),
              return_value);
}

/**
 * Gtk4\WebKitWebView::get_network_session(): WebKitNetworkSession
 *
 * Get the #WebKitNetworkSession associated to $web_view.
 */
ZEND_METHOD(Gtk4_WebKitWebView, get_network_session) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  WebKitNetworkSession *phpgtk_ret = webkit_web_view_get_network_session(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\WebKitWebView::get_page_id(): int
 *
 * Get the identifier of the #WebKitWebPage corresponding to the #WebKitWebView
 */
ZEND_METHOD(Gtk4_WebKitWebView, get_page_id) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  RETURN_LONG(static_cast<zend_long>(webkit_web_view_get_page_id(self)));
}

/**
 * Gtk4\WebKitWebView::get_session_state(): WebKitWebViewSessionState
 *
 * Gets the current session state of $web_view
 */
ZEND_METHOD(Gtk4_WebKitWebView, get_session_state) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  WebKitWebViewSessionState *phpgtk_ret = webkit_web_view_get_session_state(self);
  wrap_boxed(WEBKIT_TYPE_WEB_VIEW_SESSION_STATE, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(WEBKIT_TYPE_WEB_VIEW_SESSION_STATE, phpgtk_ret);
}

/**
 * Gtk4\WebKitWebView::get_settings(): WebKitSettings
 *
 * Gets the #WebKitSettings currently applied to $web_view.
 */
ZEND_METHOD(Gtk4_WebKitWebView, get_settings) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  WebKitSettings *phpgtk_ret = webkit_web_view_get_settings(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

namespace {
// AsyncReadyCallback trampoline for WebKitWebView::get_snapshot(): wraps the C arguments, invokes
// the PHP callable once and releases it (async scope).
void cb_get_snapshot_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\WebKitWebView::get_snapshot(WebKitSnapshotRegion $region, int $options, ?GCancellable
 * $cancellable, ?callable $callback): void
 *
 * Asynchronously retrieves a snapshot of $web_view for $region.
 */
ZEND_METHOD(Gtk4_WebKitWebView, get_snapshot) {
  zval *region;
  zend_long options;
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(4, 4)
  Z_PARAM_OBJECT_OF_CLASS(region, enum_class_for_type(WEBKIT_TYPE_SNAPSHOT_REGION))
  Z_PARAM_LONG(options)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC_OR_NULL(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  gint region_v = 0;
  if (!enum_from_php(region, WEBKIT_TYPE_SNAPSHOT_REGION, &region_v)) RETURN_THROWS();
  if (!phpgtk::check_flags(WEBKIT_TYPE_SNAPSHOT_OPTIONS, options, 2)) RETURN_THROWS();
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback =
      ZEND_FCI_INITIALIZED(fci_callback)
          ? callback_new(&fci_callback.function_name, "WebKitWebView::get_snapshot")
          : nullptr;
  webkit_web_view_get_snapshot(self, static_cast<WebKitSnapshotRegion>(region_v),
                               static_cast<WebKitSnapshotOptions>(options),
                               cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
                               cb_callback != nullptr ? cb_get_snapshot_callback : nullptr,
                               cb_callback);
}

/**
 * Gtk4\WebKitWebView::get_snapshot_finish(GAsyncResult $result): GdkTexture
 *
 * Finishes an asynchronous operation started with webkit_web_view_get_snapshot(), producing an
 * image of the snapshot using the BGRA8888 pixel format.
 */
ZEND_METHOD(Gtk4_WebKitWebView, get_snapshot_finish) {
  zval *result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  GObject *result_o = unwrap(result, G_TYPE_ASYNC_RESULT);
  if (result_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  GdkTexture *phpgtk_ret =
      webkit_web_view_get_snapshot_finish(self, G_ASYNC_RESULT(result_o), &error);
  if (phpgtk_ret == nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * Gtk4\WebKitWebView::get_theme_color(): ?GdkRGBA
 *
 * Gets the theme color that is specified by the content in the $web_view. If the $web_view doesn't
 * have a theme color it will fill the $rgba with transparent black content.
 */
ZEND_METHOD(Gtk4_WebKitWebView, get_theme_color) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  GdkRGBA rgba{};
  if (!webkit_web_view_get_theme_color(self, &rgba)) RETURN_NULL();
  wrap_boxed(GDK_TYPE_RGBA, &rgba, return_value);
}

/**
 * Gtk4\WebKitWebView::get_title(): string
 *
 * Gets the value of the #WebKitWebView:title property.
 */
ZEND_METHOD(Gtk4_WebKitWebView, get_title) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  const char *phpgtk_ret = webkit_web_view_get_title(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

/**
 * Gtk4\WebKitWebView::get_tls_info(): ?array
 *
 * Retrieves the #GTlsCertificate associated with the main resource of $web_view.
 */
ZEND_METHOD(Gtk4_WebKitWebView, get_tls_info) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  GTlsCertificate *certificate = nullptr;
  GTlsCertificateFlags errors = static_cast<GTlsCertificateFlags>(0);
  if (!webkit_web_view_get_tls_info(self, &certificate, &errors)) RETURN_NULL();
  array_init_size(return_value, 2);
  {
    zval item;
    wrap(certificate != nullptr ? G_OBJECT(certificate) : nullptr, &item);
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(errors));
    add_next_index_zval(return_value, &item);
  }
}

/**
 * Gtk4\WebKitWebView::get_uri(): string
 *
 * Returns the current active URI of $web_view.
 */
ZEND_METHOD(Gtk4_WebKitWebView, get_uri) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  const char *phpgtk_ret = webkit_web_view_get_uri(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

/**
 * Gtk4\WebKitWebView::get_user_content_manager(): WebKitUserContentManager
 *
 * Gets the user content manager associated to $web_view.
 */
ZEND_METHOD(Gtk4_WebKitWebView, get_user_content_manager) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  WebKitUserContentManager *phpgtk_ret = webkit_web_view_get_user_content_manager(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\WebKitWebView::get_web_extension_mode(): WebKitWebExtensionMode
 *
 * Get the view's #WebKitWebExtensionMode.
 */
ZEND_METHOD(Gtk4_WebKitWebView, get_web_extension_mode) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  enum_to_php(WEBKIT_TYPE_WEB_EXTENSION_MODE, webkit_web_view_get_web_extension_mode(self),
              return_value);
}

/**
 * Gtk4\WebKitWebView::get_website_policies(): WebKitWebsitePolicies
 *
 * Gets the default website policies.
 */
ZEND_METHOD(Gtk4_WebKitWebView, get_website_policies) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  WebKitWebsitePolicies *phpgtk_ret = webkit_web_view_get_website_policies(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\WebKitWebView::get_window_properties(): WebKitWindowProperties
 *
 * Get the #WebKitWindowProperties object.
 */
ZEND_METHOD(Gtk4_WebKitWebView, get_window_properties) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  WebKitWindowProperties *phpgtk_ret = webkit_web_view_get_window_properties(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\WebKitWebView::get_zoom_level(): float
 *
 * Set the zoom level of $web_view.
 */
ZEND_METHOD(Gtk4_WebKitWebView, get_zoom_level) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  RETURN_DOUBLE(webkit_web_view_get_zoom_level(self));
}

/**
 * Gtk4\WebKitWebView::go_back(): void
 *
 * Loads the previous history item.
 */
ZEND_METHOD(Gtk4_WebKitWebView, go_back) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  webkit_web_view_go_back(self);
}

/**
 * Gtk4\WebKitWebView::go_forward(): void
 *
 * Loads the next history item.
 */
ZEND_METHOD(Gtk4_WebKitWebView, go_forward) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  webkit_web_view_go_forward(self);
}

/**
 * Gtk4\WebKitWebView::go_to_back_forward_list_item(WebKitBackForwardListItem $list_item): void
 *
 * Loads the specific history item $list_item.
 */
ZEND_METHOD(Gtk4_WebKitWebView, go_to_back_forward_list_item) {
  zval *list_item;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(list_item, class_for_gtype(WEBKIT_TYPE_BACK_FORWARD_LIST_ITEM))
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  GObject *list_item_o = unwrap(list_item, WEBKIT_TYPE_BACK_FORWARD_LIST_ITEM);
  if (list_item_o == nullptr) RETURN_THROWS();
  webkit_web_view_go_to_back_forward_list_item(self, WEBKIT_BACK_FORWARD_LIST_ITEM(list_item_o));
}

/**
 * Gtk4\WebKitWebView::is_controlled_by_automation(): bool
 *
 * Get whether a #WebKitWebView was created with #WebKitWebView:is-controlled-by-automation
 * property enabled.
 */
ZEND_METHOD(Gtk4_WebKitWebView, is_controlled_by_automation) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  RETURN_BOOL(webkit_web_view_is_controlled_by_automation(self));
}

/**
 * Gtk4\WebKitWebView::is_editable(): bool
 *
 * Gets whether the user is allowed to edit the HTML document.
 */
ZEND_METHOD(Gtk4_WebKitWebView, is_editable) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  RETURN_BOOL(webkit_web_view_is_editable(self));
}

/**
 * Gtk4\WebKitWebView::is_immersive_mode_enabled(): bool
 *
 * Gets whether $web_view is in immersive mode.
 */
ZEND_METHOD(Gtk4_WebKitWebView, is_immersive_mode_enabled) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  RETURN_BOOL(webkit_web_view_is_immersive_mode_enabled(self));
}

/**
 * Gtk4\WebKitWebView::is_loading(): bool
 *
 * Gets the value of the #WebKitWebView:is-loading property.
 */
ZEND_METHOD(Gtk4_WebKitWebView, is_loading) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  RETURN_BOOL(webkit_web_view_is_loading(self));
}

/**
 * Gtk4\WebKitWebView::is_playing_audio(): bool
 *
 * Gets the value of the #WebKitWebView:is-playing-audio property.
 */
ZEND_METHOD(Gtk4_WebKitWebView, is_playing_audio) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  RETURN_BOOL(webkit_web_view_is_playing_audio(self));
}

/**
 * Gtk4\WebKitWebView::leave_immersive_mode(): void
 *
 * Requests to leave the immersive mode this #WebKitWebView is in.
 */
ZEND_METHOD(Gtk4_WebKitWebView, leave_immersive_mode) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  webkit_web_view_leave_immersive_mode(self);
}

/**
 * Gtk4\WebKitWebView::load_alternate_html(string $content, string $content_uri, ?string $base_uri):
 * void
 *
 * Load the given $content string for the URI $content_uri.
 */
ZEND_METHOD(Gtk4_WebKitWebView, load_alternate_html) {
  zend_string *content;
  zend_string *content_uri;
  zend_string *base_uri = nullptr;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_STR(content)
  Z_PARAM_STR(content_uri)
  Z_PARAM_STR_OR_NULL(base_uri)
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!phpgtk::check_utf8(content, 1)) RETURN_THROWS();
  if (!phpgtk::check_utf8(content_uri, 2)) RETURN_THROWS();
  if (base_uri != nullptr && !phpgtk::check_utf8(base_uri, 3)) RETURN_THROWS();
  webkit_web_view_load_alternate_html(self, ZSTR_VAL(content), ZSTR_VAL(content_uri),
                                      base_uri != nullptr ? ZSTR_VAL(base_uri) : nullptr);
}

/**
 * Gtk4\WebKitWebView::load_bytes(string $bytes, ?string $mime_type, ?string $encoding, ?string
 * $base_uri): void
 *
 * Load the specified $bytes into $web_view using the given $mime_type and $encoding.
 */
ZEND_METHOD(Gtk4_WebKitWebView, load_bytes) {
  zend_string *bytes;
  zend_string *mime_type = nullptr;
  zend_string *encoding = nullptr;
  zend_string *base_uri = nullptr;
  ZEND_PARSE_PARAMETERS_START(4, 4)
  Z_PARAM_STR(bytes)
  Z_PARAM_STR_OR_NULL(mime_type)
  Z_PARAM_STR_OR_NULL(encoding)
  Z_PARAM_STR_OR_NULL(base_uri)
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  GBytes *bytes_b = g_bytes_new(ZSTR_VAL(bytes), ZSTR_LEN(bytes));
  if (mime_type != nullptr && !phpgtk::check_utf8(mime_type, 2)) RETURN_THROWS();
  if (encoding != nullptr && !phpgtk::check_utf8(encoding, 3)) RETURN_THROWS();
  if (base_uri != nullptr && !phpgtk::check_utf8(base_uri, 4)) RETURN_THROWS();
  webkit_web_view_load_bytes(self, bytes_b, mime_type != nullptr ? ZSTR_VAL(mime_type) : nullptr,
                             encoding != nullptr ? ZSTR_VAL(encoding) : nullptr,
                             base_uri != nullptr ? ZSTR_VAL(base_uri) : nullptr);
  g_bytes_unref(bytes_b);
}

/**
 * Gtk4\WebKitWebView::load_html(string $content, ?string $base_uri): void
 *
 * Load the given $content string with the specified $base_uri.
 */
ZEND_METHOD(Gtk4_WebKitWebView, load_html) {
  zend_string *content;
  zend_string *base_uri = nullptr;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_STR(content)
  Z_PARAM_STR_OR_NULL(base_uri)
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!phpgtk::check_utf8(content, 1)) RETURN_THROWS();
  if (base_uri != nullptr && !phpgtk::check_utf8(base_uri, 2)) RETURN_THROWS();
  webkit_web_view_load_html(self, ZSTR_VAL(content),
                            base_uri != nullptr ? ZSTR_VAL(base_uri) : nullptr);
}

/**
 * Gtk4\WebKitWebView::load_plain_text(string $plain_text): void
 *
 * Load the specified $plain_text string into $web_view.
 */
ZEND_METHOD(Gtk4_WebKitWebView, load_plain_text) {
  zend_string *plain_text;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(plain_text)
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!phpgtk::check_utf8(plain_text, 1)) RETURN_THROWS();
  webkit_web_view_load_plain_text(self, ZSTR_VAL(plain_text));
}

/**
 * Gtk4\WebKitWebView::load_request(WebKitURIRequest $request): void
 *
 * Requests loading of the specified #WebKitURIRequest.
 */
ZEND_METHOD(Gtk4_WebKitWebView, load_request) {
  zval *request;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(request, class_for_gtype(WEBKIT_TYPE_URI_REQUEST))
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  GObject *request_o = unwrap(request, WEBKIT_TYPE_URI_REQUEST);
  if (request_o == nullptr) RETURN_THROWS();
  webkit_web_view_load_request(self, WEBKIT_URI_REQUEST(request_o));
}

/**
 * Gtk4\WebKitWebView::load_uri(string $uri): void
 *
 * Requests loading of the specified URI string.
 */
ZEND_METHOD(Gtk4_WebKitWebView, load_uri) {
  zend_string *uri;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(uri)
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!phpgtk::check_utf8(uri, 1)) RETURN_THROWS();
  webkit_web_view_load_uri(self, ZSTR_VAL(uri));
}

/**
 * Gtk4\WebKitWebView::reload(): void
 *
 * Reloads the current contents of $web_view.
 */
ZEND_METHOD(Gtk4_WebKitWebView, reload) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  webkit_web_view_reload(self);
}

/**
 * Gtk4\WebKitWebView::reload_bypass_cache(): void
 *
 * Reloads the current contents of $web_view without using any cached data.
 */
ZEND_METHOD(Gtk4_WebKitWebView, reload_bypass_cache) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  webkit_web_view_reload_bypass_cache(self);
}

/**
 * Gtk4\WebKitWebView::restore_session_state(WebKitWebViewSessionState $state): void
 *
 * Restore the $web_view session state from $state
 */
ZEND_METHOD(Gtk4_WebKitWebView, restore_session_state) {
  zval *state;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(state, boxed_class_for_type(WEBKIT_TYPE_WEB_VIEW_SESSION_STATE)->ce)
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  gpointer state_b = unwrap_boxed(state, WEBKIT_TYPE_WEB_VIEW_SESSION_STATE);
  if (state_b == nullptr) RETURN_THROWS();
  webkit_web_view_restore_session_state(self, static_cast<WebKitWebViewSessionState *>(state_b));
}

namespace {
// AsyncReadyCallback trampoline for WebKitWebView::save(): wraps the C arguments, invokes the PHP
// callable once and releases it (async scope).
void cb_save_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\WebKitWebView::save(WebKitSaveMode $save_mode, ?GCancellable $cancellable, ?callable
 * $callback): void
 *
 * Asynchronously save the current web page.
 */
ZEND_METHOD(Gtk4_WebKitWebView, save) {
  zval *save_mode;
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_OBJECT_OF_CLASS(save_mode, enum_class_for_type(WEBKIT_TYPE_SAVE_MODE))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC_OR_NULL(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  gint save_mode_v = 0;
  if (!enum_from_php(save_mode, WEBKIT_TYPE_SAVE_MODE, &save_mode_v)) RETURN_THROWS();
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback = ZEND_FCI_INITIALIZED(fci_callback)
                              ? callback_new(&fci_callback.function_name, "WebKitWebView::save")
                              : nullptr;
  webkit_web_view_save(self, static_cast<WebKitSaveMode>(save_mode_v),
                       cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
                       cb_callback != nullptr ? cb_save_callback : nullptr, cb_callback);
}

/**
 * Gtk4\WebKitWebView::save_finish(GAsyncResult $result): GInputStream
 *
 * Finish an asynchronous operation started with webkit_web_view_save().
 */
ZEND_METHOD(Gtk4_WebKitWebView, save_finish) {
  zval *result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  GObject *result_o = unwrap(result, G_TYPE_ASYNC_RESULT);
  if (result_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  GInputStream *phpgtk_ret = webkit_web_view_save_finish(self, G_ASYNC_RESULT(result_o), &error);
  if (phpgtk_ret == nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

namespace {
// AsyncReadyCallback trampoline for WebKitWebView::save_to_file(): wraps the C arguments, invokes
// the PHP callable once and releases it (async scope).
void cb_save_to_file_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\WebKitWebView::save_to_file(string $file, WebKitSaveMode $save_mode, ?GCancellable
 * $cancellable, ?callable $callback): void
 *
 * Asynchronously save the current web page.
 */
ZEND_METHOD(Gtk4_WebKitWebView, save_to_file) {
  zend_string *file;
  zval *save_mode;
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(4, 4)
  Z_PARAM_PATH_STR(file)
  Z_PARAM_OBJECT_OF_CLASS(save_mode, enum_class_for_type(WEBKIT_TYPE_SAVE_MODE))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC_OR_NULL(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  GFile *file_f = g_file_new_for_commandline_arg(ZSTR_VAL(file));
  gint save_mode_v = 0;
  if (!enum_from_php(save_mode, WEBKIT_TYPE_SAVE_MODE, &save_mode_v)) RETURN_THROWS();
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback =
      ZEND_FCI_INITIALIZED(fci_callback)
          ? callback_new(&fci_callback.function_name, "WebKitWebView::save_to_file")
          : nullptr;
  webkit_web_view_save_to_file(self, file_f, static_cast<WebKitSaveMode>(save_mode_v),
                               cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
                               cb_callback != nullptr ? cb_save_to_file_callback : nullptr,
                               cb_callback);
  if (file_f != nullptr) g_object_unref(file_f);
}

/**
 * Gtk4\WebKitWebView::save_to_file_finish(GAsyncResult $result): bool
 *
 * Finish an asynchronous operation started with webkit_web_view_save_to_file().
 */
ZEND_METHOD(Gtk4_WebKitWebView, save_to_file_finish) {
  zval *result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  GObject *result_o = unwrap(result, G_TYPE_ASYNC_RESULT);
  if (result_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  const gboolean ok = webkit_web_view_save_to_file_finish(self, G_ASYNC_RESULT(result_o), &error);
  if (!ok) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  RETURN_BOOL(ok);
}

namespace {
// AsyncReadyCallback trampoline for WebKitWebView::send_message_to_page(): wraps the C arguments,
// invokes the PHP callable once and releases it (async scope).
void cb_send_message_to_page_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\WebKitWebView::send_message_to_page(WebKitUserMessage $message, ?GCancellable $cancellable,
 * ?callable $callback): void
 *
 * Send $message to the #WebKitWebPage corresponding to $web_view.
 */
ZEND_METHOD(Gtk4_WebKitWebView, send_message_to_page) {
  zval *message;
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_OBJECT_OF_CLASS(message, class_for_gtype(WEBKIT_TYPE_USER_MESSAGE))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC_OR_NULL(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  GObject *message_o = unwrap(message, WEBKIT_TYPE_USER_MESSAGE);
  if (message_o == nullptr) RETURN_THROWS();
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback =
      ZEND_FCI_INITIALIZED(fci_callback)
          ? callback_new(&fci_callback.function_name, "WebKitWebView::send_message_to_page")
          : nullptr;
  webkit_web_view_send_message_to_page(
      self, WEBKIT_USER_MESSAGE(message_o),
      cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
      cb_callback != nullptr ? cb_send_message_to_page_callback : nullptr, cb_callback);
}

/**
 * Gtk4\WebKitWebView::send_message_to_page_finish(GAsyncResult $result): WebKitUserMessage
 *
 * Finish an asynchronous operation started with webkit_web_view_send_message_to_page().
 */
ZEND_METHOD(Gtk4_WebKitWebView, send_message_to_page_finish) {
  zval *result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  GObject *result_o = unwrap(result, G_TYPE_ASYNC_RESULT);
  if (result_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  WebKitUserMessage *phpgtk_ret =
      webkit_web_view_send_message_to_page_finish(self, G_ASYNC_RESULT(result_o), &error);
  if (phpgtk_ret == nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * Gtk4\WebKitWebView::set_background_color(GdkRGBA $rgba): void
 *
 * Sets the color that will be used to draw the $web_view background.
 */
ZEND_METHOD(Gtk4_WebKitWebView, set_background_color) {
  zval *rgba;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(rgba, boxed_class_for_type(GDK_TYPE_RGBA)->ce)
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  gpointer rgba_b = unwrap_boxed(rgba, GDK_TYPE_RGBA);
  if (rgba_b == nullptr) RETURN_THROWS();
  webkit_web_view_set_background_color(self, static_cast<GdkRGBA *>(rgba_b));
}

/**
 * Gtk4\WebKitWebView::set_camera_capture_state(WebKitMediaCaptureState $state): void
 *
 * Set the camera capture state of a #WebKitWebView.
 */
ZEND_METHOD(Gtk4_WebKitWebView, set_camera_capture_state) {
  zval *state;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(state, enum_class_for_type(WEBKIT_TYPE_MEDIA_CAPTURE_STATE))
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  gint state_v = 0;
  if (!enum_from_php(state, WEBKIT_TYPE_MEDIA_CAPTURE_STATE, &state_v)) RETURN_THROWS();
  webkit_web_view_set_camera_capture_state(self, static_cast<WebKitMediaCaptureState>(state_v));
}

/**
 * Gtk4\WebKitWebView::set_cors_allowlist(?array $allowlist): void
 *
 * Sets the $allowlist for CORS.
 */
ZEND_METHOD(Gtk4_WebKitWebView, set_cors_allowlist) {
  zval *allowlist = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_ARRAY_OR_NULL(allowlist)
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  char **allowlist_v = nullptr;
  if (allowlist != nullptr) {
    allowlist_v = strv_from_php(allowlist);
    if (allowlist_v == nullptr) RETURN_THROWS();
  }
  webkit_web_view_set_cors_allowlist(self, const_cast<const char **>(allowlist_v));
  if (allowlist_v != nullptr) g_strfreev(allowlist_v);
}

/**
 * Gtk4\WebKitWebView::set_custom_charset(?string $charset): void
 *
 * Sets the current custom character encoding override of $web_view.
 */
ZEND_METHOD(Gtk4_WebKitWebView, set_custom_charset) {
  zend_string *charset = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR_OR_NULL(charset)
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (charset != nullptr && !phpgtk::check_utf8(charset, 1)) RETURN_THROWS();
  webkit_web_view_set_custom_charset(self, charset != nullptr ? ZSTR_VAL(charset) : nullptr);
}

/**
 * Gtk4\WebKitWebView::set_display_capture_state(WebKitMediaCaptureState $state): void
 *
 * Set the display capture state of a #WebKitWebView.
 */
ZEND_METHOD(Gtk4_WebKitWebView, set_display_capture_state) {
  zval *state;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(state, enum_class_for_type(WEBKIT_TYPE_MEDIA_CAPTURE_STATE))
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  gint state_v = 0;
  if (!enum_from_php(state, WEBKIT_TYPE_MEDIA_CAPTURE_STATE, &state_v)) RETURN_THROWS();
  webkit_web_view_set_display_capture_state(self, static_cast<WebKitMediaCaptureState>(state_v));
}

/**
 * Gtk4\WebKitWebView::set_editable(bool $editable): void
 *
 * Sets whether the user is allowed to edit the HTML document.
 */
ZEND_METHOD(Gtk4_WebKitWebView, set_editable) {
  bool editable;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(editable)
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  webkit_web_view_set_editable(self, editable);
}

/**
 * Gtk4\WebKitWebView::set_input_method_context(?WebKitInputMethodContext $context): void
 *
 * Set the #WebKitInputMethodContext to be used by $web_view.
 */
ZEND_METHOD(Gtk4_WebKitWebView, set_input_method_context) {
  zval *context = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(context, class_for_gtype(WEBKIT_TYPE_INPUT_METHOD_CONTEXT))
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  GObject *context_o = nullptr;
  if (context != nullptr) {
    context_o = unwrap(context, WEBKIT_TYPE_INPUT_METHOD_CONTEXT);
    if (context_o == nullptr) RETURN_THROWS();
  }
  webkit_web_view_set_input_method_context(
      self, context_o != nullptr ? WEBKIT_INPUT_METHOD_CONTEXT(context_o) : nullptr);
}

/**
 * Gtk4\WebKitWebView::set_is_muted(bool $muted): void
 *
 * Sets the mute state of $web_view.
 */
ZEND_METHOD(Gtk4_WebKitWebView, set_is_muted) {
  bool muted;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(muted)
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  webkit_web_view_set_is_muted(self, muted);
}

/**
 * Gtk4\WebKitWebView::set_microphone_capture_state(WebKitMediaCaptureState $state): void
 *
 * Set the microphone capture state of a #WebKitWebView.
 */
ZEND_METHOD(Gtk4_WebKitWebView, set_microphone_capture_state) {
  zval *state;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(state, enum_class_for_type(WEBKIT_TYPE_MEDIA_CAPTURE_STATE))
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  gint state_v = 0;
  if (!enum_from_php(state, WEBKIT_TYPE_MEDIA_CAPTURE_STATE, &state_v)) RETURN_THROWS();
  webkit_web_view_set_microphone_capture_state(self, static_cast<WebKitMediaCaptureState>(state_v));
}

/**
 * Gtk4\WebKitWebView::set_settings(WebKitSettings $settings): void
 *
 * Sets the #WebKitSettings to be applied to $web_view.
 */
ZEND_METHOD(Gtk4_WebKitWebView, set_settings) {
  zval *settings;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(settings, class_for_gtype(WEBKIT_TYPE_SETTINGS))
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  GObject *settings_o = unwrap(settings, WEBKIT_TYPE_SETTINGS);
  if (settings_o == nullptr) RETURN_THROWS();
  webkit_web_view_set_settings(self, WEBKIT_SETTINGS(settings_o));
}

/**
 * Gtk4\WebKitWebView::set_zoom_level(float $zoom_level): void
 *
 * Set the zoom level of $web_view.
 */
ZEND_METHOD(Gtk4_WebKitWebView, set_zoom_level) {
  double zoom_level;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_DOUBLE(zoom_level)
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  webkit_web_view_set_zoom_level(self, zoom_level);
}

/**
 * Gtk4\WebKitWebView::stop_loading(): void
 *
 * Stops any ongoing loading operation in $web_view.
 */
ZEND_METHOD(Gtk4_WebKitWebView, stop_loading) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  webkit_web_view_stop_loading(self);
}

/**
 * Gtk4\WebKitWebView::terminate_web_process(): void
 *
 * Terminates the web process associated to $web_view.
 */
ZEND_METHOD(Gtk4_WebKitWebView, terminate_web_process) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  webkit_web_view_terminate_web_process(self);
}

/**
 * Gtk4\WebKitWebView::try_close(): void
 *
 * Tries to close the $web_view.
 */
ZEND_METHOD(Gtk4_WebKitWebView, try_close) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  webkit_web_view_try_close(self);
}

// vfunc thunks and installers: file-local, installed by class_init of a PHP subtype
namespace {

// vfunc thunk: WEBKIT_WEB_VIEW_CLASS->authenticate -> $this->vfunc_authenticate() on a PHP subclass
gboolean vfunc_thunk_authenticate(WebKitWebView *self, WebKitAuthenticationRequest *request) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_authenticate", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->authenticate != nullptr ? native->authenticate(self, request) : FALSE;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 1> args{};
  zval *argv = args.data();
  wrap(request != nullptr ? G_OBJECT(request) : nullptr, &argv[0]);
  zval ret;
  ZVAL_UNDEF(&ret);
  gboolean result = FALSE;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = zend_is_true(&ret) ? TRUE : FALSE;
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("WebKitWebView::vfunc_authenticate");
  zend_exception_restore();  // the parked one, previous of whatever this threw
  return result;
}

// vfunc installer: WEBKIT_WEB_VIEW_CLASS->authenticate (called from class_init / iface_init of a
// PHP subtype)
void vfunc_install_authenticate(gpointer klass) {
  WEBKIT_WEB_VIEW_CLASS(klass)->authenticate = vfunc_thunk_authenticate;
}

// vfunc thunk: WEBKIT_WEB_VIEW_CLASS->close -> $this->vfunc_close() on a PHP subclass
void vfunc_thunk_close(WebKitWebView *self) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_close", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->close != nullptr) native->close(self);
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("WebKitWebView::vfunc_close");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: WEBKIT_WEB_VIEW_CLASS->close (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_close(gpointer klass) {
  WEBKIT_WEB_VIEW_CLASS(klass)->close = vfunc_thunk_close;
}

// vfunc thunk: WEBKIT_WEB_VIEW_CLASS->context_menu -> $this->vfunc_context_menu() on a PHP subclass
gboolean vfunc_thunk_context_menu(WebKitWebView *self, WebKitContextMenu *context_menu,
                                  WebKitHitTestResult *hit_test_result) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_context_menu", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->context_menu != nullptr
               ? native->context_menu(self, context_menu, hit_test_result)
               : FALSE;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(context_menu != nullptr ? G_OBJECT(context_menu) : nullptr, &argv[0]);
  wrap(hit_test_result != nullptr ? G_OBJECT(hit_test_result) : nullptr, &argv[1]);
  zval ret;
  ZVAL_UNDEF(&ret);
  gboolean result = FALSE;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 2, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = zend_is_true(&ret) ? TRUE : FALSE;
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("WebKitWebView::vfunc_context_menu");
  zend_exception_restore();  // the parked one, previous of whatever this threw
  return result;
}

// vfunc installer: WEBKIT_WEB_VIEW_CLASS->context_menu (called from class_init / iface_init of a
// PHP subtype)
void vfunc_install_context_menu(gpointer klass) {
  WEBKIT_WEB_VIEW_CLASS(klass)->context_menu = vfunc_thunk_context_menu;
}

// vfunc thunk: WEBKIT_WEB_VIEW_CLASS->context_menu_dismissed ->
// $this->vfunc_context_menu_dismissed() on a PHP subclass
void vfunc_thunk_context_menu_dismissed(WebKitWebView *self) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_context_menu_dismissed", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->context_menu_dismissed != nullptr) native->context_menu_dismissed(self);
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("WebKitWebView::vfunc_context_menu_dismissed");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: WEBKIT_WEB_VIEW_CLASS->context_menu_dismissed (called from class_init /
// iface_init of a PHP subtype)
void vfunc_install_context_menu_dismissed(gpointer klass) {
  WEBKIT_WEB_VIEW_CLASS(klass)->context_menu_dismissed = vfunc_thunk_context_menu_dismissed;
}

// vfunc thunk: WEBKIT_WEB_VIEW_CLASS->create -> $this->vfunc_create() on a PHP subclass
GtkWidget *vfunc_thunk_create(WebKitWebView *self, WebKitNavigationAction *navigation_action) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_create", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->create != nullptr ? native->create(self, navigation_action) : nullptr;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 1> args{};
  zval *argv = args.data();
  wrap_boxed(WEBKIT_TYPE_NAVIGATION_ACTION, navigation_action, &argv[0]);
  zval ret;
  ZVAL_UNDEF(&ret);
  GtkWidget *result = nullptr;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    if (Z_TYPE(ret) == IS_OBJECT) {
      GObject *o = unwrap(&ret, GTK_TYPE_WIDGET);
      result = o != nullptr ? GTK_WIDGET(o) : nullptr;
    }
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("WebKitWebView::vfunc_create");
  zend_exception_restore();  // the parked one, previous of whatever this threw
  return result;
}

// vfunc installer: WEBKIT_WEB_VIEW_CLASS->create (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_create(gpointer klass) {
  WEBKIT_WEB_VIEW_CLASS(klass)->create = vfunc_thunk_create;
}

// vfunc thunk: WEBKIT_WEB_VIEW_CLASS->decide_policy -> $this->vfunc_decide_policy() on a PHP
// subclass
gboolean vfunc_thunk_decide_policy(WebKitWebView *self, WebKitPolicyDecision *decision,
                                   WebKitPolicyDecisionType type) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_decide_policy", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->decide_policy != nullptr ? native->decide_policy(self, decision, type) : FALSE;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(decision != nullptr ? G_OBJECT(decision) : nullptr, &argv[0]);
  enum_to_php(WEBKIT_TYPE_POLICY_DECISION_TYPE, static_cast<gint>(type), &argv[1]);
  zval ret;
  ZVAL_UNDEF(&ret);
  gboolean result = FALSE;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 2, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = zend_is_true(&ret) ? TRUE : FALSE;
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("WebKitWebView::vfunc_decide_policy");
  zend_exception_restore();  // the parked one, previous of whatever this threw
  return result;
}

// vfunc installer: WEBKIT_WEB_VIEW_CLASS->decide_policy (called from class_init / iface_init of a
// PHP subtype)
void vfunc_install_decide_policy(gpointer klass) {
  WEBKIT_WEB_VIEW_CLASS(klass)->decide_policy = vfunc_thunk_decide_policy;
}

// vfunc thunk: WEBKIT_WEB_VIEW_CLASS->enter_fullscreen -> $this->vfunc_enter_fullscreen() on a PHP
// subclass
gboolean vfunc_thunk_enter_fullscreen(WebKitWebView *self) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_enter_fullscreen", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->enter_fullscreen != nullptr ? native->enter_fullscreen(self) : FALSE;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  zval ret;
  ZVAL_UNDEF(&ret);
  gboolean result = FALSE;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = zend_is_true(&ret) ? TRUE : FALSE;
  }
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("WebKitWebView::vfunc_enter_fullscreen");
  zend_exception_restore();  // the parked one, previous of whatever this threw
  return result;
}

// vfunc installer: WEBKIT_WEB_VIEW_CLASS->enter_fullscreen (called from class_init / iface_init of
// a PHP subtype)
void vfunc_install_enter_fullscreen(gpointer klass) {
  WEBKIT_WEB_VIEW_CLASS(klass)->enter_fullscreen = vfunc_thunk_enter_fullscreen;
}

// vfunc thunk: WEBKIT_WEB_VIEW_CLASS->insecure_content_detected ->
// $this->vfunc_insecure_content_detected() on a PHP subclass
void vfunc_thunk_insecure_content_detected(WebKitWebView *self, WebKitInsecureContentEvent event) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_insecure_content_detected", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->insecure_content_detected != nullptr)
      native->insecure_content_detected(self, event);
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 1> args{};
  zval *argv = args.data();
  enum_to_php(WEBKIT_TYPE_INSECURE_CONTENT_EVENT, static_cast<gint>(event), &argv[0]);
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("WebKitWebView::vfunc_insecure_content_detected");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: WEBKIT_WEB_VIEW_CLASS->insecure_content_detected (called from class_init /
// iface_init of a PHP subtype)
void vfunc_install_insecure_content_detected(gpointer klass) {
  WEBKIT_WEB_VIEW_CLASS(klass)->insecure_content_detected = vfunc_thunk_insecure_content_detected;
}

// vfunc thunk: WEBKIT_WEB_VIEW_CLASS->leave_fullscreen -> $this->vfunc_leave_fullscreen() on a PHP
// subclass
gboolean vfunc_thunk_leave_fullscreen(WebKitWebView *self) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_leave_fullscreen", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->leave_fullscreen != nullptr ? native->leave_fullscreen(self) : FALSE;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  zval ret;
  ZVAL_UNDEF(&ret);
  gboolean result = FALSE;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = zend_is_true(&ret) ? TRUE : FALSE;
  }
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("WebKitWebView::vfunc_leave_fullscreen");
  zend_exception_restore();  // the parked one, previous of whatever this threw
  return result;
}

// vfunc installer: WEBKIT_WEB_VIEW_CLASS->leave_fullscreen (called from class_init / iface_init of
// a PHP subtype)
void vfunc_install_leave_fullscreen(gpointer klass) {
  WEBKIT_WEB_VIEW_CLASS(klass)->leave_fullscreen = vfunc_thunk_leave_fullscreen;
}

// vfunc thunk: WEBKIT_WEB_VIEW_CLASS->load_changed -> $this->vfunc_load_changed() on a PHP subclass
void vfunc_thunk_load_changed(WebKitWebView *self, WebKitLoadEvent load_event) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_load_changed", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->load_changed != nullptr) native->load_changed(self, load_event);
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 1> args{};
  zval *argv = args.data();
  enum_to_php(WEBKIT_TYPE_LOAD_EVENT, static_cast<gint>(load_event), &argv[0]);
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("WebKitWebView::vfunc_load_changed");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: WEBKIT_WEB_VIEW_CLASS->load_changed (called from class_init / iface_init of a
// PHP subtype)
void vfunc_install_load_changed(gpointer klass) {
  WEBKIT_WEB_VIEW_CLASS(klass)->load_changed = vfunc_thunk_load_changed;
}

// vfunc thunk: WEBKIT_WEB_VIEW_CLASS->load_failed -> $this->vfunc_load_failed() on a PHP subclass
gboolean vfunc_thunk_load_failed(WebKitWebView *self, WebKitLoadEvent load_event,
                                 const gchar *failing_uri, GError *error) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_load_failed", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->load_failed != nullptr
               ? native->load_failed(self, load_event, failing_uri, error)
               : FALSE;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 3> args{};
  zval *argv = args.data();
  enum_to_php(WEBKIT_TYPE_LOAD_EVENT, static_cast<gint>(load_event), &argv[0]);
  if (failing_uri == nullptr) {
    ZVAL_NULL(&argv[1]);
  } else {
    ZVAL_STRING(&argv[1], failing_uri);
  }
  wrap_boxed(G_TYPE_ERROR, error, &argv[2]);
  zval ret;
  ZVAL_UNDEF(&ret);
  gboolean result = FALSE;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 3, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = zend_is_true(&ret) ? TRUE : FALSE;
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("WebKitWebView::vfunc_load_failed");
  zend_exception_restore();  // the parked one, previous of whatever this threw
  return result;
}

// vfunc installer: WEBKIT_WEB_VIEW_CLASS->load_failed (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_load_failed(gpointer klass) {
  WEBKIT_WEB_VIEW_CLASS(klass)->load_failed = vfunc_thunk_load_failed;
}

// vfunc thunk: WEBKIT_WEB_VIEW_CLASS->load_failed_with_tls_errors ->
// $this->vfunc_load_failed_with_tls_errors() on a PHP subclass
gboolean vfunc_thunk_load_failed_with_tls_errors(WebKitWebView *self, const gchar *failing_uri,
                                                 GTlsCertificate *certificate,
                                                 GTlsCertificateFlags errors) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_load_failed_with_tls_errors", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->load_failed_with_tls_errors != nullptr
               ? native->load_failed_with_tls_errors(self, failing_uri, certificate, errors)
               : FALSE;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 3> args{};
  zval *argv = args.data();
  if (failing_uri == nullptr) {
    ZVAL_NULL(&argv[0]);
  } else {
    ZVAL_STRING(&argv[0], failing_uri);
  }
  wrap(certificate != nullptr ? G_OBJECT(certificate) : nullptr, &argv[1]);
  ZVAL_LONG(&argv[2], static_cast<zend_long>(errors));
  zval ret;
  ZVAL_UNDEF(&ret);
  gboolean result = FALSE;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 3, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = zend_is_true(&ret) ? TRUE : FALSE;
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("WebKitWebView::vfunc_load_failed_with_tls_errors");
  zend_exception_restore();  // the parked one, previous of whatever this threw
  return result;
}

// vfunc installer: WEBKIT_WEB_VIEW_CLASS->load_failed_with_tls_errors (called from class_init /
// iface_init of a PHP subtype)
void vfunc_install_load_failed_with_tls_errors(gpointer klass) {
  WEBKIT_WEB_VIEW_CLASS(klass)->load_failed_with_tls_errors =
      vfunc_thunk_load_failed_with_tls_errors;
}

// vfunc thunk: WEBKIT_WEB_VIEW_CLASS->mouse_target_changed -> $this->vfunc_mouse_target_changed()
// on a PHP subclass
void vfunc_thunk_mouse_target_changed(WebKitWebView *self, WebKitHitTestResult *hit_test_result,
                                      guint modifiers) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_mouse_target_changed", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->mouse_target_changed != nullptr)
      native->mouse_target_changed(self, hit_test_result, modifiers);
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(hit_test_result != nullptr ? G_OBJECT(hit_test_result) : nullptr, &argv[0]);
  ZVAL_LONG(&argv[1], static_cast<zend_long>(modifiers));
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 2, args.data());
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("WebKitWebView::vfunc_mouse_target_changed");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: WEBKIT_WEB_VIEW_CLASS->mouse_target_changed (called from class_init / iface_init
// of a PHP subtype)
void vfunc_install_mouse_target_changed(gpointer klass) {
  WEBKIT_WEB_VIEW_CLASS(klass)->mouse_target_changed = vfunc_thunk_mouse_target_changed;
}

// vfunc thunk: WEBKIT_WEB_VIEW_CLASS->permission_request -> $this->vfunc_permission_request() on a
// PHP subclass
gboolean vfunc_thunk_permission_request(WebKitWebView *self,
                                        WebKitPermissionRequest *permission_request) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_permission_request", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->permission_request != nullptr
               ? native->permission_request(self, permission_request)
               : FALSE;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 1> args{};
  zval *argv = args.data();
  wrap(permission_request != nullptr ? G_OBJECT(permission_request) : nullptr, &argv[0]);
  zval ret;
  ZVAL_UNDEF(&ret);
  gboolean result = FALSE;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = zend_is_true(&ret) ? TRUE : FALSE;
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("WebKitWebView::vfunc_permission_request");
  zend_exception_restore();  // the parked one, previous of whatever this threw
  return result;
}

// vfunc installer: WEBKIT_WEB_VIEW_CLASS->permission_request (called from class_init / iface_init
// of a PHP subtype)
void vfunc_install_permission_request(gpointer klass) {
  WEBKIT_WEB_VIEW_CLASS(klass)->permission_request = vfunc_thunk_permission_request;
}

// vfunc thunk: WEBKIT_WEB_VIEW_CLASS->print -> $this->vfunc_print() on a PHP subclass
gboolean vfunc_thunk_print(WebKitWebView *self, WebKitPrintOperation *print_operation) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_print", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->print != nullptr ? native->print(self, print_operation) : FALSE;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 1> args{};
  zval *argv = args.data();
  wrap(print_operation != nullptr ? G_OBJECT(print_operation) : nullptr, &argv[0]);
  zval ret;
  ZVAL_UNDEF(&ret);
  gboolean result = FALSE;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = zend_is_true(&ret) ? TRUE : FALSE;
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("WebKitWebView::vfunc_print");
  zend_exception_restore();  // the parked one, previous of whatever this threw
  return result;
}

// vfunc installer: WEBKIT_WEB_VIEW_CLASS->print (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_print(gpointer klass) {
  WEBKIT_WEB_VIEW_CLASS(klass)->print = vfunc_thunk_print;
}

// vfunc thunk: WEBKIT_WEB_VIEW_CLASS->query_permission_state ->
// $this->vfunc_query_permission_state() on a PHP subclass
gboolean vfunc_thunk_query_permission_state(WebKitWebView *self,
                                            WebKitPermissionStateQuery *query) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_query_permission_state", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->query_permission_state != nullptr ? native->query_permission_state(self, query)
                                                     : FALSE;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 1> args{};
  zval *argv = args.data();
  wrap_boxed(WEBKIT_TYPE_PERMISSION_STATE_QUERY, query, &argv[0]);
  zval ret;
  ZVAL_UNDEF(&ret);
  gboolean result = FALSE;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = zend_is_true(&ret) ? TRUE : FALSE;
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("WebKitWebView::vfunc_query_permission_state");
  zend_exception_restore();  // the parked one, previous of whatever this threw
  return result;
}

// vfunc installer: WEBKIT_WEB_VIEW_CLASS->query_permission_state (called from class_init /
// iface_init of a PHP subtype)
void vfunc_install_query_permission_state(gpointer klass) {
  WEBKIT_WEB_VIEW_CLASS(klass)->query_permission_state = vfunc_thunk_query_permission_state;
}

// vfunc thunk: WEBKIT_WEB_VIEW_CLASS->ready_to_show -> $this->vfunc_ready_to_show() on a PHP
// subclass
void vfunc_thunk_ready_to_show(WebKitWebView *self) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_ready_to_show", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->ready_to_show != nullptr) native->ready_to_show(self);
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("WebKitWebView::vfunc_ready_to_show");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: WEBKIT_WEB_VIEW_CLASS->ready_to_show (called from class_init / iface_init of a
// PHP subtype)
void vfunc_install_ready_to_show(gpointer klass) {
  WEBKIT_WEB_VIEW_CLASS(klass)->ready_to_show = vfunc_thunk_ready_to_show;
}

// vfunc thunk: WEBKIT_WEB_VIEW_CLASS->resource_load_started -> $this->vfunc_resource_load_started()
// on a PHP subclass
void vfunc_thunk_resource_load_started(WebKitWebView *self, WebKitWebResource *resource,
                                       WebKitURIRequest *request) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_resource_load_started", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->resource_load_started != nullptr)
      native->resource_load_started(self, resource, request);
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(resource != nullptr ? G_OBJECT(resource) : nullptr, &argv[0]);
  wrap(request != nullptr ? G_OBJECT(request) : nullptr, &argv[1]);
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 2, args.data());
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("WebKitWebView::vfunc_resource_load_started");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: WEBKIT_WEB_VIEW_CLASS->resource_load_started (called from class_init /
// iface_init of a PHP subtype)
void vfunc_install_resource_load_started(gpointer klass) {
  WEBKIT_WEB_VIEW_CLASS(klass)->resource_load_started = vfunc_thunk_resource_load_started;
}

// vfunc thunk: WEBKIT_WEB_VIEW_CLASS->run_as_modal -> $this->vfunc_run_as_modal() on a PHP subclass
void vfunc_thunk_run_as_modal(WebKitWebView *self) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_run_as_modal", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->run_as_modal != nullptr) native->run_as_modal(self);
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("WebKitWebView::vfunc_run_as_modal");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: WEBKIT_WEB_VIEW_CLASS->run_as_modal (called from class_init / iface_init of a
// PHP subtype)
void vfunc_install_run_as_modal(gpointer klass) {
  WEBKIT_WEB_VIEW_CLASS(klass)->run_as_modal = vfunc_thunk_run_as_modal;
}

// vfunc thunk: WEBKIT_WEB_VIEW_CLASS->run_color_chooser -> $this->vfunc_run_color_chooser() on a
// PHP subclass
gboolean vfunc_thunk_run_color_chooser(WebKitWebView *self, WebKitColorChooserRequest *request) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_run_color_chooser", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->run_color_chooser != nullptr ? native->run_color_chooser(self, request) : FALSE;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 1> args{};
  zval *argv = args.data();
  wrap(request != nullptr ? G_OBJECT(request) : nullptr, &argv[0]);
  zval ret;
  ZVAL_UNDEF(&ret);
  gboolean result = FALSE;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = zend_is_true(&ret) ? TRUE : FALSE;
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("WebKitWebView::vfunc_run_color_chooser");
  zend_exception_restore();  // the parked one, previous of whatever this threw
  return result;
}

// vfunc installer: WEBKIT_WEB_VIEW_CLASS->run_color_chooser (called from class_init / iface_init of
// a PHP subtype)
void vfunc_install_run_color_chooser(gpointer klass) {
  WEBKIT_WEB_VIEW_CLASS(klass)->run_color_chooser = vfunc_thunk_run_color_chooser;
}

// vfunc thunk: WEBKIT_WEB_VIEW_CLASS->run_file_chooser -> $this->vfunc_run_file_chooser() on a PHP
// subclass
gboolean vfunc_thunk_run_file_chooser(WebKitWebView *self, WebKitFileChooserRequest *request) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_run_file_chooser", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->run_file_chooser != nullptr ? native->run_file_chooser(self, request) : FALSE;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 1> args{};
  zval *argv = args.data();
  wrap(request != nullptr ? G_OBJECT(request) : nullptr, &argv[0]);
  zval ret;
  ZVAL_UNDEF(&ret);
  gboolean result = FALSE;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = zend_is_true(&ret) ? TRUE : FALSE;
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("WebKitWebView::vfunc_run_file_chooser");
  zend_exception_restore();  // the parked one, previous of whatever this threw
  return result;
}

// vfunc installer: WEBKIT_WEB_VIEW_CLASS->run_file_chooser (called from class_init / iface_init of
// a PHP subtype)
void vfunc_install_run_file_chooser(gpointer klass) {
  WEBKIT_WEB_VIEW_CLASS(klass)->run_file_chooser = vfunc_thunk_run_file_chooser;
}

// vfunc thunk: WEBKIT_WEB_VIEW_CLASS->script_dialog -> $this->vfunc_script_dialog() on a PHP
// subclass
gboolean vfunc_thunk_script_dialog(WebKitWebView *self, WebKitScriptDialog *dialog) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_script_dialog", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->script_dialog != nullptr ? native->script_dialog(self, dialog) : FALSE;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 1> args{};
  zval *argv = args.data();
  wrap_boxed(WEBKIT_TYPE_SCRIPT_DIALOG, dialog, &argv[0]);
  zval ret;
  ZVAL_UNDEF(&ret);
  gboolean result = FALSE;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = zend_is_true(&ret) ? TRUE : FALSE;
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("WebKitWebView::vfunc_script_dialog");
  zend_exception_restore();  // the parked one, previous of whatever this threw
  return result;
}

// vfunc installer: WEBKIT_WEB_VIEW_CLASS->script_dialog (called from class_init / iface_init of a
// PHP subtype)
void vfunc_install_script_dialog(gpointer klass) {
  WEBKIT_WEB_VIEW_CLASS(klass)->script_dialog = vfunc_thunk_script_dialog;
}

// vfunc thunk: WEBKIT_WEB_VIEW_CLASS->show_notification -> $this->vfunc_show_notification() on a
// PHP subclass
gboolean vfunc_thunk_show_notification(WebKitWebView *self, WebKitNotification *notification) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_show_notification", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->show_notification != nullptr ? native->show_notification(self, notification)
                                                : FALSE;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 1> args{};
  zval *argv = args.data();
  wrap(notification != nullptr ? G_OBJECT(notification) : nullptr, &argv[0]);
  zval ret;
  ZVAL_UNDEF(&ret);
  gboolean result = FALSE;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = zend_is_true(&ret) ? TRUE : FALSE;
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("WebKitWebView::vfunc_show_notification");
  zend_exception_restore();  // the parked one, previous of whatever this threw
  return result;
}

// vfunc installer: WEBKIT_WEB_VIEW_CLASS->show_notification (called from class_init / iface_init of
// a PHP subtype)
void vfunc_install_show_notification(gpointer klass) {
  WEBKIT_WEB_VIEW_CLASS(klass)->show_notification = vfunc_thunk_show_notification;
}

// vfunc thunk: WEBKIT_WEB_VIEW_CLASS->show_option_menu -> $this->vfunc_show_option_menu() on a PHP
// subclass
gboolean vfunc_thunk_show_option_menu(WebKitWebView *self, WebKitOptionMenu *menu,
                                      GdkRectangle *rectangle) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_show_option_menu", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->show_option_menu != nullptr ? native->show_option_menu(self, menu, rectangle)
                                               : FALSE;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(menu != nullptr ? G_OBJECT(menu) : nullptr, &argv[0]);
  wrap_boxed(GDK_TYPE_RECTANGLE, rectangle, &argv[1]);
  zval ret;
  ZVAL_UNDEF(&ret);
  gboolean result = FALSE;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 2, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = zend_is_true(&ret) ? TRUE : FALSE;
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("WebKitWebView::vfunc_show_option_menu");
  zend_exception_restore();  // the parked one, previous of whatever this threw
  return result;
}

// vfunc installer: WEBKIT_WEB_VIEW_CLASS->show_option_menu (called from class_init / iface_init of
// a PHP subtype)
void vfunc_install_show_option_menu(gpointer klass) {
  WEBKIT_WEB_VIEW_CLASS(klass)->show_option_menu = vfunc_thunk_show_option_menu;
}

// vfunc thunk: WEBKIT_WEB_VIEW_CLASS->submit_form -> $this->vfunc_submit_form() on a PHP subclass
void vfunc_thunk_submit_form(WebKitWebView *self, WebKitFormSubmissionRequest *request) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_submit_form", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->submit_form != nullptr) native->submit_form(self, request);
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 1> args{};
  zval *argv = args.data();
  wrap(request != nullptr ? G_OBJECT(request) : nullptr, &argv[0]);
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("WebKitWebView::vfunc_submit_form");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: WEBKIT_WEB_VIEW_CLASS->submit_form (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_submit_form(gpointer klass) {
  WEBKIT_WEB_VIEW_CLASS(klass)->submit_form = vfunc_thunk_submit_form;
}

// vfunc thunk: WEBKIT_WEB_VIEW_CLASS->user_message_received -> $this->vfunc_user_message_received()
// on a PHP subclass
gboolean vfunc_thunk_user_message_received(WebKitWebView *self, WebKitUserMessage *message) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_user_message_received", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->user_message_received != nullptr ? native->user_message_received(self, message)
                                                    : FALSE;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 1> args{};
  zval *argv = args.data();
  wrap(message != nullptr ? G_OBJECT(message) : nullptr, &argv[0]);
  zval ret;
  ZVAL_UNDEF(&ret);
  gboolean result = FALSE;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = zend_is_true(&ret) ? TRUE : FALSE;
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("WebKitWebView::vfunc_user_message_received");
  zend_exception_restore();  // the parked one, previous of whatever this threw
  return result;
}

// vfunc installer: WEBKIT_WEB_VIEW_CLASS->user_message_received (called from class_init /
// iface_init of a PHP subtype)
void vfunc_install_user_message_received(gpointer klass) {
  WEBKIT_WEB_VIEW_CLASS(klass)->user_message_received = vfunc_thunk_user_message_received;
}

// vfunc thunk: WEBKIT_WEB_VIEW_CLASS->web_process_crashed -> $this->vfunc_web_process_crashed() on
// a PHP subclass
gboolean vfunc_thunk_web_process_crashed(WebKitWebView *self) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_web_process_crashed", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->web_process_crashed != nullptr ? native->web_process_crashed(self) : FALSE;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  zval ret;
  ZVAL_UNDEF(&ret);
  gboolean result = FALSE;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = zend_is_true(&ret) ? TRUE : FALSE;
  }
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("WebKitWebView::vfunc_web_process_crashed");
  zend_exception_restore();  // the parked one, previous of whatever this threw
  return result;
}

// vfunc installer: WEBKIT_WEB_VIEW_CLASS->web_process_crashed (called from class_init / iface_init
// of a PHP subtype)
void vfunc_install_web_process_crashed(gpointer klass) {
  WEBKIT_WEB_VIEW_CLASS(klass)->web_process_crashed = vfunc_thunk_web_process_crashed;
}

// vfunc thunk: WEBKIT_WEB_VIEW_CLASS->web_process_terminated ->
// $this->vfunc_web_process_terminated() on a PHP subclass
void vfunc_thunk_web_process_terminated(WebKitWebView *self,
                                        WebKitWebProcessTerminationReason reason) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_web_process_terminated", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->web_process_terminated != nullptr) native->web_process_terminated(self, reason);
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 1> args{};
  zval *argv = args.data();
  enum_to_php(WEBKIT_TYPE_WEB_PROCESS_TERMINATION_REASON, static_cast<gint>(reason), &argv[0]);
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("WebKitWebView::vfunc_web_process_terminated");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: WEBKIT_WEB_VIEW_CLASS->web_process_terminated (called from class_init /
// iface_init of a PHP subtype)
void vfunc_install_web_process_terminated(gpointer klass) {
  WEBKIT_WEB_VIEW_CLASS(klass)->web_process_terminated = vfunc_thunk_web_process_terminated;
}

}  // namespace

/**
 * Gtk4\WebKitWebView::vfunc_authenticate(WebKitAuthenticationRequest $request): bool
 *
 * Native `authenticate` (WebViewClass.authenticate): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_authenticate()` from an override.
 */
ZEND_METHOD(Gtk4_WebKitWebView, vfunc_authenticate) {
  zval *request;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(request, class_for_gtype(WEBKIT_TYPE_AUTHENTICATION_REQUEST))
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "WebKitWebView::vfunc_authenticate(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->authenticate == nullptr) {
    RETURN_FALSE;
  }
  GObject *request_o = unwrap(request, WEBKIT_TYPE_AUTHENTICATION_REQUEST);
  if (request_o == nullptr) RETURN_THROWS();
  RETURN_BOOL(klass->authenticate(self, WEBKIT_AUTHENTICATION_REQUEST(request_o)));
}

/**
 * Gtk4\WebKitWebView::vfunc_close(): void
 *
 * Native `close` (WebViewClass.close): the GTK implementation below any PHP subclass, for
 * `parent::vfunc_close()` from an override.
 */
ZEND_METHOD(Gtk4_WebKitWebView, vfunc_close) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "WebKitWebView::vfunc_close(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->close == nullptr) {
    return;
  }
  klass->close(self);
}

/**
 * Gtk4\WebKitWebView::vfunc_context_menu(WebKitContextMenu $context_menu, WebKitHitTestResult
 * $hit_test_result): bool
 *
 * Native `context_menu` (WebViewClass.context_menu): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_context_menu()` from an override.
 */
ZEND_METHOD(Gtk4_WebKitWebView, vfunc_context_menu) {
  zval *context_menu;
  zval *hit_test_result;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(context_menu, class_for_gtype(WEBKIT_TYPE_CONTEXT_MENU))
  Z_PARAM_OBJECT_OF_CLASS(hit_test_result, class_for_gtype(WEBKIT_TYPE_HIT_TEST_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "WebKitWebView::vfunc_context_menu(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->context_menu == nullptr) {
    RETURN_FALSE;
  }
  GObject *context_menu_o = unwrap(context_menu, WEBKIT_TYPE_CONTEXT_MENU);
  if (context_menu_o == nullptr) RETURN_THROWS();
  GObject *hit_test_result_o = unwrap(hit_test_result, WEBKIT_TYPE_HIT_TEST_RESULT);
  if (hit_test_result_o == nullptr) RETURN_THROWS();
  RETURN_BOOL(klass->context_menu(self, WEBKIT_CONTEXT_MENU(context_menu_o),
                                  WEBKIT_HIT_TEST_RESULT(hit_test_result_o)));
}

/**
 * Gtk4\WebKitWebView::vfunc_context_menu_dismissed(): void
 *
 * Native `context_menu_dismissed` (WebViewClass.context_menu_dismissed): the GTK implementation
 * below any PHP subclass, for `parent::vfunc_context_menu_dismissed()` from an override.
 */
ZEND_METHOD(Gtk4_WebKitWebView, vfunc_context_menu_dismissed) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "WebKitWebView::vfunc_context_menu_dismissed(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->context_menu_dismissed == nullptr) {
    return;
  }
  klass->context_menu_dismissed(self);
}

/**
 * Gtk4\WebKitWebView::vfunc_create(WebKitNavigationAction $navigation_action): GtkWidget
 *
 * Native `create` (WebViewClass.create): the GTK implementation below any PHP subclass, for
 * `parent::vfunc_create()` from an override.
 */
ZEND_METHOD(Gtk4_WebKitWebView, vfunc_create) {
  zval *navigation_action;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(navigation_action,
                          boxed_class_for_type(WEBKIT_TYPE_NAVIGATION_ACTION)->ce)
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "WebKitWebView::vfunc_create(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->create == nullptr) {
    enum_to_php(G_TYPE_NONE, 0, return_value);
    return;
  }
  gpointer navigation_action_b = unwrap_boxed(navigation_action, WEBKIT_TYPE_NAVIGATION_ACTION);
  if (navigation_action_b == nullptr) RETURN_THROWS();
  GtkWidget *phpgtk_ret =
      klass->create(self, static_cast<WebKitNavigationAction *>(navigation_action_b));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\WebKitWebView::vfunc_decide_policy(WebKitPolicyDecision $decision, WebKitPolicyDecisionType
 * $type): bool
 *
 * Native `decide_policy` (WebViewClass.decide_policy): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_decide_policy()` from an override.
 */
ZEND_METHOD(Gtk4_WebKitWebView, vfunc_decide_policy) {
  zval *decision;
  zval *type;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(decision, class_for_gtype(WEBKIT_TYPE_POLICY_DECISION))
  Z_PARAM_OBJECT_OF_CLASS(type, enum_class_for_type(WEBKIT_TYPE_POLICY_DECISION_TYPE))
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "WebKitWebView::vfunc_decide_policy(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->decide_policy == nullptr) {
    RETURN_FALSE;
  }
  GObject *decision_o = unwrap(decision, WEBKIT_TYPE_POLICY_DECISION);
  if (decision_o == nullptr) RETURN_THROWS();
  gint type_v = 0;
  if (!enum_from_php(type, WEBKIT_TYPE_POLICY_DECISION_TYPE, &type_v)) RETURN_THROWS();
  RETURN_BOOL(klass->decide_policy(self, WEBKIT_POLICY_DECISION(decision_o),
                                   static_cast<WebKitPolicyDecisionType>(type_v)));
}

/**
 * Gtk4\WebKitWebView::vfunc_enter_fullscreen(): bool
 *
 * Native `enter_fullscreen` (WebViewClass.enter_fullscreen): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_enter_fullscreen()` from an override.
 */
ZEND_METHOD(Gtk4_WebKitWebView, vfunc_enter_fullscreen) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "WebKitWebView::vfunc_enter_fullscreen(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->enter_fullscreen == nullptr) {
    RETURN_FALSE;
  }
  RETURN_BOOL(klass->enter_fullscreen(self));
}

/**
 * Gtk4\WebKitWebView::vfunc_insecure_content_detected(WebKitInsecureContentEvent $event): void
 *
 * Native `insecure_content_detected` (WebViewClass.insecure_content_detected): the GTK
 * implementation below any PHP subclass, for `parent::vfunc_insecure_content_detected()` from an
 * override.
 */
ZEND_METHOD(Gtk4_WebKitWebView, vfunc_insecure_content_detected) {
  zval *event;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(event, enum_class_for_type(WEBKIT_TYPE_INSECURE_CONTENT_EVENT))
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(spl_ce_LogicException, 0,
                            "WebKitWebView::vfunc_insecure_content_detected(): for parent:: "
                            "chaining from a PHP subclass "
                            "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->insecure_content_detected == nullptr) {
    return;
  }
  gint event_v = 0;
  if (!enum_from_php(event, WEBKIT_TYPE_INSECURE_CONTENT_EVENT, &event_v)) RETURN_THROWS();
  klass->insecure_content_detected(self, static_cast<WebKitInsecureContentEvent>(event_v));
}

/**
 * Gtk4\WebKitWebView::vfunc_leave_fullscreen(): bool
 *
 * Native `leave_fullscreen` (WebViewClass.leave_fullscreen): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_leave_fullscreen()` from an override.
 */
ZEND_METHOD(Gtk4_WebKitWebView, vfunc_leave_fullscreen) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "WebKitWebView::vfunc_leave_fullscreen(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->leave_fullscreen == nullptr) {
    RETURN_FALSE;
  }
  RETURN_BOOL(klass->leave_fullscreen(self));
}

/**
 * Gtk4\WebKitWebView::vfunc_load_changed(WebKitLoadEvent $load_event): void
 *
 * Native `load_changed` (WebViewClass.load_changed): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_load_changed()` from an override.
 */
ZEND_METHOD(Gtk4_WebKitWebView, vfunc_load_changed) {
  zval *load_event;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(load_event, enum_class_for_type(WEBKIT_TYPE_LOAD_EVENT))
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "WebKitWebView::vfunc_load_changed(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->load_changed == nullptr) {
    return;
  }
  gint load_event_v = 0;
  if (!enum_from_php(load_event, WEBKIT_TYPE_LOAD_EVENT, &load_event_v)) RETURN_THROWS();
  klass->load_changed(self, static_cast<WebKitLoadEvent>(load_event_v));
}

/**
 * Gtk4\WebKitWebView::vfunc_load_failed(WebKitLoadEvent $load_event, string $failing_uri, GError
 * $error): bool
 *
 * Native `load_failed` (WebViewClass.load_failed): the GTK implementation below any PHP subclass,
 * for `parent::vfunc_load_failed()` from an override.
 */
ZEND_METHOD(Gtk4_WebKitWebView, vfunc_load_failed) {
  zval *load_event;
  zend_string *failing_uri;
  zval *error;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_OBJECT_OF_CLASS(load_event, enum_class_for_type(WEBKIT_TYPE_LOAD_EVENT))
  Z_PARAM_STR(failing_uri)
  Z_PARAM_OBJECT_OF_CLASS(error, ce_GError)
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "WebKitWebView::vfunc_load_failed(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->load_failed == nullptr) {
    RETURN_FALSE;
  }
  gint load_event_v = 0;
  if (!enum_from_php(load_event, WEBKIT_TYPE_LOAD_EVENT, &load_event_v)) RETURN_THROWS();
  if (!phpgtk::check_utf8(failing_uri, 2)) RETURN_THROWS();
  GError *error_e = gerror_from_php(error);
  gboolean call_result = klass->load_failed(self, static_cast<WebKitLoadEvent>(load_event_v),
                                            ZSTR_VAL(failing_uri), error_e);
  if (error_e != nullptr) g_error_free(error_e);
  RETURN_BOOL(call_result);
}

/**
 * Gtk4\WebKitWebView::vfunc_load_failed_with_tls_errors(string $failing_uri, GTlsCertificate
 * $certificate, int $errors): bool
 *
 * Native `load_failed_with_tls_errors` (WebViewClass.load_failed_with_tls_errors): the GTK
 * implementation below any PHP subclass, for `parent::vfunc_load_failed_with_tls_errors()` from an
 * override.
 */
ZEND_METHOD(Gtk4_WebKitWebView, vfunc_load_failed_with_tls_errors) {
  zend_string *failing_uri;
  zval *certificate;
  zend_long errors;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_STR(failing_uri)
  Z_PARAM_OBJECT_OF_CLASS(certificate, class_for_gtype(G_TYPE_TLS_CERTIFICATE))
  Z_PARAM_LONG(errors)
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(spl_ce_LogicException, 0,
                            "WebKitWebView::vfunc_load_failed_with_tls_errors(): for parent:: "
                            "chaining from a PHP subclass "
                            "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->load_failed_with_tls_errors == nullptr) {
    RETURN_FALSE;
  }
  if (!phpgtk::check_utf8(failing_uri, 1)) RETURN_THROWS();
  GObject *certificate_o = unwrap(certificate, G_TYPE_TLS_CERTIFICATE);
  if (certificate_o == nullptr) RETURN_THROWS();
  if (!phpgtk::check_flags(G_TYPE_TLS_CERTIFICATE_FLAGS, errors, 3)) RETURN_THROWS();
  RETURN_BOOL(klass->load_failed_with_tls_errors(self, ZSTR_VAL(failing_uri),
                                                 G_TLS_CERTIFICATE(certificate_o),
                                                 static_cast<GTlsCertificateFlags>(errors)));
}

/**
 * Gtk4\WebKitWebView::vfunc_mouse_target_changed(WebKitHitTestResult $hit_test_result, int
 * $modifiers): void
 *
 * Native `mouse_target_changed` (WebViewClass.mouse_target_changed): the GTK implementation below
 * any PHP subclass, for `parent::vfunc_mouse_target_changed()` from an override.
 */
ZEND_METHOD(Gtk4_WebKitWebView, vfunc_mouse_target_changed) {
  zval *hit_test_result;
  zend_long modifiers;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(hit_test_result, class_for_gtype(WEBKIT_TYPE_HIT_TEST_RESULT))
  Z_PARAM_LONG(modifiers)
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "WebKitWebView::vfunc_mouse_target_changed(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->mouse_target_changed == nullptr) {
    return;
  }
  GObject *hit_test_result_o = unwrap(hit_test_result, WEBKIT_TYPE_HIT_TEST_RESULT);
  if (hit_test_result_o == nullptr) RETURN_THROWS();
  if (!phpgtk::check_range<guint>(modifiers, 2)) RETURN_THROWS();
  klass->mouse_target_changed(self, WEBKIT_HIT_TEST_RESULT(hit_test_result_o),
                              static_cast<guint>(modifiers));
}

/**
 * Gtk4\WebKitWebView::vfunc_permission_request(WebKitPermissionRequest $permission_request): bool
 *
 * Native `permission_request` (WebViewClass.permission_request): the GTK implementation below any
 * PHP subclass, for `parent::vfunc_permission_request()` from an override.
 */
ZEND_METHOD(Gtk4_WebKitWebView, vfunc_permission_request) {
  zval *permission_request;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(permission_request, class_for_gtype(WEBKIT_TYPE_PERMISSION_REQUEST))
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "WebKitWebView::vfunc_permission_request(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->permission_request == nullptr) {
    RETURN_FALSE;
  }
  GObject *permission_request_o = unwrap(permission_request, WEBKIT_TYPE_PERMISSION_REQUEST);
  if (permission_request_o == nullptr) RETURN_THROWS();
  RETURN_BOOL(klass->permission_request(self, WEBKIT_PERMISSION_REQUEST(permission_request_o)));
}

/**
 * Gtk4\WebKitWebView::vfunc_print(WebKitPrintOperation $print_operation): bool
 *
 * Native `print` (WebViewClass.print): the GTK implementation below any PHP subclass, for
 * `parent::vfunc_print()` from an override.
 */
ZEND_METHOD(Gtk4_WebKitWebView, vfunc_print) {
  zval *print_operation;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(print_operation, class_for_gtype(WEBKIT_TYPE_PRINT_OPERATION))
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "WebKitWebView::vfunc_print(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->print == nullptr) {
    RETURN_FALSE;
  }
  GObject *print_operation_o = unwrap(print_operation, WEBKIT_TYPE_PRINT_OPERATION);
  if (print_operation_o == nullptr) RETURN_THROWS();
  RETURN_BOOL(klass->print(self, WEBKIT_PRINT_OPERATION(print_operation_o)));
}

/**
 * Gtk4\WebKitWebView::vfunc_query_permission_state(WebKitPermissionStateQuery $query): bool
 *
 * Native `query_permission_state` (WebViewClass.query_permission_state): the GTK implementation
 * below any PHP subclass, for `parent::vfunc_query_permission_state()` from an override.
 */
ZEND_METHOD(Gtk4_WebKitWebView, vfunc_query_permission_state) {
  zval *query;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(query, boxed_class_for_type(WEBKIT_TYPE_PERMISSION_STATE_QUERY)->ce)
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "WebKitWebView::vfunc_query_permission_state(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->query_permission_state == nullptr) {
    RETURN_FALSE;
  }
  gpointer query_b = unwrap_boxed(query, WEBKIT_TYPE_PERMISSION_STATE_QUERY);
  if (query_b == nullptr) RETURN_THROWS();
  RETURN_BOOL(
      klass->query_permission_state(self, static_cast<WebKitPermissionStateQuery *>(query_b)));
}

/**
 * Gtk4\WebKitWebView::vfunc_ready_to_show(): void
 *
 * Native `ready_to_show` (WebViewClass.ready_to_show): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_ready_to_show()` from an override.
 */
ZEND_METHOD(Gtk4_WebKitWebView, vfunc_ready_to_show) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "WebKitWebView::vfunc_ready_to_show(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->ready_to_show == nullptr) {
    return;
  }
  klass->ready_to_show(self);
}

/**
 * Gtk4\WebKitWebView::vfunc_resource_load_started(WebKitWebResource $resource, WebKitURIRequest
 * $request): void
 *
 * Native `resource_load_started` (WebViewClass.resource_load_started): the GTK implementation
 * below any PHP subclass, for `parent::vfunc_resource_load_started()` from an override.
 */
ZEND_METHOD(Gtk4_WebKitWebView, vfunc_resource_load_started) {
  zval *resource;
  zval *request;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(resource, class_for_gtype(WEBKIT_TYPE_WEB_RESOURCE))
  Z_PARAM_OBJECT_OF_CLASS(request, class_for_gtype(WEBKIT_TYPE_URI_REQUEST))
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "WebKitWebView::vfunc_resource_load_started(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->resource_load_started == nullptr) {
    return;
  }
  GObject *resource_o = unwrap(resource, WEBKIT_TYPE_WEB_RESOURCE);
  if (resource_o == nullptr) RETURN_THROWS();
  GObject *request_o = unwrap(request, WEBKIT_TYPE_URI_REQUEST);
  if (request_o == nullptr) RETURN_THROWS();
  klass->resource_load_started(self, WEBKIT_WEB_RESOURCE(resource_o),
                               WEBKIT_URI_REQUEST(request_o));
}

/**
 * Gtk4\WebKitWebView::vfunc_run_as_modal(): void
 *
 * Native `run_as_modal` (WebViewClass.run_as_modal): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_run_as_modal()` from an override.
 */
ZEND_METHOD(Gtk4_WebKitWebView, vfunc_run_as_modal) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "WebKitWebView::vfunc_run_as_modal(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->run_as_modal == nullptr) {
    return;
  }
  klass->run_as_modal(self);
}

/**
 * Gtk4\WebKitWebView::vfunc_run_color_chooser(WebKitColorChooserRequest $request): bool
 *
 * Native `run_color_chooser` (WebViewClass.run_color_chooser): the GTK implementation below any
 * PHP subclass, for `parent::vfunc_run_color_chooser()` from an override.
 */
ZEND_METHOD(Gtk4_WebKitWebView, vfunc_run_color_chooser) {
  zval *request;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(request, class_for_gtype(WEBKIT_TYPE_COLOR_CHOOSER_REQUEST))
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "WebKitWebView::vfunc_run_color_chooser(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->run_color_chooser == nullptr) {
    RETURN_FALSE;
  }
  GObject *request_o = unwrap(request, WEBKIT_TYPE_COLOR_CHOOSER_REQUEST);
  if (request_o == nullptr) RETURN_THROWS();
  RETURN_BOOL(klass->run_color_chooser(self, WEBKIT_COLOR_CHOOSER_REQUEST(request_o)));
}

/**
 * Gtk4\WebKitWebView::vfunc_run_file_chooser(WebKitFileChooserRequest $request): bool
 *
 * Native `run_file_chooser` (WebViewClass.run_file_chooser): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_run_file_chooser()` from an override.
 */
ZEND_METHOD(Gtk4_WebKitWebView, vfunc_run_file_chooser) {
  zval *request;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(request, class_for_gtype(WEBKIT_TYPE_FILE_CHOOSER_REQUEST))
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "WebKitWebView::vfunc_run_file_chooser(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->run_file_chooser == nullptr) {
    RETURN_FALSE;
  }
  GObject *request_o = unwrap(request, WEBKIT_TYPE_FILE_CHOOSER_REQUEST);
  if (request_o == nullptr) RETURN_THROWS();
  RETURN_BOOL(klass->run_file_chooser(self, WEBKIT_FILE_CHOOSER_REQUEST(request_o)));
}

/**
 * Gtk4\WebKitWebView::vfunc_script_dialog(WebKitScriptDialog $dialog): bool
 *
 * Native `script_dialog` (WebViewClass.script_dialog): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_script_dialog()` from an override.
 */
ZEND_METHOD(Gtk4_WebKitWebView, vfunc_script_dialog) {
  zval *dialog;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(dialog, boxed_class_for_type(WEBKIT_TYPE_SCRIPT_DIALOG)->ce)
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "WebKitWebView::vfunc_script_dialog(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->script_dialog == nullptr) {
    RETURN_FALSE;
  }
  gpointer dialog_b = unwrap_boxed(dialog, WEBKIT_TYPE_SCRIPT_DIALOG);
  if (dialog_b == nullptr) RETURN_THROWS();
  RETURN_BOOL(klass->script_dialog(self, static_cast<WebKitScriptDialog *>(dialog_b)));
}

/**
 * Gtk4\WebKitWebView::vfunc_show_notification(WebKitNotification $notification): bool
 *
 * Native `show_notification` (WebViewClass.show_notification): the GTK implementation below any
 * PHP subclass, for `parent::vfunc_show_notification()` from an override.
 */
ZEND_METHOD(Gtk4_WebKitWebView, vfunc_show_notification) {
  zval *notification;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(notification, class_for_gtype(WEBKIT_TYPE_NOTIFICATION))
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "WebKitWebView::vfunc_show_notification(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->show_notification == nullptr) {
    RETURN_FALSE;
  }
  GObject *notification_o = unwrap(notification, WEBKIT_TYPE_NOTIFICATION);
  if (notification_o == nullptr) RETURN_THROWS();
  RETURN_BOOL(klass->show_notification(self, WEBKIT_NOTIFICATION(notification_o)));
}

/**
 * Gtk4\WebKitWebView::vfunc_show_option_menu(WebKitOptionMenu $menu, GdkRectangle $rectangle): bool
 *
 * Native `show_option_menu` (WebViewClass.show_option_menu): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_show_option_menu()` from an override.
 */
ZEND_METHOD(Gtk4_WebKitWebView, vfunc_show_option_menu) {
  zval *menu;
  zval *rectangle;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(menu, class_for_gtype(WEBKIT_TYPE_OPTION_MENU))
  Z_PARAM_OBJECT_OF_CLASS(rectangle, boxed_class_for_type(GDK_TYPE_RECTANGLE)->ce)
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "WebKitWebView::vfunc_show_option_menu(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->show_option_menu == nullptr) {
    RETURN_FALSE;
  }
  GObject *menu_o = unwrap(menu, WEBKIT_TYPE_OPTION_MENU);
  if (menu_o == nullptr) RETURN_THROWS();
  gpointer rectangle_b = unwrap_boxed(rectangle, GDK_TYPE_RECTANGLE);
  if (rectangle_b == nullptr) RETURN_THROWS();
  RETURN_BOOL(klass->show_option_menu(self, WEBKIT_OPTION_MENU(menu_o),
                                      static_cast<GdkRectangle *>(rectangle_b)));
}

/**
 * Gtk4\WebKitWebView::vfunc_submit_form(WebKitFormSubmissionRequest $request): void
 *
 * Native `submit_form` (WebViewClass.submit_form): the GTK implementation below any PHP subclass,
 * for `parent::vfunc_submit_form()` from an override.
 */
ZEND_METHOD(Gtk4_WebKitWebView, vfunc_submit_form) {
  zval *request;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(request, class_for_gtype(WEBKIT_TYPE_FORM_SUBMISSION_REQUEST))
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "WebKitWebView::vfunc_submit_form(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->submit_form == nullptr) {
    return;
  }
  GObject *request_o = unwrap(request, WEBKIT_TYPE_FORM_SUBMISSION_REQUEST);
  if (request_o == nullptr) RETURN_THROWS();
  klass->submit_form(self, WEBKIT_FORM_SUBMISSION_REQUEST(request_o));
}

/**
 * Gtk4\WebKitWebView::vfunc_user_message_received(WebKitUserMessage $message): bool
 *
 * Native `user_message_received` (WebViewClass.user_message_received): the GTK implementation
 * below any PHP subclass, for `parent::vfunc_user_message_received()` from an override.
 */
ZEND_METHOD(Gtk4_WebKitWebView, vfunc_user_message_received) {
  zval *message;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(message, class_for_gtype(WEBKIT_TYPE_USER_MESSAGE))
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "WebKitWebView::vfunc_user_message_received(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->user_message_received == nullptr) {
    RETURN_FALSE;
  }
  GObject *message_o = unwrap(message, WEBKIT_TYPE_USER_MESSAGE);
  if (message_o == nullptr) RETURN_THROWS();
  RETURN_BOOL(klass->user_message_received(self, WEBKIT_USER_MESSAGE(message_o)));
}

/**
 * Gtk4\WebKitWebView::vfunc_web_process_crashed(): bool
 *
 * Native `web_process_crashed` (WebViewClass.web_process_crashed): the GTK implementation below
 * any PHP subclass, for `parent::vfunc_web_process_crashed()` from an override.
 */
ZEND_METHOD(Gtk4_WebKitWebView, vfunc_web_process_crashed) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "WebKitWebView::vfunc_web_process_crashed(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->web_process_crashed == nullptr) {
    RETURN_FALSE;
  }
  RETURN_BOOL(klass->web_process_crashed(self));
}

/**
 * Gtk4\WebKitWebView::vfunc_web_process_terminated(WebKitWebProcessTerminationReason $reason): void
 *
 * Native `web_process_terminated` (WebViewClass.web_process_terminated): the GTK implementation
 * below any PHP subclass, for `parent::vfunc_web_process_terminated()` from an override.
 */
ZEND_METHOD(Gtk4_WebKitWebView, vfunc_web_process_terminated) {
  zval *reason;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(reason, enum_class_for_type(WEBKIT_TYPE_WEB_PROCESS_TERMINATION_REASON))
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebView *self = PHPGTK_SELF(WebKitWebView, WEBKIT_TYPE_WEB_VIEW);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "WebKitWebView::vfunc_web_process_terminated(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = WEBKIT_WEB_VIEW_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->web_process_terminated == nullptr) {
    return;
  }
  gint reason_v = 0;
  if (!enum_from_php(reason, WEBKIT_TYPE_WEB_PROCESS_TERMINATION_REASON, &reason_v))
    RETURN_THROWS();
  klass->web_process_terminated(self, static_cast<WebKitWebProcessTerminationReason>(reason_v));
}

// MINIT: the vfunc thunks of WebKitWebView (core/subtype.h).
void register_vfuncs_WebKitWebView() {
  register_vfunc(WEBKIT_TYPE_WEB_VIEW, "authenticate", vfunc_install_authenticate);
  register_vfunc(WEBKIT_TYPE_WEB_VIEW, "close", vfunc_install_close);
  register_vfunc(WEBKIT_TYPE_WEB_VIEW, "context_menu", vfunc_install_context_menu);
  register_vfunc(WEBKIT_TYPE_WEB_VIEW, "context_menu_dismissed",
                 vfunc_install_context_menu_dismissed);
  register_vfunc(WEBKIT_TYPE_WEB_VIEW, "create", vfunc_install_create);
  register_vfunc(WEBKIT_TYPE_WEB_VIEW, "decide_policy", vfunc_install_decide_policy);
  register_vfunc(WEBKIT_TYPE_WEB_VIEW, "enter_fullscreen", vfunc_install_enter_fullscreen);
  register_vfunc(WEBKIT_TYPE_WEB_VIEW, "insecure_content_detected",
                 vfunc_install_insecure_content_detected);
  register_vfunc(WEBKIT_TYPE_WEB_VIEW, "leave_fullscreen", vfunc_install_leave_fullscreen);
  register_vfunc(WEBKIT_TYPE_WEB_VIEW, "load_changed", vfunc_install_load_changed);
  register_vfunc(WEBKIT_TYPE_WEB_VIEW, "load_failed", vfunc_install_load_failed);
  register_vfunc(WEBKIT_TYPE_WEB_VIEW, "load_failed_with_tls_errors",
                 vfunc_install_load_failed_with_tls_errors);
  register_vfunc(WEBKIT_TYPE_WEB_VIEW, "mouse_target_changed", vfunc_install_mouse_target_changed);
  register_vfunc(WEBKIT_TYPE_WEB_VIEW, "permission_request", vfunc_install_permission_request);
  register_vfunc(WEBKIT_TYPE_WEB_VIEW, "print", vfunc_install_print);
  register_vfunc(WEBKIT_TYPE_WEB_VIEW, "query_permission_state",
                 vfunc_install_query_permission_state);
  register_vfunc(WEBKIT_TYPE_WEB_VIEW, "ready_to_show", vfunc_install_ready_to_show);
  register_vfunc(WEBKIT_TYPE_WEB_VIEW, "resource_load_started",
                 vfunc_install_resource_load_started);
  register_vfunc(WEBKIT_TYPE_WEB_VIEW, "run_as_modal", vfunc_install_run_as_modal);
  register_vfunc(WEBKIT_TYPE_WEB_VIEW, "run_color_chooser", vfunc_install_run_color_chooser);
  register_vfunc(WEBKIT_TYPE_WEB_VIEW, "run_file_chooser", vfunc_install_run_file_chooser);
  register_vfunc(WEBKIT_TYPE_WEB_VIEW, "script_dialog", vfunc_install_script_dialog);
  register_vfunc(WEBKIT_TYPE_WEB_VIEW, "show_notification", vfunc_install_show_notification);
  register_vfunc(WEBKIT_TYPE_WEB_VIEW, "show_option_menu", vfunc_install_show_option_menu);
  register_vfunc(WEBKIT_TYPE_WEB_VIEW, "submit_form", vfunc_install_submit_form);
  register_vfunc(WEBKIT_TYPE_WEB_VIEW, "user_message_received",
                 vfunc_install_user_message_received);
  register_vfunc(WEBKIT_TYPE_WEB_VIEW, "web_process_crashed", vfunc_install_web_process_crashed);
  register_vfunc(WEBKIT_TYPE_WEB_VIEW, "web_process_terminated",
                 vfunc_install_web_process_terminated);
}
