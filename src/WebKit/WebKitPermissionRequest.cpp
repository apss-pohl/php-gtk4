// GENERATED by gen/gir.php from WebKit.PermissionRequest - do not edit (gen/overrides,
// gen/skip.txt). Gtk4\WebKitPermissionRequest
#include "php_gtk4.h"
#include <webkit/webkit.h>
#include "core/object.h"
#include "core/subtype.h"
#include "core/error.h"

using namespace phpgtk;

/**
 * Gtk4\WebKitPermissionRequest::allow(): void
 *
 * Allow the action which triggered this request.
 */
ZEND_METHOD(Gtk4_WebKitPermissionRequest, allow) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitPermissionRequest *self =
      PHPGTK_SELF(WebKitPermissionRequest, WEBKIT_TYPE_PERMISSION_REQUEST);
  webkit_permission_request_allow(self);
}

/**
 * Gtk4\WebKitPermissionRequest::deny(): void
 *
 * Deny the action which triggered this request.
 */
ZEND_METHOD(Gtk4_WebKitPermissionRequest, deny) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitPermissionRequest *self =
      PHPGTK_SELF(WebKitPermissionRequest, WEBKIT_TYPE_PERMISSION_REQUEST);
  webkit_permission_request_deny(self);
}

// vfunc thunks and installers: file-local, installed by class_init of a PHP subtype
namespace {

// vfunc thunk: static_cast<WebKitPermissionRequestInterface *>->allow -> $this->allow() on a PHP
// subclass
void vfunc_thunk_allow(WebKitPermissionRequest *self) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "allow", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    // an interface implemented in PHP has no native implementation below it
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("WebKitPermissionRequest::allow");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: static_cast<WebKitPermissionRequestInterface *>->allow (called from class_init /
// iface_init of a PHP subtype)
void vfunc_install_allow(gpointer klass) {
  static_cast<WebKitPermissionRequestInterface *>(klass)->allow = vfunc_thunk_allow;
}

// vfunc thunk: static_cast<WebKitPermissionRequestInterface *>->deny -> $this->deny() on a PHP
// subclass
void vfunc_thunk_deny(WebKitPermissionRequest *self) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "deny", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    // an interface implemented in PHP has no native implementation below it
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("WebKitPermissionRequest::deny");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: static_cast<WebKitPermissionRequestInterface *>->deny (called from class_init /
// iface_init of a PHP subtype)
void vfunc_install_deny(gpointer klass) {
  static_cast<WebKitPermissionRequestInterface *>(klass)->deny = vfunc_thunk_deny;
}

}  // namespace

// MINIT: the vfunc thunks of WebKitPermissionRequest (core/subtype.h).
void register_vfuncs_WebKitPermissionRequest() {
  register_iface_vfunc(WEBKIT_TYPE_PERMISSION_REQUEST, "allow", vfunc_install_allow);
  register_iface_vfunc(WEBKIT_TYPE_PERMISSION_REQUEST, "deny", vfunc_install_deny);
}

/**
 * Gtk4\WebKitPermissionRequestObject::__construct()
 *
 * Never called: these handles only come from wrap().
 */
ZEND_METHOD(Gtk4_WebKitPermissionRequestObject, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
}
