// GENERATED by gen/gir.php from WebKit.FaviconDatabase - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\WebKitFaviconDatabase
#include "php_gtk4.h"
#include <webkit/webkit.h>
#include "core/object.h"
#include "core/gerror.h"
#include "core/callback.h"
#include <array>

using namespace phpgtk;

/**
 * Gtk4\WebKitFaviconDatabase::__construct()
 *
 * WebKitFaviconDatabase has no constructor in GTK: instances come from GTK, never from `new`.
 */
ZEND_METHOD(Gtk4_WebKitFaviconDatabase, __construct) {
  // Private: never called (object_init_ex() in wrap() skips constructors).
  ZEND_PARSE_PARAMETERS_NONE();
}

/**
 * Gtk4\WebKitFaviconDatabase::clear(): void
 *
 * Clears all icons from the database.
 */
ZEND_METHOD(Gtk4_WebKitFaviconDatabase, clear) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitFaviconDatabase *self = PHPGTK_SELF(WebKitFaviconDatabase, WEBKIT_TYPE_FAVICON_DATABASE);
  webkit_favicon_database_clear(self);
}

namespace {
// AsyncReadyCallback trampoline for WebKitFaviconDatabase::get_favicon(): wraps the C arguments,
// invokes the PHP callable once and releases it (async scope).
void cb_get_favicon_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\WebKitFaviconDatabase::get_favicon(string $page_uri, ?GCancellable $cancellable, ?callable
 * $callback): void
 *
 * Asynchronously obtains a favicon image.
 */
ZEND_METHOD(Gtk4_WebKitFaviconDatabase, get_favicon) {
  zend_string *page_uri;
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_STR(page_uri)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC_OR_NULL(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  WebKitFaviconDatabase *self = PHPGTK_SELF(WebKitFaviconDatabase, WEBKIT_TYPE_FAVICON_DATABASE);
  if (!phpgtk::check_utf8(page_uri, 1)) RETURN_THROWS();
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback =
      ZEND_FCI_INITIALIZED(fci_callback)
          ? callback_new(&fci_callback.function_name, "WebKitFaviconDatabase::get_favicon")
          : nullptr;
  webkit_favicon_database_get_favicon(
      self, ZSTR_VAL(page_uri), cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
      cb_callback != nullptr ? cb_get_favicon_callback : nullptr, cb_callback);
}

/**
 * Gtk4\WebKitFaviconDatabase::get_favicon_finish(GAsyncResult $result): GdkTexture
 *
 * Finishes an operation started with webkit_favicon_database_get_favicon().
 */
ZEND_METHOD(Gtk4_WebKitFaviconDatabase, get_favicon_finish) {
  zval *result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  WebKitFaviconDatabase *self = PHPGTK_SELF(WebKitFaviconDatabase, WEBKIT_TYPE_FAVICON_DATABASE);
  GObject *result_o = unwrap(result, G_TYPE_ASYNC_RESULT);
  if (result_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  GdkTexture *phpgtk_ret =
      webkit_favicon_database_get_favicon_finish(self, G_ASYNC_RESULT(result_o), &error);
  if (phpgtk_ret == nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * Gtk4\WebKitFaviconDatabase::get_favicon_uri(string $page_uri): string
 *
 * Obtains the URI of the favicon for the given $page_uri.
 */
ZEND_METHOD(Gtk4_WebKitFaviconDatabase, get_favicon_uri) {
  zend_string *page_uri;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(page_uri)
  ZEND_PARSE_PARAMETERS_END();
  WebKitFaviconDatabase *self = PHPGTK_SELF(WebKitFaviconDatabase, WEBKIT_TYPE_FAVICON_DATABASE);
  if (!phpgtk::check_utf8(page_uri, 1)) RETURN_THROWS();
  char *phpgtk_ret = webkit_favicon_database_get_favicon_uri(self, ZSTR_VAL(page_uri));
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETVAL_STRING(phpgtk_ret);
  g_free(phpgtk_ret);
}
