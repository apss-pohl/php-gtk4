// GENERATED by gen/gir.php from WebKit.WebResource - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\WebKitWebResource
#include "php_gtk4.h"
#include <webkit/webkit.h>
#include "core/object.h"
#include "core/gerror.h"
#include "core/callback.h"
#include <array>

using namespace phpgtk;

/**
 * Gtk4\WebKitWebResource::__construct()
 *
 * WebKitWebResource has no constructor in GTK: instances come from GTK, never from `new`.
 */
ZEND_METHOD(Gtk4_WebKitWebResource, __construct) {
  // Private: never called (object_init_ex() in wrap() skips constructors).
  ZEND_PARSE_PARAMETERS_NONE();
}

namespace {
// AsyncReadyCallback trampoline for WebKitWebResource::get_data(): wraps the C arguments, invokes
// the PHP callable once and releases it (async scope).
void cb_get_data_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\WebKitWebResource::get_data(?GCancellable $cancellable, ?callable $callback): void
 *
 * Asynchronously get the raw data for $resource.
 */
ZEND_METHOD(Gtk4_WebKitWebResource, get_data) {
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC_OR_NULL(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebResource *self = PHPGTK_SELF(WebKitWebResource, WEBKIT_TYPE_WEB_RESOURCE);
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback =
      ZEND_FCI_INITIALIZED(fci_callback)
          ? callback_new(&fci_callback.function_name, "WebKitWebResource::get_data")
          : nullptr;
  webkit_web_resource_get_data(
      self, cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
      cb_callback != nullptr ? cb_get_data_callback : nullptr, cb_callback);
}

/**
 * Gtk4\WebKitWebResource::get_response(): WebKitURIResponse
 *
 * Retrieves the #WebKitURIResponse of the resource load operation.
 */
ZEND_METHOD(Gtk4_WebKitWebResource, get_response) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebResource *self = PHPGTK_SELF(WebKitWebResource, WEBKIT_TYPE_WEB_RESOURCE);
  WebKitURIResponse *phpgtk_ret = webkit_web_resource_get_response(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\WebKitWebResource::get_uri(): string
 *
 * Returns the current active URI of $resource.
 */
ZEND_METHOD(Gtk4_WebKitWebResource, get_uri) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebResource *self = PHPGTK_SELF(WebKitWebResource, WEBKIT_TYPE_WEB_RESOURCE);
  const char *phpgtk_ret = webkit_web_resource_get_uri(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

/**
 * public function get_data_finish(GAsyncResult $result): string
 * Finish an asynchronous operation started with get_data(): the resource's data as bytes
 * (a page's HTML, an image's file). Throws the GError the load failed with.
 */
ZEND_METHOD(Gtk4_WebKitWebResource, get_data_finish) {
  zval *result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebResource *self = PHPGTK_SELF(WebKitWebResource, WEBKIT_TYPE_WEB_RESOURCE);
  GObject *result_o = unwrap(result, G_TYPE_ASYNC_RESULT);
  if (result_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  gsize length = 0;
  guchar *data =
      webkit_web_resource_get_data_finish(self, G_ASYNC_RESULT(result_o), &length, &error);
  if (error != nullptr) {
    g_free(data);
    throw_gerror(error);
    RETURN_THROWS();
  }
  if (data == nullptr) RETURN_EMPTY_STRING();
  RETVAL_STRINGL(reinterpret_cast<const char *>(data), length);
  g_free(data);
}
