// GENERATED by gen/gir.php from WebKit.WebsiteData - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\WebKitWebsiteData (boxed value type)
#include "php_gtk4.h"
#include <webkit/webkit.h>
#include "core/boxed.h"
#include "core/object.h"
#include <cstring>

using namespace phpgtk;

namespace {

const char *const fields[] = {nullptr};

// Boxed field reader: the public scalar fields as PHP properties.
bool read(gpointer data, const char *field, zval *rv) {
  (void)data;
  (void)field;
  (void)rv;
  return false;
}

// Boxed field writer: each field converts like a parameter of its type (core/boxed).
bool write(gpointer data, const char *field, zval *v) {
  (void)data;
  (void)field;
  (void)v;
  return false;
}

}  // namespace

/**
 * Gtk4\WebKitWebsiteData::__construct()
 *
 * WebKitWebsiteData values come from GTK, never from `new`.
 */
ZEND_METHOD(Gtk4_WebKitWebsiteData, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
}

/**
 * Gtk4\WebKitWebsiteData::get_name(): string
 *
 * Gets the name of #WebKitWebsiteData.
 */
ZEND_METHOD(Gtk4_WebKitWebsiteData, get_name) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebsiteData *self = PHPGTK_BOXED_SELF(WebKitWebsiteData);
  const char *phpgtk_ret = webkit_website_data_get_name(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

/**
 * Gtk4\WebKitWebsiteData::get_size(int $types): int
 *
 * Gets the size of the data of types $types in a #WebKitWebsiteData.
 */
ZEND_METHOD(Gtk4_WebKitWebsiteData, get_size) {
  zend_long types;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(types)
  ZEND_PARSE_PARAMETERS_END();
  WebKitWebsiteData *self = PHPGTK_BOXED_SELF(WebKitWebsiteData);
  if (!phpgtk::check_flags(WEBKIT_TYPE_WEBSITE_DATA_TYPES, types, 1)) RETURN_THROWS();
  RETURN_LONG(static_cast<zend_long>(
      webkit_website_data_get_size(self, static_cast<WebKitWebsiteDataTypes>(types))));
}

/**
 * Gtk4\WebKitWebsiteData::get_types(): int
 *
 * Gets the types of data stored in the client for a #WebKitWebsiteData.
 */
ZEND_METHOD(Gtk4_WebKitWebsiteData, get_types) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitWebsiteData *self = PHPGTK_BOXED_SELF(WebKitWebsiteData);
  RETURN_LONG(static_cast<zend_long>(webkit_website_data_get_types(self)));
}

namespace phpgtk {
// MINIT: bind the PHP class to WEBKIT_TYPE_WEBSITE_DATA with its field table.
void register_WebKitWebsiteData(zend_class_entry *ce) {
  register_boxed(BoxedClass{
      .type = WEBKIT_TYPE_WEBSITE_DATA, .ce = ce, .fields = fields, .read = read, .write = write});
}
}  // namespace phpgtk
