// GENERATED by gen/gir.php from WebKit.Credential - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\WebKitCredential (boxed value type)
#include "php_gtk4.h"
#include <webkit/webkit.h>
#include "core/boxed.h"
#include "core/object.h"
#include <cstring>
#include "core/enums.h"

using namespace phpgtk;

namespace {

const char *const fields[] = {nullptr};

// Boxed field reader: the public scalar fields as PHP properties.
bool read(gpointer data, const char *field, zval *rv) {
  (void)data;
  (void)field;
  (void)rv;
  return false;
}

// Boxed field writer: each field converts like a parameter of its type (core/boxed).
bool write(gpointer data, const char *field, zval *v) {
  (void)data;
  (void)field;
  (void)v;
  return false;
}

}  // namespace

/**
 * Gtk4\WebKitCredential::__construct(string $username, string $password,
 * WebKitCredentialPersistence $persistence)
 *
 * Create a new credential from the provided username, password and persistence mode.
 */
ZEND_METHOD(Gtk4_WebKitCredential, __construct) {
  zend_string *username;
  zend_string *password;
  zval *persistence;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_STR(username)
  Z_PARAM_STR(password)
  Z_PARAM_OBJECT_OF_CLASS(persistence, enum_class_for_type(WEBKIT_TYPE_CREDENTIAL_PERSISTENCE))
  ZEND_PARSE_PARAMETERS_END();
  if (!phpgtk::check_utf8(username, 1)) RETURN_THROWS();
  if (!phpgtk::check_utf8(password, 2)) RETURN_THROWS();
  gint persistence_v = 0;
  if (!enum_from_php(persistence, WEBKIT_TYPE_CREDENTIAL_PERSISTENCE, &persistence_v))
    RETURN_THROWS();
  gpointer obj = webkit_credential_new(ZSTR_VAL(username), ZSTR_VAL(password),
                                       static_cast<WebKitCredentialPersistence>(persistence_v));
  if (obj == nullptr) {
    zend_throw_error(nullptr, "%s(): GTK refused to create the value (see the CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  boxed_adopt(boxed_from_zval(ZEND_THIS), WEBKIT_TYPE_CREDENTIAL, obj);
}

/**
 * static Gtk4\WebKitCredential::new_for_certificate_pin(string $pin, WebKitCredentialPersistence
 * $persistence): WebKitCredential
 *
 * Create a new credential from the provided PIN and persistence mode.
 */
ZEND_METHOD(Gtk4_WebKitCredential, new_for_certificate_pin) {
  zend_string *pin;
  zval *persistence;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_STR(pin)
  Z_PARAM_OBJECT_OF_CLASS(persistence, enum_class_for_type(WEBKIT_TYPE_CREDENTIAL_PERSISTENCE))
  ZEND_PARSE_PARAMETERS_END();
  if (!phpgtk::check_utf8(pin, 1)) RETURN_THROWS();
  gint persistence_v = 0;
  if (!enum_from_php(persistence, WEBKIT_TYPE_CREDENTIAL_PERSISTENCE, &persistence_v))
    RETURN_THROWS();
  gpointer phpgtk_ret = webkit_credential_new_for_certificate_pin(
      ZSTR_VAL(pin), static_cast<WebKitCredentialPersistence>(persistence_v));
  wrap_boxed(WEBKIT_TYPE_CREDENTIAL, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(WEBKIT_TYPE_CREDENTIAL, phpgtk_ret);
}

/**
 * Gtk4\WebKitCredential::get_password(): string
 *
 * Get the password currently held by this #WebKitCredential.
 */
ZEND_METHOD(Gtk4_WebKitCredential, get_password) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitCredential *self = PHPGTK_BOXED_SELF(WebKitCredential);
  const char *phpgtk_ret = webkit_credential_get_password(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

/**
 * Gtk4\WebKitCredential::get_persistence(): WebKitCredentialPersistence
 *
 * Get the persistence mode currently held by this #WebKitCredential.
 */
ZEND_METHOD(Gtk4_WebKitCredential, get_persistence) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitCredential *self = PHPGTK_BOXED_SELF(WebKitCredential);
  enum_to_php(WEBKIT_TYPE_CREDENTIAL_PERSISTENCE, webkit_credential_get_persistence(self),
              return_value);
}

/**
 * Gtk4\WebKitCredential::get_username(): string
 *
 * Get the username currently held by this #WebKitCredential.
 */
ZEND_METHOD(Gtk4_WebKitCredential, get_username) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitCredential *self = PHPGTK_BOXED_SELF(WebKitCredential);
  const char *phpgtk_ret = webkit_credential_get_username(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

/**
 * Gtk4\WebKitCredential::has_password(): bool
 *
 * Determine whether this credential has a password stored.
 */
ZEND_METHOD(Gtk4_WebKitCredential, has_password) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitCredential *self = PHPGTK_BOXED_SELF(WebKitCredential);
  RETURN_BOOL(webkit_credential_has_password(self));
}

namespace phpgtk {
// MINIT: bind the PHP class to WEBKIT_TYPE_CREDENTIAL with its field table.
void register_WebKitCredential(zend_class_entry *ce) {
  register_boxed(BoxedClass{
      .type = WEBKIT_TYPE_CREDENTIAL, .ce = ce, .fields = fields, .read = read, .write = write});
}
}  // namespace phpgtk
