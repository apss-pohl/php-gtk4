// GENERATED by gen/gir.php from WebKit.SecurityOrigin - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\WebKitSecurityOrigin (boxed value type)
#include "php_gtk4.h"
#include <webkit/webkit.h>
#include "core/boxed.h"
#include "core/object.h"
#include <cstring>

using namespace phpgtk;

namespace {

const char *const fields[] = {nullptr};

// Boxed field reader: the public scalar fields as PHP properties.
bool read(gpointer data, const char *field, zval *rv) {
  (void)data;
  (void)field;
  (void)rv;
  return false;
}

// Boxed field writer: each field converts like a parameter of its type (core/boxed).
bool write(gpointer data, const char *field, zval *v) {
  (void)data;
  (void)field;
  (void)v;
  return false;
}

}  // namespace

/**
 * Gtk4\WebKitSecurityOrigin::__construct(string $protocol, string $host, int $port)
 *
 * Create a new security origin from the provided protocol, host and port.
 */
ZEND_METHOD(Gtk4_WebKitSecurityOrigin, __construct) {
  zend_string *protocol;
  zend_string *host;
  zend_long port;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_STR(protocol)
  Z_PARAM_STR(host)
  Z_PARAM_LONG(port)
  ZEND_PARSE_PARAMETERS_END();
  if (!phpgtk::check_utf8(protocol, 1)) RETURN_THROWS();
  if (!phpgtk::check_utf8(host, 2)) RETURN_THROWS();
  if (!phpgtk::check_range<guint16>(port, 3)) RETURN_THROWS();
  gpointer obj =
      webkit_security_origin_new(ZSTR_VAL(protocol), ZSTR_VAL(host), static_cast<guint16>(port));
  if (obj == nullptr) {
    zend_throw_error(nullptr, "%s(): GTK refused to create the value (see the CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  boxed_adopt(boxed_from_zval(ZEND_THIS), WEBKIT_TYPE_SECURITY_ORIGIN, obj);
}

/**
 * static Gtk4\WebKitSecurityOrigin::new_for_uri(string $uri): WebKitSecurityOrigin
 *
 * Create a new security origin from the provided.
 */
ZEND_METHOD(Gtk4_WebKitSecurityOrigin, new_for_uri) {
  zend_string *uri;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(uri)
  ZEND_PARSE_PARAMETERS_END();
  if (!phpgtk::check_utf8(uri, 1)) RETURN_THROWS();
  gpointer phpgtk_ret = webkit_security_origin_new_for_uri(ZSTR_VAL(uri));
  wrap_boxed(WEBKIT_TYPE_SECURITY_ORIGIN, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(WEBKIT_TYPE_SECURITY_ORIGIN, phpgtk_ret);
}

/**
 * Gtk4\WebKitSecurityOrigin::get_host(): ?string
 *
 * Gets the hostname of $origin.
 */
ZEND_METHOD(Gtk4_WebKitSecurityOrigin, get_host) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitSecurityOrigin *self = PHPGTK_BOXED_SELF(WebKitSecurityOrigin);
  PHPGTK_RETURN_STRING_OR_NULL(webkit_security_origin_get_host(self));
}

/**
 * Gtk4\WebKitSecurityOrigin::get_port(): int
 *
 * Gets the port of $origin.
 */
ZEND_METHOD(Gtk4_WebKitSecurityOrigin, get_port) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitSecurityOrigin *self = PHPGTK_BOXED_SELF(WebKitSecurityOrigin);
  RETURN_LONG(static_cast<zend_long>(webkit_security_origin_get_port(self)));
}

/**
 * Gtk4\WebKitSecurityOrigin::get_protocol(): ?string
 *
 * Gets the protocol of $origin.
 */
ZEND_METHOD(Gtk4_WebKitSecurityOrigin, get_protocol) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitSecurityOrigin *self = PHPGTK_BOXED_SELF(WebKitSecurityOrigin);
  PHPGTK_RETURN_STRING_OR_NULL(webkit_security_origin_get_protocol(self));
}

/**
 * Gtk4\WebKitSecurityOrigin::to_string(): ?string
 *
 * Gets a string representation of $origin.
 */
ZEND_METHOD(Gtk4_WebKitSecurityOrigin, to_string) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitSecurityOrigin *self = PHPGTK_BOXED_SELF(WebKitSecurityOrigin);
  char *phpgtk_ret = webkit_security_origin_to_string(self);
  if (phpgtk_ret == nullptr) RETURN_NULL();
  RETVAL_STRING(phpgtk_ret);
  g_free(phpgtk_ret);
}

namespace phpgtk {
// MINIT: bind the PHP class to WEBKIT_TYPE_SECURITY_ORIGIN with its field table.
void register_WebKitSecurityOrigin(zend_class_entry *ce) {
  register_boxed(BoxedClass{.type = WEBKIT_TYPE_SECURITY_ORIGIN,
                            .ce = ce,
                            .fields = fields,
                            .read = read,
                            .write = write});
}
}  // namespace phpgtk
