// GENERATED by gen/gir.php from WebKit.CookieManager - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\WebKitCookieManager
#include "php_gtk4.h"
#include <webkit/webkit.h>
#include "core/object.h"
#include "core/enums.h"
#include "core/collections.h"
#include "core/gerror.h"
#include "core/boxed.h"
#include "core/callback.h"
#include <array>

using namespace phpgtk;

/**
 * Gtk4\WebKitCookieManager::__construct()
 *
 * WebKitCookieManager has no constructor in GTK: instances come from GTK, never from `new`.
 */
ZEND_METHOD(Gtk4_WebKitCookieManager, __construct) {
  // Private: never called (object_init_ex() in wrap() skips constructors).
  ZEND_PARSE_PARAMETERS_NONE();
}

namespace {
// AsyncReadyCallback trampoline for WebKitCookieManager::add_cookie(): wraps the C arguments,
// invokes the PHP callable once and releases it (async scope).
void cb_add_cookie_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\WebKitCookieManager::add_cookie(SoupCookie $cookie, ?GCancellable $cancellable, ?callable
 * $callback): void
 *
 * Asynchronously add a #SoupCookie to the underlying storage.
 */
ZEND_METHOD(Gtk4_WebKitCookieManager, add_cookie) {
  zval *cookie;
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_OBJECT_OF_CLASS(cookie, boxed_class_for_type(SOUP_TYPE_COOKIE)->ce)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC_OR_NULL(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  WebKitCookieManager *self = PHPGTK_SELF(WebKitCookieManager, WEBKIT_TYPE_COOKIE_MANAGER);
  gpointer cookie_b = unwrap_boxed(cookie, SOUP_TYPE_COOKIE);
  if (cookie_b == nullptr) RETURN_THROWS();
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback =
      ZEND_FCI_INITIALIZED(fci_callback)
          ? callback_new(&fci_callback.function_name, "WebKitCookieManager::add_cookie")
          : nullptr;
  webkit_cookie_manager_add_cookie(
      self, static_cast<SoupCookie *>(cookie_b),
      cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
      cb_callback != nullptr ? cb_add_cookie_callback : nullptr, cb_callback);
}

/**
 * Gtk4\WebKitCookieManager::add_cookie_finish(GAsyncResult $result): bool
 *
 * Finish an asynchronous operation started with webkit_cookie_manager_add_cookie().
 */
ZEND_METHOD(Gtk4_WebKitCookieManager, add_cookie_finish) {
  zval *result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  WebKitCookieManager *self = PHPGTK_SELF(WebKitCookieManager, WEBKIT_TYPE_COOKIE_MANAGER);
  GObject *result_o = unwrap(result, G_TYPE_ASYNC_RESULT);
  if (result_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  const gboolean ok =
      webkit_cookie_manager_add_cookie_finish(self, G_ASYNC_RESULT(result_o), &error);
  if (!ok) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  RETURN_BOOL(ok);
}

namespace {
// AsyncReadyCallback trampoline for WebKitCookieManager::delete_cookie(): wraps the C arguments,
// invokes the PHP callable once and releases it (async scope).
void cb_delete_cookie_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\WebKitCookieManager::delete_cookie(SoupCookie $cookie, ?GCancellable $cancellable, ?callable
 * $callback): void
 *
 * Asynchronously delete a #SoupCookie from the current session.
 */
ZEND_METHOD(Gtk4_WebKitCookieManager, delete_cookie) {
  zval *cookie;
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_OBJECT_OF_CLASS(cookie, boxed_class_for_type(SOUP_TYPE_COOKIE)->ce)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC_OR_NULL(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  WebKitCookieManager *self = PHPGTK_SELF(WebKitCookieManager, WEBKIT_TYPE_COOKIE_MANAGER);
  gpointer cookie_b = unwrap_boxed(cookie, SOUP_TYPE_COOKIE);
  if (cookie_b == nullptr) RETURN_THROWS();
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback =
      ZEND_FCI_INITIALIZED(fci_callback)
          ? callback_new(&fci_callback.function_name, "WebKitCookieManager::delete_cookie")
          : nullptr;
  webkit_cookie_manager_delete_cookie(
      self, static_cast<SoupCookie *>(cookie_b),
      cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
      cb_callback != nullptr ? cb_delete_cookie_callback : nullptr, cb_callback);
}

/**
 * Gtk4\WebKitCookieManager::delete_cookie_finish(GAsyncResult $result): bool
 *
 * Finish an asynchronous operation started with webkit_cookie_manager_delete_cookie().
 */
ZEND_METHOD(Gtk4_WebKitCookieManager, delete_cookie_finish) {
  zval *result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  WebKitCookieManager *self = PHPGTK_SELF(WebKitCookieManager, WEBKIT_TYPE_COOKIE_MANAGER);
  GObject *result_o = unwrap(result, G_TYPE_ASYNC_RESULT);
  if (result_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  const gboolean ok =
      webkit_cookie_manager_delete_cookie_finish(self, G_ASYNC_RESULT(result_o), &error);
  if (!ok) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  RETURN_BOOL(ok);
}

namespace {
// AsyncReadyCallback trampoline for WebKitCookieManager::get_accept_policy(): wraps the C
// arguments, invokes the PHP callable once and releases it (async scope).
void cb_get_accept_policy_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\WebKitCookieManager::get_accept_policy(?GCancellable $cancellable, ?callable $callback):
 * void
 *
 * Asynchronously get the cookie acceptance policy of $cookie_manager.
 */
ZEND_METHOD(Gtk4_WebKitCookieManager, get_accept_policy) {
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC_OR_NULL(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  WebKitCookieManager *self = PHPGTK_SELF(WebKitCookieManager, WEBKIT_TYPE_COOKIE_MANAGER);
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback =
      ZEND_FCI_INITIALIZED(fci_callback)
          ? callback_new(&fci_callback.function_name, "WebKitCookieManager::get_accept_policy")
          : nullptr;
  webkit_cookie_manager_get_accept_policy(
      self, cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
      cb_callback != nullptr ? cb_get_accept_policy_callback : nullptr, cb_callback);
}

/**
 * Gtk4\WebKitCookieManager::get_accept_policy_finish(GAsyncResult $result):
 * WebKitCookieAcceptPolicy
 *
 * Finish an asynchronous operation started with webkit_cookie_manager_get_accept_policy().
 */
ZEND_METHOD(Gtk4_WebKitCookieManager, get_accept_policy_finish) {
  zval *result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  WebKitCookieManager *self = PHPGTK_SELF(WebKitCookieManager, WEBKIT_TYPE_COOKIE_MANAGER);
  GObject *result_o = unwrap(result, G_TYPE_ASYNC_RESULT);
  if (result_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  enum_to_php(
      WEBKIT_TYPE_COOKIE_ACCEPT_POLICY,
      webkit_cookie_manager_get_accept_policy_finish(self, G_ASYNC_RESULT(result_o), &error),
      return_value);
}

namespace {
// AsyncReadyCallback trampoline for WebKitCookieManager::get_all_cookies(): wraps the C arguments,
// invokes the PHP callable once and releases it (async scope).
void cb_get_all_cookies_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\WebKitCookieManager::get_all_cookies(?GCancellable $cancellable, ?callable $callback): void
 *
 * Asynchronously get a list of #SoupCookie from $cookie_manager.
 */
ZEND_METHOD(Gtk4_WebKitCookieManager, get_all_cookies) {
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC_OR_NULL(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  WebKitCookieManager *self = PHPGTK_SELF(WebKitCookieManager, WEBKIT_TYPE_COOKIE_MANAGER);
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback =
      ZEND_FCI_INITIALIZED(fci_callback)
          ? callback_new(&fci_callback.function_name, "WebKitCookieManager::get_all_cookies")
          : nullptr;
  webkit_cookie_manager_get_all_cookies(
      self, cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
      cb_callback != nullptr ? cb_get_all_cookies_callback : nullptr, cb_callback);
}

/**
 * Gtk4\WebKitCookieManager::get_all_cookies_finish(GAsyncResult $result): array
 *
 * Finish an asynchronous operation started with webkit_cookie_manager_get_all_cookies().
 */
ZEND_METHOD(Gtk4_WebKitCookieManager, get_all_cookies_finish) {
  zval *result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  WebKitCookieManager *self = PHPGTK_SELF(WebKitCookieManager, WEBKIT_TYPE_COOKIE_MANAGER);
  GObject *result_o = unwrap(result, G_TYPE_ASYNC_RESULT);
  if (result_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  GList *phpgtk_list =
      webkit_cookie_manager_get_all_cookies_finish(self, G_ASYNC_RESULT(result_o), &error);
  if (error != nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  glist_to_php(phpgtk_list, SOUP_TYPE_COOKIE, Transfer::Full, return_value);
}

namespace {
// AsyncReadyCallback trampoline for WebKitCookieManager::get_cookies(): wraps the C arguments,
// invokes the PHP callable once and releases it (async scope).
void cb_get_cookies_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\WebKitCookieManager::get_cookies(string $uri, ?GCancellable $cancellable, ?callable
 * $callback): void
 *
 * Asynchronously get a list of #SoupCookie from $cookie_manager.
 */
ZEND_METHOD(Gtk4_WebKitCookieManager, get_cookies) {
  zend_string *uri;
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_STR(uri)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC_OR_NULL(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  WebKitCookieManager *self = PHPGTK_SELF(WebKitCookieManager, WEBKIT_TYPE_COOKIE_MANAGER);
  if (!phpgtk::check_utf8(uri, 1)) RETURN_THROWS();
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback =
      ZEND_FCI_INITIALIZED(fci_callback)
          ? callback_new(&fci_callback.function_name, "WebKitCookieManager::get_cookies")
          : nullptr;
  webkit_cookie_manager_get_cookies(
      self, ZSTR_VAL(uri), cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
      cb_callback != nullptr ? cb_get_cookies_callback : nullptr, cb_callback);
}

/**
 * Gtk4\WebKitCookieManager::get_cookies_finish(GAsyncResult $result): array
 *
 * Finish an asynchronous operation started with webkit_cookie_manager_get_cookies().
 */
ZEND_METHOD(Gtk4_WebKitCookieManager, get_cookies_finish) {
  zval *result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  WebKitCookieManager *self = PHPGTK_SELF(WebKitCookieManager, WEBKIT_TYPE_COOKIE_MANAGER);
  GObject *result_o = unwrap(result, G_TYPE_ASYNC_RESULT);
  if (result_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  GList *phpgtk_list =
      webkit_cookie_manager_get_cookies_finish(self, G_ASYNC_RESULT(result_o), &error);
  if (error != nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  glist_to_php(phpgtk_list, SOUP_TYPE_COOKIE, Transfer::Full, return_value);
}

/**
 * Gtk4\WebKitCookieManager::replace_cookies_finish(GAsyncResult $result): bool
 *
 * Finish an asynchronous operation started with webkit_cookie_manager_replace_cookies().
 */
ZEND_METHOD(Gtk4_WebKitCookieManager, replace_cookies_finish) {
  zval *result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  WebKitCookieManager *self = PHPGTK_SELF(WebKitCookieManager, WEBKIT_TYPE_COOKIE_MANAGER);
  GObject *result_o = unwrap(result, G_TYPE_ASYNC_RESULT);
  if (result_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  const gboolean ok =
      webkit_cookie_manager_replace_cookies_finish(self, G_ASYNC_RESULT(result_o), &error);
  if (!ok) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  RETURN_BOOL(ok);
}

/**
 * Gtk4\WebKitCookieManager::set_accept_policy(WebKitCookieAcceptPolicy $policy): void
 *
 * Set the cookie acceptance policy of $cookie_manager as $policy.
 */
ZEND_METHOD(Gtk4_WebKitCookieManager, set_accept_policy) {
  zval *policy;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(policy, enum_class_for_type(WEBKIT_TYPE_COOKIE_ACCEPT_POLICY))
  ZEND_PARSE_PARAMETERS_END();
  WebKitCookieManager *self = PHPGTK_SELF(WebKitCookieManager, WEBKIT_TYPE_COOKIE_MANAGER);
  gint policy_v = 0;
  if (!enum_from_php(policy, WEBKIT_TYPE_COOKIE_ACCEPT_POLICY, &policy_v)) RETURN_THROWS();
  webkit_cookie_manager_set_accept_policy(self, static_cast<WebKitCookieAcceptPolicy>(policy_v));
}

/**
 * Gtk4\WebKitCookieManager::set_persistent_storage(string $filename, WebKitCookiePersistentStorage
 * $storage): void
 *
 * Set non-session cookies.
 */
ZEND_METHOD(Gtk4_WebKitCookieManager, set_persistent_storage) {
  zend_string *filename;
  zval *storage;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_STR(filename)
  Z_PARAM_OBJECT_OF_CLASS(storage, enum_class_for_type(WEBKIT_TYPE_COOKIE_PERSISTENT_STORAGE))
  ZEND_PARSE_PARAMETERS_END();
  WebKitCookieManager *self = PHPGTK_SELF(WebKitCookieManager, WEBKIT_TYPE_COOKIE_MANAGER);
  if (!phpgtk::check_utf8(filename, 1)) RETURN_THROWS();
  gint storage_v = 0;
  if (!enum_from_php(storage, WEBKIT_TYPE_COOKIE_PERSISTENT_STORAGE, &storage_v)) RETURN_THROWS();
  webkit_cookie_manager_set_persistent_storage(
      self, ZSTR_VAL(filename), static_cast<WebKitCookiePersistentStorage>(storage_v));
}
