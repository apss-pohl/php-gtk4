// GENERATED by gen/gir.php from WebKit.FeatureList - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\WebKitFeatureList (boxed value type)
#include "php_gtk4.h"
#include <webkit/webkit.h>
#include "core/boxed.h"
#include "core/object.h"
#include <cstring>

using namespace phpgtk;

namespace {

const char *const fields[] = {nullptr};

// Boxed field reader: the public scalar fields as PHP properties.
bool read(gpointer data, const char *field, zval *rv) {
  (void)data;
  (void)field;
  (void)rv;
  return false;
}

// Boxed field writer: each field converts like a parameter of its type (core/boxed).
bool write(gpointer data, const char *field, zval *v) {
  (void)data;
  (void)field;
  (void)v;
  return false;
}

}  // namespace

/**
 * Gtk4\WebKitFeatureList::__construct()
 *
 * WebKitFeatureList values come from GTK, never from `new`.
 */
ZEND_METHOD(Gtk4_WebKitFeatureList, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
}

/**
 * Gtk4\WebKitFeatureList::get(int $index): WebKitFeature
 *
 * Gets a feature given its index.
 */
ZEND_METHOD(Gtk4_WebKitFeatureList, get) {
  zend_long index;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(index)
  ZEND_PARSE_PARAMETERS_END();
  WebKitFeatureList *self = PHPGTK_BOXED_SELF(WebKitFeatureList);
  if (!phpgtk::check_range<gsize>(index, 1)) RETURN_THROWS();
  const bool precondition_0 = static_cast<gsize>(index) < webkit_feature_list_get_length(self);
  if (!precondition_0) {
    zend_argument_value_error(1, "must be below the number of features (get_length())");
    RETURN_THROWS();
  }
  WebKitFeature *phpgtk_ret = webkit_feature_list_get(self, static_cast<gsize>(index));
  wrap_boxed(WEBKIT_TYPE_FEATURE, phpgtk_ret, return_value);
}

/**
 * Gtk4\WebKitFeatureList::get_length(): int
 *
 * Gets the number of elements in the feature list.
 */
ZEND_METHOD(Gtk4_WebKitFeatureList, get_length) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitFeatureList *self = PHPGTK_BOXED_SELF(WebKitFeatureList);
  RETURN_LONG(static_cast<zend_long>(webkit_feature_list_get_length(self)));
}

namespace phpgtk {
// MINIT: bind the PHP class to WEBKIT_TYPE_FEATURE_LIST with its field table.
void register_WebKitFeatureList(zend_class_entry *ce) {
  register_boxed(BoxedClass{
      .type = WEBKIT_TYPE_FEATURE_LIST, .ce = ce, .fields = fields, .read = read, .write = write});
}
}  // namespace phpgtk
