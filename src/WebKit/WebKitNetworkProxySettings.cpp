// GENERATED by gen/gir.php from WebKit.NetworkProxySettings - do not edit (gen/overrides,
// gen/skip.txt). Gtk4\WebKitNetworkProxySettings (boxed value type)
#include "php_gtk4.h"
#include <webkit/webkit.h>
#include "core/boxed.h"
#include "core/object.h"
#include <cstring>
#include "core/collections.h"

using namespace phpgtk;

namespace {

const char *const fields[] = {nullptr};

// Boxed field reader: the public scalar fields as PHP properties.
bool read(gpointer data, const char *field, zval *rv) {
  (void)data;
  (void)field;
  (void)rv;
  return false;
}

// Boxed field writer: each field converts like a parameter of its type (core/boxed).
bool write(gpointer data, const char *field, zval *v) {
  (void)data;
  (void)field;
  (void)v;
  return false;
}

}  // namespace

/**
 * Gtk4\WebKitNetworkProxySettings::__construct(?string $default_proxy_uri = null, ?array
 * $ignore_hosts = null)
 *
 * Create a new #WebKitNetworkProxySettings with the given $default_proxy_uri and $ignore_hosts.
 */
ZEND_METHOD(Gtk4_WebKitNetworkProxySettings, __construct) {
  zend_string *default_proxy_uri = nullptr;
  zval *ignore_hosts = nullptr;
  ZEND_PARSE_PARAMETERS_START(0, 2)
  Z_PARAM_OPTIONAL
  Z_PARAM_STR_OR_NULL(default_proxy_uri)
  Z_PARAM_ARRAY_OR_NULL(ignore_hosts)
  ZEND_PARSE_PARAMETERS_END();
  if (default_proxy_uri != nullptr && !phpgtk::check_utf8(default_proxy_uri, 1)) RETURN_THROWS();
  char **ignore_hosts_v = nullptr;
  if (ignore_hosts != nullptr) {
    ignore_hosts_v = strv_from_php(ignore_hosts);
    if (ignore_hosts_v == nullptr) RETURN_THROWS();
  }
  gpointer obj = webkit_network_proxy_settings_new(
      default_proxy_uri != nullptr ? ZSTR_VAL(default_proxy_uri) : nullptr,
      const_cast<const char **>(ignore_hosts_v));
  if (ignore_hosts_v != nullptr) g_strfreev(ignore_hosts_v);
  if (obj == nullptr) {
    zend_throw_error(nullptr, "%s(): GTK refused to create the value (see the CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  boxed_adopt(boxed_from_zval(ZEND_THIS), WEBKIT_TYPE_NETWORK_NETWORK_PROXY_SETTINGS, obj);
}

/**
 * Gtk4\WebKitNetworkProxySettings::add_proxy_for_scheme(string $scheme, string $proxy_uri): void
 *
 * Adds a URI-scheme-specific proxy.
 */
ZEND_METHOD(Gtk4_WebKitNetworkProxySettings, add_proxy_for_scheme) {
  zend_string *scheme;
  zend_string *proxy_uri;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_STR(scheme)
  Z_PARAM_STR(proxy_uri)
  ZEND_PARSE_PARAMETERS_END();
  WebKitNetworkProxySettings *self = PHPGTK_BOXED_SELF(WebKitNetworkProxySettings);
  if (!phpgtk::check_utf8(scheme, 1)) RETURN_THROWS();
  if (!phpgtk::check_utf8(proxy_uri, 2)) RETURN_THROWS();
  if (!(g_uri_is_valid(ZSTR_VAL(proxy_uri), G_URI_FLAGS_NONE, nullptr))) {
    zend_argument_value_error(2, "must be a valid URI");
    RETURN_THROWS();
  }
  webkit_network_proxy_settings_add_proxy_for_scheme(self, ZSTR_VAL(scheme), ZSTR_VAL(proxy_uri));
}

namespace phpgtk {
// MINIT: bind the PHP class to WEBKIT_TYPE_NETWORK_NETWORK_PROXY_SETTINGS with its field table.
void register_WebKitNetworkProxySettings(zend_class_entry *ce) {
  register_boxed(BoxedClass{.type = WEBKIT_TYPE_NETWORK_NETWORK_PROXY_SETTINGS,
                            .ce = ce,
                            .fields = fields,
                            .read = read,
                            .write = write});
}
}  // namespace phpgtk
