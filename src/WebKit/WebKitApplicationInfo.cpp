// GENERATED by gen/gir.php from WebKit.ApplicationInfo - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\WebKitApplicationInfo (boxed value type)
#include "php_gtk4.h"
#include <webkit/webkit.h>
#include "core/boxed.h"
#include "core/object.h"
#include <cstring>

using namespace phpgtk;

namespace {

const char *const fields[] = {nullptr};

// Boxed field reader: the public scalar fields as PHP properties.
bool read(gpointer data, const char *field, zval *rv) {
  (void)data;
  (void)field;
  (void)rv;
  return false;
}

// Boxed field writer: each field converts like a parameter of its type (core/boxed).
bool write(gpointer data, const char *field, zval *v) {
  (void)data;
  (void)field;
  (void)v;
  return false;
}

}  // namespace

/**
 * Gtk4\WebKitApplicationInfo::__construct()
 *
 * Creates a new #WebKitApplicationInfo
 */
ZEND_METHOD(Gtk4_WebKitApplicationInfo, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  gpointer obj = webkit_application_info_new();
  if (obj == nullptr) {
    zend_throw_error(nullptr, "%s(): GTK refused to create the value (see the CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  boxed_adopt(boxed_from_zval(ZEND_THIS), WEBKIT_TYPE_APPLICATION_INFO, obj);
}

/**
 * Gtk4\WebKitApplicationInfo::get_name(): string
 *
 * Get the name of the application.
 */
ZEND_METHOD(Gtk4_WebKitApplicationInfo, get_name) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitApplicationInfo *self = PHPGTK_BOXED_SELF(WebKitApplicationInfo);
  const char *phpgtk_ret = webkit_application_info_get_name(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

/**
 * Gtk4\WebKitApplicationInfo::get_version(): array
 *
 * Get the application version previously set with webkit_application_info_set_version().
 */
ZEND_METHOD(Gtk4_WebKitApplicationInfo, get_version) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitApplicationInfo *self = PHPGTK_BOXED_SELF(WebKitApplicationInfo);
  guint64 major = 0;
  guint64 minor = 0;
  guint64 micro = 0;
  webkit_application_info_get_version(self, &major, &minor, &micro);
  array_init_size(return_value, 3);
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(major));
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(minor));
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(micro));
    add_next_index_zval(return_value, &item);
  }
}

/**
 * Gtk4\WebKitApplicationInfo::set_name(string $name): void
 *
 * Set the name of the application.
 */
ZEND_METHOD(Gtk4_WebKitApplicationInfo, set_name) {
  zend_string *name;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(name)
  ZEND_PARSE_PARAMETERS_END();
  WebKitApplicationInfo *self = PHPGTK_BOXED_SELF(WebKitApplicationInfo);
  if (!phpgtk::check_utf8(name, 1)) RETURN_THROWS();
  webkit_application_info_set_name(self, ZSTR_VAL(name));
}

/**
 * Gtk4\WebKitApplicationInfo::set_version(int $major, int $minor, int $micro): void
 *
 * Set the application version.
 */
ZEND_METHOD(Gtk4_WebKitApplicationInfo, set_version) {
  zend_long major;
  zend_long minor;
  zend_long micro;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_LONG(major)
  Z_PARAM_LONG(minor)
  Z_PARAM_LONG(micro)
  ZEND_PARSE_PARAMETERS_END();
  WebKitApplicationInfo *self = PHPGTK_BOXED_SELF(WebKitApplicationInfo);
  if (!phpgtk::check_range<guint64>(major, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<guint64>(minor, 2)) RETURN_THROWS();
  if (!phpgtk::check_range<guint64>(micro, 3)) RETURN_THROWS();
  webkit_application_info_set_version(self, static_cast<guint64>(major),
                                      static_cast<guint64>(minor), static_cast<guint64>(micro));
}

namespace phpgtk {
// MINIT: bind the PHP class to WEBKIT_TYPE_APPLICATION_INFO with its field table.
void register_WebKitApplicationInfo(zend_class_entry *ce) {
  register_boxed(BoxedClass{.type = WEBKIT_TYPE_APPLICATION_INFO,
                            .ce = ce,
                            .fields = fields,
                            .read = read,
                            .write = write});
}
}  // namespace phpgtk
