// GENERATED by gen/gir.php from WebKit.NetworkSession - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\WebKitNetworkSession
#include "php_gtk4.h"
#include <webkit/webkit.h>
#include "core/object.h"
#include "core/enums.h"
#include "core/collections.h"
#include "core/gerror.h"
#include "core/boxed.h"
#include "core/subtype.h"
#include "core/callback.h"
#include <array>

using namespace phpgtk;

/**
 * Gtk4\WebKitNetworkSession::__construct(?string $data_directory = null, ?string $cache_directory =
 * null)
 *
 * Creates a new #WebKitNetworkSession with a persistent #WebKitWebsiteDataManager. The parameters
 * $data_directory and $cache_directory will be used as construct properties of the
 * #WebKitWebsiteDataManager of the network session. Note that if `null` is passed, the default
 * directory will be passed to #WebKitWebsiteDataManager so that
 * webkit_website_data_manager_get_base_data_directory() and
 * webkit_website_data_manager_get_base_cache_directory() always return a value for non ephemeral
 * sessions.
 */
ZEND_METHOD(Gtk4_WebKitNetworkSession, __construct) {
  zend_string *data_directory = nullptr;
  zend_string *cache_directory = nullptr;
  ZEND_PARSE_PARAMETERS_START(0, 2)
  Z_PARAM_OPTIONAL
  Z_PARAM_STR_OR_NULL(data_directory)
  Z_PARAM_STR_OR_NULL(cache_directory)
  ZEND_PARSE_PARAMETERS_END();
  if (data_directory != nullptr && !phpgtk::check_utf8(data_directory, 1)) RETURN_THROWS();
  if (cache_directory != nullptr && !phpgtk::check_utf8(cache_directory, 2)) RETURN_THROWS();
  GObject *obj = subtype_new(
      ZEND_THIS, "data-directory", data_directory != nullptr ? ZSTR_VAL(data_directory) : nullptr,
      "cache-directory", cache_directory != nullptr ? ZSTR_VAL(cache_directory) : nullptr, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(webkit_network_session_new(
        data_directory != nullptr ? ZSTR_VAL(data_directory) : nullptr,
        cache_directory != nullptr ? ZSTR_VAL(cache_directory) : nullptr));
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * static Gtk4\WebKitNetworkSession::new_ephemeral(): WebKitNetworkSession
 *
 * Creates a new #WebKitNetworkSession with an ephemeral #WebKitWebsiteDataManager.
 */
ZEND_METHOD(Gtk4_WebKitNetworkSession, new_ephemeral) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = G_OBJECT(webkit_network_session_new_ephemeral());
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * static Gtk4\WebKitNetworkSession::get_default(): WebKitNetworkSession
 *
 * Get the default network session. The default network session is created using
 * webkit_network_session_new() and passing `null` as data and cache directories.
 */
ZEND_METHOD(Gtk4_WebKitNetworkSession, get_default) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitNetworkSession *phpgtk_ret = webkit_network_session_get_default();
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * static Gtk4\WebKitNetworkSession::set_memory_pressure_settings(WebKitMemoryPressureSettings
 * $settings): void
 *
 * Sets $settings as the #WebKitMemoryPressureSettings.
 */
ZEND_METHOD(Gtk4_WebKitNetworkSession, set_memory_pressure_settings) {
  zval *settings;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(settings, boxed_class_for_type(WEBKIT_TYPE_MEMORY_PRESSURE_SETTINGS)->ce)
  ZEND_PARSE_PARAMETERS_END();
  gpointer settings_b = unwrap_boxed(settings, WEBKIT_TYPE_MEMORY_PRESSURE_SETTINGS);
  if (settings_b == nullptr) RETURN_THROWS();
  webkit_network_session_set_memory_pressure_settings(
      static_cast<WebKitMemoryPressureSettings *>(settings_b));
}

/**
 * Gtk4\WebKitNetworkSession::download_uri(string $uri): WebKitDownload
 *
 * Requests downloading of the specified URI string.
 */
ZEND_METHOD(Gtk4_WebKitNetworkSession, download_uri) {
  zend_string *uri;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(uri)
  ZEND_PARSE_PARAMETERS_END();
  WebKitNetworkSession *self = PHPGTK_SELF(WebKitNetworkSession, WEBKIT_TYPE_NETWORK_SESSION);
  if (!phpgtk::check_utf8(uri, 1)) RETURN_THROWS();
  WebKitDownload *phpgtk_ret = webkit_network_session_download_uri(self, ZSTR_VAL(uri));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * Gtk4\WebKitNetworkSession::get_cookie_manager(): WebKitCookieManager
 *
 * Get the #WebKitCookieManager of $session.
 */
ZEND_METHOD(Gtk4_WebKitNetworkSession, get_cookie_manager) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitNetworkSession *self = PHPGTK_SELF(WebKitNetworkSession, WEBKIT_TYPE_NETWORK_SESSION);
  WebKitCookieManager *phpgtk_ret = webkit_network_session_get_cookie_manager(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\WebKitNetworkSession::get_itp_enabled(): bool
 *
 * Get whether Intelligent Tracking Prevention (ITP) is enabled or not.
 */
ZEND_METHOD(Gtk4_WebKitNetworkSession, get_itp_enabled) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitNetworkSession *self = PHPGTK_SELF(WebKitNetworkSession, WEBKIT_TYPE_NETWORK_SESSION);
  RETURN_BOOL(webkit_network_session_get_itp_enabled(self));
}

namespace {
// AsyncReadyCallback trampoline for WebKitNetworkSession::get_itp_summary(): wraps the C arguments,
// invokes the PHP callable once and releases it (async scope).
void cb_get_itp_summary_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\WebKitNetworkSession::get_itp_summary(?GCancellable $cancellable, ?callable $callback): void
 *
 * Asynchronously get the list of #WebKitITPThirdParty seen for $session.
 */
ZEND_METHOD(Gtk4_WebKitNetworkSession, get_itp_summary) {
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC_OR_NULL(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  WebKitNetworkSession *self = PHPGTK_SELF(WebKitNetworkSession, WEBKIT_TYPE_NETWORK_SESSION);
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback =
      ZEND_FCI_INITIALIZED(fci_callback)
          ? callback_new(&fci_callback.function_name, "WebKitNetworkSession::get_itp_summary")
          : nullptr;
  webkit_network_session_get_itp_summary(
      self, cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
      cb_callback != nullptr ? cb_get_itp_summary_callback : nullptr, cb_callback);
}

/**
 * Gtk4\WebKitNetworkSession::get_itp_summary_finish(GAsyncResult $result): array
 *
 * Finish an asynchronous operation started with webkit_network_session_get_itp_summary().
 */
ZEND_METHOD(Gtk4_WebKitNetworkSession, get_itp_summary_finish) {
  zval *result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  WebKitNetworkSession *self = PHPGTK_SELF(WebKitNetworkSession, WEBKIT_TYPE_NETWORK_SESSION);
  GObject *result_o = unwrap(result, G_TYPE_ASYNC_RESULT);
  if (result_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  GList *phpgtk_list =
      webkit_network_session_get_itp_summary_finish(self, G_ASYNC_RESULT(result_o), &error);
  if (error != nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  glist_to_php(phpgtk_list, WEBKIT_TYPE_ITP_THIRD_PARTY, Transfer::Full, return_value);
}

/**
 * Gtk4\WebKitNetworkSession::get_persistent_credential_storage_enabled(): bool
 *
 * Get whether persistent credential storage is enabled or not.
 */
ZEND_METHOD(Gtk4_WebKitNetworkSession, get_persistent_credential_storage_enabled) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitNetworkSession *self = PHPGTK_SELF(WebKitNetworkSession, WEBKIT_TYPE_NETWORK_SESSION);
  RETURN_BOOL(webkit_network_session_get_persistent_credential_storage_enabled(self));
}

/**
 * Gtk4\WebKitNetworkSession::get_tls_errors_policy(): WebKitTLSErrorsPolicy
 *
 * Get the TLS errors policy of $session.
 */
ZEND_METHOD(Gtk4_WebKitNetworkSession, get_tls_errors_policy) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitNetworkSession *self = PHPGTK_SELF(WebKitNetworkSession, WEBKIT_TYPE_NETWORK_SESSION);
  enum_to_php(WEBKIT_TYPE_TLS_ERRORS_POLICY, webkit_network_session_get_tls_errors_policy(self),
              return_value);
}

/**
 * Gtk4\WebKitNetworkSession::get_website_data_manager(): WebKitWebsiteDataManager
 *
 * Get the #WebKitWebsiteDataManager of $session.
 */
ZEND_METHOD(Gtk4_WebKitNetworkSession, get_website_data_manager) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitNetworkSession *self = PHPGTK_SELF(WebKitNetworkSession, WEBKIT_TYPE_NETWORK_SESSION);
  WebKitWebsiteDataManager *phpgtk_ret = webkit_network_session_get_website_data_manager(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\WebKitNetworkSession::is_ephemeral(): bool
 *
 * Get whether $session is ephemeral. A #WebKitNetworkSession is ephemeral when its
 * #WebKitWebsiteDataManager is ephemeral. See #WebKitWebsiteDataManager:is-ephemeral for more
 * details.
 */
ZEND_METHOD(Gtk4_WebKitNetworkSession, is_ephemeral) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitNetworkSession *self = PHPGTK_SELF(WebKitNetworkSession, WEBKIT_TYPE_NETWORK_SESSION);
  RETURN_BOOL(webkit_network_session_is_ephemeral(self));
}

/**
 * Gtk4\WebKitNetworkSession::prefetch_dns(string $hostname): void
 *
 * Resolve the domain name of the given $hostname in advance, so that if a URI of $hostname is
 * requested the load will be performed more quickly.
 */
ZEND_METHOD(Gtk4_WebKitNetworkSession, prefetch_dns) {
  zend_string *hostname;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(hostname)
  ZEND_PARSE_PARAMETERS_END();
  WebKitNetworkSession *self = PHPGTK_SELF(WebKitNetworkSession, WEBKIT_TYPE_NETWORK_SESSION);
  if (!phpgtk::check_utf8(hostname, 1)) RETURN_THROWS();
  webkit_network_session_prefetch_dns(self, ZSTR_VAL(hostname));
}

/**
 * Gtk4\WebKitNetworkSession::set_itp_enabled(bool $enabled): void
 *
 * Enable or disable Intelligent Tracking Prevention (ITP).
 */
ZEND_METHOD(Gtk4_WebKitNetworkSession, set_itp_enabled) {
  bool enabled;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(enabled)
  ZEND_PARSE_PARAMETERS_END();
  WebKitNetworkSession *self = PHPGTK_SELF(WebKitNetworkSession, WEBKIT_TYPE_NETWORK_SESSION);
  webkit_network_session_set_itp_enabled(self, enabled);
}

/**
 * Gtk4\WebKitNetworkSession::set_persistent_credential_storage_enabled(bool $enabled): void
 *
 * Enable or disable persistent credential storage.
 */
ZEND_METHOD(Gtk4_WebKitNetworkSession, set_persistent_credential_storage_enabled) {
  bool enabled;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(enabled)
  ZEND_PARSE_PARAMETERS_END();
  WebKitNetworkSession *self = PHPGTK_SELF(WebKitNetworkSession, WEBKIT_TYPE_NETWORK_SESSION);
  webkit_network_session_set_persistent_credential_storage_enabled(self, enabled);
}

/**
 * Gtk4\WebKitNetworkSession::set_proxy_settings(WebKitNetworkProxyMode $proxy_mode,
 * ?WebKitNetworkProxySettings $proxy_settings): void
 *
 * Set the network proxy settings to be used by connections started in $session session.
 */
ZEND_METHOD(Gtk4_WebKitNetworkSession, set_proxy_settings) {
  zval *proxy_mode;
  zval *proxy_settings = nullptr;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(proxy_mode, enum_class_for_type(WEBKIT_TYPE_NETWORK_PROXY_MODE))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(
      proxy_settings, boxed_class_for_type(WEBKIT_TYPE_NETWORK_NETWORK_PROXY_SETTINGS)->ce)
  ZEND_PARSE_PARAMETERS_END();
  WebKitNetworkSession *self = PHPGTK_SELF(WebKitNetworkSession, WEBKIT_TYPE_NETWORK_SESSION);
  gint proxy_mode_v = 0;
  if (!enum_from_php(proxy_mode, WEBKIT_TYPE_NETWORK_PROXY_MODE, &proxy_mode_v)) RETURN_THROWS();
  gpointer proxy_settings_b = nullptr;
  if (proxy_settings != nullptr) {
    proxy_settings_b = unwrap_boxed(proxy_settings, WEBKIT_TYPE_NETWORK_NETWORK_PROXY_SETTINGS);
    if (proxy_settings_b == nullptr) RETURN_THROWS();
  }
  webkit_network_session_set_proxy_settings(
      self, static_cast<WebKitNetworkProxyMode>(proxy_mode_v),
      static_cast<WebKitNetworkProxySettings *>(proxy_settings_b));
}

/**
 * Gtk4\WebKitNetworkSession::set_tls_errors_policy(WebKitTLSErrorsPolicy $policy): void
 *
 * Set the TLS errors policy of $session as $policy.
 */
ZEND_METHOD(Gtk4_WebKitNetworkSession, set_tls_errors_policy) {
  zval *policy;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(policy, enum_class_for_type(WEBKIT_TYPE_TLS_ERRORS_POLICY))
  ZEND_PARSE_PARAMETERS_END();
  WebKitNetworkSession *self = PHPGTK_SELF(WebKitNetworkSession, WEBKIT_TYPE_NETWORK_SESSION);
  gint policy_v = 0;
  if (!enum_from_php(policy, WEBKIT_TYPE_TLS_ERRORS_POLICY, &policy_v)) RETURN_THROWS();
  webkit_network_session_set_tls_errors_policy(self, static_cast<WebKitTLSErrorsPolicy>(policy_v));
}
