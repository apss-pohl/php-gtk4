// GENERATED by gen/gir.php from WebKit.UserContentFilter - do not edit (gen/overrides,
// gen/skip.txt). Gtk4\WebKitUserContentFilter (boxed value type)
#include "php_gtk4.h"
#include <webkit/webkit.h>
#include "core/boxed.h"
#include "core/object.h"
#include <cstring>

using namespace phpgtk;

namespace {

const char *const fields[] = {nullptr};

// Boxed field reader: the public scalar fields as PHP properties.
bool read(gpointer data, const char *field, zval *rv) {
  (void)data;
  (void)field;
  (void)rv;
  return false;
}

// Boxed field writer: each field converts like a parameter of its type (core/boxed).
bool write(gpointer data, const char *field, zval *v) {
  (void)data;
  (void)field;
  (void)v;
  return false;
}

}  // namespace

/**
 * Gtk4\WebKitUserContentFilter::__construct()
 *
 * WebKitUserContentFilter values come from GTK, never from `new`.
 */
ZEND_METHOD(Gtk4_WebKitUserContentFilter, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
}

/**
 * Gtk4\WebKitUserContentFilter::get_identifier(): string
 *
 * Obtain the identifier previously used to save the $user_content_filter.
 */
ZEND_METHOD(Gtk4_WebKitUserContentFilter, get_identifier) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitUserContentFilter *self = PHPGTK_BOXED_SELF(WebKitUserContentFilter);
  const char *phpgtk_ret = webkit_user_content_filter_get_identifier(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

namespace phpgtk {
// MINIT: bind the PHP class to WEBKIT_TYPE_USER_CONTENT_FILTER with its field table.
void register_WebKitUserContentFilter(zend_class_entry *ce) {
  register_boxed(BoxedClass{.type = WEBKIT_TYPE_USER_CONTENT_FILTER,
                            .ce = ce,
                            .fields = fields,
                            .read = read,
                            .write = write});
}
}  // namespace phpgtk
