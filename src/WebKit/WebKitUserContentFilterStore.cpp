// GENERATED by gen/gir.php from WebKit.UserContentFilterStore - do not edit (gen/overrides,
// gen/skip.txt). Gtk4\WebKitUserContentFilterStore
#include "php_gtk4.h"
#include <webkit/webkit.h>
#include "core/object.h"
#include "core/collections.h"
#include "core/gerror.h"
#include "core/boxed.h"
#include "core/callback.h"
#include <array>

using namespace phpgtk;

/**
 * Gtk4\WebKitUserContentFilterStore::__construct(string $storage_path)
 *
 * Create a new #WebKitUserContentFilterStore to manipulate filters stored at $storage_path.
 */
ZEND_METHOD(Gtk4_WebKitUserContentFilterStore, __construct) {
  zend_string *storage_path;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(storage_path)
  ZEND_PARSE_PARAMETERS_END();
  if (!phpgtk::check_utf8(storage_path, 1)) RETURN_THROWS();
  GObject *obj = G_OBJECT(webkit_user_content_filter_store_new(ZSTR_VAL(storage_path)));
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

namespace {
// AsyncReadyCallback trampoline for WebKitUserContentFilterStore::fetch_identifiers(): wraps the C
// arguments, invokes the PHP callable once and releases it (async scope).
void cb_fetch_identifiers_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\WebKitUserContentFilterStore::fetch_identifiers(?GCancellable $cancellable, callable
 * $callback): void
 *
 * Asynchronously retrieve a list of the identifiers for all the stored filters.
 */
ZEND_METHOD(Gtk4_WebKitUserContentFilterStore, fetch_identifiers) {
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  WebKitUserContentFilterStore *self =
      PHPGTK_SELF(WebKitUserContentFilterStore, WEBKIT_TYPE_USER_CONTENT_FILTER_STORE);
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback =
      callback_new(&fci_callback.function_name, "WebKitUserContentFilterStore::fetch_identifiers");
  webkit_user_content_filter_store_fetch_identifiers(
      self, cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
      cb_fetch_identifiers_callback, cb_callback);
}

/**
 * Gtk4\WebKitUserContentFilterStore::fetch_identifiers_finish(GAsyncResult $result): array
 *
 * Finishes an asynchronous fetch of the list of stored filters.
 */
ZEND_METHOD(Gtk4_WebKitUserContentFilterStore, fetch_identifiers_finish) {
  zval *result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  WebKitUserContentFilterStore *self =
      PHPGTK_SELF(WebKitUserContentFilterStore, WEBKIT_TYPE_USER_CONTENT_FILTER_STORE);
  GObject *result_o = unwrap(result, G_TYPE_ASYNC_RESULT);
  if (result_o == nullptr) RETURN_THROWS();
  strv_to_php(
      webkit_user_content_filter_store_fetch_identifiers_finish(self, G_ASYNC_RESULT(result_o)),
      Transfer::Full, return_value);
}

/**
 * Gtk4\WebKitUserContentFilterStore::get_path(): string
 *
 * Gets the storage path for user content filters.
 */
ZEND_METHOD(Gtk4_WebKitUserContentFilterStore, get_path) {
  ZEND_PARSE_PARAMETERS_NONE();
  WebKitUserContentFilterStore *self =
      PHPGTK_SELF(WebKitUserContentFilterStore, WEBKIT_TYPE_USER_CONTENT_FILTER_STORE);
  const char *phpgtk_ret = webkit_user_content_filter_store_get_path(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

namespace {
// AsyncReadyCallback trampoline for WebKitUserContentFilterStore::load(): wraps the C arguments,
// invokes the PHP callable once and releases it (async scope).
void cb_load_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\WebKitUserContentFilterStore::load(string $identifier, ?GCancellable $cancellable, callable
 * $callback): void
 *
 * Asynchronously load a content filter given its $identifier.
 */
ZEND_METHOD(Gtk4_WebKitUserContentFilterStore, load) {
  zend_string *identifier;
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_STR(identifier)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  WebKitUserContentFilterStore *self =
      PHPGTK_SELF(WebKitUserContentFilterStore, WEBKIT_TYPE_USER_CONTENT_FILTER_STORE);
  if (!phpgtk::check_utf8(identifier, 1)) RETURN_THROWS();
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback =
      callback_new(&fci_callback.function_name, "WebKitUserContentFilterStore::load");
  webkit_user_content_filter_store_load(
      self, ZSTR_VAL(identifier), cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
      cb_load_callback, cb_callback);
}

/**
 * Gtk4\WebKitUserContentFilterStore::load_finish(GAsyncResult $result): WebKitUserContentFilter
 *
 * Finishes an asynchronous filter load previously started with
 * webkit_user_content_filter_store_load().
 */
ZEND_METHOD(Gtk4_WebKitUserContentFilterStore, load_finish) {
  zval *result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  WebKitUserContentFilterStore *self =
      PHPGTK_SELF(WebKitUserContentFilterStore, WEBKIT_TYPE_USER_CONTENT_FILTER_STORE);
  GObject *result_o = unwrap(result, G_TYPE_ASYNC_RESULT);
  if (result_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  WebKitUserContentFilter *phpgtk_ret =
      webkit_user_content_filter_store_load_finish(self, G_ASYNC_RESULT(result_o), &error);
  wrap_boxed(WEBKIT_TYPE_USER_CONTENT_FILTER, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(WEBKIT_TYPE_USER_CONTENT_FILTER, phpgtk_ret);
}

namespace {
// AsyncReadyCallback trampoline for WebKitUserContentFilterStore::remove(): wraps the C arguments,
// invokes the PHP callable once and releases it (async scope).
void cb_remove_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\WebKitUserContentFilterStore::remove(string $identifier, ?GCancellable $cancellable,
 * callable $callback): void
 *
 * Asynchronously remove a content filter given its $identifier.
 */
ZEND_METHOD(Gtk4_WebKitUserContentFilterStore, remove) {
  zend_string *identifier;
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_STR(identifier)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  WebKitUserContentFilterStore *self =
      PHPGTK_SELF(WebKitUserContentFilterStore, WEBKIT_TYPE_USER_CONTENT_FILTER_STORE);
  if (!phpgtk::check_utf8(identifier, 1)) RETURN_THROWS();
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback =
      callback_new(&fci_callback.function_name, "WebKitUserContentFilterStore::remove");
  webkit_user_content_filter_store_remove(
      self, ZSTR_VAL(identifier), cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
      cb_remove_callback, cb_callback);
}

/**
 * Gtk4\WebKitUserContentFilterStore::remove_finish(GAsyncResult $result): bool
 *
 * Finishes an asynchronous filter removal previously started with
 * webkit_user_content_filter_store_remove().
 */
ZEND_METHOD(Gtk4_WebKitUserContentFilterStore, remove_finish) {
  zval *result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  WebKitUserContentFilterStore *self =
      PHPGTK_SELF(WebKitUserContentFilterStore, WEBKIT_TYPE_USER_CONTENT_FILTER_STORE);
  GObject *result_o = unwrap(result, G_TYPE_ASYNC_RESULT);
  if (result_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  const gboolean ok =
      webkit_user_content_filter_store_remove_finish(self, G_ASYNC_RESULT(result_o), &error);
  if (!ok) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  RETURN_BOOL(ok);
}

namespace {
// AsyncReadyCallback trampoline for WebKitUserContentFilterStore::save(): wraps the C arguments,
// invokes the PHP callable once and releases it (async scope).
void cb_save_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\WebKitUserContentFilterStore::save(string $identifier, string $source, ?GCancellable
 * $cancellable, callable $callback): void
 *
 * Asynchronously save a content filter from a set source rule.
 */
ZEND_METHOD(Gtk4_WebKitUserContentFilterStore, save) {
  zend_string *identifier;
  zend_string *source;
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(4, 4)
  Z_PARAM_STR(identifier)
  Z_PARAM_STR(source)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  WebKitUserContentFilterStore *self =
      PHPGTK_SELF(WebKitUserContentFilterStore, WEBKIT_TYPE_USER_CONTENT_FILTER_STORE);
  if (!phpgtk::check_utf8(identifier, 1)) RETURN_THROWS();
  GBytes *source_b = g_bytes_new(ZSTR_VAL(source), ZSTR_LEN(source));
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback =
      callback_new(&fci_callback.function_name, "WebKitUserContentFilterStore::save");
  webkit_user_content_filter_store_save(
      self, ZSTR_VAL(identifier), source_b,
      cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr, cb_save_callback,
      cb_callback);
  g_bytes_unref(source_b);
}

/**
 * Gtk4\WebKitUserContentFilterStore::save_finish(GAsyncResult $result): WebKitUserContentFilter
 *
 * Finishes an asynchronous filter save previously started with
 * webkit_user_content_filter_store_save().
 */
ZEND_METHOD(Gtk4_WebKitUserContentFilterStore, save_finish) {
  zval *result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  WebKitUserContentFilterStore *self =
      PHPGTK_SELF(WebKitUserContentFilterStore, WEBKIT_TYPE_USER_CONTENT_FILTER_STORE);
  GObject *result_o = unwrap(result, G_TYPE_ASYNC_RESULT);
  if (result_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  WebKitUserContentFilter *phpgtk_ret =
      webkit_user_content_filter_store_save_finish(self, G_ASYNC_RESULT(result_o), &error);
  wrap_boxed(WEBKIT_TYPE_USER_CONTENT_FILTER, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(WEBKIT_TYPE_USER_CONTENT_FILTER, phpgtk_ret);
}

namespace {
// AsyncReadyCallback trampoline for WebKitUserContentFilterStore::save_from_file(): wraps the C
// arguments, invokes the PHP callable once and releases it (async scope).
void cb_save_from_file_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\WebKitUserContentFilterStore::save_from_file(string $identifier, string $file, ?GCancellable
 * $cancellable, callable $callback): void
 *
 * Asynchronously save a content filter from the contents of a file.
 */
ZEND_METHOD(Gtk4_WebKitUserContentFilterStore, save_from_file) {
  zend_string *identifier;
  zend_string *file;
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(4, 4)
  Z_PARAM_STR(identifier)
  Z_PARAM_PATH_STR(file)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  WebKitUserContentFilterStore *self =
      PHPGTK_SELF(WebKitUserContentFilterStore, WEBKIT_TYPE_USER_CONTENT_FILTER_STORE);
  if (!phpgtk::check_utf8(identifier, 1)) RETURN_THROWS();
  GFile *file_f = g_file_new_for_commandline_arg(ZSTR_VAL(file));
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback =
      callback_new(&fci_callback.function_name, "WebKitUserContentFilterStore::save_from_file");
  webkit_user_content_filter_store_save_from_file(
      self, ZSTR_VAL(identifier), file_f,
      cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr, cb_save_from_file_callback,
      cb_callback);
  if (file_f != nullptr) g_object_unref(file_f);
}

/**
 * Gtk4\WebKitUserContentFilterStore::save_from_file_finish(GAsyncResult $result):
 * WebKitUserContentFilter
 *
 * Finishes and asynchronous filter save previously started with
 * webkit_user_content_filter_store_save_from_file().
 */
ZEND_METHOD(Gtk4_WebKitUserContentFilterStore, save_from_file_finish) {
  zval *result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  WebKitUserContentFilterStore *self =
      PHPGTK_SELF(WebKitUserContentFilterStore, WEBKIT_TYPE_USER_CONTENT_FILTER_STORE);
  GObject *result_o = unwrap(result, G_TYPE_ASYNC_RESULT);
  if (result_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  WebKitUserContentFilter *phpgtk_ret = webkit_user_content_filter_store_save_from_file_finish(
      self, G_ASYNC_RESULT(result_o), &error);
  wrap_boxed(WEBKIT_TYPE_USER_CONTENT_FILTER, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(WEBKIT_TYPE_USER_CONTENT_FILTER, phpgtk_ret);
}
