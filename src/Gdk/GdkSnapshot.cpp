// GENERATED by gen/gir.php from Gdk.Snapshot - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GdkSnapshot
#include "php_gtk4.h"
#include "core/object.h"
#include "core/subtype.h"

using namespace phpgtk;

/**
 * Gtk4\GdkSnapshot::__construct()
 *
 * GdkSnapshot is abstract in GTK: `new` only works on a PHP subclass (which gets its own GType).
 */
ZEND_METHOD(Gtk4_GdkSnapshot, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) == nullptr) {
      zend_throw_error(nullptr,
                       "GdkSnapshot is abstract in GTK: subclass it in PHP (new MyClass())");
    }
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}
