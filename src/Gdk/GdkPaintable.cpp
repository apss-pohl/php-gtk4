// GENERATED by gen/gir.php from Gdk.Paintable - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GdkPaintable
#include "php_gtk4.h"
#include "core/object.h"
#include "core/subtype.h"
#include "core/error.h"

using namespace phpgtk;

/**
 * Gtk4\GdkPaintable::compute_concrete_size(float $specified_width, float $specified_height, float
 * $default_width, float $default_height): array
 *
 * Compute a concrete size for the `GdkPaintable`.
 */
ZEND_METHOD(Gtk4_GdkPaintable, compute_concrete_size) {
  double specified_width;
  double specified_height;
  double default_width;
  double default_height;
  ZEND_PARSE_PARAMETERS_START(4, 4)
  Z_PARAM_DOUBLE(specified_width)
  Z_PARAM_DOUBLE(specified_height)
  Z_PARAM_DOUBLE(default_width)
  Z_PARAM_DOUBLE(default_height)
  ZEND_PARSE_PARAMETERS_END();
  GdkPaintable *self = PHPGTK_SELF(GdkPaintable, GDK_TYPE_PAINTABLE);
  double concrete_width = 0;
  double concrete_height = 0;
  gdk_paintable_compute_concrete_size(self, specified_width, specified_height, default_width,
                                      default_height, &concrete_width, &concrete_height);
  array_init_size(return_value, 2);
  {
    zval item;
    ZVAL_DOUBLE(&item, concrete_width);
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_DOUBLE(&item, concrete_height);
    add_next_index_zval(return_value, &item);
  }
}

/**
 * Gtk4\GdkPaintable::get_current_image(): GdkPaintable
 *
 * Gets an immutable paintable for the current contents displayed by $paintable.
 */
ZEND_METHOD(Gtk4_GdkPaintable, get_current_image) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkPaintable *self = PHPGTK_SELF(GdkPaintable, GDK_TYPE_PAINTABLE);
  GdkPaintable *result = gdk_paintable_get_current_image(self);
  wrap(result != nullptr ? G_OBJECT(result) : nullptr, return_value);
  if (result != nullptr) g_object_unref(result);  // the handle took its own ref
}

/**
 * Gtk4\GdkPaintable::get_flags(): int
 *
 * Get flags for the paintable.
 */
ZEND_METHOD(Gtk4_GdkPaintable, get_flags) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkPaintable *self = PHPGTK_SELF(GdkPaintable, GDK_TYPE_PAINTABLE);
  RETURN_LONG(static_cast<zend_long>(gdk_paintable_get_flags(self)));
}

/**
 * Gtk4\GdkPaintable::get_intrinsic_aspect_ratio(): float
 *
 * Gets the preferred aspect ratio the $paintable would like to be displayed at.
 */
ZEND_METHOD(Gtk4_GdkPaintable, get_intrinsic_aspect_ratio) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkPaintable *self = PHPGTK_SELF(GdkPaintable, GDK_TYPE_PAINTABLE);
  RETURN_DOUBLE(gdk_paintable_get_intrinsic_aspect_ratio(self));
}

/**
 * Gtk4\GdkPaintable::get_intrinsic_height(): int
 *
 * Gets the preferred height the $paintable would like to be displayed at.
 */
ZEND_METHOD(Gtk4_GdkPaintable, get_intrinsic_height) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkPaintable *self = PHPGTK_SELF(GdkPaintable, GDK_TYPE_PAINTABLE);
  RETURN_LONG(static_cast<zend_long>(gdk_paintable_get_intrinsic_height(self)));
}

/**
 * Gtk4\GdkPaintable::get_intrinsic_width(): int
 *
 * Gets the preferred width the $paintable would like to be displayed at.
 */
ZEND_METHOD(Gtk4_GdkPaintable, get_intrinsic_width) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkPaintable *self = PHPGTK_SELF(GdkPaintable, GDK_TYPE_PAINTABLE);
  RETURN_LONG(static_cast<zend_long>(gdk_paintable_get_intrinsic_width(self)));
}

/**
 * Gtk4\GdkPaintable::invalidate_contents(): void
 *
 * Called by implementations of `GdkPaintable` to invalidate their contents.
 */
ZEND_METHOD(Gtk4_GdkPaintable, invalidate_contents) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkPaintable *self = PHPGTK_SELF(GdkPaintable, GDK_TYPE_PAINTABLE);
  gdk_paintable_invalidate_contents(self);
}

/**
 * Gtk4\GdkPaintable::invalidate_size(): void
 *
 * Called by implementations of `GdkPaintable` to invalidate their size.
 */
ZEND_METHOD(Gtk4_GdkPaintable, invalidate_size) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkPaintable *self = PHPGTK_SELF(GdkPaintable, GDK_TYPE_PAINTABLE);
  gdk_paintable_invalidate_size(self);
}

// vfunc thunks and installers: file-local, installed by class_init of a PHP subtype
namespace {

// vfunc thunk: static_cast<GdkPaintableInterface *>->get_current_image ->
// $this->get_current_image() on a PHP subclass
GdkPaintable *vfunc_thunk_get_current_image(GdkPaintable *self) {
  zval zself;
  zend_function *fn = EG(exception) == nullptr
                          ? subtype_vfunc(G_OBJECT(self), "get_current_image", &zself)
                          : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    // an interface implemented in PHP has no native implementation below it
    return nullptr;
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  GdkPaintable *result = nullptr;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    if (Z_TYPE(ret) == IS_OBJECT) {
      GObject *o = unwrap(&ret, GDK_TYPE_PAINTABLE);
      result = o != nullptr ? GDK_PAINTABLE(o) : nullptr;
      if (result != nullptr) g_object_ref(result);  // transfer full
    }
  }
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GdkPaintable::get_current_image");
  return result;
}

// vfunc installer: static_cast<GdkPaintableInterface *>->get_current_image (called from class_init
// / iface_init of a PHP subtype)
void vfunc_install_get_current_image(gpointer klass) {
  static_cast<GdkPaintableInterface *>(klass)->get_current_image = vfunc_thunk_get_current_image;
}

// vfunc thunk: static_cast<GdkPaintableInterface *>->get_flags -> $this->get_flags() on a PHP
// subclass
GdkPaintableFlags vfunc_thunk_get_flags(GdkPaintable *self) {
  zval zself;
  zend_function *fn =
      EG(exception) == nullptr ? subtype_vfunc(G_OBJECT(self), "get_flags", &zself) : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    // an interface implemented in PHP has no native implementation below it
    return static_cast<GdkPaintableFlags>(0);
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  GdkPaintableFlags result = static_cast<GdkPaintableFlags>(0);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = static_cast<GdkPaintableFlags>(zval_get_long(&ret));
  }
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GdkPaintable::get_flags");
  return result;
}

// vfunc installer: static_cast<GdkPaintableInterface *>->get_flags (called from class_init /
// iface_init of a PHP subtype)
void vfunc_install_get_flags(gpointer klass) {
  static_cast<GdkPaintableInterface *>(klass)->get_flags = vfunc_thunk_get_flags;
}

// vfunc thunk: static_cast<GdkPaintableInterface *>->get_intrinsic_aspect_ratio ->
// $this->get_intrinsic_aspect_ratio() on a PHP subclass
double vfunc_thunk_get_intrinsic_aspect_ratio(GdkPaintable *self) {
  zval zself;
  zend_function *fn = EG(exception) == nullptr
                          ? subtype_vfunc(G_OBJECT(self), "get_intrinsic_aspect_ratio", &zself)
                          : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    // an interface implemented in PHP has no native implementation below it
    return 0;
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  double result = 0;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = zval_get_double(&ret);
  }
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GdkPaintable::get_intrinsic_aspect_ratio");
  return result;
}

// vfunc installer: static_cast<GdkPaintableInterface *>->get_intrinsic_aspect_ratio (called from
// class_init / iface_init of a PHP subtype)
void vfunc_install_get_intrinsic_aspect_ratio(gpointer klass) {
  static_cast<GdkPaintableInterface *>(klass)->get_intrinsic_aspect_ratio =
      vfunc_thunk_get_intrinsic_aspect_ratio;
}

// vfunc thunk: static_cast<GdkPaintableInterface *>->get_intrinsic_height ->
// $this->get_intrinsic_height() on a PHP subclass
int vfunc_thunk_get_intrinsic_height(GdkPaintable *self) {
  zval zself;
  zend_function *fn = EG(exception) == nullptr
                          ? subtype_vfunc(G_OBJECT(self), "get_intrinsic_height", &zself)
                          : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    // an interface implemented in PHP has no native implementation below it
    return 0;
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  int result = 0;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = static_cast<int>(zval_get_long(&ret));
  }
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GdkPaintable::get_intrinsic_height");
  return result;
}

// vfunc installer: static_cast<GdkPaintableInterface *>->get_intrinsic_height (called from
// class_init / iface_init of a PHP subtype)
void vfunc_install_get_intrinsic_height(gpointer klass) {
  static_cast<GdkPaintableInterface *>(klass)->get_intrinsic_height =
      vfunc_thunk_get_intrinsic_height;
}

// vfunc thunk: static_cast<GdkPaintableInterface *>->get_intrinsic_width ->
// $this->get_intrinsic_width() on a PHP subclass
int vfunc_thunk_get_intrinsic_width(GdkPaintable *self) {
  zval zself;
  zend_function *fn = EG(exception) == nullptr
                          ? subtype_vfunc(G_OBJECT(self), "get_intrinsic_width", &zself)
                          : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    // an interface implemented in PHP has no native implementation below it
    return 0;
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  int result = 0;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = static_cast<int>(zval_get_long(&ret));
  }
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GdkPaintable::get_intrinsic_width");
  return result;
}

// vfunc installer: static_cast<GdkPaintableInterface *>->get_intrinsic_width (called from
// class_init / iface_init of a PHP subtype)
void vfunc_install_get_intrinsic_width(gpointer klass) {
  static_cast<GdkPaintableInterface *>(klass)->get_intrinsic_width =
      vfunc_thunk_get_intrinsic_width;
}

}  // namespace

// MINIT: the vfunc thunks of GdkPaintable (core/subtype.h).
void register_vfuncs_GdkPaintable() {
  register_iface_vfunc(GDK_TYPE_PAINTABLE, "get_current_image", vfunc_install_get_current_image);
  register_iface_vfunc(GDK_TYPE_PAINTABLE, "get_flags", vfunc_install_get_flags);
  register_iface_vfunc(GDK_TYPE_PAINTABLE, "get_intrinsic_aspect_ratio",
                       vfunc_install_get_intrinsic_aspect_ratio);
  register_iface_vfunc(GDK_TYPE_PAINTABLE, "get_intrinsic_height",
                       vfunc_install_get_intrinsic_height);
  register_iface_vfunc(GDK_TYPE_PAINTABLE, "get_intrinsic_width",
                       vfunc_install_get_intrinsic_width);
}

/**
 * Gtk4\GdkPaintableObject::__construct()
 *
 * Never called: these handles only come from wrap().
 */
ZEND_METHOD(Gtk4_GdkPaintableObject, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
}
