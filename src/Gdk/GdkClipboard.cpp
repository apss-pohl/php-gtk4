// GENERATED by gen/gir.php from Gdk.Clipboard - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GdkClipboard
#include "php_gtk4.h"
#include "core/object.h"
#include "core/collections.h"
#include "core/gerror.h"
#include "core/boxed.h"
#include "core/callback.h"
#include <array>

using namespace phpgtk;

// The typed payload half of the clipboard: GIR's GValue and GType parameters, as an ordinary PHP
// value and a type name (core/marshal, gtype_from_php_name).
#include "core/marshal.h"

/**
 * Gtk4\GdkClipboard::__construct()
 *
 * GdkClipboard has no constructor in GTK: instances come from GTK, never from `new`.
 */
ZEND_METHOD(Gtk4_GdkClipboard, __construct) {
  // Private: never called (object_init_ex() in wrap() skips constructors).
  ZEND_PARSE_PARAMETERS_NONE();
}

/**
 * Gtk4\GdkClipboard::get_content(): ?GdkContentProvider
 *
 * Returns the `GdkContentProvider` currently set on $clipboard.
 */
ZEND_METHOD(Gtk4_GdkClipboard, get_content) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkClipboard *self = PHPGTK_SELF(GdkClipboard, GDK_TYPE_CLIPBOARD);
  GdkContentProvider *phpgtk_ret = gdk_clipboard_get_content(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GdkClipboard::get_display(): GdkDisplay
 *
 * Gets the `GdkDisplay` that the clipboard was created for.
 */
ZEND_METHOD(Gtk4_GdkClipboard, get_display) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkClipboard *self = PHPGTK_SELF(GdkClipboard, GDK_TYPE_CLIPBOARD);
  GdkDisplay *phpgtk_ret = gdk_clipboard_get_display(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GdkClipboard::get_formats(): GdkContentFormats
 *
 * Gets the formats that the clipboard can provide its current contents in.
 */
ZEND_METHOD(Gtk4_GdkClipboard, get_formats) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkClipboard *self = PHPGTK_SELF(GdkClipboard, GDK_TYPE_CLIPBOARD);
  GdkContentFormats *phpgtk_ret = gdk_clipboard_get_formats(self);
  wrap_boxed(GDK_TYPE_CONTENT_FORMATS, phpgtk_ret, return_value);
}

/**
 * Gtk4\GdkClipboard::is_local(): bool
 *
 * Returns if the clipboard is local.
 */
ZEND_METHOD(Gtk4_GdkClipboard, is_local) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkClipboard *self = PHPGTK_SELF(GdkClipboard, GDK_TYPE_CLIPBOARD);
  RETURN_BOOL(gdk_clipboard_is_local(self));
}

namespace {
// AsyncReadyCallback trampoline for GdkClipboard::read_async(): wraps the C arguments, invokes the
// PHP callable once and releases it (async scope).
void cb_read_async_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\GdkClipboard::read_async(array $mime_types, int $io_priority, ?GCancellable $cancellable,
 * ?callable $callback): void
 *
 * Asynchronously requests an input stream to read the $clipboard's contents from.
 */
ZEND_METHOD(Gtk4_GdkClipboard, read_async) {
  zval *mime_types;
  zend_long io_priority;
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(4, 4)
  Z_PARAM_ARRAY(mime_types)
  Z_PARAM_LONG(io_priority)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC_OR_NULL(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  GdkClipboard *self = PHPGTK_SELF(GdkClipboard, GDK_TYPE_CLIPBOARD);
  char **mime_types_v = strv_from_php(mime_types);
  if (mime_types_v == nullptr) RETURN_THROWS();
  if (!phpgtk::check_range<int>(io_priority, 2)) RETURN_THROWS();
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback =
      ZEND_FCI_INITIALIZED(fci_callback)
          ? callback_new(&fci_callback.function_name, "GdkClipboard::read_async")
          : nullptr;
  gdk_clipboard_read_async(self, const_cast<const char **>(mime_types_v),
                           static_cast<int>(io_priority),
                           cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
                           cb_callback != nullptr ? cb_read_async_callback : nullptr, cb_callback);
  g_strfreev(mime_types_v);
}

namespace {
// AsyncReadyCallback trampoline for GdkClipboard::read_text_async(): wraps the C arguments, invokes
// the PHP callable once and releases it (async scope).
void cb_read_text_async_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\GdkClipboard::read_text_async(?GCancellable $cancellable, ?callable $callback): void
 *
 * Asynchronously request the $clipboard contents converted to a string.
 */
ZEND_METHOD(Gtk4_GdkClipboard, read_text_async) {
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC_OR_NULL(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  GdkClipboard *self = PHPGTK_SELF(GdkClipboard, GDK_TYPE_CLIPBOARD);
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback =
      ZEND_FCI_INITIALIZED(fci_callback)
          ? callback_new(&fci_callback.function_name, "GdkClipboard::read_text_async")
          : nullptr;
  gdk_clipboard_read_text_async(
      self, cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
      cb_callback != nullptr ? cb_read_text_async_callback : nullptr, cb_callback);
}

/**
 * Gtk4\GdkClipboard::read_text_finish(GAsyncResult $result): ?string
 *
 * Finishes an asynchronous clipboard read.
 */
ZEND_METHOD(Gtk4_GdkClipboard, read_text_finish) {
  zval *result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  GdkClipboard *self = PHPGTK_SELF(GdkClipboard, GDK_TYPE_CLIPBOARD);
  GObject *result_o = unwrap(result, G_TYPE_ASYNC_RESULT);
  if (result_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  char *phpgtk_ret = gdk_clipboard_read_text_finish(self, G_ASYNC_RESULT(result_o), &error);
  if (phpgtk_ret == nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  if (phpgtk_ret == nullptr) RETURN_NULL();
  RETVAL_STRING(phpgtk_ret);
  g_free(phpgtk_ret);
}

namespace {
// AsyncReadyCallback trampoline for GdkClipboard::read_texture_async(): wraps the C arguments,
// invokes the PHP callable once and releases it (async scope).
void cb_read_texture_async_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\GdkClipboard::read_texture_async(?GCancellable $cancellable, ?callable $callback): void
 *
 * Asynchronously request the $clipboard contents converted to a `GdkPixbuf`.
 */
ZEND_METHOD(Gtk4_GdkClipboard, read_texture_async) {
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC_OR_NULL(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  GdkClipboard *self = PHPGTK_SELF(GdkClipboard, GDK_TYPE_CLIPBOARD);
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback =
      ZEND_FCI_INITIALIZED(fci_callback)
          ? callback_new(&fci_callback.function_name, "GdkClipboard::read_texture_async")
          : nullptr;
  gdk_clipboard_read_texture_async(
      self, cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
      cb_callback != nullptr ? cb_read_texture_async_callback : nullptr, cb_callback);
}

/**
 * Gtk4\GdkClipboard::read_texture_finish(GAsyncResult $result): ?GdkTexture
 *
 * Finishes an asynchronous clipboard read.
 */
ZEND_METHOD(Gtk4_GdkClipboard, read_texture_finish) {
  zval *result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  GdkClipboard *self = PHPGTK_SELF(GdkClipboard, GDK_TYPE_CLIPBOARD);
  GObject *result_o = unwrap(result, G_TYPE_ASYNC_RESULT);
  if (result_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  GdkTexture *phpgtk_ret =
      gdk_clipboard_read_texture_finish(self, G_ASYNC_RESULT(result_o), &error);
  if (phpgtk_ret == nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * Gtk4\GdkClipboard::set_content(?GdkContentProvider $provider): bool
 *
 * Sets a new content provider on $clipboard.
 */
ZEND_METHOD(Gtk4_GdkClipboard, set_content) {
  zval *provider = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(provider, class_for_gtype(GDK_TYPE_CONTENT_PROVIDER))
  ZEND_PARSE_PARAMETERS_END();
  GdkClipboard *self = PHPGTK_SELF(GdkClipboard, GDK_TYPE_CLIPBOARD);
  GObject *provider_o = nullptr;
  if (provider != nullptr) {
    provider_o = unwrap(provider, GDK_TYPE_CONTENT_PROVIDER);
    if (provider_o == nullptr) RETURN_THROWS();
  }
  RETURN_BOOL(gdk_clipboard_set_content(
      self, provider_o != nullptr ? GDK_CONTENT_PROVIDER(provider_o) : nullptr));
}

/**
 * Gtk4\GdkClipboard::set_text(string $text): void
 *
 * Puts the given $text into the clipboard.
 */
ZEND_METHOD(Gtk4_GdkClipboard, set_text) {
  zend_string *text;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(text)
  ZEND_PARSE_PARAMETERS_END();
  GdkClipboard *self = PHPGTK_SELF(GdkClipboard, GDK_TYPE_CLIPBOARD);
  if (!phpgtk::check_utf8(text, 1)) RETURN_THROWS();
  gdk_clipboard_set_text(self, ZSTR_VAL(text));
}

/**
 * Gtk4\GdkClipboard::set_texture(GdkTexture $texture): void
 *
 * Puts the given $texture into the clipboard.
 */
ZEND_METHOD(Gtk4_GdkClipboard, set_texture) {
  zval *texture;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(texture, class_for_gtype(GDK_TYPE_TEXTURE))
  ZEND_PARSE_PARAMETERS_END();
  GdkClipboard *self = PHPGTK_SELF(GdkClipboard, GDK_TYPE_CLIPBOARD);
  GObject *texture_o = unwrap(texture, GDK_TYPE_TEXTURE);
  if (texture_o == nullptr) RETURN_THROWS();
  gdk_clipboard_set_texture(self, GDK_TEXTURE(texture_o));
}

namespace {
// AsyncReadyCallback trampoline for GdkClipboard::store_async(): wraps the C arguments, invokes the
// PHP callable once and releases it (async scope).
void cb_store_async_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\GdkClipboard::store_async(int $io_priority, ?GCancellable $cancellable, ?callable
 * $callback): void
 *
 * Asynchronously instructs the $clipboard to store its contents remotely.
 */
ZEND_METHOD(Gtk4_GdkClipboard, store_async) {
  zend_long io_priority;
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_LONG(io_priority)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC_OR_NULL(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  GdkClipboard *self = PHPGTK_SELF(GdkClipboard, GDK_TYPE_CLIPBOARD);
  if (!phpgtk::check_range<int>(io_priority, 1)) RETURN_THROWS();
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback =
      ZEND_FCI_INITIALIZED(fci_callback)
          ? callback_new(&fci_callback.function_name, "GdkClipboard::store_async")
          : nullptr;
  gdk_clipboard_store_async(self, static_cast<int>(io_priority),
                            cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
                            cb_callback != nullptr ? cb_store_async_callback : nullptr,
                            cb_callback);
}

/**
 * Gtk4\GdkClipboard::store_finish(GAsyncResult $result): bool
 *
 * Finishes an asynchronous clipboard store.
 */
ZEND_METHOD(Gtk4_GdkClipboard, store_finish) {
  zval *result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  GdkClipboard *self = PHPGTK_SELF(GdkClipboard, GDK_TYPE_CLIPBOARD);
  GObject *result_o = unwrap(result, G_TYPE_ASYNC_RESULT);
  if (result_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  const gboolean ok = gdk_clipboard_store_finish(self, G_ASYNC_RESULT(result_o), &error);
  if (!ok) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  RETURN_BOOL(ok);
}

/**
 * public function set_value(mixed $value, ?string $type = null): void
 * Put $value on the clipboard, typed as $type (inferred from $value when omitted).
 *
 * The typed counterpart of set_text()/set_texture(): GIR takes a GValue, and the marshaller is
 * what turns an ordinary PHP value into one. The type is named as everywhere else -
 * "string"/"int"/"float"/"bool" or a registered class name.
 */
ZEND_METHOD(Gtk4_GdkClipboard, set_value) {
  zval *value;
  zend_string *type = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 2)
  Z_PARAM_ZVAL(value)
  Z_PARAM_OPTIONAL
  Z_PARAM_STR_OR_NULL(type)
  ZEND_PARSE_PARAMETERS_END();
  GdkClipboard *self = PHPGTK_SELF(GdkClipboard, GDK_TYPE_CLIPBOARD);

  GType t = 0;
  if (type != nullptr) {
    t = gtype_from_php_name(type, 2);
    if (t == 0) RETURN_THROWS();
  } else {
    switch (Z_TYPE_P(value)) {
      case IS_STRING:
        t = G_TYPE_STRING;
        break;
      case IS_LONG:
        t = G_TYPE_INT64;
        break;
      case IS_DOUBLE:
        t = G_TYPE_DOUBLE;
        break;
      case IS_TRUE:
      case IS_FALSE:
        t = G_TYPE_BOOLEAN;
        break;
      case IS_OBJECT: {
        GObject *obj = unwrap(value, G_TYPE_OBJECT);
        if (obj == nullptr) RETURN_THROWS();
        t = G_OBJECT_TYPE(obj);
        break;
      }
      default:
        zend_argument_type_error(1,
                                 "must be a string, int, float, bool or GObject handle to be "
                                 "set without naming a type");
        RETURN_THROWS();
    }
  }
  GValue gv = G_VALUE_INIT;
  if (!to_gvalue(value, t, &gv)) RETURN_THROWS();
  gdk_clipboard_set_value(self, &gv);
  g_value_unset(&gv);
}
