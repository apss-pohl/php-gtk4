// GENERATED by gen/gir.php from Gdk.Drag - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GdkDrag
#include "php_gtk4.h"
#include "core/object.h"
#include "core/boxed.h"

using namespace phpgtk;

/**
 * Gtk4\GdkDrag::__construct()
 *
 * GdkDrag is abstract in GTK: instances come from GTK, never from `new`.
 */
ZEND_METHOD(Gtk4_GdkDrag, __construct) {
  // Private: never called (object_init_ex() in wrap() skips constructors).
  ZEND_PARSE_PARAMETERS_NONE();
}

/**
 * static Gtk4\GdkDrag::begin(GdkSurface $surface, GdkDevice $device, GdkContentProvider $content,
 * int $actions, float $dx, float $dy): ?GdkDrag
 *
 * Starts a drag and creates a new drag context for it.
 */
ZEND_METHOD(Gtk4_GdkDrag, begin) {
  zval *surface;
  zval *device;
  zval *content;
  zend_long actions;
  double dx;
  double dy;
  ZEND_PARSE_PARAMETERS_START(6, 6)
  Z_PARAM_OBJECT_OF_CLASS(surface, class_for_gtype(GDK_TYPE_SURFACE))
  Z_PARAM_OBJECT_OF_CLASS(device, class_for_gtype(GDK_TYPE_DEVICE))
  Z_PARAM_OBJECT_OF_CLASS(content, class_for_gtype(GDK_TYPE_CONTENT_PROVIDER))
  Z_PARAM_LONG(actions)
  Z_PARAM_DOUBLE(dx)
  Z_PARAM_DOUBLE(dy)
  ZEND_PARSE_PARAMETERS_END();
  GObject *surface_o = unwrap(surface, GDK_TYPE_SURFACE);
  if (surface_o == nullptr) RETURN_THROWS();
  GObject *device_o = unwrap(device, GDK_TYPE_DEVICE);
  if (device_o == nullptr) RETURN_THROWS();
  GObject *content_o = unwrap(content, GDK_TYPE_CONTENT_PROVIDER);
  if (content_o == nullptr) RETURN_THROWS();
  if (!phpgtk::check_flags(GDK_TYPE_DRAG_ACTION, actions, 4)) RETURN_THROWS();
  GdkDrag *phpgtk_ret =
      gdk_drag_begin(GDK_SURFACE(surface_o), GDK_DEVICE(device_o), GDK_CONTENT_PROVIDER(content_o),
                     static_cast<GdkDragAction>(actions), dx, dy);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * Gtk4\GdkDrag::drop_done(bool $success): void
 *
 * Informs GDK that the drop ended.
 */
ZEND_METHOD(Gtk4_GdkDrag, drop_done) {
  bool success;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(success)
  ZEND_PARSE_PARAMETERS_END();
  GdkDrag *self = PHPGTK_SELF(GdkDrag, GDK_TYPE_DRAG);
  gdk_drag_drop_done(self, success);
}

/**
 * Gtk4\GdkDrag::get_actions(): int
 *
 * Determines the bitmask of possible actions proposed by the source.
 */
ZEND_METHOD(Gtk4_GdkDrag, get_actions) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkDrag *self = PHPGTK_SELF(GdkDrag, GDK_TYPE_DRAG);
  RETURN_LONG(static_cast<zend_long>(gdk_drag_get_actions(self)));
}

/**
 * Gtk4\GdkDrag::get_content(): GdkContentProvider
 *
 * Returns the `GdkContentProvider` associated to the `GdkDrag` object.
 */
ZEND_METHOD(Gtk4_GdkDrag, get_content) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkDrag *self = PHPGTK_SELF(GdkDrag, GDK_TYPE_DRAG);
  GdkContentProvider *phpgtk_ret = gdk_drag_get_content(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GdkDrag::get_device(): GdkDevice
 *
 * Returns the `GdkDevice` associated to the `GdkDrag` object.
 */
ZEND_METHOD(Gtk4_GdkDrag, get_device) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkDrag *self = PHPGTK_SELF(GdkDrag, GDK_TYPE_DRAG);
  GdkDevice *phpgtk_ret = gdk_drag_get_device(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GdkDrag::get_display(): GdkDisplay
 *
 * Gets the `GdkDisplay` that the drag object was created for.
 */
ZEND_METHOD(Gtk4_GdkDrag, get_display) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkDrag *self = PHPGTK_SELF(GdkDrag, GDK_TYPE_DRAG);
  GdkDisplay *phpgtk_ret = gdk_drag_get_display(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GdkDrag::get_drag_surface(): ?GdkSurface
 *
 * Returns the surface on which the drag icon should be rendered during the drag operation.
 */
ZEND_METHOD(Gtk4_GdkDrag, get_drag_surface) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkDrag *self = PHPGTK_SELF(GdkDrag, GDK_TYPE_DRAG);
  GdkSurface *phpgtk_ret = gdk_drag_get_drag_surface(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GdkDrag::get_formats(): GdkContentFormats
 *
 * Retrieves the formats supported by this `GdkDrag` object.
 */
ZEND_METHOD(Gtk4_GdkDrag, get_formats) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkDrag *self = PHPGTK_SELF(GdkDrag, GDK_TYPE_DRAG);
  GdkContentFormats *phpgtk_ret = gdk_drag_get_formats(self);
  wrap_boxed(GDK_TYPE_CONTENT_FORMATS, phpgtk_ret, return_value);
}

/**
 * Gtk4\GdkDrag::get_selected_action(): int
 *
 * Determines the action chosen by the drag destination.
 */
ZEND_METHOD(Gtk4_GdkDrag, get_selected_action) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkDrag *self = PHPGTK_SELF(GdkDrag, GDK_TYPE_DRAG);
  RETURN_LONG(static_cast<zend_long>(gdk_drag_get_selected_action(self)));
}

/**
 * Gtk4\GdkDrag::get_surface(): GdkSurface
 *
 * Returns the `GdkSurface` where the drag originates.
 */
ZEND_METHOD(Gtk4_GdkDrag, get_surface) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkDrag *self = PHPGTK_SELF(GdkDrag, GDK_TYPE_DRAG);
  GdkSurface *phpgtk_ret = gdk_drag_get_surface(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GdkDrag::set_hotspot(int $hot_x, int $hot_y): void
 *
 * Sets the position of the drag surface that will be kept under the cursor hotspot.
 */
ZEND_METHOD(Gtk4_GdkDrag, set_hotspot) {
  zend_long hot_x;
  zend_long hot_y;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_LONG(hot_x)
  Z_PARAM_LONG(hot_y)
  ZEND_PARSE_PARAMETERS_END();
  GdkDrag *self = PHPGTK_SELF(GdkDrag, GDK_TYPE_DRAG);
  if (!phpgtk::check_range<int>(hot_x, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(hot_y, 2)) RETURN_THROWS();
  gdk_drag_set_hotspot(self, static_cast<int>(hot_x), static_cast<int>(hot_y));
}
