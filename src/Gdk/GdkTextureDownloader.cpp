// GENERATED by gen/gir.php from Gdk.TextureDownloader - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GdkTextureDownloader (boxed value type)
#include "php_gtk4.h"
#include "core/boxed.h"
#include "core/object.h"
#include <cstring>
#include "core/enums.h"

using namespace phpgtk;

namespace {

const char *const fields[] = {nullptr};

// Boxed field reader: the public scalar fields as PHP properties.
bool read(gpointer data, const char *field, zval *rv) {
  (void)data;
  (void)field;
  (void)rv;
  return false;
}

// Boxed field writer: each field converts like a parameter of its type (core/boxed).
bool write(gpointer data, const char *field, zval *v) {
  (void)data;
  (void)field;
  (void)v;
  return false;
}

}  // namespace

/**
 * Gtk4\GdkTextureDownloader::__construct(GdkTexture $texture)
 *
 * Creates a new texture downloader for $texture.
 */
ZEND_METHOD(Gtk4_GdkTextureDownloader, __construct) {
  zval *texture;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(texture, class_for_gtype(GDK_TYPE_TEXTURE))
  ZEND_PARSE_PARAMETERS_END();
  GObject *texture_o = unwrap(texture, GDK_TYPE_TEXTURE);
  if (texture_o == nullptr) RETURN_THROWS();
  gpointer obj = gdk_texture_downloader_new(GDK_TEXTURE(texture_o));
  if (obj == nullptr) {
    zend_throw_error(nullptr, "%s(): GTK refused to create the value (see the CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  boxed_adopt(boxed_from_zval(ZEND_THIS), GDK_TYPE_TEXTURE_DOWNLOADER, obj);
}

/**
 * Gtk4\GdkTextureDownloader::get_format(): GdkMemoryFormat
 *
 * Gets the format that the data will be downloaded in.
 */
ZEND_METHOD(Gtk4_GdkTextureDownloader, get_format) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkTextureDownloader *self = PHPGTK_BOXED_SELF(GdkTextureDownloader);
  enum_to_php(GDK_TYPE_MEMORY_FORMAT, gdk_texture_downloader_get_format(self), return_value);
}

/**
 * Gtk4\GdkTextureDownloader::get_texture(): GdkTexture
 *
 * Gets the texture that the downloader will download.
 */
ZEND_METHOD(Gtk4_GdkTextureDownloader, get_texture) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkTextureDownloader *self = PHPGTK_BOXED_SELF(GdkTextureDownloader);
  GdkTexture *phpgtk_ret = gdk_texture_downloader_get_texture(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GdkTextureDownloader::set_format(GdkMemoryFormat $format): void
 *
 * Sets the format the downloader will download.
 */
ZEND_METHOD(Gtk4_GdkTextureDownloader, set_format) {
  zval *format;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(format, enum_class_for_type(GDK_TYPE_MEMORY_FORMAT))
  ZEND_PARSE_PARAMETERS_END();
  GdkTextureDownloader *self = PHPGTK_BOXED_SELF(GdkTextureDownloader);
  gint format_v = 0;
  if (!enum_from_php(format, GDK_TYPE_MEMORY_FORMAT, &format_v)) RETURN_THROWS();
  gdk_texture_downloader_set_format(self, static_cast<GdkMemoryFormat>(format_v));
}

/**
 * Gtk4\GdkTextureDownloader::set_texture(GdkTexture $texture): void
 *
 * Changes the texture the downloader will download.
 */
ZEND_METHOD(Gtk4_GdkTextureDownloader, set_texture) {
  zval *texture;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(texture, class_for_gtype(GDK_TYPE_TEXTURE))
  ZEND_PARSE_PARAMETERS_END();
  GdkTextureDownloader *self = PHPGTK_BOXED_SELF(GdkTextureDownloader);
  GObject *texture_o = unwrap(texture, GDK_TYPE_TEXTURE);
  if (texture_o == nullptr) RETURN_THROWS();
  gdk_texture_downloader_set_texture(self, GDK_TEXTURE(texture_o));
}

/**
 * public function download_bytes(): array
 * The texture's pixels in the downloader's format (set_format();
 * GdkMemoryFormat::B8g8r8a8Premultiplied by default) as `[string $bytes, int $stride]` - the row
 * stride is what GdkPixbuf::new_from_bytes() or GdkMemoryTexture::new() need next to the bytes.
 *
 * @return array{string, int}
 */
ZEND_METHOD(Gtk4_GdkTextureDownloader, download_bytes) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkTextureDownloader *self = PHPGTK_BOXED_SELF(GdkTextureDownloader);
  gsize stride = 0;
  GBytes *bytes = gdk_texture_downloader_download_bytes(self, &stride);
  gsize length = 0;
  const auto *data = static_cast<const char *>(g_bytes_get_data(bytes, &length));
  array_init_size(return_value, 2);
  add_next_index_stringl(return_value, data == nullptr ? "" : data, length);
  add_next_index_long(return_value, static_cast<zend_long>(stride));
  g_bytes_unref(bytes);
}

namespace phpgtk {
// MINIT: bind the PHP class to GDK_TYPE_TEXTURE_DOWNLOADER with its field table.
void register_GdkTextureDownloader(zend_class_entry *ce) {
  register_boxed(BoxedClass{.type = GDK_TYPE_TEXTURE_DOWNLOADER,
                            .ce = ce,
                            .fields = fields,
                            .read = read,
                            .write = write});
}
}  // namespace phpgtk
