// GENERATED by gen/gir.php from Gdk.ToplevelLayout - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GdkToplevelLayout (boxed value type)
#include "php_gtk4.h"
#include "core/boxed.h"
#include "core/object.h"
#include <cstring>

using namespace phpgtk;

namespace {

const char *const fields[] = {nullptr};

// Boxed field reader: the public scalar fields as PHP properties.
bool read(gpointer data, const char *field, zval *rv) {
  (void)data;
  (void)field;
  (void)rv;
  return false;
}

// Boxed field writer: each field converts like a parameter of its type (core/boxed).
bool write(gpointer data, const char *field, zval *v) {
  (void)data;
  (void)field;
  (void)v;
  return false;
}

}  // namespace

/**
 * Gtk4\GdkToplevelLayout::__construct()
 *
 * Create a toplevel layout description.
 */
ZEND_METHOD(Gtk4_GdkToplevelLayout, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  gpointer obj = gdk_toplevel_layout_new();
  if (obj == nullptr) {
    zend_throw_error(nullptr, "%s(): GTK refused to create the value (see the CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  boxed_adopt(boxed_from_zval(ZEND_THIS), GDK_TYPE_TOPLEVEL_LAYOUT, obj);
}

/**
 * Gtk4\GdkToplevelLayout::equal(GdkToplevelLayout $other): bool
 *
 * Check whether $layout and $other has identical layout properties.
 */
ZEND_METHOD(Gtk4_GdkToplevelLayout, equal) {
  zval *other;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(other, boxed_class_for_type(GDK_TYPE_TOPLEVEL_LAYOUT)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GdkToplevelLayout *self = PHPGTK_BOXED_SELF(GdkToplevelLayout);
  gpointer other_b = unwrap_boxed(other, GDK_TYPE_TOPLEVEL_LAYOUT);
  if (other_b == nullptr) RETURN_THROWS();
  RETURN_BOOL(gdk_toplevel_layout_equal(self, static_cast<GdkToplevelLayout *>(other_b)));
}

/**
 * Gtk4\GdkToplevelLayout::get_fullscreen(): ?bool
 *
 * If the layout specifies whether to the toplevel should go fullscreen, the value pointed to by
 * $fullscreen is set to `true` if it should go fullscreen, or `false`, if it should go
 * unfullscreen.
 */
ZEND_METHOD(Gtk4_GdkToplevelLayout, get_fullscreen) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkToplevelLayout *self = PHPGTK_BOXED_SELF(GdkToplevelLayout);
  gboolean fullscreen = FALSE;
  if (!gdk_toplevel_layout_get_fullscreen(self, &fullscreen)) RETURN_NULL();
  ZVAL_BOOL(return_value, fullscreen != FALSE);
}

/**
 * Gtk4\GdkToplevelLayout::get_fullscreen_monitor(): ?GdkMonitor
 *
 * Returns the monitor that the layout is fullscreening the surface on.
 */
ZEND_METHOD(Gtk4_GdkToplevelLayout, get_fullscreen_monitor) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkToplevelLayout *self = PHPGTK_BOXED_SELF(GdkToplevelLayout);
  GdkMonitor *phpgtk_ret = gdk_toplevel_layout_get_fullscreen_monitor(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GdkToplevelLayout::get_maximized(): ?bool
 *
 * If the layout specifies whether to the toplevel should go maximized, the value pointed to by
 * $maximized is set to `true` if it should go fullscreen, or `false`, if it should go unmaximized.
 */
ZEND_METHOD(Gtk4_GdkToplevelLayout, get_maximized) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkToplevelLayout *self = PHPGTK_BOXED_SELF(GdkToplevelLayout);
  gboolean maximized = FALSE;
  if (!gdk_toplevel_layout_get_maximized(self, &maximized)) RETURN_NULL();
  ZVAL_BOOL(return_value, maximized != FALSE);
}

/**
 * Gtk4\GdkToplevelLayout::get_resizable(): bool
 *
 * Returns whether the layout should allow the user to resize the surface.
 */
ZEND_METHOD(Gtk4_GdkToplevelLayout, get_resizable) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkToplevelLayout *self = PHPGTK_BOXED_SELF(GdkToplevelLayout);
  RETURN_BOOL(gdk_toplevel_layout_get_resizable(self));
}

/**
 * Gtk4\GdkToplevelLayout::set_fullscreen(bool $fullscreen, ?GdkMonitor $monitor): void
 *
 * Sets whether the layout should cause the surface to be fullscreen when presented.
 */
ZEND_METHOD(Gtk4_GdkToplevelLayout, set_fullscreen) {
  bool fullscreen;
  zval *monitor = nullptr;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_BOOL(fullscreen)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(monitor, class_for_gtype(GDK_TYPE_MONITOR))
  ZEND_PARSE_PARAMETERS_END();
  GdkToplevelLayout *self = PHPGTK_BOXED_SELF(GdkToplevelLayout);
  GObject *monitor_o = nullptr;
  if (monitor != nullptr) {
    monitor_o = unwrap(monitor, GDK_TYPE_MONITOR);
    if (monitor_o == nullptr) RETURN_THROWS();
  }
  gdk_toplevel_layout_set_fullscreen(self, fullscreen,
                                     monitor_o != nullptr ? GDK_MONITOR(monitor_o) : nullptr);
}

/**
 * Gtk4\GdkToplevelLayout::set_maximized(bool $maximized): void
 *
 * Sets whether the layout should cause the surface to be maximized when presented.
 */
ZEND_METHOD(Gtk4_GdkToplevelLayout, set_maximized) {
  bool maximized;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(maximized)
  ZEND_PARSE_PARAMETERS_END();
  GdkToplevelLayout *self = PHPGTK_BOXED_SELF(GdkToplevelLayout);
  gdk_toplevel_layout_set_maximized(self, maximized);
}

/**
 * Gtk4\GdkToplevelLayout::set_resizable(bool $resizable): void
 *
 * Sets whether the layout should allow the user to resize the surface after it has been presented.
 */
ZEND_METHOD(Gtk4_GdkToplevelLayout, set_resizable) {
  bool resizable;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(resizable)
  ZEND_PARSE_PARAMETERS_END();
  GdkToplevelLayout *self = PHPGTK_BOXED_SELF(GdkToplevelLayout);
  gdk_toplevel_layout_set_resizable(self, resizable);
}

namespace phpgtk {
// MINIT: bind the PHP class to GDK_TYPE_TOPLEVEL_LAYOUT with its field table.
void register_GdkToplevelLayout(zend_class_entry *ce) {
  register_boxed(BoxedClass{
      .type = GDK_TYPE_TOPLEVEL_LAYOUT,
      .ce = ce,
      .fields = fields,
      .read = read,
      .write = write,
      // NOLINTBEGIN(cppcoreguidelines-pro-type-const-cast) gdk_toplevel_layout_copy() takes its
      // operand non-const
      .copy =
          [](gconstpointer d) {
            return static_cast<gpointer>(gdk_toplevel_layout_copy(
                static_cast<GdkToplevelLayout *>(const_cast<gpointer>(d))));
          }
      // NOLINTEND(cppcoreguidelines-pro-type-const-cast)
      ,
      // NOLINTBEGIN(cppcoreguidelines-pro-type-const-cast) gdk_toplevel_layout_equal() takes
      // non-const operands
      .equal =
          [](gconstpointer a, gconstpointer b) {
            return gdk_toplevel_layout_equal(
                       static_cast<GdkToplevelLayout *>(const_cast<gpointer>(a)),
                       static_cast<GdkToplevelLayout *>(const_cast<gpointer>(b))) != FALSE;
          }
      // NOLINTEND(cppcoreguidelines-pro-type-const-cast)
  });
}
}  // namespace phpgtk
