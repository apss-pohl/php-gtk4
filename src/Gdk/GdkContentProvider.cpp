// GENERATED by gen/gir.php from Gdk.ContentProvider - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GdkContentProvider
#include "php_gtk4.h"
#include "core/object.h"
#include "core/gerror.h"
#include "core/boxed.h"
#include "core/subtype.h"
#include "core/error.h"
#include <array>

using namespace phpgtk;

// The GValue boundary drag and drop is built on: a payload is a GValue of a declared GType, and
// PHP names that type the way it already does for a list store's items - a class name - plus the
// four scalars a drag realistically carries (core/marshal, gtype_from_php_name).
#include "core/marshal.h"

/**
 * Gtk4\GdkContentProvider::__construct()
 *
 * A GdkContentProvider with default properties (GTK's own constructor is varargs-only; set the
 * properties afterwards).
 */
ZEND_METHOD(Gtk4_GdkContentProvider, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = static_cast<GObject *>(g_object_new(GDK_TYPE_CONTENT_PROVIDER, nullptr));
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * static Gtk4\GdkContentProvider::new_for_bytes(string $mime_type, string $bytes):
 * GdkContentProvider
 *
 * Create a content provider that provides the given $bytes as data for the given $mime_type.
 */
ZEND_METHOD(Gtk4_GdkContentProvider, new_for_bytes) {
  zend_string *mime_type;
  zend_string *bytes;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_STR(mime_type)
  Z_PARAM_STR(bytes)
  ZEND_PARSE_PARAMETERS_END();
  if (!phpgtk::check_utf8(mime_type, 1)) RETURN_THROWS();
  GBytes *bytes_b = g_bytes_new(ZSTR_VAL(bytes), ZSTR_LEN(bytes));
  GdkContentProvider *call_result =
      gdk_content_provider_new_for_bytes(ZSTR_VAL(mime_type), bytes_b);
  g_bytes_unref(bytes_b);
  GObject *obj = G_OBJECT(call_result);
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * Gtk4\GdkContentProvider::content_changed(): void
 *
 * Emits the ::content-changed signal.
 */
ZEND_METHOD(Gtk4_GdkContentProvider, content_changed) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkContentProvider *self = PHPGTK_SELF(GdkContentProvider, GDK_TYPE_CONTENT_PROVIDER);
  gdk_content_provider_content_changed(self);
}

/**
 * Gtk4\GdkContentProvider::ref_formats(): GdkContentFormats
 *
 * Gets the formats that the provider can provide its current contents in.
 */
ZEND_METHOD(Gtk4_GdkContentProvider, ref_formats) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkContentProvider *self = PHPGTK_SELF(GdkContentProvider, GDK_TYPE_CONTENT_PROVIDER);
  GdkContentFormats *phpgtk_ret = gdk_content_provider_ref_formats(self);
  wrap_boxed(GDK_TYPE_CONTENT_FORMATS, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(GDK_TYPE_CONTENT_FORMATS, phpgtk_ret);
}

/**
 * Gtk4\GdkContentProvider::ref_storable_formats(): GdkContentFormats
 *
 * Gets the formats that the provider suggests other applications to store the data in.
 */
ZEND_METHOD(Gtk4_GdkContentProvider, ref_storable_formats) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkContentProvider *self = PHPGTK_SELF(GdkContentProvider, GDK_TYPE_CONTENT_PROVIDER);
  GdkContentFormats *phpgtk_ret = gdk_content_provider_ref_storable_formats(self);
  wrap_boxed(GDK_TYPE_CONTENT_FORMATS, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(GDK_TYPE_CONTENT_FORMATS, phpgtk_ret);
}

/**
 * Gtk4\GdkContentProvider::write_mime_type_finish(GAsyncResult $result): bool
 *
 * Finishes an asynchronous write operation.
 */
ZEND_METHOD(Gtk4_GdkContentProvider, write_mime_type_finish) {
  zval *result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  GdkContentProvider *self = PHPGTK_SELF(GdkContentProvider, GDK_TYPE_CONTENT_PROVIDER);
  GObject *result_o = unwrap(result, G_TYPE_ASYNC_RESULT);
  if (result_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  const gboolean ok =
      gdk_content_provider_write_mime_type_finish(self, G_ASYNC_RESULT(result_o), &error);
  if (!ok) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  RETURN_BOOL(ok);
}

/**
 * public function get_value(?string $type = null): mixed
 * The value this provider offers, as $type (the first type it advertises when omitted).
 *
 * GIR's GValue is caller-allocated *and* caller-typed: GDK fills it only if it can supply that
 * type, and asserts on an uninitialised one. So the type is asked for explicitly or taken from
 * the provider's own formats, and the marshaller turns the result back into a PHP value.
 */
ZEND_METHOD(Gtk4_GdkContentProvider, get_value) {
  zend_string *type = nullptr;
  ZEND_PARSE_PARAMETERS_START(0, 1)
  Z_PARAM_OPTIONAL
  Z_PARAM_STR_OR_NULL(type)
  ZEND_PARSE_PARAMETERS_END();
  GdkContentProvider *self = PHPGTK_SELF(GdkContentProvider, GDK_TYPE_CONTENT_PROVIDER);

  GType wanted = 0;
  if (type != nullptr) {
    wanted = gtype_from_php_name(type, 1);
    if (wanted == 0) RETURN_THROWS();
  } else {
    GdkContentFormats *formats = gdk_content_provider_ref_formats(self);
    gsize count = 0;
    const GType *types = gdk_content_formats_get_gtypes(formats, &count);
    if (types != nullptr && count > 0) wanted = types[0];
    gdk_content_formats_unref(formats);
    if (wanted == 0) {
      zend_throw_exception(spl_ce_LogicException,
                           "Gtk4\\GdkContentProvider::get_value(): this provider offers no typed "
                           "value; name the type you want",
                           0);
      RETURN_THROWS();
    }
  }

  GValue gv = G_VALUE_INIT;
  g_value_init(&gv, wanted);
  GError *error = nullptr;
  if (gdk_content_provider_get_value(self, &gv, &error) == FALSE) {
    g_value_unset(&gv);
    throw_gerror(error);
    RETURN_THROWS();
  }
  to_php(&gv, return_value);
  g_value_unset(&gv);
}

/**
 * public static function new_for_value(mixed $value, ?string $type = null): GdkContentProvider
 * A provider offering $value, typed as $type (inferred from $value when omitted).
 *
 * GIR takes a GValue, which PHP has no spelling for; the marshaller converts one either way, so
 * the payload is an ordinary PHP value and the GType is named the way a list store's item type
 * is - "string"/"int"/"float"/"bool" or a registered class name.
 */
ZEND_METHOD(Gtk4_GdkContentProvider, new_for_value) {
  zval *value;
  zend_string *type = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 2)
  Z_PARAM_ZVAL(value)
  Z_PARAM_OPTIONAL
  Z_PARAM_STR_OR_NULL(type)
  ZEND_PARSE_PARAMETERS_END();

  GType t = 0;
  if (type != nullptr) {
    t = gtype_from_php_name(type, 2);
    if (t == 0) RETURN_THROWS();
  } else {
    switch (Z_TYPE_P(value)) {
      case IS_STRING:
        t = G_TYPE_STRING;
        break;
      case IS_LONG:
        t = G_TYPE_INT64;
        break;
      case IS_DOUBLE:
        t = G_TYPE_DOUBLE;
        break;
      case IS_TRUE:
      case IS_FALSE:
        t = G_TYPE_BOOLEAN;
        break;
      case IS_OBJECT:
        t = G_OBJECT_TYPE(unwrap(value, G_TYPE_OBJECT));
        break;
      default:
        zend_argument_type_error(1,
                                 "must be a string, int, float, bool or GObject handle to be "
                                 "offered without naming a type");
        RETURN_THROWS();
    }
    if (EG(exception) != nullptr) RETURN_THROWS();
  }
  GValue gv = G_VALUE_INIT;
  if (!to_gvalue(value, t, &gv)) RETURN_THROWS();
  GdkContentProvider *provider = gdk_content_provider_new_for_value(&gv);
  g_value_unset(&gv);
  wrap(G_OBJECT(provider), return_value);
  g_object_unref(provider);
}

// vfunc thunks and installers: file-local, installed by class_init of a PHP subtype
namespace {

// vfunc thunk: GDK_CONTENT_PROVIDER_CLASS->attach_clipboard -> $this->vfunc_attach_clipboard() on a
// PHP subclass
void vfunc_thunk_attach_clipboard(GdkContentProvider *self, GdkClipboard *clipboard) {
  zval zself;
  zend_function *fn = EG(exception) == nullptr
                          ? subtype_vfunc(G_OBJECT(self), "vfunc_attach_clipboard", &zself)
                          : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    auto *native = GDK_CONTENT_PROVIDER_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->attach_clipboard != nullptr) native->attach_clipboard(self, clipboard);
    return;
  }
  std::array<zval, 1> args{};
  zval *argv = args.data();
  wrap(clipboard != nullptr ? G_OBJECT(clipboard) : nullptr, &argv[0]);
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GdkContentProvider::vfunc_attach_clipboard");
}

// vfunc installer: GDK_CONTENT_PROVIDER_CLASS->attach_clipboard (called from class_init /
// iface_init of a PHP subtype)
void vfunc_install_attach_clipboard(gpointer klass) {
  GDK_CONTENT_PROVIDER_CLASS(klass)->attach_clipboard = vfunc_thunk_attach_clipboard;
}

// vfunc thunk: GDK_CONTENT_PROVIDER_CLASS->content_changed -> $this->vfunc_content_changed() on a
// PHP subclass
void vfunc_thunk_content_changed(GdkContentProvider *self) {
  zval zself;
  zend_function *fn = EG(exception) == nullptr
                          ? subtype_vfunc(G_OBJECT(self), "vfunc_content_changed", &zself)
                          : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    auto *native = GDK_CONTENT_PROVIDER_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->content_changed != nullptr) native->content_changed(self);
    return;
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GdkContentProvider::vfunc_content_changed");
}

// vfunc installer: GDK_CONTENT_PROVIDER_CLASS->content_changed (called from class_init / iface_init
// of a PHP subtype)
void vfunc_install_content_changed(gpointer klass) {
  GDK_CONTENT_PROVIDER_CLASS(klass)->content_changed = vfunc_thunk_content_changed;
}

// vfunc thunk: GDK_CONTENT_PROVIDER_CLASS->detach_clipboard -> $this->vfunc_detach_clipboard() on a
// PHP subclass
void vfunc_thunk_detach_clipboard(GdkContentProvider *self, GdkClipboard *clipboard) {
  zval zself;
  zend_function *fn = EG(exception) == nullptr
                          ? subtype_vfunc(G_OBJECT(self), "vfunc_detach_clipboard", &zself)
                          : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    auto *native = GDK_CONTENT_PROVIDER_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->detach_clipboard != nullptr) native->detach_clipboard(self, clipboard);
    return;
  }
  std::array<zval, 1> args{};
  zval *argv = args.data();
  wrap(clipboard != nullptr ? G_OBJECT(clipboard) : nullptr, &argv[0]);
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GdkContentProvider::vfunc_detach_clipboard");
}

// vfunc installer: GDK_CONTENT_PROVIDER_CLASS->detach_clipboard (called from class_init /
// iface_init of a PHP subtype)
void vfunc_install_detach_clipboard(gpointer klass) {
  GDK_CONTENT_PROVIDER_CLASS(klass)->detach_clipboard = vfunc_thunk_detach_clipboard;
}

}  // namespace

/**
 * Gtk4\GdkContentProvider::vfunc_attach_clipboard(GdkClipboard $clipboard): void
 *
 * Native `attach_clipboard` (ContentProviderClass.attach_clipboard): the GTK implementation below
 * any PHP subclass, for `parent::vfunc_attach_clipboard()` from an override.
 */
ZEND_METHOD(Gtk4_GdkContentProvider, vfunc_attach_clipboard) {
  zval *clipboard;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(clipboard, class_for_gtype(GDK_TYPE_CLIPBOARD))
  ZEND_PARSE_PARAMETERS_END();
  GdkContentProvider *self = PHPGTK_SELF(GdkContentProvider, GDK_TYPE_CONTENT_PROVIDER);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GdkContentProvider::vfunc_attach_clipboard(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GDK_CONTENT_PROVIDER_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->attach_clipboard == nullptr) {
    return;
  }
  GObject *clipboard_o = unwrap(clipboard, GDK_TYPE_CLIPBOARD);
  if (clipboard_o == nullptr) RETURN_THROWS();
  klass->attach_clipboard(self, GDK_CLIPBOARD(clipboard_o));
}

/**
 * Gtk4\GdkContentProvider::vfunc_content_changed(): void
 *
 * Native `content_changed` (ContentProviderClass.content_changed): the GTK implementation below
 * any PHP subclass, for `parent::vfunc_content_changed()` from an override. Emits the
 * ::content-changed signal.
 */
ZEND_METHOD(Gtk4_GdkContentProvider, vfunc_content_changed) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkContentProvider *self = PHPGTK_SELF(GdkContentProvider, GDK_TYPE_CONTENT_PROVIDER);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GdkContentProvider::vfunc_content_changed(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GDK_CONTENT_PROVIDER_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->content_changed == nullptr) {
    return;
  }
  klass->content_changed(self);
}

/**
 * Gtk4\GdkContentProvider::vfunc_detach_clipboard(GdkClipboard $clipboard): void
 *
 * Native `detach_clipboard` (ContentProviderClass.detach_clipboard): the GTK implementation below
 * any PHP subclass, for `parent::vfunc_detach_clipboard()` from an override.
 */
ZEND_METHOD(Gtk4_GdkContentProvider, vfunc_detach_clipboard) {
  zval *clipboard;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(clipboard, class_for_gtype(GDK_TYPE_CLIPBOARD))
  ZEND_PARSE_PARAMETERS_END();
  GdkContentProvider *self = PHPGTK_SELF(GdkContentProvider, GDK_TYPE_CONTENT_PROVIDER);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GdkContentProvider::vfunc_detach_clipboard(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GDK_CONTENT_PROVIDER_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->detach_clipboard == nullptr) {
    return;
  }
  GObject *clipboard_o = unwrap(clipboard, GDK_TYPE_CLIPBOARD);
  if (clipboard_o == nullptr) RETURN_THROWS();
  klass->detach_clipboard(self, GDK_CLIPBOARD(clipboard_o));
}

// MINIT: the vfunc thunks of GdkContentProvider (core/subtype.h).
void register_vfuncs_GdkContentProvider() {
  register_vfunc(GDK_TYPE_CONTENT_PROVIDER, "attach_clipboard", vfunc_install_attach_clipboard);
  register_vfunc(GDK_TYPE_CONTENT_PROVIDER, "content_changed", vfunc_install_content_changed);
  register_vfunc(GDK_TYPE_CONTENT_PROVIDER, "detach_clipboard", vfunc_install_detach_clipboard);
}
