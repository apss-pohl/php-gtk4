// GENERATED by gen/gir.php from Gdk.Texture - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GdkTexture
#include "php_gtk4.h"
#include "core/object.h"
#include "core/enums.h"
#include "core/gerror.h"
#include "core/subtype.h"

using namespace phpgtk;

/**
 * Gtk4\GdkTexture::__construct()
 *
 * GdkTexture is abstract in GTK: `new` only works on a PHP subclass (which gets its own GType).
 */
ZEND_METHOD(Gtk4_GdkTexture, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) == nullptr) {
      zend_throw_error(nullptr,
                       "GdkTexture is abstract in GTK: subclass it in PHP (new MyClass())");
    }
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * static Gtk4\GdkTexture::new_from_bytes(string $bytes): GdkTexture
 *
 * Creates a new texture by loading an image from memory,
 */
ZEND_METHOD(Gtk4_GdkTexture, new_from_bytes) {
  zend_string *bytes;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(bytes)
  ZEND_PARSE_PARAMETERS_END();
  GBytes *bytes_b = g_bytes_new(ZSTR_VAL(bytes), ZSTR_LEN(bytes));
  GError *error = nullptr;
  GdkTexture *call_result = gdk_texture_new_from_bytes(bytes_b, &error);
  g_bytes_unref(bytes_b);
  GObject *obj = G_OBJECT(call_result);
  if (obj == nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * static Gtk4\GdkTexture::new_from_filename(string $path): GdkTexture
 *
 * Creates a new texture by loading an image from a file.
 */
ZEND_METHOD(Gtk4_GdkTexture, new_from_filename) {
  zend_string *path;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_PATH_STR(path)
  ZEND_PARSE_PARAMETERS_END();
  GError *error = nullptr;
  GObject *obj = G_OBJECT(gdk_texture_new_from_filename(ZSTR_VAL(path), &error));
  if (obj == nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * Gtk4\GdkTexture::get_format(): GdkMemoryFormat
 *
 * Gets the memory format most closely associated with the data of the texture.
 */
ZEND_METHOD(Gtk4_GdkTexture, get_format) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkTexture *self = PHPGTK_SELF(GdkTexture, GDK_TYPE_TEXTURE);
  enum_to_php(GDK_TYPE_MEMORY_FORMAT, gdk_texture_get_format(self), return_value);
}

/**
 * Gtk4\GdkTexture::get_height(): int
 *
 * Returns the height of the $texture, in pixels.
 */
ZEND_METHOD(Gtk4_GdkTexture, get_height) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkTexture *self = PHPGTK_SELF(GdkTexture, GDK_TYPE_TEXTURE);
  RETURN_LONG(static_cast<zend_long>(gdk_texture_get_height(self)));
}

/**
 * Gtk4\GdkTexture::get_width(): int
 *
 * Returns the width of $texture, in pixels.
 */
ZEND_METHOD(Gtk4_GdkTexture, get_width) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkTexture *self = PHPGTK_SELF(GdkTexture, GDK_TYPE_TEXTURE);
  RETURN_LONG(static_cast<zend_long>(gdk_texture_get_width(self)));
}

/**
 * Gtk4\GdkTexture::save_to_png(string $filename): bool
 *
 * Store the given $texture to the $filename as a PNG file.
 */
ZEND_METHOD(Gtk4_GdkTexture, save_to_png) {
  zend_string *filename;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_PATH_STR(filename)
  ZEND_PARSE_PARAMETERS_END();
  GdkTexture *self = PHPGTK_SELF(GdkTexture, GDK_TYPE_TEXTURE);
  RETURN_BOOL(gdk_texture_save_to_png(self, ZSTR_VAL(filename)));
}

/**
 * Gtk4\GdkTexture::save_to_png_bytes(): string
 *
 * Store the given $texture in memory as a PNG file.
 */
ZEND_METHOD(Gtk4_GdkTexture, save_to_png_bytes) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkTexture *self = PHPGTK_SELF(GdkTexture, GDK_TYPE_TEXTURE);
  GBytes *bytes = gdk_texture_save_to_png_bytes(self);
  gsize size = 0;
  const auto *data = static_cast<const char *>(g_bytes_get_data(bytes, &size));
  RETVAL_STRINGL(data, size);
  g_bytes_unref(bytes);
}

/**
 * Gtk4\GdkTexture::save_to_tiff(string $filename): bool
 *
 * Store the given $texture to the $filename as a TIFF file.
 */
ZEND_METHOD(Gtk4_GdkTexture, save_to_tiff) {
  zend_string *filename;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_PATH_STR(filename)
  ZEND_PARSE_PARAMETERS_END();
  GdkTexture *self = PHPGTK_SELF(GdkTexture, GDK_TYPE_TEXTURE);
  RETURN_BOOL(gdk_texture_save_to_tiff(self, ZSTR_VAL(filename)));
}

/**
 * Gtk4\GdkTexture::save_to_tiff_bytes(): string
 *
 * Store the given $texture in memory as a TIFF file.
 */
ZEND_METHOD(Gtk4_GdkTexture, save_to_tiff_bytes) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkTexture *self = PHPGTK_SELF(GdkTexture, GDK_TYPE_TEXTURE);
  GBytes *bytes = gdk_texture_save_to_tiff_bytes(self);
  gsize size = 0;
  const auto *data = static_cast<const char *>(g_bytes_get_data(bytes, &size));
  RETVAL_STRINGL(data, size);
  g_bytes_unref(bytes);
}
