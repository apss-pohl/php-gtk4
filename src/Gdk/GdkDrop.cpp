// GENERATED by gen/gir.php from Gdk.Drop - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GdkDrop
#include "php_gtk4.h"
#include "core/object.h"
#include "core/collections.h"
#include "core/boxed.h"
#include "core/callback.h"
#include <array>

using namespace phpgtk;

/**
 * Gtk4\GdkDrop::__construct()
 *
 * GdkDrop is abstract in GTK: instances come from GTK, never from `new`.
 */
ZEND_METHOD(Gtk4_GdkDrop, __construct) {
  // Private: never called (object_init_ex() in wrap() skips constructors).
  ZEND_PARSE_PARAMETERS_NONE();
}

/**
 * Gtk4\GdkDrop::finish(int $action): void
 *
 * Ends the drag operation after a drop.
 */
ZEND_METHOD(Gtk4_GdkDrop, finish) {
  zend_long action;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(action)
  ZEND_PARSE_PARAMETERS_END();
  GdkDrop *self = PHPGTK_SELF(GdkDrop, GDK_TYPE_DROP);
  if (!phpgtk::check_flags(GDK_TYPE_DRAG_ACTION, action, 1)) RETURN_THROWS();
  gdk_drop_finish(self, static_cast<GdkDragAction>(action));
}

/**
 * Gtk4\GdkDrop::get_actions(): int
 *
 * Returns the possible actions for this `GdkDrop`.
 */
ZEND_METHOD(Gtk4_GdkDrop, get_actions) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkDrop *self = PHPGTK_SELF(GdkDrop, GDK_TYPE_DROP);
  RETURN_LONG(static_cast<zend_long>(gdk_drop_get_actions(self)));
}

/**
 * Gtk4\GdkDrop::get_device(): GdkDevice
 *
 * Returns the `GdkDevice` performing the drop.
 */
ZEND_METHOD(Gtk4_GdkDrop, get_device) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkDrop *self = PHPGTK_SELF(GdkDrop, GDK_TYPE_DROP);
  GdkDevice *phpgtk_ret = gdk_drop_get_device(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GdkDrop::get_display(): GdkDisplay
 *
 * Gets the `GdkDisplay` that $self was created for.
 */
ZEND_METHOD(Gtk4_GdkDrop, get_display) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkDrop *self = PHPGTK_SELF(GdkDrop, GDK_TYPE_DROP);
  GdkDisplay *phpgtk_ret = gdk_drop_get_display(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GdkDrop::get_drag(): ?GdkDrag
 *
 * If this is an in-app drag-and-drop operation, returns the `GdkDrag` that corresponds to this
 * drop.
 */
ZEND_METHOD(Gtk4_GdkDrop, get_drag) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkDrop *self = PHPGTK_SELF(GdkDrop, GDK_TYPE_DROP);
  GdkDrag *phpgtk_ret = gdk_drop_get_drag(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GdkDrop::get_formats(): GdkContentFormats
 *
 * Returns the `GdkContentFormats` that the drop offers the data to be read in.
 */
ZEND_METHOD(Gtk4_GdkDrop, get_formats) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkDrop *self = PHPGTK_SELF(GdkDrop, GDK_TYPE_DROP);
  GdkContentFormats *phpgtk_ret = gdk_drop_get_formats(self);
  wrap_boxed(GDK_TYPE_CONTENT_FORMATS, phpgtk_ret, return_value);
}

/**
 * Gtk4\GdkDrop::get_surface(): GdkSurface
 *
 * Returns the `GdkSurface` performing the drop.
 */
ZEND_METHOD(Gtk4_GdkDrop, get_surface) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkDrop *self = PHPGTK_SELF(GdkDrop, GDK_TYPE_DROP);
  GdkSurface *phpgtk_ret = gdk_drop_get_surface(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

namespace {
// AsyncReadyCallback trampoline for GdkDrop::read_async(): wraps the C arguments, invokes the PHP
// callable once and releases it (async scope).
void cb_read_async_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\GdkDrop::read_async(array $mime_types, int $io_priority, ?GCancellable $cancellable,
 * ?callable $callback): void
 *
 * Asynchronously read the dropped data from a `GdkDrop` in a format that complies with one of the
 * mime types.
 */
ZEND_METHOD(Gtk4_GdkDrop, read_async) {
  zval *mime_types;
  zend_long io_priority;
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(4, 4)
  Z_PARAM_ARRAY(mime_types)
  Z_PARAM_LONG(io_priority)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC_OR_NULL(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  GdkDrop *self = PHPGTK_SELF(GdkDrop, GDK_TYPE_DROP);
  char **mime_types_v = strv_from_php(mime_types);
  if (mime_types_v == nullptr) RETURN_THROWS();
  if (!phpgtk::check_range<int>(io_priority, 2)) RETURN_THROWS();
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback = ZEND_FCI_INITIALIZED(fci_callback)
                              ? callback_new(&fci_callback.function_name, "GdkDrop::read_async")
                              : nullptr;
  gdk_drop_read_async(self, const_cast<const char **>(mime_types_v), static_cast<int>(io_priority),
                      cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
                      cb_callback != nullptr ? cb_read_async_callback : nullptr, cb_callback);
  g_strfreev(mime_types_v);
}

/**
 * Gtk4\GdkDrop::status(int $actions, int $preferred): void
 *
 * Selects all actions that are potentially supported by the destination.
 */
ZEND_METHOD(Gtk4_GdkDrop, status) {
  zend_long actions;
  zend_long preferred;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_LONG(actions)
  Z_PARAM_LONG(preferred)
  ZEND_PARSE_PARAMETERS_END();
  GdkDrop *self = PHPGTK_SELF(GdkDrop, GDK_TYPE_DROP);
  if (!phpgtk::check_flags(GDK_TYPE_DRAG_ACTION, actions, 1)) RETURN_THROWS();
  if (!phpgtk::check_flags(GDK_TYPE_DRAG_ACTION, preferred, 2)) RETURN_THROWS();
  gdk_drop_status(self, static_cast<GdkDragAction>(actions), static_cast<GdkDragAction>(preferred));
}
