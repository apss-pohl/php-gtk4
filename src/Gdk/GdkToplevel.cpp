// GENERATED by gen/gir.php from Gdk.Toplevel - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GdkToplevel
#include "php_gtk4.h"
#include "core/object.h"
#include "core/enums.h"
#include "core/boxed.h"
#include "core/fundamental.h"

using namespace phpgtk;

/**
 * Gtk4\GdkToplevel::begin_move(GdkDevice $device, int $button, float $x, float $y, int $timestamp):
 * void
 *
 * Begins an interactive move operation.
 */
ZEND_METHOD(Gtk4_GdkToplevel, begin_move) {
  zval *device;
  zend_long button;
  double x;
  double y;
  zend_long timestamp;
  ZEND_PARSE_PARAMETERS_START(5, 5)
  Z_PARAM_OBJECT_OF_CLASS(device, class_for_gtype(GDK_TYPE_DEVICE))
  Z_PARAM_LONG(button)
  Z_PARAM_DOUBLE(x)
  Z_PARAM_DOUBLE(y)
  Z_PARAM_LONG(timestamp)
  ZEND_PARSE_PARAMETERS_END();
  GdkToplevel *self = PHPGTK_SELF(GdkToplevel, GDK_TYPE_TOPLEVEL);
  GObject *device_o = unwrap(device, GDK_TYPE_DEVICE);
  if (device_o == nullptr) RETURN_THROWS();
  if (!phpgtk::check_range<int>(button, 2)) RETURN_THROWS();
  if (!phpgtk::check_range<guint32>(timestamp, 5)) RETURN_THROWS();
  gdk_toplevel_begin_move(self, GDK_DEVICE(device_o), static_cast<int>(button), x, y,
                          static_cast<guint32>(timestamp));
}

/**
 * Gtk4\GdkToplevel::begin_resize(GdkSurfaceEdge $edge, ?GdkDevice $device, int $button, float $x,
 * float $y, int $timestamp): void
 *
 * Begins an interactive resize operation.
 */
ZEND_METHOD(Gtk4_GdkToplevel, begin_resize) {
  zval *edge;
  zval *device = nullptr;
  zend_long button;
  double x;
  double y;
  zend_long timestamp;
  ZEND_PARSE_PARAMETERS_START(6, 6)
  Z_PARAM_OBJECT_OF_CLASS(edge, enum_class_for_type(GDK_TYPE_SURFACE_EDGE))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(device, class_for_gtype(GDK_TYPE_DEVICE))
  Z_PARAM_LONG(button)
  Z_PARAM_DOUBLE(x)
  Z_PARAM_DOUBLE(y)
  Z_PARAM_LONG(timestamp)
  ZEND_PARSE_PARAMETERS_END();
  GdkToplevel *self = PHPGTK_SELF(GdkToplevel, GDK_TYPE_TOPLEVEL);
  gint edge_v = 0;
  if (!enum_from_php(edge, GDK_TYPE_SURFACE_EDGE, &edge_v)) RETURN_THROWS();
  GObject *device_o = nullptr;
  if (device != nullptr) {
    device_o = unwrap(device, GDK_TYPE_DEVICE);
    if (device_o == nullptr) RETURN_THROWS();
  }
  if (!phpgtk::check_range<int>(button, 3)) RETURN_THROWS();
  if (!phpgtk::check_range<guint32>(timestamp, 6)) RETURN_THROWS();
  gdk_toplevel_begin_resize(self, static_cast<GdkSurfaceEdge>(edge_v),
                            device_o != nullptr ? GDK_DEVICE(device_o) : nullptr,
                            static_cast<int>(button), x, y, static_cast<guint32>(timestamp));
}

/**
 * Gtk4\GdkToplevel::focus(int $timestamp): void
 *
 * Sets keyboard focus to $surface.
 */
ZEND_METHOD(Gtk4_GdkToplevel, focus) {
  zend_long timestamp;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(timestamp)
  ZEND_PARSE_PARAMETERS_END();
  GdkToplevel *self = PHPGTK_SELF(GdkToplevel, GDK_TYPE_TOPLEVEL);
  if (!phpgtk::check_range<guint32>(timestamp, 1)) RETURN_THROWS();
  gdk_toplevel_focus(self, static_cast<guint32>(timestamp));
}

/**
 * Gtk4\GdkToplevel::get_state(): int
 *
 * Gets the bitwise or of the currently active surface state flags, from the `GdkToplevelState`
 * enumeration.
 */
ZEND_METHOD(Gtk4_GdkToplevel, get_state) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkToplevel *self = PHPGTK_SELF(GdkToplevel, GDK_TYPE_TOPLEVEL);
  RETURN_LONG(static_cast<zend_long>(gdk_toplevel_get_state(self)));
}

/**
 * Gtk4\GdkToplevel::inhibit_system_shortcuts(?GdkEvent $event): void
 *
 * Requests that the $toplevel inhibit the system shortcuts.
 */
ZEND_METHOD(Gtk4_GdkToplevel, inhibit_system_shortcuts) {
  zval *event = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(event, fundamental_class_for_type(GDK_TYPE_EVENT)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GdkToplevel *self = PHPGTK_SELF(GdkToplevel, GDK_TYPE_TOPLEVEL);
  gpointer event_f = nullptr;
  if (event != nullptr) {
    event_f = unwrap_fundamental(event, GDK_TYPE_EVENT);
    if (event_f == nullptr) RETURN_THROWS();
  }
  gdk_toplevel_inhibit_system_shortcuts(self, static_cast<GdkEvent *>(event_f));
}

/**
 * Gtk4\GdkToplevel::lower(): bool
 *
 * Asks to lower the $toplevel below other windows.
 */
ZEND_METHOD(Gtk4_GdkToplevel, lower) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkToplevel *self = PHPGTK_SELF(GdkToplevel, GDK_TYPE_TOPLEVEL);
  RETURN_BOOL(gdk_toplevel_lower(self));
}

/**
 * Gtk4\GdkToplevel::minimize(): bool
 *
 * Asks to minimize the $toplevel.
 */
ZEND_METHOD(Gtk4_GdkToplevel, minimize) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkToplevel *self = PHPGTK_SELF(GdkToplevel, GDK_TYPE_TOPLEVEL);
  RETURN_BOOL(gdk_toplevel_minimize(self));
}

/**
 * Gtk4\GdkToplevel::present(GdkToplevelLayout $layout): void
 *
 * Present $toplevel after having processed the `GdkToplevelLayout` rules.
 */
ZEND_METHOD(Gtk4_GdkToplevel, present) {
  zval *layout;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(layout, boxed_class_for_type(GDK_TYPE_TOPLEVEL_LAYOUT)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GdkToplevel *self = PHPGTK_SELF(GdkToplevel, GDK_TYPE_TOPLEVEL);
  gpointer layout_b = unwrap_boxed(layout, GDK_TYPE_TOPLEVEL_LAYOUT);
  if (layout_b == nullptr) RETURN_THROWS();
  gdk_toplevel_present(self, static_cast<GdkToplevelLayout *>(layout_b));
}

/**
 * Gtk4\GdkToplevel::restore_system_shortcuts(): void
 *
 * Restore default system keyboard shortcuts which were previously inhibited.
 */
ZEND_METHOD(Gtk4_GdkToplevel, restore_system_shortcuts) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkToplevel *self = PHPGTK_SELF(GdkToplevel, GDK_TYPE_TOPLEVEL);
  gdk_toplevel_restore_system_shortcuts(self);
}

/**
 * Gtk4\GdkToplevel::set_decorated(bool $decorated): void
 *
 * Sets the toplevel to be decorated.
 */
ZEND_METHOD(Gtk4_GdkToplevel, set_decorated) {
  bool decorated;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(decorated)
  ZEND_PARSE_PARAMETERS_END();
  GdkToplevel *self = PHPGTK_SELF(GdkToplevel, GDK_TYPE_TOPLEVEL);
  gdk_toplevel_set_decorated(self, decorated);
}

/**
 * Gtk4\GdkToplevel::set_deletable(bool $deletable): void
 *
 * Sets the toplevel to be deletable.
 */
ZEND_METHOD(Gtk4_GdkToplevel, set_deletable) {
  bool deletable;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(deletable)
  ZEND_PARSE_PARAMETERS_END();
  GdkToplevel *self = PHPGTK_SELF(GdkToplevel, GDK_TYPE_TOPLEVEL);
  gdk_toplevel_set_deletable(self, deletable);
}

/**
 * Gtk4\GdkToplevel::set_startup_id(string $startup_id): void
 *
 * Sets the startup notification ID.
 */
ZEND_METHOD(Gtk4_GdkToplevel, set_startup_id) {
  zend_string *startup_id;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(startup_id)
  ZEND_PARSE_PARAMETERS_END();
  GdkToplevel *self = PHPGTK_SELF(GdkToplevel, GDK_TYPE_TOPLEVEL);
  if (!phpgtk::check_utf8(startup_id, 1)) RETURN_THROWS();
  gdk_toplevel_set_startup_id(self, ZSTR_VAL(startup_id));
}

/**
 * Gtk4\GdkToplevel::set_title(string $title): void
 *
 * Sets the title of a toplevel surface.
 */
ZEND_METHOD(Gtk4_GdkToplevel, set_title) {
  zend_string *title;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(title)
  ZEND_PARSE_PARAMETERS_END();
  GdkToplevel *self = PHPGTK_SELF(GdkToplevel, GDK_TYPE_TOPLEVEL);
  if (!phpgtk::check_utf8(title, 1)) RETURN_THROWS();
  gdk_toplevel_set_title(self, ZSTR_VAL(title));
}

/**
 * Gtk4\GdkToplevel::set_transient_for(GdkSurface $parent): void
 *
 * Sets a transient-for parent.
 */
ZEND_METHOD(Gtk4_GdkToplevel, set_transient_for) {
  zval *parent;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(parent, class_for_gtype(GDK_TYPE_SURFACE))
  ZEND_PARSE_PARAMETERS_END();
  GdkToplevel *self = PHPGTK_SELF(GdkToplevel, GDK_TYPE_TOPLEVEL);
  GObject *parent_o = unwrap(parent, GDK_TYPE_SURFACE);
  if (parent_o == nullptr) RETURN_THROWS();
  gdk_toplevel_set_transient_for(self, GDK_SURFACE(parent_o));
}

/**
 * Gtk4\GdkToplevel::show_window_menu(GdkEvent $event): bool
 *
 * Asks the windowing system to show the window menu.
 */
ZEND_METHOD(Gtk4_GdkToplevel, show_window_menu) {
  zval *event;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(event, fundamental_class_for_type(GDK_TYPE_EVENT)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GdkToplevel *self = PHPGTK_SELF(GdkToplevel, GDK_TYPE_TOPLEVEL);
  gpointer event_f = unwrap_fundamental(event, GDK_TYPE_EVENT);
  if (event_f == nullptr) RETURN_THROWS();
  RETURN_BOOL(gdk_toplevel_show_window_menu(self, static_cast<GdkEvent *>(event_f)));
}

/**
 * Gtk4\GdkToplevel::supports_edge_constraints(): bool
 *
 * Returns whether the desktop environment supports tiled window states.
 */
ZEND_METHOD(Gtk4_GdkToplevel, supports_edge_constraints) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkToplevel *self = PHPGTK_SELF(GdkToplevel, GDK_TYPE_TOPLEVEL);
  RETURN_BOOL(gdk_toplevel_supports_edge_constraints(self));
}

/**
 * Gtk4\GdkToplevel::titlebar_gesture(GdkTitlebarGesture $gesture): bool
 */
ZEND_METHOD(Gtk4_GdkToplevel, titlebar_gesture) {
  zval *gesture;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(gesture, enum_class_for_type(GDK_TYPE_TITLEBAR_GESTURE))
  ZEND_PARSE_PARAMETERS_END();
  GdkToplevel *self = PHPGTK_SELF(GdkToplevel, GDK_TYPE_TOPLEVEL);
  gint gesture_v = 0;
  if (!enum_from_php(gesture, GDK_TYPE_TITLEBAR_GESTURE, &gesture_v)) RETURN_THROWS();
  RETURN_BOOL(gdk_toplevel_titlebar_gesture(self, static_cast<GdkTitlebarGesture>(gesture_v)));
}

/**
 * public function set_icon_list(array $surfaces): void
 * Sets a list of icons for the surface.
 *
 * One of these will be used to represent the surface in iconic form. The icon may be shown in
 * window lists or task bars. Which icon size is shown depends on the window manager. The window
 * manager can scale the icon but setting several size icons can give better image quality.
 * GIR's `GList` of textures is a PHP list of {@see GdkTexture} here (different sizes of the
 * same icon); an empty list unsets the icons. GTK 4 dropped `gtk_window_set_icon()`, so this
 * and {@see GtkWindow::set_icon_name()} are what remain for a window's own icon.
 */
ZEND_METHOD(Gtk4_GdkToplevel, set_icon_list) {
  HashTable *surfaces = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_ARRAY_HT(surfaces)
  ZEND_PARSE_PARAMETERS_END();
  GdkToplevel *self = PHPGTK_SELF(GdkToplevel, GDK_TYPE_TOPLEVEL);
  GList *list = nullptr;
  zval *entry = nullptr;
  // NOLINTNEXTLINE(readability-math-missing-parentheses) Zend macro expansion
  ZEND_HASH_FOREACH_VAL(surfaces, entry) {
    GObject *texture = Z_TYPE_P(entry) == IS_OBJECT ? unwrap(entry, GDK_TYPE_TEXTURE) : nullptr;
    if (texture == nullptr) {
      g_list_free(list);
      if (!EG(exception)) zend_argument_type_error(1, "must be a list of GdkTexture instances");
      RETURN_THROWS();
    }
    list = g_list_prepend(list, texture);
  }
  ZEND_HASH_FOREACH_END();
  list = g_list_reverse(list);
  gdk_toplevel_set_icon_list(self, list);  // GDK copies the list and refs the textures
  g_list_free(list);
}

/**
 * Gtk4\GdkToplevelObject::__construct()
 *
 * Never called: these handles only come from wrap().
 */
ZEND_METHOD(Gtk4_GdkToplevelObject, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
}
