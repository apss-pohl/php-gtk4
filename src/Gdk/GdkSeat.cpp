// GENERATED by gen/gir.php from Gdk.Seat - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GdkSeat
#include "php_gtk4.h"
#include "core/object.h"
#include "core/collections.h"

using namespace phpgtk;

/**
 * Gtk4\GdkSeat::__construct()
 *
 * GdkSeat is abstract in GTK: instances come from GTK, never from `new`.
 */
ZEND_METHOD(Gtk4_GdkSeat, __construct) {
  // Private: never called (object_init_ex() in wrap() skips constructors).
  ZEND_PARSE_PARAMETERS_NONE();
}

/**
 * Gtk4\GdkSeat::get_capabilities(): int
 *
 * Returns the capabilities this `GdkSeat` currently has.
 */
ZEND_METHOD(Gtk4_GdkSeat, get_capabilities) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkSeat *self = PHPGTK_SELF(GdkSeat, GDK_TYPE_SEAT);
  RETURN_LONG(static_cast<zend_long>(gdk_seat_get_capabilities(self)));
}

/**
 * Gtk4\GdkSeat::get_devices(int $capabilities): array
 *
 * Returns the devices that match the given capabilities.
 */
ZEND_METHOD(Gtk4_GdkSeat, get_devices) {
  zend_long capabilities;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(capabilities)
  ZEND_PARSE_PARAMETERS_END();
  GdkSeat *self = PHPGTK_SELF(GdkSeat, GDK_TYPE_SEAT);
  if (!phpgtk::check_flags(GDK_TYPE_SEAT_CAPABILITIES, capabilities, 1)) RETURN_THROWS();
  glist_to_php(gdk_seat_get_devices(self, static_cast<GdkSeatCapabilities>(capabilities)),
               GDK_TYPE_DEVICE, Transfer::Container, return_value);
}

/**
 * Gtk4\GdkSeat::get_display(): GdkDisplay
 *
 * Returns the `GdkDisplay` this seat belongs to.
 */
ZEND_METHOD(Gtk4_GdkSeat, get_display) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkSeat *self = PHPGTK_SELF(GdkSeat, GDK_TYPE_SEAT);
  GdkDisplay *phpgtk_ret = gdk_seat_get_display(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GdkSeat::get_keyboard(): ?GdkDevice
 *
 * Returns the device that routes keyboard events.
 */
ZEND_METHOD(Gtk4_GdkSeat, get_keyboard) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkSeat *self = PHPGTK_SELF(GdkSeat, GDK_TYPE_SEAT);
  GdkDevice *phpgtk_ret = gdk_seat_get_keyboard(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GdkSeat::get_pointer(): ?GdkDevice
 *
 * Returns the device that routes pointer events.
 */
ZEND_METHOD(Gtk4_GdkSeat, get_pointer) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkSeat *self = PHPGTK_SELF(GdkSeat, GDK_TYPE_SEAT);
  GdkDevice *phpgtk_ret = gdk_seat_get_pointer(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}
