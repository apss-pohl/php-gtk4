// GENERATED by gen/gir.php from Gdk.Surface - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GdkSurface
#include "php_gtk4.h"
#include "core/object.h"

using namespace phpgtk;

/**
 * Gtk4\GdkSurface::__construct()
 *
 * GdkSurface is abstract in GTK: instances come from GTK, never from `new`.
 */
ZEND_METHOD(Gtk4_GdkSurface, __construct) {
  // Private: never called (object_init_ex() in wrap() skips constructors).
  ZEND_PARSE_PARAMETERS_NONE();
}

/**
 * static Gtk4\GdkSurface::new_popup(GdkSurface $parent, bool $autohide): GdkSurface
 *
 * Create a new popup surface.
 */
ZEND_METHOD(Gtk4_GdkSurface, new_popup) {
  zval *parent;
  bool autohide;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(parent, class_for_gtype(GDK_TYPE_SURFACE))
  Z_PARAM_BOOL(autohide)
  ZEND_PARSE_PARAMETERS_END();
  GObject *parent_o = unwrap(parent, GDK_TYPE_SURFACE);
  if (parent_o == nullptr) RETURN_THROWS();
  GObject *obj = G_OBJECT(gdk_surface_new_popup(GDK_SURFACE(parent_o), autohide));
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * static Gtk4\GdkSurface::new_toplevel(GdkDisplay $display): GdkSurface
 *
 * Creates a new toplevel surface.
 */
ZEND_METHOD(Gtk4_GdkSurface, new_toplevel) {
  zval *display;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(display, class_for_gtype(GDK_TYPE_DISPLAY))
  ZEND_PARSE_PARAMETERS_END();
  GObject *display_o = unwrap(display, GDK_TYPE_DISPLAY);
  if (display_o == nullptr) RETURN_THROWS();
  GObject *obj = G_OBJECT(gdk_surface_new_toplevel(GDK_DISPLAY(display_o)));
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * Gtk4\GdkSurface::beep(): void
 *
 * Emits a short beep associated to $surface.
 */
ZEND_METHOD(Gtk4_GdkSurface, beep) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkSurface *self = PHPGTK_SELF(GdkSurface, GDK_TYPE_SURFACE);
  gdk_surface_beep(self);
}

/**
 * Gtk4\GdkSurface::destroy(): void
 *
 * Destroys the window system resources associated with $surface and decrements $surface's
 * reference count.
 */
ZEND_METHOD(Gtk4_GdkSurface, destroy) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkSurface *self = PHPGTK_SELF(GdkSurface, GDK_TYPE_SURFACE);
  gdk_surface_destroy(self);
}

/**
 * Gtk4\GdkSurface::get_cursor(): ?GdkCursor
 *
 * Retrieves a `GdkCursor` pointer for the cursor currently set on the `GdkSurface`.
 */
ZEND_METHOD(Gtk4_GdkSurface, get_cursor) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkSurface *self = PHPGTK_SELF(GdkSurface, GDK_TYPE_SURFACE);
  GdkCursor *phpgtk_ret = gdk_surface_get_cursor(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GdkSurface::get_display(): GdkDisplay
 *
 * Gets the `GdkDisplay` associated with a `GdkSurface`.
 */
ZEND_METHOD(Gtk4_GdkSurface, get_display) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkSurface *self = PHPGTK_SELF(GdkSurface, GDK_TYPE_SURFACE);
  GdkDisplay *phpgtk_ret = gdk_surface_get_display(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GdkSurface::get_height(): int
 *
 * Returns the height of the given $surface.
 */
ZEND_METHOD(Gtk4_GdkSurface, get_height) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkSurface *self = PHPGTK_SELF(GdkSurface, GDK_TYPE_SURFACE);
  RETURN_LONG(static_cast<zend_long>(gdk_surface_get_height(self)));
}

/**
 * Gtk4\GdkSurface::get_mapped(): bool
 *
 * Checks whether the surface has been mapped.
 */
ZEND_METHOD(Gtk4_GdkSurface, get_mapped) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkSurface *self = PHPGTK_SELF(GdkSurface, GDK_TYPE_SURFACE);
  RETURN_BOOL(gdk_surface_get_mapped(self));
}

/**
 * Gtk4\GdkSurface::get_scale(): float
 *
 * Returns the internal scale that maps from surface coordinates to the actual device pixels.
 */
ZEND_METHOD(Gtk4_GdkSurface, get_scale) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkSurface *self = PHPGTK_SELF(GdkSurface, GDK_TYPE_SURFACE);
  RETURN_DOUBLE(gdk_surface_get_scale(self));
}

/**
 * Gtk4\GdkSurface::get_scale_factor(): int
 *
 * Returns the internal scale factor that maps from surface coordinates to the actual device
 * pixels.
 */
ZEND_METHOD(Gtk4_GdkSurface, get_scale_factor) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkSurface *self = PHPGTK_SELF(GdkSurface, GDK_TYPE_SURFACE);
  RETURN_LONG(static_cast<zend_long>(gdk_surface_get_scale_factor(self)));
}

/**
 * Gtk4\GdkSurface::get_width(): int
 *
 * Returns the width of the given $surface.
 */
ZEND_METHOD(Gtk4_GdkSurface, get_width) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkSurface *self = PHPGTK_SELF(GdkSurface, GDK_TYPE_SURFACE);
  RETURN_LONG(static_cast<zend_long>(gdk_surface_get_width(self)));
}

/**
 * Gtk4\GdkSurface::hide(): void
 *
 * Hide the surface.
 */
ZEND_METHOD(Gtk4_GdkSurface, hide) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkSurface *self = PHPGTK_SELF(GdkSurface, GDK_TYPE_SURFACE);
  gdk_surface_hide(self);
}

/**
 * Gtk4\GdkSurface::is_destroyed(): bool
 *
 * Check to see if a surface is destroyed.
 */
ZEND_METHOD(Gtk4_GdkSurface, is_destroyed) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkSurface *self = PHPGTK_SELF(GdkSurface, GDK_TYPE_SURFACE);
  RETURN_BOOL(gdk_surface_is_destroyed(self));
}

/**
 * Gtk4\GdkSurface::queue_render(): void
 *
 * Forces a [signal@Gdk.Surface::render] signal emission for $surface to be scheduled.
 */
ZEND_METHOD(Gtk4_GdkSurface, queue_render) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkSurface *self = PHPGTK_SELF(GdkSurface, GDK_TYPE_SURFACE);
  gdk_surface_queue_render(self);
}

/**
 * Gtk4\GdkSurface::request_layout(): void
 *
 * Request a layout phase from the surface's frame clock.
 */
ZEND_METHOD(Gtk4_GdkSurface, request_layout) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkSurface *self = PHPGTK_SELF(GdkSurface, GDK_TYPE_SURFACE);
  gdk_surface_request_layout(self);
}

/**
 * Gtk4\GdkSurface::set_cursor(?GdkCursor $cursor): void
 *
 * Sets the default mouse pointer for a `GdkSurface`.
 */
ZEND_METHOD(Gtk4_GdkSurface, set_cursor) {
  zval *cursor = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cursor, class_for_gtype(GDK_TYPE_CURSOR))
  ZEND_PARSE_PARAMETERS_END();
  GdkSurface *self = PHPGTK_SELF(GdkSurface, GDK_TYPE_SURFACE);
  GObject *cursor_o = nullptr;
  if (cursor != nullptr) {
    cursor_o = unwrap(cursor, GDK_TYPE_CURSOR);
    if (cursor_o == nullptr) RETURN_THROWS();
  }
  gdk_surface_set_cursor(self, cursor_o != nullptr ? GDK_CURSOR(cursor_o) : nullptr);
}
