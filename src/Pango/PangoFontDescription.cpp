// GENERATED by gen/gir.php from Pango.FontDescription - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\PangoFontDescription (boxed value type)
#include "php_gtk4.h"
#include "core/boxed.h"
#include "core/object.h"
#include <cstring>
#include "core/enums.h"
#include "core/variant.h"

using namespace phpgtk;

namespace {

const char *const fields[] = {nullptr};

// Boxed field reader: the public scalar fields as PHP properties.
bool read(gpointer data, const char *field, zval *rv) {
  (void)data;
  (void)field;
  (void)rv;
  return false;
}

// Boxed field writer: coerces to the field's C type.
bool write(gpointer data, const char *field, zval *v) {
  (void)data;
  (void)field;
  (void)v;
  return false;
}

}  // namespace

/**
 * Gtk4\PangoFontDescription::__construct()
 *
 * Creates a new font description structure with all fields unset.
 */
ZEND_METHOD(Gtk4_PangoFontDescription, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  gpointer obj = pango_font_description_new();
  if (obj == nullptr) {
    zend_throw_error(nullptr, "%s(): GTK refused to create the value (see the CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  boxed_adopt(boxed_from_zval(ZEND_THIS), PANGO_TYPE_FONT_DESCRIPTION, obj);
}

/**
 * Gtk4\PangoFontDescription::better_match(?PangoFontDescription $old_match, PangoFontDescription
 * $new_match): bool
 *
 * Determines if the style attributes of $new_match are a closer match for $desc than those of
 * $old_match are, or if $old_match is `null`, determines if $new_match is a match at all.
 */
ZEND_METHOD(Gtk4_PangoFontDescription, better_match) {
  zval *old_match = nullptr;
  zval *new_match;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(old_match, boxed_class_for_type(PANGO_TYPE_FONT_DESCRIPTION)->ce)
  Z_PARAM_OBJECT_OF_CLASS(new_match, boxed_class_for_type(PANGO_TYPE_FONT_DESCRIPTION)->ce)
  ZEND_PARSE_PARAMETERS_END();
  PangoFontDescription *self = PHPGTK_BOXED_SELF(PangoFontDescription);
  gpointer old_match_b = nullptr;
  if (old_match != nullptr) {
    old_match_b = unwrap_boxed(old_match, PANGO_TYPE_FONT_DESCRIPTION);
    if (old_match_b == nullptr) RETURN_THROWS();
  }
  gpointer new_match_b = unwrap_boxed(new_match, PANGO_TYPE_FONT_DESCRIPTION);
  if (new_match_b == nullptr) RETURN_THROWS();
  RETURN_BOOL(
      pango_font_description_better_match(self, static_cast<PangoFontDescription *>(old_match_b),
                                          static_cast<PangoFontDescription *>(new_match_b)));
}

/**
 * Gtk4\PangoFontDescription::copy_static(): ?PangoFontDescription
 *
 * Make a copy of a `PangoFontDescription`, but don't duplicate allocated fields.
 */
ZEND_METHOD(Gtk4_PangoFontDescription, copy_static) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontDescription *self = PHPGTK_BOXED_SELF(PangoFontDescription);
  PangoFontDescription *phpgtk_ret = pango_font_description_copy_static(self);
  wrap_boxed(PANGO_TYPE_FONT_DESCRIPTION, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(PANGO_TYPE_FONT_DESCRIPTION, phpgtk_ret);
}

/**
 * Gtk4\PangoFontDescription::equal(PangoFontDescription $desc2): bool
 *
 * Compares two font descriptions for equality.
 */
ZEND_METHOD(Gtk4_PangoFontDescription, equal) {
  zval *desc2;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(desc2, boxed_class_for_type(PANGO_TYPE_FONT_DESCRIPTION)->ce)
  ZEND_PARSE_PARAMETERS_END();
  PangoFontDescription *self = PHPGTK_BOXED_SELF(PangoFontDescription);
  gpointer desc2_b = unwrap_boxed(desc2, PANGO_TYPE_FONT_DESCRIPTION);
  if (desc2_b == nullptr) RETURN_THROWS();
  RETURN_BOOL(pango_font_description_equal(self, static_cast<PangoFontDescription *>(desc2_b)));
}

/**
 * Gtk4\PangoFontDescription::get_family(): ?string
 *
 * Gets the family name field of a font description.
 */
ZEND_METHOD(Gtk4_PangoFontDescription, get_family) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontDescription *self = PHPGTK_BOXED_SELF(PangoFontDescription);
  PHPGTK_RETURN_STRING_OR_NULL(pango_font_description_get_family(self));
}

/**
 * Gtk4\PangoFontDescription::get_gravity(): PangoGravity
 *
 * Gets the gravity field of a font description.
 */
ZEND_METHOD(Gtk4_PangoFontDescription, get_gravity) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontDescription *self = PHPGTK_BOXED_SELF(PangoFontDescription);
  enum_to_php(PANGO_TYPE_GRAVITY, pango_font_description_get_gravity(self), return_value);
}

/**
 * Gtk4\PangoFontDescription::get_set_fields(): int
 *
 * Determines which fields in a font description have been set.
 */
ZEND_METHOD(Gtk4_PangoFontDescription, get_set_fields) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontDescription *self = PHPGTK_BOXED_SELF(PangoFontDescription);
  RETURN_LONG(static_cast<zend_long>(pango_font_description_get_set_fields(self)));
}

/**
 * Gtk4\PangoFontDescription::get_size(): int
 *
 * Gets the size field of a font description.
 */
ZEND_METHOD(Gtk4_PangoFontDescription, get_size) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontDescription *self = PHPGTK_BOXED_SELF(PangoFontDescription);
  RETURN_LONG(static_cast<zend_long>(pango_font_description_get_size(self)));
}

/**
 * Gtk4\PangoFontDescription::get_size_is_absolute(): bool
 *
 * Determines whether the size of the font is in points (not absolute) or device units (absolute).
 */
ZEND_METHOD(Gtk4_PangoFontDescription, get_size_is_absolute) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontDescription *self = PHPGTK_BOXED_SELF(PangoFontDescription);
  RETURN_BOOL(pango_font_description_get_size_is_absolute(self));
}

/**
 * Gtk4\PangoFontDescription::get_stretch(): PangoStretch
 *
 * Gets the stretch field of a font description.
 */
ZEND_METHOD(Gtk4_PangoFontDescription, get_stretch) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontDescription *self = PHPGTK_BOXED_SELF(PangoFontDescription);
  enum_to_php(PANGO_TYPE_STRETCH, pango_font_description_get_stretch(self), return_value);
}

/**
 * Gtk4\PangoFontDescription::get_style(): PangoStyle
 *
 * Gets the style field of a `PangoFontDescription`.
 */
ZEND_METHOD(Gtk4_PangoFontDescription, get_style) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontDescription *self = PHPGTK_BOXED_SELF(PangoFontDescription);
  enum_to_php(PANGO_TYPE_STYLE, pango_font_description_get_style(self), return_value);
}

/**
 * Gtk4\PangoFontDescription::get_variant(): PangoVariant
 *
 * Gets the variant field of a `PangoFontDescription`.
 */
ZEND_METHOD(Gtk4_PangoFontDescription, get_variant) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontDescription *self = PHPGTK_BOXED_SELF(PangoFontDescription);
  enum_to_php(PANGO_TYPE_VARIANT, pango_font_description_get_variant(self), return_value);
}

/**
 * Gtk4\PangoFontDescription::get_variations(): ?string
 *
 * Gets the variations field of a font description.
 */
ZEND_METHOD(Gtk4_PangoFontDescription, get_variations) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontDescription *self = PHPGTK_BOXED_SELF(PangoFontDescription);
  PHPGTK_RETURN_STRING_OR_NULL(pango_font_description_get_variations(self));
}

/**
 * Gtk4\PangoFontDescription::get_weight(): PangoWeight
 *
 * Gets the weight field of a font description.
 */
ZEND_METHOD(Gtk4_PangoFontDescription, get_weight) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontDescription *self = PHPGTK_BOXED_SELF(PangoFontDescription);
  enum_to_php(PANGO_TYPE_WEIGHT, pango_font_description_get_weight(self), return_value);
}

/**
 * Gtk4\PangoFontDescription::hash(): int
 *
 * Computes a hash of a `PangoFontDescription` structure.
 */
ZEND_METHOD(Gtk4_PangoFontDescription, hash) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontDescription *self = PHPGTK_BOXED_SELF(PangoFontDescription);
  RETURN_LONG(static_cast<zend_long>(pango_font_description_hash(self)));
}

/**
 * Gtk4\PangoFontDescription::merge(?PangoFontDescription $desc_to_merge, bool $replace_existing):
 * void
 *
 * Merges the fields that are set in $desc_to_merge into the fields in $desc.
 */
ZEND_METHOD(Gtk4_PangoFontDescription, merge) {
  zval *desc_to_merge = nullptr;
  bool replace_existing;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(desc_to_merge,
                                  boxed_class_for_type(PANGO_TYPE_FONT_DESCRIPTION)->ce)
  Z_PARAM_BOOL(replace_existing)
  ZEND_PARSE_PARAMETERS_END();
  PangoFontDescription *self = PHPGTK_BOXED_SELF(PangoFontDescription);
  gpointer desc_to_merge_b = nullptr;
  if (desc_to_merge != nullptr) {
    desc_to_merge_b = unwrap_boxed(desc_to_merge, PANGO_TYPE_FONT_DESCRIPTION);
    if (desc_to_merge_b == nullptr) RETURN_THROWS();
  }
  pango_font_description_merge(self, static_cast<PangoFontDescription *>(desc_to_merge_b),
                               replace_existing);
}

/**
 * Gtk4\PangoFontDescription::merge_static(PangoFontDescription $desc_to_merge, bool
 * $replace_existing): void
 *
 * Merges the fields that are set in $desc_to_merge into the fields in $desc, without copying
 * allocated fields.
 */
ZEND_METHOD(Gtk4_PangoFontDescription, merge_static) {
  zval *desc_to_merge;
  bool replace_existing;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(desc_to_merge, boxed_class_for_type(PANGO_TYPE_FONT_DESCRIPTION)->ce)
  Z_PARAM_BOOL(replace_existing)
  ZEND_PARSE_PARAMETERS_END();
  PangoFontDescription *self = PHPGTK_BOXED_SELF(PangoFontDescription);
  gpointer desc_to_merge_b = unwrap_boxed(desc_to_merge, PANGO_TYPE_FONT_DESCRIPTION);
  if (desc_to_merge_b == nullptr) RETURN_THROWS();
  pango_font_description_merge_static(self, static_cast<PangoFontDescription *>(desc_to_merge_b),
                                      replace_existing);
}

/**
 * Gtk4\PangoFontDescription::set_absolute_size(float $size): void
 *
 * Sets the size field of a font description, in device units.
 */
ZEND_METHOD(Gtk4_PangoFontDescription, set_absolute_size) {
  double size;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_DOUBLE(size)
  ZEND_PARSE_PARAMETERS_END();
  PangoFontDescription *self = PHPGTK_BOXED_SELF(PangoFontDescription);
  pango_font_description_set_absolute_size(self, size);
}

/**
 * Gtk4\PangoFontDescription::set_family(string $family): void
 *
 * Sets the family name field of a font description.
 */
ZEND_METHOD(Gtk4_PangoFontDescription, set_family) {
  zend_string *family;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(family)
  ZEND_PARSE_PARAMETERS_END();
  PangoFontDescription *self = PHPGTK_BOXED_SELF(PangoFontDescription);
  if (!phpgtk::check_utf8(family, 1)) RETURN_THROWS();
  pango_font_description_set_family(self, ZSTR_VAL(family));
}

/**
 * Gtk4\PangoFontDescription::set_family_static(string $family): void
 *
 * Sets the family name field of a font description, without copying the string.
 */
ZEND_METHOD(Gtk4_PangoFontDescription, set_family_static) {
  zend_string *family;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(family)
  ZEND_PARSE_PARAMETERS_END();
  PangoFontDescription *self = PHPGTK_BOXED_SELF(PangoFontDescription);
  if (!phpgtk::check_utf8(family, 1)) RETURN_THROWS();
  pango_font_description_set_family_static(self, ZSTR_VAL(family));
}

/**
 * Gtk4\PangoFontDescription::set_gravity(PangoGravity $gravity): void
 *
 * Sets the gravity field of a font description.
 */
ZEND_METHOD(Gtk4_PangoFontDescription, set_gravity) {
  zval *gravity;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(gravity, enum_class_for_type(PANGO_TYPE_GRAVITY))
  ZEND_PARSE_PARAMETERS_END();
  PangoFontDescription *self = PHPGTK_BOXED_SELF(PangoFontDescription);
  gint gravity_v = 0;
  if (!enum_from_php(gravity, PANGO_TYPE_GRAVITY, &gravity_v)) RETURN_THROWS();
  pango_font_description_set_gravity(self, static_cast<PangoGravity>(gravity_v));
}

/**
 * Gtk4\PangoFontDescription::set_size(int $size): void
 *
 * Sets the size field of a font description in fractional points.
 */
ZEND_METHOD(Gtk4_PangoFontDescription, set_size) {
  zend_long size;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(size)
  ZEND_PARSE_PARAMETERS_END();
  PangoFontDescription *self = PHPGTK_BOXED_SELF(PangoFontDescription);
  if (!phpgtk::check_range<gint>(size, 1)) RETURN_THROWS();
  pango_font_description_set_size(self, static_cast<gint>(size));
}

/**
 * Gtk4\PangoFontDescription::set_stretch(PangoStretch $stretch): void
 *
 * Sets the stretch field of a font description.
 */
ZEND_METHOD(Gtk4_PangoFontDescription, set_stretch) {
  zval *stretch;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(stretch, enum_class_for_type(PANGO_TYPE_STRETCH))
  ZEND_PARSE_PARAMETERS_END();
  PangoFontDescription *self = PHPGTK_BOXED_SELF(PangoFontDescription);
  gint stretch_v = 0;
  if (!enum_from_php(stretch, PANGO_TYPE_STRETCH, &stretch_v)) RETURN_THROWS();
  pango_font_description_set_stretch(self, static_cast<PangoStretch>(stretch_v));
}

/**
 * Gtk4\PangoFontDescription::set_style(PangoStyle $style): void
 *
 * Sets the style field of a `PangoFontDescription`.
 */
ZEND_METHOD(Gtk4_PangoFontDescription, set_style) {
  zval *style;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(style, enum_class_for_type(PANGO_TYPE_STYLE))
  ZEND_PARSE_PARAMETERS_END();
  PangoFontDescription *self = PHPGTK_BOXED_SELF(PangoFontDescription);
  gint style_v = 0;
  if (!enum_from_php(style, PANGO_TYPE_STYLE, &style_v)) RETURN_THROWS();
  pango_font_description_set_style(self, static_cast<PangoStyle>(style_v));
}

/**
 * Gtk4\PangoFontDescription::set_variant(PangoVariant $variant): void
 *
 * Sets the variant field of a font description.
 */
ZEND_METHOD(Gtk4_PangoFontDescription, set_variant) {
  zval *variant;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(variant, enum_class_for_type(PANGO_TYPE_VARIANT))
  ZEND_PARSE_PARAMETERS_END();
  PangoFontDescription *self = PHPGTK_BOXED_SELF(PangoFontDescription);
  gint variant_v = 0;
  if (!enum_from_php(variant, PANGO_TYPE_VARIANT, &variant_v)) RETURN_THROWS();
  pango_font_description_set_variant(self, static_cast<PangoVariant>(variant_v));
}

/**
 * Gtk4\PangoFontDescription::set_variations(?string $variations): void
 *
 * Sets the variations field of a font description.
 */
ZEND_METHOD(Gtk4_PangoFontDescription, set_variations) {
  zend_string *variations = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR_OR_NULL(variations)
  ZEND_PARSE_PARAMETERS_END();
  PangoFontDescription *self = PHPGTK_BOXED_SELF(PangoFontDescription);
  if (variations != nullptr && !phpgtk::check_utf8(variations, 1)) RETURN_THROWS();
  pango_font_description_set_variations(self,
                                        variations != nullptr ? ZSTR_VAL(variations) : nullptr);
}

/**
 * Gtk4\PangoFontDescription::set_variations_static(string $variations): void
 *
 * Sets the variations field of a font description.
 */
ZEND_METHOD(Gtk4_PangoFontDescription, set_variations_static) {
  zend_string *variations;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(variations)
  ZEND_PARSE_PARAMETERS_END();
  PangoFontDescription *self = PHPGTK_BOXED_SELF(PangoFontDescription);
  if (!phpgtk::check_utf8(variations, 1)) RETURN_THROWS();
  pango_font_description_set_variations_static(self, ZSTR_VAL(variations));
}

/**
 * Gtk4\PangoFontDescription::set_weight(PangoWeight $weight): void
 *
 * Sets the weight field of a font description.
 */
ZEND_METHOD(Gtk4_PangoFontDescription, set_weight) {
  zval *weight;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(weight, enum_class_for_type(PANGO_TYPE_WEIGHT))
  ZEND_PARSE_PARAMETERS_END();
  PangoFontDescription *self = PHPGTK_BOXED_SELF(PangoFontDescription);
  gint weight_v = 0;
  if (!enum_from_php(weight, PANGO_TYPE_WEIGHT, &weight_v)) RETURN_THROWS();
  pango_font_description_set_weight(self, static_cast<PangoWeight>(weight_v));
}

/**
 * Gtk4\PangoFontDescription::to_filename(): ?string
 *
 * Creates a filename representation of a font description.
 */
ZEND_METHOD(Gtk4_PangoFontDescription, to_filename) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontDescription *self = PHPGTK_BOXED_SELF(PangoFontDescription);
  char *phpgtk_ret = pango_font_description_to_filename(self);
  if (phpgtk_ret == nullptr) RETURN_NULL();
  RETVAL_STRING(phpgtk_ret);
  g_free(phpgtk_ret);
}

/**
 * Gtk4\PangoFontDescription::to_string(): string
 *
 * Creates a string representation of a font description.
 */
ZEND_METHOD(Gtk4_PangoFontDescription, to_string) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontDescription *self = PHPGTK_BOXED_SELF(PangoFontDescription);
  char *phpgtk_ret = pango_font_description_to_string(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETVAL_STRING(phpgtk_ret);
  g_free(phpgtk_ret);
}

/**
 * Gtk4\PangoFontDescription::unset_fields(int $to_unset): void
 *
 * Unsets some of the fields in a `PangoFontDescription`.
 */
ZEND_METHOD(Gtk4_PangoFontDescription, unset_fields) {
  zend_long to_unset;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(to_unset)
  ZEND_PARSE_PARAMETERS_END();
  PangoFontDescription *self = PHPGTK_BOXED_SELF(PangoFontDescription);
  if (!phpgtk::check_flags(PANGO_TYPE_FONT_MASK, to_unset, 1)) RETURN_THROWS();
  pango_font_description_unset_fields(self, static_cast<PangoFontMask>(to_unset));
}

/**
 * static Gtk4\PangoFontDescription::from_string(string $str): PangoFontDescription
 *
 * Creates a new font description from a string representation.
 */
ZEND_METHOD(Gtk4_PangoFontDescription, from_string) {
  zend_string *str;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(str)
  ZEND_PARSE_PARAMETERS_END();
  if (!phpgtk::check_utf8(str, 1)) RETURN_THROWS();
  PangoFontDescription *phpgtk_ret = pango_font_description_from_string(ZSTR_VAL(str));
  wrap_boxed(PANGO_TYPE_FONT_DESCRIPTION, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(PANGO_TYPE_FONT_DESCRIPTION, phpgtk_ret);
}

namespace phpgtk {
// MINIT: bind the PHP class to PANGO_TYPE_FONT_DESCRIPTION with its field table.
void register_PangoFontDescription(zend_class_entry *ce) {
  register_boxed(BoxedClass{.type = PANGO_TYPE_FONT_DESCRIPTION,
                            .ce = ce,
                            .fields = fields,
                            .read = read,
                            .write = write,
                            .equal = [](gconstpointer a, gconstpointer b) {
                              return pango_font_description_equal(
                                         static_cast<const PangoFontDescription *>(a),
                                         static_cast<const PangoFontDescription *>(b)) != FALSE;
                            }});
}
}  // namespace phpgtk
