// GENERATED by gen/gir.php from Pango.FontMap - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\PangoFontMap
#include "php_gtk4.h"
#include "core/object.h"
#include "core/subtype.h"
#include "core/error.h"

using namespace phpgtk;

/**
 * Gtk4\PangoFontMap::__construct()
 *
 * PangoFontMap is abstract in GTK: instances come from GTK, never from `new`.
 */
ZEND_METHOD(Gtk4_PangoFontMap, __construct) {
  // Private: never called (object_init_ex() in wrap() skips constructors).
  ZEND_PARSE_PARAMETERS_NONE();
}

/**
 * Gtk4\PangoFontMap::changed(): void
 *
 * Forces a change in the context, which will cause any `PangoContext` using this fontmap to
 * change.
 */
ZEND_METHOD(Gtk4_PangoFontMap, changed) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontMap *self = PHPGTK_SELF(PangoFontMap, PANGO_TYPE_FONT_MAP);
  pango_font_map_changed(self);
}

/**
 * Gtk4\PangoFontMap::create_context(): PangoContext
 *
 * Creates a `PangoContext` connected to $fontmap.
 */
ZEND_METHOD(Gtk4_PangoFontMap, create_context) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontMap *self = PHPGTK_SELF(PangoFontMap, PANGO_TYPE_FONT_MAP);
  PangoContext *phpgtk_ret = pango_font_map_create_context(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * Gtk4\PangoFontMap::get_serial(): int
 *
 * Returns the current serial number of $fontmap.
 */
ZEND_METHOD(Gtk4_PangoFontMap, get_serial) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontMap *self = PHPGTK_SELF(PangoFontMap, PANGO_TYPE_FONT_MAP);
  RETURN_LONG(static_cast<zend_long>(pango_font_map_get_serial(self)));
}

// vfunc thunks and installers: file-local, installed by class_init of a PHP subtype
namespace {

// vfunc thunk: PANGO_FONT_MAP_CLASS->changed -> $this->vfunc_changed() on a PHP subclass
void vfunc_thunk_changed(PangoFontMap *self) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_changed", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = PANGO_FONT_MAP_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->changed != nullptr) native->changed(self);
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("PangoFontMap::vfunc_changed");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: PANGO_FONT_MAP_CLASS->changed (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_changed(gpointer klass) {
  PANGO_FONT_MAP_CLASS(klass)->changed = vfunc_thunk_changed;
}

// vfunc thunk: PANGO_FONT_MAP_CLASS->get_serial -> $this->vfunc_get_serial() on a PHP subclass
guint vfunc_thunk_get_serial(PangoFontMap *self) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_get_serial", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = PANGO_FONT_MAP_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->get_serial != nullptr ? native->get_serial(self) : 0;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  zval ret;
  ZVAL_UNDEF(&ret);
  guint result = 0;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    if (Z_TYPE(ret) == IS_LONG && phpgtk::check_range<guint>(Z_LVAL(ret), 0)) {
      result = static_cast<guint>(Z_LVAL(ret));
    }
  }
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("PangoFontMap::vfunc_get_serial");
  zend_exception_restore();  // the parked one, previous of whatever this threw
  return result;
}

// vfunc installer: PANGO_FONT_MAP_CLASS->get_serial (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_get_serial(gpointer klass) {
  PANGO_FONT_MAP_CLASS(klass)->get_serial = vfunc_thunk_get_serial;
}

}  // namespace

/**
 * Gtk4\PangoFontMap::vfunc_changed(): void
 *
 * Native `changed` (FontMapClass.changed): the GTK implementation below any PHP subclass, for
 * `parent::vfunc_changed()` from an override. Forces a change in the context, which will cause any
 * `PangoContext` using this fontmap to change.
 */
ZEND_METHOD(Gtk4_PangoFontMap, vfunc_changed) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontMap *self = PHPGTK_SELF(PangoFontMap, PANGO_TYPE_FONT_MAP);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "PangoFontMap::vfunc_changed(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = PANGO_FONT_MAP_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->changed == nullptr) {
    return;
  }
  klass->changed(self);
}

/**
 * Gtk4\PangoFontMap::vfunc_get_serial(): int
 *
 * Native `get_serial` (FontMapClass.get_serial): the GTK implementation below any PHP subclass,
 * for `parent::vfunc_get_serial()` from an override. Returns the current serial number of
 * $fontmap.
 */
ZEND_METHOD(Gtk4_PangoFontMap, vfunc_get_serial) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontMap *self = PHPGTK_SELF(PangoFontMap, PANGO_TYPE_FONT_MAP);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "PangoFontMap::vfunc_get_serial(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = PANGO_FONT_MAP_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->get_serial == nullptr) {
    RETURN_LONG(0);
  }
  RETURN_LONG(static_cast<zend_long>(klass->get_serial(self)));
}

// MINIT: the vfunc thunks of PangoFontMap (core/subtype.h).
void register_vfuncs_PangoFontMap() {
  register_vfunc(PANGO_TYPE_FONT_MAP, "changed", vfunc_install_changed);
  register_vfunc(PANGO_TYPE_FONT_MAP, "get_serial", vfunc_install_get_serial);
}
