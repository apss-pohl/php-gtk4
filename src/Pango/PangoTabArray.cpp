// GENERATED by gen/gir.php from Pango.TabArray - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\PangoTabArray (boxed value type)
#include "php_gtk4.h"
#include "core/boxed.h"
#include "core/object.h"
#include <cstring>
#include "core/enums.h"

using namespace phpgtk;

namespace {

const char *const fields[] = {nullptr};

// Boxed field reader: the public scalar fields as PHP properties.
bool read(gpointer data, const char *field, zval *rv) {
  (void)data;
  (void)field;
  (void)rv;
  return false;
}

// Boxed field writer: each field converts like a parameter of its type (core/boxed).
bool write(gpointer data, const char *field, zval *v) {
  (void)data;
  (void)field;
  (void)v;
  return false;
}

}  // namespace

/**
 * Gtk4\PangoTabArray::__construct(int $initial_size, bool $positions_in_pixels)
 *
 * Creates an array of $initial_size tab stops.
 */
ZEND_METHOD(Gtk4_PangoTabArray, __construct) {
  zend_long initial_size;
  bool positions_in_pixels;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_LONG(initial_size)
  Z_PARAM_BOOL(positions_in_pixels)
  ZEND_PARSE_PARAMETERS_END();
  if (!phpgtk::check_range<gint>(initial_size, 1)) RETURN_THROWS();
  gpointer obj = pango_tab_array_new(static_cast<gint>(initial_size), positions_in_pixels);
  if (obj == nullptr) {
    zend_throw_error(nullptr, "%s(): GTK refused to create the value (see the CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  boxed_adopt(boxed_from_zval(ZEND_THIS), PANGO_TYPE_TAB_ARRAY, obj);
}

/**
 * Gtk4\PangoTabArray::get_decimal_point(int $tab_index): int
 *
 * Gets the Unicode character to use as decimal point.
 */
ZEND_METHOD(Gtk4_PangoTabArray, get_decimal_point) {
  zend_long tab_index;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(tab_index)
  ZEND_PARSE_PARAMETERS_END();
  PangoTabArray *self = PHPGTK_BOXED_SELF(PangoTabArray);
  if (!phpgtk::check_range<int>(tab_index, 1)) RETURN_THROWS();
  RETURN_LONG(
      static_cast<zend_long>(pango_tab_array_get_decimal_point(self, static_cast<int>(tab_index))));
}

/**
 * Gtk4\PangoTabArray::get_positions_in_pixels(): bool
 *
 * Returns `true` if the tab positions are in pixels, `false` if they are in Pango units.
 */
ZEND_METHOD(Gtk4_PangoTabArray, get_positions_in_pixels) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoTabArray *self = PHPGTK_BOXED_SELF(PangoTabArray);
  RETURN_BOOL(pango_tab_array_get_positions_in_pixels(self));
}

/**
 * Gtk4\PangoTabArray::get_size(): int
 *
 * Gets the number of tab stops in $tab_array.
 */
ZEND_METHOD(Gtk4_PangoTabArray, get_size) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoTabArray *self = PHPGTK_BOXED_SELF(PangoTabArray);
  RETURN_LONG(static_cast<zend_long>(pango_tab_array_get_size(self)));
}

/**
 * Gtk4\PangoTabArray::get_tab(int $tab_index): array
 *
 * Gets the alignment and position of a tab stop.
 */
ZEND_METHOD(Gtk4_PangoTabArray, get_tab) {
  zend_long tab_index;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(tab_index)
  ZEND_PARSE_PARAMETERS_END();
  PangoTabArray *self = PHPGTK_BOXED_SELF(PangoTabArray);
  if (!phpgtk::check_range<gint>(tab_index, 1)) RETURN_THROWS();
  PangoTabAlign alignment = static_cast<PangoTabAlign>(0);
  gint location = 0;
  pango_tab_array_get_tab(self, static_cast<gint>(tab_index), &alignment, &location);
  array_init_size(return_value, 2);
  {
    zval item;
    enum_to_php(PANGO_TYPE_TAB_ALIGN, static_cast<gint>(alignment), &item);
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(location));
    add_next_index_zval(return_value, &item);
  }
}

/**
 * Gtk4\PangoTabArray::resize(int $new_size): void
 *
 * Resizes a tab array.
 */
ZEND_METHOD(Gtk4_PangoTabArray, resize) {
  zend_long new_size;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(new_size)
  ZEND_PARSE_PARAMETERS_END();
  PangoTabArray *self = PHPGTK_BOXED_SELF(PangoTabArray);
  if (!phpgtk::check_range<gint>(new_size, 1)) RETURN_THROWS();
  pango_tab_array_resize(self, static_cast<gint>(new_size));
}

/**
 * Gtk4\PangoTabArray::set_decimal_point(int $tab_index, int $decimal_point): void
 *
 * Sets the Unicode character to use as decimal point.
 */
ZEND_METHOD(Gtk4_PangoTabArray, set_decimal_point) {
  zend_long tab_index;
  zend_long decimal_point;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_LONG(tab_index)
  Z_PARAM_LONG(decimal_point)
  ZEND_PARSE_PARAMETERS_END();
  PangoTabArray *self = PHPGTK_BOXED_SELF(PangoTabArray);
  if (!phpgtk::check_range<int>(tab_index, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<gunichar>(decimal_point, 2)) RETURN_THROWS();
  pango_tab_array_set_decimal_point(self, static_cast<int>(tab_index),
                                    static_cast<gunichar>(decimal_point));
}

/**
 * Gtk4\PangoTabArray::set_positions_in_pixels(bool $positions_in_pixels): void
 *
 * Sets whether positions in this array are specified in pixels.
 */
ZEND_METHOD(Gtk4_PangoTabArray, set_positions_in_pixels) {
  bool positions_in_pixels;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(positions_in_pixels)
  ZEND_PARSE_PARAMETERS_END();
  PangoTabArray *self = PHPGTK_BOXED_SELF(PangoTabArray);
  pango_tab_array_set_positions_in_pixels(self, positions_in_pixels);
}

/**
 * Gtk4\PangoTabArray::set_tab(int $tab_index, PangoTabAlign $alignment, int $location): void
 *
 * Sets the alignment and location of a tab stop.
 */
ZEND_METHOD(Gtk4_PangoTabArray, set_tab) {
  zend_long tab_index;
  zval *alignment;
  zend_long location;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_LONG(tab_index)
  Z_PARAM_OBJECT_OF_CLASS(alignment, enum_class_for_type(PANGO_TYPE_TAB_ALIGN))
  Z_PARAM_LONG(location)
  ZEND_PARSE_PARAMETERS_END();
  PangoTabArray *self = PHPGTK_BOXED_SELF(PangoTabArray);
  if (!phpgtk::check_range<gint>(tab_index, 1)) RETURN_THROWS();
  gint alignment_v = 0;
  if (!enum_from_php(alignment, PANGO_TYPE_TAB_ALIGN, &alignment_v)) RETURN_THROWS();
  if (!phpgtk::check_range<gint>(location, 3)) RETURN_THROWS();
  pango_tab_array_set_tab(self, static_cast<gint>(tab_index),
                          static_cast<PangoTabAlign>(alignment_v), static_cast<gint>(location));
}

/**
 * Gtk4\PangoTabArray::sort(): void
 *
 * Utility function to ensure that the tab stops are in increasing order.
 */
ZEND_METHOD(Gtk4_PangoTabArray, sort) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoTabArray *self = PHPGTK_BOXED_SELF(PangoTabArray);
  pango_tab_array_sort(self);
}

/**
 * Gtk4\PangoTabArray::to_string(): string
 *
 * Serializes a `PangoTabArray` to a string.
 */
ZEND_METHOD(Gtk4_PangoTabArray, to_string) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoTabArray *self = PHPGTK_BOXED_SELF(PangoTabArray);
  char *phpgtk_ret = pango_tab_array_to_string(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETVAL_STRING(phpgtk_ret);
  g_free(phpgtk_ret);
}

/**
 * static Gtk4\PangoTabArray::from_string(string $text): ?PangoTabArray
 *
 * Deserializes a `PangoTabArray` from a string.
 */
ZEND_METHOD(Gtk4_PangoTabArray, from_string) {
  zend_string *text;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(text)
  ZEND_PARSE_PARAMETERS_END();
  if (!phpgtk::check_utf8(text, 1)) RETURN_THROWS();
  PangoTabArray *phpgtk_ret = pango_tab_array_from_string(ZSTR_VAL(text));
  wrap_boxed(PANGO_TYPE_TAB_ARRAY, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(PANGO_TYPE_TAB_ARRAY, phpgtk_ret);
}

namespace phpgtk {
// MINIT: bind the PHP class to PANGO_TYPE_TAB_ARRAY with its field table.
void register_PangoTabArray(zend_class_entry *ce) {
  register_boxed(BoxedClass{
      .type = PANGO_TYPE_TAB_ARRAY, .ce = ce, .fields = fields, .read = read, .write = write});
}
}  // namespace phpgtk
