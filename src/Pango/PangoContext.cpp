// GENERATED by gen/gir.php from Pango.Context - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\PangoContext
#include "php_gtk4.h"
#include "core/object.h"
#include "core/enums.h"
#include "core/boxed.h"
#include "core/subtype.h"

using namespace phpgtk;

/**
 * Gtk4\PangoContext::__construct()
 *
 * Creates a new `PangoContext` initialized to default values.
 */
ZEND_METHOD(Gtk4_PangoContext, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(pango_context_new());
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\PangoContext::changed(): void
 *
 * Forces a change in the context, which will cause any `PangoLayout` using this context to
 * re-layout.
 */
ZEND_METHOD(Gtk4_PangoContext, changed) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoContext *self = PHPGTK_SELF(PangoContext, PANGO_TYPE_CONTEXT);
  pango_context_changed(self);
}

/**
 * Gtk4\PangoContext::get_base_dir(): PangoDirection
 *
 * Retrieves the base direction for the context.
 */
ZEND_METHOD(Gtk4_PangoContext, get_base_dir) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoContext *self = PHPGTK_SELF(PangoContext, PANGO_TYPE_CONTEXT);
  enum_to_php(PANGO_TYPE_DIRECTION, pango_context_get_base_dir(self), return_value);
}

/**
 * Gtk4\PangoContext::get_base_gravity(): PangoGravity
 *
 * Retrieves the base gravity for the context.
 */
ZEND_METHOD(Gtk4_PangoContext, get_base_gravity) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoContext *self = PHPGTK_SELF(PangoContext, PANGO_TYPE_CONTEXT);
  enum_to_php(PANGO_TYPE_GRAVITY, pango_context_get_base_gravity(self), return_value);
}

/**
 * Gtk4\PangoContext::get_font_description(): ?PangoFontDescription
 *
 * Retrieve the default font description for the context.
 */
ZEND_METHOD(Gtk4_PangoContext, get_font_description) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoContext *self = PHPGTK_SELF(PangoContext, PANGO_TYPE_CONTEXT);
  PangoFontDescription *phpgtk_ret = pango_context_get_font_description(self);
  wrap_boxed(PANGO_TYPE_FONT_DESCRIPTION, phpgtk_ret, return_value);
}

/**
 * Gtk4\PangoContext::get_font_map(): ?PangoFontMap
 *
 * Gets the `PangoFontMap` used to look up fonts for this context.
 */
ZEND_METHOD(Gtk4_PangoContext, get_font_map) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoContext *self = PHPGTK_SELF(PangoContext, PANGO_TYPE_CONTEXT);
  PangoFontMap *phpgtk_ret = pango_context_get_font_map(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\PangoContext::get_gravity(): PangoGravity
 *
 * Retrieves the gravity for the context.
 */
ZEND_METHOD(Gtk4_PangoContext, get_gravity) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoContext *self = PHPGTK_SELF(PangoContext, PANGO_TYPE_CONTEXT);
  enum_to_php(PANGO_TYPE_GRAVITY, pango_context_get_gravity(self), return_value);
}

/**
 * Gtk4\PangoContext::get_gravity_hint(): PangoGravityHint
 *
 * Retrieves the gravity hint for the context.
 */
ZEND_METHOD(Gtk4_PangoContext, get_gravity_hint) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoContext *self = PHPGTK_SELF(PangoContext, PANGO_TYPE_CONTEXT);
  enum_to_php(PANGO_TYPE_GRAVITY_HINT, pango_context_get_gravity_hint(self), return_value);
}

/**
 * Gtk4\PangoContext::get_language(): ?PangoLanguage
 *
 * Retrieves the global language tag for the context.
 */
ZEND_METHOD(Gtk4_PangoContext, get_language) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoContext *self = PHPGTK_SELF(PangoContext, PANGO_TYPE_CONTEXT);
  PangoLanguage *phpgtk_ret = pango_context_get_language(self);
  wrap_boxed(PANGO_TYPE_LANGUAGE, phpgtk_ret, return_value);
}

/**
 * Gtk4\PangoContext::get_metrics(?PangoFontDescription $desc, ?PangoLanguage $language):
 * PangoFontMetrics
 *
 * Get overall metric information for a particular font description.
 */
ZEND_METHOD(Gtk4_PangoContext, get_metrics) {
  zval *desc = nullptr;
  zval *language = nullptr;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(desc, boxed_class_for_type(PANGO_TYPE_FONT_DESCRIPTION)->ce)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(language, boxed_class_for_type(PANGO_TYPE_LANGUAGE)->ce)
  ZEND_PARSE_PARAMETERS_END();
  PangoContext *self = PHPGTK_SELF(PangoContext, PANGO_TYPE_CONTEXT);
  gpointer desc_b = nullptr;
  if (desc != nullptr) {
    desc_b = unwrap_boxed(desc, PANGO_TYPE_FONT_DESCRIPTION);
    if (desc_b == nullptr) RETURN_THROWS();
  }
  gpointer language_b = nullptr;
  if (language != nullptr) {
    language_b = unwrap_boxed(language, PANGO_TYPE_LANGUAGE);
    if (language_b == nullptr) RETURN_THROWS();
  }
  const bool state_holds = pango_context_get_font_map(self) != nullptr;
  if (!state_holds) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "%s(): the context has no font map - use a widget's context or set_font_map() first",
        ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  PangoFontMetrics *phpgtk_ret = pango_context_get_metrics(
      self, static_cast<PangoFontDescription *>(desc_b), static_cast<PangoLanguage *>(language_b));
  wrap_boxed(PANGO_TYPE_FONT_METRICS, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(PANGO_TYPE_FONT_METRICS, phpgtk_ret);
}

/**
 * Gtk4\PangoContext::get_round_glyph_positions(): bool
 *
 * Returns whether font rendering with this context should round glyph positions and widths.
 */
ZEND_METHOD(Gtk4_PangoContext, get_round_glyph_positions) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoContext *self = PHPGTK_SELF(PangoContext, PANGO_TYPE_CONTEXT);
  RETURN_BOOL(pango_context_get_round_glyph_positions(self));
}

/**
 * Gtk4\PangoContext::get_serial(): int
 *
 * Returns the current serial number of $context.
 */
ZEND_METHOD(Gtk4_PangoContext, get_serial) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoContext *self = PHPGTK_SELF(PangoContext, PANGO_TYPE_CONTEXT);
  RETURN_LONG(static_cast<zend_long>(pango_context_get_serial(self)));
}

/**
 * Gtk4\PangoContext::load_font(PangoFontDescription $desc): ?PangoFont
 *
 * Loads the font in one of the fontmaps in the context that is the closest match for $desc.
 */
ZEND_METHOD(Gtk4_PangoContext, load_font) {
  zval *desc;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(desc, boxed_class_for_type(PANGO_TYPE_FONT_DESCRIPTION)->ce)
  ZEND_PARSE_PARAMETERS_END();
  PangoContext *self = PHPGTK_SELF(PangoContext, PANGO_TYPE_CONTEXT);
  gpointer desc_b = unwrap_boxed(desc, PANGO_TYPE_FONT_DESCRIPTION);
  if (desc_b == nullptr) RETURN_THROWS();
  const bool state_holds = pango_context_get_font_map(self) != nullptr;
  if (!state_holds) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "%s(): the context has no font map - use a widget's context or set_font_map() first",
        ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  PangoFont *phpgtk_ret =
      pango_context_load_font(self, static_cast<PangoFontDescription *>(desc_b));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * Gtk4\PangoContext::load_fontset(PangoFontDescription $desc, PangoLanguage $language):
 * ?PangoFontset
 *
 * Load a set of fonts in the context that can be used to render a font matching $desc.
 */
ZEND_METHOD(Gtk4_PangoContext, load_fontset) {
  zval *desc;
  zval *language;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(desc, boxed_class_for_type(PANGO_TYPE_FONT_DESCRIPTION)->ce)
  Z_PARAM_OBJECT_OF_CLASS(language, boxed_class_for_type(PANGO_TYPE_LANGUAGE)->ce)
  ZEND_PARSE_PARAMETERS_END();
  PangoContext *self = PHPGTK_SELF(PangoContext, PANGO_TYPE_CONTEXT);
  gpointer desc_b = unwrap_boxed(desc, PANGO_TYPE_FONT_DESCRIPTION);
  if (desc_b == nullptr) RETURN_THROWS();
  gpointer language_b = unwrap_boxed(language, PANGO_TYPE_LANGUAGE);
  if (language_b == nullptr) RETURN_THROWS();
  const bool state_holds = pango_context_get_font_map(self) != nullptr;
  if (!state_holds) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "%s(): the context has no font map - use a widget's context or set_font_map() first",
        ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  PangoFontset *phpgtk_ret = pango_context_load_fontset(
      self, static_cast<PangoFontDescription *>(desc_b), static_cast<PangoLanguage *>(language_b));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * Gtk4\PangoContext::set_base_dir(PangoDirection $direction): void
 *
 * Sets the base direction for the context.
 */
ZEND_METHOD(Gtk4_PangoContext, set_base_dir) {
  zval *direction;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(direction, enum_class_for_type(PANGO_TYPE_DIRECTION))
  ZEND_PARSE_PARAMETERS_END();
  PangoContext *self = PHPGTK_SELF(PangoContext, PANGO_TYPE_CONTEXT);
  gint direction_v = 0;
  if (!enum_from_php(direction, PANGO_TYPE_DIRECTION, &direction_v)) RETURN_THROWS();
  pango_context_set_base_dir(self, static_cast<PangoDirection>(direction_v));
}

/**
 * Gtk4\PangoContext::set_base_gravity(PangoGravity $gravity): void
 *
 * Sets the base gravity for the context.
 */
ZEND_METHOD(Gtk4_PangoContext, set_base_gravity) {
  zval *gravity;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(gravity, enum_class_for_type(PANGO_TYPE_GRAVITY))
  ZEND_PARSE_PARAMETERS_END();
  PangoContext *self = PHPGTK_SELF(PangoContext, PANGO_TYPE_CONTEXT);
  gint gravity_v = 0;
  if (!enum_from_php(gravity, PANGO_TYPE_GRAVITY, &gravity_v)) RETURN_THROWS();
  pango_context_set_base_gravity(self, static_cast<PangoGravity>(gravity_v));
}

/**
 * Gtk4\PangoContext::set_font_description(PangoFontDescription $desc): void
 *
 * Set the default font description for the context
 */
ZEND_METHOD(Gtk4_PangoContext, set_font_description) {
  zval *desc;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(desc, boxed_class_for_type(PANGO_TYPE_FONT_DESCRIPTION)->ce)
  ZEND_PARSE_PARAMETERS_END();
  PangoContext *self = PHPGTK_SELF(PangoContext, PANGO_TYPE_CONTEXT);
  gpointer desc_b = unwrap_boxed(desc, PANGO_TYPE_FONT_DESCRIPTION);
  if (desc_b == nullptr) RETURN_THROWS();
  pango_context_set_font_description(self, static_cast<PangoFontDescription *>(desc_b));
}

/**
 * Gtk4\PangoContext::set_font_map(?PangoFontMap $font_map): void
 *
 * Sets the font map to be searched when fonts are looked-up in this context.
 */
ZEND_METHOD(Gtk4_PangoContext, set_font_map) {
  zval *font_map = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(font_map, class_for_gtype(PANGO_TYPE_FONT_MAP))
  ZEND_PARSE_PARAMETERS_END();
  PangoContext *self = PHPGTK_SELF(PangoContext, PANGO_TYPE_CONTEXT);
  GObject *font_map_o = nullptr;
  if (font_map != nullptr) {
    font_map_o = unwrap(font_map, PANGO_TYPE_FONT_MAP);
    if (font_map_o == nullptr) RETURN_THROWS();
  }
  pango_context_set_font_map(self, font_map_o != nullptr ? PANGO_FONT_MAP(font_map_o) : nullptr);
}

/**
 * Gtk4\PangoContext::set_gravity_hint(PangoGravityHint $hint): void
 *
 * Sets the gravity hint for the context.
 */
ZEND_METHOD(Gtk4_PangoContext, set_gravity_hint) {
  zval *hint;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(hint, enum_class_for_type(PANGO_TYPE_GRAVITY_HINT))
  ZEND_PARSE_PARAMETERS_END();
  PangoContext *self = PHPGTK_SELF(PangoContext, PANGO_TYPE_CONTEXT);
  gint hint_v = 0;
  if (!enum_from_php(hint, PANGO_TYPE_GRAVITY_HINT, &hint_v)) RETURN_THROWS();
  pango_context_set_gravity_hint(self, static_cast<PangoGravityHint>(hint_v));
}

/**
 * Gtk4\PangoContext::set_language(?PangoLanguage $language): void
 *
 * Sets the global language tag for the context.
 */
ZEND_METHOD(Gtk4_PangoContext, set_language) {
  zval *language = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(language, boxed_class_for_type(PANGO_TYPE_LANGUAGE)->ce)
  ZEND_PARSE_PARAMETERS_END();
  PangoContext *self = PHPGTK_SELF(PangoContext, PANGO_TYPE_CONTEXT);
  gpointer language_b = nullptr;
  if (language != nullptr) {
    language_b = unwrap_boxed(language, PANGO_TYPE_LANGUAGE);
    if (language_b == nullptr) RETURN_THROWS();
  }
  pango_context_set_language(self, static_cast<PangoLanguage *>(language_b));
}

/**
 * Gtk4\PangoContext::set_round_glyph_positions(bool $round_positions): void
 *
 * Sets whether font rendering with this context should round glyph positions and widths to
 * integral positions, in device units.
 */
ZEND_METHOD(Gtk4_PangoContext, set_round_glyph_positions) {
  bool round_positions;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(round_positions)
  ZEND_PARSE_PARAMETERS_END();
  PangoContext *self = PHPGTK_SELF(PangoContext, PANGO_TYPE_CONTEXT);
  pango_context_set_round_glyph_positions(self, round_positions);
}
