// GENERATED by gen/gir.php from Pango.GlyphString - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\PangoGlyphString (boxed value type)
#include "php_gtk4.h"
#include "core/boxed.h"
#include "core/object.h"
#include <cstring>
#include "Pango/PangoRectangleType.h"

using namespace phpgtk;

namespace {

const char *const fields[] = {"num_glyphs", nullptr};

// Boxed field reader: the public scalar fields as PHP properties.
bool read(gpointer data, const char *field, zval *rv) {
  auto *value = static_cast<PangoGlyphString *>(data);
  if (strcmp(field, "num_glyphs") == 0) {
    ZVAL_LONG(rv, static_cast<zend_long>(value->num_glyphs));
    return true;
  }
  return false;
}

// Boxed field writer: each field converts like a parameter of its type (core/boxed).
bool write(gpointer data, const char *field, zval *v) {
  auto *value = static_cast<PangoGlyphString *>(data);
  if (strcmp(field, "num_glyphs") == 0) {
    zend_long l = 0;
    if (boxed_field_long(v, "Gtk4\\PangoGlyphString", "num_glyphs", &l) &&
        phpgtk::check_range<int>(l, 0))
      value->num_glyphs = static_cast<int>(l);
    return true;
  }
  return false;
}

}  // namespace

/**
 * Gtk4\PangoGlyphString::__construct()
 *
 * Create a new `PangoGlyphString`.
 */
ZEND_METHOD(Gtk4_PangoGlyphString, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  gpointer obj = pango_glyph_string_new();
  if (obj == nullptr) {
    zend_throw_error(nullptr, "%s(): GTK refused to create the value (see the CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  boxed_adopt(boxed_from_zval(ZEND_THIS), PANGO_TYPE_GLYPH_STRING, obj);
}

/**
 * Gtk4\PangoGlyphString::extents(PangoFont $font): array
 *
 * Compute the logical and ink extents of a glyph string.
 */
ZEND_METHOD(Gtk4_PangoGlyphString, extents) {
  zval *font;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(font, class_for_gtype(PANGO_TYPE_FONT))
  ZEND_PARSE_PARAMETERS_END();
  PangoGlyphString *self = PHPGTK_BOXED_SELF(PangoGlyphString);
  GObject *font_o = unwrap(font, PANGO_TYPE_FONT);
  if (font_o == nullptr) RETURN_THROWS();
  PangoRectangle ink_rect{};
  PangoRectangle logical_rect{};
  pango_glyph_string_extents(self, PANGO_FONT(font_o), &ink_rect, &logical_rect);
  array_init_size(return_value, 2);
  {
    zval item;
    wrap_boxed(PHPGTK_TYPE_PANGO_RECTANGLE, &ink_rect, &item);
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    wrap_boxed(PHPGTK_TYPE_PANGO_RECTANGLE, &logical_rect, &item);
    add_next_index_zval(return_value, &item);
  }
}

/**
 * Gtk4\PangoGlyphString::extents_range(int $start, int $end, PangoFont $font): array
 *
 * Computes the extents of a sub-portion of a glyph string.
 */
ZEND_METHOD(Gtk4_PangoGlyphString, extents_range) {
  zend_long start;
  zend_long end;
  zval *font;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_LONG(start)
  Z_PARAM_LONG(end)
  Z_PARAM_OBJECT_OF_CLASS(font, class_for_gtype(PANGO_TYPE_FONT))
  ZEND_PARSE_PARAMETERS_END();
  PangoGlyphString *self = PHPGTK_BOXED_SELF(PangoGlyphString);
  if (!phpgtk::check_range<int>(start, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(end, 2)) RETURN_THROWS();
  GObject *font_o = unwrap(font, PANGO_TYPE_FONT);
  if (font_o == nullptr) RETURN_THROWS();
  PangoRectangle ink_rect{};
  PangoRectangle logical_rect{};
  pango_glyph_string_extents_range(self, static_cast<int>(start), static_cast<int>(end),
                                   PANGO_FONT(font_o), &ink_rect, &logical_rect);
  array_init_size(return_value, 2);
  {
    zval item;
    wrap_boxed(PHPGTK_TYPE_PANGO_RECTANGLE, &ink_rect, &item);
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    wrap_boxed(PHPGTK_TYPE_PANGO_RECTANGLE, &logical_rect, &item);
    add_next_index_zval(return_value, &item);
  }
}

/**
 * Gtk4\PangoGlyphString::get_width(): int
 *
 * Computes the logical width of the glyph string.
 */
ZEND_METHOD(Gtk4_PangoGlyphString, get_width) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoGlyphString *self = PHPGTK_BOXED_SELF(PangoGlyphString);
  RETURN_LONG(static_cast<zend_long>(pango_glyph_string_get_width(self)));
}

/**
 * public function set_size(int $new_len): void
 * Resize the glyph string to $new_len glyphs.
 *
 * Pango reallocates and leaves the new entries as the allocator left them, so a string that
 * grew would answer extents() and get_width() from uninitialised memory; here every grown entry
 * is PANGO_GLYPH_EMPTY with no geometry and cluster 0 - the glyph Pango draws and measures as
 * nothing - so a fresh string measures as empty until something shapes into it. A negative
 * length is refused before Pango's own assertion sees it.
 */
ZEND_METHOD(Gtk4_PangoGlyphString, set_size) {
  zend_long new_len;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(new_len)
  ZEND_PARSE_PARAMETERS_END();
  PangoGlyphString *self = PHPGTK_BOXED_SELF(PangoGlyphString);
  if (!phpgtk::check_range<int>(new_len, 1)) RETURN_THROWS();
  if (new_len < 0) {
    zend_argument_value_error(1, "must be greater than or equal to 0");
    RETURN_THROWS();
  }
  const int old_len = self->num_glyphs;
  pango_glyph_string_set_size(self, static_cast<int>(new_len));
  if (new_len > old_len) {
    const auto grown = static_cast<size_t>(new_len - old_len);
    memset(self->glyphs + old_len, 0, grown * sizeof(PangoGlyphInfo));
    memset(self->log_clusters + old_len, 0, grown * sizeof(int));
    for (int i = old_len; i < new_len; i++) self->glyphs[i].glyph = PANGO_GLYPH_EMPTY;
  }
}

namespace phpgtk {
// MINIT: bind the PHP class to PANGO_TYPE_GLYPH_STRING with its field table.
void register_PangoGlyphString(zend_class_entry *ce) {
  register_boxed(BoxedClass{
      .type = PANGO_TYPE_GLYPH_STRING, .ce = ce, .fields = fields, .read = read, .write = write});
}
}  // namespace phpgtk
