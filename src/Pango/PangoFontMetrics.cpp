// GENERATED by gen/gir.php from Pango.FontMetrics - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\PangoFontMetrics (boxed value type)
#include "php_gtk4.h"
#include "core/boxed.h"
#include "core/object.h"
#include <cstring>

using namespace phpgtk;

namespace {

const char *const fields[] = {nullptr};

// Boxed field reader: the public scalar fields as PHP properties.
bool read(gpointer data, const char *field, zval *rv) {
  (void)data;
  (void)field;
  (void)rv;
  return false;
}

// Boxed field writer: each field converts like a parameter of its type (core/boxed).
bool write(gpointer data, const char *field, zval *v) {
  (void)data;
  (void)field;
  (void)v;
  return false;
}

}  // namespace

/**
 * Gtk4\PangoFontMetrics::__construct()
 *
 * PangoFontMetrics values come from GTK, never from `new`.
 */
ZEND_METHOD(Gtk4_PangoFontMetrics, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
}

/**
 * Gtk4\PangoFontMetrics::get_approximate_char_width(): int
 *
 * Gets the approximate character width for a font metrics structure.
 */
ZEND_METHOD(Gtk4_PangoFontMetrics, get_approximate_char_width) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontMetrics *self = PHPGTK_BOXED_SELF(PangoFontMetrics);
  RETURN_LONG(static_cast<zend_long>(pango_font_metrics_get_approximate_char_width(self)));
}

/**
 * Gtk4\PangoFontMetrics::get_approximate_digit_width(): int
 *
 * Gets the approximate digit width for a font metrics structure.
 */
ZEND_METHOD(Gtk4_PangoFontMetrics, get_approximate_digit_width) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontMetrics *self = PHPGTK_BOXED_SELF(PangoFontMetrics);
  RETURN_LONG(static_cast<zend_long>(pango_font_metrics_get_approximate_digit_width(self)));
}

/**
 * Gtk4\PangoFontMetrics::get_ascent(): int
 *
 * Gets the ascent from a font metrics structure.
 */
ZEND_METHOD(Gtk4_PangoFontMetrics, get_ascent) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontMetrics *self = PHPGTK_BOXED_SELF(PangoFontMetrics);
  RETURN_LONG(static_cast<zend_long>(pango_font_metrics_get_ascent(self)));
}

/**
 * Gtk4\PangoFontMetrics::get_descent(): int
 *
 * Gets the descent from a font metrics structure.
 */
ZEND_METHOD(Gtk4_PangoFontMetrics, get_descent) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontMetrics *self = PHPGTK_BOXED_SELF(PangoFontMetrics);
  RETURN_LONG(static_cast<zend_long>(pango_font_metrics_get_descent(self)));
}

/**
 * Gtk4\PangoFontMetrics::get_height(): int
 *
 * Gets the line height from a font metrics structure.
 */
ZEND_METHOD(Gtk4_PangoFontMetrics, get_height) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontMetrics *self = PHPGTK_BOXED_SELF(PangoFontMetrics);
  RETURN_LONG(static_cast<zend_long>(pango_font_metrics_get_height(self)));
}

/**
 * Gtk4\PangoFontMetrics::get_strikethrough_position(): int
 *
 * Gets the suggested position to draw the strikethrough.
 */
ZEND_METHOD(Gtk4_PangoFontMetrics, get_strikethrough_position) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontMetrics *self = PHPGTK_BOXED_SELF(PangoFontMetrics);
  RETURN_LONG(static_cast<zend_long>(pango_font_metrics_get_strikethrough_position(self)));
}

/**
 * Gtk4\PangoFontMetrics::get_strikethrough_thickness(): int
 *
 * Gets the suggested thickness to draw for the strikethrough.
 */
ZEND_METHOD(Gtk4_PangoFontMetrics, get_strikethrough_thickness) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontMetrics *self = PHPGTK_BOXED_SELF(PangoFontMetrics);
  RETURN_LONG(static_cast<zend_long>(pango_font_metrics_get_strikethrough_thickness(self)));
}

/**
 * Gtk4\PangoFontMetrics::get_underline_position(): int
 *
 * Gets the suggested position to draw the underline.
 */
ZEND_METHOD(Gtk4_PangoFontMetrics, get_underline_position) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontMetrics *self = PHPGTK_BOXED_SELF(PangoFontMetrics);
  RETURN_LONG(static_cast<zend_long>(pango_font_metrics_get_underline_position(self)));
}

/**
 * Gtk4\PangoFontMetrics::get_underline_thickness(): int
 *
 * Gets the suggested thickness to draw for the underline.
 */
ZEND_METHOD(Gtk4_PangoFontMetrics, get_underline_thickness) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontMetrics *self = PHPGTK_BOXED_SELF(PangoFontMetrics);
  RETURN_LONG(static_cast<zend_long>(pango_font_metrics_get_underline_thickness(self)));
}

namespace phpgtk {
// MINIT: bind the PHP class to PANGO_TYPE_FONT_METRICS with its field table.
void register_PangoFontMetrics(zend_class_entry *ce) {
  register_boxed(BoxedClass{
      .type = PANGO_TYPE_FONT_METRICS, .ce = ce, .fields = fields, .read = read, .write = write});
}
}  // namespace phpgtk
