// GENERATED by gen/gir.php from Pango.AttrList - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\PangoAttrList (boxed value type)
#include "php_gtk4.h"
#include "core/boxed.h"
#include "core/object.h"
#include <cstring>

using namespace phpgtk;

namespace {

const char *const fields[] = {nullptr};

// Boxed field reader: the public scalar fields as PHP properties.
bool read(gpointer data, const char *field, zval *rv) {
  (void)data;
  (void)field;
  (void)rv;
  return false;
}

// Boxed field writer: each field converts like a parameter of its type (core/boxed).
bool write(gpointer data, const char *field, zval *v) {
  (void)data;
  (void)field;
  (void)v;
  return false;
}

}  // namespace

/**
 * Gtk4\PangoAttrList::__construct()
 *
 * Create a new empty attribute list with a reference count of one.
 */
ZEND_METHOD(Gtk4_PangoAttrList, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  gpointer obj = pango_attr_list_new();
  if (obj == nullptr) {
    zend_throw_error(nullptr, "%s(): GTK refused to create the value (see the CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  boxed_adopt(boxed_from_zval(ZEND_THIS), PANGO_TYPE_ATTR_LIST, obj);
}

/**
 * Gtk4\PangoAttrList::equal(PangoAttrList $other_list): bool
 *
 * Checks whether $list and $other_list contain the same attributes and whether those attributes
 * apply to the same ranges.
 */
ZEND_METHOD(Gtk4_PangoAttrList, equal) {
  zval *other_list;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(other_list, boxed_class_for_type(PANGO_TYPE_ATTR_LIST)->ce)
  ZEND_PARSE_PARAMETERS_END();
  PangoAttrList *self = PHPGTK_BOXED_SELF(PangoAttrList);
  gpointer other_list_b = unwrap_boxed(other_list, PANGO_TYPE_ATTR_LIST);
  if (other_list_b == nullptr) RETURN_THROWS();
  RETURN_BOOL(pango_attr_list_equal(self, static_cast<PangoAttrList *>(other_list_b)));
}

/**
 * Gtk4\PangoAttrList::splice(PangoAttrList $other, int $pos, int $len): void
 *
 * This function opens up a hole in $list, fills it in with attributes from the left, and then
 * merges $other on top of the hole.
 */
ZEND_METHOD(Gtk4_PangoAttrList, splice) {
  zval *other;
  zend_long pos;
  zend_long len;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_OBJECT_OF_CLASS(other, boxed_class_for_type(PANGO_TYPE_ATTR_LIST)->ce)
  Z_PARAM_LONG(pos)
  Z_PARAM_LONG(len)
  ZEND_PARSE_PARAMETERS_END();
  PangoAttrList *self = PHPGTK_BOXED_SELF(PangoAttrList);
  gpointer other_b = unwrap_boxed(other, PANGO_TYPE_ATTR_LIST);
  if (other_b == nullptr) RETURN_THROWS();
  if (!phpgtk::check_range<int>(pos, 2)) RETURN_THROWS();
  if (!phpgtk::check_domain(pos, 0, ZEND_LONG_MAX, 2)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(len, 3)) RETURN_THROWS();
  if (!phpgtk::check_domain(len, 0, ZEND_LONG_MAX, 3)) RETURN_THROWS();
  pango_attr_list_splice(self, static_cast<PangoAttrList *>(other_b), static_cast<int>(pos),
                         static_cast<int>(len));
}

/**
 * Gtk4\PangoAttrList::to_string(): string
 *
 * Serializes a `PangoAttrList` to a string.
 */
ZEND_METHOD(Gtk4_PangoAttrList, to_string) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoAttrList *self = PHPGTK_BOXED_SELF(PangoAttrList);
  char *phpgtk_ret = pango_attr_list_to_string(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETVAL_STRING(phpgtk_ret);
  g_free(phpgtk_ret);
}

/**
 * Gtk4\PangoAttrList::update(int $pos, int $remove, int $add): void
 *
 * Update indices of attributes in $list for a change in the text they refer to.
 */
ZEND_METHOD(Gtk4_PangoAttrList, update) {
  zend_long pos;
  zend_long remove;
  zend_long add;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_LONG(pos)
  Z_PARAM_LONG(remove)
  Z_PARAM_LONG(add)
  ZEND_PARSE_PARAMETERS_END();
  PangoAttrList *self = PHPGTK_BOXED_SELF(PangoAttrList);
  if (!phpgtk::check_range<int>(pos, 1)) RETURN_THROWS();
  if (!phpgtk::check_domain(pos, 0, ZEND_LONG_MAX, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(remove, 2)) RETURN_THROWS();
  if (!phpgtk::check_domain(remove, 0, ZEND_LONG_MAX, 2)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(add, 3)) RETURN_THROWS();
  if (!phpgtk::check_domain(add, 0, ZEND_LONG_MAX, 3)) RETURN_THROWS();
  pango_attr_list_update(self, static_cast<int>(pos), static_cast<int>(remove),
                         static_cast<int>(add));
}

/**
 * static Gtk4\PangoAttrList::from_string(string $text): ?PangoAttrList
 *
 * Deserializes a `PangoAttrList` from a string.
 */
ZEND_METHOD(Gtk4_PangoAttrList, from_string) {
  zend_string *text;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(text)
  ZEND_PARSE_PARAMETERS_END();
  if (!phpgtk::check_utf8(text, 1)) RETURN_THROWS();
  PangoAttrList *phpgtk_ret = pango_attr_list_from_string(ZSTR_VAL(text));
  wrap_boxed(PANGO_TYPE_ATTR_LIST, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(PANGO_TYPE_ATTR_LIST, phpgtk_ret);
}

namespace phpgtk {
// MINIT: bind the PHP class to PANGO_TYPE_ATTR_LIST with its field table.
void register_PangoAttrList(zend_class_entry *ce) {
  register_boxed(BoxedClass{
      .type = PANGO_TYPE_ATTR_LIST,
      .ce = ce,
      .fields = fields,
      .read = read,
      .write = write,
      // NOLINTBEGIN(cppcoreguidelines-pro-type-const-cast) pango_attr_list_copy() takes its operand
      // non-const
      .copy =
          [](gconstpointer d) {
            return static_cast<gpointer>(
                pango_attr_list_copy(static_cast<PangoAttrList *>(const_cast<gpointer>(d))));
          }
      // NOLINTEND(cppcoreguidelines-pro-type-const-cast)
      ,
      // NOLINTBEGIN(cppcoreguidelines-pro-type-const-cast) pango_attr_list_equal() takes non-const
      // operands
      .equal =
          [](gconstpointer a, gconstpointer b) {
            return pango_attr_list_equal(static_cast<PangoAttrList *>(const_cast<gpointer>(a)),
                                         static_cast<PangoAttrList *>(const_cast<gpointer>(b))) !=
                   FALSE;
          }
      // NOLINTEND(cppcoreguidelines-pro-type-const-cast)
  });
}
}  // namespace phpgtk
