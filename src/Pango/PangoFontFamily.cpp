// GENERATED by gen/gir.php from Pango.FontFamily - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\PangoFontFamily
#include "php_gtk4.h"
#include "core/object.h"
#include "core/subtype.h"
#include "core/error.h"
#include <array>

using namespace phpgtk;

/**
 * Gtk4\PangoFontFamily::__construct()
 *
 * PangoFontFamily is abstract in GTK: instances come from GTK, never from `new`.
 */
ZEND_METHOD(Gtk4_PangoFontFamily, __construct) {
  // Private: never called (object_init_ex() in wrap() skips constructors).
  ZEND_PARSE_PARAMETERS_NONE();
}

/**
 * Gtk4\PangoFontFamily::get_face(?string $name): ?PangoFontFace
 *
 * Gets the `PangoFontFace` of $family with the given name.
 */
ZEND_METHOD(Gtk4_PangoFontFamily, get_face) {
  zend_string *name = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR_OR_NULL(name)
  ZEND_PARSE_PARAMETERS_END();
  PangoFontFamily *self = PHPGTK_SELF(PangoFontFamily, PANGO_TYPE_FONT_FAMILY);
  if (name != nullptr && !phpgtk::check_utf8(name, 1)) RETURN_THROWS();
  PangoFontFace *phpgtk_ret =
      pango_font_family_get_face(self, name != nullptr ? ZSTR_VAL(name) : nullptr);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\PangoFontFamily::get_name(): string
 *
 * Gets the name of the family.
 */
ZEND_METHOD(Gtk4_PangoFontFamily, get_name) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontFamily *self = PHPGTK_SELF(PangoFontFamily, PANGO_TYPE_FONT_FAMILY);
  const char *phpgtk_ret = pango_font_family_get_name(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

/**
 * Gtk4\PangoFontFamily::is_monospace(): bool
 *
 * A monospace font is a font designed for text display where the the characters form a regular
 * grid.
 */
ZEND_METHOD(Gtk4_PangoFontFamily, is_monospace) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontFamily *self = PHPGTK_SELF(PangoFontFamily, PANGO_TYPE_FONT_FAMILY);
  RETURN_BOOL(pango_font_family_is_monospace(self));
}

/**
 * Gtk4\PangoFontFamily::is_variable(): bool
 *
 * A variable font is a font which has axes that can be modified to produce different faces.
 */
ZEND_METHOD(Gtk4_PangoFontFamily, is_variable) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontFamily *self = PHPGTK_SELF(PangoFontFamily, PANGO_TYPE_FONT_FAMILY);
  RETURN_BOOL(pango_font_family_is_variable(self));
}

// vfunc thunks and installers: file-local, installed by class_init of a PHP subtype
namespace {

// vfunc thunk: PANGO_FONT_FAMILY_CLASS->get_face -> $this->vfunc_get_face() on a PHP subclass
PangoFontFace *vfunc_thunk_get_face(PangoFontFamily *self, const char *name) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_get_face", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = PANGO_FONT_FAMILY_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->get_face != nullptr ? native->get_face(self, name) : nullptr;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 1> args{};
  zval *argv = args.data();
  if (name == nullptr) {
    ZVAL_NULL(&argv[0]);
  } else {
    ZVAL_STRING(&argv[0], name);
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  PangoFontFace *result = nullptr;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    if (Z_TYPE(ret) == IS_OBJECT) {
      GObject *o = unwrap(&ret, PANGO_TYPE_FONT_FACE);
      result = o != nullptr ? PANGO_FONT_FACE(o) : nullptr;
    }
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("PangoFontFamily::vfunc_get_face");
  zend_exception_restore();  // the parked one, previous of whatever this threw
  return result;
}

// vfunc installer: PANGO_FONT_FAMILY_CLASS->get_face (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_get_face(gpointer klass) {
  PANGO_FONT_FAMILY_CLASS(klass)->get_face = vfunc_thunk_get_face;
}

// vfunc thunk: PANGO_FONT_FAMILY_CLASS->get_name -> $this->vfunc_get_name() on a PHP subclass
const char *vfunc_thunk_get_name(PangoFontFamily *self) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_get_name", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = PANGO_FONT_FAMILY_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->get_name != nullptr ? native->get_name(self) : nullptr;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  zval ret;
  ZVAL_UNDEF(&ret);
  const char *result = nullptr;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    if (Z_TYPE(ret) == IS_STRING && phpgtk::check_utf8(Z_STR(ret), 0)) {
      result = subtype_keep_string(G_OBJECT(self), "get_name", Z_STRVAL(ret));
    }
  }
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("PangoFontFamily::vfunc_get_name");
  zend_exception_restore();  // the parked one, previous of whatever this threw
  return result;
}

// vfunc installer: PANGO_FONT_FAMILY_CLASS->get_name (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_get_name(gpointer klass) {
  PANGO_FONT_FAMILY_CLASS(klass)->get_name = vfunc_thunk_get_name;
}

// vfunc thunk: PANGO_FONT_FAMILY_CLASS->is_monospace -> $this->vfunc_is_monospace() on a PHP
// subclass
gboolean vfunc_thunk_is_monospace(PangoFontFamily *self) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_is_monospace", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = PANGO_FONT_FAMILY_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->is_monospace != nullptr ? native->is_monospace(self) : FALSE;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  zval ret;
  ZVAL_UNDEF(&ret);
  gboolean result = FALSE;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = zend_is_true(&ret) ? TRUE : FALSE;
  }
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("PangoFontFamily::vfunc_is_monospace");
  zend_exception_restore();  // the parked one, previous of whatever this threw
  return result;
}

// vfunc installer: PANGO_FONT_FAMILY_CLASS->is_monospace (called from class_init / iface_init of a
// PHP subtype)
void vfunc_install_is_monospace(gpointer klass) {
  PANGO_FONT_FAMILY_CLASS(klass)->is_monospace = vfunc_thunk_is_monospace;
}

// vfunc thunk: PANGO_FONT_FAMILY_CLASS->is_variable -> $this->vfunc_is_variable() on a PHP subclass
gboolean vfunc_thunk_is_variable(PangoFontFamily *self) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_is_variable", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = PANGO_FONT_FAMILY_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->is_variable != nullptr ? native->is_variable(self) : FALSE;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  zval ret;
  ZVAL_UNDEF(&ret);
  gboolean result = FALSE;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = zend_is_true(&ret) ? TRUE : FALSE;
  }
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("PangoFontFamily::vfunc_is_variable");
  zend_exception_restore();  // the parked one, previous of whatever this threw
  return result;
}

// vfunc installer: PANGO_FONT_FAMILY_CLASS->is_variable (called from class_init / iface_init of a
// PHP subtype)
void vfunc_install_is_variable(gpointer klass) {
  PANGO_FONT_FAMILY_CLASS(klass)->is_variable = vfunc_thunk_is_variable;
}

}  // namespace

/**
 * Gtk4\PangoFontFamily::vfunc_get_face(?string $name): ?PangoFontFace
 *
 * Native `get_face` (FontFamilyClass.get_face): the GTK implementation below any PHP subclass, for
 * `parent::vfunc_get_face()` from an override. Gets the `PangoFontFace` of $family with the given
 * name.
 */
ZEND_METHOD(Gtk4_PangoFontFamily, vfunc_get_face) {
  zend_string *name = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR_OR_NULL(name)
  ZEND_PARSE_PARAMETERS_END();
  PangoFontFamily *self = PHPGTK_SELF(PangoFontFamily, PANGO_TYPE_FONT_FAMILY);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "PangoFontFamily::vfunc_get_face(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = PANGO_FONT_FAMILY_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->get_face == nullptr) {
    RETURN_NULL();
  }
  if (name != nullptr && !phpgtk::check_utf8(name, 1)) RETURN_THROWS();
  PangoFontFace *phpgtk_ret = klass->get_face(self, name != nullptr ? ZSTR_VAL(name) : nullptr);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\PangoFontFamily::vfunc_get_name(): string
 *
 * Native `get_name` (FontFamilyClass.get_name): the GTK implementation below any PHP subclass, for
 * `parent::vfunc_get_name()` from an override. Gets the name of the family.
 */
ZEND_METHOD(Gtk4_PangoFontFamily, vfunc_get_name) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontFamily *self = PHPGTK_SELF(PangoFontFamily, PANGO_TYPE_FONT_FAMILY);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "PangoFontFamily::vfunc_get_name(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = PANGO_FONT_FAMILY_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->get_name == nullptr) {
    RETURN_EMPTY_STRING();
  }
  const char *phpgtk_ret = klass->get_name(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

/**
 * Gtk4\PangoFontFamily::vfunc_is_monospace(): bool
 *
 * Native `is_monospace` (FontFamilyClass.is_monospace): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_is_monospace()` from an override. A monospace font is a font
 * designed for text display where the the characters form a regular grid.
 */
ZEND_METHOD(Gtk4_PangoFontFamily, vfunc_is_monospace) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontFamily *self = PHPGTK_SELF(PangoFontFamily, PANGO_TYPE_FONT_FAMILY);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "PangoFontFamily::vfunc_is_monospace(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = PANGO_FONT_FAMILY_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->is_monospace == nullptr) {
    RETURN_FALSE;
  }
  RETURN_BOOL(klass->is_monospace(self));
}

/**
 * Gtk4\PangoFontFamily::vfunc_is_variable(): bool
 *
 * Native `is_variable` (FontFamilyClass.is_variable): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_is_variable()` from an override. A variable font is a font which
 * has axes that can be modified to produce different faces.
 */
ZEND_METHOD(Gtk4_PangoFontFamily, vfunc_is_variable) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontFamily *self = PHPGTK_SELF(PangoFontFamily, PANGO_TYPE_FONT_FAMILY);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "PangoFontFamily::vfunc_is_variable(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = PANGO_FONT_FAMILY_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->is_variable == nullptr) {
    RETURN_FALSE;
  }
  RETURN_BOOL(klass->is_variable(self));
}

// MINIT: the vfunc thunks of PangoFontFamily (core/subtype.h).
void register_vfuncs_PangoFontFamily() {
  register_vfunc(PANGO_TYPE_FONT_FAMILY, "get_face", vfunc_install_get_face);
  register_vfunc(PANGO_TYPE_FONT_FAMILY, "get_name", vfunc_install_get_name);
  register_vfunc(PANGO_TYPE_FONT_FAMILY, "is_monospace", vfunc_install_is_monospace);
  register_vfunc(PANGO_TYPE_FONT_FAMILY, "is_variable", vfunc_install_is_variable);
}
