// GENERATED by gen/gir.php from Pango.Language - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\PangoLanguage (boxed value type)
#include "php_gtk4.h"
#include "core/boxed.h"
#include "core/object.h"
#include <cstring>
#include "core/enums.h"

using namespace phpgtk;

namespace {

const char *const fields[] = {nullptr};

// Boxed field reader: the public scalar fields as PHP properties.
bool read(gpointer data, const char *field, zval *rv) {
  (void)data;
  (void)field;
  (void)rv;
  return false;
}

// Boxed field writer: each field converts like a parameter of its type (core/boxed).
bool write(gpointer data, const char *field, zval *v) {
  (void)data;
  (void)field;
  (void)v;
  return false;
}

}  // namespace

/**
 * Gtk4\PangoLanguage::__construct()
 *
 * PangoLanguage values come from GTK, never from `new`.
 */
ZEND_METHOD(Gtk4_PangoLanguage, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
}

/**
 * Gtk4\PangoLanguage::get_sample_string(): string
 *
 * Get a string that is representative of the characters needed to render a particular language.
 */
ZEND_METHOD(Gtk4_PangoLanguage, get_sample_string) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoLanguage *self = PHPGTK_BOXED_SELF(PangoLanguage);
  const char *phpgtk_ret = pango_language_get_sample_string(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

/**
 * Gtk4\PangoLanguage::includes_script(PangoScript $script): bool
 *
 * Determines if $script is one of the scripts used to write $language.
 */
ZEND_METHOD(Gtk4_PangoLanguage, includes_script) {
  zval *script;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(script, enum_class_for_type(PANGO_TYPE_SCRIPT))
  ZEND_PARSE_PARAMETERS_END();
  PangoLanguage *self = PHPGTK_BOXED_SELF(PangoLanguage);
  gint script_v = 0;
  if (!enum_from_php(script, PANGO_TYPE_SCRIPT, &script_v)) RETURN_THROWS();
  RETURN_BOOL(pango_language_includes_script(self, static_cast<PangoScript>(script_v)));
}

/**
 * Gtk4\PangoLanguage::matches(string $range_list): bool
 *
 * Checks if a language tag matches one of the elements in a list of language ranges.
 */
ZEND_METHOD(Gtk4_PangoLanguage, matches) {
  zend_string *range_list;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(range_list)
  ZEND_PARSE_PARAMETERS_END();
  PangoLanguage *self = PHPGTK_BOXED_SELF(PangoLanguage);
  if (!phpgtk::check_utf8(range_list, 1)) RETURN_THROWS();
  RETURN_BOOL(pango_language_matches(self, ZSTR_VAL(range_list)));
}

/**
 * Gtk4\PangoLanguage::to_string(): string
 *
 * Gets the RFC-3066 format string representing the given language tag.
 */
ZEND_METHOD(Gtk4_PangoLanguage, to_string) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoLanguage *self = PHPGTK_BOXED_SELF(PangoLanguage);
  const char *phpgtk_ret = pango_language_to_string(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

/**
 * static Gtk4\PangoLanguage::from_string(?string $language = null): ?PangoLanguage
 *
 * Convert a language tag to a `PangoLanguage`.
 */
ZEND_METHOD(Gtk4_PangoLanguage, from_string) {
  zend_string *language = nullptr;
  ZEND_PARSE_PARAMETERS_START(0, 1)
  Z_PARAM_OPTIONAL
  Z_PARAM_STR_OR_NULL(language)
  ZEND_PARSE_PARAMETERS_END();
  if (language != nullptr && !phpgtk::check_utf8(language, 1)) RETURN_THROWS();
  PangoLanguage *phpgtk_ret =
      pango_language_from_string(language != nullptr ? ZSTR_VAL(language) : nullptr);
  wrap_boxed(PANGO_TYPE_LANGUAGE, phpgtk_ret, return_value);
}

/**
 * static Gtk4\PangoLanguage::get_default(): PangoLanguage
 *
 * Returns the `PangoLanguage` for the current locale of the process.
 */
ZEND_METHOD(Gtk4_PangoLanguage, get_default) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoLanguage *phpgtk_ret = pango_language_get_default();
  wrap_boxed(PANGO_TYPE_LANGUAGE, phpgtk_ret, return_value);
}

namespace phpgtk {
// MINIT: bind the PHP class to PANGO_TYPE_LANGUAGE with its field table.
void register_PangoLanguage(zend_class_entry *ce) {
  register_boxed(BoxedClass{
      .type = PANGO_TYPE_LANGUAGE, .ce = ce, .fields = fields, .read = read, .write = write});
}
}  // namespace phpgtk
