// GENERATED by gen/gir.php from Pango.Fontset - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\PangoFontset
#include "php_gtk4.h"
#include "core/object.h"
#include "core/enums.h"
#include "core/boxed.h"
#include "core/subtype.h"
#include "core/error.h"
#include <array>

using namespace phpgtk;

/**
 * Gtk4\PangoFontset::__construct()
 *
 * PangoFontset is abstract in GTK: instances come from GTK, never from `new`.
 */
ZEND_METHOD(Gtk4_PangoFontset, __construct) {
  // Private: never called (object_init_ex() in wrap() skips constructors).
  ZEND_PARSE_PARAMETERS_NONE();
}

/**
 * Gtk4\PangoFontset::get_font(int $wc): PangoFont
 *
 * Returns the font in the fontset that contains the best glyph for a Unicode character.
 */
ZEND_METHOD(Gtk4_PangoFontset, get_font) {
  zend_long wc;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(wc)
  ZEND_PARSE_PARAMETERS_END();
  PangoFontset *self = PHPGTK_SELF(PangoFontset, PANGO_TYPE_FONTSET);
  if (!phpgtk::check_range<guint>(wc, 1)) RETURN_THROWS();
  PangoFont *phpgtk_ret = pango_fontset_get_font(self, static_cast<guint>(wc));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * Gtk4\PangoFontset::get_metrics(): PangoFontMetrics
 *
 * Get overall metric information for the fonts in the fontset.
 */
ZEND_METHOD(Gtk4_PangoFontset, get_metrics) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontset *self = PHPGTK_SELF(PangoFontset, PANGO_TYPE_FONTSET);
  PangoFontMetrics *phpgtk_ret = pango_fontset_get_metrics(self);
  wrap_boxed(PANGO_TYPE_FONT_METRICS, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(PANGO_TYPE_FONT_METRICS, phpgtk_ret);
}

// vfunc thunks and installers: file-local, installed by class_init of a PHP subtype
namespace {

// vfunc thunk: PANGO_FONTSET_CLASS->get_font -> $this->vfunc_get_font() on a PHP subclass
PangoFont *vfunc_thunk_get_font(PangoFontset *self, guint wc) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_get_font", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = PANGO_FONTSET_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->get_font != nullptr ? native->get_font(self, wc) : nullptr;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 1> args{};
  zval *argv = args.data();
  ZVAL_LONG(&argv[0], static_cast<zend_long>(wc));
  zval ret;
  ZVAL_UNDEF(&ret);
  PangoFont *result = nullptr;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    if (Z_TYPE(ret) == IS_OBJECT) {
      GObject *o = unwrap(&ret, PANGO_TYPE_FONT);
      result = o != nullptr ? PANGO_FONT(o) : nullptr;
      if (result != nullptr) g_object_ref(result);  // transfer full
    }
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("PangoFontset::vfunc_get_font");
  zend_exception_restore();  // the parked one, previous of whatever this threw
  return result;
}

// vfunc installer: PANGO_FONTSET_CLASS->get_font (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_get_font(gpointer klass) {
  PANGO_FONTSET_CLASS(klass)->get_font = vfunc_thunk_get_font;
}

}  // namespace

/**
 * Gtk4\PangoFontset::vfunc_get_font(int $wc): PangoFont
 *
 * Native `get_font` (FontsetClass.get_font): the GTK implementation below any PHP subclass, for
 * `parent::vfunc_get_font()` from an override. Returns the font in the fontset that contains the
 * best glyph for a Unicode character.
 */
ZEND_METHOD(Gtk4_PangoFontset, vfunc_get_font) {
  zend_long wc;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(wc)
  ZEND_PARSE_PARAMETERS_END();
  PangoFontset *self = PHPGTK_SELF(PangoFontset, PANGO_TYPE_FONTSET);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "PangoFontset::vfunc_get_font(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = PANGO_FONTSET_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->get_font == nullptr) {
    enum_to_php(G_TYPE_NONE, 0, return_value);
    return;
  }
  if (!phpgtk::check_range<guint>(wc, 1)) RETURN_THROWS();
  PangoFont *phpgtk_ret = klass->get_font(self, static_cast<guint>(wc));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

// MINIT: the vfunc thunks of PangoFontset (core/subtype.h).
void register_vfuncs_PangoFontset() {
  register_vfunc(PANGO_TYPE_FONTSET, "get_font", vfunc_install_get_font);
}
