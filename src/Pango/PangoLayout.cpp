// GENERATED by gen/gir.php from Pango.Layout - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\PangoLayout
#include "php_gtk4.h"
#include "core/object.h"
#include "core/enums.h"
#include "core/gerror.h"
#include "core/boxed.h"

using namespace phpgtk;

/**
 * Gtk4\PangoLayout::__construct(PangoContext $context)
 *
 * Create a new `PangoLayout` object with attributes initialized to default values for a particular
 * `PangoContext`.
 */
ZEND_METHOD(Gtk4_PangoLayout, __construct) {
  zval *context;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(context, class_for_gtype(PANGO_TYPE_CONTEXT))
  ZEND_PARSE_PARAMETERS_END();
  GObject *context_o = unwrap(context, PANGO_TYPE_CONTEXT);
  if (context_o == nullptr) RETURN_THROWS();
  GObject *obj = G_OBJECT(pango_layout_new(PANGO_CONTEXT(context_o)));
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * static Gtk4\PangoLayout::deserialize(PangoContext $context, string $bytes, int $flags):
 * ?PangoLayout
 *
 * Loads data previously created via `serialize`.
 */
ZEND_METHOD(Gtk4_PangoLayout, deserialize) {
  zval *context;
  zend_string *bytes;
  zend_long flags;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_OBJECT_OF_CLASS(context, class_for_gtype(PANGO_TYPE_CONTEXT))
  Z_PARAM_STR(bytes)
  Z_PARAM_LONG(flags)
  ZEND_PARSE_PARAMETERS_END();
  GObject *context_o = unwrap(context, PANGO_TYPE_CONTEXT);
  if (context_o == nullptr) RETURN_THROWS();
  GBytes *bytes_b = g_bytes_new(ZSTR_VAL(bytes), ZSTR_LEN(bytes));
  if (!phpgtk::check_flags(PANGO_TYPE_LAYOUT_DESERIALIZE_FLAGS, flags, 3)) RETURN_THROWS();
  GError *error = nullptr;
  PangoLayout *call_result = pango_layout_deserialize(
      PANGO_CONTEXT(context_o), bytes_b, static_cast<PangoLayoutDeserializeFlags>(flags), &error);
  g_bytes_unref(bytes_b);
  PangoLayout *phpgtk_ret = call_result;
  if (phpgtk_ret == nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * Gtk4\PangoLayout::context_changed(): void
 *
 * Forces recomputation of any state in the `PangoLayout` that might depend on the layout's
 * context.
 */
ZEND_METHOD(Gtk4_PangoLayout, context_changed) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  pango_layout_context_changed(self);
}

/**
 * Gtk4\PangoLayout::copy(): PangoLayout
 *
 * Creates a deep copy-by-value of the layout.
 */
ZEND_METHOD(Gtk4_PangoLayout, copy) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  PangoLayout *phpgtk_ret = pango_layout_copy(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * Gtk4\PangoLayout::get_alignment(): PangoAlignment
 *
 * Gets the alignment for the layout: how partial lines are positioned within the horizontal space
 * available.
 */
ZEND_METHOD(Gtk4_PangoLayout, get_alignment) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  enum_to_php(PANGO_TYPE_ALIGNMENT, pango_layout_get_alignment(self), return_value);
}

/**
 * Gtk4\PangoLayout::get_attributes(): ?PangoAttrList
 *
 * Gets the attribute list for the layout, if any.
 */
ZEND_METHOD(Gtk4_PangoLayout, get_attributes) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  PangoAttrList *phpgtk_ret = pango_layout_get_attributes(self);
  wrap_boxed(PANGO_TYPE_ATTR_LIST, phpgtk_ret, return_value);
}

/**
 * Gtk4\PangoLayout::get_auto_dir(): bool
 *
 * Gets whether to calculate the base direction for the layout according to its contents.
 */
ZEND_METHOD(Gtk4_PangoLayout, get_auto_dir) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  RETURN_BOOL(pango_layout_get_auto_dir(self));
}

/**
 * Gtk4\PangoLayout::get_baseline(): int
 *
 * Gets the Y position of baseline of the first line in $layout.
 */
ZEND_METHOD(Gtk4_PangoLayout, get_baseline) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  RETURN_LONG(static_cast<zend_long>(pango_layout_get_baseline(self)));
}

/**
 * Gtk4\PangoLayout::get_character_count(): int
 *
 * Returns the number of Unicode characters in the the text of $layout.
 */
ZEND_METHOD(Gtk4_PangoLayout, get_character_count) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  RETURN_LONG(static_cast<zend_long>(pango_layout_get_character_count(self)));
}

/**
 * Gtk4\PangoLayout::get_context(): PangoContext
 *
 * Retrieves the `PangoContext` used for this layout.
 */
ZEND_METHOD(Gtk4_PangoLayout, get_context) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  PangoContext *phpgtk_ret = pango_layout_get_context(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\PangoLayout::get_direction(int $index): PangoDirection
 *
 * Gets the text direction at the given character position in $layout.
 */
ZEND_METHOD(Gtk4_PangoLayout, get_direction) {
  zend_long index;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(index)
  ZEND_PARSE_PARAMETERS_END();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  if (!phpgtk::check_range<int>(index, 1)) RETURN_THROWS();
  enum_to_php(PANGO_TYPE_DIRECTION, pango_layout_get_direction(self, static_cast<int>(index)),
              return_value);
}

/**
 * Gtk4\PangoLayout::get_ellipsize(): PangoEllipsizeMode
 *
 * Gets the type of ellipsization being performed for $layout.
 */
ZEND_METHOD(Gtk4_PangoLayout, get_ellipsize) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  enum_to_php(PANGO_TYPE_ELLIPSIZE_MODE, pango_layout_get_ellipsize(self), return_value);
}

/**
 * Gtk4\PangoLayout::get_font_description(): ?PangoFontDescription
 *
 * Gets the font description for the layout, if any.
 */
ZEND_METHOD(Gtk4_PangoLayout, get_font_description) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  const PangoFontDescription *phpgtk_ret = pango_layout_get_font_description(self);
  wrap_boxed(PANGO_TYPE_FONT_DESCRIPTION, phpgtk_ret, return_value);
}

/**
 * Gtk4\PangoLayout::get_height(): int
 *
 * Gets the height of layout used for ellipsization.
 */
ZEND_METHOD(Gtk4_PangoLayout, get_height) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  RETURN_LONG(static_cast<zend_long>(pango_layout_get_height(self)));
}

/**
 * Gtk4\PangoLayout::get_indent(): int
 *
 * Gets the paragraph indent width in Pango units.
 */
ZEND_METHOD(Gtk4_PangoLayout, get_indent) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  RETURN_LONG(static_cast<zend_long>(pango_layout_get_indent(self)));
}

/**
 * Gtk4\PangoLayout::get_justify(): bool
 *
 * Gets whether each complete line should be stretched to fill the entire width of the layout.
 */
ZEND_METHOD(Gtk4_PangoLayout, get_justify) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  RETURN_BOOL(pango_layout_get_justify(self));
}

/**
 * Gtk4\PangoLayout::get_justify_last_line(): bool
 *
 * Gets whether the last line should be stretched to fill the entire width of the layout.
 */
ZEND_METHOD(Gtk4_PangoLayout, get_justify_last_line) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  RETURN_BOOL(pango_layout_get_justify_last_line(self));
}

/**
 * Gtk4\PangoLayout::get_line_count(): int
 *
 * Retrieves the count of lines for the $layout.
 */
ZEND_METHOD(Gtk4_PangoLayout, get_line_count) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  RETURN_LONG(static_cast<zend_long>(pango_layout_get_line_count(self)));
}

/**
 * Gtk4\PangoLayout::get_line_spacing(): float
 *
 * Gets the line spacing factor of $layout.
 */
ZEND_METHOD(Gtk4_PangoLayout, get_line_spacing) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  RETURN_DOUBLE(pango_layout_get_line_spacing(self));
}

/**
 * Gtk4\PangoLayout::get_pixel_size(): array
 *
 * Determines the logical width and height of a `PangoLayout` in device units.
 */
ZEND_METHOD(Gtk4_PangoLayout, get_pixel_size) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  int width = 0;
  int height = 0;
  pango_layout_get_pixel_size(self, &width, &height);
  array_init_size(return_value, 2);
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(width));
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(height));
    add_next_index_zval(return_value, &item);
  }
}

/**
 * Gtk4\PangoLayout::get_serial(): int
 *
 * Returns the current serial number of $layout.
 */
ZEND_METHOD(Gtk4_PangoLayout, get_serial) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  RETURN_LONG(static_cast<zend_long>(pango_layout_get_serial(self)));
}

/**
 * Gtk4\PangoLayout::get_single_paragraph_mode(): bool
 *
 * Obtains whether $layout is in single paragraph mode.
 */
ZEND_METHOD(Gtk4_PangoLayout, get_single_paragraph_mode) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  RETURN_BOOL(pango_layout_get_single_paragraph_mode(self));
}

/**
 * Gtk4\PangoLayout::get_size(): array
 *
 * Determines the logical width and height of a `PangoLayout` in Pango units.
 */
ZEND_METHOD(Gtk4_PangoLayout, get_size) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  int width = 0;
  int height = 0;
  pango_layout_get_size(self, &width, &height);
  array_init_size(return_value, 2);
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(width));
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(height));
    add_next_index_zval(return_value, &item);
  }
}

/**
 * Gtk4\PangoLayout::get_spacing(): int
 *
 * Gets the amount of spacing between the lines of the layout.
 */
ZEND_METHOD(Gtk4_PangoLayout, get_spacing) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  RETURN_LONG(static_cast<zend_long>(pango_layout_get_spacing(self)));
}

/**
 * Gtk4\PangoLayout::get_tabs(): ?PangoTabArray
 *
 * Gets the current `PangoTabArray` used by this layout.
 */
ZEND_METHOD(Gtk4_PangoLayout, get_tabs) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  PangoTabArray *phpgtk_ret = pango_layout_get_tabs(self);
  wrap_boxed(PANGO_TYPE_TAB_ARRAY, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(PANGO_TYPE_TAB_ARRAY, phpgtk_ret);
}

/**
 * Gtk4\PangoLayout::get_text(): string
 *
 * Gets the text in the layout.
 */
ZEND_METHOD(Gtk4_PangoLayout, get_text) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  const char *phpgtk_ret = pango_layout_get_text(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

/**
 * Gtk4\PangoLayout::get_unknown_glyphs_count(): int
 *
 * Counts the number of unknown glyphs in $layout.
 */
ZEND_METHOD(Gtk4_PangoLayout, get_unknown_glyphs_count) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  RETURN_LONG(static_cast<zend_long>(pango_layout_get_unknown_glyphs_count(self)));
}

/**
 * Gtk4\PangoLayout::get_width(): int
 *
 * Gets the width to which the lines of the `PangoLayout` should wrap.
 */
ZEND_METHOD(Gtk4_PangoLayout, get_width) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  RETURN_LONG(static_cast<zend_long>(pango_layout_get_width(self)));
}

/**
 * Gtk4\PangoLayout::get_wrap(): PangoWrapMode
 *
 * Gets the wrap mode for the layout.
 */
ZEND_METHOD(Gtk4_PangoLayout, get_wrap) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  enum_to_php(PANGO_TYPE_WRAP_MODE, pango_layout_get_wrap(self), return_value);
}

/**
 * Gtk4\PangoLayout::index_to_line_x(int $index, bool $trailing): array
 *
 * Converts from byte $index_ within the $layout to line and X position.
 */
ZEND_METHOD(Gtk4_PangoLayout, index_to_line_x) {
  zend_long index;
  bool trailing;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_LONG(index)
  Z_PARAM_BOOL(trailing)
  ZEND_PARSE_PARAMETERS_END();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  if (!phpgtk::check_range<int>(index, 1)) RETURN_THROWS();
  int line = 0;
  int x_pos = 0;
  pango_layout_index_to_line_x(self, static_cast<int>(index), trailing, &line, &x_pos);
  array_init_size(return_value, 2);
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(line));
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(x_pos));
    add_next_index_zval(return_value, &item);
  }
}

/**
 * Gtk4\PangoLayout::is_ellipsized(): bool
 *
 * Queries whether the layout had to ellipsize any paragraphs.
 */
ZEND_METHOD(Gtk4_PangoLayout, is_ellipsized) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  RETURN_BOOL(pango_layout_is_ellipsized(self));
}

/**
 * Gtk4\PangoLayout::is_wrapped(): bool
 *
 * Queries whether the layout had to wrap any paragraphs.
 */
ZEND_METHOD(Gtk4_PangoLayout, is_wrapped) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  RETURN_BOOL(pango_layout_is_wrapped(self));
}

/**
 * Gtk4\PangoLayout::move_cursor_visually(bool $strong, int $old_index, int $old_trailing, int
 * $direction): array
 *
 * Computes a new cursor position from an old position and a direction.
 */
ZEND_METHOD(Gtk4_PangoLayout, move_cursor_visually) {
  bool strong;
  zend_long old_index;
  zend_long old_trailing;
  zend_long direction;
  ZEND_PARSE_PARAMETERS_START(4, 4)
  Z_PARAM_BOOL(strong)
  Z_PARAM_LONG(old_index)
  Z_PARAM_LONG(old_trailing)
  Z_PARAM_LONG(direction)
  ZEND_PARSE_PARAMETERS_END();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  if (!phpgtk::check_range<int>(old_index, 2)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(old_trailing, 3)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(direction, 4)) RETURN_THROWS();
  int new_index = 0;
  int new_trailing = 0;
  pango_layout_move_cursor_visually(self, strong, static_cast<int>(old_index),
                                    static_cast<int>(old_trailing), static_cast<int>(direction),
                                    &new_index, &new_trailing);
  array_init_size(return_value, 2);
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(new_index));
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(new_trailing));
    add_next_index_zval(return_value, &item);
  }
}

/**
 * Gtk4\PangoLayout::serialize(int $flags): string
 *
 * Serializes the $layout for later deserialization via `deserialize`.
 */
ZEND_METHOD(Gtk4_PangoLayout, serialize) {
  zend_long flags;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(flags)
  ZEND_PARSE_PARAMETERS_END();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  if (!phpgtk::check_flags(PANGO_TYPE_LAYOUT_SERIALIZE_FLAGS, flags, 1)) RETURN_THROWS();
  GBytes *bytes = pango_layout_serialize(self, static_cast<PangoLayoutSerializeFlags>(flags));
  gsize size = 0;
  const auto *data = static_cast<const char *>(g_bytes_get_data(bytes, &size));
  RETVAL_STRINGL(data, size);
  g_bytes_unref(bytes);
}

/**
 * Gtk4\PangoLayout::set_alignment(PangoAlignment $alignment): void
 *
 * Sets the alignment for the layout: how partial lines are positioned within the horizontal space
 * available.
 */
ZEND_METHOD(Gtk4_PangoLayout, set_alignment) {
  zval *alignment;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(alignment, enum_class_for_type(PANGO_TYPE_ALIGNMENT))
  ZEND_PARSE_PARAMETERS_END();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  gint alignment_v = 0;
  if (!enum_from_php(alignment, PANGO_TYPE_ALIGNMENT, &alignment_v)) RETURN_THROWS();
  pango_layout_set_alignment(self, static_cast<PangoAlignment>(alignment_v));
}

/**
 * Gtk4\PangoLayout::set_attributes(?PangoAttrList $attrs): void
 *
 * Sets the text attributes for a layout object.
 */
ZEND_METHOD(Gtk4_PangoLayout, set_attributes) {
  zval *attrs = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(attrs, boxed_class_for_type(PANGO_TYPE_ATTR_LIST)->ce)
  ZEND_PARSE_PARAMETERS_END();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  gpointer attrs_b = nullptr;
  if (attrs != nullptr) {
    attrs_b = unwrap_boxed(attrs, PANGO_TYPE_ATTR_LIST);
    if (attrs_b == nullptr) RETURN_THROWS();
  }
  pango_layout_set_attributes(self, static_cast<PangoAttrList *>(attrs_b));
}

/**
 * Gtk4\PangoLayout::set_auto_dir(bool $auto_dir): void
 *
 * Sets whether to calculate the base direction for the layout according to its contents.
 */
ZEND_METHOD(Gtk4_PangoLayout, set_auto_dir) {
  bool auto_dir;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(auto_dir)
  ZEND_PARSE_PARAMETERS_END();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  pango_layout_set_auto_dir(self, auto_dir);
}

/**
 * Gtk4\PangoLayout::set_ellipsize(PangoEllipsizeMode $ellipsize): void
 *
 * Sets the type of ellipsization being performed for $layout.
 */
ZEND_METHOD(Gtk4_PangoLayout, set_ellipsize) {
  zval *ellipsize;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(ellipsize, enum_class_for_type(PANGO_TYPE_ELLIPSIZE_MODE))
  ZEND_PARSE_PARAMETERS_END();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  gint ellipsize_v = 0;
  if (!enum_from_php(ellipsize, PANGO_TYPE_ELLIPSIZE_MODE, &ellipsize_v)) RETURN_THROWS();
  pango_layout_set_ellipsize(self, static_cast<PangoEllipsizeMode>(ellipsize_v));
}

/**
 * Gtk4\PangoLayout::set_font_description(?PangoFontDescription $desc): void
 *
 * Sets the default font description for the layout.
 */
ZEND_METHOD(Gtk4_PangoLayout, set_font_description) {
  zval *desc = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(desc, boxed_class_for_type(PANGO_TYPE_FONT_DESCRIPTION)->ce)
  ZEND_PARSE_PARAMETERS_END();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  gpointer desc_b = nullptr;
  if (desc != nullptr) {
    desc_b = unwrap_boxed(desc, PANGO_TYPE_FONT_DESCRIPTION);
    if (desc_b == nullptr) RETURN_THROWS();
  }
  pango_layout_set_font_description(self, static_cast<PangoFontDescription *>(desc_b));
}

/**
 * Gtk4\PangoLayout::set_height(int $height): void
 *
 * Sets the height to which the `PangoLayout` should be ellipsized at.
 */
ZEND_METHOD(Gtk4_PangoLayout, set_height) {
  zend_long height;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(height)
  ZEND_PARSE_PARAMETERS_END();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  if (!phpgtk::check_range<int>(height, 1)) RETURN_THROWS();
  pango_layout_set_height(self, static_cast<int>(height));
}

/**
 * Gtk4\PangoLayout::set_indent(int $indent): void
 *
 * Sets the width in Pango units to indent each paragraph.
 */
ZEND_METHOD(Gtk4_PangoLayout, set_indent) {
  zend_long indent;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(indent)
  ZEND_PARSE_PARAMETERS_END();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  if (!phpgtk::check_range<int>(indent, 1)) RETURN_THROWS();
  pango_layout_set_indent(self, static_cast<int>(indent));
}

/**
 * Gtk4\PangoLayout::set_justify(bool $justify): void
 *
 * Sets whether each complete line should be stretched to fill the entire width of the layout.
 */
ZEND_METHOD(Gtk4_PangoLayout, set_justify) {
  bool justify;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(justify)
  ZEND_PARSE_PARAMETERS_END();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  pango_layout_set_justify(self, justify);
}

/**
 * Gtk4\PangoLayout::set_justify_last_line(bool $justify): void
 *
 * Sets whether the last line should be stretched to fill the entire width of the layout.
 */
ZEND_METHOD(Gtk4_PangoLayout, set_justify_last_line) {
  bool justify;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(justify)
  ZEND_PARSE_PARAMETERS_END();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  pango_layout_set_justify_last_line(self, justify);
}

/**
 * Gtk4\PangoLayout::set_line_spacing(float $factor): void
 *
 * Sets a factor for line spacing.
 */
ZEND_METHOD(Gtk4_PangoLayout, set_line_spacing) {
  double factor;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_DOUBLE(factor)
  ZEND_PARSE_PARAMETERS_END();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  pango_layout_set_line_spacing(self, static_cast<float>(factor));
}

/**
 * Gtk4\PangoLayout::set_markup(string $markup, int $length): void
 *
 * Sets the layout text and attribute list from marked-up text.
 */
ZEND_METHOD(Gtk4_PangoLayout, set_markup) {
  zend_string *markup;
  zend_long length;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_STR(markup)
  Z_PARAM_LONG(length)
  ZEND_PARSE_PARAMETERS_END();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  if (!phpgtk::check_utf8(markup, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(length, 2)) RETURN_THROWS();
  pango_layout_set_markup(self, ZSTR_VAL(markup), static_cast<int>(length));
}

/**
 * Gtk4\PangoLayout::set_markup_with_accel(string $markup, int $length, int $accel_marker): int
 *
 * Sets the layout text and attribute list from marked-up text.
 */
ZEND_METHOD(Gtk4_PangoLayout, set_markup_with_accel) {
  zend_string *markup;
  zend_long length;
  zend_long accel_marker;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_STR(markup)
  Z_PARAM_LONG(length)
  Z_PARAM_LONG(accel_marker)
  ZEND_PARSE_PARAMETERS_END();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  if (!phpgtk::check_utf8(markup, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(length, 2)) RETURN_THROWS();
  if (!phpgtk::check_range<gunichar>(accel_marker, 3)) RETURN_THROWS();
  gunichar accel_char = 0;
  pango_layout_set_markup_with_accel(self, ZSTR_VAL(markup), static_cast<int>(length),
                                     static_cast<gunichar>(accel_marker), &accel_char);
  ZVAL_LONG(return_value, static_cast<zend_long>(accel_char));
}

/**
 * Gtk4\PangoLayout::set_single_paragraph_mode(bool $setting): void
 *
 * Sets the single paragraph mode of $layout.
 */
ZEND_METHOD(Gtk4_PangoLayout, set_single_paragraph_mode) {
  bool setting;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(setting)
  ZEND_PARSE_PARAMETERS_END();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  pango_layout_set_single_paragraph_mode(self, setting);
}

/**
 * Gtk4\PangoLayout::set_spacing(int $spacing): void
 *
 * Sets the amount of spacing in Pango units between the lines of the layout.
 */
ZEND_METHOD(Gtk4_PangoLayout, set_spacing) {
  zend_long spacing;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(spacing)
  ZEND_PARSE_PARAMETERS_END();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  if (!phpgtk::check_range<int>(spacing, 1)) RETURN_THROWS();
  pango_layout_set_spacing(self, static_cast<int>(spacing));
}

/**
 * Gtk4\PangoLayout::set_tabs(?PangoTabArray $tabs): void
 *
 * Sets the tabs to use for $layout, overriding the default tabs.
 */
ZEND_METHOD(Gtk4_PangoLayout, set_tabs) {
  zval *tabs = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(tabs, boxed_class_for_type(PANGO_TYPE_TAB_ARRAY)->ce)
  ZEND_PARSE_PARAMETERS_END();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  gpointer tabs_b = nullptr;
  if (tabs != nullptr) {
    tabs_b = unwrap_boxed(tabs, PANGO_TYPE_TAB_ARRAY);
    if (tabs_b == nullptr) RETURN_THROWS();
  }
  pango_layout_set_tabs(self, static_cast<PangoTabArray *>(tabs_b));
}

/**
 * Gtk4\PangoLayout::set_text(string $text, int $length): void
 *
 * Sets the text of the layout.
 */
ZEND_METHOD(Gtk4_PangoLayout, set_text) {
  zend_string *text;
  zend_long length;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_STR(text)
  Z_PARAM_LONG(length)
  ZEND_PARSE_PARAMETERS_END();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  if (!phpgtk::check_utf8(text, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(length, 2)) RETURN_THROWS();
  pango_layout_set_text(self, ZSTR_VAL(text), static_cast<int>(length));
}

/**
 * Gtk4\PangoLayout::set_width(int $width): void
 *
 * Sets the width to which the lines of the `PangoLayout` should wrap or ellipsized.
 */
ZEND_METHOD(Gtk4_PangoLayout, set_width) {
  zend_long width;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(width)
  ZEND_PARSE_PARAMETERS_END();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  if (!phpgtk::check_range<int>(width, 1)) RETURN_THROWS();
  pango_layout_set_width(self, static_cast<int>(width));
}

/**
 * Gtk4\PangoLayout::set_wrap(PangoWrapMode $wrap): void
 *
 * Sets the wrap mode.
 */
ZEND_METHOD(Gtk4_PangoLayout, set_wrap) {
  zval *wrap;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(wrap, enum_class_for_type(PANGO_TYPE_WRAP_MODE))
  ZEND_PARSE_PARAMETERS_END();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  gint wrap_v = 0;
  if (!enum_from_php(wrap, PANGO_TYPE_WRAP_MODE, &wrap_v)) RETURN_THROWS();
  pango_layout_set_wrap(self, static_cast<PangoWrapMode>(wrap_v));
}

/**
 * Gtk4\PangoLayout::write_to_file(int $flags, string $filename): bool
 *
 * A convenience method to serialize a layout to a file.
 */
ZEND_METHOD(Gtk4_PangoLayout, write_to_file) {
  zend_long flags;
  zend_string *filename;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_LONG(flags)
  Z_PARAM_PATH_STR(filename)
  ZEND_PARSE_PARAMETERS_END();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  if (!phpgtk::check_flags(PANGO_TYPE_LAYOUT_SERIALIZE_FLAGS, flags, 1)) RETURN_THROWS();
  zend_string *filename_abs = phpgtk::absolute_filename(filename, 2);
  if (filename_abs == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  const gboolean ok = pango_layout_write_to_file(
      self, static_cast<PangoLayoutSerializeFlags>(flags), ZSTR_VAL(filename_abs), &error);
  if (filename_abs != nullptr) zend_string_release(filename_abs);
  if (error != nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  RETURN_BOOL(ok);
}

/**
 * Gtk4\PangoLayout::xy_to_index(int $x, int $y): ?array
 *
 * Converts from X and Y position within a layout to the byte index to the character at that
 * logical position.
 */
ZEND_METHOD(Gtk4_PangoLayout, xy_to_index) {
  zend_long x;
  zend_long y;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_LONG(x)
  Z_PARAM_LONG(y)
  ZEND_PARSE_PARAMETERS_END();
  PangoLayout *self = PHPGTK_SELF(PangoLayout, PANGO_TYPE_LAYOUT);
  if (!phpgtk::check_range<int>(x, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(y, 2)) RETURN_THROWS();
  int index = 0;
  int trailing = 0;
  if (!pango_layout_xy_to_index(self, static_cast<int>(x), static_cast<int>(y), &index, &trailing))
    RETURN_NULL();
  array_init_size(return_value, 2);
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(index));
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_LONG(&item, static_cast<zend_long>(trailing));
    add_next_index_zval(return_value, &item);
  }
}
