// GENERATED by gen/gir.php from Pango.FontFace - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\PangoFontFace
#include "php_gtk4.h"
#include "core/object.h"
#include "core/enums.h"
#include "core/boxed.h"
#include "core/subtype.h"
#include "core/error.h"

using namespace phpgtk;

/**
 * Gtk4\PangoFontFace::__construct()
 *
 * PangoFontFace is abstract in GTK: instances come from GTK, never from `new`.
 */
ZEND_METHOD(Gtk4_PangoFontFace, __construct) {
  // Private: never called (object_init_ex() in wrap() skips constructors).
  ZEND_PARSE_PARAMETERS_NONE();
}

/**
 * Gtk4\PangoFontFace::describe(): PangoFontDescription
 *
 * Returns a font description that matches the face.
 */
ZEND_METHOD(Gtk4_PangoFontFace, describe) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontFace *self = PHPGTK_SELF(PangoFontFace, PANGO_TYPE_FONT_FACE);
  PangoFontDescription *phpgtk_ret = pango_font_face_describe(self);
  wrap_boxed(PANGO_TYPE_FONT_DESCRIPTION, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(PANGO_TYPE_FONT_DESCRIPTION, phpgtk_ret);
}

/**
 * Gtk4\PangoFontFace::get_face_name(): string
 *
 * Gets a name representing the style of this face.
 */
ZEND_METHOD(Gtk4_PangoFontFace, get_face_name) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontFace *self = PHPGTK_SELF(PangoFontFace, PANGO_TYPE_FONT_FACE);
  const char *phpgtk_ret = pango_font_face_get_face_name(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

/**
 * Gtk4\PangoFontFace::get_family(): PangoFontFamily
 *
 * Gets the `PangoFontFamily` that $face belongs to.
 */
ZEND_METHOD(Gtk4_PangoFontFace, get_family) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontFace *self = PHPGTK_SELF(PangoFontFace, PANGO_TYPE_FONT_FACE);
  PangoFontFamily *phpgtk_ret = pango_font_face_get_family(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\PangoFontFace::is_synthesized(): bool
 *
 * Returns whether a `PangoFontFace` is synthesized.
 */
ZEND_METHOD(Gtk4_PangoFontFace, is_synthesized) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontFace *self = PHPGTK_SELF(PangoFontFace, PANGO_TYPE_FONT_FACE);
  RETURN_BOOL(pango_font_face_is_synthesized(self));
}

// vfunc thunks and installers: file-local, installed by class_init of a PHP subtype
namespace {

// vfunc thunk: PANGO_FONT_FACE_CLASS->get_face_name -> $this->vfunc_get_face_name() on a PHP
// subclass
const char *vfunc_thunk_get_face_name(PangoFontFace *self) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_get_face_name", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = PANGO_FONT_FACE_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->get_face_name != nullptr ? native->get_face_name(self) : nullptr;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  zval ret;
  ZVAL_UNDEF(&ret);
  const char *result = nullptr;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    if (Z_TYPE(ret) == IS_STRING && phpgtk::check_utf8(Z_STR(ret), 0)) {
      result = subtype_keep_string(G_OBJECT(self), "get_face_name", Z_STRVAL(ret));
    }
  }
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("PangoFontFace::vfunc_get_face_name");
  zend_exception_restore();  // the parked one, previous of whatever this threw
  return result;
}

// vfunc installer: PANGO_FONT_FACE_CLASS->get_face_name (called from class_init / iface_init of a
// PHP subtype)
void vfunc_install_get_face_name(gpointer klass) {
  PANGO_FONT_FACE_CLASS(klass)->get_face_name = vfunc_thunk_get_face_name;
}

// vfunc thunk: PANGO_FONT_FACE_CLASS->get_family -> $this->vfunc_get_family() on a PHP subclass
PangoFontFamily *vfunc_thunk_get_family(PangoFontFace *self) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_get_family", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = PANGO_FONT_FACE_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->get_family != nullptr ? native->get_family(self) : nullptr;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  zval ret;
  ZVAL_UNDEF(&ret);
  PangoFontFamily *result = nullptr;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    if (Z_TYPE(ret) == IS_OBJECT) {
      GObject *o = unwrap(&ret, PANGO_TYPE_FONT_FAMILY);
      result = o != nullptr ? PANGO_FONT_FAMILY(o) : nullptr;
    }
  }
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("PangoFontFace::vfunc_get_family");
  zend_exception_restore();  // the parked one, previous of whatever this threw
  return result;
}

// vfunc installer: PANGO_FONT_FACE_CLASS->get_family (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_get_family(gpointer klass) {
  PANGO_FONT_FACE_CLASS(klass)->get_family = vfunc_thunk_get_family;
}

// vfunc thunk: PANGO_FONT_FACE_CLASS->is_synthesized -> $this->vfunc_is_synthesized() on a PHP
// subclass
gboolean vfunc_thunk_is_synthesized(PangoFontFace *self) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_is_synthesized", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = PANGO_FONT_FACE_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->is_synthesized != nullptr ? native->is_synthesized(self) : FALSE;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  zval ret;
  ZVAL_UNDEF(&ret);
  gboolean result = FALSE;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = zend_is_true(&ret) ? TRUE : FALSE;
  }
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("PangoFontFace::vfunc_is_synthesized");
  zend_exception_restore();  // the parked one, previous of whatever this threw
  return result;
}

// vfunc installer: PANGO_FONT_FACE_CLASS->is_synthesized (called from class_init / iface_init of a
// PHP subtype)
void vfunc_install_is_synthesized(gpointer klass) {
  PANGO_FONT_FACE_CLASS(klass)->is_synthesized = vfunc_thunk_is_synthesized;
}

}  // namespace

/**
 * Gtk4\PangoFontFace::vfunc_get_face_name(): string
 *
 * Native `get_face_name` (FontFaceClass.get_face_name): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_get_face_name()` from an override. Gets a name representing the
 * style of this face.
 */
ZEND_METHOD(Gtk4_PangoFontFace, vfunc_get_face_name) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontFace *self = PHPGTK_SELF(PangoFontFace, PANGO_TYPE_FONT_FACE);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "PangoFontFace::vfunc_get_face_name(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = PANGO_FONT_FACE_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->get_face_name == nullptr) {
    RETURN_EMPTY_STRING();
  }
  const char *phpgtk_ret = klass->get_face_name(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

/**
 * Gtk4\PangoFontFace::vfunc_get_family(): PangoFontFamily
 *
 * Native `get_family` (FontFaceClass.get_family): the GTK implementation below any PHP subclass,
 * for `parent::vfunc_get_family()` from an override. Gets the `PangoFontFamily` that $face belongs
 * to.
 */
ZEND_METHOD(Gtk4_PangoFontFace, vfunc_get_family) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontFace *self = PHPGTK_SELF(PangoFontFace, PANGO_TYPE_FONT_FACE);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "PangoFontFace::vfunc_get_family(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = PANGO_FONT_FACE_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->get_family == nullptr) {
    enum_to_php(G_TYPE_NONE, 0, return_value);
    return;
  }
  PangoFontFamily *phpgtk_ret = klass->get_family(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\PangoFontFace::vfunc_is_synthesized(): bool
 *
 * Native `is_synthesized` (FontFaceClass.is_synthesized): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_is_synthesized()` from an override. Returns whether a
 * `PangoFontFace` is synthesized.
 */
ZEND_METHOD(Gtk4_PangoFontFace, vfunc_is_synthesized) {
  ZEND_PARSE_PARAMETERS_NONE();
  PangoFontFace *self = PHPGTK_SELF(PangoFontFace, PANGO_TYPE_FONT_FACE);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "PangoFontFace::vfunc_is_synthesized(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = PANGO_FONT_FACE_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->is_synthesized == nullptr) {
    RETURN_FALSE;
  }
  RETURN_BOOL(klass->is_synthesized(self));
}

// MINIT: the vfunc thunks of PangoFontFace (core/subtype.h).
void register_vfuncs_PangoFontFace() {
  register_vfunc(PANGO_TYPE_FONT_FACE, "get_face_name", vfunc_install_get_face_name);
  register_vfunc(PANGO_TYPE_FONT_FACE, "get_family", vfunc_install_get_family);
  register_vfunc(PANGO_TYPE_FONT_FACE, "is_synthesized", vfunc_install_is_synthesized);
}
