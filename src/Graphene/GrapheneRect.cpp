// GENERATED by gen/gir.php from Graphene.Rect - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GrapheneRect (boxed value type)
#include "php_gtk4.h"
#include "core/boxed.h"
#include "core/object.h"
#include <cstring>

using namespace phpgtk;

namespace {

const char *const fields[] = {nullptr};

// Boxed field reader: the public scalar fields as PHP properties.
bool read(gpointer data, const char *field, zval *rv) {
  (void)data;
  (void)field;
  (void)rv;
  return false;
}

// Boxed field writer: each field converts like a parameter of its type (core/boxed).
bool write(gpointer data, const char *field, zval *v) {
  (void)data;
  (void)field;
  (void)v;
  return false;
}

}  // namespace

/**
 * Gtk4\GrapheneRect::__construct()
 *
 * GrapheneRect values come from GTK, never from `new`.
 */
ZEND_METHOD(Gtk4_GrapheneRect, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
}

/**
 * Gtk4\GrapheneRect::contains_point(GraphenePoint $p): bool
 *
 * Checks whether a #graphene_rect_t contains the given coordinates.
 */
ZEND_METHOD(Gtk4_GrapheneRect, contains_point) {
  zval *p;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(p, boxed_class_for_type(GRAPHENE_TYPE_POINT)->ce)
  ZEND_PARSE_PARAMETERS_END();
  graphene_rect_t *self = PHPGTK_BOXED_SELF(graphene_rect_t);
  gpointer p_b = unwrap_boxed(p, GRAPHENE_TYPE_POINT);
  if (p_b == nullptr) RETURN_THROWS();
  RETURN_BOOL(graphene_rect_contains_point(self, static_cast<graphene_point_t *>(p_b)));
}

/**
 * Gtk4\GrapheneRect::contains_rect(GrapheneRect $b): bool
 *
 * Checks whether a #graphene_rect_t fully contains the given rectangle.
 */
ZEND_METHOD(Gtk4_GrapheneRect, contains_rect) {
  zval *b;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(b, boxed_class_for_type(GRAPHENE_TYPE_RECT)->ce)
  ZEND_PARSE_PARAMETERS_END();
  graphene_rect_t *self = PHPGTK_BOXED_SELF(graphene_rect_t);
  gpointer b_b = unwrap_boxed(b, GRAPHENE_TYPE_RECT);
  if (b_b == nullptr) RETURN_THROWS();
  RETURN_BOOL(graphene_rect_contains_rect(self, static_cast<graphene_rect_t *>(b_b)));
}

/**
 * Gtk4\GrapheneRect::equal(GrapheneRect $b): bool
 *
 * Checks whether the two given rectangle are equal.
 */
ZEND_METHOD(Gtk4_GrapheneRect, equal) {
  zval *b;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(b, boxed_class_for_type(GRAPHENE_TYPE_RECT)->ce)
  ZEND_PARSE_PARAMETERS_END();
  graphene_rect_t *self = PHPGTK_BOXED_SELF(graphene_rect_t);
  gpointer b_b = unwrap_boxed(b, GRAPHENE_TYPE_RECT);
  if (b_b == nullptr) RETURN_THROWS();
  RETURN_BOOL(graphene_rect_equal(self, static_cast<graphene_rect_t *>(b_b)));
}

/**
 * Gtk4\GrapheneRect::expand(GraphenePoint $p): GrapheneRect
 *
 * Expands a #graphene_rect_t to contain the given #graphene_point_t.
 */
ZEND_METHOD(Gtk4_GrapheneRect, expand) {
  zval *p;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(p, boxed_class_for_type(GRAPHENE_TYPE_POINT)->ce)
  ZEND_PARSE_PARAMETERS_END();
  graphene_rect_t *self = PHPGTK_BOXED_SELF(graphene_rect_t);
  gpointer p_b = unwrap_boxed(p, GRAPHENE_TYPE_POINT);
  if (p_b == nullptr) RETURN_THROWS();
  graphene_rect_t res{};
  graphene_rect_expand(self, static_cast<graphene_point_t *>(p_b), &res);
  wrap_boxed(GRAPHENE_TYPE_RECT, &res, return_value);
}

/**
 * Gtk4\GrapheneRect::get_area(): float
 *
 * Compute the area of given normalized rectangle.
 */
ZEND_METHOD(Gtk4_GrapheneRect, get_area) {
  ZEND_PARSE_PARAMETERS_NONE();
  graphene_rect_t *self = PHPGTK_BOXED_SELF(graphene_rect_t);
  RETURN_DOUBLE(graphene_rect_get_area(self));
}

/**
 * Gtk4\GrapheneRect::get_bottom_left(): GraphenePoint
 *
 * Retrieves the coordinates of the bottom-left corner of the given rectangle.
 */
ZEND_METHOD(Gtk4_GrapheneRect, get_bottom_left) {
  ZEND_PARSE_PARAMETERS_NONE();
  graphene_rect_t *self = PHPGTK_BOXED_SELF(graphene_rect_t);
  graphene_point_t p{};
  graphene_rect_get_bottom_left(self, &p);
  wrap_boxed(GRAPHENE_TYPE_POINT, &p, return_value);
}

/**
 * Gtk4\GrapheneRect::get_bottom_right(): GraphenePoint
 *
 * Retrieves the coordinates of the bottom-right corner of the given rectangle.
 */
ZEND_METHOD(Gtk4_GrapheneRect, get_bottom_right) {
  ZEND_PARSE_PARAMETERS_NONE();
  graphene_rect_t *self = PHPGTK_BOXED_SELF(graphene_rect_t);
  graphene_point_t p{};
  graphene_rect_get_bottom_right(self, &p);
  wrap_boxed(GRAPHENE_TYPE_POINT, &p, return_value);
}

/**
 * Gtk4\GrapheneRect::get_center(): GraphenePoint
 *
 * Retrieves the coordinates of the center of the given rectangle.
 */
ZEND_METHOD(Gtk4_GrapheneRect, get_center) {
  ZEND_PARSE_PARAMETERS_NONE();
  graphene_rect_t *self = PHPGTK_BOXED_SELF(graphene_rect_t);
  graphene_point_t p{};
  graphene_rect_get_center(self, &p);
  wrap_boxed(GRAPHENE_TYPE_POINT, &p, return_value);
}

/**
 * Gtk4\GrapheneRect::get_height(): float
 *
 * Retrieves the normalized height of the given rectangle.
 */
ZEND_METHOD(Gtk4_GrapheneRect, get_height) {
  ZEND_PARSE_PARAMETERS_NONE();
  graphene_rect_t *self = PHPGTK_BOXED_SELF(graphene_rect_t);
  RETURN_DOUBLE(graphene_rect_get_height(self));
}

/**
 * Gtk4\GrapheneRect::get_top_left(): GraphenePoint
 *
 * Retrieves the coordinates of the top-left corner of the given rectangle.
 */
ZEND_METHOD(Gtk4_GrapheneRect, get_top_left) {
  ZEND_PARSE_PARAMETERS_NONE();
  graphene_rect_t *self = PHPGTK_BOXED_SELF(graphene_rect_t);
  graphene_point_t p{};
  graphene_rect_get_top_left(self, &p);
  wrap_boxed(GRAPHENE_TYPE_POINT, &p, return_value);
}

/**
 * Gtk4\GrapheneRect::get_top_right(): GraphenePoint
 *
 * Retrieves the coordinates of the top-right corner of the given rectangle.
 */
ZEND_METHOD(Gtk4_GrapheneRect, get_top_right) {
  ZEND_PARSE_PARAMETERS_NONE();
  graphene_rect_t *self = PHPGTK_BOXED_SELF(graphene_rect_t);
  graphene_point_t p{};
  graphene_rect_get_top_right(self, &p);
  wrap_boxed(GRAPHENE_TYPE_POINT, &p, return_value);
}

/**
 * Gtk4\GrapheneRect::get_width(): float
 *
 * Retrieves the normalized width of the given rectangle.
 */
ZEND_METHOD(Gtk4_GrapheneRect, get_width) {
  ZEND_PARSE_PARAMETERS_NONE();
  graphene_rect_t *self = PHPGTK_BOXED_SELF(graphene_rect_t);
  RETURN_DOUBLE(graphene_rect_get_width(self));
}

/**
 * Gtk4\GrapheneRect::get_x(): float
 *
 * Retrieves the normalized X coordinate of the origin of the given rectangle.
 */
ZEND_METHOD(Gtk4_GrapheneRect, get_x) {
  ZEND_PARSE_PARAMETERS_NONE();
  graphene_rect_t *self = PHPGTK_BOXED_SELF(graphene_rect_t);
  RETURN_DOUBLE(graphene_rect_get_x(self));
}

/**
 * Gtk4\GrapheneRect::get_y(): float
 *
 * Retrieves the normalized Y coordinate of the origin of the given rectangle.
 */
ZEND_METHOD(Gtk4_GrapheneRect, get_y) {
  ZEND_PARSE_PARAMETERS_NONE();
  graphene_rect_t *self = PHPGTK_BOXED_SELF(graphene_rect_t);
  RETURN_DOUBLE(graphene_rect_get_y(self));
}

/**
 * Gtk4\GrapheneRect::init(float $x, float $y, float $width, float $height): GrapheneRect
 *
 * Initializes the given #graphene_rect_t with the given values.
 */
ZEND_METHOD(Gtk4_GrapheneRect, init) {
  double x;
  double y;
  double width;
  double height;
  ZEND_PARSE_PARAMETERS_START(4, 4)
  Z_PARAM_DOUBLE(x)
  Z_PARAM_DOUBLE(y)
  Z_PARAM_DOUBLE(width)
  Z_PARAM_DOUBLE(height)
  ZEND_PARSE_PARAMETERS_END();
  graphene_rect_t *self = PHPGTK_BOXED_SELF(graphene_rect_t);
  graphene_rect_t *phpgtk_ret =
      graphene_rect_init(self, static_cast<float>(x), static_cast<float>(y),
                         static_cast<float>(width), static_cast<float>(height));
  wrap_boxed(GRAPHENE_TYPE_RECT, phpgtk_ret, return_value);
}

/**
 * Gtk4\GrapheneRect::init_from_rect(GrapheneRect $src): GrapheneRect
 *
 * Initializes $r using the given $src rectangle.
 */
ZEND_METHOD(Gtk4_GrapheneRect, init_from_rect) {
  zval *src;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(src, boxed_class_for_type(GRAPHENE_TYPE_RECT)->ce)
  ZEND_PARSE_PARAMETERS_END();
  graphene_rect_t *self = PHPGTK_BOXED_SELF(graphene_rect_t);
  gpointer src_b = unwrap_boxed(src, GRAPHENE_TYPE_RECT);
  if (src_b == nullptr) RETURN_THROWS();
  graphene_rect_t *phpgtk_ret =
      graphene_rect_init_from_rect(self, static_cast<graphene_rect_t *>(src_b));
  wrap_boxed(GRAPHENE_TYPE_RECT, phpgtk_ret, return_value);
}

/**
 * Gtk4\GrapheneRect::interpolate(GrapheneRect $b, float $factor): GrapheneRect
 *
 * Linearly interpolates the origin and size of the two given rectangles.
 */
ZEND_METHOD(Gtk4_GrapheneRect, interpolate) {
  zval *b;
  double factor;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(b, boxed_class_for_type(GRAPHENE_TYPE_RECT)->ce)
  Z_PARAM_DOUBLE(factor)
  ZEND_PARSE_PARAMETERS_END();
  graphene_rect_t *self = PHPGTK_BOXED_SELF(graphene_rect_t);
  gpointer b_b = unwrap_boxed(b, GRAPHENE_TYPE_RECT);
  if (b_b == nullptr) RETURN_THROWS();
  graphene_rect_t res{};
  graphene_rect_interpolate(self, static_cast<graphene_rect_t *>(b_b), factor, &res);
  wrap_boxed(GRAPHENE_TYPE_RECT, &res, return_value);
}

/**
 * Gtk4\GrapheneRect::intersection(GrapheneRect $b): ?GrapheneRect
 *
 * Computes the intersection of the two given rectangles.
 */
ZEND_METHOD(Gtk4_GrapheneRect, intersection) {
  zval *b;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(b, boxed_class_for_type(GRAPHENE_TYPE_RECT)->ce)
  ZEND_PARSE_PARAMETERS_END();
  graphene_rect_t *self = PHPGTK_BOXED_SELF(graphene_rect_t);
  gpointer b_b = unwrap_boxed(b, GRAPHENE_TYPE_RECT);
  if (b_b == nullptr) RETURN_THROWS();
  graphene_rect_t res{};
  if (!graphene_rect_intersection(self, static_cast<graphene_rect_t *>(b_b), &res)) RETURN_NULL();
  wrap_boxed(GRAPHENE_TYPE_RECT, &res, return_value);
}

/**
 * Gtk4\GrapheneRect::round_extents(): GrapheneRect
 *
 * Rounds the origin of the given rectangle to its nearest integer value and and recompute the size
 * so that the rectangle is large enough to contain all the conrners of the original rectangle.
 */
ZEND_METHOD(Gtk4_GrapheneRect, round_extents) {
  ZEND_PARSE_PARAMETERS_NONE();
  graphene_rect_t *self = PHPGTK_BOXED_SELF(graphene_rect_t);
  graphene_rect_t res{};
  graphene_rect_round_extents(self, &res);
  wrap_boxed(GRAPHENE_TYPE_RECT, &res, return_value);
}

/**
 * Gtk4\GrapheneRect::scale(float $s_h, float $s_v): GrapheneRect
 *
 * Scales the size and origin of a rectangle horizontaly by $s_h, and vertically by $s_v. The
 * result $res is normalized.
 */
ZEND_METHOD(Gtk4_GrapheneRect, scale) {
  double s_h;
  double s_v;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_DOUBLE(s_h)
  Z_PARAM_DOUBLE(s_v)
  ZEND_PARSE_PARAMETERS_END();
  graphene_rect_t *self = PHPGTK_BOXED_SELF(graphene_rect_t);
  graphene_rect_t res{};
  graphene_rect_scale(self, static_cast<float>(s_h), static_cast<float>(s_v), &res);
  wrap_boxed(GRAPHENE_TYPE_RECT, &res, return_value);
}

/**
 * Gtk4\GrapheneRect::union(GrapheneRect $b): GrapheneRect
 *
 * Computes the union of the two given rectangles.
 */
ZEND_METHOD(Gtk4_GrapheneRect, union) {
  zval *b;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(b, boxed_class_for_type(GRAPHENE_TYPE_RECT)->ce)
  ZEND_PARSE_PARAMETERS_END();
  graphene_rect_t *self = PHPGTK_BOXED_SELF(graphene_rect_t);
  gpointer b_b = unwrap_boxed(b, GRAPHENE_TYPE_RECT);
  if (b_b == nullptr) RETURN_THROWS();
  graphene_rect_t res{};
  graphene_rect_union(self, static_cast<graphene_rect_t *>(b_b), &res);
  wrap_boxed(GRAPHENE_TYPE_RECT, &res, return_value);
}

/**
 * static Gtk4\GrapheneRect::alloc(): GrapheneRect
 *
 * Allocates a new #graphene_rect_t.
 */
ZEND_METHOD(Gtk4_GrapheneRect, alloc) {
  ZEND_PARSE_PARAMETERS_NONE();
  graphene_rect_t *phpgtk_ret = graphene_rect_alloc();
  wrap_boxed(GRAPHENE_TYPE_RECT, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(GRAPHENE_TYPE_RECT, phpgtk_ret);
}

/**
 * static Gtk4\GrapheneRect::zero(): GrapheneRect
 *
 * Returns a degenerate rectangle with origin fixed at (0, 0) and a size of 0, 0.
 */
ZEND_METHOD(Gtk4_GrapheneRect, zero) {
  ZEND_PARSE_PARAMETERS_NONE();
  const graphene_rect_t *phpgtk_ret = graphene_rect_zero();
  wrap_boxed(GRAPHENE_TYPE_RECT, phpgtk_ret, return_value);
}

/**
 * public function inset(float $d_x, float $d_y): GrapheneRect
 * Shrink the rectangle by $d_x on each vertical edge and $d_y on each horizontal one.
 *
 * graphene's plain form writes into the rectangle it is given and hands the same one back;
 * a boxed handle here is a *value* (clone and compare by value), so a method that silently
 * rewrote the receiver would break that. This is the `inset_r()` form, which leaves the
 * receiver alone - and is why `inset_r()` itself is not bound separately.
 */
ZEND_METHOD(Gtk4_GrapheneRect, inset) {
  double d_x;
  double d_y;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_DOUBLE(d_x)
  Z_PARAM_DOUBLE(d_y)
  ZEND_PARSE_PARAMETERS_END();
  auto *self = static_cast<graphene_rect_t *>(boxed_from_zval(ZEND_THIS)->data);
  graphene_rect_t out;
  graphene_rect_inset_r(self, static_cast<float>(d_x), static_cast<float>(d_y), &out);
  wrap_boxed(GRAPHENE_TYPE_RECT, &out, return_value);
}

/**
 * public function normalize(): GrapheneRect
 * The same rectangle with a non-negative width and height.
 *
 * graphene's plain form writes into the rectangle it is given and hands the same one back;
 * a boxed handle here is a *value* (clone and compare by value), so a method that silently
 * rewrote the receiver would break that. This is the `normalize_r()` form, which leaves the
 * receiver alone - and is why `normalize_r()` itself is not bound separately.
 */
ZEND_METHOD(Gtk4_GrapheneRect, normalize) {
  ZEND_PARSE_PARAMETERS_NONE();
  auto *self = static_cast<graphene_rect_t *>(boxed_from_zval(ZEND_THIS)->data);
  graphene_rect_t out;
  graphene_rect_normalize_r(self, &out);
  wrap_boxed(GRAPHENE_TYPE_RECT, &out, return_value);
}

/**
 * public function offset(float $d_x, float $d_y): GrapheneRect
 * Move the rectangle by $d_x and $d_y.
 *
 * graphene's plain form writes into the rectangle it is given and hands the same one back;
 * a boxed handle here is a *value* (clone and compare by value), so a method that silently
 * rewrote the receiver would break that. This is the `offset_r()` form, which leaves the
 * receiver alone - and is why `offset_r()` itself is not bound separately.
 */
ZEND_METHOD(Gtk4_GrapheneRect, offset) {
  double d_x;
  double d_y;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_DOUBLE(d_x)
  Z_PARAM_DOUBLE(d_y)
  ZEND_PARSE_PARAMETERS_END();
  auto *self = static_cast<graphene_rect_t *>(boxed_from_zval(ZEND_THIS)->data);
  graphene_rect_t out;
  graphene_rect_offset_r(self, static_cast<float>(d_x), static_cast<float>(d_y), &out);
  wrap_boxed(GRAPHENE_TYPE_RECT, &out, return_value);
}

namespace phpgtk {
// MINIT: bind the PHP class to GRAPHENE_TYPE_RECT with its field table.
void register_GrapheneRect(zend_class_entry *ce) {
  register_boxed(BoxedClass{.type = GRAPHENE_TYPE_RECT,
                            .ce = ce,
                            .fields = fields,
                            .read = read,
                            .write = write,
                            .equal = [](gconstpointer a, gconstpointer b) {
                              return graphene_rect_equal(static_cast<const graphene_rect_t *>(a),
                                                         static_cast<const graphene_rect_t *>(b)) !=
                                     FALSE;
                            }});
}
}  // namespace phpgtk
