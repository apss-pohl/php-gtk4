// GENERATED by gen/gir.php from Graphene.Point - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GraphenePoint (boxed value type)
#include "php_gtk4.h"
#include "core/boxed.h"
#include "core/object.h"
#include <cstring>

using namespace phpgtk;

namespace {

const char *const fields[] = {"x", "y", nullptr};

// Boxed field reader: the public scalar fields as PHP properties.
bool read(gpointer data, const char *field, zval *rv) {
  auto *value = static_cast<graphene_point_t *>(data);
  if (strcmp(field, "x") == 0) {
    ZVAL_DOUBLE(rv, value->x);
    return true;
  }
  if (strcmp(field, "y") == 0) {
    ZVAL_DOUBLE(rv, value->y);
    return true;
  }
  return false;
}

// Boxed field writer: each field converts like a parameter of its type (core/boxed).
bool write(gpointer data, const char *field, zval *v) {
  auto *value = static_cast<graphene_point_t *>(data);
  if (strcmp(field, "x") == 0) {
    double d = 0;
    if (boxed_field_double(v, "Gtk4\\GraphenePoint", "x", &d)) value->x = static_cast<float>(d);
    return true;
  }
  if (strcmp(field, "y") == 0) {
    double d = 0;
    if (boxed_field_double(v, "Gtk4\\GraphenePoint", "y", &d)) value->y = static_cast<float>(d);
    return true;
  }
  return false;
}

}  // namespace

/**
 * Gtk4\GraphenePoint::__construct(float $x = 0.0, float $y = 0.0)
 *
 * A value from its fields (all optional, zero by default).
 */
ZEND_METHOD(Gtk4_GraphenePoint, __construct) {
  double x = 0;
  double y = 0;
  ZEND_PARSE_PARAMETERS_START(0, 2)
  Z_PARAM_OPTIONAL
  Z_PARAM_DOUBLE(x)
  Z_PARAM_DOUBLE(y)
  ZEND_PARSE_PARAMETERS_END();
  graphene_point_t value{};
  value.x = static_cast<float>(x);
  value.y = static_cast<float>(y);
  boxed_adopt(boxed_from_zval(ZEND_THIS), GRAPHENE_TYPE_POINT,
              g_boxed_copy(GRAPHENE_TYPE_POINT, &value));
}

/**
 * static Gtk4\GraphenePoint::alloc(): GraphenePoint
 *
 * Allocates a new #graphene_point_t structure.
 */
ZEND_METHOD(Gtk4_GraphenePoint, alloc) {
  ZEND_PARSE_PARAMETERS_NONE();
  gpointer phpgtk_ret = graphene_point_alloc();
  wrap_boxed(GRAPHENE_TYPE_POINT, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(GRAPHENE_TYPE_POINT, phpgtk_ret);
}

/**
 * Gtk4\GraphenePoint::equal(GraphenePoint $b): bool
 *
 * Checks if the two points $a and $b point to the same coordinates.
 */
ZEND_METHOD(Gtk4_GraphenePoint, equal) {
  zval *b;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(b, boxed_class_for_type(GRAPHENE_TYPE_POINT)->ce)
  ZEND_PARSE_PARAMETERS_END();
  graphene_point_t *self = PHPGTK_BOXED_SELF(graphene_point_t);
  gpointer b_b = unwrap_boxed(b, GRAPHENE_TYPE_POINT);
  if (b_b == nullptr) RETURN_THROWS();
  RETURN_BOOL(graphene_point_equal(self, static_cast<graphene_point_t *>(b_b)));
}

/**
 * Gtk4\GraphenePoint::init(float $x, float $y): GraphenePoint
 *
 * Initializes $p to the given $x and $y coordinates.
 */
ZEND_METHOD(Gtk4_GraphenePoint, init) {
  double x;
  double y;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_DOUBLE(x)
  Z_PARAM_DOUBLE(y)
  ZEND_PARSE_PARAMETERS_END();
  graphene_point_t *self = PHPGTK_BOXED_SELF(graphene_point_t);
  graphene_point_t *phpgtk_ret =
      graphene_point_init(self, static_cast<float>(x), static_cast<float>(y));
  wrap_boxed(GRAPHENE_TYPE_POINT, phpgtk_ret, return_value);
}

/**
 * Gtk4\GraphenePoint::init_from_point(GraphenePoint $src): GraphenePoint
 *
 * Initializes $p with the same coordinates of $src.
 */
ZEND_METHOD(Gtk4_GraphenePoint, init_from_point) {
  zval *src;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(src, boxed_class_for_type(GRAPHENE_TYPE_POINT)->ce)
  ZEND_PARSE_PARAMETERS_END();
  graphene_point_t *self = PHPGTK_BOXED_SELF(graphene_point_t);
  gpointer src_b = unwrap_boxed(src, GRAPHENE_TYPE_POINT);
  if (src_b == nullptr) RETURN_THROWS();
  graphene_point_t *phpgtk_ret =
      graphene_point_init_from_point(self, static_cast<graphene_point_t *>(src_b));
  wrap_boxed(GRAPHENE_TYPE_POINT, phpgtk_ret, return_value);
}

/**
 * Gtk4\GraphenePoint::interpolate(GraphenePoint $b, float $factor): GraphenePoint
 *
 * Linearly interpolates the coordinates of $a and $b using the given $factor.
 */
ZEND_METHOD(Gtk4_GraphenePoint, interpolate) {
  zval *b;
  double factor;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(b, boxed_class_for_type(GRAPHENE_TYPE_POINT)->ce)
  Z_PARAM_DOUBLE(factor)
  ZEND_PARSE_PARAMETERS_END();
  graphene_point_t *self = PHPGTK_BOXED_SELF(graphene_point_t);
  gpointer b_b = unwrap_boxed(b, GRAPHENE_TYPE_POINT);
  if (b_b == nullptr) RETURN_THROWS();
  graphene_point_t res{};
  graphene_point_interpolate(self, static_cast<graphene_point_t *>(b_b), factor, &res);
  wrap_boxed(GRAPHENE_TYPE_POINT, &res, return_value);
}

/**
 * Gtk4\GraphenePoint::near(GraphenePoint $b, float $epsilon): bool
 *
 * Checks whether the two points $a and $b are within the threshold of $epsilon.
 */
ZEND_METHOD(Gtk4_GraphenePoint, near) {
  zval *b;
  double epsilon;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(b, boxed_class_for_type(GRAPHENE_TYPE_POINT)->ce)
  Z_PARAM_DOUBLE(epsilon)
  ZEND_PARSE_PARAMETERS_END();
  graphene_point_t *self = PHPGTK_BOXED_SELF(graphene_point_t);
  gpointer b_b = unwrap_boxed(b, GRAPHENE_TYPE_POINT);
  if (b_b == nullptr) RETURN_THROWS();
  RETURN_BOOL(
      graphene_point_near(self, static_cast<graphene_point_t *>(b_b), static_cast<float>(epsilon)));
}

/**
 * static Gtk4\GraphenePoint::zero(): GraphenePoint
 *
 * Returns a point fixed at (0, 0).
 */
ZEND_METHOD(Gtk4_GraphenePoint, zero) {
  ZEND_PARSE_PARAMETERS_NONE();
  const graphene_point_t *phpgtk_ret = graphene_point_zero();
  wrap_boxed(GRAPHENE_TYPE_POINT, phpgtk_ret, return_value);
}

namespace phpgtk {
// MINIT: bind the PHP class to GRAPHENE_TYPE_POINT with its field table.
void register_GraphenePoint(zend_class_entry *ce) {
  register_boxed(BoxedClass{.type = GRAPHENE_TYPE_POINT,
                            .ce = ce,
                            .fields = fields,
                            .read = read,
                            .write = write,
                            .equal = [](gconstpointer a, gconstpointer b) {
                              return graphene_point_equal(
                                         static_cast<const graphene_point_t *>(a),
                                         static_cast<const graphene_point_t *>(b)) != FALSE;
                            }});
}
}  // namespace phpgtk
