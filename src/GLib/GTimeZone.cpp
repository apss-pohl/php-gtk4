// GENERATED by gen/gir.php from GLib.TimeZone - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GTimeZone (boxed value type)
#include "php_gtk4.h"
#include "core/boxed.h"
#include "core/object.h"
#include <cstring>

using namespace phpgtk;

namespace {

const char *const fields[] = {nullptr};

// Boxed field reader: the public scalar fields as PHP properties.
bool read(gpointer data, const char *field, zval *rv) {
  (void)data;
  (void)field;
  (void)rv;
  return false;
}

// Boxed field writer: each field converts like a parameter of its type (core/boxed).
bool write(gpointer data, const char *field, zval *v) {
  (void)data;
  (void)field;
  (void)v;
  return false;
}

}  // namespace

/**
 * Gtk4\GTimeZone::__construct()
 *
 * GTimeZone values come from GTK, never from `new`.
 */
ZEND_METHOD(Gtk4_GTimeZone, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
}

/**
 * static Gtk4\GTimeZone::new_identifier(?string $identifier = null): ?GTimeZone
 *
 * Creates a #GTimeZone corresponding to $identifier. If $identifier cannot be parsed or loaded,
 * `null` is returned.
 */
ZEND_METHOD(Gtk4_GTimeZone, new_identifier) {
  zend_string *identifier = nullptr;
  ZEND_PARSE_PARAMETERS_START(0, 1)
  Z_PARAM_OPTIONAL
  Z_PARAM_STR_OR_NULL(identifier)
  ZEND_PARSE_PARAMETERS_END();
  if (identifier != nullptr && !phpgtk::check_utf8(identifier, 1)) RETURN_THROWS();
  gpointer phpgtk_ret =
      g_time_zone_new_identifier(identifier != nullptr ? ZSTR_VAL(identifier) : nullptr);
  wrap_boxed(G_TYPE_TIME_ZONE, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(G_TYPE_TIME_ZONE, phpgtk_ret);
}

/**
 * static Gtk4\GTimeZone::new_local(): GTimeZone
 *
 * Creates a #GTimeZone corresponding to local time. The local time zone may change between
 * invocations to this function; for example, if the system administrator changes it.
 */
ZEND_METHOD(Gtk4_GTimeZone, new_local) {
  ZEND_PARSE_PARAMETERS_NONE();
  gpointer phpgtk_ret = g_time_zone_new_local();
  wrap_boxed(G_TYPE_TIME_ZONE, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(G_TYPE_TIME_ZONE, phpgtk_ret);
}

/**
 * static Gtk4\GTimeZone::new_offset(int $seconds): GTimeZone
 *
 * Creates a #GTimeZone corresponding to the given constant offset from UTC, in seconds.
 */
ZEND_METHOD(Gtk4_GTimeZone, new_offset) {
  zend_long seconds;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(seconds)
  ZEND_PARSE_PARAMETERS_END();
  if (!phpgtk::check_range<gint32>(seconds, 1)) RETURN_THROWS();
  gpointer phpgtk_ret = g_time_zone_new_offset(static_cast<gint32>(seconds));
  wrap_boxed(G_TYPE_TIME_ZONE, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(G_TYPE_TIME_ZONE, phpgtk_ret);
}

/**
 * static Gtk4\GTimeZone::new_utc(): GTimeZone
 *
 * Creates a #GTimeZone corresponding to UTC.
 */
ZEND_METHOD(Gtk4_GTimeZone, new_utc) {
  ZEND_PARSE_PARAMETERS_NONE();
  gpointer phpgtk_ret = g_time_zone_new_utc();
  wrap_boxed(G_TYPE_TIME_ZONE, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(G_TYPE_TIME_ZONE, phpgtk_ret);
}

/**
 * Gtk4\GTimeZone::find_interval(int $type, int $time): int
 *
 * Finds an interval within $tz that corresponds to the given $time_. The meaning of $time_ depends
 * on $type.
 */
ZEND_METHOD(Gtk4_GTimeZone, find_interval) {
  zend_long type;
  zend_long time;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_LONG(type)
  Z_PARAM_LONG(time)
  ZEND_PARSE_PARAMETERS_END();
  GTimeZone *self = PHPGTK_BOXED_SELF(GTimeZone);
  RETURN_LONG(static_cast<zend_long>(
      g_time_zone_find_interval(self, static_cast<GTimeType>(type), static_cast<gint64>(time))));
}

/**
 * Gtk4\GTimeZone::get_abbreviation(int $interval): string
 *
 * Determines the time zone abbreviation to be used during a particular $interval of time in the
 * time zone $tz.
 */
ZEND_METHOD(Gtk4_GTimeZone, get_abbreviation) {
  zend_long interval;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(interval)
  ZEND_PARSE_PARAMETERS_END();
  GTimeZone *self = PHPGTK_BOXED_SELF(GTimeZone);
  if (!phpgtk::check_range<gint>(interval, 1)) RETURN_THROWS();
  const char *phpgtk_ret = g_time_zone_get_abbreviation(self, static_cast<gint>(interval));
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

/**
 * Gtk4\GTimeZone::get_identifier(): string
 *
 * Get the identifier of this #GTimeZone, as passed to g_time_zone_new(). If the identifier passed
 * at construction time was not recognised, `UTC` will be returned. If it was `null`, the
 * identifier of the local timezone at construction time will be returned.
 */
ZEND_METHOD(Gtk4_GTimeZone, get_identifier) {
  ZEND_PARSE_PARAMETERS_NONE();
  GTimeZone *self = PHPGTK_BOXED_SELF(GTimeZone);
  const char *phpgtk_ret = g_time_zone_get_identifier(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

/**
 * Gtk4\GTimeZone::get_offset(int $interval): int
 *
 * Determines the offset to UTC in effect during a particular $interval of time in the time zone
 * $tz.
 */
ZEND_METHOD(Gtk4_GTimeZone, get_offset) {
  zend_long interval;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(interval)
  ZEND_PARSE_PARAMETERS_END();
  GTimeZone *self = PHPGTK_BOXED_SELF(GTimeZone);
  if (!phpgtk::check_range<gint>(interval, 1)) RETURN_THROWS();
  RETURN_LONG(static_cast<zend_long>(g_time_zone_get_offset(self, static_cast<gint>(interval))));
}

/**
 * Gtk4\GTimeZone::is_dst(int $interval): bool
 *
 * Determines if daylight savings time is in effect during a particular $interval of time in the
 * time zone $tz.
 */
ZEND_METHOD(Gtk4_GTimeZone, is_dst) {
  zend_long interval;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(interval)
  ZEND_PARSE_PARAMETERS_END();
  GTimeZone *self = PHPGTK_BOXED_SELF(GTimeZone);
  if (!phpgtk::check_range<gint>(interval, 1)) RETURN_THROWS();
  RETURN_BOOL(g_time_zone_is_dst(self, static_cast<gint>(interval)));
}

namespace phpgtk {
// MINIT: bind the PHP class to G_TYPE_TIME_ZONE with its field table.
void register_GTimeZone(zend_class_entry *ce) {
  register_boxed(BoxedClass{
      .type = G_TYPE_TIME_ZONE, .ce = ce, .fields = fields, .read = read, .write = write});
}
}  // namespace phpgtk
