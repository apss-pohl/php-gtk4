// GENERATED by gen/gir.php from GdkPixbuf.PixbufLoader - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GdkPixbufLoader
#include "php_gtk4.h"
#include "core/object.h"
#include "core/gerror.h"
#include "core/boxed.h"
#include "core/subtype.h"
#include "core/error.h"
#include <array>

using namespace phpgtk;

/**
 * Gtk4\GdkPixbufLoader::__construct()
 *
 * Creates a new pixbuf loader object.
 */
ZEND_METHOD(Gtk4_GdkPixbufLoader, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gdk_pixbuf_loader_new());
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * static Gtk4\GdkPixbufLoader::new_with_mime_type(string $mime_type): GdkPixbufLoader
 *
 * Creates a new pixbuf loader object that always attempts to parse image data as if it were an
 * image of MIME type $mime_type, instead of identifying the type automatically.
 */
ZEND_METHOD(Gtk4_GdkPixbufLoader, new_with_mime_type) {
  zend_string *mime_type;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(mime_type)
  ZEND_PARSE_PARAMETERS_END();
  if (!phpgtk::check_utf8(mime_type, 1)) RETURN_THROWS();
  GError *error = nullptr;
  GObject *obj = G_OBJECT(gdk_pixbuf_loader_new_with_mime_type(ZSTR_VAL(mime_type), &error));
  if (obj == nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * static Gtk4\GdkPixbufLoader::new_with_type(string $image_type): GdkPixbufLoader
 *
 * Creates a new pixbuf loader object that always attempts to parse image data as if it were an
 * image of type $image_type, instead of identifying the type automatically.
 */
ZEND_METHOD(Gtk4_GdkPixbufLoader, new_with_type) {
  zend_string *image_type;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(image_type)
  ZEND_PARSE_PARAMETERS_END();
  if (!phpgtk::check_utf8(image_type, 1)) RETURN_THROWS();
  GError *error = nullptr;
  GObject *obj = G_OBJECT(gdk_pixbuf_loader_new_with_type(ZSTR_VAL(image_type), &error));
  if (obj == nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * Gtk4\GdkPixbufLoader::close(): bool
 *
 * Informs a pixbuf loader that no further writes with gdk_pixbuf_loader_write() will occur, so
 * that it can free its internal loading structures.
 */
ZEND_METHOD(Gtk4_GdkPixbufLoader, close) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkPixbufLoader *self = PHPGTK_SELF(GdkPixbufLoader, GDK_TYPE_PIXBUF_LOADER);
  GError *error = nullptr;
  const gboolean ok = gdk_pixbuf_loader_close(self, &error);
  if (error != nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  RETURN_BOOL(ok);
}

/**
 * Gtk4\GdkPixbufLoader::get_animation(): ?GdkPixbufAnimation
 *
 * Queries the #GdkPixbufAnimation that a pixbuf loader is currently creating.
 */
ZEND_METHOD(Gtk4_GdkPixbufLoader, get_animation) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkPixbufLoader *self = PHPGTK_SELF(GdkPixbufLoader, GDK_TYPE_PIXBUF_LOADER);
  GdkPixbufAnimation *phpgtk_ret = gdk_pixbuf_loader_get_animation(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GdkPixbufLoader::get_format(): ?GdkPixbufFormat
 *
 * Obtains the available information about the format of the currently loading image file.
 */
ZEND_METHOD(Gtk4_GdkPixbufLoader, get_format) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkPixbufLoader *self = PHPGTK_SELF(GdkPixbufLoader, GDK_TYPE_PIXBUF_LOADER);
  GdkPixbufFormat *phpgtk_ret = gdk_pixbuf_loader_get_format(self);
  wrap_boxed(gdk_pixbuf_format_get_type(), phpgtk_ret, return_value);
}

/**
 * Gtk4\GdkPixbufLoader::get_pixbuf(): ?GdkPixbuf
 *
 * Queries the #GdkPixbuf that a pixbuf loader is currently creating.
 */
ZEND_METHOD(Gtk4_GdkPixbufLoader, get_pixbuf) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkPixbufLoader *self = PHPGTK_SELF(GdkPixbufLoader, GDK_TYPE_PIXBUF_LOADER);
  GdkPixbuf *phpgtk_ret = gdk_pixbuf_loader_get_pixbuf(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GdkPixbufLoader::set_size(int $width, int $height): void
 *
 * Causes the image to be scaled while it is loaded.
 */
ZEND_METHOD(Gtk4_GdkPixbufLoader, set_size) {
  zend_long width;
  zend_long height;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_LONG(width)
  Z_PARAM_LONG(height)
  ZEND_PARSE_PARAMETERS_END();
  GdkPixbufLoader *self = PHPGTK_SELF(GdkPixbufLoader, GDK_TYPE_PIXBUF_LOADER);
  if (!phpgtk::check_range<int>(width, 1)) RETURN_THROWS();
  if (!phpgtk::check_domain(width, 0, ZEND_LONG_MAX, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(height, 2)) RETURN_THROWS();
  if (!phpgtk::check_domain(height, 0, ZEND_LONG_MAX, 2)) RETURN_THROWS();
  gdk_pixbuf_loader_set_size(self, static_cast<int>(width), static_cast<int>(height));
}

/**
 * Gtk4\GdkPixbufLoader::write_bytes(string $buffer): bool
 *
 * Parses the next contents of the given image buffer.
 */
ZEND_METHOD(Gtk4_GdkPixbufLoader, write_bytes) {
  zend_string *buffer;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(buffer)
  ZEND_PARSE_PARAMETERS_END();
  GdkPixbufLoader *self = PHPGTK_SELF(GdkPixbufLoader, GDK_TYPE_PIXBUF_LOADER);
  GBytes *buffer_b = g_bytes_new(ZSTR_VAL(buffer), ZSTR_LEN(buffer));
  GError *error = nullptr;
  const gboolean ok = gdk_pixbuf_loader_write_bytes(self, buffer_b, &error);
  g_bytes_unref(buffer_b);
  if (error != nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  RETURN_BOOL(ok);
}

// vfunc thunks and installers: file-local, installed by class_init of a PHP subtype
namespace {

// vfunc thunk: GDK_PIXBUF_LOADER_CLASS->area_prepared -> $this->vfunc_area_prepared() on a PHP
// subclass
void vfunc_thunk_area_prepared(GdkPixbufLoader *self) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_area_prepared", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = GDK_PIXBUF_LOADER_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->area_prepared != nullptr) native->area_prepared(self);
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GdkPixbufLoader::vfunc_area_prepared");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: GDK_PIXBUF_LOADER_CLASS->area_prepared (called from class_init / iface_init of a
// PHP subtype)
void vfunc_install_area_prepared(gpointer klass) {
  GDK_PIXBUF_LOADER_CLASS(klass)->area_prepared = vfunc_thunk_area_prepared;
}

// vfunc thunk: GDK_PIXBUF_LOADER_CLASS->area_updated -> $this->vfunc_area_updated() on a PHP
// subclass
void vfunc_thunk_area_updated(GdkPixbufLoader *self, int x, int y, int width, int height) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_area_updated", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = GDK_PIXBUF_LOADER_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->area_updated != nullptr) native->area_updated(self, x, y, width, height);
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 4> args{};
  zval *argv = args.data();
  ZVAL_LONG(&argv[0], static_cast<zend_long>(x));
  ZVAL_LONG(&argv[1], static_cast<zend_long>(y));
  ZVAL_LONG(&argv[2], static_cast<zend_long>(width));
  ZVAL_LONG(&argv[3], static_cast<zend_long>(height));
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 4, args.data());
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GdkPixbufLoader::vfunc_area_updated");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: GDK_PIXBUF_LOADER_CLASS->area_updated (called from class_init / iface_init of a
// PHP subtype)
void vfunc_install_area_updated(gpointer klass) {
  GDK_PIXBUF_LOADER_CLASS(klass)->area_updated = vfunc_thunk_area_updated;
}

// vfunc thunk: GDK_PIXBUF_LOADER_CLASS->closed -> $this->vfunc_closed() on a PHP subclass
void vfunc_thunk_closed(GdkPixbufLoader *self) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_closed", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = GDK_PIXBUF_LOADER_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->closed != nullptr) native->closed(self);
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GdkPixbufLoader::vfunc_closed");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: GDK_PIXBUF_LOADER_CLASS->closed (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_closed(gpointer klass) {
  GDK_PIXBUF_LOADER_CLASS(klass)->closed = vfunc_thunk_closed;
}

// vfunc thunk: GDK_PIXBUF_LOADER_CLASS->size_prepared -> $this->vfunc_size_prepared() on a PHP
// subclass
void vfunc_thunk_size_prepared(GdkPixbufLoader *self, int width, int height) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_size_prepared", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = GDK_PIXBUF_LOADER_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->size_prepared != nullptr) native->size_prepared(self, width, height);
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 2> args{};
  zval *argv = args.data();
  ZVAL_LONG(&argv[0], static_cast<zend_long>(width));
  ZVAL_LONG(&argv[1], static_cast<zend_long>(height));
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 2, args.data());
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GdkPixbufLoader::vfunc_size_prepared");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: GDK_PIXBUF_LOADER_CLASS->size_prepared (called from class_init / iface_init of a
// PHP subtype)
void vfunc_install_size_prepared(gpointer klass) {
  GDK_PIXBUF_LOADER_CLASS(klass)->size_prepared = vfunc_thunk_size_prepared;
}

}  // namespace

/**
 * Gtk4\GdkPixbufLoader::vfunc_area_prepared(): void
 *
 * Native `area_prepared` (PixbufLoaderClass.area_prepared): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_area_prepared()` from an override.
 */
ZEND_METHOD(Gtk4_GdkPixbufLoader, vfunc_area_prepared) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkPixbufLoader *self = PHPGTK_SELF(GdkPixbufLoader, GDK_TYPE_PIXBUF_LOADER);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GdkPixbufLoader::vfunc_area_prepared(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GDK_PIXBUF_LOADER_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->area_prepared == nullptr) {
    return;
  }
  klass->area_prepared(self);
}

/**
 * Gtk4\GdkPixbufLoader::vfunc_area_updated(int $x, int $y, int $width, int $height): void
 *
 * Native `area_updated` (PixbufLoaderClass.area_updated): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_area_updated()` from an override.
 */
ZEND_METHOD(Gtk4_GdkPixbufLoader, vfunc_area_updated) {
  zend_long x;
  zend_long y;
  zend_long width;
  zend_long height;
  ZEND_PARSE_PARAMETERS_START(4, 4)
  Z_PARAM_LONG(x)
  Z_PARAM_LONG(y)
  Z_PARAM_LONG(width)
  Z_PARAM_LONG(height)
  ZEND_PARSE_PARAMETERS_END();
  GdkPixbufLoader *self = PHPGTK_SELF(GdkPixbufLoader, GDK_TYPE_PIXBUF_LOADER);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GdkPixbufLoader::vfunc_area_updated(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GDK_PIXBUF_LOADER_CLASS(subtype_native_class(G_OBJECT(self)));
  if (!phpgtk::check_range<int>(x, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(y, 2)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(width, 3)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(height, 4)) RETURN_THROWS();
  if (klass->area_updated == nullptr) {
    return;
  }
  klass->area_updated(self, static_cast<int>(x), static_cast<int>(y), static_cast<int>(width),
                      static_cast<int>(height));
}

/**
 * Gtk4\GdkPixbufLoader::vfunc_closed(): void
 *
 * Native `closed` (PixbufLoaderClass.closed): the GTK implementation below any PHP subclass, for
 * `parent::vfunc_closed()` from an override.
 */
ZEND_METHOD(Gtk4_GdkPixbufLoader, vfunc_closed) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkPixbufLoader *self = PHPGTK_SELF(GdkPixbufLoader, GDK_TYPE_PIXBUF_LOADER);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GdkPixbufLoader::vfunc_closed(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GDK_PIXBUF_LOADER_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->closed == nullptr) {
    return;
  }
  klass->closed(self);
}

/**
 * Gtk4\GdkPixbufLoader::vfunc_size_prepared(int $width, int $height): void
 *
 * Native `size_prepared` (PixbufLoaderClass.size_prepared): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_size_prepared()` from an override.
 */
ZEND_METHOD(Gtk4_GdkPixbufLoader, vfunc_size_prepared) {
  zend_long width;
  zend_long height;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_LONG(width)
  Z_PARAM_LONG(height)
  ZEND_PARSE_PARAMETERS_END();
  GdkPixbufLoader *self = PHPGTK_SELF(GdkPixbufLoader, GDK_TYPE_PIXBUF_LOADER);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GdkPixbufLoader::vfunc_size_prepared(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = GDK_PIXBUF_LOADER_CLASS(subtype_native_class(G_OBJECT(self)));
  if (!phpgtk::check_range<int>(width, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(height, 2)) RETURN_THROWS();
  if (klass->size_prepared == nullptr) {
    return;
  }
  klass->size_prepared(self, static_cast<int>(width), static_cast<int>(height));
}

// MINIT: the vfunc thunks of GdkPixbufLoader (core/subtype.h).
void register_vfuncs_GdkPixbufLoader() {
  register_vfunc(GDK_TYPE_PIXBUF_LOADER, "area_prepared", vfunc_install_area_prepared);
  register_vfunc(GDK_TYPE_PIXBUF_LOADER, "area_updated", vfunc_install_area_updated);
  register_vfunc(GDK_TYPE_PIXBUF_LOADER, "closed", vfunc_install_closed);
  register_vfunc(GDK_TYPE_PIXBUF_LOADER, "size_prepared", vfunc_install_size_prepared);
}
