// GENERATED by gen/gir.php from GdkPixbuf.PixbufAnimation - do not edit (gen/overrides,
// gen/skip.txt). Gtk4\GdkPixbufAnimation
#include "php_gtk4.h"
#include "core/object.h"
#include "core/gerror.h"

using namespace phpgtk;

/**
 * Gtk4\GdkPixbufAnimation::__construct()
 *
 * GdkPixbufAnimation has no constructor in GTK: instances come from GTK, never from `new`.
 */
ZEND_METHOD(Gtk4_GdkPixbufAnimation, __construct) {
  // Private: never called (object_init_ex() in wrap() skips constructors).
  ZEND_PARSE_PARAMETERS_NONE();
}

/**
 * static Gtk4\GdkPixbufAnimation::new_from_file(string $filename): GdkPixbufAnimation
 *
 * Creates a new animation by loading it from a file.
 */
ZEND_METHOD(Gtk4_GdkPixbufAnimation, new_from_file) {
  zend_string *filename;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_PATH_STR(filename)
  ZEND_PARSE_PARAMETERS_END();
  zend_string *filename_abs = phpgtk::absolute_filename(filename, 1);
  if (filename_abs == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  GdkPixbufAnimation *call_result =
      gdk_pixbuf_animation_new_from_file(ZSTR_VAL(filename_abs), &error);
  if (filename_abs != nullptr) zend_string_release(filename_abs);
  GObject *obj = G_OBJECT(call_result);
  if (obj == nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * static Gtk4\GdkPixbufAnimation::new_from_resource(string $resource_path): GdkPixbufAnimation
 *
 * Creates a new pixbuf animation by loading an image from an resource.
 */
ZEND_METHOD(Gtk4_GdkPixbufAnimation, new_from_resource) {
  zend_string *resource_path;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(resource_path)
  ZEND_PARSE_PARAMETERS_END();
  if (!phpgtk::check_utf8(resource_path, 1)) RETURN_THROWS();
  GError *error = nullptr;
  GObject *obj = G_OBJECT(gdk_pixbuf_animation_new_from_resource(ZSTR_VAL(resource_path), &error));
  if (obj == nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * static Gtk4\GdkPixbufAnimation::new_from_stream_finish(GAsyncResult $async_result):
 * GdkPixbufAnimation
 *
 * Finishes an asynchronous pixbuf animation creation operation started with
 * `new_from_stream_async`.
 */
ZEND_METHOD(Gtk4_GdkPixbufAnimation, new_from_stream_finish) {
  zval *async_result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(async_result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  GObject *async_result_o = unwrap(async_result, G_TYPE_ASYNC_RESULT);
  if (async_result_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  GObject *obj =
      G_OBJECT(gdk_pixbuf_animation_new_from_stream_finish(G_ASYNC_RESULT(async_result_o), &error));
  if (obj == nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * Gtk4\GdkPixbufAnimation::get_height(): int
 *
 * Queries the height of the bounding box of a pixbuf animation.
 */
ZEND_METHOD(Gtk4_GdkPixbufAnimation, get_height) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkPixbufAnimation *self = PHPGTK_SELF(GdkPixbufAnimation, GDK_TYPE_PIXBUF_ANIMATION);
  RETURN_LONG(static_cast<zend_long>(gdk_pixbuf_animation_get_height(self)));
}

/**
 * Gtk4\GdkPixbufAnimation::get_static_image(): GdkPixbuf
 *
 * Retrieves a static image for the animation.
 */
ZEND_METHOD(Gtk4_GdkPixbufAnimation, get_static_image) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkPixbufAnimation *self = PHPGTK_SELF(GdkPixbufAnimation, GDK_TYPE_PIXBUF_ANIMATION);
  GdkPixbuf *phpgtk_ret = gdk_pixbuf_animation_get_static_image(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GdkPixbufAnimation::get_width(): int
 *
 * Queries the width of the bounding box of a pixbuf animation.
 */
ZEND_METHOD(Gtk4_GdkPixbufAnimation, get_width) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkPixbufAnimation *self = PHPGTK_SELF(GdkPixbufAnimation, GDK_TYPE_PIXBUF_ANIMATION);
  RETURN_LONG(static_cast<zend_long>(gdk_pixbuf_animation_get_width(self)));
}

/**
 * Gtk4\GdkPixbufAnimation::is_static_image(): bool
 *
 * Checks whether the animation is a static image.
 */
ZEND_METHOD(Gtk4_GdkPixbufAnimation, is_static_image) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkPixbufAnimation *self = PHPGTK_SELF(GdkPixbufAnimation, GDK_TYPE_PIXBUF_ANIMATION);
  RETURN_BOOL(gdk_pixbuf_animation_is_static_image(self));
}
