// GENERATED by gen/gir.php from GdkPixbuf.Pixbuf - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GdkPixbuf
#include "php_gtk4.h"
#include "core/object.h"
#include "core/enums.h"
#include "core/gerror.h"
#include "core/boxed.h"
#include "core/subtype.h"
#include "core/callback.h"
#include <array>

using namespace phpgtk;

// save_to_bufferv()/savev()/get_options()/get_file_info(): option maps as PHP arrays, the
// buffer as a PHP string, the file's format as a boxed handle.
#include "core/collections.h"

#include <vector>

namespace {

// A PHP map of option name => value (both strings) as the two parallel NULL-terminated arrays
// gdk_pixbuf_save*() take. False with an argument error naming `arg` pending; the vectors keep
// the strings alive for the call.
bool options_from_php(zval *options, uint32_t arg, std::vector<char *> &keys,
                      std::vector<char *> &values) {
  zend_string *key = nullptr;
  zend_ulong index = 0;
  zval *value;
  // NOLINTNEXTLINE(readability-math-missing-parentheses) Zend macro expansion
  ZEND_HASH_FOREACH_KEY_VAL(Z_ARRVAL_P(options), index, key, value) {
    (void)index;
    if (key == nullptr || Z_TYPE_P(value) != IS_STRING) {
      zend_argument_value_error(arg, "must map option names to string values ('quality' => '90')");
      return false;
    }
    keys.push_back(ZSTR_VAL(key));
    values.push_back(Z_STRVAL_P(value));
  }
  ZEND_HASH_FOREACH_END();
  keys.push_back(nullptr);
  values.push_back(nullptr);
  return true;
}

}  // namespace

/**
 * Gtk4\GdkPixbuf::__construct(GdkColorspace $colorspace, bool $has_alpha, int $bits_per_sample, int
 * $width, int $height)
 *
 * Creates a new `GdkPixbuf` structure and allocates a buffer for it.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, __construct) {
  zval *colorspace;
  bool has_alpha;
  zend_long bits_per_sample;
  zend_long width;
  zend_long height;
  ZEND_PARSE_PARAMETERS_START(5, 5)
  Z_PARAM_OBJECT_OF_CLASS(colorspace, enum_class_for_type(GDK_TYPE_COLORSPACE))
  Z_PARAM_BOOL(has_alpha)
  Z_PARAM_LONG(bits_per_sample)
  Z_PARAM_LONG(width)
  Z_PARAM_LONG(height)
  ZEND_PARSE_PARAMETERS_END();
  gint colorspace_v = 0;
  if (!enum_from_php(colorspace, GDK_TYPE_COLORSPACE, &colorspace_v)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(bits_per_sample, 3)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(width, 4)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(height, 5)) RETURN_THROWS();
  GObject *obj =
      subtype_new(ZEND_THIS, "colorspace", static_cast<GdkColorspace>(colorspace_v), "has-alpha",
                  has_alpha, "bits-per-sample", static_cast<int>(bits_per_sample), "width",
                  static_cast<int>(width), "height", static_cast<int>(height), nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(gdk_pixbuf_new(static_cast<GdkColorspace>(colorspace_v), has_alpha,
                                  static_cast<int>(bits_per_sample), static_cast<int>(width),
                                  static_cast<int>(height)));
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * static Gtk4\GdkPixbuf::new_from_bytes(string $data, GdkColorspace $colorspace, bool $has_alpha,
 * int $bits_per_sample, int $width, int $height, int $rowstride): GdkPixbuf
 *
 * Creates a new #GdkPixbuf out of in-memory readonly image data.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, new_from_bytes) {
  zend_string *data;
  zval *colorspace;
  bool has_alpha;
  zend_long bits_per_sample;
  zend_long width;
  zend_long height;
  zend_long rowstride;
  ZEND_PARSE_PARAMETERS_START(7, 7)
  Z_PARAM_STR(data)
  Z_PARAM_OBJECT_OF_CLASS(colorspace, enum_class_for_type(GDK_TYPE_COLORSPACE))
  Z_PARAM_BOOL(has_alpha)
  Z_PARAM_LONG(bits_per_sample)
  Z_PARAM_LONG(width)
  Z_PARAM_LONG(height)
  Z_PARAM_LONG(rowstride)
  ZEND_PARSE_PARAMETERS_END();
  GBytes *data_b = g_bytes_new(ZSTR_VAL(data), ZSTR_LEN(data));
  gint colorspace_v = 0;
  if (!enum_from_php(colorspace, GDK_TYPE_COLORSPACE, &colorspace_v)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(bits_per_sample, 4)) RETURN_THROWS();
  if (!phpgtk::check_domain(bits_per_sample, 8, 8, 4)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(width, 5)) RETURN_THROWS();
  if (!phpgtk::check_domain(width, 1, ZEND_LONG_MAX, 5)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(height, 6)) RETURN_THROWS();
  if (!phpgtk::check_domain(height, 1, ZEND_LONG_MAX, 6)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(rowstride, 7)) RETURN_THROWS();
  if (!phpgtk::check_domain(rowstride, 1, ZEND_LONG_MAX, 7)) RETURN_THROWS();
  GdkPixbuf *call_result =
      gdk_pixbuf_new_from_bytes(data_b, static_cast<GdkColorspace>(colorspace_v), has_alpha,
                                static_cast<int>(bits_per_sample), static_cast<int>(width),
                                static_cast<int>(height), static_cast<int>(rowstride));
  g_bytes_unref(data_b);
  GObject *obj = G_OBJECT(call_result);
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * static Gtk4\GdkPixbuf::new_from_file(string $filename): GdkPixbuf
 *
 * Creates a new pixbuf by loading an image from a file.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, new_from_file) {
  zend_string *filename;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_PATH_STR(filename)
  ZEND_PARSE_PARAMETERS_END();
  zend_string *filename_abs = phpgtk::absolute_filename(filename, 1);
  if (filename_abs == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  GdkPixbuf *call_result = gdk_pixbuf_new_from_file(ZSTR_VAL(filename_abs), &error);
  if (filename_abs != nullptr) zend_string_release(filename_abs);
  GObject *obj = G_OBJECT(call_result);
  if (obj == nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * static Gtk4\GdkPixbuf::new_from_file_at_scale(string $filename, int $width, int $height, bool
 * $preserve_aspect_ratio): GdkPixbuf
 *
 * Creates a new pixbuf by loading an image from a file.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, new_from_file_at_scale) {
  zend_string *filename;
  zend_long width;
  zend_long height;
  bool preserve_aspect_ratio;
  ZEND_PARSE_PARAMETERS_START(4, 4)
  Z_PARAM_PATH_STR(filename)
  Z_PARAM_LONG(width)
  Z_PARAM_LONG(height)
  Z_PARAM_BOOL(preserve_aspect_ratio)
  ZEND_PARSE_PARAMETERS_END();
  zend_string *filename_abs = phpgtk::absolute_filename(filename, 1);
  if (filename_abs == nullptr) RETURN_THROWS();
  if (!phpgtk::check_range<int>(width, 2)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(height, 3)) RETURN_THROWS();
  GError *error = nullptr;
  GdkPixbuf *call_result =
      gdk_pixbuf_new_from_file_at_scale(ZSTR_VAL(filename_abs), static_cast<int>(width),
                                        static_cast<int>(height), preserve_aspect_ratio, &error);
  if (filename_abs != nullptr) zend_string_release(filename_abs);
  GObject *obj = G_OBJECT(call_result);
  if (obj == nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * static Gtk4\GdkPixbuf::new_from_file_at_size(string $filename, int $width, int $height):
 * GdkPixbuf
 *
 * Creates a new pixbuf by loading an image from a file.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, new_from_file_at_size) {
  zend_string *filename;
  zend_long width;
  zend_long height;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_PATH_STR(filename)
  Z_PARAM_LONG(width)
  Z_PARAM_LONG(height)
  ZEND_PARSE_PARAMETERS_END();
  zend_string *filename_abs = phpgtk::absolute_filename(filename, 1);
  if (filename_abs == nullptr) RETURN_THROWS();
  if (!phpgtk::check_range<int>(width, 2)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(height, 3)) RETURN_THROWS();
  GError *error = nullptr;
  GdkPixbuf *call_result = gdk_pixbuf_new_from_file_at_size(
      ZSTR_VAL(filename_abs), static_cast<int>(width), static_cast<int>(height), &error);
  if (filename_abs != nullptr) zend_string_release(filename_abs);
  GObject *obj = G_OBJECT(call_result);
  if (obj == nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * static Gtk4\GdkPixbuf::new_from_resource(string $resource_path): GdkPixbuf
 *
 * Creates a new pixbuf by loading an image from an resource.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, new_from_resource) {
  zend_string *resource_path;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(resource_path)
  ZEND_PARSE_PARAMETERS_END();
  if (!phpgtk::check_utf8(resource_path, 1)) RETURN_THROWS();
  GError *error = nullptr;
  GObject *obj = G_OBJECT(gdk_pixbuf_new_from_resource(ZSTR_VAL(resource_path), &error));
  if (obj == nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * static Gtk4\GdkPixbuf::new_from_resource_at_scale(string $resource_path, int $width, int $height,
 * bool $preserve_aspect_ratio): GdkPixbuf
 *
 * Creates a new pixbuf by loading an image from an resource.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, new_from_resource_at_scale) {
  zend_string *resource_path;
  zend_long width;
  zend_long height;
  bool preserve_aspect_ratio;
  ZEND_PARSE_PARAMETERS_START(4, 4)
  Z_PARAM_STR(resource_path)
  Z_PARAM_LONG(width)
  Z_PARAM_LONG(height)
  Z_PARAM_BOOL(preserve_aspect_ratio)
  ZEND_PARSE_PARAMETERS_END();
  if (!phpgtk::check_utf8(resource_path, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(width, 2)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(height, 3)) RETURN_THROWS();
  GError *error = nullptr;
  GObject *obj = G_OBJECT(gdk_pixbuf_new_from_resource_at_scale(
      ZSTR_VAL(resource_path), static_cast<int>(width), static_cast<int>(height),
      preserve_aspect_ratio, &error));
  if (obj == nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * static Gtk4\GdkPixbuf::new_from_stream(GInputStream $stream, ?GCancellable $cancellable = null):
 * GdkPixbuf
 *
 * Creates a new pixbuf by loading an image from an input stream.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, new_from_stream) {
  zval *stream;
  zval *cancellable = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 2)
  Z_PARAM_OBJECT_OF_CLASS(stream, class_for_gtype(G_TYPE_INPUT_STREAM))
  Z_PARAM_OPTIONAL
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  ZEND_PARSE_PARAMETERS_END();
  GObject *stream_o = unwrap(stream, G_TYPE_INPUT_STREAM);
  if (stream_o == nullptr) RETURN_THROWS();
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  GError *error = nullptr;
  GObject *obj = G_OBJECT(gdk_pixbuf_new_from_stream(
      G_INPUT_STREAM(stream_o), cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
      &error));
  if (obj == nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * static Gtk4\GdkPixbuf::new_from_stream_at_scale(GInputStream $stream, int $width, int $height,
 * bool $preserve_aspect_ratio, ?GCancellable $cancellable = null): GdkPixbuf
 *
 * Creates a new pixbuf by loading an image from an input stream.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, new_from_stream_at_scale) {
  zval *stream;
  zend_long width;
  zend_long height;
  bool preserve_aspect_ratio;
  zval *cancellable = nullptr;
  ZEND_PARSE_PARAMETERS_START(4, 5)
  Z_PARAM_OBJECT_OF_CLASS(stream, class_for_gtype(G_TYPE_INPUT_STREAM))
  Z_PARAM_LONG(width)
  Z_PARAM_LONG(height)
  Z_PARAM_BOOL(preserve_aspect_ratio)
  Z_PARAM_OPTIONAL
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  ZEND_PARSE_PARAMETERS_END();
  GObject *stream_o = unwrap(stream, G_TYPE_INPUT_STREAM);
  if (stream_o == nullptr) RETURN_THROWS();
  if (!phpgtk::check_range<gint>(width, 2)) RETURN_THROWS();
  if (!phpgtk::check_range<gint>(height, 3)) RETURN_THROWS();
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  GError *error = nullptr;
  GObject *obj = G_OBJECT(gdk_pixbuf_new_from_stream_at_scale(
      G_INPUT_STREAM(stream_o), static_cast<gint>(width), static_cast<gint>(height),
      preserve_aspect_ratio, cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
      &error));
  if (obj == nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * static Gtk4\GdkPixbuf::new_from_stream_finish(GAsyncResult $async_result): GdkPixbuf
 *
 * Finishes an asynchronous pixbuf creation operation started with
 * gdk_pixbuf_new_from_stream_async().
 */
ZEND_METHOD(Gtk4_GdkPixbuf, new_from_stream_finish) {
  zval *async_result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(async_result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  GObject *async_result_o = unwrap(async_result, G_TYPE_ASYNC_RESULT);
  if (async_result_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  GObject *obj =
      G_OBJECT(gdk_pixbuf_new_from_stream_finish(G_ASYNC_RESULT(async_result_o), &error));
  if (obj == nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * static Gtk4\GdkPixbuf::new_from_xpm_data(array $data): GdkPixbuf
 *
 * Creates a new pixbuf by parsing XPM data in memory.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, new_from_xpm_data) {
  zval *data;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_ARRAY(data)
  ZEND_PARSE_PARAMETERS_END();
  char **data_v = strv_from_php(data);
  if (data_v == nullptr) RETURN_THROWS();
  GdkPixbuf *call_result = gdk_pixbuf_new_from_xpm_data(const_cast<const char **>(data_v));
  g_strfreev(data_v);
  GObject *obj = G_OBJECT(call_result);
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * static Gtk4\GdkPixbuf::calculate_rowstride(GdkColorspace $colorspace, bool $has_alpha, int
 * $bits_per_sample, int $width, int $height): int
 *
 * Calculates the rowstride that an image created with those values would have.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, calculate_rowstride) {
  zval *colorspace;
  bool has_alpha;
  zend_long bits_per_sample;
  zend_long width;
  zend_long height;
  ZEND_PARSE_PARAMETERS_START(5, 5)
  Z_PARAM_OBJECT_OF_CLASS(colorspace, enum_class_for_type(GDK_TYPE_COLORSPACE))
  Z_PARAM_BOOL(has_alpha)
  Z_PARAM_LONG(bits_per_sample)
  Z_PARAM_LONG(width)
  Z_PARAM_LONG(height)
  ZEND_PARSE_PARAMETERS_END();
  gint colorspace_v = 0;
  if (!enum_from_php(colorspace, GDK_TYPE_COLORSPACE, &colorspace_v)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(bits_per_sample, 3)) RETURN_THROWS();
  if (!phpgtk::check_domain(bits_per_sample, 8, 8, 3)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(width, 4)) RETURN_THROWS();
  if (!phpgtk::check_domain(width, 1, ZEND_LONG_MAX, 4)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(height, 5)) RETURN_THROWS();
  if (!phpgtk::check_domain(height, 1, ZEND_LONG_MAX, 5)) RETURN_THROWS();
  RETURN_LONG(static_cast<zend_long>(gdk_pixbuf_calculate_rowstride(
      static_cast<GdkColorspace>(colorspace_v), has_alpha, static_cast<int>(bits_per_sample),
      static_cast<int>(width), static_cast<int>(height))));
}

namespace {
// AsyncReadyCallback trampoline for GdkPixbuf::get_file_info_async(): wraps the C arguments,
// invokes the PHP callable once and releases it (async scope).
void cb_get_file_info_async_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * static Gtk4\GdkPixbuf::get_file_info_async(string $filename, ?GCancellable $cancellable, callable
 * $callback): void
 *
 * Asynchronously parses an image file far enough to determine its format and size.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, get_file_info_async) {
  zend_string *filename;
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_PATH_STR(filename)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  zend_string *filename_abs = phpgtk::absolute_filename(filename, 1);
  if (filename_abs == nullptr) RETURN_THROWS();
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback =
      callback_new(&fci_callback.function_name, "GdkPixbuf::get_file_info_async");
  gdk_pixbuf_get_file_info_async(ZSTR_VAL(filename_abs),
                                 cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
                                 cb_get_file_info_async_callback, cb_callback);
  if (filename_abs != nullptr) zend_string_release(filename_abs);
}

/**
 * static Gtk4\GdkPixbuf::get_formats(): array
 *
 * Obtains the available information about the image formats supported by GdkPixbuf.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, get_formats) {
  ZEND_PARSE_PARAMETERS_NONE();
  gslist_to_php(gdk_pixbuf_get_formats(), gdk_pixbuf_format_get_type(), Transfer::Container,
                return_value);
}

/**
 * static Gtk4\GdkPixbuf::init_modules(string $path): bool
 *
 * Initalizes the gdk-pixbuf loader modules referenced by the `loaders.cache` file present inside
 * that directory.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, init_modules) {
  zend_string *path;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(path)
  ZEND_PARSE_PARAMETERS_END();
  if (!phpgtk::check_utf8(path, 1)) RETURN_THROWS();
  GError *error = nullptr;
  const gboolean ok = gdk_pixbuf_init_modules(ZSTR_VAL(path), &error);
  if (error != nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  RETURN_BOOL(ok);
}

namespace {
// AsyncReadyCallback trampoline for GdkPixbuf::new_from_stream_async(): wraps the C arguments,
// invokes the PHP callable once and releases it (async scope).
void cb_new_from_stream_async_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * static Gtk4\GdkPixbuf::new_from_stream_async(GInputStream $stream, ?GCancellable $cancellable =
 * null, ?callable $callback = null): void
 *
 * Creates a new pixbuf by asynchronously loading an image from an input stream.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, new_from_stream_async) {
  zval *stream;
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(1, 3)
  Z_PARAM_OBJECT_OF_CLASS(stream, class_for_gtype(G_TYPE_INPUT_STREAM))
  Z_PARAM_OPTIONAL
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC_OR_NULL(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  GObject *stream_o = unwrap(stream, G_TYPE_INPUT_STREAM);
  if (stream_o == nullptr) RETURN_THROWS();
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback =
      ZEND_FCI_INITIALIZED(fci_callback)
          ? callback_new(&fci_callback.function_name, "GdkPixbuf::new_from_stream_async")
          : nullptr;
  gdk_pixbuf_new_from_stream_async(
      G_INPUT_STREAM(stream_o), cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
      cb_callback != nullptr ? cb_new_from_stream_async_callback : nullptr, cb_callback);
}

namespace {
// AsyncReadyCallback trampoline for GdkPixbuf::new_from_stream_at_scale_async(): wraps the C
// arguments, invokes the PHP callable once and releases it (async scope).
void cb_new_from_stream_at_scale_async_callback(GObject *source_object, GAsyncResult *res,
                                                gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * static Gtk4\GdkPixbuf::new_from_stream_at_scale_async(GInputStream $stream, int $width, int
 * $height, bool $preserve_aspect_ratio, ?GCancellable $cancellable = null, ?callable $callback =
 * null): void
 *
 * Creates a new pixbuf by asynchronously loading an image from an input stream.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, new_from_stream_at_scale_async) {
  zval *stream;
  zend_long width;
  zend_long height;
  bool preserve_aspect_ratio;
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(4, 6)
  Z_PARAM_OBJECT_OF_CLASS(stream, class_for_gtype(G_TYPE_INPUT_STREAM))
  Z_PARAM_LONG(width)
  Z_PARAM_LONG(height)
  Z_PARAM_BOOL(preserve_aspect_ratio)
  Z_PARAM_OPTIONAL
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC_OR_NULL(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  GObject *stream_o = unwrap(stream, G_TYPE_INPUT_STREAM);
  if (stream_o == nullptr) RETURN_THROWS();
  if (!phpgtk::check_range<gint>(width, 2)) RETURN_THROWS();
  if (!phpgtk::check_range<gint>(height, 3)) RETURN_THROWS();
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback =
      ZEND_FCI_INITIALIZED(fci_callback)
          ? callback_new(&fci_callback.function_name, "GdkPixbuf::new_from_stream_at_scale_async")
          : nullptr;
  gdk_pixbuf_new_from_stream_at_scale_async(
      G_INPUT_STREAM(stream_o), static_cast<gint>(width), static_cast<gint>(height),
      preserve_aspect_ratio, cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
      cb_callback != nullptr ? cb_new_from_stream_at_scale_async_callback : nullptr, cb_callback);
}

/**
 * static Gtk4\GdkPixbuf::save_to_stream_finish(GAsyncResult $async_result): bool
 *
 * Finishes an asynchronous pixbuf save operation started with gdk_pixbuf_save_to_stream_async().
 */
ZEND_METHOD(Gtk4_GdkPixbuf, save_to_stream_finish) {
  zval *async_result;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(async_result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  ZEND_PARSE_PARAMETERS_END();
  GObject *async_result_o = unwrap(async_result, G_TYPE_ASYNC_RESULT);
  if (async_result_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  const gboolean ok = gdk_pixbuf_save_to_stream_finish(G_ASYNC_RESULT(async_result_o), &error);
  if (error != nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  RETURN_BOOL(ok);
}

/**
 * Gtk4\GdkPixbuf::add_alpha(bool $substitute_color, int $r, int $g, int $b): GdkPixbuf
 *
 * Takes an existing pixbuf and adds an alpha channel to it.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, add_alpha) {
  bool substitute_color;
  zend_long r;
  zend_long g;
  zend_long b;
  ZEND_PARSE_PARAMETERS_START(4, 4)
  Z_PARAM_BOOL(substitute_color)
  Z_PARAM_LONG(r)
  Z_PARAM_LONG(g)
  Z_PARAM_LONG(b)
  ZEND_PARSE_PARAMETERS_END();
  GdkPixbuf *self = PHPGTK_SELF(GdkPixbuf, GDK_TYPE_PIXBUF);
  if (!phpgtk::check_range<guchar>(r, 2)) RETURN_THROWS();
  if (!phpgtk::check_range<guchar>(g, 3)) RETURN_THROWS();
  if (!phpgtk::check_range<guchar>(b, 4)) RETURN_THROWS();
  GdkPixbuf *phpgtk_ret = gdk_pixbuf_add_alpha(self, substitute_color, static_cast<guchar>(r),
                                               static_cast<guchar>(g), static_cast<guchar>(b));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * Gtk4\GdkPixbuf::apply_embedded_orientation(): ?GdkPixbuf
 *
 * Takes an existing pixbuf and checks for the presence of an associated "orientation" option.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, apply_embedded_orientation) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkPixbuf *self = PHPGTK_SELF(GdkPixbuf, GDK_TYPE_PIXBUF);
  GdkPixbuf *phpgtk_ret = gdk_pixbuf_apply_embedded_orientation(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * Gtk4\GdkPixbuf::composite(GdkPixbuf $dest, int $dest_x, int $dest_y, int $dest_width, int
 * $dest_height, float $offset_x, float $offset_y, float $scale_x, float $scale_y, GdkInterpType
 * $interp_type, int $overall_alpha): void
 *
 * Creates a transformation of the source image $src by scaling by $scale_x and $scale_y then
 * translating by $offset_x and $offset_y.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, composite) {
  zval *dest;
  zend_long dest_x;
  zend_long dest_y;
  zend_long dest_width;
  zend_long dest_height;
  double offset_x;
  double offset_y;
  double scale_x;
  double scale_y;
  zval *interp_type;
  zend_long overall_alpha;
  ZEND_PARSE_PARAMETERS_START(11, 11)
  Z_PARAM_OBJECT_OF_CLASS(dest, class_for_gtype(GDK_TYPE_PIXBUF))
  Z_PARAM_LONG(dest_x)
  Z_PARAM_LONG(dest_y)
  Z_PARAM_LONG(dest_width)
  Z_PARAM_LONG(dest_height)
  Z_PARAM_DOUBLE(offset_x)
  Z_PARAM_DOUBLE(offset_y)
  Z_PARAM_DOUBLE(scale_x)
  Z_PARAM_DOUBLE(scale_y)
  Z_PARAM_OBJECT_OF_CLASS(interp_type, enum_class_for_type(GDK_TYPE_INTERP_TYPE))
  Z_PARAM_LONG(overall_alpha)
  ZEND_PARSE_PARAMETERS_END();
  GdkPixbuf *self = PHPGTK_SELF(GdkPixbuf, GDK_TYPE_PIXBUF);
  GObject *dest_o = unwrap(dest, GDK_TYPE_PIXBUF);
  if (dest_o == nullptr) RETURN_THROWS();
  if (!phpgtk::check_range<int>(dest_x, 2)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(dest_y, 3)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(dest_width, 4)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(dest_height, 5)) RETURN_THROWS();
  gint interp_type_v = 0;
  if (!enum_from_php(interp_type, GDK_TYPE_INTERP_TYPE, &interp_type_v)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(overall_alpha, 11)) RETURN_THROWS();
  gdk_pixbuf_composite(self, GDK_PIXBUF(dest_o), static_cast<int>(dest_x), static_cast<int>(dest_y),
                       static_cast<int>(dest_width), static_cast<int>(dest_height), offset_x,
                       offset_y, scale_x, scale_y, static_cast<GdkInterpType>(interp_type_v),
                       static_cast<int>(overall_alpha));
}

/**
 * Gtk4\GdkPixbuf::composite_color_simple(int $dest_width, int $dest_height, GdkInterpType
 * $interp_type, int $overall_alpha, int $check_size, int $color1, int $color2): ?GdkPixbuf
 *
 * Creates a new pixbuf by scaling `src` to `dest_width` x `dest_height` and alpha blending the
 * result with a checkboard of colors `color1` and `color2`.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, composite_color_simple) {
  zend_long dest_width;
  zend_long dest_height;
  zval *interp_type;
  zend_long overall_alpha;
  zend_long check_size;
  zend_long color1;
  zend_long color2;
  ZEND_PARSE_PARAMETERS_START(7, 7)
  Z_PARAM_LONG(dest_width)
  Z_PARAM_LONG(dest_height)
  Z_PARAM_OBJECT_OF_CLASS(interp_type, enum_class_for_type(GDK_TYPE_INTERP_TYPE))
  Z_PARAM_LONG(overall_alpha)
  Z_PARAM_LONG(check_size)
  Z_PARAM_LONG(color1)
  Z_PARAM_LONG(color2)
  ZEND_PARSE_PARAMETERS_END();
  GdkPixbuf *self = PHPGTK_SELF(GdkPixbuf, GDK_TYPE_PIXBUF);
  if (!phpgtk::check_range<int>(dest_width, 1)) RETURN_THROWS();
  if (!phpgtk::check_domain(dest_width, 1, ZEND_LONG_MAX, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(dest_height, 2)) RETURN_THROWS();
  if (!phpgtk::check_domain(dest_height, 1, ZEND_LONG_MAX, 2)) RETURN_THROWS();
  gint interp_type_v = 0;
  if (!enum_from_php(interp_type, GDK_TYPE_INTERP_TYPE, &interp_type_v)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(overall_alpha, 4)) RETURN_THROWS();
  if (!phpgtk::check_domain(overall_alpha, 0, 255, 4)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(check_size, 5)) RETURN_THROWS();
  if (!phpgtk::check_domain(check_size, 0, ZEND_LONG_MAX, 5)) RETURN_THROWS();
  if (!phpgtk::check_range<guint32>(color1, 6)) RETURN_THROWS();
  if (!phpgtk::check_range<guint32>(color2, 7)) RETURN_THROWS();
  GdkPixbuf *phpgtk_ret = gdk_pixbuf_composite_color_simple(
      self, static_cast<int>(dest_width), static_cast<int>(dest_height),
      static_cast<GdkInterpType>(interp_type_v), static_cast<int>(overall_alpha),
      static_cast<int>(check_size), static_cast<guint32>(color1), static_cast<guint32>(color2));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * Gtk4\GdkPixbuf::copy(): ?GdkPixbuf
 *
 * Creates a new `GdkPixbuf` with a copy of the information in the specified `pixbuf`.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, copy) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkPixbuf *self = PHPGTK_SELF(GdkPixbuf, GDK_TYPE_PIXBUF);
  GdkPixbuf *phpgtk_ret = gdk_pixbuf_copy(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * Gtk4\GdkPixbuf::copy_area(int $src_x, int $src_y, int $width, int $height, GdkPixbuf
 * $dest_pixbuf, int $dest_x, int $dest_y): void
 *
 * Copies a rectangular area from `src_pixbuf` to `dest_pixbuf`.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, copy_area) {
  zend_long src_x;
  zend_long src_y;
  zend_long width;
  zend_long height;
  zval *dest_pixbuf;
  zend_long dest_x;
  zend_long dest_y;
  ZEND_PARSE_PARAMETERS_START(7, 7)
  Z_PARAM_LONG(src_x)
  Z_PARAM_LONG(src_y)
  Z_PARAM_LONG(width)
  Z_PARAM_LONG(height)
  Z_PARAM_OBJECT_OF_CLASS(dest_pixbuf, class_for_gtype(GDK_TYPE_PIXBUF))
  Z_PARAM_LONG(dest_x)
  Z_PARAM_LONG(dest_y)
  ZEND_PARSE_PARAMETERS_END();
  GdkPixbuf *self = PHPGTK_SELF(GdkPixbuf, GDK_TYPE_PIXBUF);
  if (!phpgtk::check_range<int>(src_x, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(src_y, 2)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(width, 3)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(height, 4)) RETURN_THROWS();
  GObject *dest_pixbuf_o = unwrap(dest_pixbuf, GDK_TYPE_PIXBUF);
  if (dest_pixbuf_o == nullptr) RETURN_THROWS();
  if (!phpgtk::check_range<int>(dest_x, 6)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(dest_y, 7)) RETURN_THROWS();
  gdk_pixbuf_copy_area(self, static_cast<int>(src_x), static_cast<int>(src_y),
                       static_cast<int>(width), static_cast<int>(height), GDK_PIXBUF(dest_pixbuf_o),
                       static_cast<int>(dest_x), static_cast<int>(dest_y));
}

/**
 * Gtk4\GdkPixbuf::copy_options(GdkPixbuf $dest_pixbuf): bool
 *
 * Copies the key/value pair options attached to a `GdkPixbuf` to another `GdkPixbuf`.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, copy_options) {
  zval *dest_pixbuf;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(dest_pixbuf, class_for_gtype(GDK_TYPE_PIXBUF))
  ZEND_PARSE_PARAMETERS_END();
  GdkPixbuf *self = PHPGTK_SELF(GdkPixbuf, GDK_TYPE_PIXBUF);
  GObject *dest_pixbuf_o = unwrap(dest_pixbuf, GDK_TYPE_PIXBUF);
  if (dest_pixbuf_o == nullptr) RETURN_THROWS();
  RETURN_BOOL(gdk_pixbuf_copy_options(self, GDK_PIXBUF(dest_pixbuf_o)));
}

/**
 * Gtk4\GdkPixbuf::fill(int $pixel): void
 *
 * Clears a pixbuf to the given RGBA value, converting the RGBA value into the pixbuf's pixel
 * format.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, fill) {
  zend_long pixel;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(pixel)
  ZEND_PARSE_PARAMETERS_END();
  GdkPixbuf *self = PHPGTK_SELF(GdkPixbuf, GDK_TYPE_PIXBUF);
  if (!phpgtk::check_range<guint32>(pixel, 1)) RETURN_THROWS();
  gdk_pixbuf_fill(self, static_cast<guint32>(pixel));
}

/**
 * Gtk4\GdkPixbuf::flip(bool $horizontal): ?GdkPixbuf
 *
 * Flips a pixbuf horizontally or vertically and returns the result in a new pixbuf.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, flip) {
  bool horizontal;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(horizontal)
  ZEND_PARSE_PARAMETERS_END();
  GdkPixbuf *self = PHPGTK_SELF(GdkPixbuf, GDK_TYPE_PIXBUF);
  GdkPixbuf *phpgtk_ret = gdk_pixbuf_flip(self, horizontal);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * Gtk4\GdkPixbuf::get_bits_per_sample(): int
 *
 * Queries the number of bits per color sample in a pixbuf.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, get_bits_per_sample) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkPixbuf *self = PHPGTK_SELF(GdkPixbuf, GDK_TYPE_PIXBUF);
  RETURN_LONG(static_cast<zend_long>(gdk_pixbuf_get_bits_per_sample(self)));
}

/**
 * Gtk4\GdkPixbuf::get_byte_length(): int
 *
 * Returns the length of the pixel data, in bytes.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, get_byte_length) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkPixbuf *self = PHPGTK_SELF(GdkPixbuf, GDK_TYPE_PIXBUF);
  RETURN_LONG(static_cast<zend_long>(gdk_pixbuf_get_byte_length(self)));
}

/**
 * Gtk4\GdkPixbuf::get_colorspace(): GdkColorspace
 *
 * Queries the color space of a pixbuf.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, get_colorspace) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkPixbuf *self = PHPGTK_SELF(GdkPixbuf, GDK_TYPE_PIXBUF);
  enum_to_php(GDK_TYPE_COLORSPACE, gdk_pixbuf_get_colorspace(self), return_value);
}

/**
 * Gtk4\GdkPixbuf::get_has_alpha(): bool
 *
 * Queries whether a pixbuf has an alpha channel (opacity information).
 */
ZEND_METHOD(Gtk4_GdkPixbuf, get_has_alpha) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkPixbuf *self = PHPGTK_SELF(GdkPixbuf, GDK_TYPE_PIXBUF);
  RETURN_BOOL(gdk_pixbuf_get_has_alpha(self));
}

/**
 * Gtk4\GdkPixbuf::get_height(): int
 *
 * Queries the height of a pixbuf.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, get_height) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkPixbuf *self = PHPGTK_SELF(GdkPixbuf, GDK_TYPE_PIXBUF);
  RETURN_LONG(static_cast<zend_long>(gdk_pixbuf_get_height(self)));
}

/**
 * Gtk4\GdkPixbuf::get_n_channels(): int
 *
 * Queries the number of channels of a pixbuf.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, get_n_channels) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkPixbuf *self = PHPGTK_SELF(GdkPixbuf, GDK_TYPE_PIXBUF);
  RETURN_LONG(static_cast<zend_long>(gdk_pixbuf_get_n_channels(self)));
}

/**
 * Gtk4\GdkPixbuf::get_option(string $key): ?string
 *
 * Looks up $key in the list of options that may have been attached to the $pixbuf when it was
 * loaded, or that may have been attached by another function using gdk_pixbuf_set_option().
 */
ZEND_METHOD(Gtk4_GdkPixbuf, get_option) {
  zend_string *key;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(key)
  ZEND_PARSE_PARAMETERS_END();
  GdkPixbuf *self = PHPGTK_SELF(GdkPixbuf, GDK_TYPE_PIXBUF);
  if (!phpgtk::check_utf8(key, 1)) RETURN_THROWS();
  PHPGTK_RETURN_STRING_OR_NULL(gdk_pixbuf_get_option(self, ZSTR_VAL(key)));
}

/**
 * Gtk4\GdkPixbuf::get_rowstride(): int
 *
 * Queries the rowstride of a pixbuf, which is the number of bytes between the start of a row and
 * the start of the next row.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, get_rowstride) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkPixbuf *self = PHPGTK_SELF(GdkPixbuf, GDK_TYPE_PIXBUF);
  RETURN_LONG(static_cast<zend_long>(gdk_pixbuf_get_rowstride(self)));
}

/**
 * Gtk4\GdkPixbuf::get_width(): int
 *
 * Queries the width of a pixbuf.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, get_width) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkPixbuf *self = PHPGTK_SELF(GdkPixbuf, GDK_TYPE_PIXBUF);
  RETURN_LONG(static_cast<zend_long>(gdk_pixbuf_get_width(self)));
}

/**
 * Gtk4\GdkPixbuf::new_subpixbuf(int $src_x, int $src_y, int $width, int $height): GdkPixbuf
 *
 * Creates a new pixbuf which represents a sub-region of `src_pixbuf`.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, new_subpixbuf) {
  zend_long src_x;
  zend_long src_y;
  zend_long width;
  zend_long height;
  ZEND_PARSE_PARAMETERS_START(4, 4)
  Z_PARAM_LONG(src_x)
  Z_PARAM_LONG(src_y)
  Z_PARAM_LONG(width)
  Z_PARAM_LONG(height)
  ZEND_PARSE_PARAMETERS_END();
  GdkPixbuf *self = PHPGTK_SELF(GdkPixbuf, GDK_TYPE_PIXBUF);
  if (!phpgtk::check_range<int>(src_x, 1)) RETURN_THROWS();
  if (!phpgtk::check_domain(src_x, 0, ZEND_LONG_MAX, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(src_y, 2)) RETURN_THROWS();
  if (!phpgtk::check_domain(src_y, 0, ZEND_LONG_MAX, 2)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(width, 3)) RETURN_THROWS();
  if (!phpgtk::check_domain(width, 1, ZEND_LONG_MAX, 3)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(height, 4)) RETURN_THROWS();
  if (!phpgtk::check_domain(height, 1, ZEND_LONG_MAX, 4)) RETURN_THROWS();
  const bool precondition_0 = src_x + width <= gdk_pixbuf_get_width(self);
  if (!precondition_0) {
    zend_argument_value_error(1, "plus the width must not pass the right edge");
    RETURN_THROWS();
  }
  const bool precondition_1 = src_y + height <= gdk_pixbuf_get_height(self);
  if (!precondition_1) {
    zend_argument_value_error(2, "plus the height must not pass the bottom edge");
    RETURN_THROWS();
  }
  GdkPixbuf *phpgtk_ret =
      gdk_pixbuf_new_subpixbuf(self, static_cast<int>(src_x), static_cast<int>(src_y),
                               static_cast<int>(width), static_cast<int>(height));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * Gtk4\GdkPixbuf::read_pixel_bytes(): string
 *
 * Provides a #GBytes buffer containing the raw pixel data; the data must not be modified.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, read_pixel_bytes) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkPixbuf *self = PHPGTK_SELF(GdkPixbuf, GDK_TYPE_PIXBUF);
  GBytes *bytes = gdk_pixbuf_read_pixel_bytes(self);
  gsize size = 0;
  const auto *data = static_cast<const char *>(g_bytes_get_data(bytes, &size));
  RETVAL_STRINGL(data, size);
  g_bytes_unref(bytes);
}

/**
 * Gtk4\GdkPixbuf::remove_option(string $key): bool
 *
 * Removes the key/value pair option attached to a `GdkPixbuf`.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, remove_option) {
  zend_string *key;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(key)
  ZEND_PARSE_PARAMETERS_END();
  GdkPixbuf *self = PHPGTK_SELF(GdkPixbuf, GDK_TYPE_PIXBUF);
  if (!phpgtk::check_utf8(key, 1)) RETURN_THROWS();
  RETURN_BOOL(gdk_pixbuf_remove_option(self, ZSTR_VAL(key)));
}

/**
 * Gtk4\GdkPixbuf::rotate_simple(GdkPixbufRotation $angle): ?GdkPixbuf
 *
 * Rotates a pixbuf by a multiple of 90 degrees, and returns the result in a new pixbuf.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, rotate_simple) {
  zval *angle;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(angle, enum_class_for_type(GDK_TYPE_PIXBUF_ROTATION))
  ZEND_PARSE_PARAMETERS_END();
  GdkPixbuf *self = PHPGTK_SELF(GdkPixbuf, GDK_TYPE_PIXBUF);
  gint angle_v = 0;
  if (!enum_from_php(angle, GDK_TYPE_PIXBUF_ROTATION, &angle_v)) RETURN_THROWS();
  GdkPixbuf *phpgtk_ret = gdk_pixbuf_rotate_simple(self, static_cast<GdkPixbufRotation>(angle_v));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * Gtk4\GdkPixbuf::saturate_and_pixelate(GdkPixbuf $dest, float $saturation, bool $pixelate): void
 *
 * Modifies saturation and optionally pixelates `src`, placing the result in `dest`.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, saturate_and_pixelate) {
  zval *dest;
  double saturation;
  bool pixelate;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_OBJECT_OF_CLASS(dest, class_for_gtype(GDK_TYPE_PIXBUF))
  Z_PARAM_DOUBLE(saturation)
  Z_PARAM_BOOL(pixelate)
  ZEND_PARSE_PARAMETERS_END();
  GdkPixbuf *self = PHPGTK_SELF(GdkPixbuf, GDK_TYPE_PIXBUF);
  GObject *dest_o = unwrap(dest, GDK_TYPE_PIXBUF);
  if (dest_o == nullptr) RETURN_THROWS();
  gdk_pixbuf_saturate_and_pixelate(self, GDK_PIXBUF(dest_o), static_cast<float>(saturation),
                                   pixelate);
}

/**
 * Gtk4\GdkPixbuf::save_to_streamv(GOutputStream $stream, string $type, ?array $option_keys, ?array
 * $option_values, ?GCancellable $cancellable): bool
 *
 * Saves `pixbuf` to an output stream.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, save_to_streamv) {
  zval *stream;
  zend_string *type;
  zval *option_keys = nullptr;
  zval *option_values = nullptr;
  zval *cancellable = nullptr;
  ZEND_PARSE_PARAMETERS_START(5, 5)
  Z_PARAM_OBJECT_OF_CLASS(stream, class_for_gtype(G_TYPE_OUTPUT_STREAM))
  Z_PARAM_STR(type)
  Z_PARAM_ARRAY_OR_NULL(option_keys)
  Z_PARAM_ARRAY_OR_NULL(option_values)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  ZEND_PARSE_PARAMETERS_END();
  GdkPixbuf *self = PHPGTK_SELF(GdkPixbuf, GDK_TYPE_PIXBUF);
  GObject *stream_o = unwrap(stream, G_TYPE_OUTPUT_STREAM);
  if (stream_o == nullptr) RETURN_THROWS();
  if (!phpgtk::check_utf8(type, 2)) RETURN_THROWS();
  char **option_keys_v = nullptr;
  if (option_keys != nullptr) {
    option_keys_v = strv_from_php(option_keys);
    if (option_keys_v == nullptr) RETURN_THROWS();
  }
  char **option_values_v = nullptr;
  if (option_values != nullptr) {
    option_values_v = strv_from_php(option_values);
    if (option_values_v == nullptr) RETURN_THROWS();
  }
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  GError *error = nullptr;
  const gboolean ok = gdk_pixbuf_save_to_streamv(
      self, G_OUTPUT_STREAM(stream_o), ZSTR_VAL(type), option_keys_v, option_values_v,
      cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr, &error);
  if (option_keys_v != nullptr) g_strfreev(option_keys_v);
  if (option_values_v != nullptr) g_strfreev(option_values_v);
  if (error != nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  RETURN_BOOL(ok);
}

namespace {
// AsyncReadyCallback trampoline for GdkPixbuf::save_to_streamv_async(): wraps the C arguments,
// invokes the PHP callable once and releases it (async scope).
void cb_save_to_streamv_async_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\GdkPixbuf::save_to_streamv_async(GOutputStream $stream, string $type, ?array $option_keys,
 * ?array $option_values, ?GCancellable $cancellable, ?callable $callback): void
 *
 * Saves `pixbuf` to an output stream asynchronously.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, save_to_streamv_async) {
  zval *stream;
  zend_string *type;
  zval *option_keys = nullptr;
  zval *option_values = nullptr;
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(6, 6)
  Z_PARAM_OBJECT_OF_CLASS(stream, class_for_gtype(G_TYPE_OUTPUT_STREAM))
  Z_PARAM_STR(type)
  Z_PARAM_ARRAY_OR_NULL(option_keys)
  Z_PARAM_ARRAY_OR_NULL(option_values)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC_OR_NULL(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  GdkPixbuf *self = PHPGTK_SELF(GdkPixbuf, GDK_TYPE_PIXBUF);
  GObject *stream_o = unwrap(stream, G_TYPE_OUTPUT_STREAM);
  if (stream_o == nullptr) RETURN_THROWS();
  if (!phpgtk::check_utf8(type, 2)) RETURN_THROWS();
  char **option_keys_v = nullptr;
  if (option_keys != nullptr) {
    option_keys_v = strv_from_php(option_keys);
    if (option_keys_v == nullptr) RETURN_THROWS();
  }
  char **option_values_v = nullptr;
  if (option_values != nullptr) {
    option_values_v = strv_from_php(option_values);
    if (option_values_v == nullptr) RETURN_THROWS();
  }
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback =
      ZEND_FCI_INITIALIZED(fci_callback)
          ? callback_new(&fci_callback.function_name, "GdkPixbuf::save_to_streamv_async")
          : nullptr;
  gdk_pixbuf_save_to_streamv_async(
      self, G_OUTPUT_STREAM(stream_o), ZSTR_VAL(type), option_keys_v, option_values_v,
      cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
      cb_callback != nullptr ? cb_save_to_streamv_async_callback : nullptr, cb_callback);
  if (option_keys_v != nullptr) g_strfreev(option_keys_v);
  if (option_values_v != nullptr) g_strfreev(option_values_v);
}

/**
 * Gtk4\GdkPixbuf::scale(GdkPixbuf $dest, int $dest_x, int $dest_y, int $dest_width, int
 * $dest_height, float $offset_x, float $offset_y, float $scale_x, float $scale_y, GdkInterpType
 * $interp_type): void
 *
 * Creates a transformation of the source image $src by scaling by $scale_x and $scale_y then
 * translating by $offset_x and $offset_y, then renders the rectangle ($dest_x, $dest_y,
 * $dest_width, $dest_height) of the resulting image onto the destination image replacing the
 * previous contents.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, scale) {
  zval *dest;
  zend_long dest_x;
  zend_long dest_y;
  zend_long dest_width;
  zend_long dest_height;
  double offset_x;
  double offset_y;
  double scale_x;
  double scale_y;
  zval *interp_type;
  ZEND_PARSE_PARAMETERS_START(10, 10)
  Z_PARAM_OBJECT_OF_CLASS(dest, class_for_gtype(GDK_TYPE_PIXBUF))
  Z_PARAM_LONG(dest_x)
  Z_PARAM_LONG(dest_y)
  Z_PARAM_LONG(dest_width)
  Z_PARAM_LONG(dest_height)
  Z_PARAM_DOUBLE(offset_x)
  Z_PARAM_DOUBLE(offset_y)
  Z_PARAM_DOUBLE(scale_x)
  Z_PARAM_DOUBLE(scale_y)
  Z_PARAM_OBJECT_OF_CLASS(interp_type, enum_class_for_type(GDK_TYPE_INTERP_TYPE))
  ZEND_PARSE_PARAMETERS_END();
  GdkPixbuf *self = PHPGTK_SELF(GdkPixbuf, GDK_TYPE_PIXBUF);
  GObject *dest_o = unwrap(dest, GDK_TYPE_PIXBUF);
  if (dest_o == nullptr) RETURN_THROWS();
  if (!phpgtk::check_range<int>(dest_x, 2)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(dest_y, 3)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(dest_width, 4)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(dest_height, 5)) RETURN_THROWS();
  gint interp_type_v = 0;
  if (!enum_from_php(interp_type, GDK_TYPE_INTERP_TYPE, &interp_type_v)) RETURN_THROWS();
  gdk_pixbuf_scale(self, GDK_PIXBUF(dest_o), static_cast<int>(dest_x), static_cast<int>(dest_y),
                   static_cast<int>(dest_width), static_cast<int>(dest_height), offset_x, offset_y,
                   scale_x, scale_y, static_cast<GdkInterpType>(interp_type_v));
}

/**
 * Gtk4\GdkPixbuf::scale_simple(int $dest_width, int $dest_height, GdkInterpType $interp_type):
 * ?GdkPixbuf
 *
 * Create a new pixbuf containing a copy of `src` scaled to `dest_width` x `dest_height`.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, scale_simple) {
  zend_long dest_width;
  zend_long dest_height;
  zval *interp_type;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_LONG(dest_width)
  Z_PARAM_LONG(dest_height)
  Z_PARAM_OBJECT_OF_CLASS(interp_type, enum_class_for_type(GDK_TYPE_INTERP_TYPE))
  ZEND_PARSE_PARAMETERS_END();
  GdkPixbuf *self = PHPGTK_SELF(GdkPixbuf, GDK_TYPE_PIXBUF);
  if (!phpgtk::check_range<int>(dest_width, 1)) RETURN_THROWS();
  if (!phpgtk::check_domain(dest_width, 1, ZEND_LONG_MAX, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(dest_height, 2)) RETURN_THROWS();
  if (!phpgtk::check_domain(dest_height, 1, ZEND_LONG_MAX, 2)) RETURN_THROWS();
  gint interp_type_v = 0;
  if (!enum_from_php(interp_type, GDK_TYPE_INTERP_TYPE, &interp_type_v)) RETURN_THROWS();
  GdkPixbuf *phpgtk_ret =
      gdk_pixbuf_scale_simple(self, static_cast<int>(dest_width), static_cast<int>(dest_height),
                              static_cast<GdkInterpType>(interp_type_v));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * Gtk4\GdkPixbuf::set_option(string $key, string $value): bool
 *
 * Attaches a key/value pair as an option to a `GdkPixbuf`.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, set_option) {
  zend_string *key;
  zend_string *value;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_STR(key)
  Z_PARAM_STR(value)
  ZEND_PARSE_PARAMETERS_END();
  GdkPixbuf *self = PHPGTK_SELF(GdkPixbuf, GDK_TYPE_PIXBUF);
  if (!phpgtk::check_utf8(key, 1)) RETURN_THROWS();
  if (!phpgtk::check_utf8(value, 2)) RETURN_THROWS();
  RETURN_BOOL(gdk_pixbuf_set_option(self, ZSTR_VAL(key), ZSTR_VAL(value)));
}

/**
 * public static function get_file_info(string $filename): ?array
 * The format, width and height of an image file, read from its header without decoding it -
 * `[GdkPixbufFormat $format, int $width, int $height]` - or null when no loader recognises it.
 *
 * @return array{GdkPixbufFormat, int, int}|null
 */
ZEND_METHOD(Gtk4_GdkPixbuf, get_file_info) {
  zend_string *filename;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_PATH_STR(filename)
  ZEND_PARSE_PARAMETERS_END();
  zend_string *path = phpgtk::absolute_filename(filename, 1);
  if (path == nullptr) RETURN_THROWS();
  gint width = 0;
  gint height = 0;
  GdkPixbufFormat *format = gdk_pixbuf_get_file_info(ZSTR_VAL(path), &width, &height);
  zend_string_release(path);
  if (format == nullptr) RETURN_NULL();
  array_init_size(return_value, 3);
  zval boxed;
  wrap_boxed(gdk_pixbuf_format_get_type(), format, &boxed);  // GdkPixbuf owns the format; a copy
  add_next_index_zval(return_value, &boxed);
  add_next_index_long(return_value, width);
  add_next_index_long(return_value, height);
}

/**
 * public function get_options(): array
 * The key/value options the loader attached (an "orientation" from EXIF, "x-dpi"/"y-dpi", the
 * ICC profile) as a map of option name to string value; empty when there are none.
 *
 * @return array<string, string>
 */
ZEND_METHOD(Gtk4_GdkPixbuf, get_options) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkPixbuf *self = PHPGTK_SELF(GdkPixbuf, GDK_TYPE_PIXBUF);
  array_init(return_value);
  GHashTable *table = gdk_pixbuf_get_options(self);
  if (table == nullptr) return;
  GHashTableIter iter;
  gpointer key = nullptr;
  gpointer value = nullptr;
  g_hash_table_iter_init(&iter, table);
  while (g_hash_table_iter_next(&iter, &key, &value)) {
    add_assoc_string(return_value, static_cast<const char *>(key),
                     static_cast<const char *>(value));
  }
  g_hash_table_unref(table);
}

/**
 * public function save_to_bufferv(string $type, array $options = []): string
 * The image encoded as $type ("png", "jpeg", "tiff", "bmp", ...; get_formats() lists what is
 * writable) with the encoder's $options (a map of option name to string value, "quality" =>
 * "90" for JPEG), as the file's bytes.
 *
 * GIR fills a caller-provided buffer and takes the options as two parallel arrays; PHP gets the
 * bytes back and passes a map.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, save_to_bufferv) {
  zend_string *type;
  zval *options = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 2)
  Z_PARAM_STR(type)
  Z_PARAM_OPTIONAL
  Z_PARAM_ARRAY(options)
  ZEND_PARSE_PARAMETERS_END();
  GdkPixbuf *self = PHPGTK_SELF(GdkPixbuf, GDK_TYPE_PIXBUF);
  if (!phpgtk::check_utf8(type, 1)) RETURN_THROWS();
  std::vector<char *> keys;
  std::vector<char *> values;
  if (options != nullptr && !options_from_php(options, 2, keys, values)) RETURN_THROWS();
  gchar *buffer = nullptr;
  gsize length = 0;
  GError *error = nullptr;
  const gboolean ok = gdk_pixbuf_save_to_bufferv(
      self, &buffer, &length, ZSTR_VAL(type), options != nullptr ? keys.data() : nullptr,
      options != nullptr ? values.data() : nullptr, &error);
  if (!ok) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  RETVAL_STRINGL(buffer, length);
  g_free(buffer);
}

/**
 * public function savev(string $filename, string $type, array $options = []): bool
 * Writes the image to $filename encoded as $type ("png", "jpeg", ...) with the encoder's
 * $options (a map of option name to string value, "quality" => "90" for JPEG). A failure is a
 * GError; the boolean is GTK's own (always true when it returns).
 *
 * GIR takes the options as two parallel arrays; PHP passes a map.
 */
ZEND_METHOD(Gtk4_GdkPixbuf, savev) {
  zend_string *filename;
  zend_string *type;
  zval *options = nullptr;
  ZEND_PARSE_PARAMETERS_START(2, 3)
  Z_PARAM_PATH_STR(filename)
  Z_PARAM_STR(type)
  Z_PARAM_OPTIONAL
  Z_PARAM_ARRAY(options)
  ZEND_PARSE_PARAMETERS_END();
  GdkPixbuf *self = PHPGTK_SELF(GdkPixbuf, GDK_TYPE_PIXBUF);
  if (!phpgtk::check_utf8(type, 2)) RETURN_THROWS();
  std::vector<char *> keys;
  std::vector<char *> values;
  if (options != nullptr && !options_from_php(options, 3, keys, values)) RETURN_THROWS();
  zend_string *path = phpgtk::absolute_filename(filename, 1);
  if (path == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  const gboolean ok = gdk_pixbuf_savev(self, ZSTR_VAL(path), ZSTR_VAL(type),
                                       options != nullptr ? keys.data() : nullptr,
                                       options != nullptr ? values.data() : nullptr, &error);
  zend_string_release(path);
  if (!ok) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  RETURN_TRUE;
}
