// GENERATED by gen/gir.php from GdkPixbuf.PixbufFormat - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GdkPixbufFormat (boxed value type)
#include "php_gtk4.h"
#include "core/boxed.h"
#include "core/object.h"
#include <cstring>
#include "core/collections.h"

using namespace phpgtk;

namespace {

const char *const fields[] = {nullptr};

// Boxed field reader: the public scalar fields as PHP properties.
bool read(gpointer data, const char *field, zval *rv) {
  (void)data;
  (void)field;
  (void)rv;
  return false;
}

// Boxed field writer: each field converts like a parameter of its type (core/boxed).
bool write(gpointer data, const char *field, zval *v) {
  (void)data;
  (void)field;
  (void)v;
  return false;
}

}  // namespace

/**
 * Gtk4\GdkPixbufFormat::__construct()
 *
 * GdkPixbufFormat values come from GTK, never from `new`.
 */
ZEND_METHOD(Gtk4_GdkPixbufFormat, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
}

/**
 * Gtk4\GdkPixbufFormat::get_description(): string
 *
 * Returns a description of the format.
 */
ZEND_METHOD(Gtk4_GdkPixbufFormat, get_description) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkPixbufFormat *self = PHPGTK_BOXED_SELF(GdkPixbufFormat);
  char *phpgtk_ret = gdk_pixbuf_format_get_description(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETVAL_STRING(phpgtk_ret);
  g_free(phpgtk_ret);
}

/**
 * Gtk4\GdkPixbufFormat::get_extensions(): array
 *
 * Returns the filename extensions typically used for files in the given format.
 */
ZEND_METHOD(Gtk4_GdkPixbufFormat, get_extensions) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkPixbufFormat *self = PHPGTK_BOXED_SELF(GdkPixbufFormat);
  strv_to_php(gdk_pixbuf_format_get_extensions(self), Transfer::Full, return_value);
}

/**
 * Gtk4\GdkPixbufFormat::get_license(): string
 *
 * Returns information about the license of the image loader for the format.
 */
ZEND_METHOD(Gtk4_GdkPixbufFormat, get_license) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkPixbufFormat *self = PHPGTK_BOXED_SELF(GdkPixbufFormat);
  char *phpgtk_ret = gdk_pixbuf_format_get_license(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETVAL_STRING(phpgtk_ret);
  g_free(phpgtk_ret);
}

/**
 * Gtk4\GdkPixbufFormat::get_mime_types(): array
 *
 * Returns the mime types supported by the format.
 */
ZEND_METHOD(Gtk4_GdkPixbufFormat, get_mime_types) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkPixbufFormat *self = PHPGTK_BOXED_SELF(GdkPixbufFormat);
  strv_to_php(gdk_pixbuf_format_get_mime_types(self), Transfer::Full, return_value);
}

/**
 * Gtk4\GdkPixbufFormat::get_name(): string
 *
 * Returns the name of the format.
 */
ZEND_METHOD(Gtk4_GdkPixbufFormat, get_name) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkPixbufFormat *self = PHPGTK_BOXED_SELF(GdkPixbufFormat);
  char *phpgtk_ret = gdk_pixbuf_format_get_name(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETVAL_STRING(phpgtk_ret);
  g_free(phpgtk_ret);
}

/**
 * Gtk4\GdkPixbufFormat::is_disabled(): bool
 *
 * Returns whether this image format is disabled.
 */
ZEND_METHOD(Gtk4_GdkPixbufFormat, is_disabled) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkPixbufFormat *self = PHPGTK_BOXED_SELF(GdkPixbufFormat);
  RETURN_BOOL(gdk_pixbuf_format_is_disabled(self));
}

/**
 * Gtk4\GdkPixbufFormat::is_save_option_supported(string $option_key): bool
 *
 * Returns `TRUE` if the save option specified by $option_key is supported when saving a pixbuf
 * using the module implementing $format.
 */
ZEND_METHOD(Gtk4_GdkPixbufFormat, is_save_option_supported) {
  zend_string *option_key;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(option_key)
  ZEND_PARSE_PARAMETERS_END();
  GdkPixbufFormat *self = PHPGTK_BOXED_SELF(GdkPixbufFormat);
  if (!phpgtk::check_utf8(option_key, 1)) RETURN_THROWS();
  RETURN_BOOL(gdk_pixbuf_format_is_save_option_supported(self, ZSTR_VAL(option_key)));
}

/**
 * Gtk4\GdkPixbufFormat::is_scalable(): bool
 *
 * Returns whether this image format is scalable.
 */
ZEND_METHOD(Gtk4_GdkPixbufFormat, is_scalable) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkPixbufFormat *self = PHPGTK_BOXED_SELF(GdkPixbufFormat);
  RETURN_BOOL(gdk_pixbuf_format_is_scalable(self));
}

/**
 * Gtk4\GdkPixbufFormat::is_writable(): bool
 *
 * Returns whether pixbufs can be saved in the given format.
 */
ZEND_METHOD(Gtk4_GdkPixbufFormat, is_writable) {
  ZEND_PARSE_PARAMETERS_NONE();
  GdkPixbufFormat *self = PHPGTK_BOXED_SELF(GdkPixbufFormat);
  RETURN_BOOL(gdk_pixbuf_format_is_writable(self));
}

/**
 * Gtk4\GdkPixbufFormat::set_disabled(bool $disabled): void
 *
 * Disables or enables an image format.
 */
ZEND_METHOD(Gtk4_GdkPixbufFormat, set_disabled) {
  bool disabled;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(disabled)
  ZEND_PARSE_PARAMETERS_END();
  GdkPixbufFormat *self = PHPGTK_BOXED_SELF(GdkPixbufFormat);
  gdk_pixbuf_format_set_disabled(self, disabled);
}

namespace phpgtk {
// MINIT: bind the PHP class to gdk_pixbuf_format_get_type() with its field table.
void register_GdkPixbufFormat(zend_class_entry *ce) {
  register_boxed(BoxedClass{.type = gdk_pixbuf_format_get_type(),
                            .ce = ce,
                            .fields = fields,
                            .read = read,
                            .write = write});
}
}  // namespace phpgtk
