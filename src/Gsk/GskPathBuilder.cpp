// GENERATED by gen/gir.php from Gsk.PathBuilder - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GskPathBuilder (boxed value type)
#include "php_gtk4.h"
#include "core/boxed.h"
#include "core/object.h"
#include <cstring>
#include "Gsk/GskRoundedRectType.h"

using namespace phpgtk;

namespace {

const char *const fields[] = {nullptr};

// Boxed field reader: the public scalar fields as PHP properties.
bool read(gpointer data, const char *field, zval *rv) {
  (void)data;
  (void)field;
  (void)rv;
  return false;
}

// Boxed field writer: each field converts like a parameter of its type (core/boxed).
bool write(gpointer data, const char *field, zval *v) {
  (void)data;
  (void)field;
  (void)v;
  return false;
}

}  // namespace

/**
 * Gtk4\GskPathBuilder::__construct()
 *
 * Create a new `GskPathBuilder` object.
 */
ZEND_METHOD(Gtk4_GskPathBuilder, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  gpointer obj = gsk_path_builder_new();
  if (obj == nullptr) {
    zend_throw_error(nullptr, "%s(): GTK refused to create the value (see the CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  boxed_adopt(boxed_from_zval(ZEND_THIS), GSK_TYPE_PATH_BUILDER, obj);
}

/**
 * Gtk4\GskPathBuilder::add_circle(GraphenePoint $center, float $radius): void
 *
 * Adds a circle with the $center and $radius.
 */
ZEND_METHOD(Gtk4_GskPathBuilder, add_circle) {
  zval *center;
  double radius;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(center, boxed_class_for_type(GRAPHENE_TYPE_POINT)->ce)
  Z_PARAM_DOUBLE(radius)
  ZEND_PARSE_PARAMETERS_END();
  GskPathBuilder *self = PHPGTK_BOXED_SELF(GskPathBuilder);
  gpointer center_b = unwrap_boxed(center, GRAPHENE_TYPE_POINT);
  if (center_b == nullptr) RETURN_THROWS();
  if (!phpgtk::check_domain_double(radius, 0.0, HUGE_VAL, 2)) RETURN_THROWS();
  gsk_path_builder_add_circle(self, static_cast<graphene_point_t *>(center_b),
                              static_cast<float>(radius));
}

/**
 * Gtk4\GskPathBuilder::add_layout(PangoLayout $layout): void
 *
 * Adds the outlines for the glyphs in $layout to the builder.
 */
ZEND_METHOD(Gtk4_GskPathBuilder, add_layout) {
  zval *layout;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(layout, class_for_gtype(PANGO_TYPE_LAYOUT))
  ZEND_PARSE_PARAMETERS_END();
  GskPathBuilder *self = PHPGTK_BOXED_SELF(GskPathBuilder);
  GObject *layout_o = unwrap(layout, PANGO_TYPE_LAYOUT);
  if (layout_o == nullptr) RETURN_THROWS();
  gsk_path_builder_add_layout(self, PANGO_LAYOUT(layout_o));
}

/**
 * Gtk4\GskPathBuilder::add_path(GskPath $path): void
 *
 * Appends all of $path to the builder.
 */
ZEND_METHOD(Gtk4_GskPathBuilder, add_path) {
  zval *path;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(path, boxed_class_for_type(GSK_TYPE_PATH)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GskPathBuilder *self = PHPGTK_BOXED_SELF(GskPathBuilder);
  gpointer path_b = unwrap_boxed(path, GSK_TYPE_PATH);
  if (path_b == nullptr) RETURN_THROWS();
  gsk_path_builder_add_path(self, static_cast<GskPath *>(path_b));
}

/**
 * Gtk4\GskPathBuilder::add_rect(GrapheneRect $rect): void
 *
 * Adds $rect as a new contour to the path built by the builder.
 */
ZEND_METHOD(Gtk4_GskPathBuilder, add_rect) {
  zval *rect;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(rect, boxed_class_for_type(GRAPHENE_TYPE_RECT)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GskPathBuilder *self = PHPGTK_BOXED_SELF(GskPathBuilder);
  gpointer rect_b = unwrap_boxed(rect, GRAPHENE_TYPE_RECT);
  if (rect_b == nullptr) RETURN_THROWS();
  gsk_path_builder_add_rect(self, static_cast<graphene_rect_t *>(rect_b));
}

/**
 * Gtk4\GskPathBuilder::add_reverse_path(GskPath $path): void
 *
 * Appends all of $path to the builder, in reverse order.
 */
ZEND_METHOD(Gtk4_GskPathBuilder, add_reverse_path) {
  zval *path;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(path, boxed_class_for_type(GSK_TYPE_PATH)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GskPathBuilder *self = PHPGTK_BOXED_SELF(GskPathBuilder);
  gpointer path_b = unwrap_boxed(path, GSK_TYPE_PATH);
  if (path_b == nullptr) RETURN_THROWS();
  gsk_path_builder_add_reverse_path(self, static_cast<GskPath *>(path_b));
}

/**
 * Gtk4\GskPathBuilder::add_rounded_rect(GskRoundedRect $rect): void
 *
 * Adds $rect as a new contour to the path built in $self.
 */
ZEND_METHOD(Gtk4_GskPathBuilder, add_rounded_rect) {
  zval *rect;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(rect, boxed_class_for_type(PHPGTK_TYPE_GSK_ROUNDED_RECT)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GskPathBuilder *self = PHPGTK_BOXED_SELF(GskPathBuilder);
  gpointer rect_b = unwrap_boxed(rect, PHPGTK_TYPE_GSK_ROUNDED_RECT);
  if (rect_b == nullptr) RETURN_THROWS();
  gsk_path_builder_add_rounded_rect(self, static_cast<GskRoundedRect *>(rect_b));
}

/**
 * Gtk4\GskPathBuilder::add_segment(GskPath $path, GskPathPoint $start, GskPathPoint $end): void
 *
 * Adds to $self the segment of $path from $start to $end.
 */
ZEND_METHOD(Gtk4_GskPathBuilder, add_segment) {
  zval *path;
  zval *start;
  zval *end;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_OBJECT_OF_CLASS(path, boxed_class_for_type(GSK_TYPE_PATH)->ce)
  Z_PARAM_OBJECT_OF_CLASS(start, boxed_class_for_type(GSK_TYPE_PATH_POINT)->ce)
  Z_PARAM_OBJECT_OF_CLASS(end, boxed_class_for_type(GSK_TYPE_PATH_POINT)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GskPathBuilder *self = PHPGTK_BOXED_SELF(GskPathBuilder);
  gpointer path_b = unwrap_boxed(path, GSK_TYPE_PATH);
  if (path_b == nullptr) RETURN_THROWS();
  gpointer start_b = unwrap_boxed(start, GSK_TYPE_PATH_POINT);
  if (start_b == nullptr) RETURN_THROWS();
  gpointer end_b = unwrap_boxed(end, GSK_TYPE_PATH_POINT);
  if (end_b == nullptr) RETURN_THROWS();
  gsk_path_builder_add_segment(self, static_cast<GskPath *>(path_b),
                               static_cast<GskPathPoint *>(start_b),
                               static_cast<GskPathPoint *>(end_b));
}

/**
 * Gtk4\GskPathBuilder::arc_to(float $x1, float $y1, float $x2, float $y2): void
 *
 * Adds an elliptical arc from the current point to $x2, $y2 with $x1, $y1 determining the tangent
 * directions.
 */
ZEND_METHOD(Gtk4_GskPathBuilder, arc_to) {
  double x1;
  double y1;
  double x2;
  double y2;
  ZEND_PARSE_PARAMETERS_START(4, 4)
  Z_PARAM_DOUBLE(x1)
  Z_PARAM_DOUBLE(y1)
  Z_PARAM_DOUBLE(x2)
  Z_PARAM_DOUBLE(y2)
  ZEND_PARSE_PARAMETERS_END();
  GskPathBuilder *self = PHPGTK_BOXED_SELF(GskPathBuilder);
  gsk_path_builder_arc_to(self, static_cast<float>(x1), static_cast<float>(y1),
                          static_cast<float>(x2), static_cast<float>(y2));
}

/**
 * Gtk4\GskPathBuilder::close(): void
 *
 * Ends the current contour with a line back to the start point.
 */
ZEND_METHOD(Gtk4_GskPathBuilder, close) {
  ZEND_PARSE_PARAMETERS_NONE();
  GskPathBuilder *self = PHPGTK_BOXED_SELF(GskPathBuilder);
  gsk_path_builder_close(self);
}

/**
 * Gtk4\GskPathBuilder::conic_to(float $x1, float $y1, float $x2, float $y2, float $weight): void
 *
 * Adds a [conic curve](https://en.wikipedia.org/wiki/Non-uniform_rational_B-spline) from the
 * current point to $x2, $y2 with the given $weight and $x1, $y1 as the control point.
 */
ZEND_METHOD(Gtk4_GskPathBuilder, conic_to) {
  double x1;
  double y1;
  double x2;
  double y2;
  double weight;
  ZEND_PARSE_PARAMETERS_START(5, 5)
  Z_PARAM_DOUBLE(x1)
  Z_PARAM_DOUBLE(y1)
  Z_PARAM_DOUBLE(x2)
  Z_PARAM_DOUBLE(y2)
  Z_PARAM_DOUBLE(weight)
  ZEND_PARSE_PARAMETERS_END();
  GskPathBuilder *self = PHPGTK_BOXED_SELF(GskPathBuilder);
  if (!phpgtk::check_domain_above(weight, 0.0, 5)) RETURN_THROWS();
  gsk_path_builder_conic_to(self, static_cast<float>(x1), static_cast<float>(y1),
                            static_cast<float>(x2), static_cast<float>(y2),
                            static_cast<float>(weight));
}

/**
 * Gtk4\GskPathBuilder::cubic_to(float $x1, float $y1, float $x2, float $y2, float $x3, float $y3):
 * void
 *
 * Adds a [cubic Bézier curve](https://en.wikipedia.org/wiki/B%C3%A9zier_curve) from the current
 * point to $x3, $y3 with $x1, $y1 and $x2, $y2 as the control points.
 */
ZEND_METHOD(Gtk4_GskPathBuilder, cubic_to) {
  double x1;
  double y1;
  double x2;
  double y2;
  double x3;
  double y3;
  ZEND_PARSE_PARAMETERS_START(6, 6)
  Z_PARAM_DOUBLE(x1)
  Z_PARAM_DOUBLE(y1)
  Z_PARAM_DOUBLE(x2)
  Z_PARAM_DOUBLE(y2)
  Z_PARAM_DOUBLE(x3)
  Z_PARAM_DOUBLE(y3)
  ZEND_PARSE_PARAMETERS_END();
  GskPathBuilder *self = PHPGTK_BOXED_SELF(GskPathBuilder);
  gsk_path_builder_cubic_to(self, static_cast<float>(x1), static_cast<float>(y1),
                            static_cast<float>(x2), static_cast<float>(y2), static_cast<float>(x3),
                            static_cast<float>(y3));
}

/**
 * Gtk4\GskPathBuilder::get_current_point(): GraphenePoint
 *
 * Gets the current point.
 */
ZEND_METHOD(Gtk4_GskPathBuilder, get_current_point) {
  ZEND_PARSE_PARAMETERS_NONE();
  GskPathBuilder *self = PHPGTK_BOXED_SELF(GskPathBuilder);
  const graphene_point_t *phpgtk_ret = gsk_path_builder_get_current_point(self);
  wrap_boxed(GRAPHENE_TYPE_POINT, phpgtk_ret, return_value);
}

/**
 * Gtk4\GskPathBuilder::html_arc_to(float $x1, float $y1, float $x2, float $y2, float $radius): void
 *
 * Implements arc-to according to the HTML Canvas spec.
 */
ZEND_METHOD(Gtk4_GskPathBuilder, html_arc_to) {
  double x1;
  double y1;
  double x2;
  double y2;
  double radius;
  ZEND_PARSE_PARAMETERS_START(5, 5)
  Z_PARAM_DOUBLE(x1)
  Z_PARAM_DOUBLE(y1)
  Z_PARAM_DOUBLE(x2)
  Z_PARAM_DOUBLE(y2)
  Z_PARAM_DOUBLE(radius)
  ZEND_PARSE_PARAMETERS_END();
  GskPathBuilder *self = PHPGTK_BOXED_SELF(GskPathBuilder);
  if (!phpgtk::check_domain_above(radius, 0.0, 5)) RETURN_THROWS();
  gsk_path_builder_html_arc_to(self, static_cast<float>(x1), static_cast<float>(y1),
                               static_cast<float>(x2), static_cast<float>(y2),
                               static_cast<float>(radius));
}

/**
 * Gtk4\GskPathBuilder::line_to(float $x, float $y): void
 *
 * Draws a line from the current point to $x, $y and makes it the new current point.
 */
ZEND_METHOD(Gtk4_GskPathBuilder, line_to) {
  double x;
  double y;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_DOUBLE(x)
  Z_PARAM_DOUBLE(y)
  ZEND_PARSE_PARAMETERS_END();
  GskPathBuilder *self = PHPGTK_BOXED_SELF(GskPathBuilder);
  gsk_path_builder_line_to(self, static_cast<float>(x), static_cast<float>(y));
}

/**
 * Gtk4\GskPathBuilder::move_to(float $x, float $y): void
 *
 * Starts a new contour by placing the pen at $x, $y.
 */
ZEND_METHOD(Gtk4_GskPathBuilder, move_to) {
  double x;
  double y;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_DOUBLE(x)
  Z_PARAM_DOUBLE(y)
  ZEND_PARSE_PARAMETERS_END();
  GskPathBuilder *self = PHPGTK_BOXED_SELF(GskPathBuilder);
  gsk_path_builder_move_to(self, static_cast<float>(x), static_cast<float>(y));
}

/**
 * Gtk4\GskPathBuilder::quad_to(float $x1, float $y1, float $x2, float $y2): void
 *
 * Adds a [quadratic Bézier curve](https://en.wikipedia.org/wiki/B%C3%A9zier_curve) from the
 * current point to $x2, $y2 with $x1, $y1 as the control point.
 */
ZEND_METHOD(Gtk4_GskPathBuilder, quad_to) {
  double x1;
  double y1;
  double x2;
  double y2;
  ZEND_PARSE_PARAMETERS_START(4, 4)
  Z_PARAM_DOUBLE(x1)
  Z_PARAM_DOUBLE(y1)
  Z_PARAM_DOUBLE(x2)
  Z_PARAM_DOUBLE(y2)
  ZEND_PARSE_PARAMETERS_END();
  GskPathBuilder *self = PHPGTK_BOXED_SELF(GskPathBuilder);
  gsk_path_builder_quad_to(self, static_cast<float>(x1), static_cast<float>(y1),
                           static_cast<float>(x2), static_cast<float>(y2));
}

/**
 * Gtk4\GskPathBuilder::rel_arc_to(float $x1, float $y1, float $x2, float $y2): void
 *
 * Adds an elliptical arc from the current point to $x2, $y2 with $x1, $y1 determining the tangent
 * directions.
 */
ZEND_METHOD(Gtk4_GskPathBuilder, rel_arc_to) {
  double x1;
  double y1;
  double x2;
  double y2;
  ZEND_PARSE_PARAMETERS_START(4, 4)
  Z_PARAM_DOUBLE(x1)
  Z_PARAM_DOUBLE(y1)
  Z_PARAM_DOUBLE(x2)
  Z_PARAM_DOUBLE(y2)
  ZEND_PARSE_PARAMETERS_END();
  GskPathBuilder *self = PHPGTK_BOXED_SELF(GskPathBuilder);
  gsk_path_builder_rel_arc_to(self, static_cast<float>(x1), static_cast<float>(y1),
                              static_cast<float>(x2), static_cast<float>(y2));
}

/**
 * Gtk4\GskPathBuilder::rel_conic_to(float $x1, float $y1, float $x2, float $y2, float $weight):
 * void
 *
 * Adds a [conic curve](https://en.wikipedia.org/wiki/Non-uniform_rational_B-spline) from the
 * current point to $x2, $y2 with the given $weight and $x1, $y1 as the control point.
 */
ZEND_METHOD(Gtk4_GskPathBuilder, rel_conic_to) {
  double x1;
  double y1;
  double x2;
  double y2;
  double weight;
  ZEND_PARSE_PARAMETERS_START(5, 5)
  Z_PARAM_DOUBLE(x1)
  Z_PARAM_DOUBLE(y1)
  Z_PARAM_DOUBLE(x2)
  Z_PARAM_DOUBLE(y2)
  Z_PARAM_DOUBLE(weight)
  ZEND_PARSE_PARAMETERS_END();
  GskPathBuilder *self = PHPGTK_BOXED_SELF(GskPathBuilder);
  if (!phpgtk::check_domain_above(weight, 0.0, 5)) RETURN_THROWS();
  gsk_path_builder_rel_conic_to(self, static_cast<float>(x1), static_cast<float>(y1),
                                static_cast<float>(x2), static_cast<float>(y2),
                                static_cast<float>(weight));
}

/**
 * Gtk4\GskPathBuilder::rel_cubic_to(float $x1, float $y1, float $x2, float $y2, float $x3, float
 * $y3): void
 *
 * Adds a [cubic Bézier curve](https://en.wikipedia.org/wiki/B%C3%A9zier_curve) from the current
 * point to $x3, $y3 with $x1, $y1 and $x2, $y2 as the control points.
 */
ZEND_METHOD(Gtk4_GskPathBuilder, rel_cubic_to) {
  double x1;
  double y1;
  double x2;
  double y2;
  double x3;
  double y3;
  ZEND_PARSE_PARAMETERS_START(6, 6)
  Z_PARAM_DOUBLE(x1)
  Z_PARAM_DOUBLE(y1)
  Z_PARAM_DOUBLE(x2)
  Z_PARAM_DOUBLE(y2)
  Z_PARAM_DOUBLE(x3)
  Z_PARAM_DOUBLE(y3)
  ZEND_PARSE_PARAMETERS_END();
  GskPathBuilder *self = PHPGTK_BOXED_SELF(GskPathBuilder);
  gsk_path_builder_rel_cubic_to(self, static_cast<float>(x1), static_cast<float>(y1),
                                static_cast<float>(x2), static_cast<float>(y2),
                                static_cast<float>(x3), static_cast<float>(y3));
}

/**
 * Gtk4\GskPathBuilder::rel_html_arc_to(float $x1, float $y1, float $x2, float $y2, float $radius):
 * void
 *
 * Implements arc-to according to the HTML Canvas spec.
 */
ZEND_METHOD(Gtk4_GskPathBuilder, rel_html_arc_to) {
  double x1;
  double y1;
  double x2;
  double y2;
  double radius;
  ZEND_PARSE_PARAMETERS_START(5, 5)
  Z_PARAM_DOUBLE(x1)
  Z_PARAM_DOUBLE(y1)
  Z_PARAM_DOUBLE(x2)
  Z_PARAM_DOUBLE(y2)
  Z_PARAM_DOUBLE(radius)
  ZEND_PARSE_PARAMETERS_END();
  GskPathBuilder *self = PHPGTK_BOXED_SELF(GskPathBuilder);
  if (!phpgtk::check_domain_above(radius, 0.0, 5)) RETURN_THROWS();
  gsk_path_builder_rel_html_arc_to(self, static_cast<float>(x1), static_cast<float>(y1),
                                   static_cast<float>(x2), static_cast<float>(y2),
                                   static_cast<float>(radius));
}

/**
 * Gtk4\GskPathBuilder::rel_line_to(float $x, float $y): void
 *
 * Draws a line from the current point to a point offset from it by $x, $y and makes it the new
 * current point.
 */
ZEND_METHOD(Gtk4_GskPathBuilder, rel_line_to) {
  double x;
  double y;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_DOUBLE(x)
  Z_PARAM_DOUBLE(y)
  ZEND_PARSE_PARAMETERS_END();
  GskPathBuilder *self = PHPGTK_BOXED_SELF(GskPathBuilder);
  gsk_path_builder_rel_line_to(self, static_cast<float>(x), static_cast<float>(y));
}

/**
 * Gtk4\GskPathBuilder::rel_move_to(float $x, float $y): void
 *
 * Starts a new contour by placing the pen at $x, $y relative to the current point.
 */
ZEND_METHOD(Gtk4_GskPathBuilder, rel_move_to) {
  double x;
  double y;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_DOUBLE(x)
  Z_PARAM_DOUBLE(y)
  ZEND_PARSE_PARAMETERS_END();
  GskPathBuilder *self = PHPGTK_BOXED_SELF(GskPathBuilder);
  gsk_path_builder_rel_move_to(self, static_cast<float>(x), static_cast<float>(y));
}

/**
 * Gtk4\GskPathBuilder::rel_quad_to(float $x1, float $y1, float $x2, float $y2): void
 *
 * Adds a [quadratic Bézier curve](https://en.wikipedia.org/wiki/B%C3%A9zier_curve) from the
 * current point to $x2, $y2 with $x1, $y1 the control point.
 */
ZEND_METHOD(Gtk4_GskPathBuilder, rel_quad_to) {
  double x1;
  double y1;
  double x2;
  double y2;
  ZEND_PARSE_PARAMETERS_START(4, 4)
  Z_PARAM_DOUBLE(x1)
  Z_PARAM_DOUBLE(y1)
  Z_PARAM_DOUBLE(x2)
  Z_PARAM_DOUBLE(y2)
  ZEND_PARSE_PARAMETERS_END();
  GskPathBuilder *self = PHPGTK_BOXED_SELF(GskPathBuilder);
  gsk_path_builder_rel_quad_to(self, static_cast<float>(x1), static_cast<float>(y1),
                               static_cast<float>(x2), static_cast<float>(y2));
}

/**
 * Gtk4\GskPathBuilder::rel_svg_arc_to(float $rx, float $ry, float $x_axis_rotation, bool
 * $large_arc, bool $positive_sweep, float $x, float $y): void
 *
 * Implements arc-to according to the SVG spec.
 */
ZEND_METHOD(Gtk4_GskPathBuilder, rel_svg_arc_to) {
  double rx;
  double ry;
  double x_axis_rotation;
  bool large_arc;
  bool positive_sweep;
  double x;
  double y;
  ZEND_PARSE_PARAMETERS_START(7, 7)
  Z_PARAM_DOUBLE(rx)
  Z_PARAM_DOUBLE(ry)
  Z_PARAM_DOUBLE(x_axis_rotation)
  Z_PARAM_BOOL(large_arc)
  Z_PARAM_BOOL(positive_sweep)
  Z_PARAM_DOUBLE(x)
  Z_PARAM_DOUBLE(y)
  ZEND_PARSE_PARAMETERS_END();
  GskPathBuilder *self = PHPGTK_BOXED_SELF(GskPathBuilder);
  gsk_path_builder_rel_svg_arc_to(self, static_cast<float>(rx), static_cast<float>(ry),
                                  static_cast<float>(x_axis_rotation), large_arc, positive_sweep,
                                  static_cast<float>(x), static_cast<float>(y));
}

/**
 * Gtk4\GskPathBuilder::svg_arc_to(float $rx, float $ry, float $x_axis_rotation, bool $large_arc,
 * bool $positive_sweep, float $x, float $y): void
 *
 * Implements arc-to according to the SVG spec.
 */
ZEND_METHOD(Gtk4_GskPathBuilder, svg_arc_to) {
  double rx;
  double ry;
  double x_axis_rotation;
  bool large_arc;
  bool positive_sweep;
  double x;
  double y;
  ZEND_PARSE_PARAMETERS_START(7, 7)
  Z_PARAM_DOUBLE(rx)
  Z_PARAM_DOUBLE(ry)
  Z_PARAM_DOUBLE(x_axis_rotation)
  Z_PARAM_BOOL(large_arc)
  Z_PARAM_BOOL(positive_sweep)
  Z_PARAM_DOUBLE(x)
  Z_PARAM_DOUBLE(y)
  ZEND_PARSE_PARAMETERS_END();
  GskPathBuilder *self = PHPGTK_BOXED_SELF(GskPathBuilder);
  gsk_path_builder_svg_arc_to(self, static_cast<float>(rx), static_cast<float>(ry),
                              static_cast<float>(x_axis_rotation), large_arc, positive_sweep,
                              static_cast<float>(x), static_cast<float>(y));
}

/**
 * Gtk4\GskPathBuilder::to_path(): GskPath
 *
 * Creates a new `GskPath` from the given builder.
 */
ZEND_METHOD(Gtk4_GskPathBuilder, to_path) {
  ZEND_PARSE_PARAMETERS_NONE();
  GskPathBuilder *self = PHPGTK_BOXED_SELF(GskPathBuilder);
  GskPath *phpgtk_ret = gsk_path_builder_to_path(self);
  wrap_boxed(GSK_TYPE_PATH, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(GSK_TYPE_PATH, phpgtk_ret);
}

namespace phpgtk {
// MINIT: bind the PHP class to GSK_TYPE_PATH_BUILDER with its field table.
void register_GskPathBuilder(zend_class_entry *ce) {
  register_boxed(BoxedClass{
      .type = GSK_TYPE_PATH_BUILDER, .ce = ce, .fields = fields, .read = read, .write = write});
}
}  // namespace phpgtk
