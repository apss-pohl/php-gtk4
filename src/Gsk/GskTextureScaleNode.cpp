// GENERATED by gen/gir.php from Gsk.TextureScaleNode - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GskTextureScaleNode (fundamental handle)
#include "php_gtk4.h"
#include "core/fundamental.h"
#include "core/object.h"
#include "core/enums.h"
#include "core/boxed.h"

using namespace phpgtk;

/**
 * Gtk4\GskTextureScaleNode::__construct(GdkTexture $texture, GrapheneRect $bounds, GskScalingFilter
 * $filter)
 *
 * Creates a node that scales the texture to the size given by the bounds using the filter and then
 * places it at the bounds' position.
 */
ZEND_METHOD(Gtk4_GskTextureScaleNode, __construct) {
  zval *texture;
  zval *bounds;
  zval *filter;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_OBJECT_OF_CLASS(texture, class_for_gtype(GDK_TYPE_TEXTURE))
  Z_PARAM_OBJECT_OF_CLASS(bounds, boxed_class_for_type(GRAPHENE_TYPE_RECT)->ce)
  Z_PARAM_OBJECT_OF_CLASS(filter, enum_class_for_type(GSK_TYPE_SCALING_FILTER))
  ZEND_PARSE_PARAMETERS_END();
  GObject *texture_o = unwrap(texture, GDK_TYPE_TEXTURE);
  if (texture_o == nullptr) RETURN_THROWS();
  gpointer bounds_b = unwrap_boxed(bounds, GRAPHENE_TYPE_RECT);
  if (bounds_b == nullptr) RETURN_THROWS();
  gint filter_v = 0;
  if (!enum_from_php(filter, GSK_TYPE_SCALING_FILTER, &filter_v)) RETURN_THROWS();
  gpointer obj =
      gsk_texture_scale_node_new(GDK_TEXTURE(texture_o), static_cast<graphene_rect_t *>(bounds_b),
                                 static_cast<GskScalingFilter>(filter_v));
  if (obj == nullptr) {
    zend_throw_error(nullptr, "%s(): GTK refused to create the instance (see the CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  fundamental_adopt(fundamental_from_zval(ZEND_THIS), GSK_TYPE_TEXTURE_SCALE_NODE, obj);
}

/**
 * Gtk4\GskTextureScaleNode::get_filter(): GskScalingFilter
 *
 * Retrieves the `GskScalingFilter` used when creating this `GskRenderNode`.
 */
ZEND_METHOD(Gtk4_GskTextureScaleNode, get_filter) {
  ZEND_PARSE_PARAMETERS_NONE();
  auto *self = static_cast<GskRenderNode *>(fundamental_self(execute_data));
  if (self == nullptr) RETURN_THROWS();
  enum_to_php(GSK_TYPE_SCALING_FILTER, gsk_texture_scale_node_get_filter(self), return_value);
}

/**
 * Gtk4\GskTextureScaleNode::get_texture(): GdkTexture
 *
 * Retrieves the `GdkTexture` used when creating this `GskRenderNode`.
 */
ZEND_METHOD(Gtk4_GskTextureScaleNode, get_texture) {
  ZEND_PARSE_PARAMETERS_NONE();
  auto *self = static_cast<GskRenderNode *>(fundamental_self(execute_data));
  if (self == nullptr) RETURN_THROWS();
  GdkTexture *phpgtk_ret = gsk_texture_scale_node_get_texture(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

namespace {

// gsk_render_node_ref() as the fundamental registry's RefFn.
gpointer fundamental_ref(gpointer p) {
  return gsk_render_node_ref(static_cast<GskRenderNode *>(p));
}

// gsk_render_node_unref() as the fundamental registry's UnrefFn.
void fundamental_unref(gpointer p) {
  gsk_render_node_unref(static_cast<GskRenderNode *>(p));
}

}  // namespace

namespace phpgtk {
// MINIT: bind the PHP class to GSK_TYPE_TEXTURE_SCALE_NODE on the fundamental registry.
void register_GskTextureScaleNode(zend_class_entry *ce) {
  register_fundamental(FundamentalClass{.type = GSK_TYPE_TEXTURE_SCALE_NODE,
                                        .ce = ce,
                                        .ref = fundamental_ref,
                                        .unref = fundamental_unref});
}
}  // namespace phpgtk
