// GENERATED by gen/gir.php from Gsk.ContainerNode - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GskContainerNode (fundamental handle)
#include "php_gtk4.h"
#include "core/fundamental.h"
#include "core/object.h"

using namespace phpgtk;

// __construct(): the children come as a PHP list, not a C array.
#include <vector>

/**
 * Gtk4\GskContainerNode::get_child(int $idx): GskRenderNode
 *
 * Gets one of the children of $container.
 */
ZEND_METHOD(Gtk4_GskContainerNode, get_child) {
  zend_long idx;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(idx)
  ZEND_PARSE_PARAMETERS_END();
  auto *self = static_cast<GskRenderNode *>(fundamental_self(execute_data));
  if (self == nullptr) RETURN_THROWS();
  if (!phpgtk::check_range<guint>(idx, 1)) RETURN_THROWS();
  gpointer phpgtk_ret = gsk_container_node_get_child(self, static_cast<guint>(idx));
  wrap_fundamental(GSK_TYPE_RENDER_NODE, phpgtk_ret, return_value);
}

/**
 * Gtk4\GskContainerNode::get_n_children(): int
 *
 * Retrieves the number of direct children of $node.
 */
ZEND_METHOD(Gtk4_GskContainerNode, get_n_children) {
  ZEND_PARSE_PARAMETERS_NONE();
  auto *self = static_cast<GskRenderNode *>(fundamental_self(execute_data));
  if (self == nullptr) RETURN_THROWS();
  RETURN_LONG(static_cast<zend_long>(gsk_container_node_get_n_children(self)));
}

/**
 * public function __construct(array $children)
 * A node drawing $children (a list of GskRenderNode) one after the other, in order.
 *
 * GIR takes a C array with its length; a PHP list is the same thing. The container references
 * every child, so the list may be dropped afterwards.
 */
ZEND_METHOD(Gtk4_GskContainerNode, __construct) {
  zval *children;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_ARRAY(children)
  ZEND_PARSE_PARAMETERS_END();
  std::vector<GskRenderNode *> nodes;
  zval *item;
  // NOLINTNEXTLINE(readability-math-missing-parentheses) Zend macro expansion
  ZEND_HASH_FOREACH_VAL(Z_ARRVAL_P(children), item) {
    gpointer node = unwrap_fundamental(item, GSK_TYPE_RENDER_NODE);
    if (node == nullptr) {
      if (EG(exception) != nullptr) zend_clear_exception();
      zend_argument_type_error(1, "must be a list of GskRenderNode, %s found in it",
                               zend_zval_value_name(item));
      RETURN_THROWS();
    }
    nodes.push_back(static_cast<GskRenderNode *>(node));
  }
  ZEND_HASH_FOREACH_END();
  // The count is a guint, not a gsize: MSVC's /W3 fails the Windows build on the implicit
  // narrowing (GCC does not warn), so it is spelled out here as everywhere else.
  GskRenderNode *self = gsk_container_node_new(nodes.data(), static_cast<guint>(nodes.size()));
  fundamental_adopt(fundamental_from_zval(ZEND_THIS), GSK_TYPE_CONTAINER_NODE, self);
}

namespace {

// gsk_render_node_ref() as the fundamental registry's RefFn.
gpointer fundamental_ref(gpointer p) {
  return gsk_render_node_ref(static_cast<GskRenderNode *>(p));
}

// gsk_render_node_unref() as the fundamental registry's UnrefFn.
void fundamental_unref(gpointer p) {
  gsk_render_node_unref(static_cast<GskRenderNode *>(p));
}

}  // namespace

namespace phpgtk {
// MINIT: bind the PHP class to GSK_TYPE_CONTAINER_NODE on the fundamental registry.
void register_GskContainerNode(zend_class_entry *ce) {
  register_fundamental(FundamentalClass{.type = GSK_TYPE_CONTAINER_NODE,
                                        .ce = ce,
                                        .ref = fundamental_ref,
                                        .unref = fundamental_unref});
}
}  // namespace phpgtk
