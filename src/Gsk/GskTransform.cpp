// GENERATED by gen/gir.php from Gsk.Transform - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GskTransform (boxed value type)
#include "php_gtk4.h"
#include "core/boxed.h"
#include "core/object.h"
#include <cstring>
#include "core/enums.h"

using namespace phpgtk;

namespace {

const char *const fields[] = {nullptr};

// Boxed field reader: the public scalar fields as PHP properties.
bool read(gpointer data, const char *field, zval *rv) {
  (void)data;
  (void)field;
  (void)rv;
  return false;
}

// Boxed field writer: each field converts like a parameter of its type (core/boxed).
bool write(gpointer data, const char *field, zval *v) {
  (void)data;
  (void)field;
  (void)v;
  return false;
}

}  // namespace

/**
 * Gtk4\GskTransform::__construct()
 */
ZEND_METHOD(Gtk4_GskTransform, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  gpointer obj = gsk_transform_new();
  if (obj == nullptr) {
    zend_throw_error(nullptr, "%s(): GTK refused to create the value (see the CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  boxed_adopt(boxed_from_zval(ZEND_THIS), GSK_TYPE_TRANSFORM, obj);
}

/**
 * Gtk4\GskTransform::equal(?GskTransform $second): bool
 *
 * Checks two transforms for equality.
 */
ZEND_METHOD(Gtk4_GskTransform, equal) {
  zval *second = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(second, boxed_class_for_type(GSK_TYPE_TRANSFORM)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GskTransform *self = PHPGTK_BOXED_SELF(GskTransform);
  gpointer second_b = nullptr;
  if (second != nullptr) {
    second_b = unwrap_boxed(second, GSK_TYPE_TRANSFORM);
    if (second_b == nullptr) RETURN_THROWS();
  }
  RETURN_BOOL(gsk_transform_equal(self, static_cast<GskTransform *>(second_b)));
}

/**
 * Gtk4\GskTransform::get_category(): GskTransformCategory
 *
 * Returns the category this transform belongs to.
 */
ZEND_METHOD(Gtk4_GskTransform, get_category) {
  ZEND_PARSE_PARAMETERS_NONE();
  GskTransform *self = PHPGTK_BOXED_SELF(GskTransform);
  enum_to_php(GSK_TYPE_TRANSFORM_CATEGORY, gsk_transform_get_category(self), return_value);
}

/**
 * Gtk4\GskTransform::invert(): ?GskTransform
 *
 * Inverts the given transform.
 */
ZEND_METHOD(Gtk4_GskTransform, invert) {
  ZEND_PARSE_PARAMETERS_NONE();
  GskTransform *owned = PHPGTK_BOXED_SELF(GskTransform);
  auto *self = static_cast<GskTransform *>(
      g_boxed_copy(GSK_TYPE_TRANSFORM, owned));  // the call takes ownership of it
  GskTransform *phpgtk_ret = gsk_transform_invert(self);
  wrap_boxed(GSK_TYPE_TRANSFORM, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(GSK_TYPE_TRANSFORM, phpgtk_ret);
}

/**
 * Gtk4\GskTransform::perspective(float $depth): GskTransform
 *
 * Applies a perspective projection transform.
 */
ZEND_METHOD(Gtk4_GskTransform, perspective) {
  double depth;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_DOUBLE(depth)
  ZEND_PARSE_PARAMETERS_END();
  GskTransform *owned = PHPGTK_BOXED_SELF(GskTransform);
  auto *self = static_cast<GskTransform *>(
      g_boxed_copy(GSK_TYPE_TRANSFORM, owned));  // the call takes ownership of it
  GskTransform *phpgtk_ret = gsk_transform_perspective(self, static_cast<float>(depth));
  wrap_boxed(GSK_TYPE_TRANSFORM, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(GSK_TYPE_TRANSFORM, phpgtk_ret);
}

/**
 * Gtk4\GskTransform::rotate(float $angle): ?GskTransform
 *
 * Rotates $next $angle degrees in 2D - or in 3D-speak, around the Z axis. The rotation happens
 * around the origin point of (0, 0).
 */
ZEND_METHOD(Gtk4_GskTransform, rotate) {
  double angle;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_DOUBLE(angle)
  ZEND_PARSE_PARAMETERS_END();
  GskTransform *owned = PHPGTK_BOXED_SELF(GskTransform);
  auto *self = static_cast<GskTransform *>(
      g_boxed_copy(GSK_TYPE_TRANSFORM, owned));  // the call takes ownership of it
  GskTransform *phpgtk_ret = gsk_transform_rotate(self, static_cast<float>(angle));
  wrap_boxed(GSK_TYPE_TRANSFORM, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(GSK_TYPE_TRANSFORM, phpgtk_ret);
}

/**
 * Gtk4\GskTransform::scale(float $factor_x, float $factor_y): ?GskTransform
 *
 * Scales $next in 2-dimensional space by the given factors.
 */
ZEND_METHOD(Gtk4_GskTransform, scale) {
  double factor_x;
  double factor_y;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_DOUBLE(factor_x)
  Z_PARAM_DOUBLE(factor_y)
  ZEND_PARSE_PARAMETERS_END();
  GskTransform *owned = PHPGTK_BOXED_SELF(GskTransform);
  auto *self = static_cast<GskTransform *>(
      g_boxed_copy(GSK_TYPE_TRANSFORM, owned));  // the call takes ownership of it
  GskTransform *phpgtk_ret =
      gsk_transform_scale(self, static_cast<float>(factor_x), static_cast<float>(factor_y));
  wrap_boxed(GSK_TYPE_TRANSFORM, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(GSK_TYPE_TRANSFORM, phpgtk_ret);
}

/**
 * Gtk4\GskTransform::scale_3d(float $factor_x, float $factor_y, float $factor_z): ?GskTransform
 *
 * Scales $next by the given factors.
 */
ZEND_METHOD(Gtk4_GskTransform, scale_3d) {
  double factor_x;
  double factor_y;
  double factor_z;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_DOUBLE(factor_x)
  Z_PARAM_DOUBLE(factor_y)
  Z_PARAM_DOUBLE(factor_z)
  ZEND_PARSE_PARAMETERS_END();
  GskTransform *owned = PHPGTK_BOXED_SELF(GskTransform);
  auto *self = static_cast<GskTransform *>(
      g_boxed_copy(GSK_TYPE_TRANSFORM, owned));  // the call takes ownership of it
  GskTransform *phpgtk_ret =
      gsk_transform_scale_3d(self, static_cast<float>(factor_x), static_cast<float>(factor_y),
                             static_cast<float>(factor_z));
  wrap_boxed(GSK_TYPE_TRANSFORM, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(GSK_TYPE_TRANSFORM, phpgtk_ret);
}

/**
 * Gtk4\GskTransform::skew(float $skew_x, float $skew_y): ?GskTransform
 *
 * Applies a skew transform.
 */
ZEND_METHOD(Gtk4_GskTransform, skew) {
  double skew_x;
  double skew_y;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_DOUBLE(skew_x)
  Z_PARAM_DOUBLE(skew_y)
  ZEND_PARSE_PARAMETERS_END();
  GskTransform *owned = PHPGTK_BOXED_SELF(GskTransform);
  auto *self = static_cast<GskTransform *>(
      g_boxed_copy(GSK_TYPE_TRANSFORM, owned));  // the call takes ownership of it
  GskTransform *phpgtk_ret =
      gsk_transform_skew(self, static_cast<float>(skew_x), static_cast<float>(skew_y));
  wrap_boxed(GSK_TYPE_TRANSFORM, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(GSK_TYPE_TRANSFORM, phpgtk_ret);
}

/**
 * Gtk4\GskTransform::to_2d(): array
 *
 * Converts a `GskTransform` to a 2D transformation matrix.
 */
ZEND_METHOD(Gtk4_GskTransform, to_2d) {
  ZEND_PARSE_PARAMETERS_NONE();
  GskTransform *self = PHPGTK_BOXED_SELF(GskTransform);
  float out_xx = 0;
  float out_yx = 0;
  float out_xy = 0;
  float out_yy = 0;
  float out_dx = 0;
  float out_dy = 0;
  gsk_transform_to_2d(self, &out_xx, &out_yx, &out_xy, &out_yy, &out_dx, &out_dy);
  array_init_size(return_value, 6);
  {
    zval item;
    ZVAL_DOUBLE(&item, out_xx);
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_DOUBLE(&item, out_yx);
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_DOUBLE(&item, out_xy);
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_DOUBLE(&item, out_yy);
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_DOUBLE(&item, out_dx);
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_DOUBLE(&item, out_dy);
    add_next_index_zval(return_value, &item);
  }
}

/**
 * Gtk4\GskTransform::to_2d_components(): array
 *
 * Converts a `GskTransform` to 2D transformation factors.
 */
ZEND_METHOD(Gtk4_GskTransform, to_2d_components) {
  ZEND_PARSE_PARAMETERS_NONE();
  GskTransform *self = PHPGTK_BOXED_SELF(GskTransform);
  float out_skew_x = 0;
  float out_skew_y = 0;
  float out_scale_x = 0;
  float out_scale_y = 0;
  float out_angle = 0;
  float out_dx = 0;
  float out_dy = 0;
  gsk_transform_to_2d_components(self, &out_skew_x, &out_skew_y, &out_scale_x, &out_scale_y,
                                 &out_angle, &out_dx, &out_dy);
  array_init_size(return_value, 7);
  {
    zval item;
    ZVAL_DOUBLE(&item, out_skew_x);
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_DOUBLE(&item, out_skew_y);
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_DOUBLE(&item, out_scale_x);
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_DOUBLE(&item, out_scale_y);
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_DOUBLE(&item, out_angle);
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_DOUBLE(&item, out_dx);
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_DOUBLE(&item, out_dy);
    add_next_index_zval(return_value, &item);
  }
}

/**
 * Gtk4\GskTransform::to_affine(): array
 *
 * Converts a `GskTransform` to 2D affine transformation factors.
 */
ZEND_METHOD(Gtk4_GskTransform, to_affine) {
  ZEND_PARSE_PARAMETERS_NONE();
  GskTransform *self = PHPGTK_BOXED_SELF(GskTransform);
  float out_scale_x = 0;
  float out_scale_y = 0;
  float out_dx = 0;
  float out_dy = 0;
  gsk_transform_to_affine(self, &out_scale_x, &out_scale_y, &out_dx, &out_dy);
  array_init_size(return_value, 4);
  {
    zval item;
    ZVAL_DOUBLE(&item, out_scale_x);
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_DOUBLE(&item, out_scale_y);
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_DOUBLE(&item, out_dx);
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_DOUBLE(&item, out_dy);
    add_next_index_zval(return_value, &item);
  }
}

/**
 * Gtk4\GskTransform::to_string(): string
 *
 * Converts a matrix into a string that is suitable for printing.
 */
ZEND_METHOD(Gtk4_GskTransform, to_string) {
  ZEND_PARSE_PARAMETERS_NONE();
  GskTransform *self = PHPGTK_BOXED_SELF(GskTransform);
  char *phpgtk_ret = gsk_transform_to_string(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETVAL_STRING(phpgtk_ret);
  g_free(phpgtk_ret);
}

/**
 * Gtk4\GskTransform::to_translate(): array
 *
 * Converts a `GskTransform` to a translation operation.
 */
ZEND_METHOD(Gtk4_GskTransform, to_translate) {
  ZEND_PARSE_PARAMETERS_NONE();
  GskTransform *self = PHPGTK_BOXED_SELF(GskTransform);
  float out_dx = 0;
  float out_dy = 0;
  gsk_transform_to_translate(self, &out_dx, &out_dy);
  array_init_size(return_value, 2);
  {
    zval item;
    ZVAL_DOUBLE(&item, out_dx);
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_DOUBLE(&item, out_dy);
    add_next_index_zval(return_value, &item);
  }
}

/**
 * Gtk4\GskTransform::transform(?GskTransform $other): ?GskTransform
 *
 * Applies all the operations from $other to $next.
 */
ZEND_METHOD(Gtk4_GskTransform, transform) {
  zval *other = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(other, boxed_class_for_type(GSK_TYPE_TRANSFORM)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GskTransform *owned = PHPGTK_BOXED_SELF(GskTransform);
  auto *self = static_cast<GskTransform *>(
      g_boxed_copy(GSK_TYPE_TRANSFORM, owned));  // the call takes ownership of it
  gpointer other_b = nullptr;
  if (other != nullptr) {
    other_b = unwrap_boxed(other, GSK_TYPE_TRANSFORM);
    if (other_b == nullptr) RETURN_THROWS();
  }
  GskTransform *phpgtk_ret = gsk_transform_transform(self, static_cast<GskTransform *>(other_b));
  wrap_boxed(GSK_TYPE_TRANSFORM, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(GSK_TYPE_TRANSFORM, phpgtk_ret);
}

/**
 * Gtk4\GskTransform::transform_bounds(GrapheneRect $rect): GrapheneRect
 *
 * Transforms a `graphene_rect_t` using the given transform $self.
 */
ZEND_METHOD(Gtk4_GskTransform, transform_bounds) {
  zval *rect;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(rect, boxed_class_for_type(GRAPHENE_TYPE_RECT)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GskTransform *self = PHPGTK_BOXED_SELF(GskTransform);
  gpointer rect_b = unwrap_boxed(rect, GRAPHENE_TYPE_RECT);
  if (rect_b == nullptr) RETURN_THROWS();
  graphene_rect_t out_rect{};
  gsk_transform_transform_bounds(self, static_cast<graphene_rect_t *>(rect_b), &out_rect);
  wrap_boxed(GRAPHENE_TYPE_RECT, &out_rect, return_value);
}

/**
 * Gtk4\GskTransform::transform_point(GraphenePoint $point): GraphenePoint
 *
 * Transforms a `graphene_point_t` using the given transform $self.
 */
ZEND_METHOD(Gtk4_GskTransform, transform_point) {
  zval *point;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(point, boxed_class_for_type(GRAPHENE_TYPE_POINT)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GskTransform *self = PHPGTK_BOXED_SELF(GskTransform);
  gpointer point_b = unwrap_boxed(point, GRAPHENE_TYPE_POINT);
  if (point_b == nullptr) RETURN_THROWS();
  graphene_point_t out_point{};
  gsk_transform_transform_point(self, static_cast<graphene_point_t *>(point_b), &out_point);
  wrap_boxed(GRAPHENE_TYPE_POINT, &out_point, return_value);
}

/**
 * Gtk4\GskTransform::translate(GraphenePoint $point): ?GskTransform
 *
 * Translates $next in 2-dimensional space by $point.
 */
ZEND_METHOD(Gtk4_GskTransform, translate) {
  zval *point;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(point, boxed_class_for_type(GRAPHENE_TYPE_POINT)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GskTransform *owned = PHPGTK_BOXED_SELF(GskTransform);
  auto *self = static_cast<GskTransform *>(
      g_boxed_copy(GSK_TYPE_TRANSFORM, owned));  // the call takes ownership of it
  gpointer point_b = unwrap_boxed(point, GRAPHENE_TYPE_POINT);
  if (point_b == nullptr) RETURN_THROWS();
  GskTransform *phpgtk_ret =
      gsk_transform_translate(self, static_cast<graphene_point_t *>(point_b));
  wrap_boxed(GSK_TYPE_TRANSFORM, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(GSK_TYPE_TRANSFORM, phpgtk_ret);
}

/**
 * public static function parse(string $string): ?GskTransform
 * The transform to_string() wrote (or CSS transform syntax: "translate(5, 5) scale(2)"), or
 * null when $string is not one - an empty string is the identity, also null.
 *
 * GIR answers through an out parameter next to a boolean; PHP gets the transform or null.
 */
ZEND_METHOD(Gtk4_GskTransform, parse) {
  zend_string *text;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(text)
  ZEND_PARSE_PARAMETERS_END();
  if (!phpgtk::check_utf8(text, 1)) RETURN_THROWS();
  GskTransform *out = nullptr;
  if (!gsk_transform_parse(ZSTR_VAL(text), &out) || out == nullptr) RETURN_NULL();
  wrap_boxed(GSK_TYPE_TRANSFORM, out, return_value);
  gsk_transform_unref(out);
}

namespace phpgtk {
// MINIT: bind the PHP class to GSK_TYPE_TRANSFORM with its field table.
void register_GskTransform(zend_class_entry *ce) {
  register_boxed(BoxedClass{
      .type = GSK_TYPE_TRANSFORM,
      .ce = ce,
      .fields = fields,
      .read = read,
      .write = write,
      // NOLINTBEGIN(cppcoreguidelines-pro-type-const-cast) gsk_transform_equal() takes non-const
      // operands
      .equal =
          [](gconstpointer a, gconstpointer b) {
            return gsk_transform_equal(static_cast<GskTransform *>(const_cast<gpointer>(a)),
                                       static_cast<GskTransform *>(const_cast<gpointer>(b))) !=
                   FALSE;
          }
      // NOLINTEND(cppcoreguidelines-pro-type-const-cast)
  });
}
}  // namespace phpgtk
