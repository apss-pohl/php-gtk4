// GENERATED by gen/gir.php from Gsk.CairoNode - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GskCairoNode (fundamental handle)
#include "php_gtk4.h"
#include "core/fundamental.h"
#include "core/object.h"
#include "core/boxed.h"
#include <cairo-gobject.h>

using namespace phpgtk;

/**
 * Gtk4\GskCairoNode::__construct(GrapheneRect $bounds)
 *
 * Creates a `GskRenderNode` that will render a cairo surface into the area given by $bounds.
 */
ZEND_METHOD(Gtk4_GskCairoNode, __construct) {
  zval *bounds;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(bounds, boxed_class_for_type(GRAPHENE_TYPE_RECT)->ce)
  ZEND_PARSE_PARAMETERS_END();
  gpointer bounds_b = unwrap_boxed(bounds, GRAPHENE_TYPE_RECT);
  if (bounds_b == nullptr) RETURN_THROWS();
  gpointer obj = gsk_cairo_node_new(static_cast<graphene_rect_t *>(bounds_b));
  if (obj == nullptr) {
    zend_throw_error(nullptr, "%s(): GTK refused to create the instance (see the CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  fundamental_adopt(fundamental_from_zval(ZEND_THIS), GSK_TYPE_CAIRO_NODE, obj);
}

/**
 * Gtk4\GskCairoNode::get_draw_context(): CairoContext
 *
 * Creates a Cairo context for drawing using the surface associated to the render node.
 */
ZEND_METHOD(Gtk4_GskCairoNode, get_draw_context) {
  ZEND_PARSE_PARAMETERS_NONE();
  auto *self = static_cast<GskRenderNode *>(fundamental_self(execute_data));
  if (self == nullptr) RETURN_THROWS();
  gpointer phpgtk_ret = gsk_cairo_node_get_draw_context(self);
  wrap_fundamental(CAIRO_GOBJECT_TYPE_CONTEXT, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) cairo_destroy(static_cast<cairo_t *>(phpgtk_ret));
}

/**
 * Gtk4\GskCairoNode::get_surface(): CairoSurface
 *
 * Retrieves the Cairo surface used by the render node.
 */
ZEND_METHOD(Gtk4_GskCairoNode, get_surface) {
  ZEND_PARSE_PARAMETERS_NONE();
  auto *self = static_cast<GskRenderNode *>(fundamental_self(execute_data));
  if (self == nullptr) RETURN_THROWS();
  gpointer phpgtk_ret = gsk_cairo_node_get_surface(self);
  wrap_fundamental(CAIRO_GOBJECT_TYPE_SURFACE, phpgtk_ret, return_value);
}

namespace {

// gsk_render_node_ref() as the fundamental registry's RefFn.
gpointer fundamental_ref(gpointer p) {
  return gsk_render_node_ref(static_cast<GskRenderNode *>(p));
}

// gsk_render_node_unref() as the fundamental registry's UnrefFn.
void fundamental_unref(gpointer p) {
  gsk_render_node_unref(static_cast<GskRenderNode *>(p));
}

}  // namespace

namespace phpgtk {
// MINIT: bind the PHP class to GSK_TYPE_CAIRO_NODE on the fundamental registry.
void register_GskCairoNode(zend_class_entry *ce) {
  register_fundamental(FundamentalClass{
      .type = GSK_TYPE_CAIRO_NODE, .ce = ce, .ref = fundamental_ref, .unref = fundamental_unref});
}
}  // namespace phpgtk
