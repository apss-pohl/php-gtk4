// GENERATED by gen/gir.php from Gsk.CrossFadeNode - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GskCrossFadeNode (fundamental handle)
#include "php_gtk4.h"
#include "core/fundamental.h"
#include "core/object.h"

using namespace phpgtk;

/**
 * Gtk4\GskCrossFadeNode::__construct(GskRenderNode $start, GskRenderNode $end, float $progress)
 *
 * Creates a `GskRenderNode` that will do a cross-fade between $start and $end.
 */
ZEND_METHOD(Gtk4_GskCrossFadeNode, __construct) {
  zval *start;
  zval *end;
  double progress;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_OBJECT_OF_CLASS(start, fundamental_class_for_type(GSK_TYPE_RENDER_NODE)->ce)
  Z_PARAM_OBJECT_OF_CLASS(end, fundamental_class_for_type(GSK_TYPE_RENDER_NODE)->ce)
  Z_PARAM_DOUBLE(progress)
  ZEND_PARSE_PARAMETERS_END();
  gpointer start_f = unwrap_fundamental(start, GSK_TYPE_RENDER_NODE);
  if (start_f == nullptr) RETURN_THROWS();
  gpointer end_f = unwrap_fundamental(end, GSK_TYPE_RENDER_NODE);
  if (end_f == nullptr) RETURN_THROWS();
  gpointer obj =
      gsk_cross_fade_node_new(static_cast<GskRenderNode *>(start_f),
                              static_cast<GskRenderNode *>(end_f), static_cast<float>(progress));
  if (obj == nullptr) {
    zend_throw_error(nullptr, "%s(): GTK refused to create the instance (see the CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  fundamental_adopt(fundamental_from_zval(ZEND_THIS), GSK_TYPE_CROSS_FADE_NODE, obj);
}

/**
 * Gtk4\GskCrossFadeNode::get_end_child(): GskRenderNode
 *
 * Retrieves the child `GskRenderNode` at the end of the cross-fade.
 */
ZEND_METHOD(Gtk4_GskCrossFadeNode, get_end_child) {
  ZEND_PARSE_PARAMETERS_NONE();
  auto *self = static_cast<GskRenderNode *>(fundamental_self(execute_data));
  if (self == nullptr) RETURN_THROWS();
  gpointer phpgtk_ret = gsk_cross_fade_node_get_end_child(self);
  wrap_fundamental(GSK_TYPE_RENDER_NODE, phpgtk_ret, return_value);
}

/**
 * Gtk4\GskCrossFadeNode::get_progress(): float
 *
 * Retrieves the progress value of the cross fade.
 */
ZEND_METHOD(Gtk4_GskCrossFadeNode, get_progress) {
  ZEND_PARSE_PARAMETERS_NONE();
  auto *self = static_cast<GskRenderNode *>(fundamental_self(execute_data));
  if (self == nullptr) RETURN_THROWS();
  RETURN_DOUBLE(gsk_cross_fade_node_get_progress(self));
}

/**
 * Gtk4\GskCrossFadeNode::get_start_child(): GskRenderNode
 *
 * Retrieves the child `GskRenderNode` at the beginning of the cross-fade.
 */
ZEND_METHOD(Gtk4_GskCrossFadeNode, get_start_child) {
  ZEND_PARSE_PARAMETERS_NONE();
  auto *self = static_cast<GskRenderNode *>(fundamental_self(execute_data));
  if (self == nullptr) RETURN_THROWS();
  gpointer phpgtk_ret = gsk_cross_fade_node_get_start_child(self);
  wrap_fundamental(GSK_TYPE_RENDER_NODE, phpgtk_ret, return_value);
}

namespace {

// gsk_render_node_ref() as the fundamental registry's RefFn.
gpointer fundamental_ref(gpointer p) {
  return gsk_render_node_ref(static_cast<GskRenderNode *>(p));
}

// gsk_render_node_unref() as the fundamental registry's UnrefFn.
void fundamental_unref(gpointer p) {
  gsk_render_node_unref(static_cast<GskRenderNode *>(p));
}

}  // namespace

namespace phpgtk {
// MINIT: bind the PHP class to GSK_TYPE_CROSS_FADE_NODE on the fundamental registry.
void register_GskCrossFadeNode(zend_class_entry *ce) {
  register_fundamental(FundamentalClass{.type = GSK_TYPE_CROSS_FADE_NODE,
                                        .ce = ce,
                                        .ref = fundamental_ref,
                                        .unref = fundamental_unref});
}
}  // namespace phpgtk
