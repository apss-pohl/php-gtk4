// GENERATED by gen/gir.php from Gsk.PathPoint - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GskPathPoint (boxed value type)
#include "php_gtk4.h"
#include "core/boxed.h"
#include "core/object.h"
#include <cstring>
#include "core/enums.h"

using namespace phpgtk;

namespace {

const char *const fields[] = {nullptr};

// Boxed field reader: the public scalar fields as PHP properties.
bool read(gpointer data, const char *field, zval *rv) {
  (void)data;
  (void)field;
  (void)rv;
  return false;
}

// Boxed field writer: each field converts like a parameter of its type (core/boxed).
bool write(gpointer data, const char *field, zval *v) {
  (void)data;
  (void)field;
  (void)v;
  return false;
}

}  // namespace

/**
 * Gtk4\GskPathPoint::__construct()
 *
 * GskPathPoint values come from GTK, never from `new`.
 */
ZEND_METHOD(Gtk4_GskPathPoint, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
}

/**
 * Gtk4\GskPathPoint::compare(GskPathPoint $point2): int
 *
 * Returns whether $point1 is before or after $point2.
 */
ZEND_METHOD(Gtk4_GskPathPoint, compare) {
  zval *point2;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(point2, boxed_class_for_type(GSK_TYPE_PATH_POINT)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GskPathPoint *self = PHPGTK_BOXED_SELF(GskPathPoint);
  gpointer point2_b = unwrap_boxed(point2, GSK_TYPE_PATH_POINT);
  if (point2_b == nullptr) RETURN_THROWS();
  RETURN_LONG(
      static_cast<zend_long>(gsk_path_point_compare(self, static_cast<GskPathPoint *>(point2_b))));
}

/**
 * Gtk4\GskPathPoint::equal(GskPathPoint $point2): bool
 *
 * Returns whether the two path points refer to the same location on all paths.
 */
ZEND_METHOD(Gtk4_GskPathPoint, equal) {
  zval *point2;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(point2, boxed_class_for_type(GSK_TYPE_PATH_POINT)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GskPathPoint *self = PHPGTK_BOXED_SELF(GskPathPoint);
  gpointer point2_b = unwrap_boxed(point2, GSK_TYPE_PATH_POINT);
  if (point2_b == nullptr) RETURN_THROWS();
  RETURN_BOOL(gsk_path_point_equal(self, static_cast<GskPathPoint *>(point2_b)));
}

/**
 * Gtk4\GskPathPoint::get_distance(GskPathMeasure $measure): float
 *
 * Returns the distance from the beginning of the path to $point.
 */
ZEND_METHOD(Gtk4_GskPathPoint, get_distance) {
  zval *measure;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(measure, boxed_class_for_type(GSK_TYPE_PATH_MEASURE)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GskPathPoint *self = PHPGTK_BOXED_SELF(GskPathPoint);
  gpointer measure_b = unwrap_boxed(measure, GSK_TYPE_PATH_MEASURE);
  if (measure_b == nullptr) RETURN_THROWS();
  RETURN_DOUBLE(gsk_path_point_get_distance(self, static_cast<GskPathMeasure *>(measure_b)));
}

/**
 * Gtk4\GskPathPoint::get_position(GskPath $path): GraphenePoint
 *
 * Gets the position of the point.
 */
ZEND_METHOD(Gtk4_GskPathPoint, get_position) {
  zval *path;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(path, boxed_class_for_type(GSK_TYPE_PATH)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GskPathPoint *self = PHPGTK_BOXED_SELF(GskPathPoint);
  gpointer path_b = unwrap_boxed(path, GSK_TYPE_PATH);
  if (path_b == nullptr) RETURN_THROWS();
  graphene_point_t position{};
  gsk_path_point_get_position(self, static_cast<GskPath *>(path_b), &position);
  wrap_boxed(GRAPHENE_TYPE_POINT, &position, return_value);
}

/**
 * Gtk4\GskPathPoint::get_rotation(GskPath $path, GskPathDirection $direction): float
 *
 * Gets the direction of the tangent at a given point.
 */
ZEND_METHOD(Gtk4_GskPathPoint, get_rotation) {
  zval *path;
  zval *direction;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(path, boxed_class_for_type(GSK_TYPE_PATH)->ce)
  Z_PARAM_OBJECT_OF_CLASS(direction, enum_class_for_type(GSK_TYPE_PATH_DIRECTION))
  ZEND_PARSE_PARAMETERS_END();
  GskPathPoint *self = PHPGTK_BOXED_SELF(GskPathPoint);
  gpointer path_b = unwrap_boxed(path, GSK_TYPE_PATH);
  if (path_b == nullptr) RETURN_THROWS();
  gint direction_v = 0;
  if (!enum_from_php(direction, GSK_TYPE_PATH_DIRECTION, &direction_v)) RETURN_THROWS();
  RETURN_DOUBLE(gsk_path_point_get_rotation(self, static_cast<GskPath *>(path_b),
                                            static_cast<GskPathDirection>(direction_v)));
}

/**
 * Gtk4\GskPathPoint::get_tangent(GskPath $path, GskPathDirection $direction): GrapheneVec2
 *
 * Gets the tangent of the path at the point.
 */
ZEND_METHOD(Gtk4_GskPathPoint, get_tangent) {
  zval *path;
  zval *direction;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(path, boxed_class_for_type(GSK_TYPE_PATH)->ce)
  Z_PARAM_OBJECT_OF_CLASS(direction, enum_class_for_type(GSK_TYPE_PATH_DIRECTION))
  ZEND_PARSE_PARAMETERS_END();
  GskPathPoint *self = PHPGTK_BOXED_SELF(GskPathPoint);
  gpointer path_b = unwrap_boxed(path, GSK_TYPE_PATH);
  if (path_b == nullptr) RETURN_THROWS();
  gint direction_v = 0;
  if (!enum_from_php(direction, GSK_TYPE_PATH_DIRECTION, &direction_v)) RETURN_THROWS();
  graphene_vec2_t tangent{};
  gsk_path_point_get_tangent(self, static_cast<GskPath *>(path_b),
                             static_cast<GskPathDirection>(direction_v), &tangent);
  wrap_boxed(GRAPHENE_TYPE_VEC2, &tangent, return_value);
}

namespace phpgtk {
// MINIT: bind the PHP class to GSK_TYPE_PATH_POINT with its field table.
void register_GskPathPoint(zend_class_entry *ce) {
  register_boxed(BoxedClass{.type = GSK_TYPE_PATH_POINT,
                            .ce = ce,
                            .fields = fields,
                            .read = read,
                            .write = write,
                            .equal = [](gconstpointer a, gconstpointer b) {
                              return gsk_path_point_equal(static_cast<const GskPathPoint *>(a),
                                                          static_cast<const GskPathPoint *>(b)) !=
                                     FALSE;
                            }});
}
}  // namespace phpgtk
