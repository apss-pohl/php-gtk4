// GENERATED by gen/gir.php from Gsk.Stroke - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GskStroke (boxed value type)
#include "php_gtk4.h"
#include "core/boxed.h"
#include "core/object.h"
#include <cstring>
#include "core/enums.h"
#include "core/fundamental.h"
#include <cairo-gobject.h>

using namespace phpgtk;

// set_dash()/get_dash(): the dash pattern is a PHP list of floats, not a C array.
#include <vector>

namespace {

const char *const fields[] = {nullptr};

// Boxed field reader: the public scalar fields as PHP properties.
bool read(gpointer data, const char *field, zval *rv) {
  (void)data;
  (void)field;
  (void)rv;
  return false;
}

// Boxed field writer: each field converts like a parameter of its type (core/boxed).
bool write(gpointer data, const char *field, zval *v) {
  (void)data;
  (void)field;
  (void)v;
  return false;
}

}  // namespace

/**
 * Gtk4\GskStroke::__construct(float $line_width)
 *
 * Creates a new `GskStroke` with the given $line_width.
 */
ZEND_METHOD(Gtk4_GskStroke, __construct) {
  double line_width;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_DOUBLE(line_width)
  ZEND_PARSE_PARAMETERS_END();
  if (!phpgtk::check_domain_above(line_width, 0.0, 1)) RETURN_THROWS();
  gpointer obj = gsk_stroke_new(static_cast<float>(line_width));
  if (obj == nullptr) {
    zend_throw_error(nullptr, "%s(): GTK refused to create the value (see the CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  boxed_adopt(boxed_from_zval(ZEND_THIS), GSK_TYPE_STROKE, obj);
}

/**
 * Gtk4\GskStroke::get_dash_offset(): float
 *
 * Returns the dash_offset of a `GskStroke`.
 */
ZEND_METHOD(Gtk4_GskStroke, get_dash_offset) {
  ZEND_PARSE_PARAMETERS_NONE();
  GskStroke *self = PHPGTK_BOXED_SELF(GskStroke);
  RETURN_DOUBLE(gsk_stroke_get_dash_offset(self));
}

/**
 * Gtk4\GskStroke::get_line_cap(): GskLineCap
 *
 * Gets the line cap used.
 */
ZEND_METHOD(Gtk4_GskStroke, get_line_cap) {
  ZEND_PARSE_PARAMETERS_NONE();
  GskStroke *self = PHPGTK_BOXED_SELF(GskStroke);
  enum_to_php(GSK_TYPE_LINE_CAP, gsk_stroke_get_line_cap(self), return_value);
}

/**
 * Gtk4\GskStroke::get_line_join(): GskLineJoin
 *
 * Gets the line join used.
 */
ZEND_METHOD(Gtk4_GskStroke, get_line_join) {
  ZEND_PARSE_PARAMETERS_NONE();
  GskStroke *self = PHPGTK_BOXED_SELF(GskStroke);
  enum_to_php(GSK_TYPE_LINE_JOIN, gsk_stroke_get_line_join(self), return_value);
}

/**
 * Gtk4\GskStroke::get_line_width(): float
 *
 * Gets the line width used.
 */
ZEND_METHOD(Gtk4_GskStroke, get_line_width) {
  ZEND_PARSE_PARAMETERS_NONE();
  GskStroke *self = PHPGTK_BOXED_SELF(GskStroke);
  RETURN_DOUBLE(gsk_stroke_get_line_width(self));
}

/**
 * Gtk4\GskStroke::get_miter_limit(): float
 *
 * Returns the miter limit of a `GskStroke`.
 */
ZEND_METHOD(Gtk4_GskStroke, get_miter_limit) {
  ZEND_PARSE_PARAMETERS_NONE();
  GskStroke *self = PHPGTK_BOXED_SELF(GskStroke);
  RETURN_DOUBLE(gsk_stroke_get_miter_limit(self));
}

/**
 * Gtk4\GskStroke::set_dash_offset(float $offset): void
 *
 * Sets the offset into the dash pattern where dashing should begin.
 */
ZEND_METHOD(Gtk4_GskStroke, set_dash_offset) {
  double offset;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_DOUBLE(offset)
  ZEND_PARSE_PARAMETERS_END();
  GskStroke *self = PHPGTK_BOXED_SELF(GskStroke);
  gsk_stroke_set_dash_offset(self, static_cast<float>(offset));
}

/**
 * Gtk4\GskStroke::set_line_cap(GskLineCap $line_cap): void
 *
 * Sets the line cap to be used when stroking.
 */
ZEND_METHOD(Gtk4_GskStroke, set_line_cap) {
  zval *line_cap;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(line_cap, enum_class_for_type(GSK_TYPE_LINE_CAP))
  ZEND_PARSE_PARAMETERS_END();
  GskStroke *self = PHPGTK_BOXED_SELF(GskStroke);
  gint line_cap_v = 0;
  if (!enum_from_php(line_cap, GSK_TYPE_LINE_CAP, &line_cap_v)) RETURN_THROWS();
  gsk_stroke_set_line_cap(self, static_cast<GskLineCap>(line_cap_v));
}

/**
 * Gtk4\GskStroke::set_line_join(GskLineJoin $line_join): void
 *
 * Sets the line join to be used when stroking.
 */
ZEND_METHOD(Gtk4_GskStroke, set_line_join) {
  zval *line_join;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(line_join, enum_class_for_type(GSK_TYPE_LINE_JOIN))
  ZEND_PARSE_PARAMETERS_END();
  GskStroke *self = PHPGTK_BOXED_SELF(GskStroke);
  gint line_join_v = 0;
  if (!enum_from_php(line_join, GSK_TYPE_LINE_JOIN, &line_join_v)) RETURN_THROWS();
  gsk_stroke_set_line_join(self, static_cast<GskLineJoin>(line_join_v));
}

/**
 * Gtk4\GskStroke::set_line_width(float $line_width): void
 *
 * Sets the line width to be used when stroking.
 */
ZEND_METHOD(Gtk4_GskStroke, set_line_width) {
  double line_width;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_DOUBLE(line_width)
  ZEND_PARSE_PARAMETERS_END();
  GskStroke *self = PHPGTK_BOXED_SELF(GskStroke);
  if (!phpgtk::check_domain_above(line_width, 0.0, 1)) RETURN_THROWS();
  gsk_stroke_set_line_width(self, static_cast<float>(line_width));
}

/**
 * Gtk4\GskStroke::set_miter_limit(float $limit): void
 *
 * Sets the limit for the distance from the corner where sharp turns of joins get cut off.
 */
ZEND_METHOD(Gtk4_GskStroke, set_miter_limit) {
  double limit;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_DOUBLE(limit)
  ZEND_PARSE_PARAMETERS_END();
  GskStroke *self = PHPGTK_BOXED_SELF(GskStroke);
  if (!phpgtk::check_domain_double(limit, 0.0, HUGE_VAL, 1)) RETURN_THROWS();
  gsk_stroke_set_miter_limit(self, static_cast<float>(limit));
}

/**
 * Gtk4\GskStroke::to_cairo(CairoContext $cr): void
 *
 * A helper function that sets the stroke parameters of $cr from the values found in $self.
 */
ZEND_METHOD(Gtk4_GskStroke, to_cairo) {
  zval *cr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(cr, fundamental_class_for_type(CAIRO_GOBJECT_TYPE_CONTEXT)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GskStroke *self = PHPGTK_BOXED_SELF(GskStroke);
  gpointer cr_f = unwrap_fundamental(cr, CAIRO_GOBJECT_TYPE_CONTEXT);
  if (cr_f == nullptr) RETURN_THROWS();
  gsk_stroke_to_cairo(self, static_cast<cairo_t *>(cr_f));
}

/**
 * public function get_dash(): array
 * The dash pattern set_dash() took; an empty list for a solid line.
 *
 * @return list<float>
 */
ZEND_METHOD(Gtk4_GskStroke, get_dash) {
  ZEND_PARSE_PARAMETERS_NONE();
  GskStroke *self = PHPGTK_BOXED_SELF(GskStroke);
  gsize n = 0;
  const float *dash = gsk_stroke_get_dash(self, &n);
  array_init_size(return_value, static_cast<uint32_t>(n));
  for (gsize i = 0; i < n; i++) {
    // NOLINTNEXTLINE(cppcoreguidelines-pro-bounds-pointer-arithmetic) a C array of n floats
    add_next_index_double(return_value, dash[i]);
  }
}

/**
 * public function set_dash(array $dash): void
 * The dash pattern: alternating "on" and "off" lengths (a list of floats, none negative and
 * not all zero); an empty list draws a solid line.
 */
ZEND_METHOD(Gtk4_GskStroke, set_dash) {
  zval *dash;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_ARRAY(dash)
  ZEND_PARSE_PARAMETERS_END();
  GskStroke *self = PHPGTK_BOXED_SELF(GskStroke);
  std::vector<float> lengths;
  bool any = false;
  zval *item;
  // NOLINTNEXTLINE(readability-math-missing-parentheses) Zend macro expansion
  ZEND_HASH_FOREACH_VAL(Z_ARRVAL_P(dash), item) {
    if (Z_TYPE_P(item) != IS_DOUBLE && Z_TYPE_P(item) != IS_LONG) {
      zend_argument_type_error(1, "must be a list of float, %s found in it",
                               zend_zval_value_name(item));
      RETURN_THROWS();
    }
    const double v = zval_get_double(item);
    if (v < 0) {
      zend_argument_value_error(1, "must not contain a negative length");
      RETURN_THROWS();
    }
    any = any || v > 0;
    lengths.push_back(static_cast<float>(v));
  }
  ZEND_HASH_FOREACH_END();
  if (!lengths.empty() && !any) {
    zend_argument_value_error(1, "must contain a length above zero (GSK refuses an all-zero dash)");
    RETURN_THROWS();
  }
  gsk_stroke_set_dash(self, lengths.empty() ? nullptr : lengths.data(), lengths.size());
}

namespace phpgtk {
// MINIT: bind the PHP class to GSK_TYPE_STROKE with its field table.
void register_GskStroke(zend_class_entry *ce) {
  register_boxed(BoxedClass{
      .type = GSK_TYPE_STROKE,
      .ce = ce,
      .fields = fields,
      .read = read,
      .write = write,
      // NOLINTBEGIN(cppcoreguidelines-pro-type-const-cast) gsk_stroke_equal() takes non-const
      // operands
      .equal =
          [](gconstpointer a, gconstpointer b) {
            return gsk_stroke_equal(static_cast<GskStroke *>(const_cast<gpointer>(a)),
                                    static_cast<GskStroke *>(const_cast<gpointer>(b))) != FALSE;
          }
      // NOLINTEND(cppcoreguidelines-pro-type-const-cast)
  });
}
}  // namespace phpgtk
