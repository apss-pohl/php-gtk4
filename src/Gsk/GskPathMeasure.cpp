// GENERATED by gen/gir.php from Gsk.PathMeasure - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GskPathMeasure (boxed value type)
#include "php_gtk4.h"
#include "core/boxed.h"
#include "core/object.h"
#include <cstring>

using namespace phpgtk;

namespace {

const char *const fields[] = {nullptr};

// Boxed field reader: the public scalar fields as PHP properties.
bool read(gpointer data, const char *field, zval *rv) {
  (void)data;
  (void)field;
  (void)rv;
  return false;
}

// Boxed field writer: each field converts like a parameter of its type (core/boxed).
bool write(gpointer data, const char *field, zval *v) {
  (void)data;
  (void)field;
  (void)v;
  return false;
}

}  // namespace

/**
 * Gtk4\GskPathMeasure::__construct(GskPath $path)
 *
 * Creates a measure object for the given $path with the default tolerance.
 */
ZEND_METHOD(Gtk4_GskPathMeasure, __construct) {
  zval *path;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(path, boxed_class_for_type(GSK_TYPE_PATH)->ce)
  ZEND_PARSE_PARAMETERS_END();
  gpointer path_b = unwrap_boxed(path, GSK_TYPE_PATH);
  if (path_b == nullptr) RETURN_THROWS();
  gpointer obj = gsk_path_measure_new(static_cast<GskPath *>(path_b));
  if (obj == nullptr) {
    zend_throw_error(nullptr, "%s(): GTK refused to create the value (see the CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  boxed_adopt(boxed_from_zval(ZEND_THIS), GSK_TYPE_PATH_MEASURE, obj);
}

/**
 * static Gtk4\GskPathMeasure::new_with_tolerance(GskPath $path, float $tolerance): GskPathMeasure
 *
 * Creates a measure object for the given $path and $tolerance.
 */
ZEND_METHOD(Gtk4_GskPathMeasure, new_with_tolerance) {
  zval *path;
  double tolerance;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(path, boxed_class_for_type(GSK_TYPE_PATH)->ce)
  Z_PARAM_DOUBLE(tolerance)
  ZEND_PARSE_PARAMETERS_END();
  gpointer path_b = unwrap_boxed(path, GSK_TYPE_PATH);
  if (path_b == nullptr) RETURN_THROWS();
  gpointer phpgtk_ret = gsk_path_measure_new_with_tolerance(static_cast<GskPath *>(path_b),
                                                            static_cast<float>(tolerance));
  wrap_boxed(GSK_TYPE_PATH_MEASURE, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(GSK_TYPE_PATH_MEASURE, phpgtk_ret);
}

/**
 * Gtk4\GskPathMeasure::get_length(): float
 *
 * Gets the length of the path being measured.
 */
ZEND_METHOD(Gtk4_GskPathMeasure, get_length) {
  ZEND_PARSE_PARAMETERS_NONE();
  GskPathMeasure *self = PHPGTK_BOXED_SELF(GskPathMeasure);
  RETURN_DOUBLE(gsk_path_measure_get_length(self));
}

/**
 * Gtk4\GskPathMeasure::get_path(): GskPath
 *
 * Returns the path that the measure was created for.
 */
ZEND_METHOD(Gtk4_GskPathMeasure, get_path) {
  ZEND_PARSE_PARAMETERS_NONE();
  GskPathMeasure *self = PHPGTK_BOXED_SELF(GskPathMeasure);
  GskPath *phpgtk_ret = gsk_path_measure_get_path(self);
  wrap_boxed(GSK_TYPE_PATH, phpgtk_ret, return_value);
}

/**
 * Gtk4\GskPathMeasure::get_point(float $distance): ?GskPathPoint
 *
 * Sets $result to the point at the given distance into the path.
 */
ZEND_METHOD(Gtk4_GskPathMeasure, get_point) {
  double distance;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_DOUBLE(distance)
  ZEND_PARSE_PARAMETERS_END();
  GskPathMeasure *self = PHPGTK_BOXED_SELF(GskPathMeasure);
  GskPathPoint result{};
  if (!gsk_path_measure_get_point(self, static_cast<float>(distance), &result)) RETURN_NULL();
  wrap_boxed(GSK_TYPE_PATH_POINT, &result, return_value);
}

/**
 * Gtk4\GskPathMeasure::get_tolerance(): float
 *
 * Returns the tolerance that the measure was created with.
 */
ZEND_METHOD(Gtk4_GskPathMeasure, get_tolerance) {
  ZEND_PARSE_PARAMETERS_NONE();
  GskPathMeasure *self = PHPGTK_BOXED_SELF(GskPathMeasure);
  RETURN_DOUBLE(gsk_path_measure_get_tolerance(self));
}

namespace phpgtk {
// MINIT: bind the PHP class to GSK_TYPE_PATH_MEASURE with its field table.
void register_GskPathMeasure(zend_class_entry *ce) {
  register_boxed(BoxedClass{
      .type = GSK_TYPE_PATH_MEASURE, .ce = ce, .fields = fields, .read = read, .write = write});
}
}  // namespace phpgtk
