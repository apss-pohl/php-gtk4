// GENERATED by gen/gir.php from Gsk.Renderer - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GskRenderer
#include "php_gtk4.h"
#include "core/object.h"
#include "core/gerror.h"
#include "core/boxed.h"
#include "core/fundamental.h"
#include "core/subtype.h"

using namespace phpgtk;

/**
 * Gtk4\GskRenderer::__construct()
 *
 * GskRenderer is abstract in GTK: `new` only works on a PHP subclass (which gets its own GType).
 */
ZEND_METHOD(Gtk4_GskRenderer, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) == nullptr) {
      zend_throw_error(nullptr,
                       "GskRenderer is abstract in GTK: subclass it in PHP (new MyClass())");
    }
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * static Gtk4\GskRenderer::new_for_surface(GdkSurface $surface): GskRenderer
 *
 * Creates an appropriate `GskRenderer` instance for the given $surface.
 */
ZEND_METHOD(Gtk4_GskRenderer, new_for_surface) {
  zval *surface;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(surface, class_for_gtype(GDK_TYPE_SURFACE))
  ZEND_PARSE_PARAMETERS_END();
  GObject *surface_o = unwrap(surface, GDK_TYPE_SURFACE);
  if (surface_o == nullptr) RETURN_THROWS();
  GObject *obj = G_OBJECT(gsk_renderer_new_for_surface(GDK_SURFACE(surface_o)));
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * Gtk4\GskRenderer::get_surface(): ?GdkSurface
 *
 * Retrieves the `GdkSurface` set using gsk_enderer_realize().
 */
ZEND_METHOD(Gtk4_GskRenderer, get_surface) {
  ZEND_PARSE_PARAMETERS_NONE();
  GskRenderer *self = PHPGTK_SELF(GskRenderer, GSK_TYPE_RENDERER);
  GdkSurface *phpgtk_ret = gsk_renderer_get_surface(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GskRenderer::is_realized(): bool
 *
 * Checks whether the $renderer is realized or not.
 */
ZEND_METHOD(Gtk4_GskRenderer, is_realized) {
  ZEND_PARSE_PARAMETERS_NONE();
  GskRenderer *self = PHPGTK_SELF(GskRenderer, GSK_TYPE_RENDERER);
  RETURN_BOOL(gsk_renderer_is_realized(self));
}

/**
 * Gtk4\GskRenderer::realize(?GdkSurface $surface): bool
 *
 * Creates the resources needed by the $renderer to render the scene graph.
 */
ZEND_METHOD(Gtk4_GskRenderer, realize) {
  zval *surface = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(surface, class_for_gtype(GDK_TYPE_SURFACE))
  ZEND_PARSE_PARAMETERS_END();
  GskRenderer *self = PHPGTK_SELF(GskRenderer, GSK_TYPE_RENDERER);
  GObject *surface_o = nullptr;
  if (surface != nullptr) {
    surface_o = unwrap(surface, GDK_TYPE_SURFACE);
    if (surface_o == nullptr) RETURN_THROWS();
  }
  GError *error = nullptr;
  const gboolean ok =
      gsk_renderer_realize(self, surface_o != nullptr ? GDK_SURFACE(surface_o) : nullptr, &error);
  if (!ok) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  RETURN_BOOL(ok);
}

/**
 * Gtk4\GskRenderer::realize_for_display(GdkDisplay $display): bool
 *
 * Creates the resources needed by the $renderer to render the scene graph.
 */
ZEND_METHOD(Gtk4_GskRenderer, realize_for_display) {
  zval *display;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(display, class_for_gtype(GDK_TYPE_DISPLAY))
  ZEND_PARSE_PARAMETERS_END();
  GskRenderer *self = PHPGTK_SELF(GskRenderer, GSK_TYPE_RENDERER);
  GObject *display_o = unwrap(display, GDK_TYPE_DISPLAY);
  if (display_o == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  const gboolean ok = gsk_renderer_realize_for_display(self, GDK_DISPLAY(display_o), &error);
  if (!ok) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  RETURN_BOOL(ok);
}

/**
 * Gtk4\GskRenderer::render_texture(GskRenderNode $root, ?GrapheneRect $viewport): GdkTexture
 *
 * Renders the scene graph, described by a tree of `GskRenderNode` instances, to a `GdkTexture`.
 */
ZEND_METHOD(Gtk4_GskRenderer, render_texture) {
  zval *root;
  zval *viewport = nullptr;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(root, fundamental_class_for_type(GSK_TYPE_RENDER_NODE)->ce)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(viewport, boxed_class_for_type(GRAPHENE_TYPE_RECT)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GskRenderer *self = PHPGTK_SELF(GskRenderer, GSK_TYPE_RENDERER);
  gpointer root_f = unwrap_fundamental(root, GSK_TYPE_RENDER_NODE);
  if (root_f == nullptr) RETURN_THROWS();
  gpointer viewport_b = nullptr;
  if (viewport != nullptr) {
    viewport_b = unwrap_boxed(viewport, GRAPHENE_TYPE_RECT);
    if (viewport_b == nullptr) RETURN_THROWS();
  }
  GdkTexture *phpgtk_ret = gsk_renderer_render_texture(self, static_cast<GskRenderNode *>(root_f),
                                                       static_cast<graphene_rect_t *>(viewport_b));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * Gtk4\GskRenderer::unrealize(): void
 *
 * Releases all the resources created by gsk_renderer_realize().
 */
ZEND_METHOD(Gtk4_GskRenderer, unrealize) {
  ZEND_PARSE_PARAMETERS_NONE();
  GskRenderer *self = PHPGTK_SELF(GskRenderer, GSK_TYPE_RENDERER);
  gsk_renderer_unrealize(self);
}
