// GENERATED by gen/gir.php from Gsk.ShadowNode - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GskShadowNode (fundamental handle)
#include "php_gtk4.h"
#include "core/fundamental.h"
#include "core/object.h"

using namespace phpgtk;

// __construct()/get_shadow(): a shadow is a PHP list [GdkRGBA, dx, dy, radius]
// (src/Gsk/GskColorStops.h).
#include "Gsk/GskColorStops.h"

#include <vector>

/**
 * Gtk4\GskShadowNode::get_child(): GskRenderNode
 *
 * Retrieves the child `GskRenderNode` of the shadow $node.
 */
ZEND_METHOD(Gtk4_GskShadowNode, get_child) {
  ZEND_PARSE_PARAMETERS_NONE();
  auto *self = static_cast<GskRenderNode *>(fundamental_self(execute_data));
  if (self == nullptr) RETURN_THROWS();
  gpointer phpgtk_ret = gsk_shadow_node_get_child(self);
  wrap_fundamental(GSK_TYPE_RENDER_NODE, phpgtk_ret, return_value);
}

/**
 * Gtk4\GskShadowNode::get_n_shadows(): int
 *
 * Retrieves the number of shadows in the $node.
 */
ZEND_METHOD(Gtk4_GskShadowNode, get_n_shadows) {
  ZEND_PARSE_PARAMETERS_NONE();
  auto *self = static_cast<GskRenderNode *>(fundamental_self(execute_data));
  if (self == nullptr) RETURN_THROWS();
  RETURN_LONG(static_cast<zend_long>(gsk_shadow_node_get_n_shadows(self)));
}

/**
 * public function __construct(GskRenderNode $child, array $shadows)
 * $child drawn with drop shadows behind it: at least one `[GdkRGBA $color, float $dx, float $dy,
 * float $radius]` list, painted in order.
 */
ZEND_METHOD(Gtk4_GskShadowNode, __construct) {
  zval *child;
  zval *shadows;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(child, fundamental_class_for_type(GSK_TYPE_RENDER_NODE)->ce)
  Z_PARAM_ARRAY(shadows)
  ZEND_PARSE_PARAMETERS_END();
  gpointer child_f = unwrap_fundamental(child, GSK_TYPE_RENDER_NODE);
  if (child_f == nullptr) RETURN_THROWS();
  std::vector<GskShadow> list;
  if (!shadows_from_php(shadows, 2, list)) RETURN_THROWS();
  GskRenderNode *self =
      gsk_shadow_node_new(static_cast<GskRenderNode *>(child_f), list.data(), list.size());
  fundamental_adopt(fundamental_from_zval(ZEND_THIS), GSK_TYPE_SHADOW_NODE, self);
}

/**
 * public function get_shadow(int $i): array
 * Shadow number $i (0 to get_n_shadows() - 1) as the `[GdkRGBA $color, float $dx, float $dy,
 * float $radius]` list the constructor took.
 *
 * @return array{GdkRGBA, float, float, float}
 */
ZEND_METHOD(Gtk4_GskShadowNode, get_shadow) {
  zend_long i = 0;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(i)
  ZEND_PARSE_PARAMETERS_END();
  auto *self = static_cast<GskRenderNode *>(fundamental_self(execute_data));
  if (self == nullptr) RETURN_THROWS();
  const gsize n = gsk_shadow_node_get_n_shadows(self);
  if (i < 0 || static_cast<gsize>(i) >= n) {
    zend_argument_value_error(
        1, "must be between 0 and %zu (the shadow count minus one), " ZEND_LONG_FMT " given", n - 1,
        i);
    RETURN_THROWS();
  }
  shadow_to_php(gsk_shadow_node_get_shadow(self, static_cast<gsize>(i)), return_value);
}

namespace {

// gsk_render_node_ref() as the fundamental registry's RefFn.
gpointer fundamental_ref(gpointer p) {
  return gsk_render_node_ref(static_cast<GskRenderNode *>(p));
}

// gsk_render_node_unref() as the fundamental registry's UnrefFn.
void fundamental_unref(gpointer p) {
  gsk_render_node_unref(static_cast<GskRenderNode *>(p));
}

}  // namespace

namespace phpgtk {
// MINIT: bind the PHP class to GSK_TYPE_SHADOW_NODE on the fundamental registry.
void register_GskShadowNode(zend_class_entry *ce) {
  register_fundamental(FundamentalClass{
      .type = GSK_TYPE_SHADOW_NODE, .ce = ce, .ref = fundamental_ref, .unref = fundamental_unref});
}
}  // namespace phpgtk
