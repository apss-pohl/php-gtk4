// GENERATED by gen/gir.php from Gsk.RoundedClipNode - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GskRoundedClipNode (fundamental handle)
#include "php_gtk4.h"
#include "core/fundamental.h"
#include "core/object.h"
#include "core/boxed.h"
#include "Gsk/GskRoundedRectType.h"

using namespace phpgtk;

/**
 * Gtk4\GskRoundedClipNode::__construct(GskRenderNode $child, GskRoundedRect $clip)
 *
 * Creates a `GskRenderNode` that will clip the $child to the area given by $clip.
 */
ZEND_METHOD(Gtk4_GskRoundedClipNode, __construct) {
  zval *child;
  zval *clip;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(child, fundamental_class_for_type(GSK_TYPE_RENDER_NODE)->ce)
  Z_PARAM_OBJECT_OF_CLASS(clip, boxed_class_for_type(PHPGTK_TYPE_GSK_ROUNDED_RECT)->ce)
  ZEND_PARSE_PARAMETERS_END();
  gpointer child_f = unwrap_fundamental(child, GSK_TYPE_RENDER_NODE);
  if (child_f == nullptr) RETURN_THROWS();
  gpointer clip_b = unwrap_boxed(clip, PHPGTK_TYPE_GSK_ROUNDED_RECT);
  if (clip_b == nullptr) RETURN_THROWS();
  gpointer obj = gsk_rounded_clip_node_new(static_cast<GskRenderNode *>(child_f),
                                           static_cast<GskRoundedRect *>(clip_b));
  if (obj == nullptr) {
    zend_throw_error(nullptr, "%s(): GTK refused to create the instance (see the CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  fundamental_adopt(fundamental_from_zval(ZEND_THIS), GSK_TYPE_ROUNDED_CLIP_NODE, obj);
}

/**
 * Gtk4\GskRoundedClipNode::get_child(): GskRenderNode
 *
 * Gets the child node that is getting clipped by the given $node.
 */
ZEND_METHOD(Gtk4_GskRoundedClipNode, get_child) {
  ZEND_PARSE_PARAMETERS_NONE();
  auto *self = static_cast<GskRenderNode *>(fundamental_self(execute_data));
  if (self == nullptr) RETURN_THROWS();
  gpointer phpgtk_ret = gsk_rounded_clip_node_get_child(self);
  wrap_fundamental(GSK_TYPE_RENDER_NODE, phpgtk_ret, return_value);
}

/**
 * Gtk4\GskRoundedClipNode::get_clip(): GskRoundedRect
 *
 * Retrieves the rounded rectangle used to clip the contents of the $node.
 */
ZEND_METHOD(Gtk4_GskRoundedClipNode, get_clip) {
  ZEND_PARSE_PARAMETERS_NONE();
  auto *self = static_cast<GskRenderNode *>(fundamental_self(execute_data));
  if (self == nullptr) RETURN_THROWS();
  const GskRoundedRect *phpgtk_ret = gsk_rounded_clip_node_get_clip(self);
  wrap_boxed(PHPGTK_TYPE_GSK_ROUNDED_RECT, phpgtk_ret, return_value);
}

namespace {

// gsk_render_node_ref() as the fundamental registry's RefFn.
gpointer fundamental_ref(gpointer p) {
  return gsk_render_node_ref(static_cast<GskRenderNode *>(p));
}

// gsk_render_node_unref() as the fundamental registry's UnrefFn.
void fundamental_unref(gpointer p) {
  gsk_render_node_unref(static_cast<GskRenderNode *>(p));
}

}  // namespace

namespace phpgtk {
// MINIT: bind the PHP class to GSK_TYPE_ROUNDED_CLIP_NODE on the fundamental registry.
void register_GskRoundedClipNode(zend_class_entry *ce) {
  register_fundamental(FundamentalClass{.type = GSK_TYPE_ROUNDED_CLIP_NODE,
                                        .ce = ce,
                                        .ref = fundamental_ref,
                                        .unref = fundamental_unref});
}
}  // namespace phpgtk
