// GENERATED by gen/gir.php from Gsk.MaskNode - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GskMaskNode (fundamental handle)
#include "php_gtk4.h"
#include "core/fundamental.h"
#include "core/object.h"
#include "core/enums.h"

using namespace phpgtk;

/**
 * Gtk4\GskMaskNode::__construct(GskRenderNode $source, GskRenderNode $mask, GskMaskMode $mask_mode)
 *
 * Creates a `GskRenderNode` that will mask a given node by another.
 */
ZEND_METHOD(Gtk4_GskMaskNode, __construct) {
  zval *source;
  zval *mask;
  zval *mask_mode;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_OBJECT_OF_CLASS(source, fundamental_class_for_type(GSK_TYPE_RENDER_NODE)->ce)
  Z_PARAM_OBJECT_OF_CLASS(mask, fundamental_class_for_type(GSK_TYPE_RENDER_NODE)->ce)
  Z_PARAM_OBJECT_OF_CLASS(mask_mode, enum_class_for_type(GSK_TYPE_MASK_MODE))
  ZEND_PARSE_PARAMETERS_END();
  gpointer source_f = unwrap_fundamental(source, GSK_TYPE_RENDER_NODE);
  if (source_f == nullptr) RETURN_THROWS();
  gpointer mask_f = unwrap_fundamental(mask, GSK_TYPE_RENDER_NODE);
  if (mask_f == nullptr) RETURN_THROWS();
  gint mask_mode_v = 0;
  if (!enum_from_php(mask_mode, GSK_TYPE_MASK_MODE, &mask_mode_v)) RETURN_THROWS();
  gpointer obj = gsk_mask_node_new(static_cast<GskRenderNode *>(source_f),
                                   static_cast<GskRenderNode *>(mask_f),
                                   static_cast<GskMaskMode>(mask_mode_v));
  if (obj == nullptr) {
    zend_throw_error(nullptr, "%s(): GTK refused to create the instance (see the CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  fundamental_adopt(fundamental_from_zval(ZEND_THIS), GSK_TYPE_MASK_NODE, obj);
}

/**
 * Gtk4\GskMaskNode::get_mask(): GskRenderNode
 *
 * Retrieves the mask `GskRenderNode` child of the $node.
 */
ZEND_METHOD(Gtk4_GskMaskNode, get_mask) {
  ZEND_PARSE_PARAMETERS_NONE();
  auto *self = static_cast<GskRenderNode *>(fundamental_self(execute_data));
  if (self == nullptr) RETURN_THROWS();
  gpointer phpgtk_ret = gsk_mask_node_get_mask(self);
  wrap_fundamental(GSK_TYPE_RENDER_NODE, phpgtk_ret, return_value);
}

/**
 * Gtk4\GskMaskNode::get_mask_mode(): GskMaskMode
 *
 * Retrieves the mask mode used by $node.
 */
ZEND_METHOD(Gtk4_GskMaskNode, get_mask_mode) {
  ZEND_PARSE_PARAMETERS_NONE();
  auto *self = static_cast<GskRenderNode *>(fundamental_self(execute_data));
  if (self == nullptr) RETURN_THROWS();
  enum_to_php(GSK_TYPE_MASK_MODE, gsk_mask_node_get_mask_mode(self), return_value);
}

/**
 * Gtk4\GskMaskNode::get_source(): GskRenderNode
 *
 * Retrieves the source `GskRenderNode` child of the $node.
 */
ZEND_METHOD(Gtk4_GskMaskNode, get_source) {
  ZEND_PARSE_PARAMETERS_NONE();
  auto *self = static_cast<GskRenderNode *>(fundamental_self(execute_data));
  if (self == nullptr) RETURN_THROWS();
  gpointer phpgtk_ret = gsk_mask_node_get_source(self);
  wrap_fundamental(GSK_TYPE_RENDER_NODE, phpgtk_ret, return_value);
}

namespace {

// gsk_render_node_ref() as the fundamental registry's RefFn.
gpointer fundamental_ref(gpointer p) {
  return gsk_render_node_ref(static_cast<GskRenderNode *>(p));
}

// gsk_render_node_unref() as the fundamental registry's UnrefFn.
void fundamental_unref(gpointer p) {
  gsk_render_node_unref(static_cast<GskRenderNode *>(p));
}

}  // namespace

namespace phpgtk {
// MINIT: bind the PHP class to GSK_TYPE_MASK_NODE on the fundamental registry.
void register_GskMaskNode(zend_class_entry *ce) {
  register_fundamental(FundamentalClass{
      .type = GSK_TYPE_MASK_NODE, .ce = ce, .ref = fundamental_ref, .unref = fundamental_unref});
}
}  // namespace phpgtk
