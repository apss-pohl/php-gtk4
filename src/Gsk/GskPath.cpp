// GENERATED by gen/gir.php from Gsk.Path - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GskPath (boxed value type)
#include "php_gtk4.h"
#include "core/boxed.h"
#include "core/object.h"
#include <cstring>
#include "core/enums.h"
#include "core/fundamental.h"
#include <cairo-gobject.h>

using namespace phpgtk;

namespace {

const char *const fields[] = {nullptr};

// Boxed field reader: the public scalar fields as PHP properties.
bool read(gpointer data, const char *field, zval *rv) {
  (void)data;
  (void)field;
  (void)rv;
  return false;
}

// Boxed field writer: each field converts like a parameter of its type (core/boxed).
bool write(gpointer data, const char *field, zval *v) {
  (void)data;
  (void)field;
  (void)v;
  return false;
}

}  // namespace

/**
 * Gtk4\GskPath::__construct()
 *
 * GskPath values come from GTK, never from `new`.
 */
ZEND_METHOD(Gtk4_GskPath, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
}

/**
 * Gtk4\GskPath::get_bounds(): ?GrapheneRect
 *
 * Computes the bounds of the given path.
 */
ZEND_METHOD(Gtk4_GskPath, get_bounds) {
  ZEND_PARSE_PARAMETERS_NONE();
  GskPath *self = PHPGTK_BOXED_SELF(GskPath);
  graphene_rect_t bounds{};
  if (!gsk_path_get_bounds(self, &bounds)) RETURN_NULL();
  wrap_boxed(GRAPHENE_TYPE_RECT, &bounds, return_value);
}

/**
 * Gtk4\GskPath::get_closest_point(GraphenePoint $point, float $threshold): ?array
 *
 * Computes the closest point on the path to the given point and sets the $result to it.
 */
ZEND_METHOD(Gtk4_GskPath, get_closest_point) {
  zval *point;
  double threshold;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(point, boxed_class_for_type(GRAPHENE_TYPE_POINT)->ce)
  Z_PARAM_DOUBLE(threshold)
  ZEND_PARSE_PARAMETERS_END();
  GskPath *self = PHPGTK_BOXED_SELF(GskPath);
  gpointer point_b = unwrap_boxed(point, GRAPHENE_TYPE_POINT);
  if (point_b == nullptr) RETURN_THROWS();
  if (!phpgtk::check_domain_double(threshold, 0.0, HUGE_VAL, 2)) RETURN_THROWS();
  GskPathPoint result{};
  float distance = 0;
  if (!gsk_path_get_closest_point(self, static_cast<graphene_point_t *>(point_b),
                                  static_cast<float>(threshold), &result, &distance))
    RETURN_NULL();
  array_init_size(return_value, 2);
  {
    zval item;
    wrap_boxed(GSK_TYPE_PATH_POINT, &result, &item);
    add_next_index_zval(return_value, &item);
  }
  {
    zval item;
    ZVAL_DOUBLE(&item, distance);
    add_next_index_zval(return_value, &item);
  }
}

/**
 * Gtk4\GskPath::get_end_point(): ?GskPathPoint
 *
 * Gets the end point of the path.
 */
ZEND_METHOD(Gtk4_GskPath, get_end_point) {
  ZEND_PARSE_PARAMETERS_NONE();
  GskPath *self = PHPGTK_BOXED_SELF(GskPath);
  GskPathPoint result{};
  if (!gsk_path_get_end_point(self, &result)) RETURN_NULL();
  wrap_boxed(GSK_TYPE_PATH_POINT, &result, return_value);
}

/**
 * Gtk4\GskPath::get_start_point(): ?GskPathPoint
 *
 * Gets the start point of the path.
 */
ZEND_METHOD(Gtk4_GskPath, get_start_point) {
  ZEND_PARSE_PARAMETERS_NONE();
  GskPath *self = PHPGTK_BOXED_SELF(GskPath);
  GskPathPoint result{};
  if (!gsk_path_get_start_point(self, &result)) RETURN_NULL();
  wrap_boxed(GSK_TYPE_PATH_POINT, &result, return_value);
}

/**
 * Gtk4\GskPath::get_stroke_bounds(GskStroke $stroke): ?GrapheneRect
 *
 * Computes the bounds for stroking the given path with the parameters in $stroke.
 */
ZEND_METHOD(Gtk4_GskPath, get_stroke_bounds) {
  zval *stroke;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(stroke, boxed_class_for_type(GSK_TYPE_STROKE)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GskPath *self = PHPGTK_BOXED_SELF(GskPath);
  gpointer stroke_b = unwrap_boxed(stroke, GSK_TYPE_STROKE);
  if (stroke_b == nullptr) RETURN_THROWS();
  graphene_rect_t bounds{};
  if (!gsk_path_get_stroke_bounds(self, static_cast<GskStroke *>(stroke_b), &bounds)) RETURN_NULL();
  wrap_boxed(GRAPHENE_TYPE_RECT, &bounds, return_value);
}

/**
 * Gtk4\GskPath::in_fill(GraphenePoint $point, GskFillRule $fill_rule): bool
 *
 * Returns whether the given point is inside the area that would be affected if the path was filled
 * according to $fill_rule.
 */
ZEND_METHOD(Gtk4_GskPath, in_fill) {
  zval *point;
  zval *fill_rule;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(point, boxed_class_for_type(GRAPHENE_TYPE_POINT)->ce)
  Z_PARAM_OBJECT_OF_CLASS(fill_rule, enum_class_for_type(GSK_TYPE_FILL_RULE))
  ZEND_PARSE_PARAMETERS_END();
  GskPath *self = PHPGTK_BOXED_SELF(GskPath);
  gpointer point_b = unwrap_boxed(point, GRAPHENE_TYPE_POINT);
  if (point_b == nullptr) RETURN_THROWS();
  gint fill_rule_v = 0;
  if (!enum_from_php(fill_rule, GSK_TYPE_FILL_RULE, &fill_rule_v)) RETURN_THROWS();
  RETURN_BOOL(gsk_path_in_fill(self, static_cast<graphene_point_t *>(point_b),
                               static_cast<GskFillRule>(fill_rule_v)));
}

/**
 * Gtk4\GskPath::is_closed(): bool
 *
 * Returns if the path represents a single closed contour.
 */
ZEND_METHOD(Gtk4_GskPath, is_closed) {
  ZEND_PARSE_PARAMETERS_NONE();
  GskPath *self = PHPGTK_BOXED_SELF(GskPath);
  RETURN_BOOL(gsk_path_is_closed(self));
}

/**
 * Gtk4\GskPath::is_empty(): bool
 *
 * Checks if the path is empty, i.e. contains no lines or curves.
 */
ZEND_METHOD(Gtk4_GskPath, is_empty) {
  ZEND_PARSE_PARAMETERS_NONE();
  GskPath *self = PHPGTK_BOXED_SELF(GskPath);
  RETURN_BOOL(gsk_path_is_empty(self));
}

/**
 * Gtk4\GskPath::to_cairo(CairoContext $cr): void
 *
 * Appends the given $path to the given cairo context for drawing with Cairo.
 */
ZEND_METHOD(Gtk4_GskPath, to_cairo) {
  zval *cr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(cr, fundamental_class_for_type(CAIRO_GOBJECT_TYPE_CONTEXT)->ce)
  ZEND_PARSE_PARAMETERS_END();
  GskPath *self = PHPGTK_BOXED_SELF(GskPath);
  gpointer cr_f = unwrap_fundamental(cr, CAIRO_GOBJECT_TYPE_CONTEXT);
  if (cr_f == nullptr) RETURN_THROWS();
  gsk_path_to_cairo(self, static_cast<cairo_t *>(cr_f));
}

/**
 * Gtk4\GskPath::to_string(): string
 *
 * Converts the path into a string that is suitable for printing.
 */
ZEND_METHOD(Gtk4_GskPath, to_string) {
  ZEND_PARSE_PARAMETERS_NONE();
  GskPath *self = PHPGTK_BOXED_SELF(GskPath);
  char *phpgtk_ret = gsk_path_to_string(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETVAL_STRING(phpgtk_ret);
  g_free(phpgtk_ret);
}

/**
 * static Gtk4\GskPath::parse(string $string): ?GskPath
 *
 * This is a convenience function that constructs a `GskPath` from a serialized form.
 */
ZEND_METHOD(Gtk4_GskPath, parse) {
  zend_string *string;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(string)
  ZEND_PARSE_PARAMETERS_END();
  if (!phpgtk::check_utf8(string, 1)) RETURN_THROWS();
  GskPath *phpgtk_ret = gsk_path_parse(ZSTR_VAL(string));
  wrap_boxed(GSK_TYPE_PATH, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(GSK_TYPE_PATH, phpgtk_ret);
}

namespace phpgtk {
// MINIT: bind the PHP class to GSK_TYPE_PATH with its field table.
void register_GskPath(zend_class_entry *ce) {
  register_boxed(
      BoxedClass{.type = GSK_TYPE_PATH, .ce = ce, .fields = fields, .read = read, .write = write});
}
}  // namespace phpgtk
