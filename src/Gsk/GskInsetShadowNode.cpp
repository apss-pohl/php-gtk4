// GENERATED by gen/gir.php from Gsk.InsetShadowNode - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GskInsetShadowNode (fundamental handle)
#include "php_gtk4.h"
#include "core/fundamental.h"
#include "core/object.h"
#include "core/boxed.h"
#include "Gsk/GskRoundedRectType.h"

using namespace phpgtk;

/**
 * Gtk4\GskInsetShadowNode::__construct(GskRoundedRect $outline, GdkRGBA $color, float $dx, float
 * $dy, float $spread, float $blur_radius)
 *
 * Creates a `GskRenderNode` that will render an inset shadow into the box given by $outline.
 */
ZEND_METHOD(Gtk4_GskInsetShadowNode, __construct) {
  zval *outline;
  zval *color;
  double dx;
  double dy;
  double spread;
  double blur_radius;
  ZEND_PARSE_PARAMETERS_START(6, 6)
  Z_PARAM_OBJECT_OF_CLASS(outline, boxed_class_for_type(PHPGTK_TYPE_GSK_ROUNDED_RECT)->ce)
  Z_PARAM_OBJECT_OF_CLASS(color, boxed_class_for_type(GDK_TYPE_RGBA)->ce)
  Z_PARAM_DOUBLE(dx)
  Z_PARAM_DOUBLE(dy)
  Z_PARAM_DOUBLE(spread)
  Z_PARAM_DOUBLE(blur_radius)
  ZEND_PARSE_PARAMETERS_END();
  gpointer outline_b = unwrap_boxed(outline, PHPGTK_TYPE_GSK_ROUNDED_RECT);
  if (outline_b == nullptr) RETURN_THROWS();
  gpointer color_b = unwrap_boxed(color, GDK_TYPE_RGBA);
  if (color_b == nullptr) RETURN_THROWS();
  gpointer obj = gsk_inset_shadow_node_new(static_cast<GskRoundedRect *>(outline_b),
                                           static_cast<GdkRGBA *>(color_b), static_cast<float>(dx),
                                           static_cast<float>(dy), static_cast<float>(spread),
                                           static_cast<float>(blur_radius));
  if (obj == nullptr) {
    zend_throw_error(nullptr, "%s(): GTK refused to create the instance (see the CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  fundamental_adopt(fundamental_from_zval(ZEND_THIS), GSK_TYPE_INSET_SHADOW_NODE, obj);
}

/**
 * Gtk4\GskInsetShadowNode::get_blur_radius(): float
 *
 * Retrieves the blur radius to apply to the shadow.
 */
ZEND_METHOD(Gtk4_GskInsetShadowNode, get_blur_radius) {
  ZEND_PARSE_PARAMETERS_NONE();
  auto *self = static_cast<GskRenderNode *>(fundamental_self(execute_data));
  if (self == nullptr) RETURN_THROWS();
  RETURN_DOUBLE(gsk_inset_shadow_node_get_blur_radius(self));
}

/**
 * Gtk4\GskInsetShadowNode::get_color(): GdkRGBA
 *
 * Retrieves the color of the inset shadow.
 */
ZEND_METHOD(Gtk4_GskInsetShadowNode, get_color) {
  ZEND_PARSE_PARAMETERS_NONE();
  auto *self = static_cast<GskRenderNode *>(fundamental_self(execute_data));
  if (self == nullptr) RETURN_THROWS();
  const GdkRGBA *phpgtk_ret = gsk_inset_shadow_node_get_color(self);
  wrap_boxed(GDK_TYPE_RGBA, phpgtk_ret, return_value);
}

/**
 * Gtk4\GskInsetShadowNode::get_dx(): float
 *
 * Retrieves the horizontal offset of the inset shadow.
 */
ZEND_METHOD(Gtk4_GskInsetShadowNode, get_dx) {
  ZEND_PARSE_PARAMETERS_NONE();
  auto *self = static_cast<GskRenderNode *>(fundamental_self(execute_data));
  if (self == nullptr) RETURN_THROWS();
  RETURN_DOUBLE(gsk_inset_shadow_node_get_dx(self));
}

/**
 * Gtk4\GskInsetShadowNode::get_dy(): float
 *
 * Retrieves the vertical offset of the inset shadow.
 */
ZEND_METHOD(Gtk4_GskInsetShadowNode, get_dy) {
  ZEND_PARSE_PARAMETERS_NONE();
  auto *self = static_cast<GskRenderNode *>(fundamental_self(execute_data));
  if (self == nullptr) RETURN_THROWS();
  RETURN_DOUBLE(gsk_inset_shadow_node_get_dy(self));
}

/**
 * Gtk4\GskInsetShadowNode::get_outline(): GskRoundedRect
 *
 * Retrieves the outline rectangle of the inset shadow.
 */
ZEND_METHOD(Gtk4_GskInsetShadowNode, get_outline) {
  ZEND_PARSE_PARAMETERS_NONE();
  auto *self = static_cast<GskRenderNode *>(fundamental_self(execute_data));
  if (self == nullptr) RETURN_THROWS();
  const GskRoundedRect *phpgtk_ret = gsk_inset_shadow_node_get_outline(self);
  wrap_boxed(PHPGTK_TYPE_GSK_ROUNDED_RECT, phpgtk_ret, return_value);
}

/**
 * Gtk4\GskInsetShadowNode::get_spread(): float
 *
 * Retrieves how much the shadow spreads inwards.
 */
ZEND_METHOD(Gtk4_GskInsetShadowNode, get_spread) {
  ZEND_PARSE_PARAMETERS_NONE();
  auto *self = static_cast<GskRenderNode *>(fundamental_self(execute_data));
  if (self == nullptr) RETURN_THROWS();
  RETURN_DOUBLE(gsk_inset_shadow_node_get_spread(self));
}

namespace {

// gsk_render_node_ref() as the fundamental registry's RefFn.
gpointer fundamental_ref(gpointer p) {
  return gsk_render_node_ref(static_cast<GskRenderNode *>(p));
}

// gsk_render_node_unref() as the fundamental registry's UnrefFn.
void fundamental_unref(gpointer p) {
  gsk_render_node_unref(static_cast<GskRenderNode *>(p));
}

}  // namespace

namespace phpgtk {
// MINIT: bind the PHP class to GSK_TYPE_INSET_SHADOW_NODE on the fundamental registry.
void register_GskInsetShadowNode(zend_class_entry *ce) {
  register_fundamental(FundamentalClass{.type = GSK_TYPE_INSET_SHADOW_NODE,
                                        .ce = ce,
                                        .ref = fundamental_ref,
                                        .unref = fundamental_unref});
}
}  // namespace phpgtk
