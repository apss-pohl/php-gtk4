// GENERATED by gen/gir.php from Gsk.ColorNode - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GskColorNode (fundamental handle)
#include "php_gtk4.h"
#include "core/fundamental.h"
#include "core/object.h"
#include "core/boxed.h"

using namespace phpgtk;

/**
 * Gtk4\GskColorNode::__construct(GdkRGBA $rgba, GrapheneRect $bounds)
 *
 * Creates a `GskRenderNode` that will render the color specified by $rgba into the area given by
 * $bounds.
 */
ZEND_METHOD(Gtk4_GskColorNode, __construct) {
  zval *rgba;
  zval *bounds;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(rgba, boxed_class_for_type(GDK_TYPE_RGBA)->ce)
  Z_PARAM_OBJECT_OF_CLASS(bounds, boxed_class_for_type(GRAPHENE_TYPE_RECT)->ce)
  ZEND_PARSE_PARAMETERS_END();
  gpointer rgba_b = unwrap_boxed(rgba, GDK_TYPE_RGBA);
  if (rgba_b == nullptr) RETURN_THROWS();
  gpointer bounds_b = unwrap_boxed(bounds, GRAPHENE_TYPE_RECT);
  if (bounds_b == nullptr) RETURN_THROWS();
  gpointer obj =
      gsk_color_node_new(static_cast<GdkRGBA *>(rgba_b), static_cast<graphene_rect_t *>(bounds_b));
  if (obj == nullptr) {
    zend_throw_error(nullptr, "%s(): GTK refused to create the instance (see the CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  fundamental_adopt(fundamental_from_zval(ZEND_THIS), GSK_TYPE_COLOR_NODE, obj);
}

/**
 * Gtk4\GskColorNode::get_color(): GdkRGBA
 *
 * Retrieves the color of the given $node.
 */
ZEND_METHOD(Gtk4_GskColorNode, get_color) {
  ZEND_PARSE_PARAMETERS_NONE();
  auto *self = static_cast<GskRenderNode *>(fundamental_self(execute_data));
  if (self == nullptr) RETURN_THROWS();
  const GdkRGBA *phpgtk_ret = gsk_color_node_get_color(self);
  wrap_boxed(GDK_TYPE_RGBA, phpgtk_ret, return_value);
}

namespace {

// gsk_render_node_ref() as the fundamental registry's RefFn.
gpointer fundamental_ref(gpointer p) {
  return gsk_render_node_ref(static_cast<GskRenderNode *>(p));
}

// gsk_render_node_unref() as the fundamental registry's UnrefFn.
void fundamental_unref(gpointer p) {
  gsk_render_node_unref(static_cast<GskRenderNode *>(p));
}

}  // namespace

namespace phpgtk {
// MINIT: bind the PHP class to GSK_TYPE_COLOR_NODE on the fundamental registry.
void register_GskColorNode(zend_class_entry *ce) {
  register_fundamental(FundamentalClass{
      .type = GSK_TYPE_COLOR_NODE, .ce = ce, .ref = fundamental_ref, .unref = fundamental_unref});
}
}  // namespace phpgtk
