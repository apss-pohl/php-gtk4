// GENERATED by gen/gir.php from Gsk.StrokeNode - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GskStrokeNode (fundamental handle)
#include "php_gtk4.h"
#include "core/fundamental.h"
#include "core/object.h"
#include "core/boxed.h"

using namespace phpgtk;

/**
 * Gtk4\GskStrokeNode::__construct(GskRenderNode $child, GskPath $path, GskStroke $stroke)
 *
 * Creates a #GskRenderNode that will fill the outline generated by stroking the given $path using
 * the attributes defined in $stroke.
 */
ZEND_METHOD(Gtk4_GskStrokeNode, __construct) {
  zval *child;
  zval *path;
  zval *stroke;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_OBJECT_OF_CLASS(child, fundamental_class_for_type(GSK_TYPE_RENDER_NODE)->ce)
  Z_PARAM_OBJECT_OF_CLASS(path, boxed_class_for_type(GSK_TYPE_PATH)->ce)
  Z_PARAM_OBJECT_OF_CLASS(stroke, boxed_class_for_type(GSK_TYPE_STROKE)->ce)
  ZEND_PARSE_PARAMETERS_END();
  gpointer child_f = unwrap_fundamental(child, GSK_TYPE_RENDER_NODE);
  if (child_f == nullptr) RETURN_THROWS();
  gpointer path_b = unwrap_boxed(path, GSK_TYPE_PATH);
  if (path_b == nullptr) RETURN_THROWS();
  gpointer stroke_b = unwrap_boxed(stroke, GSK_TYPE_STROKE);
  if (stroke_b == nullptr) RETURN_THROWS();
  gpointer obj =
      gsk_stroke_node_new(static_cast<GskRenderNode *>(child_f), static_cast<GskPath *>(path_b),
                          static_cast<GskStroke *>(stroke_b));
  if (obj == nullptr) {
    zend_throw_error(nullptr, "%s(): GTK refused to create the instance (see the CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  fundamental_adopt(fundamental_from_zval(ZEND_THIS), GSK_TYPE_STROKE_NODE, obj);
}

/**
 * Gtk4\GskStrokeNode::get_child(): GskRenderNode
 *
 * Gets the child node that is getting drawn by the given $node.
 */
ZEND_METHOD(Gtk4_GskStrokeNode, get_child) {
  ZEND_PARSE_PARAMETERS_NONE();
  auto *self = static_cast<GskRenderNode *>(fundamental_self(execute_data));
  if (self == nullptr) RETURN_THROWS();
  gpointer phpgtk_ret = gsk_stroke_node_get_child(self);
  wrap_fundamental(GSK_TYPE_RENDER_NODE, phpgtk_ret, return_value);
}

/**
 * Gtk4\GskStrokeNode::get_path(): GskPath
 *
 * Retrieves the path that will be stroked with the contents of the $node.
 */
ZEND_METHOD(Gtk4_GskStrokeNode, get_path) {
  ZEND_PARSE_PARAMETERS_NONE();
  auto *self = static_cast<GskRenderNode *>(fundamental_self(execute_data));
  if (self == nullptr) RETURN_THROWS();
  GskPath *phpgtk_ret = gsk_stroke_node_get_path(self);
  wrap_boxed(GSK_TYPE_PATH, phpgtk_ret, return_value);
}

/**
 * Gtk4\GskStrokeNode::get_stroke(): GskStroke
 *
 * Retrieves the stroke attributes used in this $node.
 */
ZEND_METHOD(Gtk4_GskStrokeNode, get_stroke) {
  ZEND_PARSE_PARAMETERS_NONE();
  auto *self = static_cast<GskRenderNode *>(fundamental_self(execute_data));
  if (self == nullptr) RETURN_THROWS();
  const GskStroke *phpgtk_ret = gsk_stroke_node_get_stroke(self);
  wrap_boxed(GSK_TYPE_STROKE, phpgtk_ret, return_value);
}

namespace {

// gsk_render_node_ref() as the fundamental registry's RefFn.
gpointer fundamental_ref(gpointer p) {
  return gsk_render_node_ref(static_cast<GskRenderNode *>(p));
}

// gsk_render_node_unref() as the fundamental registry's UnrefFn.
void fundamental_unref(gpointer p) {
  gsk_render_node_unref(static_cast<GskRenderNode *>(p));
}

}  // namespace

namespace phpgtk {
// MINIT: bind the PHP class to GSK_TYPE_STROKE_NODE on the fundamental registry.
void register_GskStrokeNode(zend_class_entry *ce) {
  register_fundamental(FundamentalClass{
      .type = GSK_TYPE_STROKE_NODE, .ce = ce, .ref = fundamental_ref, .unref = fundamental_unref});
}
}  // namespace phpgtk
