// GENERATED by gen/gir.php from Gsk.RepeatingRadialGradientNode - do not edit (gen/overrides,
// gen/skip.txt). Gtk4\GskRepeatingRadialGradientNode (fundamental handle)
#include "php_gtk4.h"
#include "core/fundamental.h"
#include "core/object.h"
#include "core/boxed.h"

using namespace phpgtk;

// The colour stops come as a PHP list of [offset, GdkRGBA] pairs (src/Gsk/GskColorStops.h).
#include "Gsk/GskColorStops.h"

#include <vector>

/**
 * public function __construct(GrapheneRect $bounds, GraphenePoint $center, float $hradius, float
 * $vradius, float $start, float $end, array $color_stops) A radial gradient around $center,
 * repeated over $bounds: an ellipse of $hradius by $vradius, painted from $start to $end (fractions
 * of the radii, 0.0 to 1.0), through $color_stops - at least two
 * `[float $offset, GdkRGBA $color]` pairs with offsets ascending from 0.0 to 1.0.
 */
ZEND_METHOD(Gtk4_GskRepeatingRadialGradientNode, __construct) {
  zval *bounds;
  zval *center;
  double hradius = 0;
  double vradius = 0;
  double start = 0;
  double end = 0;
  zval *stops;
  ZEND_PARSE_PARAMETERS_START(7, 7)
  Z_PARAM_OBJECT_OF_CLASS(bounds, boxed_class_for_type(GRAPHENE_TYPE_RECT)->ce)
  Z_PARAM_OBJECT_OF_CLASS(center, boxed_class_for_type(GRAPHENE_TYPE_POINT)->ce)
  Z_PARAM_DOUBLE(hradius)
  Z_PARAM_DOUBLE(vradius)
  Z_PARAM_DOUBLE(start)
  Z_PARAM_DOUBLE(end)
  Z_PARAM_ARRAY(stops)
  ZEND_PARSE_PARAMETERS_END();
  // gsk_repeating_radial_gradient_node_new()'s own preconditions, as ValueErrors
  if (!phpgtk::check_domain_above(hradius, 0.0, 3)) RETURN_THROWS();
  if (!phpgtk::check_domain_above(vradius, 0.0, 4)) RETURN_THROWS();
  if (!phpgtk::check_domain_double(start, 0.0, HUGE_VAL, 5)) RETURN_THROWS();
  if (!phpgtk::check_domain_double(end, 0.0, HUGE_VAL, 6)) RETURN_THROWS();
  std::vector<GskColorStop> cs;
  if (!color_stops_from_php(stops, 7, cs)) RETURN_THROWS();
  GskRenderNode *self = gsk_repeating_radial_gradient_node_new(
      static_cast<graphene_rect_t *>(unwrap_boxed(bounds, GRAPHENE_TYPE_RECT)),
      static_cast<graphene_point_t *>(unwrap_boxed(center, GRAPHENE_TYPE_POINT)),
      static_cast<float>(hradius), static_cast<float>(vradius), static_cast<float>(start),
      static_cast<float>(end), cs.data(), cs.size());
  fundamental_adopt(fundamental_from_zval(ZEND_THIS), GSK_TYPE_REPEATING_RADIAL_GRADIENT_NODE,
                    self);
}

namespace {

// gsk_render_node_ref() as the fundamental registry's RefFn.
gpointer fundamental_ref(gpointer p) {
  return gsk_render_node_ref(static_cast<GskRenderNode *>(p));
}

// gsk_render_node_unref() as the fundamental registry's UnrefFn.
void fundamental_unref(gpointer p) {
  gsk_render_node_unref(static_cast<GskRenderNode *>(p));
}

}  // namespace

namespace phpgtk {
// MINIT: bind the PHP class to GSK_TYPE_REPEATING_RADIAL_GRADIENT_NODE on the fundamental registry.
void register_GskRepeatingRadialGradientNode(zend_class_entry *ce) {
  register_fundamental(FundamentalClass{.type = GSK_TYPE_REPEATING_RADIAL_GRADIENT_NODE,
                                        .ce = ce,
                                        .ref = fundamental_ref,
                                        .unref = fundamental_unref});
}
}  // namespace phpgtk
