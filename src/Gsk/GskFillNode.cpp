// GENERATED by gen/gir.php from Gsk.FillNode - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GskFillNode (fundamental handle)
#include "php_gtk4.h"
#include "core/fundamental.h"
#include "core/object.h"
#include "core/enums.h"
#include "core/boxed.h"

using namespace phpgtk;

/**
 * Gtk4\GskFillNode::__construct(GskRenderNode $child, GskPath $path, GskFillRule $fill_rule)
 *
 * Creates a `GskRenderNode` that will fill the $child in the area given by $path and $fill_rule.
 */
ZEND_METHOD(Gtk4_GskFillNode, __construct) {
  zval *child;
  zval *path;
  zval *fill_rule;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_OBJECT_OF_CLASS(child, fundamental_class_for_type(GSK_TYPE_RENDER_NODE)->ce)
  Z_PARAM_OBJECT_OF_CLASS(path, boxed_class_for_type(GSK_TYPE_PATH)->ce)
  Z_PARAM_OBJECT_OF_CLASS(fill_rule, enum_class_for_type(GSK_TYPE_FILL_RULE))
  ZEND_PARSE_PARAMETERS_END();
  gpointer child_f = unwrap_fundamental(child, GSK_TYPE_RENDER_NODE);
  if (child_f == nullptr) RETURN_THROWS();
  gpointer path_b = unwrap_boxed(path, GSK_TYPE_PATH);
  if (path_b == nullptr) RETURN_THROWS();
  gint fill_rule_v = 0;
  if (!enum_from_php(fill_rule, GSK_TYPE_FILL_RULE, &fill_rule_v)) RETURN_THROWS();
  gpointer obj =
      gsk_fill_node_new(static_cast<GskRenderNode *>(child_f), static_cast<GskPath *>(path_b),
                        static_cast<GskFillRule>(fill_rule_v));
  if (obj == nullptr) {
    zend_throw_error(nullptr, "%s(): GTK refused to create the instance (see the CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  fundamental_adopt(fundamental_from_zval(ZEND_THIS), GSK_TYPE_FILL_NODE, obj);
}

/**
 * Gtk4\GskFillNode::get_child(): GskRenderNode
 *
 * Gets the child node that is getting drawn by the given $node.
 */
ZEND_METHOD(Gtk4_GskFillNode, get_child) {
  ZEND_PARSE_PARAMETERS_NONE();
  auto *self = static_cast<GskRenderNode *>(fundamental_self(execute_data));
  if (self == nullptr) RETURN_THROWS();
  gpointer phpgtk_ret = gsk_fill_node_get_child(self);
  wrap_fundamental(GSK_TYPE_RENDER_NODE, phpgtk_ret, return_value);
}

/**
 * Gtk4\GskFillNode::get_fill_rule(): GskFillRule
 *
 * Retrieves the fill rule used to determine how the path is filled.
 */
ZEND_METHOD(Gtk4_GskFillNode, get_fill_rule) {
  ZEND_PARSE_PARAMETERS_NONE();
  auto *self = static_cast<GskRenderNode *>(fundamental_self(execute_data));
  if (self == nullptr) RETURN_THROWS();
  enum_to_php(GSK_TYPE_FILL_RULE, gsk_fill_node_get_fill_rule(self), return_value);
}

/**
 * Gtk4\GskFillNode::get_path(): GskPath
 *
 * Retrieves the path used to describe the area filled with the contents of the $node.
 */
ZEND_METHOD(Gtk4_GskFillNode, get_path) {
  ZEND_PARSE_PARAMETERS_NONE();
  auto *self = static_cast<GskRenderNode *>(fundamental_self(execute_data));
  if (self == nullptr) RETURN_THROWS();
  GskPath *phpgtk_ret = gsk_fill_node_get_path(self);
  wrap_boxed(GSK_TYPE_PATH, phpgtk_ret, return_value);
}

namespace {

// gsk_render_node_ref() as the fundamental registry's RefFn.
gpointer fundamental_ref(gpointer p) {
  return gsk_render_node_ref(static_cast<GskRenderNode *>(p));
}

// gsk_render_node_unref() as the fundamental registry's UnrefFn.
void fundamental_unref(gpointer p) {
  gsk_render_node_unref(static_cast<GskRenderNode *>(p));
}

}  // namespace

namespace phpgtk {
// MINIT: bind the PHP class to GSK_TYPE_FILL_NODE on the fundamental registry.
void register_GskFillNode(zend_class_entry *ce) {
  register_fundamental(FundamentalClass{
      .type = GSK_TYPE_FILL_NODE, .ce = ce, .ref = fundamental_ref, .unref = fundamental_unref});
}
}  // namespace phpgtk
