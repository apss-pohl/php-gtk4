// GENERATED by gen/gir.php from Gsk.RepeatNode - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GskRepeatNode (fundamental handle)
#include "php_gtk4.h"
#include "core/fundamental.h"
#include "core/object.h"
#include "core/boxed.h"

using namespace phpgtk;

/**
 * Gtk4\GskRepeatNode::__construct(GrapheneRect $bounds, GskRenderNode $child, ?GrapheneRect
 * $child_bounds = null)
 *
 * Creates a `GskRenderNode` that will repeat the drawing of $child across the given $bounds.
 */
ZEND_METHOD(Gtk4_GskRepeatNode, __construct) {
  zval *bounds;
  zval *child;
  zval *child_bounds = nullptr;
  ZEND_PARSE_PARAMETERS_START(2, 3)
  Z_PARAM_OBJECT_OF_CLASS(bounds, boxed_class_for_type(GRAPHENE_TYPE_RECT)->ce)
  Z_PARAM_OBJECT_OF_CLASS(child, fundamental_class_for_type(GSK_TYPE_RENDER_NODE)->ce)
  Z_PARAM_OPTIONAL
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(child_bounds, boxed_class_for_type(GRAPHENE_TYPE_RECT)->ce)
  ZEND_PARSE_PARAMETERS_END();
  gpointer bounds_b = unwrap_boxed(bounds, GRAPHENE_TYPE_RECT);
  if (bounds_b == nullptr) RETURN_THROWS();
  gpointer child_f = unwrap_fundamental(child, GSK_TYPE_RENDER_NODE);
  if (child_f == nullptr) RETURN_THROWS();
  gpointer child_bounds_b = nullptr;
  if (child_bounds != nullptr) {
    child_bounds_b = unwrap_boxed(child_bounds, GRAPHENE_TYPE_RECT);
    if (child_bounds_b == nullptr) RETURN_THROWS();
  }
  gpointer obj = gsk_repeat_node_new(static_cast<graphene_rect_t *>(bounds_b),
                                     static_cast<GskRenderNode *>(child_f),
                                     static_cast<graphene_rect_t *>(child_bounds_b));
  if (obj == nullptr) {
    zend_throw_error(nullptr, "%s(): GTK refused to create the instance (see the CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  fundamental_adopt(fundamental_from_zval(ZEND_THIS), GSK_TYPE_REPEAT_NODE, obj);
}

/**
 * Gtk4\GskRepeatNode::get_child(): GskRenderNode
 *
 * Retrieves the child of $node.
 */
ZEND_METHOD(Gtk4_GskRepeatNode, get_child) {
  ZEND_PARSE_PARAMETERS_NONE();
  auto *self = static_cast<GskRenderNode *>(fundamental_self(execute_data));
  if (self == nullptr) RETURN_THROWS();
  gpointer phpgtk_ret = gsk_repeat_node_get_child(self);
  wrap_fundamental(GSK_TYPE_RENDER_NODE, phpgtk_ret, return_value);
}

/**
 * Gtk4\GskRepeatNode::get_child_bounds(): GrapheneRect
 *
 * Retrieves the bounding rectangle of the child of $node.
 */
ZEND_METHOD(Gtk4_GskRepeatNode, get_child_bounds) {
  ZEND_PARSE_PARAMETERS_NONE();
  auto *self = static_cast<GskRenderNode *>(fundamental_self(execute_data));
  if (self == nullptr) RETURN_THROWS();
  const graphene_rect_t *phpgtk_ret = gsk_repeat_node_get_child_bounds(self);
  wrap_boxed(GRAPHENE_TYPE_RECT, phpgtk_ret, return_value);
}

namespace {

// gsk_render_node_ref() as the fundamental registry's RefFn.
gpointer fundamental_ref(gpointer p) {
  return gsk_render_node_ref(static_cast<GskRenderNode *>(p));
}

// gsk_render_node_unref() as the fundamental registry's UnrefFn.
void fundamental_unref(gpointer p) {
  gsk_render_node_unref(static_cast<GskRenderNode *>(p));
}

}  // namespace

namespace phpgtk {
// MINIT: bind the PHP class to GSK_TYPE_REPEAT_NODE on the fundamental registry.
void register_GskRepeatNode(zend_class_entry *ce) {
  register_fundamental(FundamentalClass{
      .type = GSK_TYPE_REPEAT_NODE, .ce = ce, .ref = fundamental_ref, .unref = fundamental_unref});
}
}  // namespace phpgtk
