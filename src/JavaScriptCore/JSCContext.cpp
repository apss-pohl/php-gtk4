// GENERATED by gen/gir.php from JavaScriptCore.Context - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\JSCContext
#include "php_gtk4.h"
#include <jsc/jsc.h>
#include "core/object.h"
#include "core/subtype.h"

using namespace phpgtk;

/**
 * Gtk4\JSCContext::__construct()
 *
 * Create a new #JSCContext. The context is created in a new #JSCVirtualMachine. Use
 * jsc_context_new_with_virtual_machine() to create a new #JSCContext in an existing
 * #JSCVirtualMachine.
 */
ZEND_METHOD(Gtk4_JSCContext, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(jsc_context_new());
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * static Gtk4\JSCContext::new_with_virtual_machine(JSCVirtualMachine $vm): JSCContext
 *
 * Create a new #JSCContext in $virtual_machine.
 */
ZEND_METHOD(Gtk4_JSCContext, new_with_virtual_machine) {
  zval *vm;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(vm, class_for_gtype(JSC_TYPE_VIRTUAL_MACHINE))
  ZEND_PARSE_PARAMETERS_END();
  GObject *vm_o = unwrap(vm, JSC_TYPE_VIRTUAL_MACHINE);
  if (vm_o == nullptr) RETURN_THROWS();
  GObject *obj = G_OBJECT(jsc_context_new_with_virtual_machine(JSC_VIRTUAL_MACHINE(vm_o)));
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * static Gtk4\JSCContext::get_current(): ?JSCContext
 *
 * Get the #JSCContext that is currently executing a function. This should only be called within a
 * function or method callback, otherwise `null` will be returned.
 */
ZEND_METHOD(Gtk4_JSCContext, get_current) {
  ZEND_PARSE_PARAMETERS_NONE();
  JSCContext *phpgtk_ret = jsc_context_get_current();
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\JSCContext::clear_exception(): void
 *
 * Clear the uncaught exception in $context if any.
 */
ZEND_METHOD(Gtk4_JSCContext, clear_exception) {
  ZEND_PARSE_PARAMETERS_NONE();
  JSCContext *self = PHPGTK_SELF(JSCContext, JSC_TYPE_CONTEXT);
  jsc_context_clear_exception(self);
}

/**
 * Gtk4\JSCContext::evaluate(string $code, int $length): JSCValue
 *
 * Evaluate $code in $context.
 */
ZEND_METHOD(Gtk4_JSCContext, evaluate) {
  zend_string *code;
  zend_long length;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_STR(code)
  Z_PARAM_LONG(length)
  ZEND_PARSE_PARAMETERS_END();
  JSCContext *self = PHPGTK_SELF(JSCContext, JSC_TYPE_CONTEXT);
  if (!phpgtk::check_utf8(code, 1)) RETURN_THROWS();
  const bool precondition_0 =
      length == -1 || (length >= 0 && static_cast<gsize>(length) <= ZSTR_LEN(code));
  if (!precondition_0) {
    zend_argument_value_error(2, "must be -1 or at most the length of the code");
    RETURN_THROWS();
  }
  JSCValue *phpgtk_ret = jsc_context_evaluate(self, ZSTR_VAL(code), static_cast<gssize>(length));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * Gtk4\JSCContext::evaluate_with_source_uri(string $code, int $length, string $uri, int
 * $line_number): JSCValue
 *
 * Evaluate $code in $context using $uri as the source URI. The $line_number is the starting line
 * number in $uri; the value is one-based so the first line is 1. $uri and $line_number will be
 * shown in exceptions and they don't affect the behavior of the script.
 */
ZEND_METHOD(Gtk4_JSCContext, evaluate_with_source_uri) {
  zend_string *code;
  zend_long length;
  zend_string *uri;
  zend_long line_number;
  ZEND_PARSE_PARAMETERS_START(4, 4)
  Z_PARAM_STR(code)
  Z_PARAM_LONG(length)
  Z_PARAM_STR(uri)
  Z_PARAM_LONG(line_number)
  ZEND_PARSE_PARAMETERS_END();
  JSCContext *self = PHPGTK_SELF(JSCContext, JSC_TYPE_CONTEXT);
  if (!phpgtk::check_utf8(code, 1)) RETURN_THROWS();
  if (!phpgtk::check_utf8(uri, 3)) RETURN_THROWS();
  if (!phpgtk::check_range<guint>(line_number, 4)) RETURN_THROWS();
  const bool precondition_0 =
      length == -1 || (length >= 0 && static_cast<gsize>(length) <= ZSTR_LEN(code));
  if (!precondition_0) {
    zend_argument_value_error(2, "must be -1 or at most the length of the code");
    RETURN_THROWS();
  }
  JSCValue *phpgtk_ret =
      jsc_context_evaluate_with_source_uri(self, ZSTR_VAL(code), static_cast<gssize>(length),
                                           ZSTR_VAL(uri), static_cast<guint>(line_number));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * Gtk4\JSCContext::get_exception(): ?JSCException
 *
 * Get the last unhandled exception thrown in $context by API functions calls.
 */
ZEND_METHOD(Gtk4_JSCContext, get_exception) {
  ZEND_PARSE_PARAMETERS_NONE();
  JSCContext *self = PHPGTK_SELF(JSCContext, JSC_TYPE_CONTEXT);
  JSCException *phpgtk_ret = jsc_context_get_exception(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\JSCContext::get_global_object(): JSCValue
 *
 * Get a #JSCValue referencing the $context global object
 */
ZEND_METHOD(Gtk4_JSCContext, get_global_object) {
  ZEND_PARSE_PARAMETERS_NONE();
  JSCContext *self = PHPGTK_SELF(JSCContext, JSC_TYPE_CONTEXT);
  JSCValue *phpgtk_ret = jsc_context_get_global_object(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * Gtk4\JSCContext::get_value(string $name): JSCValue
 *
 * Get a property of $context global object with $name.
 */
ZEND_METHOD(Gtk4_JSCContext, get_value) {
  zend_string *name;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(name)
  ZEND_PARSE_PARAMETERS_END();
  JSCContext *self = PHPGTK_SELF(JSCContext, JSC_TYPE_CONTEXT);
  if (!phpgtk::check_utf8(name, 1)) RETURN_THROWS();
  JSCValue *phpgtk_ret = jsc_context_get_value(self, ZSTR_VAL(name));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * Gtk4\JSCContext::get_virtual_machine(): JSCVirtualMachine
 *
 * Get the #JSCVirtualMachine where $context was created.
 */
ZEND_METHOD(Gtk4_JSCContext, get_virtual_machine) {
  ZEND_PARSE_PARAMETERS_NONE();
  JSCContext *self = PHPGTK_SELF(JSCContext, JSC_TYPE_CONTEXT);
  JSCVirtualMachine *phpgtk_ret = jsc_context_get_virtual_machine(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\JSCContext::pop_exception_handler(): void
 *
 * Remove the last #JSCExceptionHandler previously pushed to $context with
 * jsc_context_push_exception_handler().
 */
ZEND_METHOD(Gtk4_JSCContext, pop_exception_handler) {
  ZEND_PARSE_PARAMETERS_NONE();
  JSCContext *self = PHPGTK_SELF(JSCContext, JSC_TYPE_CONTEXT);
  jsc_context_pop_exception_handler(self);
}

/**
 * Gtk4\JSCContext::set_value(string $name, JSCValue $value): void
 *
 * Set a property of $context global object with $name and $value.
 */
ZEND_METHOD(Gtk4_JSCContext, set_value) {
  zend_string *name;
  zval *value;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_STR(name)
  Z_PARAM_OBJECT_OF_CLASS(value, class_for_gtype(JSC_TYPE_VALUE))
  ZEND_PARSE_PARAMETERS_END();
  JSCContext *self = PHPGTK_SELF(JSCContext, JSC_TYPE_CONTEXT);
  if (!phpgtk::check_utf8(name, 1)) RETURN_THROWS();
  GObject *value_o = unwrap(value, JSC_TYPE_VALUE);
  if (value_o == nullptr) RETURN_THROWS();
  jsc_context_set_value(self, ZSTR_VAL(name), JSC_VALUE(value_o));
}

/**
 * Gtk4\JSCContext::throw(string $error_message): void
 *
 * Throw an exception to $context using the given error message. The created #JSCException can be
 * retrieved with jsc_context_get_exception().
 */
ZEND_METHOD(Gtk4_JSCContext, throw) {
  zend_string *error_message;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(error_message)
  ZEND_PARSE_PARAMETERS_END();
  JSCContext *self = PHPGTK_SELF(JSCContext, JSC_TYPE_CONTEXT);
  if (!phpgtk::check_utf8(error_message, 1)) RETURN_THROWS();
  jsc_context_throw(self, ZSTR_VAL(error_message));
}

/**
 * Gtk4\JSCContext::throw_exception(JSCException $exception): void
 *
 * Throw $exception to $context.
 */
ZEND_METHOD(Gtk4_JSCContext, throw_exception) {
  zval *exception;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(exception, class_for_gtype(JSC_TYPE_EXCEPTION))
  ZEND_PARSE_PARAMETERS_END();
  JSCContext *self = PHPGTK_SELF(JSCContext, JSC_TYPE_CONTEXT);
  GObject *exception_o = unwrap(exception, JSC_TYPE_EXCEPTION);
  if (exception_o == nullptr) RETURN_THROWS();
  jsc_context_throw_exception(self, JSC_EXCEPTION(exception_o));
}

/**
 * Gtk4\JSCContext::throw_with_name(string $error_name, string $error_message): void
 *
 * Throw an exception to $context using the given error name and message. The created #JSCException
 * can be retrieved with jsc_context_get_exception().
 */
ZEND_METHOD(Gtk4_JSCContext, throw_with_name) {
  zend_string *error_name;
  zend_string *error_message;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_STR(error_name)
  Z_PARAM_STR(error_message)
  ZEND_PARSE_PARAMETERS_END();
  JSCContext *self = PHPGTK_SELF(JSCContext, JSC_TYPE_CONTEXT);
  if (!phpgtk::check_utf8(error_name, 1)) RETURN_THROWS();
  if (!phpgtk::check_utf8(error_message, 2)) RETURN_THROWS();
  jsc_context_throw_with_name(self, ZSTR_VAL(error_name), ZSTR_VAL(error_message));
}
