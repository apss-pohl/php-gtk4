// GENERATED by gen/gir.php from JavaScriptCore.Value - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\JSCValue
#include "php_gtk4.h"
#include <jsc/jsc.h>
#include "core/object.h"
#include "core/collections.h"
#include "core/callback.h"
#include <array>

using namespace phpgtk;

// function_call() / constructor_call() / object_invoke_method(): the arguments come as a PHP
// list of JSCValue, not as a C array with its length in front of it.
#include <vector>

namespace {

// The JSCValue behind every element of the list `list` (argument number `pos`) into `out`; a
// wrong element is a TypeError naming that argument. False, with the exception pending, on one.
bool jsc_values_from_list(zval *list, uint32_t pos, std::vector<JSCValue *> &out) {
  zval *item;
  // NOLINTNEXTLINE(readability-math-missing-parentheses) Zend macro expansion
  ZEND_HASH_FOREACH_VAL(Z_ARRVAL_P(list), item) {
    GObject *value = unwrap(item, JSC_TYPE_VALUE);
    if (value == nullptr) {
      if (EG(exception) != nullptr) zend_clear_exception();
      zend_argument_type_error(pos, "must be a list of JSCValue, %s found in it",
                               zend_zval_value_name(item));
      return false;
    }
    out.push_back(JSC_VALUE(value));
  }
  ZEND_HASH_FOREACH_END();
  return true;
}

}  // namespace

/**
 * Gtk4\JSCValue::__construct()
 *
 * JSCValue has no constructor in GTK: instances come from GTK, never from `new`.
 */
ZEND_METHOD(Gtk4_JSCValue, __construct) {
  // Private: never called (object_init_ex() in wrap() skips constructors).
  ZEND_PARSE_PARAMETERS_NONE();
}

/**
 * static Gtk4\JSCValue::new_array_from_strv(JSCContext $context, array $strv): JSCValue
 *
 * Create a new #JSCValue referencing an array of strings with the items from $strv. If $array is
 * `null` or empty a new empty array will be created.
 */
ZEND_METHOD(Gtk4_JSCValue, new_array_from_strv) {
  zval *context;
  zval *strv;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(context, class_for_gtype(JSC_TYPE_CONTEXT))
  Z_PARAM_ARRAY(strv)
  ZEND_PARSE_PARAMETERS_END();
  GObject *context_o = unwrap(context, JSC_TYPE_CONTEXT);
  if (context_o == nullptr) RETURN_THROWS();
  char **strv_v = strv_from_php(strv);
  if (strv_v == nullptr) RETURN_THROWS();
  JSCValue *call_result =
      jsc_value_new_array_from_strv(JSC_CONTEXT(context_o), const_cast<const char **>(strv_v));
  g_strfreev(strv_v);
  GObject *obj = G_OBJECT(call_result);
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * static Gtk4\JSCValue::new_boolean(JSCContext $context, bool $value): JSCValue
 *
 * Create a new #JSCValue from $value
 */
ZEND_METHOD(Gtk4_JSCValue, new_boolean) {
  zval *context;
  bool value;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(context, class_for_gtype(JSC_TYPE_CONTEXT))
  Z_PARAM_BOOL(value)
  ZEND_PARSE_PARAMETERS_END();
  GObject *context_o = unwrap(context, JSC_TYPE_CONTEXT);
  if (context_o == nullptr) RETURN_THROWS();
  GObject *obj = G_OBJECT(jsc_value_new_boolean(JSC_CONTEXT(context_o), value));
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * static Gtk4\JSCValue::new_from_json(JSCContext $context, string $json): JSCValue
 *
 * Create a new #JSCValue referencing a new value created by parsing $json.
 */
ZEND_METHOD(Gtk4_JSCValue, new_from_json) {
  zval *context;
  zend_string *json;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(context, class_for_gtype(JSC_TYPE_CONTEXT))
  Z_PARAM_STR(json)
  ZEND_PARSE_PARAMETERS_END();
  GObject *context_o = unwrap(context, JSC_TYPE_CONTEXT);
  if (context_o == nullptr) RETURN_THROWS();
  if (!phpgtk::check_utf8(json, 2)) RETURN_THROWS();
  GObject *obj = G_OBJECT(jsc_value_new_from_json(JSC_CONTEXT(context_o), ZSTR_VAL(json)));
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * static Gtk4\JSCValue::new_null(JSCContext $context): JSCValue
 *
 * Create a new #JSCValue referencing <function>null</function> in $context.
 */
ZEND_METHOD(Gtk4_JSCValue, new_null) {
  zval *context;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(context, class_for_gtype(JSC_TYPE_CONTEXT))
  ZEND_PARSE_PARAMETERS_END();
  GObject *context_o = unwrap(context, JSC_TYPE_CONTEXT);
  if (context_o == nullptr) RETURN_THROWS();
  GObject *obj = G_OBJECT(jsc_value_new_null(JSC_CONTEXT(context_o)));
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * static Gtk4\JSCValue::new_number(JSCContext $context, float $number): JSCValue
 *
 * Create a new #JSCValue from $number.
 */
ZEND_METHOD(Gtk4_JSCValue, new_number) {
  zval *context;
  double number;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(context, class_for_gtype(JSC_TYPE_CONTEXT))
  Z_PARAM_DOUBLE(number)
  ZEND_PARSE_PARAMETERS_END();
  GObject *context_o = unwrap(context, JSC_TYPE_CONTEXT);
  if (context_o == nullptr) RETURN_THROWS();
  GObject *obj = G_OBJECT(jsc_value_new_number(JSC_CONTEXT(context_o), number));
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

namespace {
// Executor trampoline for JSCValue::new_promise(): wraps the C arguments, invokes the PHP callable
// (call scope: released after the call).
void cb_new_promise_executor(JSCValue *resolve, JSCValue *reject, gpointer user_data) {
  auto *cb = static_cast<Callback *>(user_data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(resolve != nullptr ? G_OBJECT(resolve) : nullptr, &argv[0]);
  wrap(reject != nullptr ? G_OBJECT(reject) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
}
}  // namespace

/**
 * static Gtk4\JSCValue::new_promise(JSCContext $context, callable $executor): JSCValue
 *
 * Creates a new Promise. $executor will be invoked during promise initialization and it receives
 * the $resolve and $reject objects than can be called to resolve or reject the promise. It is
 * called like a JavaScript function, so exceptions raised during the executor invocation will not
 * be propagated to the context, but handled by the promise causing a rejection.
 */
ZEND_METHOD(Gtk4_JSCValue, new_promise) {
  zval *context;
  zend_fcall_info fci_executor = empty_fcall_info;
  zend_fcall_info_cache fcc_executor = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(context, class_for_gtype(JSC_TYPE_CONTEXT))
  Z_PARAM_FUNC(fci_executor, fcc_executor)
  ZEND_PARSE_PARAMETERS_END();
  GObject *context_o = unwrap(context, JSC_TYPE_CONTEXT);
  if (context_o == nullptr) RETURN_THROWS();
  Callback *cb_executor = callback_new(&fci_executor.function_name, "JSCValue::new_promise");
  JSCValue *call_result =
      jsc_value_new_promise(JSC_CONTEXT(context_o), cb_new_promise_executor, cb_executor);
  if (cb_executor != nullptr) {
    callback_free(cb_executor);
    callback_drain();
  }
  GObject *obj = G_OBJECT(call_result);
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * static Gtk4\JSCValue::new_string(JSCContext $context, ?string $string = null): JSCValue
 *
 * Create a new #JSCValue from $string. If you need to create a #JSCValue from a string containing
 * null characters, use jsc_value_new_string_from_bytes() instead.
 */
ZEND_METHOD(Gtk4_JSCValue, new_string) {
  zval *context;
  zend_string *string = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 2)
  Z_PARAM_OBJECT_OF_CLASS(context, class_for_gtype(JSC_TYPE_CONTEXT))
  Z_PARAM_OPTIONAL
  Z_PARAM_STR_OR_NULL(string)
  ZEND_PARSE_PARAMETERS_END();
  GObject *context_o = unwrap(context, JSC_TYPE_CONTEXT);
  if (context_o == nullptr) RETURN_THROWS();
  if (string != nullptr && !phpgtk::check_utf8(string, 2)) RETURN_THROWS();
  GObject *obj = G_OBJECT(
      jsc_value_new_string(JSC_CONTEXT(context_o), string != nullptr ? ZSTR_VAL(string) : nullptr));
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * static Gtk4\JSCValue::new_string_from_bytes(JSCContext $context, ?string $bytes = null): JSCValue
 *
 * Create a new #JSCValue from $bytes.
 */
ZEND_METHOD(Gtk4_JSCValue, new_string_from_bytes) {
  zval *context;
  zend_string *bytes = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 2)
  Z_PARAM_OBJECT_OF_CLASS(context, class_for_gtype(JSC_TYPE_CONTEXT))
  Z_PARAM_OPTIONAL
  Z_PARAM_STR_OR_NULL(bytes)
  ZEND_PARSE_PARAMETERS_END();
  GObject *context_o = unwrap(context, JSC_TYPE_CONTEXT);
  if (context_o == nullptr) RETURN_THROWS();
  GBytes *bytes_b = bytes != nullptr ? g_bytes_new(ZSTR_VAL(bytes), ZSTR_LEN(bytes)) : nullptr;
  JSCValue *call_result = jsc_value_new_string_from_bytes(JSC_CONTEXT(context_o), bytes_b);
  if (bytes_b != nullptr) g_bytes_unref(bytes_b);
  GObject *obj = G_OBJECT(call_result);
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * static Gtk4\JSCValue::new_undefined(JSCContext $context): JSCValue
 *
 * Create a new #JSCValue referencing <function>undefined</function> in $context.
 */
ZEND_METHOD(Gtk4_JSCValue, new_undefined) {
  zval *context;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(context, class_for_gtype(JSC_TYPE_CONTEXT))
  ZEND_PARSE_PARAMETERS_END();
  GObject *context_o = unwrap(context, JSC_TYPE_CONTEXT);
  if (context_o == nullptr) RETURN_THROWS();
  GObject *obj = G_OBJECT(jsc_value_new_undefined(JSC_CONTEXT(context_o)));
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * Gtk4\JSCValue::array_buffer_get_data(): string
 *
 * Gets a pointer to memory that contains the array buffer data.
 */
ZEND_METHOD(Gtk4_JSCValue, array_buffer_get_data) {
  ZEND_PARSE_PARAMETERS_NONE();
  JSCValue *self = PHPGTK_SELF(JSCValue, JSC_TYPE_VALUE);
  gsize size = 0;
  const auto *phpgtk_ret = static_cast<const char *>(jsc_value_array_buffer_get_data(self, &size));
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETVAL_STRINGL(phpgtk_ret, static_cast<size_t>(size));
}

/**
 * Gtk4\JSCValue::array_buffer_get_size(): int
 *
 * Gets the size in bytes of the array buffer.
 */
ZEND_METHOD(Gtk4_JSCValue, array_buffer_get_size) {
  ZEND_PARSE_PARAMETERS_NONE();
  JSCValue *self = PHPGTK_SELF(JSCValue, JSC_TYPE_VALUE);
  RETURN_LONG(static_cast<zend_long>(jsc_value_array_buffer_get_size(self)));
}

/**
 * Gtk4\JSCValue::get_context(): JSCContext
 *
 * Get the #JSCContext in which $value was created.
 */
ZEND_METHOD(Gtk4_JSCValue, get_context) {
  ZEND_PARSE_PARAMETERS_NONE();
  JSCValue *self = PHPGTK_SELF(JSCValue, JSC_TYPE_VALUE);
  JSCContext *phpgtk_ret = jsc_value_get_context(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\JSCValue::is_array(): bool
 *
 * Get whether the value referenced by $value is an array.
 */
ZEND_METHOD(Gtk4_JSCValue, is_array) {
  ZEND_PARSE_PARAMETERS_NONE();
  JSCValue *self = PHPGTK_SELF(JSCValue, JSC_TYPE_VALUE);
  RETURN_BOOL(jsc_value_is_array(self));
}

/**
 * Gtk4\JSCValue::is_array_buffer(): bool
 *
 * Check whether the $value is an %ArrayBuffer.
 */
ZEND_METHOD(Gtk4_JSCValue, is_array_buffer) {
  ZEND_PARSE_PARAMETERS_NONE();
  JSCValue *self = PHPGTK_SELF(JSCValue, JSC_TYPE_VALUE);
  RETURN_BOOL(jsc_value_is_array_buffer(self));
}

/**
 * Gtk4\JSCValue::is_boolean(): bool
 *
 * Get whether the value referenced by $value is a boolean.
 */
ZEND_METHOD(Gtk4_JSCValue, is_boolean) {
  ZEND_PARSE_PARAMETERS_NONE();
  JSCValue *self = PHPGTK_SELF(JSCValue, JSC_TYPE_VALUE);
  RETURN_BOOL(jsc_value_is_boolean(self));
}

/**
 * Gtk4\JSCValue::is_constructor(): bool
 *
 * Get whether the value referenced by $value is a constructor.
 */
ZEND_METHOD(Gtk4_JSCValue, is_constructor) {
  ZEND_PARSE_PARAMETERS_NONE();
  JSCValue *self = PHPGTK_SELF(JSCValue, JSC_TYPE_VALUE);
  RETURN_BOOL(jsc_value_is_constructor(self));
}

/**
 * Gtk4\JSCValue::is_function(): bool
 *
 * Get whether the value referenced by $value is a function
 */
ZEND_METHOD(Gtk4_JSCValue, is_function) {
  ZEND_PARSE_PARAMETERS_NONE();
  JSCValue *self = PHPGTK_SELF(JSCValue, JSC_TYPE_VALUE);
  RETURN_BOOL(jsc_value_is_function(self));
}

/**
 * Gtk4\JSCValue::is_null(): bool
 *
 * Get whether the value referenced by $value is <function>null</function>.
 */
ZEND_METHOD(Gtk4_JSCValue, is_null) {
  ZEND_PARSE_PARAMETERS_NONE();
  JSCValue *self = PHPGTK_SELF(JSCValue, JSC_TYPE_VALUE);
  RETURN_BOOL(jsc_value_is_null(self));
}

/**
 * Gtk4\JSCValue::is_number(): bool
 *
 * Get whether the value referenced by $value is a number.
 */
ZEND_METHOD(Gtk4_JSCValue, is_number) {
  ZEND_PARSE_PARAMETERS_NONE();
  JSCValue *self = PHPGTK_SELF(JSCValue, JSC_TYPE_VALUE);
  RETURN_BOOL(jsc_value_is_number(self));
}

/**
 * Gtk4\JSCValue::is_object(): bool
 *
 * Get whether the value referenced by $value is an object.
 */
ZEND_METHOD(Gtk4_JSCValue, is_object) {
  ZEND_PARSE_PARAMETERS_NONE();
  JSCValue *self = PHPGTK_SELF(JSCValue, JSC_TYPE_VALUE);
  RETURN_BOOL(jsc_value_is_object(self));
}

/**
 * Gtk4\JSCValue::is_string(): bool
 *
 * Get whether the value referenced by $value is a string
 */
ZEND_METHOD(Gtk4_JSCValue, is_string) {
  ZEND_PARSE_PARAMETERS_NONE();
  JSCValue *self = PHPGTK_SELF(JSCValue, JSC_TYPE_VALUE);
  RETURN_BOOL(jsc_value_is_string(self));
}

/**
 * Gtk4\JSCValue::is_typed_array(): bool
 *
 * Determines whether a value is a typed array.
 */
ZEND_METHOD(Gtk4_JSCValue, is_typed_array) {
  ZEND_PARSE_PARAMETERS_NONE();
  JSCValue *self = PHPGTK_SELF(JSCValue, JSC_TYPE_VALUE);
  RETURN_BOOL(jsc_value_is_typed_array(self));
}

/**
 * Gtk4\JSCValue::is_undefined(): bool
 *
 * Get whether the value referenced by $value is <function>undefined</function>.
 */
ZEND_METHOD(Gtk4_JSCValue, is_undefined) {
  ZEND_PARSE_PARAMETERS_NONE();
  JSCValue *self = PHPGTK_SELF(JSCValue, JSC_TYPE_VALUE);
  RETURN_BOOL(jsc_value_is_undefined(self));
}

/**
 * Gtk4\JSCValue::object_define_property_data(string $property_name, int $flags, JSCValue
 * $property_value): void
 *
 * Define or modify a property with $property_name in object referenced by $value. This is
 * equivalent to JavaScript <function>Object.defineProperty()</function> when used with a data
 * descriptor.
 */
ZEND_METHOD(Gtk4_JSCValue, object_define_property_data) {
  zend_string *property_name;
  zend_long flags;
  zval *property_value;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_STR(property_name)
  Z_PARAM_LONG(flags)
  Z_PARAM_OBJECT_OF_CLASS(property_value, class_for_gtype(JSC_TYPE_VALUE))
  ZEND_PARSE_PARAMETERS_END();
  JSCValue *self = PHPGTK_SELF(JSCValue, JSC_TYPE_VALUE);
  if (!phpgtk::check_utf8(property_name, 1)) RETURN_THROWS();
  GObject *property_value_o = unwrap(property_value, JSC_TYPE_VALUE);
  if (property_value_o == nullptr) RETURN_THROWS();
  jsc_value_object_define_property_data(self, ZSTR_VAL(property_name),
                                        static_cast<JSCValuePropertyFlags>(flags),
                                        JSC_VALUE(property_value_o));
}

/**
 * Gtk4\JSCValue::object_delete_property(string $name): bool
 *
 * Try to delete property with $name from $value. This function will return `false` if the property
 * was defined without %JSC_VALUE_PROPERTY_CONFIGURABLE flag.
 */
ZEND_METHOD(Gtk4_JSCValue, object_delete_property) {
  zend_string *name;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(name)
  ZEND_PARSE_PARAMETERS_END();
  JSCValue *self = PHPGTK_SELF(JSCValue, JSC_TYPE_VALUE);
  if (!phpgtk::check_utf8(name, 1)) RETURN_THROWS();
  RETURN_BOOL(jsc_value_object_delete_property(self, ZSTR_VAL(name)));
}

/**
 * Gtk4\JSCValue::object_enumerate_properties(): array
 *
 * Get the list of property names of $value. Only properties defined with
 * %JSC_VALUE_PROPERTY_ENUMERABLE flag will be collected.
 */
ZEND_METHOD(Gtk4_JSCValue, object_enumerate_properties) {
  ZEND_PARSE_PARAMETERS_NONE();
  JSCValue *self = PHPGTK_SELF(JSCValue, JSC_TYPE_VALUE);
  strv_to_php(jsc_value_object_enumerate_properties(self), Transfer::Full, return_value);
}

/**
 * Gtk4\JSCValue::object_get_property(string $name): JSCValue
 *
 * Get property with $name from $value.
 */
ZEND_METHOD(Gtk4_JSCValue, object_get_property) {
  zend_string *name;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(name)
  ZEND_PARSE_PARAMETERS_END();
  JSCValue *self = PHPGTK_SELF(JSCValue, JSC_TYPE_VALUE);
  if (!phpgtk::check_utf8(name, 1)) RETURN_THROWS();
  JSCValue *phpgtk_ret = jsc_value_object_get_property(self, ZSTR_VAL(name));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * Gtk4\JSCValue::object_get_property_at_index(int $index): JSCValue
 *
 * Get property at $index from $value.
 */
ZEND_METHOD(Gtk4_JSCValue, object_get_property_at_index) {
  zend_long index;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(index)
  ZEND_PARSE_PARAMETERS_END();
  JSCValue *self = PHPGTK_SELF(JSCValue, JSC_TYPE_VALUE);
  if (!phpgtk::check_range<guint>(index, 1)) RETURN_THROWS();
  JSCValue *phpgtk_ret = jsc_value_object_get_property_at_index(self, static_cast<guint>(index));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * Gtk4\JSCValue::object_has_property(string $name): bool
 *
 * Get whether $value has property with $name.
 */
ZEND_METHOD(Gtk4_JSCValue, object_has_property) {
  zend_string *name;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(name)
  ZEND_PARSE_PARAMETERS_END();
  JSCValue *self = PHPGTK_SELF(JSCValue, JSC_TYPE_VALUE);
  if (!phpgtk::check_utf8(name, 1)) RETURN_THROWS();
  RETURN_BOOL(jsc_value_object_has_property(self, ZSTR_VAL(name)));
}

/**
 * Gtk4\JSCValue::object_is_instance_of(string $name): bool
 *
 * Get whether the value referenced by $value is an instance of class $name.
 */
ZEND_METHOD(Gtk4_JSCValue, object_is_instance_of) {
  zend_string *name;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(name)
  ZEND_PARSE_PARAMETERS_END();
  JSCValue *self = PHPGTK_SELF(JSCValue, JSC_TYPE_VALUE);
  if (!phpgtk::check_utf8(name, 1)) RETURN_THROWS();
  RETURN_BOOL(jsc_value_object_is_instance_of(self, ZSTR_VAL(name)));
}

/**
 * Gtk4\JSCValue::object_set_property(string $name, JSCValue $property): void
 *
 * Set $property with $name on $value.
 */
ZEND_METHOD(Gtk4_JSCValue, object_set_property) {
  zend_string *name;
  zval *property;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_STR(name)
  Z_PARAM_OBJECT_OF_CLASS(property, class_for_gtype(JSC_TYPE_VALUE))
  ZEND_PARSE_PARAMETERS_END();
  JSCValue *self = PHPGTK_SELF(JSCValue, JSC_TYPE_VALUE);
  if (!phpgtk::check_utf8(name, 1)) RETURN_THROWS();
  GObject *property_o = unwrap(property, JSC_TYPE_VALUE);
  if (property_o == nullptr) RETURN_THROWS();
  jsc_value_object_set_property(self, ZSTR_VAL(name), JSC_VALUE(property_o));
}

/**
 * Gtk4\JSCValue::object_set_property_at_index(int $index, JSCValue $property): void
 *
 * Set $property at $index on $value.
 */
ZEND_METHOD(Gtk4_JSCValue, object_set_property_at_index) {
  zend_long index;
  zval *property;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_LONG(index)
  Z_PARAM_OBJECT_OF_CLASS(property, class_for_gtype(JSC_TYPE_VALUE))
  ZEND_PARSE_PARAMETERS_END();
  JSCValue *self = PHPGTK_SELF(JSCValue, JSC_TYPE_VALUE);
  if (!phpgtk::check_range<guint>(index, 1)) RETURN_THROWS();
  GObject *property_o = unwrap(property, JSC_TYPE_VALUE);
  if (property_o == nullptr) RETURN_THROWS();
  jsc_value_object_set_property_at_index(self, static_cast<guint>(index), JSC_VALUE(property_o));
}

/**
 * Gtk4\JSCValue::to_boolean(): bool
 *
 * Convert $value to a boolean.
 */
ZEND_METHOD(Gtk4_JSCValue, to_boolean) {
  ZEND_PARSE_PARAMETERS_NONE();
  JSCValue *self = PHPGTK_SELF(JSCValue, JSC_TYPE_VALUE);
  RETURN_BOOL(jsc_value_to_boolean(self));
}

/**
 * Gtk4\JSCValue::to_double(): float
 *
 * Convert $value to a double.
 */
ZEND_METHOD(Gtk4_JSCValue, to_double) {
  ZEND_PARSE_PARAMETERS_NONE();
  JSCValue *self = PHPGTK_SELF(JSCValue, JSC_TYPE_VALUE);
  RETURN_DOUBLE(jsc_value_to_double(self));
}

/**
 * Gtk4\JSCValue::to_int32(): int
 *
 * Convert $value to a #gint32.
 */
ZEND_METHOD(Gtk4_JSCValue, to_int32) {
  ZEND_PARSE_PARAMETERS_NONE();
  JSCValue *self = PHPGTK_SELF(JSCValue, JSC_TYPE_VALUE);
  RETURN_LONG(static_cast<zend_long>(jsc_value_to_int32(self)));
}

/**
 * Gtk4\JSCValue::to_json(int $indent): string
 *
 * Create a JSON string of $value serialization. If $indent is 0, the resulting JSON will not
 * contain newlines. The size of the indent is clamped to 10 spaces.
 */
ZEND_METHOD(Gtk4_JSCValue, to_json) {
  zend_long indent;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(indent)
  ZEND_PARSE_PARAMETERS_END();
  JSCValue *self = PHPGTK_SELF(JSCValue, JSC_TYPE_VALUE);
  if (!phpgtk::check_range<guint>(indent, 1)) RETURN_THROWS();
  char *phpgtk_ret = jsc_value_to_json(self, static_cast<guint>(indent));
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETVAL_STRING(phpgtk_ret);
  g_free(phpgtk_ret);
}

/**
 * Gtk4\JSCValue::to_string(): string
 *
 * Convert $value to a string. Use jsc_value_to_string_as_bytes() instead, if you need to handle
 * strings containing null characters.
 */
ZEND_METHOD(Gtk4_JSCValue, to_string) {
  ZEND_PARSE_PARAMETERS_NONE();
  JSCValue *self = PHPGTK_SELF(JSCValue, JSC_TYPE_VALUE);
  char *phpgtk_ret = jsc_value_to_string(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETVAL_STRING(phpgtk_ret);
  g_free(phpgtk_ret);
}

/**
 * Gtk4\JSCValue::to_string_as_bytes(): string
 *
 * Convert $value to a string and return the results as #GBytes. This is needed to handle strings
 * with null characters.
 */
ZEND_METHOD(Gtk4_JSCValue, to_string_as_bytes) {
  ZEND_PARSE_PARAMETERS_NONE();
  JSCValue *self = PHPGTK_SELF(JSCValue, JSC_TYPE_VALUE);
  GBytes *bytes = jsc_value_to_string_as_bytes(self);
  gsize size = 0;
  const auto *data = static_cast<const char *>(g_bytes_get_data(bytes, &size));
  RETVAL_STRINGL(data, size);
  g_bytes_unref(bytes);
}

/**
 * Gtk4\JSCValue::typed_array_get_buffer(): JSCValue
 *
 * Obtain the %ArrayBuffer for the memory region of the typed array elements.
 */
ZEND_METHOD(Gtk4_JSCValue, typed_array_get_buffer) {
  ZEND_PARSE_PARAMETERS_NONE();
  JSCValue *self = PHPGTK_SELF(JSCValue, JSC_TYPE_VALUE);
  JSCValue *phpgtk_ret = jsc_value_typed_array_get_buffer(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * Gtk4\JSCValue::typed_array_get_length(): int
 *
 * Gets the number of elements in a typed array.
 */
ZEND_METHOD(Gtk4_JSCValue, typed_array_get_length) {
  ZEND_PARSE_PARAMETERS_NONE();
  JSCValue *self = PHPGTK_SELF(JSCValue, JSC_TYPE_VALUE);
  RETURN_LONG(static_cast<zend_long>(jsc_value_typed_array_get_length(self)));
}

/**
 * Gtk4\JSCValue::typed_array_get_offset(): int
 *
 * Gets the offset over the underlying array buffer data.
 */
ZEND_METHOD(Gtk4_JSCValue, typed_array_get_offset) {
  ZEND_PARSE_PARAMETERS_NONE();
  JSCValue *self = PHPGTK_SELF(JSCValue, JSC_TYPE_VALUE);
  RETURN_LONG(static_cast<zend_long>(jsc_value_typed_array_get_offset(self)));
}

/**
 * Gtk4\JSCValue::typed_array_get_size(): int
 *
 * Gets the size of a typed array.
 */
ZEND_METHOD(Gtk4_JSCValue, typed_array_get_size) {
  ZEND_PARSE_PARAMETERS_NONE();
  JSCValue *self = PHPGTK_SELF(JSCValue, JSC_TYPE_VALUE);
  RETURN_LONG(static_cast<zend_long>(jsc_value_typed_array_get_size(self)));
}

/**
 * Gtk4\JSCValue::typed_array_get_type(): int
 *
 * Gets the type of elements contained in a typed array.
 */
ZEND_METHOD(Gtk4_JSCValue, typed_array_get_type) {
  ZEND_PARSE_PARAMETERS_NONE();
  JSCValue *self = PHPGTK_SELF(JSCValue, JSC_TYPE_VALUE);
  RETURN_LONG(static_cast<zend_long>(jsc_value_typed_array_get_type(self)));
}

/**
 * public function constructor_call(array $parameters = []): JSCValue
 * Invoke `new` with this constructor and $parameters, a list of JSCValue; the object built. A
 * JavaScript exception does not throw here: it lands in the context
 * (`get_context()->get_exception()`).
 */
ZEND_METHOD(Gtk4_JSCValue, constructor_call) {
  zval *parameters = nullptr;
  ZEND_PARSE_PARAMETERS_START(0, 1)
  Z_PARAM_OPTIONAL
  Z_PARAM_ARRAY(parameters)
  ZEND_PARSE_PARAMETERS_END();
  JSCValue *self = PHPGTK_SELF(JSCValue, JSC_TYPE_VALUE);
  std::vector<JSCValue *> args;
  if (parameters != nullptr && !jsc_values_from_list(parameters, 1, args)) RETURN_THROWS();
  JSCValue *phpgtk_ret =
      jsc_value_constructor_callv(self, static_cast<guint>(args.size()), args.data());
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * public function function_call(array $parameters = []): JSCValue
 * Call this function with $parameters, a list of JSCValue; the result, or `undefined` when the
 * function returns nothing. A JavaScript exception does not throw here: it lands in the context
 * (`get_context()->get_exception()`).
 */
ZEND_METHOD(Gtk4_JSCValue, function_call) {
  zval *parameters = nullptr;
  ZEND_PARSE_PARAMETERS_START(0, 1)
  Z_PARAM_OPTIONAL
  Z_PARAM_ARRAY(parameters)
  ZEND_PARSE_PARAMETERS_END();
  JSCValue *self = PHPGTK_SELF(JSCValue, JSC_TYPE_VALUE);
  std::vector<JSCValue *> args;
  if (parameters != nullptr && !jsc_values_from_list(parameters, 1, args)) RETURN_THROWS();
  JSCValue *phpgtk_ret =
      jsc_value_function_callv(self, static_cast<guint>(args.size()), args.data());
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * public function object_invoke_method(string $name, array $parameters = []): JSCValue
 * Invoke the method $name of this object with $parameters, a list of JSCValue; the result, or
 * `undefined` when the method returns nothing. A JavaScript exception does not throw here: it
 * lands in the context (`get_context()->get_exception()`).
 */
ZEND_METHOD(Gtk4_JSCValue, object_invoke_method) {
  zend_string *name;
  zval *parameters = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 2)
  Z_PARAM_STR(name)
  Z_PARAM_OPTIONAL
  Z_PARAM_ARRAY(parameters)
  ZEND_PARSE_PARAMETERS_END();
  JSCValue *self = PHPGTK_SELF(JSCValue, JSC_TYPE_VALUE);
  if (!phpgtk::check_utf8(name, 1)) RETURN_THROWS();
  std::vector<JSCValue *> args;
  if (parameters != nullptr && !jsc_values_from_list(parameters, 2, args)) RETURN_THROWS();
  JSCValue *phpgtk_ret = jsc_value_object_invoke_methodv(
      self, ZSTR_VAL(name), static_cast<guint>(args.size()), args.data());
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}
