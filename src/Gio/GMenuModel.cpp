// GENERATED by gen/gir.php from Gio.MenuModel - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GMenuModel
#include "php_gtk4.h"
#include "core/object.h"
#include "core/variant.h"
#include "core/subtype.h"
#include "core/error.h"
#include <array>

using namespace phpgtk;

// Prelude for Gtk4\GMenuModel: the item-index precondition its two item queries share.

namespace {

// g_menu_model_get_item_link() and g_menu_model_get_item_attribute_value() have no defence
// against an index outside the model: a negative one indexes before the item array and
// segfaults, and one past the end reaches g_menu_model_real_*'s `g_assert_not_reached()`,
// which is a g_error() and ends the process. Neither is a CRITICAL a test could survive, so
// the range is checked here - a value the script can build must not end it (CLAUDE.md, "A
// value PHP can build must not end the process").
bool menu_item_index_is_valid(GMenuModel *self, zend_long item_index) {
  gint n_items = g_menu_model_get_n_items(self);
  if (item_index < 0 || item_index >= n_items) {
    if (n_items == 0) {
      zend_argument_value_error(1, "cannot be used on an empty menu, " ZEND_LONG_FMT " given",
                                item_index);
    } else {
      zend_argument_value_error(1, "must be between 0 and %d, " ZEND_LONG_FMT " given", n_items - 1,
                                item_index);
    }
    return false;
  }
  return true;
}

}  // namespace

/**
 * Gtk4\GMenuModel::__construct()
 *
 * GMenuModel is abstract in GTK: instances come from GTK, never from `new`.
 */
ZEND_METHOD(Gtk4_GMenuModel, __construct) {
  // Private: never called (object_init_ex() in wrap() skips constructors).
  ZEND_PARSE_PARAMETERS_NONE();
}

/**
 * Gtk4\GMenuModel::get_n_items(): int
 *
 * Query the number of items in $model.
 */
ZEND_METHOD(Gtk4_GMenuModel, get_n_items) {
  ZEND_PARSE_PARAMETERS_NONE();
  GMenuModel *self = PHPGTK_SELF(GMenuModel, G_TYPE_MENU_MODEL);
  RETURN_LONG(static_cast<zend_long>(g_menu_model_get_n_items(self)));
}

/**
 * Gtk4\GMenuModel::is_mutable(): bool
 *
 * Queries if $model is mutable.
 */
ZEND_METHOD(Gtk4_GMenuModel, is_mutable) {
  ZEND_PARSE_PARAMETERS_NONE();
  GMenuModel *self = PHPGTK_SELF(GMenuModel, G_TYPE_MENU_MODEL);
  RETURN_BOOL(g_menu_model_is_mutable(self));
}

/**
 * Gtk4\GMenuModel::items_changed(int $position, int $removed, int $added): void
 *
 * Requests emission of the #GMenuModel::items-changed signal on $model.
 */
ZEND_METHOD(Gtk4_GMenuModel, items_changed) {
  zend_long position;
  zend_long removed;
  zend_long added;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_LONG(position)
  Z_PARAM_LONG(removed)
  Z_PARAM_LONG(added)
  ZEND_PARSE_PARAMETERS_END();
  GMenuModel *self = PHPGTK_SELF(GMenuModel, G_TYPE_MENU_MODEL);
  if (!phpgtk::check_range<gint>(position, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<gint>(removed, 2)) RETURN_THROWS();
  if (!phpgtk::check_range<gint>(added, 3)) RETURN_THROWS();
  g_menu_model_items_changed(self, static_cast<gint>(position), static_cast<gint>(removed),
                             static_cast<gint>(added));
}

/**
 * public function get_item_attribute_value(int $item_index, string $attribute, ?string
 * $expected_type): mixed Queries the item at position $item_index in $model for the attribute
 * specified by $attribute.
 *
 * Generated but for the index check: GLib reads the item array unguarded, so an index outside
 * the model ends the process instead of raising. See the prelude.
 */
ZEND_METHOD(Gtk4_GMenuModel, get_item_attribute_value) {
  zend_long item_index;
  zend_string *attribute;
  zend_string *expected_type = nullptr;
  ZEND_PARSE_PARAMETERS_START(3, 3)
  Z_PARAM_LONG(item_index)
  Z_PARAM_STR(attribute)
  Z_PARAM_STR_OR_NULL(expected_type)
  ZEND_PARSE_PARAMETERS_END();
  GMenuModel *self = PHPGTK_SELF(GMenuModel, G_TYPE_MENU_MODEL);
  if (!phpgtk::check_range<gint>(item_index, 1)) RETURN_THROWS();
  if (!phpgtk::check_utf8(attribute, 2)) RETURN_THROWS();
  if (!menu_item_index_is_valid(self, item_index)) RETURN_THROWS();
  GVariantType *expected_type_t = nullptr;
  if (expected_type != nullptr) {
    if (!g_variant_type_string_is_valid(ZSTR_VAL(expected_type))) {
      zend_argument_value_error(3, "must be a valid GVariant type string, \"%s\" given",
                                ZSTR_VAL(expected_type));
      RETURN_THROWS();
    }
    expected_type_t = g_variant_type_new(ZSTR_VAL(expected_type));
  }
  GVariant *call_result = g_menu_model_get_item_attribute_value(
      self, static_cast<gint>(item_index), ZSTR_VAL(attribute), expected_type_t);
  if (expected_type_t != nullptr) g_variant_type_free(expected_type_t);
  GVariant *phpgtk_ret = call_result;
  if (phpgtk_ret == nullptr) RETURN_NULL();
  variant_to_php(phpgtk_ret, return_value);
  g_variant_unref(phpgtk_ret);
}

/**
 * public function get_item_link(int $item_index, string $link): ?GMenuModel
 * Queries the item at position $item_index in $model for the link specified by $link.
 *
 * Generated but for the index check: GLib reads the item array unguarded, so an index outside
 * the model ends the process instead of raising. See the prelude.
 */
ZEND_METHOD(Gtk4_GMenuModel, get_item_link) {
  zend_long item_index;
  zend_string *link;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_LONG(item_index)
  Z_PARAM_STR(link)
  ZEND_PARSE_PARAMETERS_END();
  GMenuModel *self = PHPGTK_SELF(GMenuModel, G_TYPE_MENU_MODEL);
  if (!phpgtk::check_range<gint>(item_index, 1)) RETURN_THROWS();
  if (!phpgtk::check_utf8(link, 2)) RETURN_THROWS();
  if (!menu_item_index_is_valid(self, item_index)) RETURN_THROWS();
  GMenuModel *phpgtk_ret =
      g_menu_model_get_item_link(self, static_cast<gint>(item_index), ZSTR_VAL(link));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

// vfunc thunks and installers: file-local, installed by class_init of a PHP subtype
namespace {

// vfunc thunk: G_MENU_MODEL_CLASS->get_item_link -> $this->vfunc_get_item_link() on a PHP subclass
GMenuModel *vfunc_thunk_get_item_link(GMenuModel *self, gint item_index, const gchar *link) {
  zval zself;
  zend_function *fn = EG(exception) == nullptr
                          ? subtype_vfunc(G_OBJECT(self), "vfunc_get_item_link", &zself)
                          : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    auto *native = G_MENU_MODEL_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->get_item_link != nullptr ? native->get_item_link(self, item_index, link)
                                            : nullptr;
  }
  std::array<zval, 2> args{};
  zval *argv = args.data();
  ZVAL_LONG(&argv[0], static_cast<zend_long>(item_index));
  if (link == nullptr) {
    ZVAL_NULL(&argv[1]);
  } else {
    ZVAL_STRING(&argv[1], link);
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  GMenuModel *result = nullptr;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 2, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    if (Z_TYPE(ret) == IS_OBJECT) {
      GObject *o = unwrap(&ret, G_TYPE_MENU_MODEL);
      result = o != nullptr ? G_MENU_MODEL(o) : nullptr;
      if (result != nullptr) g_object_ref(result);  // transfer full
    }
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GMenuModel::vfunc_get_item_link");
  return result;
}

// vfunc installer: G_MENU_MODEL_CLASS->get_item_link (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_get_item_link(gpointer klass) {
  G_MENU_MODEL_CLASS(klass)->get_item_link = vfunc_thunk_get_item_link;
}

// vfunc thunk: G_MENU_MODEL_CLASS->get_n_items -> $this->vfunc_get_n_items() on a PHP subclass
gint vfunc_thunk_get_n_items(GMenuModel *self) {
  zval zself;
  zend_function *fn = EG(exception) == nullptr
                          ? subtype_vfunc(G_OBJECT(self), "vfunc_get_n_items", &zself)
                          : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    auto *native = G_MENU_MODEL_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->get_n_items != nullptr ? native->get_n_items(self) : 0;
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  gint result = 0;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = static_cast<gint>(zval_get_long(&ret));
  }
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GMenuModel::vfunc_get_n_items");
  return result;
}

// vfunc installer: G_MENU_MODEL_CLASS->get_n_items (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_get_n_items(gpointer klass) {
  G_MENU_MODEL_CLASS(klass)->get_n_items = vfunc_thunk_get_n_items;
}

// vfunc thunk: G_MENU_MODEL_CLASS->is_mutable -> $this->vfunc_is_mutable() on a PHP subclass
gboolean vfunc_thunk_is_mutable(GMenuModel *self) {
  zval zself;
  zend_function *fn = EG(exception) == nullptr
                          ? subtype_vfunc(G_OBJECT(self), "vfunc_is_mutable", &zself)
                          : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    auto *native = G_MENU_MODEL_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->is_mutable != nullptr ? native->is_mutable(self) : FALSE;
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  gboolean result = FALSE;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = zend_is_true(&ret) ? TRUE : FALSE;
  }
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GMenuModel::vfunc_is_mutable");
  return result;
}

// vfunc installer: G_MENU_MODEL_CLASS->is_mutable (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_is_mutable(gpointer klass) {
  G_MENU_MODEL_CLASS(klass)->is_mutable = vfunc_thunk_is_mutable;
}

}  // namespace

/**
 * Gtk4\GMenuModel::vfunc_get_item_link(int $item_index, string $link): ?GMenuModel
 *
 * Native `get_item_link` (MenuModelClass.get_item_link): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_get_item_link()` from an override. Queries the item at position
 * $item_index in $model for the link specified by $link.
 */
ZEND_METHOD(Gtk4_GMenuModel, vfunc_get_item_link) {
  zend_long item_index;
  zend_string *link;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_LONG(item_index)
  Z_PARAM_STR(link)
  ZEND_PARSE_PARAMETERS_END();
  GMenuModel *self = PHPGTK_SELF(GMenuModel, G_TYPE_MENU_MODEL);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GMenuModel::vfunc_get_item_link(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = G_MENU_MODEL_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->get_item_link == nullptr) {
    RETURN_NULL();
  }
  if (!phpgtk::check_range<gint>(item_index, 1)) RETURN_THROWS();
  if (!phpgtk::check_utf8(link, 2)) RETURN_THROWS();
  GMenuModel *phpgtk_ret =
      klass->get_item_link(self, static_cast<gint>(item_index), ZSTR_VAL(link));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * Gtk4\GMenuModel::vfunc_get_n_items(): int
 *
 * Native `get_n_items` (MenuModelClass.get_n_items): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_get_n_items()` from an override. Query the number of items in
 * $model.
 */
ZEND_METHOD(Gtk4_GMenuModel, vfunc_get_n_items) {
  ZEND_PARSE_PARAMETERS_NONE();
  GMenuModel *self = PHPGTK_SELF(GMenuModel, G_TYPE_MENU_MODEL);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GMenuModel::vfunc_get_n_items(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = G_MENU_MODEL_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->get_n_items == nullptr) {
    RETURN_LONG(0);
  }
  RETURN_LONG(static_cast<zend_long>(klass->get_n_items(self)));
}

/**
 * Gtk4\GMenuModel::vfunc_is_mutable(): bool
 *
 * Native `is_mutable` (MenuModelClass.is_mutable): the GTK implementation below any PHP subclass,
 * for `parent::vfunc_is_mutable()` from an override. Queries if $model is mutable.
 */
ZEND_METHOD(Gtk4_GMenuModel, vfunc_is_mutable) {
  ZEND_PARSE_PARAMETERS_NONE();
  GMenuModel *self = PHPGTK_SELF(GMenuModel, G_TYPE_MENU_MODEL);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GMenuModel::vfunc_is_mutable(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = G_MENU_MODEL_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->is_mutable == nullptr) {
    RETURN_FALSE;
  }
  RETURN_BOOL(klass->is_mutable(self));
}

// MINIT: the vfunc thunks of GMenuModel (core/subtype.h).
void register_vfuncs_GMenuModel() {
  register_vfunc(G_TYPE_MENU_MODEL, "get_item_link", vfunc_install_get_item_link);
  register_vfunc(G_TYPE_MENU_MODEL, "get_n_items", vfunc_install_get_n_items);
  register_vfunc(G_TYPE_MENU_MODEL, "is_mutable", vfunc_install_is_mutable);
}
