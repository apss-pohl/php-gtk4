// GENERATED by gen/gir.php from Gio.DBusInterfaceInfo - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GDBusInterfaceInfo (boxed value type)
#include "php_gtk4.h"
#include "core/boxed.h"
#include "core/object.h"
#include <cstring>

using namespace phpgtk;

namespace {

const char *const fields[] = {"name", "methods", "signals", "properties", nullptr};

// Boxed field reader: the public scalar fields as PHP properties.
bool read(gpointer data, const char *field, zval *rv) {
  auto *value = static_cast<GDBusInterfaceInfo *>(data);
  if (strcmp(field, "name") == 0) {
    if (value->name == nullptr) {
      ZVAL_NULL(rv);
    } else {
      ZVAL_STRING(rv, value->name);
    }
    return true;
  }
  if (strcmp(field, "methods") == 0) {
    array_init(rv);
    for (auto **item = value->methods; item != nullptr && *item != nullptr; item++) {
      zval h;
      wrap_boxed(G_TYPE_DBUS_METHOD_INFO, *item, &h);
      add_next_index_zval(rv, &h);
    }
    return true;
  }
  if (strcmp(field, "signals") == 0) {
    array_init(rv);
    for (auto **item = value->signals; item != nullptr && *item != nullptr; item++) {
      zval h;
      wrap_boxed(G_TYPE_DBUS_SIGNAL_INFO, *item, &h);
      add_next_index_zval(rv, &h);
    }
    return true;
  }
  if (strcmp(field, "properties") == 0) {
    array_init(rv);
    for (auto **item = value->properties; item != nullptr && *item != nullptr; item++) {
      zval h;
      wrap_boxed(G_TYPE_DBUS_PROPERTY_INFO, *item, &h);
      add_next_index_zval(rv, &h);
    }
    return true;
  }
  return false;
}

// Boxed field writer: each field converts like a parameter of its type (core/boxed).
bool write(gpointer data, const char *field, zval *v) {
  (void)data;
  (void)field;
  (void)v;
  return false;
}

}  // namespace

/**
 * Gtk4\GDBusInterfaceInfo::__construct()
 *
 * GDBusInterfaceInfo values come from GTK, never from `new`.
 */
ZEND_METHOD(Gtk4_GDBusInterfaceInfo, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
}

/**
 * Gtk4\GDBusInterfaceInfo::cache_build(): void
 *
 * Builds a lookup-cache to speed up g_dbus_interface_info_lookup_method(),
 * g_dbus_interface_info_lookup_signal() and g_dbus_interface_info_lookup_property().
 */
ZEND_METHOD(Gtk4_GDBusInterfaceInfo, cache_build) {
  ZEND_PARSE_PARAMETERS_NONE();
  GDBusInterfaceInfo *self = PHPGTK_BOXED_SELF(GDBusInterfaceInfo);
  g_dbus_interface_info_cache_build(self);
}

/**
 * Gtk4\GDBusInterfaceInfo::cache_release(): void
 *
 * Decrements the usage count for the cache for $info built by g_dbus_interface_info_cache_build()
 * (if any) and frees the resources used by the cache if the usage count drops to zero.
 */
ZEND_METHOD(Gtk4_GDBusInterfaceInfo, cache_release) {
  ZEND_PARSE_PARAMETERS_NONE();
  GDBusInterfaceInfo *self = PHPGTK_BOXED_SELF(GDBusInterfaceInfo);
  g_dbus_interface_info_cache_release(self);
}

/**
 * Gtk4\GDBusInterfaceInfo::lookup_method(string $name): ?GDBusMethodInfo
 *
 * Looks up information about a method.
 */
ZEND_METHOD(Gtk4_GDBusInterfaceInfo, lookup_method) {
  zend_string *name;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(name)
  ZEND_PARSE_PARAMETERS_END();
  GDBusInterfaceInfo *self = PHPGTK_BOXED_SELF(GDBusInterfaceInfo);
  if (!phpgtk::check_utf8(name, 1)) RETURN_THROWS();
  GDBusMethodInfo *phpgtk_ret = g_dbus_interface_info_lookup_method(self, ZSTR_VAL(name));
  wrap_boxed(G_TYPE_DBUS_METHOD_INFO, phpgtk_ret, return_value);
}

/**
 * Gtk4\GDBusInterfaceInfo::lookup_property(string $name): ?GDBusPropertyInfo
 *
 * Looks up information about a property.
 */
ZEND_METHOD(Gtk4_GDBusInterfaceInfo, lookup_property) {
  zend_string *name;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(name)
  ZEND_PARSE_PARAMETERS_END();
  GDBusInterfaceInfo *self = PHPGTK_BOXED_SELF(GDBusInterfaceInfo);
  if (!phpgtk::check_utf8(name, 1)) RETURN_THROWS();
  GDBusPropertyInfo *phpgtk_ret = g_dbus_interface_info_lookup_property(self, ZSTR_VAL(name));
  wrap_boxed(G_TYPE_DBUS_PROPERTY_INFO, phpgtk_ret, return_value);
}

/**
 * Gtk4\GDBusInterfaceInfo::lookup_signal(string $name): ?GDBusSignalInfo
 *
 * Looks up information about a signal.
 */
ZEND_METHOD(Gtk4_GDBusInterfaceInfo, lookup_signal) {
  zend_string *name;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(name)
  ZEND_PARSE_PARAMETERS_END();
  GDBusInterfaceInfo *self = PHPGTK_BOXED_SELF(GDBusInterfaceInfo);
  if (!phpgtk::check_utf8(name, 1)) RETURN_THROWS();
  GDBusSignalInfo *phpgtk_ret = g_dbus_interface_info_lookup_signal(self, ZSTR_VAL(name));
  wrap_boxed(G_TYPE_DBUS_SIGNAL_INFO, phpgtk_ret, return_value);
}

namespace phpgtk {
// MINIT: bind the PHP class to G_TYPE_DBUS_INTERFACE_INFO with its field table.
void register_GDBusInterfaceInfo(zend_class_entry *ce) {
  register_boxed(BoxedClass{.type = G_TYPE_DBUS_INTERFACE_INFO,
                            .ce = ce,
                            .fields = fields,
                            .read = read,
                            .write = write});
}
}  // namespace phpgtk
