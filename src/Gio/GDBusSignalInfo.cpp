// GENERATED by gen/gir.php from Gio.DBusSignalInfo - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GDBusSignalInfo (boxed value type)
#include "php_gtk4.h"
#include "core/boxed.h"
#include "core/object.h"
#include <cstring>

using namespace phpgtk;

namespace {

const char *const fields[] = {"name", "args", nullptr};

// Boxed field reader: the public scalar fields as PHP properties.
bool read(gpointer data, const char *field, zval *rv) {
  auto *value = static_cast<GDBusSignalInfo *>(data);
  if (strcmp(field, "name") == 0) {
    if (value->name == nullptr) {
      ZVAL_NULL(rv);
    } else {
      ZVAL_STRING(rv, value->name);
    }
    return true;
  }
  if (strcmp(field, "args") == 0) {
    array_init(rv);
    for (auto **item = value->args; item != nullptr && *item != nullptr; item++) {
      zval h;
      wrap_boxed(G_TYPE_DBUS_ARG_INFO, *item, &h);
      add_next_index_zval(rv, &h);
    }
    return true;
  }
  return false;
}

// Boxed field writer: each field converts like a parameter of its type (core/boxed).
bool write(gpointer data, const char *field, zval *v) {
  (void)data;
  (void)field;
  (void)v;
  return false;
}

}  // namespace

/**
 * Gtk4\GDBusSignalInfo::__construct()
 *
 * GDBusSignalInfo values come from GTK, never from `new`.
 */
ZEND_METHOD(Gtk4_GDBusSignalInfo, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
}

namespace phpgtk {
// MINIT: bind the PHP class to G_TYPE_DBUS_SIGNAL_INFO with its field table.
void register_GDBusSignalInfo(zend_class_entry *ce) {
  register_boxed(BoxedClass{
      .type = G_TYPE_DBUS_SIGNAL_INFO, .ce = ce, .fields = fields, .read = read, .write = write});
}
}  // namespace phpgtk
