// GENERATED by gen/gir.php from Gio.Task - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GTask
#include "php_gtk4.h"
#include "core/object.h"
#include "core/gerror.h"
#include "core/callback.h"
#include <array>

using namespace phpgtk;

// GTask's "does it have a result yet?" - GLib keeps `result_set` private, and the two ways a
// result arrives need different answers: a task GLib created and handed back through a
// *_finish() callback is `completed` by then, while one PHP built and resolved synchronously is
// not (completed only turns true when the callback is dispatched). So the PHP-side return_*()
// methods leave a mark, and propagate_*() accepts either.
namespace {

// qdata key on a GTask whose result was set through this binding.
GQuark php_task_result_quark() {
  static GQuark q = 0;
  if (q == 0) q = g_quark_from_static_string("php-gtk4-task-result-set");
  return q;
}

// Whether asking for the result is meaningful; GLib CRITICALs and invents a zero otherwise.
bool task_has_result(GTask *task) {
  return g_task_get_completed(task) != FALSE ||
         g_object_get_qdata(G_OBJECT(task), php_task_result_quark()) != nullptr;
}

// Called by every bound return_*(): from here on the task has an answer.
void mark_task_result(GTask *task) {
  // NOLINTNEXTLINE(performance-no-int-to-ptr) GINT_TO_POINTER() is how GLib tags qdata
  g_object_set_qdata(G_OBJECT(task), php_task_result_quark(), GINT_TO_POINTER(1));
}

}  // namespace

namespace {
// AsyncReadyCallback trampoline for GTask::__construct(): wraps the C arguments, invokes the PHP
// callable once and releases it (async scope).
void cb_new_callback(GObject *source_object, GAsyncResult *res, gpointer data) {
  auto *cb = static_cast<Callback *>(data);
  std::array<zval, 2> args{};
  zval *argv = args.data();
  wrap(source_object != nullptr ? G_OBJECT(source_object) : nullptr, &argv[0]);
  wrap(res != nullptr ? G_OBJECT(res) : nullptr, &argv[1]);
  zval ret;
  callback_invoke(cb, 2, argv, &ret);
  if (!Z_ISUNDEF(ret)) zval_ptr_dtor(&ret);
  for (zval &arg : args) zval_ptr_dtor(&arg);
  callback_free(cb);
  callback_drain();
}
}  // namespace

/**
 * Gtk4\GTask::__construct(?GObject $source_object = null, ?GCancellable $cancellable = null,
 * ?callable $callback = null)
 *
 * Creates a #GTask acting on $source_object, which will eventually be used to invoke $callback in
 * the current [thread-default main context][g-main-context-push-thread-default].
 */
ZEND_METHOD(Gtk4_GTask, __construct) {
  zval *source_object = nullptr;
  zval *cancellable = nullptr;
  zend_fcall_info fci_callback = empty_fcall_info;
  zend_fcall_info_cache fcc_callback = empty_fcall_info_cache;
  ZEND_PARSE_PARAMETERS_START(0, 3)
  Z_PARAM_OPTIONAL
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(source_object, class_for_gtype(G_TYPE_OBJECT))
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  Z_PARAM_FUNC_OR_NULL(fci_callback, fcc_callback)
  ZEND_PARSE_PARAMETERS_END();
  GObject *source_object_o = nullptr;
  if (source_object != nullptr) {
    source_object_o = unwrap(source_object, G_TYPE_OBJECT);
    if (source_object_o == nullptr) RETURN_THROWS();
  }
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  Callback *cb_callback = ZEND_FCI_INITIALIZED(fci_callback)
                              ? callback_new(&fci_callback.function_name, "GTask::__construct")
                              : nullptr;
  GObject *obj =
      G_OBJECT(g_task_new(source_object_o != nullptr ? G_OBJECT(source_object_o) : nullptr,
                          cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr,
                          cb_callback != nullptr ? cb_new_callback : nullptr, cb_callback));
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * static Gtk4\GTask::is_valid(GAsyncResult $result, ?GObject $source_object = null): bool
 *
 * Checks that $result is a #GTask, and that $source_object is its source object (or that
 * $source_object is `null` and $result has no source object). This can be used in
 * g_return_if_fail() checks.
 */
ZEND_METHOD(Gtk4_GTask, is_valid) {
  zval *result;
  zval *source_object = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 2)
  Z_PARAM_OBJECT_OF_CLASS(result, class_for_gtype(G_TYPE_ASYNC_RESULT))
  Z_PARAM_OPTIONAL
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(source_object, class_for_gtype(G_TYPE_OBJECT))
  ZEND_PARSE_PARAMETERS_END();
  GObject *result_o = unwrap(result, G_TYPE_ASYNC_RESULT);
  if (result_o == nullptr) RETURN_THROWS();
  GObject *source_object_o = nullptr;
  if (source_object != nullptr) {
    source_object_o = unwrap(source_object, G_TYPE_OBJECT);
    if (source_object_o == nullptr) RETURN_THROWS();
  }
  RETURN_BOOL(g_task_is_valid(G_ASYNC_RESULT(result_o),
                              source_object_o != nullptr ? G_OBJECT(source_object_o) : nullptr));
}

/**
 * Gtk4\GTask::get_cancellable(): ?GCancellable
 *
 * Gets $task's #GCancellable
 */
ZEND_METHOD(Gtk4_GTask, get_cancellable) {
  ZEND_PARSE_PARAMETERS_NONE();
  GTask *self = PHPGTK_SELF(GTask, G_TYPE_TASK);
  GCancellable *phpgtk_ret = g_task_get_cancellable(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GTask::get_check_cancellable(): bool
 *
 * Gets $task's check-cancellable flag. See g_task_set_check_cancellable() for more details.
 */
ZEND_METHOD(Gtk4_GTask, get_check_cancellable) {
  ZEND_PARSE_PARAMETERS_NONE();
  GTask *self = PHPGTK_SELF(GTask, G_TYPE_TASK);
  RETURN_BOOL(g_task_get_check_cancellable(self));
}

/**
 * Gtk4\GTask::get_completed(): bool
 *
 * Gets the value of #GTask:completed. This changes from `false` to `true` after the task’s
 * callback is invoked, and will return `false` if called from inside the callback.
 */
ZEND_METHOD(Gtk4_GTask, get_completed) {
  ZEND_PARSE_PARAMETERS_NONE();
  GTask *self = PHPGTK_SELF(GTask, G_TYPE_TASK);
  RETURN_BOOL(g_task_get_completed(self));
}

/**
 * Gtk4\GTask::get_name(): ?string
 *
 * Gets $task’s name. See g_task_set_name().
 */
ZEND_METHOD(Gtk4_GTask, get_name) {
  ZEND_PARSE_PARAMETERS_NONE();
  GTask *self = PHPGTK_SELF(GTask, G_TYPE_TASK);
  PHPGTK_RETURN_STRING_OR_NULL(g_task_get_name(self));
}

/**
 * Gtk4\GTask::get_priority(): int
 *
 * Gets $task's priority
 */
ZEND_METHOD(Gtk4_GTask, get_priority) {
  ZEND_PARSE_PARAMETERS_NONE();
  GTask *self = PHPGTK_SELF(GTask, G_TYPE_TASK);
  RETURN_LONG(static_cast<zend_long>(g_task_get_priority(self)));
}

/**
 * Gtk4\GTask::get_return_on_cancel(): bool
 *
 * Gets $task's return-on-cancel flag. See g_task_set_return_on_cancel() for more details.
 */
ZEND_METHOD(Gtk4_GTask, get_return_on_cancel) {
  ZEND_PARSE_PARAMETERS_NONE();
  GTask *self = PHPGTK_SELF(GTask, G_TYPE_TASK);
  RETURN_BOOL(g_task_get_return_on_cancel(self));
}

/**
 * Gtk4\GTask::get_source_object(): ?GObject
 *
 * Gets the source object from $task. Like g_async_result_get_source_object(), but does not ref the
 * object.
 */
ZEND_METHOD(Gtk4_GTask, get_source_object) {
  ZEND_PARSE_PARAMETERS_NONE();
  GTask *self = PHPGTK_SELF(GTask, G_TYPE_TASK);
  GObject *phpgtk_ret = static_cast<GObject *>(g_task_get_source_object(self));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GTask::had_error(): bool
 *
 * Tests if $task resulted in an error.
 */
ZEND_METHOD(Gtk4_GTask, had_error) {
  ZEND_PARSE_PARAMETERS_NONE();
  GTask *self = PHPGTK_SELF(GTask, G_TYPE_TASK);
  RETURN_BOOL(g_task_had_error(self));
}

/**
 * Gtk4\GTask::return_error_if_cancelled(): bool
 *
 * Checks if $task's #GCancellable has been cancelled, and if so, sets $task's error accordingly
 * and completes the task (see g_task_return_pointer() for more discussion of exactly what this
 * means).
 */
ZEND_METHOD(Gtk4_GTask, return_error_if_cancelled) {
  ZEND_PARSE_PARAMETERS_NONE();
  GTask *self = PHPGTK_SELF(GTask, G_TYPE_TASK);
  RETURN_BOOL(g_task_return_error_if_cancelled(self));
}

/**
 * Gtk4\GTask::set_check_cancellable(bool $check_cancellable): void
 *
 * Sets or clears $task's check-cancellable flag. If this is `true` (the default), then
 * g_task_propagate_pointer(), etc, and g_task_had_error() will check the task's #GCancellable
 * first, and if it has been cancelled, then they will consider the task to have returned an
 * "Operation was cancelled" error (%G_IO_ERROR_CANCELLED), regardless of any other error or return
 * value the task may have had.
 */
ZEND_METHOD(Gtk4_GTask, set_check_cancellable) {
  bool check_cancellable;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(check_cancellable)
  ZEND_PARSE_PARAMETERS_END();
  GTask *self = PHPGTK_SELF(GTask, G_TYPE_TASK);
  g_task_set_check_cancellable(self, check_cancellable);
}

/**
 * Gtk4\GTask::set_name(?string $name): void
 *
 * Sets $task’s name, used in debugging and profiling. The name defaults to `null`.
 */
ZEND_METHOD(Gtk4_GTask, set_name) {
  zend_string *name = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR_OR_NULL(name)
  ZEND_PARSE_PARAMETERS_END();
  GTask *self = PHPGTK_SELF(GTask, G_TYPE_TASK);
  if (name != nullptr && !phpgtk::check_utf8(name, 1)) RETURN_THROWS();
  g_task_set_name(self, name != nullptr ? ZSTR_VAL(name) : nullptr);
}

/**
 * Gtk4\GTask::set_priority(int $priority): void
 *
 * Sets $task's priority. If you do not call this, it will default to %G_PRIORITY_DEFAULT.
 */
ZEND_METHOD(Gtk4_GTask, set_priority) {
  zend_long priority;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(priority)
  ZEND_PARSE_PARAMETERS_END();
  GTask *self = PHPGTK_SELF(GTask, G_TYPE_TASK);
  if (!phpgtk::check_range<gint>(priority, 1)) RETURN_THROWS();
  g_task_set_priority(self, static_cast<gint>(priority));
}

/**
 * Gtk4\GTask::set_return_on_cancel(bool $return_on_cancel): bool
 *
 * Sets or clears $task's return-on-cancel flag. This is only meaningful for tasks run via
 * g_task_run_in_thread() or g_task_run_in_thread_sync().
 */
ZEND_METHOD(Gtk4_GTask, set_return_on_cancel) {
  bool return_on_cancel;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(return_on_cancel)
  ZEND_PARSE_PARAMETERS_END();
  GTask *self = PHPGTK_SELF(GTask, G_TYPE_TASK);
  RETURN_BOOL(g_task_set_return_on_cancel(self, return_on_cancel));
}

/**
 * Gtk4\GTask::set_static_name(?string $name): void
 *
 * Sets $task’s name, used in debugging and profiling.
 */
ZEND_METHOD(Gtk4_GTask, set_static_name) {
  zend_string *name = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR_OR_NULL(name)
  ZEND_PARSE_PARAMETERS_END();
  GTask *self = PHPGTK_SELF(GTask, G_TYPE_TASK);
  if (name != nullptr && !phpgtk::check_utf8(name, 1)) RETURN_THROWS();
  g_task_set_static_name(self, name != nullptr ? ZSTR_VAL(name) : nullptr);
}

/**
 * public function propagate_boolean(): bool
 * The task's result, or a Gtk4\GError when it failed.
 *
 * A task has no result until something sets one; asking early is a GLib CRITICAL
 * (`task->result_set`) followed by a made-up zero, which is indistinguishable from a real
 * result. See the prelude for how "has a result" is decided.
 */
ZEND_METHOD(Gtk4_GTask, propagate_boolean) {
  ZEND_PARSE_PARAMETERS_NONE();
  GTask *self = PHPGTK_SELF(GTask, G_TYPE_TASK);
  if (!task_has_result(self)) {
    zend_throw_exception(spl_ce_LogicException,
                         "Gtk4\\GTask::propagate_boolean(): the task has no result yet", 0);
    RETURN_THROWS();
  }
  GError *error = nullptr;
  const gboolean ok = g_task_propagate_boolean(self, &error);
  if (ok == FALSE) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  RETURN_TRUE;
}

/**
 * public function propagate_int(): int
 * The task's result, or a Gtk4\GError when it failed.
 *
 * A task has no result until something sets one; asking early is a GLib CRITICAL
 * (`task->result_set`) followed by a made-up zero, which is indistinguishable from a real
 * result. See the prelude for how "has a result" is decided.
 */
ZEND_METHOD(Gtk4_GTask, propagate_int) {
  ZEND_PARSE_PARAMETERS_NONE();
  GTask *self = PHPGTK_SELF(GTask, G_TYPE_TASK);
  if (!task_has_result(self)) {
    zend_throw_exception(spl_ce_LogicException,
                         "Gtk4\\GTask::propagate_int(): the task has no result yet", 0);
    RETURN_THROWS();
  }
  GError *error = nullptr;
  const gssize value = g_task_propagate_int(self, &error);
  if (value == -1 && error != nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  RETURN_LONG(static_cast<zend_long>(value));
}

/**
 * public function return_boolean(bool $result): void
 * Set the task's result and schedule its callback.
 *
 * Marks the task as having an answer so propagate_boolean() can tell it apart from a fresh one
 * (see the prelude).
 */
ZEND_METHOD(Gtk4_GTask, return_boolean) {
  bool result = false;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(result)
  ZEND_PARSE_PARAMETERS_END();
  GTask *self = PHPGTK_SELF(GTask, G_TYPE_TASK);
  if (task_has_result(self)) {  // GLib asserts '!task->ever_returned' and drops the second result
    zend_throw_exception_ex(spl_ce_LogicException, 0, "%s(): the task already has a result",
                            ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  g_task_return_boolean(self, static_cast<gboolean>(result));
  mark_task_result(self);
}

/**
 * public function return_error(GError $error): void
 * Fail the task with $error and schedule its callback.
 *
 * Marks the task as having an answer so propagate_*() can tell it apart from a fresh one
 * (see the prelude).
 */
ZEND_METHOD(Gtk4_GTask, return_error) {
  zval *error;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(error, ce_GError)
  ZEND_PARSE_PARAMETERS_END();
  GTask *self = PHPGTK_SELF(GTask, G_TYPE_TASK);
  if (task_has_result(self)) {  // GLib asserts '!task->ever_returned' and drops the second result
    zend_throw_exception_ex(spl_ce_LogicException, 0, "%s(): the task already has a result",
                            ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  GError *error_e = gerror_from_php(error);
  g_task_return_error(self, error_e);  // takes ownership
  mark_task_result(self);
}

/**
 * public function return_int(int $result): void
 * Set the task's result and schedule its callback.
 *
 * Marks the task as having an answer so propagate_int() can tell it apart from a fresh one
 * (see the prelude).
 */
ZEND_METHOD(Gtk4_GTask, return_int) {
  zend_long result = 0;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(result)
  ZEND_PARSE_PARAMETERS_END();
  GTask *self = PHPGTK_SELF(GTask, G_TYPE_TASK);
  if (task_has_result(self)) {  // GLib asserts '!task->ever_returned' and drops the second result
    zend_throw_exception_ex(spl_ce_LogicException, 0, "%s(): the task already has a result",
                            ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  g_task_return_int(self, static_cast<gssize>(result));
  mark_task_result(self);
}
