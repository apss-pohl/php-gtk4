// GENERATED by gen/gir.php from Gio.Application - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GApplication
#include "php_gtk4.h"
#include "core/object.h"
#include "core/gerror.h"
#include "core/variant.h"
#include "core/subtype.h"
#include <array>

using namespace phpgtk;

// run(): argv handling and the running-loop registry for ExceptionMode::Rethrow.
#include "core/error.h"
#include "core/globals.h"
#include "core/mainloop.h"

#include <string>
#include <vector>

namespace {
// QuitFn for the running-loop registry: g_application_quit() ends run().
void quit_app(gpointer data) {
  g_application_quit(G_APPLICATION(data));
}
}  // namespace

/**
 * Gtk4\GApplication::__construct(?string $application_id, int $flags)
 *
 * Creates a new #GApplication instance.
 */
ZEND_METHOD(Gtk4_GApplication, __construct) {
  zend_string *application_id = nullptr;
  zend_long flags;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_STR_OR_NULL(application_id)
  Z_PARAM_LONG(flags)
  ZEND_PARSE_PARAMETERS_END();
  if (application_id != nullptr && !phpgtk::check_utf8(application_id, 1)) RETURN_THROWS();
  if (!phpgtk::check_flags(G_TYPE_APPLICATION_FLAGS, flags, 2)) RETURN_THROWS();
  GObject *obj = subtype_new(ZEND_THIS, "application-id",
                             application_id != nullptr ? ZSTR_VAL(application_id) : nullptr,
                             "flags", static_cast<GApplicationFlags>(flags), nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(g_application_new(application_id != nullptr ? ZSTR_VAL(application_id) : nullptr,
                                     static_cast<GApplicationFlags>(flags)));
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * static Gtk4\GApplication::get_default(): ?GApplication
 *
 * Returns the default #GApplication instance for this process.
 */
ZEND_METHOD(Gtk4_GApplication, get_default) {
  ZEND_PARSE_PARAMETERS_NONE();
  GApplication *phpgtk_ret = g_application_get_default();
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * static Gtk4\GApplication::id_is_valid(string $application_id): bool
 *
 * Checks if $application_id is a valid application identifier.
 */
ZEND_METHOD(Gtk4_GApplication, id_is_valid) {
  zend_string *application_id;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(application_id)
  ZEND_PARSE_PARAMETERS_END();
  if (!phpgtk::check_utf8(application_id, 1)) RETURN_THROWS();
  RETURN_BOOL(g_application_id_is_valid(ZSTR_VAL(application_id)));
}

/**
 * Gtk4\GApplication::activate(): void
 *
 * Activates the application.
 */
ZEND_METHOD(Gtk4_GApplication, activate) {
  ZEND_PARSE_PARAMETERS_NONE();
  GApplication *self = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  g_application_activate(self);
}

/**
 * Gtk4\GApplication::add_main_option(string $long_name, int $short_name, int $flags, int $arg,
 * string $description, ?string $arg_description): void
 *
 * Add an option to be handled by $application.
 */
ZEND_METHOD(Gtk4_GApplication, add_main_option) {
  zend_string *long_name;
  zend_long short_name;
  zend_long flags;
  zend_long arg;
  zend_string *description;
  zend_string *arg_description = nullptr;
  ZEND_PARSE_PARAMETERS_START(6, 6)
  Z_PARAM_STR(long_name)
  Z_PARAM_LONG(short_name)
  Z_PARAM_LONG(flags)
  Z_PARAM_LONG(arg)
  Z_PARAM_STR(description)
  Z_PARAM_STR_OR_NULL(arg_description)
  ZEND_PARSE_PARAMETERS_END();
  GApplication *self = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  if (!phpgtk::check_utf8(long_name, 1)) RETURN_THROWS();
  if (!phpgtk::check_range<char>(short_name, 2)) RETURN_THROWS();
  if (!phpgtk::check_utf8(description, 5)) RETURN_THROWS();
  if (arg_description != nullptr && !phpgtk::check_utf8(arg_description, 6)) RETURN_THROWS();
  const bool precondition_0 = short_name == 0 || g_ascii_isalnum(static_cast<gchar>(short_name));
  if (!precondition_0) {
    zend_argument_value_error(2, "must be 0 or a letter or digit");
    RETURN_THROWS();
  }
  g_application_add_main_option(self, ZSTR_VAL(long_name), static_cast<char>(short_name),
                                static_cast<GOptionFlags>(flags), static_cast<GOptionArg>(arg),
                                ZSTR_VAL(description),
                                arg_description != nullptr ? ZSTR_VAL(arg_description) : nullptr);
}

/**
 * Gtk4\GApplication::bind_busy_property(GObject $object, string $property): void
 *
 * Marks $application as busy (see g_application_mark_busy()) while $property on $object is `true`.
 */
ZEND_METHOD(Gtk4_GApplication, bind_busy_property) {
  zval *object;
  zend_string *property;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(object, class_for_gtype(G_TYPE_OBJECT))
  Z_PARAM_STR(property)
  ZEND_PARSE_PARAMETERS_END();
  GApplication *self = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  GObject *object_o = unwrap(object, G_TYPE_OBJECT);
  if (object_o == nullptr) RETURN_THROWS();
  if (!phpgtk::check_utf8(property, 2)) RETURN_THROWS();
  const bool precondition_0 = phpgtk::has_boolean_property(object_o, ZSTR_VAL(property));
  if (!precondition_0) {
    zend_argument_value_error(2, "must name a boolean property of the object");
    RETURN_THROWS();
  }
  g_application_bind_busy_property(self, G_OBJECT(object_o), ZSTR_VAL(property));
}

/**
 * Gtk4\GApplication::get_application_id(): ?string
 *
 * Gets the unique identifier for $application.
 */
ZEND_METHOD(Gtk4_GApplication, get_application_id) {
  ZEND_PARSE_PARAMETERS_NONE();
  GApplication *self = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  PHPGTK_RETURN_STRING_OR_NULL(g_application_get_application_id(self));
}

/**
 * Gtk4\GApplication::get_flags(): int
 *
 * Gets the flags for $application.
 */
ZEND_METHOD(Gtk4_GApplication, get_flags) {
  ZEND_PARSE_PARAMETERS_NONE();
  GApplication *self = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  RETURN_LONG(static_cast<zend_long>(g_application_get_flags(self)));
}

/**
 * Gtk4\GApplication::get_inactivity_timeout(): int
 *
 * Gets the current inactivity timeout for the application.
 */
ZEND_METHOD(Gtk4_GApplication, get_inactivity_timeout) {
  ZEND_PARSE_PARAMETERS_NONE();
  GApplication *self = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  RETURN_LONG(static_cast<zend_long>(g_application_get_inactivity_timeout(self)));
}

/**
 * Gtk4\GApplication::get_is_busy(): bool
 *
 * Gets the application's current busy state, as set through g_application_mark_busy() or
 * g_application_bind_busy_property().
 */
ZEND_METHOD(Gtk4_GApplication, get_is_busy) {
  ZEND_PARSE_PARAMETERS_NONE();
  GApplication *self = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  RETURN_BOOL(g_application_get_is_busy(self));
}

/**
 * Gtk4\GApplication::get_is_registered(): bool
 *
 * Checks if $application is registered.
 */
ZEND_METHOD(Gtk4_GApplication, get_is_registered) {
  ZEND_PARSE_PARAMETERS_NONE();
  GApplication *self = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  RETURN_BOOL(g_application_get_is_registered(self));
}

/**
 * Gtk4\GApplication::get_resource_base_path(): ?string
 *
 * Gets the resource base path of $application.
 */
ZEND_METHOD(Gtk4_GApplication, get_resource_base_path) {
  ZEND_PARSE_PARAMETERS_NONE();
  GApplication *self = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  PHPGTK_RETURN_STRING_OR_NULL(g_application_get_resource_base_path(self));
}

/**
 * Gtk4\GApplication::get_version(): ?string
 *
 * Gets the version of $application.
 */
ZEND_METHOD(Gtk4_GApplication, get_version) {
  ZEND_PARSE_PARAMETERS_NONE();
  GApplication *self = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  PHPGTK_RETURN_STRING_OR_NULL(g_application_get_version(self));
}

/**
 * Gtk4\GApplication::hold(): void
 *
 * Increases the use count of $application.
 */
ZEND_METHOD(Gtk4_GApplication, hold) {
  ZEND_PARSE_PARAMETERS_NONE();
  GApplication *self = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  g_application_hold(self);
}

/**
 * Gtk4\GApplication::mark_busy(): void
 *
 * Increases the busy count of $application.
 */
ZEND_METHOD(Gtk4_GApplication, mark_busy) {
  ZEND_PARSE_PARAMETERS_NONE();
  GApplication *self = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  g_application_mark_busy(self);
}

/**
 * Gtk4\GApplication::quit(): void
 *
 * Immediately quits the application.
 */
ZEND_METHOD(Gtk4_GApplication, quit) {
  ZEND_PARSE_PARAMETERS_NONE();
  GApplication *self = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  g_application_quit(self);
}

/**
 * Gtk4\GApplication::register(?GCancellable $cancellable): bool
 *
 * Attempts registration of the application.
 */
ZEND_METHOD(Gtk4_GApplication, register) {
  zval *cancellable = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(cancellable, class_for_gtype(G_TYPE_CANCELLABLE))
  ZEND_PARSE_PARAMETERS_END();
  GApplication *self = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  GObject *cancellable_o = nullptr;
  if (cancellable != nullptr) {
    cancellable_o = unwrap(cancellable, G_TYPE_CANCELLABLE);
    if (cancellable_o == nullptr) RETURN_THROWS();
  }
  GError *error = nullptr;
  const gboolean ok = g_application_register(
      self, cancellable_o != nullptr ? G_CANCELLABLE(cancellable_o) : nullptr, &error);
  if (error != nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  RETURN_BOOL(ok);
}

/**
 * Gtk4\GApplication::release(): void
 *
 * Decrease the use count of $application.
 */
ZEND_METHOD(Gtk4_GApplication, release) {
  ZEND_PARSE_PARAMETERS_NONE();
  GApplication *self = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  g_application_release(self);
}

/**
 * Gtk4\GApplication::send_notification(?string $id, GNotification $notification): void
 *
 * Sends a notification on behalf of $application to the desktop shell. There is no guarantee that
 * the notification is displayed immediately, or even at all.
 */
ZEND_METHOD(Gtk4_GApplication, send_notification) {
  zend_string *id = nullptr;
  zval *notification;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_STR_OR_NULL(id)
  Z_PARAM_OBJECT_OF_CLASS(notification, class_for_gtype(G_TYPE_NOTIFICATION))
  ZEND_PARSE_PARAMETERS_END();
  GApplication *self = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  if (id != nullptr && !phpgtk::check_utf8(id, 1)) RETURN_THROWS();
  GObject *notification_o = unwrap(notification, G_TYPE_NOTIFICATION);
  if (notification_o == nullptr) RETURN_THROWS();
  const bool state_holds = g_application_get_is_registered(self) == TRUE;
  if (!state_holds) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "%s(): the application is not registered yet - notifications exist from `startup` on",
        ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  g_application_send_notification(self, id != nullptr ? ZSTR_VAL(id) : nullptr,
                                  G_NOTIFICATION(notification_o));
}

/**
 * Gtk4\GApplication::set_application_id(?string $application_id): void
 *
 * Sets the unique identifier for $application.
 */
ZEND_METHOD(Gtk4_GApplication, set_application_id) {
  zend_string *application_id = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR_OR_NULL(application_id)
  ZEND_PARSE_PARAMETERS_END();
  GApplication *self = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  if (application_id != nullptr && !phpgtk::check_utf8(application_id, 1)) RETURN_THROWS();
  if (application_id != nullptr && !(g_application_id_is_valid(ZSTR_VAL(application_id)))) {
    zend_argument_value_error(1, "must be a valid application id (reverse-DNS, org.example.App)");
    RETURN_THROWS();
  }
  g_application_set_application_id(self,
                                   application_id != nullptr ? ZSTR_VAL(application_id) : nullptr);
}

/**
 * Gtk4\GApplication::set_default(): void
 *
 * Sets or unsets the default application for the process, as returned by
 * g_application_get_default().
 */
ZEND_METHOD(Gtk4_GApplication, set_default) {
  ZEND_PARSE_PARAMETERS_NONE();
  GApplication *self = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  g_application_set_default(self);
}

/**
 * Gtk4\GApplication::set_flags(int $flags): void
 *
 * Sets the flags for $application.
 */
ZEND_METHOD(Gtk4_GApplication, set_flags) {
  zend_long flags;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(flags)
  ZEND_PARSE_PARAMETERS_END();
  GApplication *self = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  if (!phpgtk::check_flags(G_TYPE_APPLICATION_FLAGS, flags, 1)) RETURN_THROWS();
  g_application_set_flags(self, static_cast<GApplicationFlags>(flags));
}

/**
 * Gtk4\GApplication::set_inactivity_timeout(int $inactivity_timeout): void
 *
 * Sets the current inactivity timeout for the application.
 */
ZEND_METHOD(Gtk4_GApplication, set_inactivity_timeout) {
  zend_long inactivity_timeout;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(inactivity_timeout)
  ZEND_PARSE_PARAMETERS_END();
  GApplication *self = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  if (!phpgtk::check_range<guint>(inactivity_timeout, 1)) RETURN_THROWS();
  g_application_set_inactivity_timeout(self, static_cast<guint>(inactivity_timeout));
}

/**
 * Gtk4\GApplication::set_option_context_description(?string $description): void
 *
 * Adds a description to the $application option context.
 */
ZEND_METHOD(Gtk4_GApplication, set_option_context_description) {
  zend_string *description = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR_OR_NULL(description)
  ZEND_PARSE_PARAMETERS_END();
  GApplication *self = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  if (description != nullptr && !phpgtk::check_utf8(description, 1)) RETURN_THROWS();
  g_application_set_option_context_description(
      self, description != nullptr ? ZSTR_VAL(description) : nullptr);
}

/**
 * Gtk4\GApplication::set_option_context_parameter_string(?string $parameter_string): void
 *
 * Sets the parameter string to be used by the commandline handling of $application.
 */
ZEND_METHOD(Gtk4_GApplication, set_option_context_parameter_string) {
  zend_string *parameter_string = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR_OR_NULL(parameter_string)
  ZEND_PARSE_PARAMETERS_END();
  GApplication *self = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  if (parameter_string != nullptr && !phpgtk::check_utf8(parameter_string, 1)) RETURN_THROWS();
  g_application_set_option_context_parameter_string(
      self, parameter_string != nullptr ? ZSTR_VAL(parameter_string) : nullptr);
}

/**
 * Gtk4\GApplication::set_option_context_summary(?string $summary): void
 *
 * Adds a summary to the $application option context.
 */
ZEND_METHOD(Gtk4_GApplication, set_option_context_summary) {
  zend_string *summary = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR_OR_NULL(summary)
  ZEND_PARSE_PARAMETERS_END();
  GApplication *self = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  if (summary != nullptr && !phpgtk::check_utf8(summary, 1)) RETURN_THROWS();
  g_application_set_option_context_summary(self, summary != nullptr ? ZSTR_VAL(summary) : nullptr);
}

/**
 * Gtk4\GApplication::set_resource_base_path(?string $resource_path): void
 *
 * Sets (or unsets) the base resource path of $application.
 */
ZEND_METHOD(Gtk4_GApplication, set_resource_base_path) {
  zend_string *resource_path = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR_OR_NULL(resource_path)
  ZEND_PARSE_PARAMETERS_END();
  GApplication *self = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  if (resource_path != nullptr && !phpgtk::check_utf8(resource_path, 1)) RETURN_THROWS();
  if (resource_path != nullptr && !(ZSTR_VAL(resource_path)[0] == '/')) {
    zend_argument_value_error(1, "must be an absolute resource path");
    RETURN_THROWS();
  }
  g_application_set_resource_base_path(
      self, resource_path != nullptr ? ZSTR_VAL(resource_path) : nullptr);
}

/**
 * Gtk4\GApplication::set_version(string $version): void
 *
 * Sets the version number of $application. This will be used to implement a `--version` command
 * line argument
 */
ZEND_METHOD(Gtk4_GApplication, set_version) {
  zend_string *version;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(version)
  ZEND_PARSE_PARAMETERS_END();
  GApplication *self = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  if (!phpgtk::check_utf8(version, 1)) RETURN_THROWS();
  g_application_set_version(self, ZSTR_VAL(version));
}

/**
 * Gtk4\GApplication::unbind_busy_property(GObject $object, string $property): void
 *
 * Destroys a binding between $property and the busy state of $application that was previously
 * created with g_application_bind_busy_property().
 */
ZEND_METHOD(Gtk4_GApplication, unbind_busy_property) {
  zval *object;
  zend_string *property;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(object, class_for_gtype(G_TYPE_OBJECT))
  Z_PARAM_STR(property)
  ZEND_PARSE_PARAMETERS_END();
  GApplication *self = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  GObject *object_o = unwrap(object, G_TYPE_OBJECT);
  if (object_o == nullptr) RETURN_THROWS();
  if (!phpgtk::check_utf8(property, 2)) RETURN_THROWS();
  g_application_unbind_busy_property(self, G_OBJECT(object_o), ZSTR_VAL(property));
}

/**
 * Gtk4\GApplication::unmark_busy(): void
 *
 * Decreases the busy count of $application.
 */
ZEND_METHOD(Gtk4_GApplication, unmark_busy) {
  ZEND_PARSE_PARAMETERS_NONE();
  GApplication *self = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  g_application_unmark_busy(self);
}

/**
 * Gtk4\GApplication::withdraw_notification(string $id): void
 *
 * Withdraws a notification that was sent with g_application_send_notification().
 */
ZEND_METHOD(Gtk4_GApplication, withdraw_notification) {
  zend_string *id;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(id)
  ZEND_PARSE_PARAMETERS_END();
  GApplication *self = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  if (!phpgtk::check_utf8(id, 1)) RETURN_THROWS();
  const bool state_holds = g_application_get_is_registered(self) == TRUE;
  if (!state_holds) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "%s(): the application is not registered yet - notifications exist from `startup` on",
        ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  g_application_withdraw_notification(self, ZSTR_VAL(id));
}

/**
 * public function get_dbus_object_path(): ?string
 * The D-Bus object path the application exports its actions on, or null when it has none.
 *
 * GLib fills it in while registering, and CRITICALs when asked before that. Registration
 * happens on `run()` / `register()`, so "not registered yet" is a state, not an absence.
 */
ZEND_METHOD(Gtk4_GApplication, get_dbus_object_path) {
  ZEND_PARSE_PARAMETERS_NONE();
  GApplication *self = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  if (g_application_get_is_registered(self) == FALSE) {
    zend_throw_exception(spl_ce_LogicException,
                         "Gtk4\\GApplication::get_dbus_object_path(): the application is not "
                         "registered yet",
                         0);
    RETURN_THROWS();
  }
  PHPGTK_RETURN_STRING_OR_NULL(g_application_get_dbus_object_path(self));
}

/**
 * public function get_is_remote(): bool
 * Whether this process is the remote end of an already-running primary instance.
 *
 * Only decided by registration; GLib CRITICALs when asked before that and answers false, which
 * is indistinguishable from "this is the primary instance".
 */
ZEND_METHOD(Gtk4_GApplication, get_is_remote) {
  ZEND_PARSE_PARAMETERS_NONE();
  GApplication *self = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  if (g_application_get_is_registered(self) == FALSE) {
    zend_throw_exception(spl_ce_LogicException,
                         "Gtk4\\GApplication::get_is_remote(): the application is not registered "
                         "yet, so there is no primary instance to be remote from",
                         0);
    RETURN_THROWS();
  }
  RETURN_BOOL(g_application_get_is_remote(self));
}

/**
 * public function run(array $argv = []): int
 * Run the application (emits `startup`, `activate`, ...) until the last window closes or
 * {@see quit()} is called. $argv is what GApplication parses for command-line handling.
 */
ZEND_METHOD(Gtk4_GApplication, run) {
  HashTable *argv_ht = nullptr;
  ZEND_PARSE_PARAMETERS_START(0, 1)
  Z_PARAM_OPTIONAL
  Z_PARAM_ARRAY_HT(argv_ht)
  ZEND_PARSE_PARAMETERS_END();
  if (!assert_gui_thread("GApplication::run()")) RETURN_THROWS();
  GApplication *app = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  // argv must stay valid for the whole run: copy the strings.
  std::vector<std::string> strings;
  if (argv_ht != nullptr) {
    zval *item;
    // NOLINTNEXTLINE(readability-math-missing-parentheses) Zend macro expansion
    ZEND_HASH_FOREACH_VAL(argv_ht, item) {
      zend_string *s = zval_get_string(item);
      strings.emplace_back(ZSTR_VAL(s), ZSTR_LEN(s));
      zend_string_release(s);
    }
    ZEND_HASH_FOREACH_END();
  }
  std::vector<char *> argv;
  argv.reserve(strings.size() + 1);
  for (auto &s : strings) argv.push_back(s.data());
  argv.push_back(nullptr);
  int status = 0;
  {
    RunningLoop running(quit_app, app);
    status = g_application_run(app, static_cast<int>(strings.size()),
                               strings.empty() ? nullptr : argv.data());
  }
  if (rethrow_parked_exception() || EG(exception) != nullptr) RETURN_THROWS();  // Rethrow mode
  RETURN_LONG(status);
}

// vfunc thunks and installers: file-local, installed by class_init of a PHP subtype
namespace {

// vfunc thunk: G_APPLICATION_CLASS->activate -> $this->vfunc_activate() on a PHP subclass
void vfunc_thunk_activate(GApplication *self) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_activate", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = G_APPLICATION_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->activate != nullptr) native->activate(self);
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GApplication::vfunc_activate");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: G_APPLICATION_CLASS->activate (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_activate(gpointer klass) {
  G_APPLICATION_CLASS(klass)->activate = vfunc_thunk_activate;
}

// vfunc thunk: G_APPLICATION_CLASS->after_emit -> $this->vfunc_after_emit() on a PHP subclass
void vfunc_thunk_after_emit(GApplication *self, GVariant *platform_data) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_after_emit", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = G_APPLICATION_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->after_emit != nullptr) native->after_emit(self, platform_data);
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 1> args{};
  zval *argv = args.data();
  if (platform_data == nullptr) {
    ZVAL_NULL(&argv[0]);
  } else {
    variant_to_php(platform_data, &argv[0]);
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GApplication::vfunc_after_emit");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: G_APPLICATION_CLASS->after_emit (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_after_emit(gpointer klass) {
  G_APPLICATION_CLASS(klass)->after_emit = vfunc_thunk_after_emit;
}

// vfunc thunk: G_APPLICATION_CLASS->before_emit -> $this->vfunc_before_emit() on a PHP subclass
void vfunc_thunk_before_emit(GApplication *self, GVariant *platform_data) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_before_emit", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = G_APPLICATION_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->before_emit != nullptr) native->before_emit(self, platform_data);
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 1> args{};
  zval *argv = args.data();
  if (platform_data == nullptr) {
    ZVAL_NULL(&argv[0]);
  } else {
    variant_to_php(platform_data, &argv[0]);
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GApplication::vfunc_before_emit");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: G_APPLICATION_CLASS->before_emit (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_before_emit(gpointer klass) {
  G_APPLICATION_CLASS(klass)->before_emit = vfunc_thunk_before_emit;
}

// vfunc thunk: G_APPLICATION_CLASS->name_lost -> $this->vfunc_name_lost() on a PHP subclass
gboolean vfunc_thunk_name_lost(GApplication *self) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_name_lost", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = G_APPLICATION_CLASS(subtype_native_class(G_OBJECT(self)));
    return native->name_lost != nullptr ? native->name_lost(self) : FALSE;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  zval ret;
  ZVAL_UNDEF(&ret);
  gboolean result = FALSE;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = zend_is_true(&ret) ? TRUE : FALSE;
  }
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GApplication::vfunc_name_lost");
  zend_exception_restore();  // the parked one, previous of whatever this threw
  return result;
}

// vfunc installer: G_APPLICATION_CLASS->name_lost (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_name_lost(gpointer klass) {
  G_APPLICATION_CLASS(klass)->name_lost = vfunc_thunk_name_lost;
}

// vfunc thunk: G_APPLICATION_CLASS->quit_mainloop -> $this->vfunc_quit_mainloop() on a PHP subclass
void vfunc_thunk_quit_mainloop(GApplication *self) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_quit_mainloop", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = G_APPLICATION_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->quit_mainloop != nullptr) native->quit_mainloop(self);
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GApplication::vfunc_quit_mainloop");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: G_APPLICATION_CLASS->quit_mainloop (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_quit_mainloop(gpointer klass) {
  G_APPLICATION_CLASS(klass)->quit_mainloop = vfunc_thunk_quit_mainloop;
}

// vfunc thunk: G_APPLICATION_CLASS->run_mainloop -> $this->vfunc_run_mainloop() on a PHP subclass
void vfunc_thunk_run_mainloop(GApplication *self) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_run_mainloop", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = G_APPLICATION_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->run_mainloop != nullptr) native->run_mainloop(self);
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GApplication::vfunc_run_mainloop");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: G_APPLICATION_CLASS->run_mainloop (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_run_mainloop(gpointer klass) {
  G_APPLICATION_CLASS(klass)->run_mainloop = vfunc_thunk_run_mainloop;
}

// vfunc thunk: G_APPLICATION_CLASS->shutdown -> $this->vfunc_shutdown() on a PHP subclass
void vfunc_thunk_shutdown(GApplication *self) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_shutdown", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = G_APPLICATION_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->shutdown != nullptr) native->shutdown(self);
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GApplication::vfunc_shutdown");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: G_APPLICATION_CLASS->shutdown (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_shutdown(gpointer klass) {
  G_APPLICATION_CLASS(klass)->shutdown = vfunc_thunk_shutdown;
}

// vfunc thunk: G_APPLICATION_CLASS->startup -> $this->vfunc_startup() on a PHP subclass
void vfunc_thunk_startup(GApplication *self) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_startup", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = G_APPLICATION_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->startup != nullptr) native->startup(self);
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GApplication::vfunc_startup");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: G_APPLICATION_CLASS->startup (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_startup(gpointer klass) {
  G_APPLICATION_CLASS(klass)->startup = vfunc_thunk_startup;
}

}  // namespace

/**
 * Gtk4\GApplication::vfunc_activate(): void
 *
 * Native `activate` (ApplicationClass.activate): the GTK implementation below any PHP subclass,
 * for `parent::vfunc_activate()` from an override. Activates the application.
 */
ZEND_METHOD(Gtk4_GApplication, vfunc_activate) {
  ZEND_PARSE_PARAMETERS_NONE();
  GApplication *self = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GApplication::vfunc_activate(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = G_APPLICATION_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->activate == nullptr) {
    return;
  }
  klass->activate(self);
}

/**
 * Gtk4\GApplication::vfunc_after_emit(mixed $platform_data = null): void
 *
 * Native `after_emit` (ApplicationClass.after_emit): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_after_emit()` from an override. invoked on the primary instance
 * after 'activate', 'open', 'command-line' or any action invocation, gets the 'platform data' from
 * the calling instance
 */
ZEND_METHOD(Gtk4_GApplication, vfunc_after_emit) {
  zval *platform_data = nullptr;
  ZEND_PARSE_PARAMETERS_START(0, 1)
  Z_PARAM_OPTIONAL
  Z_PARAM_ZVAL(platform_data)
  ZEND_PARSE_PARAMETERS_END();
  GApplication *self = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GApplication::vfunc_after_emit(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = G_APPLICATION_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->after_emit == nullptr) {
    return;
  }
  GVariant *platform_data_v = nullptr;
  if (platform_data != nullptr && Z_TYPE_P(platform_data) != IS_NULL) {
    platform_data_v = php_to_variant(platform_data, nullptr);
    if (platform_data_v == nullptr) RETURN_THROWS();
    g_variant_ref_sink(platform_data_v);
  }
  klass->after_emit(self, platform_data_v);
  if (platform_data_v != nullptr) g_variant_unref(platform_data_v);
}

/**
 * Gtk4\GApplication::vfunc_before_emit(mixed $platform_data = null): void
 *
 * Native `before_emit` (ApplicationClass.before_emit): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_before_emit()` from an override. invoked on the primary instance
 * before 'activate', 'open', 'command-line' or any action invocation, gets the 'platform data'
 * from the calling instance
 */
ZEND_METHOD(Gtk4_GApplication, vfunc_before_emit) {
  zval *platform_data = nullptr;
  ZEND_PARSE_PARAMETERS_START(0, 1)
  Z_PARAM_OPTIONAL
  Z_PARAM_ZVAL(platform_data)
  ZEND_PARSE_PARAMETERS_END();
  GApplication *self = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GApplication::vfunc_before_emit(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = G_APPLICATION_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->before_emit == nullptr) {
    return;
  }
  GVariant *platform_data_v = nullptr;
  if (platform_data != nullptr && Z_TYPE_P(platform_data) != IS_NULL) {
    platform_data_v = php_to_variant(platform_data, nullptr);
    if (platform_data_v == nullptr) RETURN_THROWS();
    g_variant_ref_sink(platform_data_v);
  }
  klass->before_emit(self, platform_data_v);
  if (platform_data_v != nullptr) g_variant_unref(platform_data_v);
}

/**
 * Gtk4\GApplication::vfunc_name_lost(): bool
 *
 * Native `name_lost` (ApplicationClass.name_lost): the GTK implementation below any PHP subclass,
 * for `parent::vfunc_name_lost()` from an override. invoked when another instance is taking over
 * the name. Since: 2.60
 */
ZEND_METHOD(Gtk4_GApplication, vfunc_name_lost) {
  ZEND_PARSE_PARAMETERS_NONE();
  GApplication *self = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GApplication::vfunc_name_lost(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = G_APPLICATION_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->name_lost == nullptr) {
    RETURN_FALSE;
  }
  RETURN_BOOL(klass->name_lost(self));
}

/**
 * Gtk4\GApplication::vfunc_quit_mainloop(): void
 *
 * Native `quit_mainloop` (ApplicationClass.quit_mainloop): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_quit_mainloop()` from an override. Used to be invoked on the
 * primary instance when the use count of the application drops to zero (and after any inactivity
 * timeout, if requested). Not used anymore since 2.32
 */
ZEND_METHOD(Gtk4_GApplication, vfunc_quit_mainloop) {
  ZEND_PARSE_PARAMETERS_NONE();
  GApplication *self = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GApplication::vfunc_quit_mainloop(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = G_APPLICATION_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->quit_mainloop == nullptr) {
    return;
  }
  klass->quit_mainloop(self);
}

/**
 * Gtk4\GApplication::vfunc_run_mainloop(): void
 *
 * Native `run_mainloop` (ApplicationClass.run_mainloop): the GTK implementation below any PHP
 * subclass, for `parent::vfunc_run_mainloop()` from an override. Used to be invoked on the primary
 * instance from g_application_run() if the use-count is non-zero. Since 2.32, GApplication is
 * iterating the main context directly and is not using $run_mainloop anymore
 */
ZEND_METHOD(Gtk4_GApplication, vfunc_run_mainloop) {
  ZEND_PARSE_PARAMETERS_NONE();
  GApplication *self = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GApplication::vfunc_run_mainloop(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = G_APPLICATION_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->run_mainloop == nullptr) {
    return;
  }
  klass->run_mainloop(self);
}

/**
 * Gtk4\GApplication::vfunc_shutdown(): void
 *
 * Native `shutdown` (ApplicationClass.shutdown): the GTK implementation below any PHP subclass,
 * for `parent::vfunc_shutdown()` from an override. invoked only on the registered primary instance
 * immediately after the main loop terminates
 */
ZEND_METHOD(Gtk4_GApplication, vfunc_shutdown) {
  ZEND_PARSE_PARAMETERS_NONE();
  GApplication *self = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GApplication::vfunc_shutdown(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = G_APPLICATION_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->shutdown == nullptr) {
    return;
  }
  klass->shutdown(self);
}

/**
 * Gtk4\GApplication::vfunc_startup(): void
 *
 * Native `startup` (ApplicationClass.startup): the GTK implementation below any PHP subclass, for
 * `parent::vfunc_startup()` from an override. invoked on the primary instance immediately after
 * registration
 */
ZEND_METHOD(Gtk4_GApplication, vfunc_startup) {
  ZEND_PARSE_PARAMETERS_NONE();
  GApplication *self = PHPGTK_SELF(GApplication, G_TYPE_APPLICATION);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GApplication::vfunc_startup(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = G_APPLICATION_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->startup == nullptr) {
    return;
  }
  klass->startup(self);
}

// MINIT: the vfunc thunks of GApplication (core/subtype.h).
void register_vfuncs_GApplication() {
  register_vfunc(G_TYPE_APPLICATION, "activate", vfunc_install_activate);
  register_vfunc(G_TYPE_APPLICATION, "after_emit", vfunc_install_after_emit);
  register_vfunc(G_TYPE_APPLICATION, "before_emit", vfunc_install_before_emit);
  register_vfunc(G_TYPE_APPLICATION, "name_lost", vfunc_install_name_lost);
  register_vfunc(G_TYPE_APPLICATION, "quit_mainloop", vfunc_install_quit_mainloop);
  register_vfunc(G_TYPE_APPLICATION, "run_mainloop", vfunc_install_run_mainloop);
  register_vfunc(G_TYPE_APPLICATION, "shutdown", vfunc_install_shutdown);
  register_vfunc(G_TYPE_APPLICATION, "startup", vfunc_install_startup);
}
