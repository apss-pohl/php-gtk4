// GENERATED by gen/gir.php from Gio.DBusMethodInfo - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GDBusMethodInfo (boxed value type)
#include "php_gtk4.h"
#include "core/boxed.h"
#include "core/object.h"
#include <cstring>

using namespace phpgtk;

namespace {

const char *const fields[] = {"name", "in_args", "out_args", nullptr};

// Boxed field reader: the public scalar fields as PHP properties.
bool read(gpointer data, const char *field, zval *rv) {
  auto *value = static_cast<GDBusMethodInfo *>(data);
  if (strcmp(field, "name") == 0) {
    if (value->name == nullptr) {
      ZVAL_NULL(rv);
    } else {
      ZVAL_STRING(rv, value->name);
    }
    return true;
  }
  if (strcmp(field, "in_args") == 0) {
    array_init(rv);
    for (auto **item = value->in_args; item != nullptr && *item != nullptr; item++) {
      zval h;
      wrap_boxed(G_TYPE_DBUS_ARG_INFO, *item, &h);
      add_next_index_zval(rv, &h);
    }
    return true;
  }
  if (strcmp(field, "out_args") == 0) {
    array_init(rv);
    for (auto **item = value->out_args; item != nullptr && *item != nullptr; item++) {
      zval h;
      wrap_boxed(G_TYPE_DBUS_ARG_INFO, *item, &h);
      add_next_index_zval(rv, &h);
    }
    return true;
  }
  return false;
}

// Boxed field writer: each field converts like a parameter of its type (core/boxed).
bool write(gpointer data, const char *field, zval *v) {
  (void)data;
  (void)field;
  (void)v;
  return false;
}

}  // namespace

/**
 * Gtk4\GDBusMethodInfo::__construct()
 *
 * GDBusMethodInfo values come from GTK, never from `new`.
 */
ZEND_METHOD(Gtk4_GDBusMethodInfo, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
}

namespace phpgtk {
// MINIT: bind the PHP class to G_TYPE_DBUS_METHOD_INFO with its field table.
void register_GDBusMethodInfo(zend_class_entry *ce) {
  register_boxed(BoxedClass{
      .type = G_TYPE_DBUS_METHOD_INFO, .ce = ce, .fields = fields, .read = read, .write = write});
}
}  // namespace phpgtk
