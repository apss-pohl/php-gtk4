// GENERATED by gen/gir.php from Gio.TlsCertificate - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GTlsCertificate
#include "php_gtk4.h"
#include "core/object.h"
#include "core/collections.h"
#include "core/gerror.h"
#include "core/boxed.h"

using namespace phpgtk;

/**
 * Gtk4\GTlsCertificate::__construct()
 *
 * GTlsCertificate is abstract in GTK: instances come from GTK, never from `new`.
 */
ZEND_METHOD(Gtk4_GTlsCertificate, __construct) {
  // Private: never called (object_init_ex() in wrap() skips constructors).
  ZEND_PARSE_PARAMETERS_NONE();
}

/**
 * static Gtk4\GTlsCertificate::new_from_file(string $file): GTlsCertificate
 *
 * Creates a #GTlsCertificate from the data in $file.
 */
ZEND_METHOD(Gtk4_GTlsCertificate, new_from_file) {
  zend_string *file;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_PATH_STR(file)
  ZEND_PARSE_PARAMETERS_END();
  zend_string *file_abs = phpgtk::absolute_filename(file, 1);
  if (file_abs == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  GTlsCertificate *call_result = g_tls_certificate_new_from_file(ZSTR_VAL(file_abs), &error);
  if (file_abs != nullptr) zend_string_release(file_abs);
  GObject *obj = G_OBJECT(call_result);
  if (obj == nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * static Gtk4\GTlsCertificate::new_from_file_with_password(string $file, string $password):
 * GTlsCertificate
 *
 * Creates a #GTlsCertificate from the data in $file.
 */
ZEND_METHOD(Gtk4_GTlsCertificate, new_from_file_with_password) {
  zend_string *file;
  zend_string *password;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_PATH_STR(file)
  Z_PARAM_STR(password)
  ZEND_PARSE_PARAMETERS_END();
  zend_string *file_abs = phpgtk::absolute_filename(file, 1);
  if (file_abs == nullptr) RETURN_THROWS();
  if (!phpgtk::check_utf8(password, 2)) RETURN_THROWS();
  GError *error = nullptr;
  GTlsCertificate *call_result =
      g_tls_certificate_new_from_file_with_password(ZSTR_VAL(file_abs), ZSTR_VAL(password), &error);
  if (file_abs != nullptr) zend_string_release(file_abs);
  GObject *obj = G_OBJECT(call_result);
  if (obj == nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * static Gtk4\GTlsCertificate::new_from_files(string $cert_file, string $key_file): GTlsCertificate
 *
 * Creates a #GTlsCertificate from the PEM-encoded data in $cert_file and $key_file. The returned
 * certificate will be the first certificate found in $cert_file. As of GLib 2.44, if $cert_file
 * contains more certificates it will try to load a certificate chain. All certificates will be
 * verified in the order found (top-level certificate should be the last one in the file) and the
 * #GTlsCertificate:issuer property of each certificate will be set accordingly if the verification
 * succeeds. If any certificate in the chain cannot be verified, the first certificate in the file
 * will still be returned.
 */
ZEND_METHOD(Gtk4_GTlsCertificate, new_from_files) {
  zend_string *cert_file;
  zend_string *key_file;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_PATH_STR(cert_file)
  Z_PARAM_PATH_STR(key_file)
  ZEND_PARSE_PARAMETERS_END();
  zend_string *cert_file_abs = phpgtk::absolute_filename(cert_file, 1);
  if (cert_file_abs == nullptr) RETURN_THROWS();
  zend_string *key_file_abs = phpgtk::absolute_filename(key_file, 2);
  if (key_file_abs == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  GTlsCertificate *call_result =
      g_tls_certificate_new_from_files(ZSTR_VAL(cert_file_abs), ZSTR_VAL(key_file_abs), &error);
  if (cert_file_abs != nullptr) zend_string_release(cert_file_abs);
  if (key_file_abs != nullptr) zend_string_release(key_file_abs);
  GObject *obj = G_OBJECT(call_result);
  if (obj == nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * static Gtk4\GTlsCertificate::new_from_pem(string $data, int $length): GTlsCertificate
 *
 * Creates a #GTlsCertificate from the PEM-encoded data in $data. If $data includes both a
 * certificate and a private key, then the returned certificate will include the private key data
 * as well. (See the #GTlsCertificate:private-key-pem property for information about supported
 * formats.)
 */
ZEND_METHOD(Gtk4_GTlsCertificate, new_from_pem) {
  zend_string *data;
  zend_long length;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_STR(data)
  Z_PARAM_LONG(length)
  ZEND_PARSE_PARAMETERS_END();
  if (!phpgtk::check_utf8(data, 1)) RETURN_THROWS();
  const bool precondition_0 =
      length == -1 || (length >= 0 && static_cast<gsize>(length) <= ZSTR_LEN(data));
  if (!precondition_0) {
    zend_argument_value_error(2, "must be -1 or at most the length of the data");
    RETURN_THROWS();
  }
  GError *error = nullptr;
  GObject *obj =
      G_OBJECT(g_tls_certificate_new_from_pem(ZSTR_VAL(data), static_cast<gssize>(length), &error));
  if (obj == nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * static Gtk4\GTlsCertificate::new_from_pkcs11_uris(string $pkcs11_uri, ?string
 * $private_key_pkcs11_uri = null): GTlsCertificate
 *
 * Creates a #GTlsCertificate from a [PKCS
 * \#11](https://docs.oasis-open.org/pkcs11/pkcs11-base/v3.0/os/pkcs11-base-v3.0-os.html) URI.
 */
ZEND_METHOD(Gtk4_GTlsCertificate, new_from_pkcs11_uris) {
  zend_string *pkcs11_uri;
  zend_string *private_key_pkcs11_uri = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 2)
  Z_PARAM_STR(pkcs11_uri)
  Z_PARAM_OPTIONAL
  Z_PARAM_STR_OR_NULL(private_key_pkcs11_uri)
  ZEND_PARSE_PARAMETERS_END();
  if (!phpgtk::check_utf8(pkcs11_uri, 1)) RETURN_THROWS();
  if (private_key_pkcs11_uri != nullptr && !phpgtk::check_utf8(private_key_pkcs11_uri, 2))
    RETURN_THROWS();
  GError *error = nullptr;
  GObject *obj = G_OBJECT(g_tls_certificate_new_from_pkcs11_uris(
      ZSTR_VAL(pkcs11_uri),
      private_key_pkcs11_uri != nullptr ? ZSTR_VAL(private_key_pkcs11_uri) : nullptr, &error));
  if (obj == nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * static Gtk4\GTlsCertificate::list_new_from_file(string $file): array
 *
 * Creates one or more #GTlsCertificates from the PEM-encoded data in $file. If $file cannot be
 * read or parsed, the function will return `null` and set $error. If $file does not contain any
 * PEM-encoded certificates, this will return an empty list and not set $error.
 */
ZEND_METHOD(Gtk4_GTlsCertificate, list_new_from_file) {
  zend_string *file;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_PATH_STR(file)
  ZEND_PARSE_PARAMETERS_END();
  zend_string *file_abs = phpgtk::absolute_filename(file, 1);
  if (file_abs == nullptr) RETURN_THROWS();
  GError *error = nullptr;
  GList *call_result = g_tls_certificate_list_new_from_file(ZSTR_VAL(file_abs), &error);
  if (file_abs != nullptr) zend_string_release(file_abs);
  GList *phpgtk_list = call_result;
  if (error != nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  glist_to_php(phpgtk_list, G_TYPE_TLS_CERTIFICATE, Transfer::Full, return_value);
}

/**
 * Gtk4\GTlsCertificate::get_dns_names(): array
 *
 * Gets the value of #GTlsCertificate:dns-names.
 */
ZEND_METHOD(Gtk4_GTlsCertificate, get_dns_names) {
  ZEND_PARSE_PARAMETERS_NONE();
  GTlsCertificate *self = PHPGTK_SELF(GTlsCertificate, G_TYPE_TLS_CERTIFICATE);
  gptrarray_to_php(g_tls_certificate_get_dns_names(self), G_TYPE_BYTES, Transfer::Container,
                   return_value);
}

/**
 * Gtk4\GTlsCertificate::get_issuer(): ?GTlsCertificate
 *
 * Gets the #GTlsCertificate representing $cert's issuer, if known
 */
ZEND_METHOD(Gtk4_GTlsCertificate, get_issuer) {
  ZEND_PARSE_PARAMETERS_NONE();
  GTlsCertificate *self = PHPGTK_SELF(GTlsCertificate, G_TYPE_TLS_CERTIFICATE);
  GTlsCertificate *phpgtk_ret = g_tls_certificate_get_issuer(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GTlsCertificate::get_issuer_name(): ?string
 *
 * Returns the issuer name from the certificate.
 */
ZEND_METHOD(Gtk4_GTlsCertificate, get_issuer_name) {
  ZEND_PARSE_PARAMETERS_NONE();
  GTlsCertificate *self = PHPGTK_SELF(GTlsCertificate, G_TYPE_TLS_CERTIFICATE);
  char *phpgtk_ret = g_tls_certificate_get_issuer_name(self);
  if (phpgtk_ret == nullptr) RETURN_NULL();
  RETVAL_STRING(phpgtk_ret);
  g_free(phpgtk_ret);
}

/**
 * Gtk4\GTlsCertificate::get_not_valid_after(): ?GDateTime
 *
 * Returns the time at which the certificate became or will become invalid.
 */
ZEND_METHOD(Gtk4_GTlsCertificate, get_not_valid_after) {
  ZEND_PARSE_PARAMETERS_NONE();
  GTlsCertificate *self = PHPGTK_SELF(GTlsCertificate, G_TYPE_TLS_CERTIFICATE);
  GDateTime *phpgtk_ret = g_tls_certificate_get_not_valid_after(self);
  wrap_boxed(G_TYPE_DATE_TIME, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(G_TYPE_DATE_TIME, phpgtk_ret);
}

/**
 * Gtk4\GTlsCertificate::get_not_valid_before(): ?GDateTime
 *
 * Returns the time at which the certificate became or will become valid.
 */
ZEND_METHOD(Gtk4_GTlsCertificate, get_not_valid_before) {
  ZEND_PARSE_PARAMETERS_NONE();
  GTlsCertificate *self = PHPGTK_SELF(GTlsCertificate, G_TYPE_TLS_CERTIFICATE);
  GDateTime *phpgtk_ret = g_tls_certificate_get_not_valid_before(self);
  wrap_boxed(G_TYPE_DATE_TIME, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(G_TYPE_DATE_TIME, phpgtk_ret);
}

/**
 * Gtk4\GTlsCertificate::get_subject_name(): ?string
 *
 * Returns the subject name from the certificate.
 */
ZEND_METHOD(Gtk4_GTlsCertificate, get_subject_name) {
  ZEND_PARSE_PARAMETERS_NONE();
  GTlsCertificate *self = PHPGTK_SELF(GTlsCertificate, G_TYPE_TLS_CERTIFICATE);
  char *phpgtk_ret = g_tls_certificate_get_subject_name(self);
  if (phpgtk_ret == nullptr) RETURN_NULL();
  RETVAL_STRING(phpgtk_ret);
  g_free(phpgtk_ret);
}

/**
 * Gtk4\GTlsCertificate::is_same(GTlsCertificate $cert_two): bool
 *
 * Check if two #GTlsCertificate objects represent the same certificate. The raw DER byte data of
 * the two certificates are checked for equality. This has the effect that two certificates may
 * compare equal even if their #GTlsCertificate:issuer, #GTlsCertificate:private-key, or
 * #GTlsCertificate:private-key-pem properties differ.
 */
ZEND_METHOD(Gtk4_GTlsCertificate, is_same) {
  zval *cert_two;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(cert_two, class_for_gtype(G_TYPE_TLS_CERTIFICATE))
  ZEND_PARSE_PARAMETERS_END();
  GTlsCertificate *self = PHPGTK_SELF(GTlsCertificate, G_TYPE_TLS_CERTIFICATE);
  GObject *cert_two_o = unwrap(cert_two, G_TYPE_TLS_CERTIFICATE);
  if (cert_two_o == nullptr) RETURN_THROWS();
  RETURN_BOOL(g_tls_certificate_is_same(self, G_TLS_CERTIFICATE(cert_two_o)));
}
