// GENERATED by gen/gir.php from Gio.ActionGroup - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GActionGroup
#include "php_gtk4.h"
#include "core/object.h"
#include "core/collections.h"
#include "core/variant.h"
#include "core/subtype.h"
#include "core/error.h"
#include <array>

using namespace phpgtk;

// Prelude for Gtk4\GActionGroup: the registration precondition every query shares.

namespace {

// A GApplication only owns its actions once it is registered (from `startup` on). Every query
// below it - g_application_query_action() and g_application_change_action_state() - is a
// g_return_if_fail() until then, so GLib CRITICALs and answers a default that reads like "no such
// action" rather than "ask me later". Refuse the call instead: the handle is in the wrong state,
// which is a LogicException by the vocabulary in CLAUDE.md. Every other action group answers at
// any time, so the check is scoped to GApplication.
bool action_group_is_queryable(GActionGroup *self, const char *method) {
  // NOLINTNEXTLINE(bugprone-assignment-in-if-condition) G_IS_APPLICATION() macro expansion
  if (G_IS_APPLICATION(self) && g_application_get_is_registered(G_APPLICATION(self)) == FALSE) {
    zend_throw_exception_ex(spl_ce_LogicException, 0,
                            "Gtk4\\GActionGroup::%s(): the application is not registered yet - "
                            "its actions exist from the `startup` signal on",
                            method);
    return false;
  }
  return true;
}

}  // namespace

/**
 * Gtk4\GActionGroup::action_added(string $action_name): void
 *
 * Emits the #GActionGroup::action-added signal on $action_group.
 */
ZEND_METHOD(Gtk4_GActionGroup, action_added) {
  zend_string *action_name;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(action_name)
  ZEND_PARSE_PARAMETERS_END();
  GActionGroup *self = PHPGTK_SELF(GActionGroup, G_TYPE_ACTION_GROUP);
  if (!phpgtk::check_utf8(action_name, 1)) RETURN_THROWS();
  g_action_group_action_added(self, ZSTR_VAL(action_name));
}

/**
 * Gtk4\GActionGroup::action_enabled_changed(string $action_name, bool $enabled): void
 *
 * Emits the #GActionGroup::action-enabled-changed signal on $action_group.
 */
ZEND_METHOD(Gtk4_GActionGroup, action_enabled_changed) {
  zend_string *action_name;
  bool enabled;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_STR(action_name)
  Z_PARAM_BOOL(enabled)
  ZEND_PARSE_PARAMETERS_END();
  GActionGroup *self = PHPGTK_SELF(GActionGroup, G_TYPE_ACTION_GROUP);
  if (!phpgtk::check_utf8(action_name, 1)) RETURN_THROWS();
  g_action_group_action_enabled_changed(self, ZSTR_VAL(action_name), enabled);
}

/**
 * Gtk4\GActionGroup::action_removed(string $action_name): void
 *
 * Emits the #GActionGroup::action-removed signal on $action_group.
 */
ZEND_METHOD(Gtk4_GActionGroup, action_removed) {
  zend_string *action_name;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(action_name)
  ZEND_PARSE_PARAMETERS_END();
  GActionGroup *self = PHPGTK_SELF(GActionGroup, G_TYPE_ACTION_GROUP);
  if (!phpgtk::check_utf8(action_name, 1)) RETURN_THROWS();
  g_action_group_action_removed(self, ZSTR_VAL(action_name));
}

/**
 * Gtk4\GActionGroup::action_state_changed(string $action_name, mixed $state = null): void
 *
 * Emits the #GActionGroup::action-state-changed signal on $action_group.
 */
ZEND_METHOD(Gtk4_GActionGroup, action_state_changed) {
  zend_string *action_name;
  zval *state = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 2)
  Z_PARAM_STR(action_name)
  Z_PARAM_OPTIONAL
  Z_PARAM_ZVAL(state)
  ZEND_PARSE_PARAMETERS_END();
  GActionGroup *self = PHPGTK_SELF(GActionGroup, G_TYPE_ACTION_GROUP);
  if (!phpgtk::check_utf8(action_name, 1)) RETURN_THROWS();
  GVariant *state_v = nullptr;
  if (state != nullptr && Z_TYPE_P(state) != IS_NULL) {
    state_v = php_to_variant(state, nullptr);
    if (state_v == nullptr) RETURN_THROWS();
    g_variant_ref_sink(state_v);
  }
  g_action_group_action_state_changed(self, ZSTR_VAL(action_name), state_v);
  if (state_v != nullptr) g_variant_unref(state_v);
}

/**
 * public function activate_action(string $action_name, mixed $parameter = null): void
 * Activate an action by name. $parameter is converted to the action's declared parameter type
 * (ValueError when the action is unknown or a required parameter is missing, TypeError when the
 * value does not fit the type).
 */
ZEND_METHOD(Gtk4_GActionGroup, activate_action) {
  zend_string *name;
  zval *param = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 2)
  Z_PARAM_STR(name)
  Z_PARAM_OPTIONAL
  Z_PARAM_ZVAL(param)
  ZEND_PARSE_PARAMETERS_END();
  GActionGroup *group = PHPGTK_SELF(GActionGroup, G_TYPE_ACTION_GROUP);
  if (!g_action_group_has_action(group, ZSTR_VAL(name))) {
    zend_argument_value_error(1, "no action '%s' on this %s", ZSTR_VAL(name),
                              G_OBJECT_TYPE_NAME(group));
    RETURN_THROWS();
  }
  const GVariantType *t = g_action_group_get_action_parameter_type(group, ZSTR_VAL(name));
  GVariant *v = nullptr;
  if (t != nullptr) {
    const bool null_given = param == nullptr || Z_TYPE_P(param) == IS_NULL;
    if (null_given && !g_variant_type_is_maybe(t)) {
      zend_argument_value_error(2, "is required: action '%s' takes a parameter of type %s",
                                ZSTR_VAL(name), g_variant_type_peek_string(t));
      RETURN_THROWS();
    }
    zval null_zv;
    ZVAL_NULL(&null_zv);
    v = php_to_variant(null_given ? &null_zv : param, t);
    if (v == nullptr) RETURN_THROWS();
    g_variant_ref_sink(v);
  }
  g_action_group_activate_action(group, ZSTR_VAL(name), v);
  if (v != nullptr) g_variant_unref(v);
  if (EG(exception) != nullptr) RETURN_THROWS();
}

/**
 * public function change_action_state(string $action_name, mixed $value = null): void
 * Request for the state of the named action within $action_group to be changed to $value.
 *
 * A GApplication only has its actions once it is registered (from `startup` on); before
 * that GLib CRITICALs and answers a default that reads like "no such action" rather than
 * "ask me later", so this refuses with a `LogicException` instead. Every other action
 * group answers at any time.
 */
ZEND_METHOD(Gtk4_GActionGroup, change_action_state) {
  zend_string *action_name;
  zval *value = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 2)
  Z_PARAM_STR(action_name)
  Z_PARAM_OPTIONAL
  Z_PARAM_ZVAL(value)
  ZEND_PARSE_PARAMETERS_END();
  GActionGroup *self = PHPGTK_SELF(GActionGroup, G_TYPE_ACTION_GROUP);
  if (!phpgtk::check_utf8(action_name, 1)) RETURN_THROWS();
  if (!action_group_is_queryable(self, "change_action_state")) RETURN_THROWS();
  GVariant *value_v = nullptr;
  if (value != nullptr && Z_TYPE_P(value) != IS_NULL) {
    value_v = php_to_variant(value, nullptr);
    if (value_v == nullptr) RETURN_THROWS();
    g_variant_ref_sink(value_v);
  }
  g_action_group_change_action_state(self, ZSTR_VAL(action_name), value_v);
  if (value_v != nullptr) g_variant_unref(value_v);
}

/**
 * public function get_action_enabled(string $action_name): bool
 * Checks if the named action within $action_group is currently enabled.
 *
 * A GApplication only has its actions once it is registered (from `startup` on); before
 * that GLib CRITICALs and answers a default that reads like "no such action" rather than
 * "ask me later", so this refuses with a `LogicException` instead. Every other action
 * group answers at any time.
 */
ZEND_METHOD(Gtk4_GActionGroup, get_action_enabled) {
  zend_string *action_name;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(action_name)
  ZEND_PARSE_PARAMETERS_END();
  GActionGroup *self = PHPGTK_SELF(GActionGroup, G_TYPE_ACTION_GROUP);
  if (!phpgtk::check_utf8(action_name, 1)) RETURN_THROWS();
  if (!action_group_is_queryable(self, "get_action_enabled")) RETURN_THROWS();
  RETURN_BOOL(g_action_group_get_action_enabled(self, ZSTR_VAL(action_name)));
}

/**
 * public function get_action_parameter_type(string $action_name): ?string
 * Queries the type of the parameter that must be given when activating the named action within
 * $action_group.
 *
 * A GApplication only has its actions once it is registered (from `startup` on); before
 * that GLib CRITICALs and answers a default that reads like "no such action" rather than
 * "ask me later", so this refuses with a `LogicException` instead. Every other action
 * group answers at any time.
 */
ZEND_METHOD(Gtk4_GActionGroup, get_action_parameter_type) {
  zend_string *action_name;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(action_name)
  ZEND_PARSE_PARAMETERS_END();
  GActionGroup *self = PHPGTK_SELF(GActionGroup, G_TYPE_ACTION_GROUP);
  if (!phpgtk::check_utf8(action_name, 1)) RETURN_THROWS();
  if (!action_group_is_queryable(self, "get_action_parameter_type")) RETURN_THROWS();
  const GVariantType *vt = g_action_group_get_action_parameter_type(self, ZSTR_VAL(action_name));
  if (vt == nullptr) RETURN_NULL();
  RETVAL_STRINGL(g_variant_type_peek_string(vt), g_variant_type_get_string_length(vt));
}

/**
 * public function get_action_state(string $action_name): mixed
 * Queries the current state of the named action within $action_group.
 *
 * A GApplication only has its actions once it is registered (from `startup` on); before
 * that GLib CRITICALs and answers a default that reads like "no such action" rather than
 * "ask me later", so this refuses with a `LogicException` instead. Every other action
 * group answers at any time.
 */
ZEND_METHOD(Gtk4_GActionGroup, get_action_state) {
  zend_string *action_name;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(action_name)
  ZEND_PARSE_PARAMETERS_END();
  GActionGroup *self = PHPGTK_SELF(GActionGroup, G_TYPE_ACTION_GROUP);
  if (!phpgtk::check_utf8(action_name, 1)) RETURN_THROWS();
  if (!action_group_is_queryable(self, "get_action_state")) RETURN_THROWS();
  GVariant *phpgtk_ret = g_action_group_get_action_state(self, ZSTR_VAL(action_name));
  if (phpgtk_ret == nullptr) RETURN_NULL();
  variant_to_php(phpgtk_ret, return_value);
  g_variant_unref(phpgtk_ret);
}

/**
 * public function get_action_state_hint(string $action_name): mixed
 * Requests a hint about the valid range of values for the state of the named action within
 * $action_group.
 *
 * A GApplication only has its actions once it is registered (from `startup` on); before
 * that GLib CRITICALs and answers a default that reads like "no such action" rather than
 * "ask me later", so this refuses with a `LogicException` instead. Every other action
 * group answers at any time.
 */
ZEND_METHOD(Gtk4_GActionGroup, get_action_state_hint) {
  zend_string *action_name;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(action_name)
  ZEND_PARSE_PARAMETERS_END();
  GActionGroup *self = PHPGTK_SELF(GActionGroup, G_TYPE_ACTION_GROUP);
  if (!phpgtk::check_utf8(action_name, 1)) RETURN_THROWS();
  if (!action_group_is_queryable(self, "get_action_state_hint")) RETURN_THROWS();
  GVariant *phpgtk_ret = g_action_group_get_action_state_hint(self, ZSTR_VAL(action_name));
  if (phpgtk_ret == nullptr) RETURN_NULL();
  variant_to_php(phpgtk_ret, return_value);
  g_variant_unref(phpgtk_ret);
}

/**
 * public function get_action_state_type(string $action_name): ?string
 * Queries the type of the state of the named action within $action_group.
 *
 * A GApplication only has its actions once it is registered (from `startup` on); before
 * that GLib CRITICALs and answers a default that reads like "no such action" rather than
 * "ask me later", so this refuses with a `LogicException` instead. Every other action
 * group answers at any time.
 */
ZEND_METHOD(Gtk4_GActionGroup, get_action_state_type) {
  zend_string *action_name;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(action_name)
  ZEND_PARSE_PARAMETERS_END();
  GActionGroup *self = PHPGTK_SELF(GActionGroup, G_TYPE_ACTION_GROUP);
  if (!phpgtk::check_utf8(action_name, 1)) RETURN_THROWS();
  if (!action_group_is_queryable(self, "get_action_state_type")) RETURN_THROWS();
  const GVariantType *vt = g_action_group_get_action_state_type(self, ZSTR_VAL(action_name));
  if (vt == nullptr) RETURN_NULL();
  RETVAL_STRINGL(g_variant_type_peek_string(vt), g_variant_type_get_string_length(vt));
}

/**
 * public function has_action(string $action_name): bool
 * Checks if the named action exists within $action_group.
 *
 * A GApplication only has its actions once it is registered (from `startup` on); before
 * that GLib CRITICALs and answers a default that reads like "no such action" rather than
 * "ask me later", so this refuses with a `LogicException` instead. Every other action
 * group answers at any time.
 */
ZEND_METHOD(Gtk4_GActionGroup, has_action) {
  zend_string *action_name;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(action_name)
  ZEND_PARSE_PARAMETERS_END();
  GActionGroup *self = PHPGTK_SELF(GActionGroup, G_TYPE_ACTION_GROUP);
  if (!phpgtk::check_utf8(action_name, 1)) RETURN_THROWS();
  if (!action_group_is_queryable(self, "has_action")) RETURN_THROWS();
  RETURN_BOOL(g_action_group_has_action(self, ZSTR_VAL(action_name)));
}

/**
 * public function list_actions(): array
 * The names of the actions in this group.
 *
 * A GApplication only has its actions once it is registered (from `startup` on); before that
 * GLib CRITICALs and answers an empty list, which reads like "no actions" rather than "ask me
 * later". Every other action group answers at any time.
 *
 * @return list<string>
 */
ZEND_METHOD(Gtk4_GActionGroup, list_actions) {
  ZEND_PARSE_PARAMETERS_NONE();
  GActionGroup *self = PHPGTK_SELF(GActionGroup, G_TYPE_ACTION_GROUP);
  if (!action_group_is_queryable(self, "list_actions")) RETURN_THROWS();
  strv_to_php(g_action_group_list_actions(self), Transfer::Full, return_value);
}

// vfunc thunks and installers: file-local, installed by class_init of a PHP subtype
namespace {

// vfunc thunk: static_cast<GActionGroupInterface *>->action_added -> $this->action_added() on a PHP
// subclass
void vfunc_thunk_action_added(GActionGroup *self, const gchar *action_name) {
  zval zself;
  zend_function *fn =
      EG(exception) == nullptr ? subtype_vfunc(G_OBJECT(self), "action_added", &zself) : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    // an interface implemented in PHP has no native implementation below it
    return;
  }
  std::array<zval, 1> args{};
  zval *argv = args.data();
  if (action_name == nullptr) {
    ZVAL_NULL(&argv[0]);
  } else {
    ZVAL_STRING(&argv[0], action_name);
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GActionGroup::action_added");
}

// vfunc installer: static_cast<GActionGroupInterface *>->action_added (called from class_init /
// iface_init of a PHP subtype)
void vfunc_install_action_added(gpointer klass) {
  static_cast<GActionGroupInterface *>(klass)->action_added = vfunc_thunk_action_added;
}

// vfunc thunk: static_cast<GActionGroupInterface *>->action_enabled_changed ->
// $this->action_enabled_changed() on a PHP subclass
void vfunc_thunk_action_enabled_changed(GActionGroup *self, const gchar *action_name,
                                        gboolean enabled) {
  zval zself;
  zend_function *fn = EG(exception) == nullptr
                          ? subtype_vfunc(G_OBJECT(self), "action_enabled_changed", &zself)
                          : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    // an interface implemented in PHP has no native implementation below it
    return;
  }
  std::array<zval, 2> args{};
  zval *argv = args.data();
  if (action_name == nullptr) {
    ZVAL_NULL(&argv[0]);
  } else {
    ZVAL_STRING(&argv[0], action_name);
  }
  ZVAL_BOOL(&argv[1], enabled != FALSE);
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 2, args.data());
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GActionGroup::action_enabled_changed");
}

// vfunc installer: static_cast<GActionGroupInterface *>->action_enabled_changed (called from
// class_init / iface_init of a PHP subtype)
void vfunc_install_action_enabled_changed(gpointer klass) {
  static_cast<GActionGroupInterface *>(klass)->action_enabled_changed =
      vfunc_thunk_action_enabled_changed;
}

// vfunc thunk: static_cast<GActionGroupInterface *>->action_removed -> $this->action_removed() on a
// PHP subclass
void vfunc_thunk_action_removed(GActionGroup *self, const gchar *action_name) {
  zval zself;
  zend_function *fn =
      EG(exception) == nullptr ? subtype_vfunc(G_OBJECT(self), "action_removed", &zself) : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    // an interface implemented in PHP has no native implementation below it
    return;
  }
  std::array<zval, 1> args{};
  zval *argv = args.data();
  if (action_name == nullptr) {
    ZVAL_NULL(&argv[0]);
  } else {
    ZVAL_STRING(&argv[0], action_name);
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GActionGroup::action_removed");
}

// vfunc installer: static_cast<GActionGroupInterface *>->action_removed (called from class_init /
// iface_init of a PHP subtype)
void vfunc_install_action_removed(gpointer klass) {
  static_cast<GActionGroupInterface *>(klass)->action_removed = vfunc_thunk_action_removed;
}

// vfunc thunk: static_cast<GActionGroupInterface *>->action_state_changed ->
// $this->action_state_changed() on a PHP subclass
void vfunc_thunk_action_state_changed(GActionGroup *self, const gchar *action_name,
                                      GVariant *state) {
  zval zself;
  zend_function *fn = EG(exception) == nullptr
                          ? subtype_vfunc(G_OBJECT(self), "action_state_changed", &zself)
                          : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    // an interface implemented in PHP has no native implementation below it
    return;
  }
  std::array<zval, 2> args{};
  zval *argv = args.data();
  if (action_name == nullptr) {
    ZVAL_NULL(&argv[0]);
  } else {
    ZVAL_STRING(&argv[0], action_name);
  }
  if (state == nullptr) {
    ZVAL_NULL(&argv[1]);
  } else {
    variant_to_php(state, &argv[1]);
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 2, args.data());
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GActionGroup::action_state_changed");
}

// vfunc installer: static_cast<GActionGroupInterface *>->action_state_changed (called from
// class_init / iface_init of a PHP subtype)
void vfunc_install_action_state_changed(gpointer klass) {
  static_cast<GActionGroupInterface *>(klass)->action_state_changed =
      vfunc_thunk_action_state_changed;
}

// vfunc thunk: static_cast<GActionGroupInterface *>->activate_action -> $this->activate_action() on
// a PHP subclass
void vfunc_thunk_activate_action(GActionGroup *self, const gchar *action_name,
                                 GVariant *parameter) {
  zval zself;
  zend_function *fn =
      EG(exception) == nullptr ? subtype_vfunc(G_OBJECT(self), "activate_action", &zself) : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    // an interface implemented in PHP has no native implementation below it
    return;
  }
  std::array<zval, 2> args{};
  zval *argv = args.data();
  if (action_name == nullptr) {
    ZVAL_NULL(&argv[0]);
  } else {
    ZVAL_STRING(&argv[0], action_name);
  }
  if (parameter == nullptr) {
    ZVAL_NULL(&argv[1]);
  } else {
    variant_to_php(parameter, &argv[1]);
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 2, args.data());
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GActionGroup::activate_action");
}

// vfunc installer: static_cast<GActionGroupInterface *>->activate_action (called from class_init /
// iface_init of a PHP subtype)
void vfunc_install_activate_action(gpointer klass) {
  static_cast<GActionGroupInterface *>(klass)->activate_action = vfunc_thunk_activate_action;
}

// vfunc thunk: static_cast<GActionGroupInterface *>->change_action_state ->
// $this->change_action_state() on a PHP subclass
void vfunc_thunk_change_action_state(GActionGroup *self, const gchar *action_name,
                                     GVariant *value) {
  zval zself;
  zend_function *fn = EG(exception) == nullptr
                          ? subtype_vfunc(G_OBJECT(self), "change_action_state", &zself)
                          : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    // an interface implemented in PHP has no native implementation below it
    return;
  }
  std::array<zval, 2> args{};
  zval *argv = args.data();
  if (action_name == nullptr) {
    ZVAL_NULL(&argv[0]);
  } else {
    ZVAL_STRING(&argv[0], action_name);
  }
  if (value == nullptr) {
    ZVAL_NULL(&argv[1]);
  } else {
    variant_to_php(value, &argv[1]);
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 2, args.data());
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GActionGroup::change_action_state");
}

// vfunc installer: static_cast<GActionGroupInterface *>->change_action_state (called from
// class_init / iface_init of a PHP subtype)
void vfunc_install_change_action_state(gpointer klass) {
  static_cast<GActionGroupInterface *>(klass)->change_action_state =
      vfunc_thunk_change_action_state;
}

// vfunc thunk: static_cast<GActionGroupInterface *>->get_action_enabled ->
// $this->get_action_enabled() on a PHP subclass
gboolean vfunc_thunk_get_action_enabled(GActionGroup *self, const gchar *action_name) {
  zval zself;
  zend_function *fn = EG(exception) == nullptr
                          ? subtype_vfunc(G_OBJECT(self), "get_action_enabled", &zself)
                          : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    // an interface implemented in PHP has no native implementation below it
    return FALSE;
  }
  std::array<zval, 1> args{};
  zval *argv = args.data();
  if (action_name == nullptr) {
    ZVAL_NULL(&argv[0]);
  } else {
    ZVAL_STRING(&argv[0], action_name);
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  gboolean result = FALSE;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = zend_is_true(&ret) ? TRUE : FALSE;
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GActionGroup::get_action_enabled");
  return result;
}

// vfunc installer: static_cast<GActionGroupInterface *>->get_action_enabled (called from class_init
// / iface_init of a PHP subtype)
void vfunc_install_get_action_enabled(gpointer klass) {
  static_cast<GActionGroupInterface *>(klass)->get_action_enabled = vfunc_thunk_get_action_enabled;
}

// vfunc thunk: static_cast<GActionGroupInterface *>->get_action_parameter_type ->
// $this->get_action_parameter_type() on a PHP subclass
const GVariantType *vfunc_thunk_get_action_parameter_type(GActionGroup *self,
                                                          const gchar *action_name) {
  zval zself;
  zend_function *fn = EG(exception) == nullptr
                          ? subtype_vfunc(G_OBJECT(self), "get_action_parameter_type", &zself)
                          : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    // an interface implemented in PHP has no native implementation below it
    return nullptr;
  }
  std::array<zval, 1> args{};
  zval *argv = args.data();
  if (action_name == nullptr) {
    ZVAL_NULL(&argv[0]);
  } else {
    ZVAL_STRING(&argv[0], action_name);
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  const GVariantType *result = nullptr;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    if (Z_TYPE(ret) == IS_STRING) {
      result = subtype_keep_variant_type(G_OBJECT(self), "GActionGroup::get_action_parameter_type",
                                         "get_action_parameter_type", Z_STRVAL(ret));
    }
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GActionGroup::get_action_parameter_type");
  return result;
}

// vfunc installer: static_cast<GActionGroupInterface *>->get_action_parameter_type (called from
// class_init / iface_init of a PHP subtype)
void vfunc_install_get_action_parameter_type(gpointer klass) {
  static_cast<GActionGroupInterface *>(klass)->get_action_parameter_type =
      vfunc_thunk_get_action_parameter_type;
}

// vfunc thunk: static_cast<GActionGroupInterface *>->get_action_state -> $this->get_action_state()
// on a PHP subclass
GVariant *vfunc_thunk_get_action_state(GActionGroup *self, const gchar *action_name) {
  zval zself;
  zend_function *fn = EG(exception) == nullptr
                          ? subtype_vfunc(G_OBJECT(self), "get_action_state", &zself)
                          : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    // an interface implemented in PHP has no native implementation below it
    return nullptr;
  }
  std::array<zval, 1> args{};
  zval *argv = args.data();
  if (action_name == nullptr) {
    ZVAL_NULL(&argv[0]);
  } else {
    ZVAL_STRING(&argv[0], action_name);
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  GVariant *result = nullptr;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    if (Z_TYPE(ret) != IS_NULL) {
      result = php_to_variant(
          &ret, g_action_group_get_action_state_type(G_ACTION_GROUP(self), action_name));
      if (result != nullptr) g_variant_ref_sink(result);  // transfer full
    }
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GActionGroup::get_action_state");
  return result;
}

// vfunc installer: static_cast<GActionGroupInterface *>->get_action_state (called from class_init /
// iface_init of a PHP subtype)
void vfunc_install_get_action_state(gpointer klass) {
  static_cast<GActionGroupInterface *>(klass)->get_action_state = vfunc_thunk_get_action_state;
}

// vfunc thunk: static_cast<GActionGroupInterface *>->get_action_state_hint ->
// $this->get_action_state_hint() on a PHP subclass
GVariant *vfunc_thunk_get_action_state_hint(GActionGroup *self, const gchar *action_name) {
  zval zself;
  zend_function *fn = EG(exception) == nullptr
                          ? subtype_vfunc(G_OBJECT(self), "get_action_state_hint", &zself)
                          : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    // an interface implemented in PHP has no native implementation below it
    return nullptr;
  }
  std::array<zval, 1> args{};
  zval *argv = args.data();
  if (action_name == nullptr) {
    ZVAL_NULL(&argv[0]);
  } else {
    ZVAL_STRING(&argv[0], action_name);
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  GVariant *result = nullptr;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    if (Z_TYPE(ret) != IS_NULL) {
      result = php_to_variant(&ret, nullptr);
      if (result != nullptr) g_variant_ref_sink(result);  // transfer full
    }
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GActionGroup::get_action_state_hint");
  return result;
}

// vfunc installer: static_cast<GActionGroupInterface *>->get_action_state_hint (called from
// class_init / iface_init of a PHP subtype)
void vfunc_install_get_action_state_hint(gpointer klass) {
  static_cast<GActionGroupInterface *>(klass)->get_action_state_hint =
      vfunc_thunk_get_action_state_hint;
}

// vfunc thunk: static_cast<GActionGroupInterface *>->get_action_state_type ->
// $this->get_action_state_type() on a PHP subclass
const GVariantType *vfunc_thunk_get_action_state_type(GActionGroup *self,
                                                      const gchar *action_name) {
  zval zself;
  zend_function *fn = EG(exception) == nullptr
                          ? subtype_vfunc(G_OBJECT(self), "get_action_state_type", &zself)
                          : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    // an interface implemented in PHP has no native implementation below it
    return nullptr;
  }
  std::array<zval, 1> args{};
  zval *argv = args.data();
  if (action_name == nullptr) {
    ZVAL_NULL(&argv[0]);
  } else {
    ZVAL_STRING(&argv[0], action_name);
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  const GVariantType *result = nullptr;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    if (Z_TYPE(ret) == IS_STRING) {
      result = subtype_keep_variant_type(G_OBJECT(self), "GActionGroup::get_action_state_type",
                                         "get_action_state_type", Z_STRVAL(ret));
    }
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GActionGroup::get_action_state_type");
  return result;
}

// vfunc installer: static_cast<GActionGroupInterface *>->get_action_state_type (called from
// class_init / iface_init of a PHP subtype)
void vfunc_install_get_action_state_type(gpointer klass) {
  static_cast<GActionGroupInterface *>(klass)->get_action_state_type =
      vfunc_thunk_get_action_state_type;
}

// vfunc thunk: static_cast<GActionGroupInterface *>->has_action -> $this->has_action() on a PHP
// subclass
gboolean vfunc_thunk_has_action(GActionGroup *self, const gchar *action_name) {
  zval zself;
  zend_function *fn =
      EG(exception) == nullptr ? subtype_vfunc(G_OBJECT(self), "has_action", &zself) : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    // an interface implemented in PHP has no native implementation below it
    return FALSE;
  }
  std::array<zval, 1> args{};
  zval *argv = args.data();
  if (action_name == nullptr) {
    ZVAL_NULL(&argv[0]);
  } else {
    ZVAL_STRING(&argv[0], action_name);
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  gboolean result = FALSE;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = zend_is_true(&ret) ? TRUE : FALSE;
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GActionGroup::has_action");
  return result;
}

// vfunc installer: static_cast<GActionGroupInterface *>->has_action (called from class_init /
// iface_init of a PHP subtype)
void vfunc_install_has_action(gpointer klass) {
  static_cast<GActionGroupInterface *>(klass)->has_action = vfunc_thunk_has_action;
}

// vfunc thunk: static_cast<GActionGroupInterface *>->list_actions -> $this->list_actions() on a PHP
// subclass
gchar **vfunc_thunk_list_actions(GActionGroup *self) {
  zval zself;
  zend_function *fn =
      EG(exception) == nullptr ? subtype_vfunc(G_OBJECT(self), "list_actions", &zself) : nullptr;
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown) or exception pending
    // an interface implemented in PHP has no native implementation below it
    return g_new0(char *, 1);
  }
  zval ret;
  ZVAL_UNDEF(&ret);
  char **result = nullptr;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    if (Z_TYPE(ret) == IS_ARRAY) result = strv_from_php(&ret);
    if (result == nullptr) result = g_new0(char *, 1);
  }
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GActionGroup::list_actions");
  return result;
}

// vfunc installer: static_cast<GActionGroupInterface *>->list_actions (called from class_init /
// iface_init of a PHP subtype)
void vfunc_install_list_actions(gpointer klass) {
  static_cast<GActionGroupInterface *>(klass)->list_actions = vfunc_thunk_list_actions;
}

}  // namespace

// MINIT: the vfunc thunks of GActionGroup (core/subtype.h).
void register_vfuncs_GActionGroup() {
  register_iface_vfunc(G_TYPE_ACTION_GROUP, "action_added", vfunc_install_action_added);
  register_iface_vfunc(G_TYPE_ACTION_GROUP, "action_enabled_changed",
                       vfunc_install_action_enabled_changed);
  register_iface_vfunc(G_TYPE_ACTION_GROUP, "action_removed", vfunc_install_action_removed);
  register_iface_vfunc(G_TYPE_ACTION_GROUP, "action_state_changed",
                       vfunc_install_action_state_changed);
  register_iface_vfunc(G_TYPE_ACTION_GROUP, "activate_action", vfunc_install_activate_action);
  register_iface_vfunc(G_TYPE_ACTION_GROUP, "change_action_state",
                       vfunc_install_change_action_state);
  register_iface_vfunc(G_TYPE_ACTION_GROUP, "get_action_enabled", vfunc_install_get_action_enabled);
  register_iface_vfunc(G_TYPE_ACTION_GROUP, "get_action_parameter_type",
                       vfunc_install_get_action_parameter_type);
  register_iface_vfunc(G_TYPE_ACTION_GROUP, "get_action_state", vfunc_install_get_action_state);
  register_iface_vfunc(G_TYPE_ACTION_GROUP, "get_action_state_hint",
                       vfunc_install_get_action_state_hint);
  register_iface_vfunc(G_TYPE_ACTION_GROUP, "get_action_state_type",
                       vfunc_install_get_action_state_type);
  register_iface_vfunc(G_TYPE_ACTION_GROUP, "has_action", vfunc_install_has_action);
  register_iface_vfunc(G_TYPE_ACTION_GROUP, "list_actions", vfunc_install_list_actions);
}

/**
 * Gtk4\GActionGroupObject::__construct()
 *
 * Never called: these handles only come from wrap().
 */
ZEND_METHOD(Gtk4_GActionGroupObject, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
}
