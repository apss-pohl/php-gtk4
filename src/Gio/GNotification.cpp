// GENERATED by gen/gir.php from Gio.Notification - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GNotification
#include "php_gtk4.h"
#include "core/object.h"
#include "core/enums.h"
#include "core/variant.h"

using namespace phpgtk;

/**
 * Gtk4\GNotification::__construct(string $title)
 *
 * Creates a new #GNotification with $title as its title.
 */
ZEND_METHOD(Gtk4_GNotification, __construct) {
  zend_string *title;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(title)
  ZEND_PARSE_PARAMETERS_END();
  if (!phpgtk::check_utf8(title, 1)) RETURN_THROWS();
  GObject *obj = G_OBJECT(g_notification_new(ZSTR_VAL(title)));
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * Gtk4\GNotification::add_button(string $label, string $detailed_action): void
 *
 * Adds a button to $notification that activates the action in $detailed_action when clicked. That
 * action must be an application-wide action (starting with "app."). If $detailed_action contains a
 * target, the action will be activated with that target as its parameter.
 */
ZEND_METHOD(Gtk4_GNotification, add_button) {
  zend_string *label;
  zend_string *detailed_action;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_STR(label)
  Z_PARAM_STR(detailed_action)
  ZEND_PARSE_PARAMETERS_END();
  GNotification *self = PHPGTK_SELF(GNotification, G_TYPE_NOTIFICATION);
  if (!phpgtk::check_utf8(label, 1)) RETURN_THROWS();
  if (!phpgtk::check_utf8(detailed_action, 2)) RETURN_THROWS();
  g_notification_add_button(self, ZSTR_VAL(label), ZSTR_VAL(detailed_action));
}

/**
 * Gtk4\GNotification::add_button_with_target(string $label, string $action, mixed $target = null):
 * void
 *
 * Adds a button to $notification that activates $action when clicked. $action must be an
 * application-wide action (it must start with "app.").
 */
ZEND_METHOD(Gtk4_GNotification, add_button_with_target) {
  zend_string *label;
  zend_string *action;
  zval *target = nullptr;
  ZEND_PARSE_PARAMETERS_START(2, 3)
  Z_PARAM_STR(label)
  Z_PARAM_STR(action)
  Z_PARAM_OPTIONAL
  Z_PARAM_ZVAL(target)
  ZEND_PARSE_PARAMETERS_END();
  GNotification *self = PHPGTK_SELF(GNotification, G_TYPE_NOTIFICATION);
  if (!phpgtk::check_utf8(label, 1)) RETURN_THROWS();
  if (!phpgtk::check_utf8(action, 2)) RETURN_THROWS();
  GVariant *target_v = nullptr;
  if (target != nullptr && Z_TYPE_P(target) != IS_NULL) {
    target_v = php_to_variant(target, nullptr);
    if (target_v == nullptr) RETURN_THROWS();
    g_variant_ref_sink(target_v);
  }
  g_notification_add_button_with_target_value(self, ZSTR_VAL(label), ZSTR_VAL(action), target_v);
  if (target_v != nullptr) g_variant_unref(target_v);
}

/**
 * Gtk4\GNotification::set_body(?string $body): void
 *
 * Sets the body of $notification to $body.
 */
ZEND_METHOD(Gtk4_GNotification, set_body) {
  zend_string *body = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR_OR_NULL(body)
  ZEND_PARSE_PARAMETERS_END();
  GNotification *self = PHPGTK_SELF(GNotification, G_TYPE_NOTIFICATION);
  if (body != nullptr && !phpgtk::check_utf8(body, 1)) RETURN_THROWS();
  g_notification_set_body(self, body != nullptr ? ZSTR_VAL(body) : nullptr);
}

/**
 * Gtk4\GNotification::set_category(?string $category): void
 *
 * Sets the type of $notification to $category. Categories have a main type like `email`, `im` or
 * `device` and can have a detail separated by a `.`, e.g. `im.received` or `email.arrived`.
 * Setting the category helps the notification server to select proper feedback to the user.
 */
ZEND_METHOD(Gtk4_GNotification, set_category) {
  zend_string *category = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR_OR_NULL(category)
  ZEND_PARSE_PARAMETERS_END();
  GNotification *self = PHPGTK_SELF(GNotification, G_TYPE_NOTIFICATION);
  if (category != nullptr && !phpgtk::check_utf8(category, 1)) RETURN_THROWS();
  g_notification_set_category(self, category != nullptr ? ZSTR_VAL(category) : nullptr);
}

/**
 * Gtk4\GNotification::set_default_action(string $detailed_action): void
 *
 * Sets the default action of $notification to $detailed_action. This action is activated when the
 * notification is clicked on.
 */
ZEND_METHOD(Gtk4_GNotification, set_default_action) {
  zend_string *detailed_action;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(detailed_action)
  ZEND_PARSE_PARAMETERS_END();
  GNotification *self = PHPGTK_SELF(GNotification, G_TYPE_NOTIFICATION);
  if (!phpgtk::check_utf8(detailed_action, 1)) RETURN_THROWS();
  g_notification_set_default_action(self, ZSTR_VAL(detailed_action));
}

/**
 * Gtk4\GNotification::set_default_action_and_target(string $action, mixed $target = null): void
 *
 * Sets the default action of $notification to $action. This action is activated when the
 * notification is clicked on. It must be an application-wide action (start with "app.").
 */
ZEND_METHOD(Gtk4_GNotification, set_default_action_and_target) {
  zend_string *action;
  zval *target = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 2)
  Z_PARAM_STR(action)
  Z_PARAM_OPTIONAL
  Z_PARAM_ZVAL(target)
  ZEND_PARSE_PARAMETERS_END();
  GNotification *self = PHPGTK_SELF(GNotification, G_TYPE_NOTIFICATION);
  if (!phpgtk::check_utf8(action, 1)) RETURN_THROWS();
  GVariant *target_v = nullptr;
  if (target != nullptr && Z_TYPE_P(target) != IS_NULL) {
    target_v = php_to_variant(target, nullptr);
    if (target_v == nullptr) RETURN_THROWS();
    g_variant_ref_sink(target_v);
  }
  g_notification_set_default_action_and_target_value(self, ZSTR_VAL(action), target_v);
  if (target_v != nullptr) g_variant_unref(target_v);
}

/**
 * Gtk4\GNotification::set_icon(GIcon $icon): void
 *
 * Sets the icon of $notification to $icon.
 */
ZEND_METHOD(Gtk4_GNotification, set_icon) {
  zval *icon;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(icon, class_for_gtype(G_TYPE_ICON))
  ZEND_PARSE_PARAMETERS_END();
  GNotification *self = PHPGTK_SELF(GNotification, G_TYPE_NOTIFICATION);
  GObject *icon_o = unwrap(icon, G_TYPE_ICON);
  if (icon_o == nullptr) RETURN_THROWS();
  g_notification_set_icon(self, G_ICON(icon_o));
}

/**
 * Gtk4\GNotification::set_priority(GNotificationPriority $priority): void
 *
 * Sets the priority of $notification to $priority. See #GNotificationPriority for possible values.
 */
ZEND_METHOD(Gtk4_GNotification, set_priority) {
  zval *priority;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(priority, enum_class_for_type(G_TYPE_NOTIFICATION_PRIORITY))
  ZEND_PARSE_PARAMETERS_END();
  GNotification *self = PHPGTK_SELF(GNotification, G_TYPE_NOTIFICATION);
  gint priority_v = 0;
  if (!enum_from_php(priority, G_TYPE_NOTIFICATION_PRIORITY, &priority_v)) RETURN_THROWS();
  g_notification_set_priority(self, static_cast<GNotificationPriority>(priority_v));
}

/**
 * Gtk4\GNotification::set_title(string $title): void
 *
 * Sets the title of $notification to $title.
 */
ZEND_METHOD(Gtk4_GNotification, set_title) {
  zend_string *title;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(title)
  ZEND_PARSE_PARAMETERS_END();
  GNotification *self = PHPGTK_SELF(GNotification, G_TYPE_NOTIFICATION);
  if (!phpgtk::check_utf8(title, 1)) RETURN_THROWS();
  g_notification_set_title(self, ZSTR_VAL(title));
}
