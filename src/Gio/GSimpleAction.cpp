// GENERATED by gen/gir.php from Gio.SimpleAction - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GSimpleAction
#include "php_gtk4.h"
#include "core/object.h"
#include "core/variant.h"

using namespace phpgtk;

/**
 * Gtk4\GSimpleAction::__construct(string $name, ?string $parameter_type = null)
 *
 * Creates a new action.
 */
ZEND_METHOD(Gtk4_GSimpleAction, __construct) {
  zend_string *name;
  zend_string *parameter_type = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 2)
  Z_PARAM_STR(name)
  Z_PARAM_OPTIONAL
  Z_PARAM_STR_OR_NULL(parameter_type)
  ZEND_PARSE_PARAMETERS_END();
  if (!phpgtk::check_utf8(name, 1)) RETURN_THROWS();
  GVariantType *parameter_type_t = nullptr;
  if (parameter_type != nullptr) {
    if (!g_variant_type_string_is_valid(ZSTR_VAL(parameter_type))) {
      zend_argument_value_error(2, "must be a valid GVariant type string, \"%s\" given",
                                ZSTR_VAL(parameter_type));
      RETURN_THROWS();
    }
    parameter_type_t = g_variant_type_new(ZSTR_VAL(parameter_type));
  }
  GSimpleAction *call_result = g_simple_action_new(ZSTR_VAL(name), parameter_type_t);
  if (parameter_type_t != nullptr) g_variant_type_free(parameter_type_t);
  GObject *obj = G_OBJECT(call_result);
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * static Gtk4\GSimpleAction::new_stateful(string $name, ?string $parameter_type, mixed $state =
 * null): GSimpleAction
 *
 * Creates a new stateful action.
 */
ZEND_METHOD(Gtk4_GSimpleAction, new_stateful) {
  zend_string *name;
  zend_string *parameter_type = nullptr;
  zval *state = nullptr;
  ZEND_PARSE_PARAMETERS_START(2, 3)
  Z_PARAM_STR(name)
  Z_PARAM_STR_OR_NULL(parameter_type)
  Z_PARAM_OPTIONAL
  Z_PARAM_ZVAL(state)
  ZEND_PARSE_PARAMETERS_END();
  if (!phpgtk::check_utf8(name, 1)) RETURN_THROWS();
  GVariantType *parameter_type_t = nullptr;
  if (parameter_type != nullptr) {
    if (!g_variant_type_string_is_valid(ZSTR_VAL(parameter_type))) {
      zend_argument_value_error(2, "must be a valid GVariant type string, \"%s\" given",
                                ZSTR_VAL(parameter_type));
      RETURN_THROWS();
    }
    parameter_type_t = g_variant_type_new(ZSTR_VAL(parameter_type));
  }
  GVariant *state_v = nullptr;
  if (state != nullptr && Z_TYPE_P(state) != IS_NULL) {
    state_v = php_to_variant(state, nullptr);
    if (state_v == nullptr) RETURN_THROWS();
    g_variant_ref_sink(state_v);
  }
  GSimpleAction *call_result =
      g_simple_action_new_stateful(ZSTR_VAL(name), parameter_type_t, state_v);
  if (parameter_type_t != nullptr) g_variant_type_free(parameter_type_t);
  if (state_v != nullptr) g_variant_unref(state_v);
  GObject *obj = G_OBJECT(call_result);
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * Gtk4\GSimpleAction::set_enabled(bool $enabled): void
 *
 * Sets the action as enabled or not.
 */
ZEND_METHOD(Gtk4_GSimpleAction, set_enabled) {
  bool enabled;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(enabled)
  ZEND_PARSE_PARAMETERS_END();
  GSimpleAction *self = PHPGTK_SELF(GSimpleAction, G_TYPE_SIMPLE_ACTION);
  g_simple_action_set_enabled(self, enabled);
}

/**
 * Gtk4\GSimpleAction::set_state_hint(mixed $state_hint = null): void
 *
 * Sets the state hint for the action.
 */
ZEND_METHOD(Gtk4_GSimpleAction, set_state_hint) {
  zval *state_hint = nullptr;
  ZEND_PARSE_PARAMETERS_START(0, 1)
  Z_PARAM_OPTIONAL
  Z_PARAM_ZVAL(state_hint)
  ZEND_PARSE_PARAMETERS_END();
  GSimpleAction *self = PHPGTK_SELF(GSimpleAction, G_TYPE_SIMPLE_ACTION);
  GVariant *state_hint_v = nullptr;
  if (state_hint != nullptr && Z_TYPE_P(state_hint) != IS_NULL) {
    state_hint_v = php_to_variant(state_hint, nullptr);
    if (state_hint_v == nullptr) RETURN_THROWS();
    g_variant_ref_sink(state_hint_v);
  }
  g_simple_action_set_state_hint(self, state_hint_v);
  if (state_hint_v != nullptr) g_variant_unref(state_hint_v);
}

/**
 * public function set_state(mixed $value): void
 * Set the state directly (emits `notify::state`, not `change-state`). The value is converted
 * to the action's state type; a stateless action throws LogicException.
 */
ZEND_METHOD(Gtk4_GSimpleAction, set_state) {
  zval *value;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_ZVAL(value)
  ZEND_PARSE_PARAMETERS_END();
  GSimpleAction *self = PHPGTK_SELF(GSimpleAction, G_TYPE_SIMPLE_ACTION);
  const GVariantType *t = g_action_get_state_type(G_ACTION(self));
  if (t == nullptr) {
    zend_throw_exception_ex(spl_ce_LogicException, 0,
                            "GSimpleAction::set_state(): action '%s' is stateless",
                            g_action_get_name(G_ACTION(self)));
    RETURN_THROWS();
  }
  GVariant *v = php_to_variant(value, t);
  if (v == nullptr) RETURN_THROWS();
  g_simple_action_set_state(self, v);
}
