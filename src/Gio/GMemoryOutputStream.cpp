// GENERATED by gen/gir.php from Gio.MemoryOutputStream - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GMemoryOutputStream
#include "php_gtk4.h"
#include "core/object.h"
#include "core/subtype.h"

using namespace phpgtk;

/**
 * Gtk4\GMemoryOutputStream::__construct()
 *
 * A GMemoryOutputStream with default properties (GTK's own constructor is varargs-only; set the
 * properties afterwards).
 */
ZEND_METHOD(Gtk4_GMemoryOutputStream, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = static_cast<GObject *>(g_object_new(G_TYPE_MEMORY_OUTPUT_STREAM, nullptr));
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * static Gtk4\GMemoryOutputStream::new_resizable(): GMemoryOutputStream
 *
 * Creates a new #GMemoryOutputStream, using g_realloc() and g_free() for memory allocation.
 */
ZEND_METHOD(Gtk4_GMemoryOutputStream, new_resizable) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = G_OBJECT(g_memory_output_stream_new_resizable());
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * Gtk4\GMemoryOutputStream::get_data_size(): int
 *
 * Returns the number of bytes from the start up to including the last byte written in the stream
 * that has not been truncated away.
 */
ZEND_METHOD(Gtk4_GMemoryOutputStream, get_data_size) {
  ZEND_PARSE_PARAMETERS_NONE();
  GMemoryOutputStream *self = PHPGTK_SELF(GMemoryOutputStream, G_TYPE_MEMORY_OUTPUT_STREAM);
  RETURN_LONG(static_cast<zend_long>(g_memory_output_stream_get_data_size(self)));
}

/**
 * Gtk4\GMemoryOutputStream::get_size(): int
 *
 * Gets the size of the currently allocated data area (available from
 * g_memory_output_stream_get_data()).
 */
ZEND_METHOD(Gtk4_GMemoryOutputStream, get_size) {
  ZEND_PARSE_PARAMETERS_NONE();
  GMemoryOutputStream *self = PHPGTK_SELF(GMemoryOutputStream, G_TYPE_MEMORY_OUTPUT_STREAM);
  RETURN_LONG(static_cast<zend_long>(g_memory_output_stream_get_size(self)));
}

/**
 * Gtk4\GMemoryOutputStream::steal_as_bytes(): string
 *
 * Returns data from the $ostream as a #GBytes. $ostream must be closed before calling this
 * function.
 */
ZEND_METHOD(Gtk4_GMemoryOutputStream, steal_as_bytes) {
  ZEND_PARSE_PARAMETERS_NONE();
  GMemoryOutputStream *self = PHPGTK_SELF(GMemoryOutputStream, G_TYPE_MEMORY_OUTPUT_STREAM);
  const bool state_holds = g_output_stream_is_closed(G_OUTPUT_STREAM(self)) == TRUE;
  if (!state_holds) {
    zend_throw_exception_ex(spl_ce_LogicException, 0,
                            "%s(): the stream is still open - close() it before taking its bytes",
                            ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  GBytes *bytes = g_memory_output_stream_steal_as_bytes(self);
  gsize size = 0;
  const auto *data = static_cast<const char *>(g_bytes_get_data(bytes, &size));
  RETVAL_STRINGL(data, size);
  g_bytes_unref(bytes);
}
