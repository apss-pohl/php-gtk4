// GENERATED by gen/gir.php from Gio.DBusArgInfo - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GDBusArgInfo (boxed value type)
#include "php_gtk4.h"
#include "core/boxed.h"
#include "core/object.h"
#include <cstring>

using namespace phpgtk;

namespace {

const char *const fields[] = {"name", "signature", nullptr};

// Boxed field reader: the public scalar fields as PHP properties.
bool read(gpointer data, const char *field, zval *rv) {
  auto *value = static_cast<GDBusArgInfo *>(data);
  if (strcmp(field, "name") == 0) {
    if (value->name == nullptr) {
      ZVAL_NULL(rv);
    } else {
      ZVAL_STRING(rv, value->name);
    }
    return true;
  }
  if (strcmp(field, "signature") == 0) {
    if (value->signature == nullptr) {
      ZVAL_NULL(rv);
    } else {
      ZVAL_STRING(rv, value->signature);
    }
    return true;
  }
  return false;
}

// Boxed field writer: each field converts like a parameter of its type (core/boxed).
bool write(gpointer data, const char *field, zval *v) {
  (void)data;
  (void)field;
  (void)v;
  return false;
}

}  // namespace

/**
 * Gtk4\GDBusArgInfo::__construct()
 *
 * GDBusArgInfo values come from GTK, never from `new`.
 */
ZEND_METHOD(Gtk4_GDBusArgInfo, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
}

namespace phpgtk {
// MINIT: bind the PHP class to G_TYPE_DBUS_ARG_INFO with its field table.
void register_GDBusArgInfo(zend_class_entry *ce) {
  register_boxed(BoxedClass{
      .type = G_TYPE_DBUS_ARG_INFO, .ce = ce, .fields = fields, .read = read, .write = write});
}
}  // namespace phpgtk
