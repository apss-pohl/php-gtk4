// GENERATED by gen/gir.php from Gio.MenuItem - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GMenuItem
#include "php_gtk4.h"
#include "core/object.h"
#include "core/variant.h"

using namespace phpgtk;

/**
 * Gtk4\GMenuItem::__construct(?string $label = null, ?string $detailed_action = null)
 *
 * Creates a new #GMenuItem.
 */
ZEND_METHOD(Gtk4_GMenuItem, __construct) {
  zend_string *label = nullptr;
  zend_string *detailed_action = nullptr;
  ZEND_PARSE_PARAMETERS_START(0, 2)
  Z_PARAM_OPTIONAL
  Z_PARAM_STR_OR_NULL(label)
  Z_PARAM_STR_OR_NULL(detailed_action)
  ZEND_PARSE_PARAMETERS_END();
  if (label != nullptr && !phpgtk::check_utf8(label, 1)) RETURN_THROWS();
  if (detailed_action != nullptr && !phpgtk::check_utf8(detailed_action, 2)) RETURN_THROWS();
  GObject *obj =
      G_OBJECT(g_menu_item_new(label != nullptr ? ZSTR_VAL(label) : nullptr,
                               detailed_action != nullptr ? ZSTR_VAL(detailed_action) : nullptr));
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * static Gtk4\GMenuItem::new_from_model(GMenuModel $model, int $item_index): GMenuItem
 *
 * Creates a #GMenuItem as an exact copy of an existing menu item in a #GMenuModel.
 */
ZEND_METHOD(Gtk4_GMenuItem, new_from_model) {
  zval *model;
  zend_long item_index;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_OBJECT_OF_CLASS(model, class_for_gtype(G_TYPE_MENU_MODEL))
  Z_PARAM_LONG(item_index)
  ZEND_PARSE_PARAMETERS_END();
  GObject *model_o = unwrap(model, G_TYPE_MENU_MODEL);
  if (model_o == nullptr) RETURN_THROWS();
  if (!phpgtk::check_range<gint>(item_index, 2)) RETURN_THROWS();
  GObject *obj =
      G_OBJECT(g_menu_item_new_from_model(G_MENU_MODEL(model_o), static_cast<gint>(item_index)));
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * static Gtk4\GMenuItem::new_section(?string $label, GMenuModel $section): GMenuItem
 *
 * Creates a new #GMenuItem representing a section.
 */
ZEND_METHOD(Gtk4_GMenuItem, new_section) {
  zend_string *label = nullptr;
  zval *section;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_STR_OR_NULL(label)
  Z_PARAM_OBJECT_OF_CLASS(section, class_for_gtype(G_TYPE_MENU_MODEL))
  ZEND_PARSE_PARAMETERS_END();
  if (label != nullptr && !phpgtk::check_utf8(label, 1)) RETURN_THROWS();
  GObject *section_o = unwrap(section, G_TYPE_MENU_MODEL);
  if (section_o == nullptr) RETURN_THROWS();
  GObject *obj = G_OBJECT(g_menu_item_new_section(label != nullptr ? ZSTR_VAL(label) : nullptr,
                                                  G_MENU_MODEL(section_o)));
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * static Gtk4\GMenuItem::new_submenu(?string $label, GMenuModel $submenu): GMenuItem
 *
 * Creates a new #GMenuItem representing a submenu.
 */
ZEND_METHOD(Gtk4_GMenuItem, new_submenu) {
  zend_string *label = nullptr;
  zval *submenu;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_STR_OR_NULL(label)
  Z_PARAM_OBJECT_OF_CLASS(submenu, class_for_gtype(G_TYPE_MENU_MODEL))
  ZEND_PARSE_PARAMETERS_END();
  if (label != nullptr && !phpgtk::check_utf8(label, 1)) RETURN_THROWS();
  GObject *submenu_o = unwrap(submenu, G_TYPE_MENU_MODEL);
  if (submenu_o == nullptr) RETURN_THROWS();
  GObject *obj = G_OBJECT(g_menu_item_new_submenu(label != nullptr ? ZSTR_VAL(label) : nullptr,
                                                  G_MENU_MODEL(submenu_o)));
  wrap(obj, return_value);
  if (obj != nullptr) g_object_unref(obj);  // the handle took its own reference
}

/**
 * Gtk4\GMenuItem::get_attribute_value(string $attribute, ?string $expected_type): mixed
 *
 * Queries the named $attribute on $menu_item.
 */
ZEND_METHOD(Gtk4_GMenuItem, get_attribute_value) {
  zend_string *attribute;
  zend_string *expected_type = nullptr;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_STR(attribute)
  Z_PARAM_STR_OR_NULL(expected_type)
  ZEND_PARSE_PARAMETERS_END();
  GMenuItem *self = PHPGTK_SELF(GMenuItem, G_TYPE_MENU_ITEM);
  if (!phpgtk::check_utf8(attribute, 1)) RETURN_THROWS();
  GVariantType *expected_type_t = nullptr;
  if (expected_type != nullptr) {
    if (!g_variant_type_string_is_valid(ZSTR_VAL(expected_type))) {
      zend_argument_value_error(2, "must be a valid GVariant type string, \"%s\" given",
                                ZSTR_VAL(expected_type));
      RETURN_THROWS();
    }
    expected_type_t = g_variant_type_new(ZSTR_VAL(expected_type));
  }
  GVariant *call_result =
      g_menu_item_get_attribute_value(self, ZSTR_VAL(attribute), expected_type_t);
  if (expected_type_t != nullptr) g_variant_type_free(expected_type_t);
  GVariant *phpgtk_ret = call_result;
  if (phpgtk_ret == nullptr) RETURN_NULL();
  variant_to_php(phpgtk_ret, return_value);
  g_variant_unref(phpgtk_ret);
}

/**
 * Gtk4\GMenuItem::get_link(string $link): ?GMenuModel
 *
 * Queries the named $link on $menu_item.
 */
ZEND_METHOD(Gtk4_GMenuItem, get_link) {
  zend_string *link;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(link)
  ZEND_PARSE_PARAMETERS_END();
  GMenuItem *self = PHPGTK_SELF(GMenuItem, G_TYPE_MENU_ITEM);
  if (!phpgtk::check_utf8(link, 1)) RETURN_THROWS();
  if (!(phpgtk::valid_menu_attribute_name(ZSTR_VAL(link)))) {
    zend_argument_value_error(1, "must be a letter followed by letters, digits and dashes");
    RETURN_THROWS();
  }
  GMenuModel *phpgtk_ret = g_menu_item_get_link(self, ZSTR_VAL(link));
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
  if (phpgtk_ret != nullptr) g_object_unref(phpgtk_ret);  // the handle took its own ref
}

/**
 * Gtk4\GMenuItem::set_action_and_target_value(?string $action, mixed $target_value = null): void
 *
 * Sets or unsets the "action" and "target" attributes of $menu_item.
 */
ZEND_METHOD(Gtk4_GMenuItem, set_action_and_target_value) {
  zend_string *action = nullptr;
  zval *target_value = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 2)
  Z_PARAM_STR_OR_NULL(action)
  Z_PARAM_OPTIONAL
  Z_PARAM_ZVAL(target_value)
  ZEND_PARSE_PARAMETERS_END();
  GMenuItem *self = PHPGTK_SELF(GMenuItem, G_TYPE_MENU_ITEM);
  if (action != nullptr && !phpgtk::check_utf8(action, 1)) RETURN_THROWS();
  GVariant *target_value_v = nullptr;
  if (target_value != nullptr && Z_TYPE_P(target_value) != IS_NULL) {
    target_value_v = php_to_variant(target_value, nullptr);
    if (target_value_v == nullptr) RETURN_THROWS();
    g_variant_ref_sink(target_value_v);
  }
  g_menu_item_set_action_and_target_value(self, action != nullptr ? ZSTR_VAL(action) : nullptr,
                                          target_value_v);
  if (target_value_v != nullptr) g_variant_unref(target_value_v);
}

/**
 * Gtk4\GMenuItem::set_attribute_value(string $attribute, mixed $value = null): void
 *
 * Sets or unsets an attribute on $menu_item.
 */
ZEND_METHOD(Gtk4_GMenuItem, set_attribute_value) {
  zend_string *attribute;
  zval *value = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 2)
  Z_PARAM_STR(attribute)
  Z_PARAM_OPTIONAL
  Z_PARAM_ZVAL(value)
  ZEND_PARSE_PARAMETERS_END();
  GMenuItem *self = PHPGTK_SELF(GMenuItem, G_TYPE_MENU_ITEM);
  if (!phpgtk::check_utf8(attribute, 1)) RETURN_THROWS();
  if (!(phpgtk::valid_menu_attribute_name(ZSTR_VAL(attribute)))) {
    zend_argument_value_error(1, "must be a letter followed by letters, digits and dashes");
    RETURN_THROWS();
  }
  GVariant *value_v = nullptr;
  if (value != nullptr && Z_TYPE_P(value) != IS_NULL) {
    value_v = php_to_variant(value, nullptr);
    if (value_v == nullptr) RETURN_THROWS();
    g_variant_ref_sink(value_v);
  }
  g_menu_item_set_attribute_value(self, ZSTR_VAL(attribute), value_v);
  if (value_v != nullptr) g_variant_unref(value_v);
}

/**
 * Gtk4\GMenuItem::set_detailed_action(string $detailed_action): void
 *
 * Sets the "action" and possibly the "target" attribute of $menu_item.
 */
ZEND_METHOD(Gtk4_GMenuItem, set_detailed_action) {
  zend_string *detailed_action;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(detailed_action)
  ZEND_PARSE_PARAMETERS_END();
  GMenuItem *self = PHPGTK_SELF(GMenuItem, G_TYPE_MENU_ITEM);
  if (!phpgtk::check_utf8(detailed_action, 1)) RETURN_THROWS();
  g_menu_item_set_detailed_action(self, ZSTR_VAL(detailed_action));
}

/**
 * Gtk4\GMenuItem::set_icon(GIcon $icon): void
 *
 * Sets (or unsets) the icon on $menu_item.
 */
ZEND_METHOD(Gtk4_GMenuItem, set_icon) {
  zval *icon;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(icon, class_for_gtype(G_TYPE_ICON))
  ZEND_PARSE_PARAMETERS_END();
  GMenuItem *self = PHPGTK_SELF(GMenuItem, G_TYPE_MENU_ITEM);
  GObject *icon_o = unwrap(icon, G_TYPE_ICON);
  if (icon_o == nullptr) RETURN_THROWS();
  g_menu_item_set_icon(self, G_ICON(icon_o));
}

/**
 * Gtk4\GMenuItem::set_label(?string $label): void
 *
 * Sets or unsets the "label" attribute of $menu_item.
 */
ZEND_METHOD(Gtk4_GMenuItem, set_label) {
  zend_string *label = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR_OR_NULL(label)
  ZEND_PARSE_PARAMETERS_END();
  GMenuItem *self = PHPGTK_SELF(GMenuItem, G_TYPE_MENU_ITEM);
  if (label != nullptr && !phpgtk::check_utf8(label, 1)) RETURN_THROWS();
  g_menu_item_set_label(self, label != nullptr ? ZSTR_VAL(label) : nullptr);
}

/**
 * Gtk4\GMenuItem::set_link(string $link, ?GMenuModel $model): void
 *
 * Creates a link from $menu_item to $model if non-`null`, or unsets it.
 */
ZEND_METHOD(Gtk4_GMenuItem, set_link) {
  zend_string *link;
  zval *model = nullptr;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_STR(link)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(model, class_for_gtype(G_TYPE_MENU_MODEL))
  ZEND_PARSE_PARAMETERS_END();
  GMenuItem *self = PHPGTK_SELF(GMenuItem, G_TYPE_MENU_ITEM);
  if (!phpgtk::check_utf8(link, 1)) RETURN_THROWS();
  if (!(phpgtk::valid_menu_attribute_name(ZSTR_VAL(link)))) {
    zend_argument_value_error(1, "must be a letter followed by letters, digits and dashes");
    RETURN_THROWS();
  }
  GObject *model_o = nullptr;
  if (model != nullptr) {
    model_o = unwrap(model, G_TYPE_MENU_MODEL);
    if (model_o == nullptr) RETURN_THROWS();
  }
  g_menu_item_set_link(self, ZSTR_VAL(link), model_o != nullptr ? G_MENU_MODEL(model_o) : nullptr);
}

/**
 * Gtk4\GMenuItem::set_section(?GMenuModel $section): void
 *
 * Sets or unsets the "section" link of $menu_item to $section.
 */
ZEND_METHOD(Gtk4_GMenuItem, set_section) {
  zval *section = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(section, class_for_gtype(G_TYPE_MENU_MODEL))
  ZEND_PARSE_PARAMETERS_END();
  GMenuItem *self = PHPGTK_SELF(GMenuItem, G_TYPE_MENU_ITEM);
  GObject *section_o = nullptr;
  if (section != nullptr) {
    section_o = unwrap(section, G_TYPE_MENU_MODEL);
    if (section_o == nullptr) RETURN_THROWS();
  }
  g_menu_item_set_section(self, section_o != nullptr ? G_MENU_MODEL(section_o) : nullptr);
}

/**
 * Gtk4\GMenuItem::set_submenu(?GMenuModel $submenu): void
 *
 * Sets or unsets the "submenu" link of $menu_item to $submenu.
 */
ZEND_METHOD(Gtk4_GMenuItem, set_submenu) {
  zval *submenu = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(submenu, class_for_gtype(G_TYPE_MENU_MODEL))
  ZEND_PARSE_PARAMETERS_END();
  GMenuItem *self = PHPGTK_SELF(GMenuItem, G_TYPE_MENU_ITEM);
  GObject *submenu_o = nullptr;
  if (submenu != nullptr) {
    submenu_o = unwrap(submenu, G_TYPE_MENU_MODEL);
    if (submenu_o == nullptr) RETURN_THROWS();
  }
  g_menu_item_set_submenu(self, submenu_o != nullptr ? G_MENU_MODEL(submenu_o) : nullptr);
}
