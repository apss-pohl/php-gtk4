// GENERATED by gen/gir.php from Gio.DBusNodeInfo - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GDBusNodeInfo (boxed value type)
#include "php_gtk4.h"
#include "core/boxed.h"
#include "core/object.h"
#include <cstring>
#include "core/gerror.h"

using namespace phpgtk;

namespace {

const char *const fields[] = {"path", "interfaces", "nodes", nullptr};

// Boxed field reader: the public scalar fields as PHP properties.
bool read(gpointer data, const char *field, zval *rv) {
  auto *value = static_cast<GDBusNodeInfo *>(data);
  if (strcmp(field, "path") == 0) {
    if (value->path == nullptr) {
      ZVAL_NULL(rv);
    } else {
      ZVAL_STRING(rv, value->path);
    }
    return true;
  }
  if (strcmp(field, "interfaces") == 0) {
    array_init(rv);
    for (auto **item = value->interfaces; item != nullptr && *item != nullptr; item++) {
      zval h;
      wrap_boxed(G_TYPE_DBUS_INTERFACE_INFO, *item, &h);
      add_next_index_zval(rv, &h);
    }
    return true;
  }
  if (strcmp(field, "nodes") == 0) {
    array_init(rv);
    for (auto **item = value->nodes; item != nullptr && *item != nullptr; item++) {
      zval h;
      wrap_boxed(G_TYPE_DBUS_NODE_INFO, *item, &h);
      add_next_index_zval(rv, &h);
    }
    return true;
  }
  return false;
}

// Boxed field writer: each field converts like a parameter of its type (core/boxed).
bool write(gpointer data, const char *field, zval *v) {
  (void)data;
  (void)field;
  (void)v;
  return false;
}

}  // namespace

/**
 * Gtk4\GDBusNodeInfo::__construct()
 *
 * GDBusNodeInfo values come from GTK, never from `new`.
 */
ZEND_METHOD(Gtk4_GDBusNodeInfo, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
}

/**
 * static Gtk4\GDBusNodeInfo::new_for_xml(string $xml_data): GDBusNodeInfo
 *
 * Parses $xml_data and returns a #GDBusNodeInfo representing the data.
 */
ZEND_METHOD(Gtk4_GDBusNodeInfo, new_for_xml) {
  zend_string *xml_data;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(xml_data)
  ZEND_PARSE_PARAMETERS_END();
  if (!phpgtk::check_utf8(xml_data, 1)) RETURN_THROWS();
  GError *error = nullptr;
  gpointer phpgtk_ret = g_dbus_node_info_new_for_xml(ZSTR_VAL(xml_data), &error);
  if (phpgtk_ret == nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  wrap_boxed(G_TYPE_DBUS_NODE_INFO, phpgtk_ret, return_value);
  if (phpgtk_ret != nullptr) g_boxed_free(G_TYPE_DBUS_NODE_INFO, phpgtk_ret);
}

/**
 * Gtk4\GDBusNodeInfo::lookup_interface(string $name): ?GDBusInterfaceInfo
 *
 * Looks up information about an interface.
 */
ZEND_METHOD(Gtk4_GDBusNodeInfo, lookup_interface) {
  zend_string *name;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(name)
  ZEND_PARSE_PARAMETERS_END();
  GDBusNodeInfo *self = PHPGTK_BOXED_SELF(GDBusNodeInfo);
  if (!phpgtk::check_utf8(name, 1)) RETURN_THROWS();
  GDBusInterfaceInfo *phpgtk_ret = g_dbus_node_info_lookup_interface(self, ZSTR_VAL(name));
  wrap_boxed(G_TYPE_DBUS_INTERFACE_INFO, phpgtk_ret, return_value);
}

namespace phpgtk {
// MINIT: bind the PHP class to G_TYPE_DBUS_NODE_INFO with its field table.
void register_GDBusNodeInfo(zend_class_entry *ce) {
  register_boxed(BoxedClass{
      .type = G_TYPE_DBUS_NODE_INFO, .ce = ce, .fields = fields, .read = read, .write = write});
}
}  // namespace phpgtk
