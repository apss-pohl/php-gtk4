// GENERATED by gen/gir.php from Gio.DBusPropertyInfo - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GDBusPropertyInfo (boxed value type)
#include "php_gtk4.h"
#include "core/boxed.h"
#include "core/object.h"
#include <cstring>

using namespace phpgtk;

namespace {

const char *const fields[] = {"name", "signature", "flags", nullptr};

// Boxed field reader: the public scalar fields as PHP properties.
bool read(gpointer data, const char *field, zval *rv) {
  auto *value = static_cast<GDBusPropertyInfo *>(data);
  if (strcmp(field, "name") == 0) {
    if (value->name == nullptr) {
      ZVAL_NULL(rv);
    } else {
      ZVAL_STRING(rv, value->name);
    }
    return true;
  }
  if (strcmp(field, "signature") == 0) {
    if (value->signature == nullptr) {
      ZVAL_NULL(rv);
    } else {
      ZVAL_STRING(rv, value->signature);
    }
    return true;
  }
  if (strcmp(field, "flags") == 0) {
    ZVAL_LONG(rv, static_cast<zend_long>(value->flags));
    return true;
  }
  return false;
}

// Boxed field writer: each field converts like a parameter of its type (core/boxed).
bool write(gpointer data, const char *field, zval *v) {
  (void)data;
  (void)field;
  (void)v;
  return false;
}

}  // namespace

/**
 * Gtk4\GDBusPropertyInfo::__construct()
 *
 * GDBusPropertyInfo values come from GTK, never from `new`.
 */
ZEND_METHOD(Gtk4_GDBusPropertyInfo, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
}

namespace phpgtk {
// MINIT: bind the PHP class to G_TYPE_DBUS_PROPERTY_INFO with its field table.
void register_GDBusPropertyInfo(zend_class_entry *ce) {
  register_boxed(BoxedClass{
      .type = G_TYPE_DBUS_PROPERTY_INFO, .ce = ce, .fields = fields, .read = read, .write = write});
}
}  // namespace phpgtk
