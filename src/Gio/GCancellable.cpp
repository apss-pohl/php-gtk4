// GENERATED by gen/gir.php from Gio.Cancellable - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GCancellable
#include "php_gtk4.h"
#include "core/object.h"
#include "core/gerror.h"
#include "core/subtype.h"
#include "core/error.h"

using namespace phpgtk;

/**
 * Gtk4\GCancellable::__construct()
 *
 * Creates a new #GCancellable object.
 */
ZEND_METHOD(Gtk4_GCancellable, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
  GObject *obj = subtype_new(ZEND_THIS, nullptr);
  if (obj == nullptr) {
    if (EG(exception) != nullptr) RETURN_THROWS();
    obj = G_OBJECT(g_cancellable_new());
  }
  if (obj == nullptr) {
    zend_throw_error(nullptr,
                     "%s(): GTK refused to create the object (see the "
                     "CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  attach_new(object_from_zval(ZEND_THIS), obj);
}

/**
 * static Gtk4\GCancellable::get_current(): ?GCancellable
 *
 * Gets the top cancellable from the stack.
 */
ZEND_METHOD(Gtk4_GCancellable, get_current) {
  ZEND_PARSE_PARAMETERS_NONE();
  GCancellable *phpgtk_ret = g_cancellable_get_current();
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GCancellable::cancel(): void
 *
 * Will set $cancellable to cancelled, and will emit the #GCancellable::cancelled signal. (However,
 * see the warning about race conditions in the documentation for that signal if you are planning
 * to connect to it.)
 */
ZEND_METHOD(Gtk4_GCancellable, cancel) {
  ZEND_PARSE_PARAMETERS_NONE();
  GCancellable *self = PHPGTK_SELF(GCancellable, G_TYPE_CANCELLABLE);
  g_cancellable_cancel(self);
}

/**
 * Gtk4\GCancellable::disconnect(int $handler_id): void
 *
 * Disconnects a handler from a cancellable instance similar to g_signal_handler_disconnect().
 * Additionally, in the event that a signal handler is currently running, this call will block
 * until the handler has finished. Calling this function from a #GCancellable::cancelled signal
 * handler will therefore result in a deadlock.
 */
ZEND_METHOD(Gtk4_GCancellable, disconnect) {
  zend_long handler_id;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(handler_id)
  ZEND_PARSE_PARAMETERS_END();
  GCancellable *self = PHPGTK_SELF(GCancellable, G_TYPE_CANCELLABLE);
  if (!phpgtk::check_range<gulong>(handler_id, 1)) RETURN_THROWS();
  const bool precondition_0 =
      handler_id == 0 ||
      g_signal_handler_is_connected(G_OBJECT(self), static_cast<gulong>(handler_id));
  if (!precondition_0) {
    zend_argument_value_error(1, "is not a connected handler id");
    RETURN_THROWS();
  }
  g_cancellable_disconnect(self, static_cast<gulong>(handler_id));
}

/**
 * Gtk4\GCancellable::get_fd(): int
 *
 * Gets the file descriptor for a cancellable job. This can be used to implement cancellable
 * operations on Unix systems. The returned fd will turn readable when $cancellable is cancelled.
 */
ZEND_METHOD(Gtk4_GCancellable, get_fd) {
  ZEND_PARSE_PARAMETERS_NONE();
  GCancellable *self = PHPGTK_SELF(GCancellable, G_TYPE_CANCELLABLE);
  RETURN_LONG(static_cast<zend_long>(g_cancellable_get_fd(self)));
}

/**
 * Gtk4\GCancellable::is_cancelled(): bool
 *
 * Checks if a cancellable job has been cancelled.
 */
ZEND_METHOD(Gtk4_GCancellable, is_cancelled) {
  ZEND_PARSE_PARAMETERS_NONE();
  GCancellable *self = PHPGTK_SELF(GCancellable, G_TYPE_CANCELLABLE);
  RETURN_BOOL(g_cancellable_is_cancelled(self));
}

/**
 * Gtk4\GCancellable::pop_current(): void
 *
 * Pops $cancellable off the cancellable stack (verifying that $cancellable is on the top of the
 * stack).
 */
ZEND_METHOD(Gtk4_GCancellable, pop_current) {
  ZEND_PARSE_PARAMETERS_NONE();
  GCancellable *self = PHPGTK_SELF(GCancellable, G_TYPE_CANCELLABLE);
  g_cancellable_pop_current(self);
}

/**
 * Gtk4\GCancellable::push_current(): void
 *
 * Pushes $cancellable onto the cancellable stack. The current cancellable can then be received
 * using g_cancellable_get_current().
 */
ZEND_METHOD(Gtk4_GCancellable, push_current) {
  ZEND_PARSE_PARAMETERS_NONE();
  GCancellable *self = PHPGTK_SELF(GCancellable, G_TYPE_CANCELLABLE);
  g_cancellable_push_current(self);
}

/**
 * Gtk4\GCancellable::reset(): void
 *
 * Resets $cancellable to its uncancelled state.
 */
ZEND_METHOD(Gtk4_GCancellable, reset) {
  ZEND_PARSE_PARAMETERS_NONE();
  GCancellable *self = PHPGTK_SELF(GCancellable, G_TYPE_CANCELLABLE);
  g_cancellable_reset(self);
}

/**
 * Gtk4\GCancellable::set_error_if_cancelled(): bool
 *
 * If the $cancellable is cancelled, sets the error to notify that the operation was cancelled.
 */
ZEND_METHOD(Gtk4_GCancellable, set_error_if_cancelled) {
  ZEND_PARSE_PARAMETERS_NONE();
  GCancellable *self = PHPGTK_SELF(GCancellable, G_TYPE_CANCELLABLE);
  GError *error = nullptr;
  const gboolean ok = g_cancellable_set_error_if_cancelled(self, &error);
  if (error != nullptr) {
    throw_gerror(error);
    RETURN_THROWS();
  }
  RETURN_BOOL(ok);
}

// vfunc thunks and installers: file-local, installed by class_init of a PHP subtype
namespace {

// vfunc thunk: G_CANCELLABLE_CLASS->cancelled -> $this->vfunc_cancelled() on a PHP subclass
void vfunc_thunk_cancelled(GCancellable *self) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "vfunc_cancelled", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    auto *native = G_CANCELLABLE_CLASS(subtype_native_class(G_OBJECT(self)));
    if (native->cancelled != nullptr) native->cancelled(self);
    return;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  zval ret;
  ZVAL_UNDEF(&ret);
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GCancellable::vfunc_cancelled");
  zend_exception_restore();  // the parked one, previous of whatever this threw
}

// vfunc installer: G_CANCELLABLE_CLASS->cancelled (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_cancelled(gpointer klass) {
  G_CANCELLABLE_CLASS(klass)->cancelled = vfunc_thunk_cancelled;
}

}  // namespace

/**
 * Gtk4\GCancellable::vfunc_cancelled(): void
 *
 * Native `cancelled` (CancellableClass.cancelled): the GTK implementation below any PHP subclass,
 * for `parent::vfunc_cancelled()` from an override.
 */
ZEND_METHOD(Gtk4_GCancellable, vfunc_cancelled) {
  ZEND_PARSE_PARAMETERS_NONE();
  GCancellable *self = PHPGTK_SELF(GCancellable, G_TYPE_CANCELLABLE);
  if (!is_php_type(G_OBJECT_TYPE(self))) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "GCancellable::vfunc_cancelled(): for parent:: chaining from a PHP subclass "
        "only; call the public method instead");
    RETURN_THROWS();
  }
  auto *klass = G_CANCELLABLE_CLASS(subtype_native_class(G_OBJECT(self)));
  if (klass->cancelled == nullptr) {
    return;
  }
  klass->cancelled(self);
}

// MINIT: the vfunc thunks of GCancellable (core/subtype.h).
void register_vfuncs_GCancellable() {
  register_vfunc(G_TYPE_CANCELLABLE, "cancelled", vfunc_install_cancelled);
}
