// GENERATED by gen/gir.php from Gio.DBusMethodInvocation - do not edit (gen/overrides,
// gen/skip.txt). Gtk4\GDBusMethodInvocation
#include "php_gtk4.h"
#include "core/object.h"
#include "core/boxed.h"

using namespace phpgtk;

// The reply methods: the reply typed from the method's introspection (core/variant tuple),
// and GLib's reference on the invocation consumed exactly once. GLib hands the method-call
// handler an invocation it expects a single return_*() to take; the handle holds a reference
// of its own, so a second answer would eat that one and leave the handle on a dead object.
#include "core/variant.h"
#include "core/gerror.h"

#include <string>

namespace {

// qdata marking an invocation that answered.
GQuark answered_quark() {
  static const GQuark quark = g_quark_from_static_string("php-gtk4-invocation-answered");
  return quark;
}

// False (LogicException pending) once the invocation answered; check before converting the
// reply, so a wrong second reply is still reported as the second one.
bool invocation_open(GDBusMethodInvocation *invocation, const char *method) {
  if (g_object_get_qdata(G_OBJECT(invocation), answered_quark()) != nullptr) {
    zend_throw_exception_ex(
        spl_ce_LogicException, 0,
        "Gtk4\\GDBusMethodInvocation::%s(): this invocation was answered already", method);
    return false;
  }
  return true;
}

// Marks the invocation answered: the next return_*() is refused. Any non-null pointer does;
// the object itself saves an int-to-pointer cast.
void invocation_answered(GDBusMethodInvocation *invocation) {
  g_object_set_qdata(G_OBJECT(invocation), answered_quark(), invocation);
}

}  // namespace

/**
 * Gtk4\GDBusMethodInvocation::__construct()
 *
 * GDBusMethodInvocation has no constructor in GTK: instances come from GTK, never from `new`.
 */
ZEND_METHOD(Gtk4_GDBusMethodInvocation, __construct) {
  // Private: never called (object_init_ex() in wrap() skips constructors).
  ZEND_PARSE_PARAMETERS_NONE();
}

/**
 * Gtk4\GDBusMethodInvocation::get_connection(): GDBusConnection
 *
 * Gets the #GDBusConnection the method was invoked on.
 */
ZEND_METHOD(Gtk4_GDBusMethodInvocation, get_connection) {
  ZEND_PARSE_PARAMETERS_NONE();
  GDBusMethodInvocation *self = PHPGTK_SELF(GDBusMethodInvocation, G_TYPE_DBUS_METHOD_INVOCATION);
  GDBusConnection *phpgtk_ret = g_dbus_method_invocation_get_connection(self);
  wrap(phpgtk_ret != nullptr ? G_OBJECT(phpgtk_ret) : nullptr, return_value);
}

/**
 * Gtk4\GDBusMethodInvocation::get_interface_name(): string
 *
 * Gets the name of the D-Bus interface the method was invoked on.
 */
ZEND_METHOD(Gtk4_GDBusMethodInvocation, get_interface_name) {
  ZEND_PARSE_PARAMETERS_NONE();
  GDBusMethodInvocation *self = PHPGTK_SELF(GDBusMethodInvocation, G_TYPE_DBUS_METHOD_INVOCATION);
  const char *phpgtk_ret = g_dbus_method_invocation_get_interface_name(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

/**
 * Gtk4\GDBusMethodInvocation::get_method_info(): ?GDBusMethodInfo
 *
 * Gets information about the method call, if any.
 */
ZEND_METHOD(Gtk4_GDBusMethodInvocation, get_method_info) {
  ZEND_PARSE_PARAMETERS_NONE();
  GDBusMethodInvocation *self = PHPGTK_SELF(GDBusMethodInvocation, G_TYPE_DBUS_METHOD_INVOCATION);
  const GDBusMethodInfo *phpgtk_ret = g_dbus_method_invocation_get_method_info(self);
  wrap_boxed(G_TYPE_DBUS_METHOD_INFO, phpgtk_ret, return_value);
}

/**
 * Gtk4\GDBusMethodInvocation::get_method_name(): string
 *
 * Gets the name of the method that was invoked.
 */
ZEND_METHOD(Gtk4_GDBusMethodInvocation, get_method_name) {
  ZEND_PARSE_PARAMETERS_NONE();
  GDBusMethodInvocation *self = PHPGTK_SELF(GDBusMethodInvocation, G_TYPE_DBUS_METHOD_INVOCATION);
  const char *phpgtk_ret = g_dbus_method_invocation_get_method_name(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

/**
 * Gtk4\GDBusMethodInvocation::get_object_path(): string
 *
 * Gets the object path the method was invoked on.
 */
ZEND_METHOD(Gtk4_GDBusMethodInvocation, get_object_path) {
  ZEND_PARSE_PARAMETERS_NONE();
  GDBusMethodInvocation *self = PHPGTK_SELF(GDBusMethodInvocation, G_TYPE_DBUS_METHOD_INVOCATION);
  const char *phpgtk_ret = g_dbus_method_invocation_get_object_path(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

/**
 * Gtk4\GDBusMethodInvocation::get_parameters(): mixed
 *
 * Gets the parameters of the method invocation. If there are no input parameters then this will
 * return a GVariant with 0 children rather than NULL.
 */
ZEND_METHOD(Gtk4_GDBusMethodInvocation, get_parameters) {
  ZEND_PARSE_PARAMETERS_NONE();
  GDBusMethodInvocation *self = PHPGTK_SELF(GDBusMethodInvocation, G_TYPE_DBUS_METHOD_INVOCATION);
  GVariant *phpgtk_ret = g_dbus_method_invocation_get_parameters(self);
  if (phpgtk_ret == nullptr) RETURN_NULL();
  variant_to_php(phpgtk_ret, return_value);
}

/**
 * Gtk4\GDBusMethodInvocation::get_property_info(): ?GDBusPropertyInfo
 *
 * Gets information about the property that this method call is for, if any.
 */
ZEND_METHOD(Gtk4_GDBusMethodInvocation, get_property_info) {
  ZEND_PARSE_PARAMETERS_NONE();
  GDBusMethodInvocation *self = PHPGTK_SELF(GDBusMethodInvocation, G_TYPE_DBUS_METHOD_INVOCATION);
  const GDBusPropertyInfo *phpgtk_ret = g_dbus_method_invocation_get_property_info(self);
  wrap_boxed(G_TYPE_DBUS_PROPERTY_INFO, phpgtk_ret, return_value);
}

/**
 * Gtk4\GDBusMethodInvocation::get_sender(): string
 *
 * Gets the bus name that invoked the method.
 */
ZEND_METHOD(Gtk4_GDBusMethodInvocation, get_sender) {
  ZEND_PARSE_PARAMETERS_NONE();
  GDBusMethodInvocation *self = PHPGTK_SELF(GDBusMethodInvocation, G_TYPE_DBUS_METHOD_INVOCATION);
  const char *phpgtk_ret = g_dbus_method_invocation_get_sender(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

/**
 * public function return_dbus_error(string $error_name, string $error_message): void
 * Finishes handling a D-Bus method call by returning an error.
 *
 * `$error_name` is a valid D-Bus error name, `org.example.Failed`. An invocation answers once:
 * a second reply of any kind is a `LogicException`.
 */
ZEND_METHOD(Gtk4_GDBusMethodInvocation, return_dbus_error) {
  zend_string *error_name;
  zend_string *error_message;
  ZEND_PARSE_PARAMETERS_START(2, 2)
  Z_PARAM_STR(error_name)
  Z_PARAM_STR(error_message)
  ZEND_PARSE_PARAMETERS_END();
  GDBusMethodInvocation *self = PHPGTK_SELF(GDBusMethodInvocation, G_TYPE_DBUS_METHOD_INVOCATION);
  if (!phpgtk::check_utf8(error_name, 1)) RETURN_THROWS();
  if (!g_dbus_is_name(ZSTR_VAL(error_name))) {
    zend_argument_value_error(1, "must be a D-Bus error name, \"%s\" given", ZSTR_VAL(error_name));
    RETURN_THROWS();
  }
  if (!phpgtk::check_utf8(error_message, 2)) RETURN_THROWS();
  if (!invocation_open(self, "return_dbus_error")) RETURN_THROWS();
  invocation_answered(self);
  g_dbus_method_invocation_return_dbus_error(self, ZSTR_VAL(error_name), ZSTR_VAL(error_message));
}

/**
 * public function return_gerror(GError $error): void
 * Like g_dbus_method_invocation_return_error() but takes a #GError instead of the error domain,
 * error code and message.
 *
 * The exception's domain, code and message become the D-Bus error. An invocation answers once:
 * a second reply of any kind is a `LogicException`.
 */
ZEND_METHOD(Gtk4_GDBusMethodInvocation, return_gerror) {
  zval *error;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(error, ce_GError)
  ZEND_PARSE_PARAMETERS_END();
  GDBusMethodInvocation *self = PHPGTK_SELF(GDBusMethodInvocation, G_TYPE_DBUS_METHOD_INVOCATION);
  if (!invocation_open(self, "return_gerror")) RETURN_THROWS();
  GError *gerror = gerror_from_php(error);
  if (gerror == nullptr) RETURN_THROWS();
  invocation_answered(self);
  g_dbus_method_invocation_return_gerror(self, gerror);
  g_error_free(gerror);
}

/**
 * public function return_value(?array $parameters = null): void
 * Finishes handling a D-Bus method call by returning parameters.
 *
 * The reply as a list (a D-Bus body is a tuple; null or `[]` for a method without out-args),
 * converted to the out-argument signatures of the method's introspection when the invocation
 * carries it ({@see get_method_info()}), by inference otherwise. An invocation answers once:
 * a second reply of any kind is a `LogicException`.
 */
ZEND_METHOD(Gtk4_GDBusMethodInvocation, return_value) {
  zval *parameters = nullptr;
  ZEND_PARSE_PARAMETERS_START(0, 1)
  Z_PARAM_OPTIONAL
  Z_PARAM_ARRAY_OR_NULL(parameters)
  ZEND_PARSE_PARAMETERS_END();
  GDBusMethodInvocation *self = PHPGTK_SELF(GDBusMethodInvocation, G_TYPE_DBUS_METHOD_INVOCATION);
  if (!invocation_open(self, "return_value")) RETURN_THROWS();
  GVariantType *type = nullptr;
  const GDBusMethodInfo *method = g_dbus_method_invocation_get_method_info(self);
  if (method != nullptr) {
    std::string sig = "(";
    for (GDBusArgInfo **arg = method->out_args; arg != nullptr && *arg != nullptr; arg++) {
      sig += (*arg)->signature;
    }
    sig += ")";
    type = g_variant_type_new(sig.c_str());
  }
  GVariant *body = php_to_variant_tuple(parameters, type, 1);
  if (type != nullptr) g_variant_type_free(type);
  if (body == nullptr) RETURN_THROWS();
  invocation_answered(self);
  g_dbus_method_invocation_return_value(self, body);  // consumes GLib's reference
}
