// GENERATED by gen/gir.php from Gio.Icon - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\GIcon
#include "php_gtk4.h"
#include "core/object.h"
#include "core/variant.h"
#include "core/subtype.h"
#include "core/error.h"
#include <array>

using namespace phpgtk;

/**
 * Gtk4\GIcon::equal(?GIcon $icon2): bool
 *
 * Checks if two icons are equal.
 */
ZEND_METHOD(Gtk4_GIcon, equal) {
  zval *icon2 = nullptr;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS_OR_NULL(icon2, class_for_gtype(G_TYPE_ICON))
  ZEND_PARSE_PARAMETERS_END();
  GIcon *self = PHPGTK_SELF(GIcon, G_TYPE_ICON);
  GObject *icon2_o = nullptr;
  if (icon2 != nullptr) {
    icon2_o = unwrap(icon2, G_TYPE_ICON);
    if (icon2_o == nullptr) RETURN_THROWS();
  }
  RETURN_BOOL(g_icon_equal(self, icon2_o != nullptr ? G_ICON(icon2_o) : nullptr));
}

/**
 * Gtk4\GIcon::hash(): int
 *
 * Gets a hash for an icon.
 */
ZEND_METHOD(Gtk4_GIcon, hash) {
  ZEND_PARSE_PARAMETERS_NONE();
  GIcon *self = PHPGTK_SELF(GIcon, G_TYPE_ICON);
  RETURN_LONG(static_cast<zend_long>(g_icon_hash(self)));
}

/**
 * Gtk4\GIcon::serialize(): mixed
 *
 * Serializes a #GIcon into a #GVariant. An equivalent #GIcon can be retrieved back by calling
 * g_icon_deserialize() on the returned value. As serialization will avoid using raw icon data when
 * possible, it only makes sense to transfer the #GVariant between processes on the same machine,
 * (as opposed to over the network), and within the same file system namespace.
 */
ZEND_METHOD(Gtk4_GIcon, serialize) {
  ZEND_PARSE_PARAMETERS_NONE();
  GIcon *self = PHPGTK_SELF(GIcon, G_TYPE_ICON);
  GVariant *phpgtk_ret = g_icon_serialize(self);
  if (phpgtk_ret == nullptr) RETURN_NULL();
  variant_to_php(phpgtk_ret, return_value);
  g_variant_unref(phpgtk_ret);
}

/**
 * Gtk4\GIcon::to_string(): ?string
 *
 * Generates a textual representation of $icon that can be used for serialization such as when
 * passing $icon to a different process or saving it to persistent storage. Use
 * g_icon_new_for_string() to get $icon back from the returned string.
 */
ZEND_METHOD(Gtk4_GIcon, to_string) {
  ZEND_PARSE_PARAMETERS_NONE();
  GIcon *self = PHPGTK_SELF(GIcon, G_TYPE_ICON);
  char *phpgtk_ret = g_icon_to_string(self);
  if (phpgtk_ret == nullptr) RETURN_NULL();
  RETVAL_STRING(phpgtk_ret);
  g_free(phpgtk_ret);
}

// vfunc thunks and installers: file-local, installed by class_init of a PHP subtype
namespace {

// vfunc thunk: static_cast<GIconIface *>->equal -> $this->equal() on a PHP subclass
gboolean vfunc_thunk_equal(GIcon *self, GIcon *icon2) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "equal", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    // an interface implemented in PHP has no native implementation below it
    return FALSE;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  std::array<zval, 1> args{};
  zval *argv = args.data();
  wrap(icon2 != nullptr ? G_OBJECT(icon2) : nullptr, &argv[0]);
  zval ret;
  ZVAL_UNDEF(&ret);
  gboolean result = FALSE;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 1, args.data());
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    result = zend_is_true(&ret) ? TRUE : FALSE;
  }
  for (zval &arg : args) zval_ptr_dtor(&arg);
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GIcon::equal");
  zend_exception_restore();  // the parked one, previous of whatever this threw
  return result;
}

// vfunc installer: static_cast<GIconIface *>->equal (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_equal(gpointer klass) {
  static_cast<GIconIface *>(klass)->equal = vfunc_thunk_equal;
}

// vfunc thunk: static_cast<GIconIface *>->hash -> $this->hash() on a PHP subclass
guint vfunc_thunk_hash(GIcon *self) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "hash", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    // an interface implemented in PHP has no native implementation below it
    return 0;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  zval ret;
  ZVAL_UNDEF(&ret);
  guint result = 0;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    if (Z_TYPE(ret) == IS_LONG && phpgtk::check_range<guint>(Z_LVAL(ret), 0)) {
      result = static_cast<guint>(Z_LVAL(ret));
    }
  }
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GIcon::hash");
  zend_exception_restore();  // the parked one, previous of whatever this threw
  return result;
}

// vfunc installer: static_cast<GIconIface *>->hash (called from class_init / iface_init of a PHP
// subtype)
void vfunc_install_hash(gpointer klass) {
  static_cast<GIconIface *>(klass)->hash = vfunc_thunk_hash;
}

// vfunc thunk: static_cast<GIconIface *>->serialize -> $this->serialize() on a PHP subclass
GVariant *vfunc_thunk_serialize(GIcon *self) {
  zval zself;
  zend_function *fn = subtype_vfunc(G_OBJECT(self), "serialize", &zself);
  if (fn == nullptr) {  // no handle (mid-construction, after shutdown)
    // an interface implemented in PHP has no native implementation below it
    return nullptr;
  }
  // PHP cannot run with an exception pending, and the default is no answer to
  // give GTK: park it for the call, as Zend does around a __destruct().
  zend_exception_save();
  zval ret;
  ZVAL_UNDEF(&ret);
  GVariant *result = nullptr;
  zend_call_known_instance_method(fn, Z_OBJ(zself), &ret, 0, nullptr);
  if (EG(exception) == nullptr && !Z_ISUNDEF(ret)) {
    if (Z_TYPE(ret) != IS_NULL) {
      result = php_to_variant(&ret, nullptr);
      if (result != nullptr) g_variant_ref_sink(result);  // transfer full
    }
  }
  zval_ptr_dtor(&ret);
  zval_ptr_dtor(&zself);
  report_pending_exception("GIcon::serialize");
  zend_exception_restore();  // the parked one, previous of whatever this threw
  return result;
}

// vfunc installer: static_cast<GIconIface *>->serialize (called from class_init / iface_init of a
// PHP subtype)
void vfunc_install_serialize(gpointer klass) {
  static_cast<GIconIface *>(klass)->serialize = vfunc_thunk_serialize;
}

}  // namespace

// MINIT: the vfunc thunks of GIcon (core/subtype.h).
void register_vfuncs_GIcon() {
  register_iface_vfunc(G_TYPE_ICON, "equal", vfunc_install_equal);
  register_iface_vfunc(G_TYPE_ICON, "hash", vfunc_install_hash);
  register_iface_vfunc(G_TYPE_ICON, "serialize", vfunc_install_serialize);
}

/**
 * Gtk4\GIconObject::__construct()
 *
 * Never called: these handles only come from wrap().
 */
ZEND_METHOD(Gtk4_GIconObject, __construct) {
  ZEND_PARSE_PARAMETERS_NONE();
}
