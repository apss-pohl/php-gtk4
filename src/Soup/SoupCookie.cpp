// GENERATED by gen/gir.php from Soup.Cookie - do not edit (gen/overrides, gen/skip.txt).
// Gtk4\SoupCookie (boxed value type)
#include "php_gtk4.h"
#include <libsoup/soup.h>
#include "core/boxed.h"
#include "core/object.h"
#include <cstring>
#include "core/enums.h"

using namespace phpgtk;

namespace {

const char *const fields[] = {nullptr};

// Boxed field reader: the public scalar fields as PHP properties.
bool read(gpointer data, const char *field, zval *rv) {
  (void)data;
  (void)field;
  (void)rv;
  return false;
}

// Boxed field writer: each field converts like a parameter of its type (core/boxed).
bool write(gpointer data, const char *field, zval *v) {
  (void)data;
  (void)field;
  (void)v;
  return false;
}

}  // namespace

/**
 * Gtk4\SoupCookie::__construct(string $name, string $value, string $domain, string $path, int
 * $max_age)
 *
 * Creates a new #SoupCookie with the given attributes.
 */
ZEND_METHOD(Gtk4_SoupCookie, __construct) {
  zend_string *name;
  zend_string *value;
  zend_string *domain;
  zend_string *path;
  zend_long max_age;
  ZEND_PARSE_PARAMETERS_START(5, 5)
  Z_PARAM_STR(name)
  Z_PARAM_STR(value)
  Z_PARAM_STR(domain)
  Z_PARAM_STR(path)
  Z_PARAM_LONG(max_age)
  ZEND_PARSE_PARAMETERS_END();
  if (!phpgtk::check_utf8(name, 1)) RETURN_THROWS();
  if (!phpgtk::check_utf8(value, 2)) RETURN_THROWS();
  if (!phpgtk::check_utf8(domain, 3)) RETURN_THROWS();
  if (!phpgtk::check_utf8(path, 4)) RETURN_THROWS();
  if (!phpgtk::check_range<int>(max_age, 5)) RETURN_THROWS();
  gpointer obj = soup_cookie_new(ZSTR_VAL(name), ZSTR_VAL(value), ZSTR_VAL(domain), ZSTR_VAL(path),
                                 static_cast<int>(max_age));
  if (obj == nullptr) {
    zend_throw_error(nullptr, "%s(): GTK refused to create the value (see the CRITICAL above)",
                     ZSTR_VAL(EX(func)->common.function_name));
    RETURN_THROWS();
  }
  boxed_adopt(boxed_from_zval(ZEND_THIS), SOUP_TYPE_COOKIE, obj);
}

/**
 * Gtk4\SoupCookie::domain_matches(string $host): bool
 *
 * Checks if the $cookie's domain and $host match.
 */
ZEND_METHOD(Gtk4_SoupCookie, domain_matches) {
  zend_string *host;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(host)
  ZEND_PARSE_PARAMETERS_END();
  SoupCookie *self = PHPGTK_BOXED_SELF(SoupCookie);
  if (!phpgtk::check_utf8(host, 1)) RETURN_THROWS();
  RETURN_BOOL(soup_cookie_domain_matches(self, ZSTR_VAL(host)));
}

/**
 * Gtk4\SoupCookie::equal(SoupCookie $cookie2): bool
 *
 * Tests if $cookie1 and $cookie2 are equal.
 */
ZEND_METHOD(Gtk4_SoupCookie, equal) {
  zval *cookie2;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(cookie2, boxed_class_for_type(SOUP_TYPE_COOKIE)->ce)
  ZEND_PARSE_PARAMETERS_END();
  SoupCookie *self = PHPGTK_BOXED_SELF(SoupCookie);
  gpointer cookie2_b = unwrap_boxed(cookie2, SOUP_TYPE_COOKIE);
  if (cookie2_b == nullptr) RETURN_THROWS();
  RETURN_BOOL(soup_cookie_equal(self, static_cast<SoupCookie *>(cookie2_b)));
}

/**
 * Gtk4\SoupCookie::get_domain(): string
 *
 * Gets $cookie's domain.
 */
ZEND_METHOD(Gtk4_SoupCookie, get_domain) {
  ZEND_PARSE_PARAMETERS_NONE();
  SoupCookie *self = PHPGTK_BOXED_SELF(SoupCookie);
  const char *phpgtk_ret = soup_cookie_get_domain(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

/**
 * Gtk4\SoupCookie::get_expires(): ?GDateTime
 *
 * Gets $cookie's expiration time.
 */
ZEND_METHOD(Gtk4_SoupCookie, get_expires) {
  ZEND_PARSE_PARAMETERS_NONE();
  SoupCookie *self = PHPGTK_BOXED_SELF(SoupCookie);
  GDateTime *phpgtk_ret = soup_cookie_get_expires(self);
  wrap_boxed(G_TYPE_DATE_TIME, phpgtk_ret, return_value);
}

/**
 * Gtk4\SoupCookie::get_http_only(): bool
 *
 * Gets $cookie's HttpOnly attribute.
 */
ZEND_METHOD(Gtk4_SoupCookie, get_http_only) {
  ZEND_PARSE_PARAMETERS_NONE();
  SoupCookie *self = PHPGTK_BOXED_SELF(SoupCookie);
  RETURN_BOOL(soup_cookie_get_http_only(self));
}

/**
 * Gtk4\SoupCookie::get_name(): string
 *
 * Gets $cookie's name.
 */
ZEND_METHOD(Gtk4_SoupCookie, get_name) {
  ZEND_PARSE_PARAMETERS_NONE();
  SoupCookie *self = PHPGTK_BOXED_SELF(SoupCookie);
  const char *phpgtk_ret = soup_cookie_get_name(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

/**
 * Gtk4\SoupCookie::get_path(): string
 *
 * Gets $cookie's path.
 */
ZEND_METHOD(Gtk4_SoupCookie, get_path) {
  ZEND_PARSE_PARAMETERS_NONE();
  SoupCookie *self = PHPGTK_BOXED_SELF(SoupCookie);
  const char *phpgtk_ret = soup_cookie_get_path(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

/**
 * Gtk4\SoupCookie::get_same_site_policy(): SoupSameSitePolicy
 *
 * Returns the same-site policy for this cookie.
 */
ZEND_METHOD(Gtk4_SoupCookie, get_same_site_policy) {
  ZEND_PARSE_PARAMETERS_NONE();
  SoupCookie *self = PHPGTK_BOXED_SELF(SoupCookie);
  enum_to_php(SOUP_TYPE_SAME_SITE_POLICY, soup_cookie_get_same_site_policy(self), return_value);
}

/**
 * Gtk4\SoupCookie::get_secure(): bool
 *
 * Gets $cookie's secure attribute.
 */
ZEND_METHOD(Gtk4_SoupCookie, get_secure) {
  ZEND_PARSE_PARAMETERS_NONE();
  SoupCookie *self = PHPGTK_BOXED_SELF(SoupCookie);
  RETURN_BOOL(soup_cookie_get_secure(self));
}

/**
 * Gtk4\SoupCookie::get_value(): string
 *
 * Gets $cookie's value.
 */
ZEND_METHOD(Gtk4_SoupCookie, get_value) {
  ZEND_PARSE_PARAMETERS_NONE();
  SoupCookie *self = PHPGTK_BOXED_SELF(SoupCookie);
  const char *phpgtk_ret = soup_cookie_get_value(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETURN_STRING(phpgtk_ret);
}

/**
 * Gtk4\SoupCookie::set_domain(string $domain): void
 *
 * Sets $cookie's domain to $domain.
 */
ZEND_METHOD(Gtk4_SoupCookie, set_domain) {
  zend_string *domain;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(domain)
  ZEND_PARSE_PARAMETERS_END();
  SoupCookie *self = PHPGTK_BOXED_SELF(SoupCookie);
  if (!phpgtk::check_utf8(domain, 1)) RETURN_THROWS();
  soup_cookie_set_domain(self, ZSTR_VAL(domain));
}

/**
 * Gtk4\SoupCookie::set_expires(GDateTime $expires): void
 *
 * Sets $cookie's expiration time to $expires.
 */
ZEND_METHOD(Gtk4_SoupCookie, set_expires) {
  zval *expires;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(expires, boxed_class_for_type(G_TYPE_DATE_TIME)->ce)
  ZEND_PARSE_PARAMETERS_END();
  SoupCookie *self = PHPGTK_BOXED_SELF(SoupCookie);
  gpointer expires_b = unwrap_boxed(expires, G_TYPE_DATE_TIME);
  if (expires_b == nullptr) RETURN_THROWS();
  soup_cookie_set_expires(self, static_cast<GDateTime *>(expires_b));
}

/**
 * Gtk4\SoupCookie::set_http_only(bool $http_only): void
 *
 * Sets $cookie's HttpOnly attribute to $http_only.
 */
ZEND_METHOD(Gtk4_SoupCookie, set_http_only) {
  bool http_only;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(http_only)
  ZEND_PARSE_PARAMETERS_END();
  SoupCookie *self = PHPGTK_BOXED_SELF(SoupCookie);
  soup_cookie_set_http_only(self, http_only);
}

/**
 * Gtk4\SoupCookie::set_max_age(int $max_age): void
 *
 * Sets $cookie's max age to $max_age.
 */
ZEND_METHOD(Gtk4_SoupCookie, set_max_age) {
  zend_long max_age;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_LONG(max_age)
  ZEND_PARSE_PARAMETERS_END();
  SoupCookie *self = PHPGTK_BOXED_SELF(SoupCookie);
  if (!phpgtk::check_range<int>(max_age, 1)) RETURN_THROWS();
  soup_cookie_set_max_age(self, static_cast<int>(max_age));
}

/**
 * Gtk4\SoupCookie::set_name(string $name): void
 *
 * Sets $cookie's name to $name.
 */
ZEND_METHOD(Gtk4_SoupCookie, set_name) {
  zend_string *name;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(name)
  ZEND_PARSE_PARAMETERS_END();
  SoupCookie *self = PHPGTK_BOXED_SELF(SoupCookie);
  if (!phpgtk::check_utf8(name, 1)) RETURN_THROWS();
  soup_cookie_set_name(self, ZSTR_VAL(name));
}

/**
 * Gtk4\SoupCookie::set_path(string $path): void
 *
 * Sets $cookie's path to $path.
 */
ZEND_METHOD(Gtk4_SoupCookie, set_path) {
  zend_string *path;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(path)
  ZEND_PARSE_PARAMETERS_END();
  SoupCookie *self = PHPGTK_BOXED_SELF(SoupCookie);
  if (!phpgtk::check_utf8(path, 1)) RETURN_THROWS();
  soup_cookie_set_path(self, ZSTR_VAL(path));
}

/**
 * Gtk4\SoupCookie::set_same_site_policy(SoupSameSitePolicy $policy): void
 *
 * When used in conjunction with `get_cookie_list_with_same_site_info` this sets the policy of when
 * this cookie should be exposed.
 */
ZEND_METHOD(Gtk4_SoupCookie, set_same_site_policy) {
  zval *policy;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_OBJECT_OF_CLASS(policy, enum_class_for_type(SOUP_TYPE_SAME_SITE_POLICY))
  ZEND_PARSE_PARAMETERS_END();
  SoupCookie *self = PHPGTK_BOXED_SELF(SoupCookie);
  gint policy_v = 0;
  if (!enum_from_php(policy, SOUP_TYPE_SAME_SITE_POLICY, &policy_v)) RETURN_THROWS();
  soup_cookie_set_same_site_policy(self, static_cast<SoupSameSitePolicy>(policy_v));
}

/**
 * Gtk4\SoupCookie::set_secure(bool $secure): void
 *
 * Sets $cookie's secure attribute to $secure.
 */
ZEND_METHOD(Gtk4_SoupCookie, set_secure) {
  bool secure;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_BOOL(secure)
  ZEND_PARSE_PARAMETERS_END();
  SoupCookie *self = PHPGTK_BOXED_SELF(SoupCookie);
  soup_cookie_set_secure(self, secure);
}

/**
 * Gtk4\SoupCookie::set_value(string $value): void
 *
 * Sets $cookie's value to $value.
 */
ZEND_METHOD(Gtk4_SoupCookie, set_value) {
  zend_string *value;
  ZEND_PARSE_PARAMETERS_START(1, 1)
  Z_PARAM_STR(value)
  ZEND_PARSE_PARAMETERS_END();
  SoupCookie *self = PHPGTK_BOXED_SELF(SoupCookie);
  if (!phpgtk::check_utf8(value, 1)) RETURN_THROWS();
  soup_cookie_set_value(self, ZSTR_VAL(value));
}

/**
 * Gtk4\SoupCookie::to_cookie_header(): string
 *
 * Serializes $cookie in the format used by the Cookie header (ie, for returning a cookie from a
 * `Session` to a server).
 */
ZEND_METHOD(Gtk4_SoupCookie, to_cookie_header) {
  ZEND_PARSE_PARAMETERS_NONE();
  SoupCookie *self = PHPGTK_BOXED_SELF(SoupCookie);
  char *phpgtk_ret = soup_cookie_to_cookie_header(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETVAL_STRING(phpgtk_ret);
  g_free(phpgtk_ret);
}

/**
 * Gtk4\SoupCookie::to_set_cookie_header(): string
 *
 * Serializes $cookie in the format used by the Set-Cookie header.
 */
ZEND_METHOD(Gtk4_SoupCookie, to_set_cookie_header) {
  ZEND_PARSE_PARAMETERS_NONE();
  SoupCookie *self = PHPGTK_BOXED_SELF(SoupCookie);
  char *phpgtk_ret = soup_cookie_to_set_cookie_header(self);
  if (phpgtk_ret == nullptr) RETURN_EMPTY_STRING();
  RETVAL_STRING(phpgtk_ret);
  g_free(phpgtk_ret);
}

namespace phpgtk {
// MINIT: bind the PHP class to SOUP_TYPE_COOKIE with its field table.
void register_SoupCookie(zend_class_entry *ce) {
  register_boxed(BoxedClass{
      .type = SOUP_TYPE_COOKIE,
      .ce = ce,
      .fields = fields,
      .read = read,
      .write = write,
      // NOLINTBEGIN(cppcoreguidelines-pro-type-const-cast) soup_cookie_equal() takes non-const
      // operands
      .equal =
          [](gconstpointer a, gconstpointer b) {
            return soup_cookie_equal(static_cast<SoupCookie *>(const_cast<gpointer>(a)),
                                     static_cast<SoupCookie *>(const_cast<gpointer>(b))) != FALSE;
          }
      // NOLINTEND(cppcoreguidelines-pro-type-const-cast)
  });
}
}  // namespace phpgtk
